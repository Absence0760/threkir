# mobile_android — test inventory

What every suite under `test/` covers. Split out of this app's `CLAUDE.md` for
the same reason as [`files.md`](files.md): it was 62 KB of reference material
loaded on every mobile session regardless of whether a test was in scope.

Read it before adding a suite, so a new case lands in the file that already owns
that surface. The house rule that a change ships with its tests lives in
[`CLAUDE.md`](CLAUDE.md) and
[docs/architecture/conventions.md](../../docs/architecture/conventions.md).

**Waiting for async work: use `test/pump_until.dart`, never a fixed delay.** Widget tests run on a fake clock, so anything that only resolves on the real event loop — a stream cancel, a file write, a platform-channel reply — needs `tester.runAsync` to make progress. The trap is `runAsync(() async { trigger(); await Future.delayed(200ms); })`: that delay is a sleep standing in for synchronisation, it passes here and fails on a loaded CI runner, and it is how the Finish-hold helper flaked on two unrelated Dependabot PRs. `pumpUntil(tester, condition, describe: ...)` alternates a real-event-loop slice with a frame pump until the condition holds, and its `timeout` is a **failure bound** — on expiry it `fail()`s naming what never happened. Never widen the timeout; find what the test is actually waiting for. The same file carries `holdFinish`, the RunScreen Finish-hold drive built on it (it waits on the `_stop()` future the button's `onHoldComplete` hands back). A delay that *models elapsed time* — a debounce, a throttle, a GPS blackout, a hanging fetcher proving a `.timeout()` fires — is the test's subject and stays a delay. See [decisions.md § 715](../../docs/architecture/decisions.md) and [testing.md § Patterns](../../docs/testing/testing.md).

**Every test in this directory is watched for a store write it did not wait for.** `test/flutter_test_config.dart` installs a `StoreWriteObserver` on `serialiseStoreWrite` for the whole suite, and a test that ends with an operation still on the chain FAILS in teardown naming the store directory and the call site that queued it — the file I/O that operation started is racing the temp directory the teardown is about to delete, which used to surface as silence or as an unattributable failure in whichever test ran next (decisions § 1129). The remedy is whichever wait is honest: an observable outcome via `pumpUntil`, the store's own `debugWritesSettled` from inside `tester.runAsync` when you hold the store, or `pumpUntilStoreWritesSettle(tester)` from `test/store_write_watch.dart` when the store belongs to the SCREEN and there is no handle and no UI signal that tracks the file rather than the in-memory row. `allowStoreWritesToOutliveTest` is not an escape hatch — its only honest use is a test whose subject IS an unsettleable write. A screen test that reaches a completion path needs no drain for `showTopBanner` — the pill owns its own clock, arming in `initState` and cancelling in `dispose` (decisions § 1195). If some other widget leaves `A Timer is still pending even after the widget tree was disposed`, that is the widget's bug, not the test's: the assertion is raised from inside the test body before any `tearDown` runs, so no harness cleanup can reach it.

Test files in `test/`:
- `run_stats_test.dart` — 27 tests: moving-time helpers + `fastestWindowOf` rolling-window scanner
- `pace_analysis_test.dart` — 13 tests for `lib/pace_analysis.dart` (mirror of `apps/web/src/lib/runs/pace_analysis.test.ts`, one case per web case): `analysePacing` degenerates on a null / one-point / distance-less / timestamp-less track; a steady run is an even split with no grade-adjusted half (no elevation carries no information); the halves are cut by distance so a fading run still reports equal halves; negative / positive verdicts and the 2 % even band's edges; a second half that climbs reads as a fade raw but even on effort; `gradeAdjustedSplitPaces` aligns index-for-index with the splits, is null throughout for a flat track, and degenerates safely; and the halves stay sane across the antimeridian.
- `local_run_store_test.dart` — 94 tests: store persistence, sync state, in-progress save/load, deleteMany batch, newer-wins guards, `_v` schema-version stamp + legacy-record load, edge cases, plus the **summary index** group (index.json written/maintained per mutation, markSynced flips in-memory only, structurally-invalid index tolerated), the **windowed API** group (runById / recentWindow / hydrateOlder / hasRun), and the **windowed cold-load** group (index-first fast path hydrates only the window, runById disk hydration, unsynced-stays-resident, drift rebuild on missing index / orphan file). Also the **durably-unsynced** group (a local edit survives a cold reload as unsynced, an edit to a windowed-out run regains residency — decisions § 308), the in-progress **save-vs-clear race** (§ 312), the per-id **index merge** across two stores over one directory (§ 309), and the `.tmp` **orphan sweep** (§ 313). Uses `residentWindow` injection to exercise windowing without seeding 200+ runs.
- `local_gym_store_test.dart` — 49 tests for `lib/local_gym_store.dart` (incl. the `OfflineSyncStore` summary-index + `loadInWindow`/`estimateRowsInWindow` + drift-rebuild coverage): createLocal mints a v4 UUID + marks pendingCreate, sets survive reload, newest-started sort; updateLocal keeps pendingCreate (and replaces sets) / flips synced→pendingUpdate / preserves an omitted set list; delete tombstone semantics; replaceFromServer preserves pending + hydrates server sets + **newer-wins** (a stale server fetch can't clobber a locally-newer synced copy; a newer fetch overwrites); drain order create → update → delete with the local id + the inline sets, per-row failure isolation, clean-store no-op; `_rewriteAll` crash-atomic ordering — orphan cleanup keeps pending + leaves no temp files, and per-write failure isolation (a directory planted at a row's file path forces that row's atomic rename to fail while the others still write and a stale orphan is still cleaned); `_v` schema-version stamp on writes + legacy-record load via the migration hook. Mirrors `local_gear_store_test.dart`.
- `local_food_store_test.dart` — 40 tests for `lib/local_food_store.dart`: same lifecycle + drain + `_rewriteAll` crash-atomic / per-write-isolation + **newer-wins** + `_v` schema-version coverage as the gym store, plus `entriesForRange` half-open day-window filtering and newest-logged sort, and the shared `OfflineSyncStore` summary-index + `loadInWindow`/`estimateRowsInWindow` + drift-rebuild coverage. Mirrors `local_gear_store_test.dart`.
- `gym_screen_test.dart` — 28 tests for `lib/screens/gym_screen.dart`: 7 for the pure list helpers (`gymWorkoutVolume` sums reps·weight ignoring incomplete sets, `gymExerciseCount` distinct case-insensitive, `gymPrWorkoutIds` drives the O(n) `RunningPrTracker` (matching the web consumer) and sorts a dateless workout last, `gymExerciseSuggestions` most-used-first keeping the original spelling) + 2 widget (offline empty state renders, a seeded workout row shows title / exercise count / PR badge) + 2 pending-sync regression tests (issue #666 U2: a pending row surfaces the failed-sync banner with Retry when an online api rejects the push, and the saved-on-device banner offline) + 5 peer-strip tests (issue #666 T1: the strip names Log + Routines and withholds Records until a weighted set exists and Sessions until a client is signed in; Records and Routines each open their screen; a signed-in gym reaches its own session plans; the strip and the failed-sync banner both fit 320dp in German) + 2 catalogue third-state tests (decisions § 1514: the SAME empty catalogue offers the composer's browse affordance when the read FAILED and hides it when the read answered — the affordance is the only thing that separates the two states) + the guided-session resume-card group.
- `pending_sync_banner_test.dart` — 8 tests for `lib/widgets/pending_sync_banner.dart` (an in-memory `OfflineSyncStore` subclass, no file I/O): `pendingCount`/`hasPending` over every sync state; renders nothing when nothing pending; offline and signed-out show the saved-on-device copy without Retry; online shows the failed copy and Retry drains + clears on success; Retry leaves the banner up when the server keeps rejecting; multi-store sum + retry across stores; the Retry action reflows below the message at 320dp in German (a `TextButton` at the row's tail takes its intrinsic width first and used to starve the `Expanded` message into a column of single characters — decisions §488).
- `full_screen_form_test.dart` — 4 widget tests for `lib/widgets/full_screen_form.dart`: `showFullScreenForm` pushes a fullscreen-dialog route with an AppBar title + body + Close affordance (not Back), resolves to the popped value; `FullScreenFormBody` renders children in a `SingleChildScrollView`; `FormSectionLabel` uppercases.
- `gear_form_sheet_test.dart` — 3 widget tests for `lib/widgets/gear_form_sheet.dart`: opens as a full-screen dialog (AppBar + Close, the "Add shoes" heading, Name field); Cancel returns null and writes nothing; entering a name + Add writes a `pendingCreate` row to `LocalGearStore` (the in-memory write before createLocal's file-write await — the popped result + dismissal are covered by `full_screen_form_test`).
- `gear_rotations_screen_test.dart` — 9 widget tests for `lib/screens/gear_rotations_screen.dart` (fake `ApiClient` + real `LocalGearStore`): the empty state renders the onboarding copy; a seeded rotation renders its name + member count; opening the member sheet + toggling a gear + Done calls `setGearRotationMembers` with the toggled id set; deleting a rotation confirms via the `AlertDialog` then calls `deleteGearRotation`; plus the next-up group — the least-worn in-service pair is named with a Make-current button, the button calls `setDefaultGear` with that id + kind, a pick already holding the star offers no move, one live pair beside a retired one offers no next-up at all (the `ranked.length` gate, not the membership count), and an all-worn rotation says so.
- `gym_compose_sheet_test.dart` — 31 widget tests for `lib/widgets/gym_compose_sheet.dart`: empty save shows the validation error and writes nothing; entering an exercise + saving creates a `pendingCreate` workout with the right title + inline set; editing pre-fills the title + set list from a synced stored workout (the update→pendingUpdate write path is covered by `local_gym_store_test`); the catalogue merge + bind, the browse/picker round trip, and — since decisions § 1513 / § 1514 — a catalogue that lands AFTER the sheet mounts still binding an id, a created custom shadowing a seeded global leaving ONE row that is the custom, and an unavailable catalogue keeping browse reachable while the create refuses. The draft group pins the whole survival contract: a workout typed but never saved is on disk one save tick later while the gym store stays empty (a draft is not a session), the next composer offers it back and restores every typed value, Discard clears it off disk, a deliberate close resolves it, and the edit path never writes one.
- `exercise_catalogue_picker_test.dart` — 22 widget tests for `lib/widgets/exercise_catalogue_picker.dart`: search / hidden-exact / folded ordering, the create path end to end (no affordance without a client, a create under a category filed there and under "all" filed under other, a refusal reported while staying on the picker), the shadowing badge, and the third state — the notice rendered, no create offered for any name, no "No exercises match." claim about a catalogue nobody has, and the rows it DOES hold still listed and pickable beside the notice.
- `challenge_form_sheet_test.dart` — 23 tests for `lib/widgets/challenge_form_sheet.dart`: the goal-unit group (a distance goal typed in km / mi lands in metres, a time goal in hours lands in seconds, an elevation goal round-trips through the same feet-per-metre factor `UnitFormat.elevation` renders with, counts are stored verbatim, an unknown metric is measured as a distance, and the suffix names the unit for every metric × unit); the form carries every field web's editor does; a typed `100` reaches `createChallenge` as `100000` (and as `160934.4` for a mile-unit reader); switching the metric clears a goal typed in the old unit; empty title / non-positive goal / a start pushed past the end each flag inline and send nothing; a throttled create renders the bucket and the wait rather than the generic error; an unrelated failure renders the generic one without leaking the exception; a second tap while the create is in flight sends nothing; and the club-anchor group (only administered clubs are offered, the picker self-hides when there are none, a failed club read leaves the form usable, `club_vs_club` drops the anchor).
- `local_routine_store_test.dart` — 11 tests for `lib/local_routine_store.dart` (gym-programming P1): createLocal mints a v4 UUID + marks pendingCreate + drops blank-named exercises; routines reload from disk newest-modified-first with inline exercises/sets surviving the round-trip; deleteLocal on a pendingCreate drops it outright; deleteLocal on a synced routine writes a pendingDelete tombstone; replaceFromServer preserves a local pendingCreate.
- `gym_compose_draft_test.dart` — 6 tests for `lib/gym_compose_draft.dart`: a half-typed composer (a weight mid-edit, an exercise block with no name yet) round-trips through disk verbatim; `clear` removes it; a corrupt record and one with no `saved_at` each answer null AND are cleared, so a later open cannot keep paying the same failed read; an untouched composer is not content worth offering back; and a store whose `path_provider` never answered degrades to inert rather than throwing.
- `entry_field_fit_test.dart` — 4 tests for `lib/entry_field_fit.dart`: the three common set-entry fields stay one row at 1.0x on 320/360/412dp phones, reflow to 2 then 1 across as the OS text scale reaches 2x and 3x, five fields split 3+2 rather than stranding one, and the answer is never below one however narrow the row. Pins the derivation, not a dp fit — `flutter_test`'s fixed-advance font makes an absolute fit claim unmeasurable (conventions § 500).
- `routine_builder_sheet_test.dart` — 2 widget tests for `lib/widgets/routine_builder_sheet.dart`: empty title shows the validation error and writes nothing; a seeded title + exercise saves a `pendingCreate` routine with the right title / exercise_key / target reps + canonical-kg weight.
- `routine_library_screen_test.dart` — 2 widget tests for `lib/screens/routine_library_screen.dart`: the empty library renders the self-hiding onboarding state (no rows); a seeded routine renders a row with its title + exercise count.
- `routine_detail_screen_test.dart` — 15 widget tests for `lib/screens/routine_detail_screen.dart`: a seeded routine renders its planned targets (single-value + range rep labels) and exactly ONE Start FAB, the guided runner's — matching web, which retired the P1 prefill-only modal when the runner shipped; a missing routine renders the not-found state (no Start FAB); the club-template group — the publish-row shows for an author of a personal routine with an admin club, hides when the viewer has no admin club / is not the author, and the "Club template" badge shows when `club_id` is set; the public-library group; plus the history group over a fake `ApiClient` — past sessions render the panel with its count / last-done / completed-of-graded line / verdict badges, an in-flight `gym_session_draft` row is not counted as performed, a routine never run renders no panel at all, a failed read renders the error card whose Retry re-reads and recovers, and a non-author never triggers the fetch.
- `food_search_test.dart` — 41 tests for `lib/food_search.dart` (Dart twin of web `food_search.ts`): `parseOffSearch` maps Open Food Facts products + drops nameless/calorie-less ones + keeps the first brand, tolerates string-typed nutriments, returns `[]` on malformed input; `scalePortion` scales per-100g to a gram portion (rounded, 0 g → 0); `searchFoods` skips the fetcher on an empty query, parses a canned response via the injected fetcher, **rethrows** a fetch failure (so the caller shows a retry state, not a misleading empty), and treats valid-but-empty JSON as genuinely empty (`[]`); plus the **barcode** group — `normaliseBarcode` strips non-digits / rejects empty, `parseOffProduct` maps a found single-product `{status, product}` response + returns null for missing (`status != 1`) / unloggable, `lookupBarcode` skips the fetcher on a blank/non-numeric code, parses a found product, returns null on a genuine no-match (`status 0`), and **rethrows** a fetch failure (so the caller can tell "not in DB" from "lookup failed").
- `nutrition_totals_test.dart` — 4 tests for `lib/nutrition_totals.dart` (Dart twin of web `nutrition_totals.ts`): `sumMacros` sums fields treating null as zero (and zeros for an empty day); `groupByMealSlot` orders slots and omits empty ones (null slot folds into snack); `ringFraction` clamps to [0,1] and returns null on a missing/zero target.
- `nutrition_screen_test.dart` — 25 tests for `lib/screens/nutrition_screen.dart`: the empty state renders with the rings + water cards when nothing is logged today; a logged meal shows under its meal-slot group with calories (350 appears twice — the calories ring centre + the meal-row value); the water tracker increments by one 250 ml unit on tap (`SharedPreferences` mock); the deferred-delete / undo trio; the `exerciseInputsForDay` group; plus the **diary day navigation** group — today cannot step forward and shows no back-fill hint, stepping back shows yesterday's entries + the hint and Today returns, an empty past day says "Nothing logged on this day" rather than "no food today", the water count is per viewed day, the composer opened on a past day is titled with that day, and a saved meal / a saved recipe logged on a past day both stamp THAT day (the load-bearing pin — a stepper that lets a runner review yesterday but silently logs to today would be worse than no stepper).
- `diary_day_test.dart` — 26 tests for `lib/diary_day.dart`, a case-for-case mirror of web `nutrition/diary_day.test.ts`: ISO formatting + strict parse (rejecting `2026-02-30`, an unpadded date, and a two-digit year the web sibling's `Date` would read as 1926), future-day clamping, calendar stepping across month / year / leap-day edges, the half-open day window, instant-not-string window membership (the Postgres `+00:00` vs `.000Z` boundary row), trailing trend buckets, the back-fill `entryTimestampFor` stamp, the deliberately unpadded water key, and the next-local-midnight delay.
- `nutrition_log_sheet_test.dart` — 8 widget tests for `lib/widgets/nutrition_log_sheet.dart`: manual entry logs a food item to the store (name + meal slot + calories); search renders Open Food Facts results via the injected fetcher; a failed search / failed save surfaces the right state; plus the **barcode-scan** group (over the injectable `BarcodeScanner` + `fetcher` seams — the camera itself isn't unit-tested): a scanned barcode that matches opens the confirm-portion dialog, a no-match shows the not-found message (with the search/manual fallback intact), a lookup failure shows the distinct scan-failed message, and a cancelled scan is a no-op. (the confirm-portion + store-write path is also covered by `local_food_store_test` + `food_search_test`).
- `log_speed_dial_test.dart` — 5 widget tests for `lib/widgets/log_speed_dial.dart`: `showLogSpeedDial` fans the three capture options + resolves to the picked one (then tears the overlay down); a scrim tap dismisses + resolves null; the recent action sits nearest the FAB (greater dy) in the upward fan; plus the anchored-fan group (the NavigationRail Log button at expanded widths) — all three items open right of the anchor with the recent action directly right and the others split above/below, and an anchored pick resolves + tears down.
- `lift_load_test.dart` — 3 tests for `lib/lift_load.dart` (mirror of web `gym/lift_load.test.ts`): `liftsFromSetHistory` groups flat set rows by workout, drops rows with no workout id / date, and the grouped lifts raise the load series (separability — run-only `atl` recoverable).
- `recent_lifts_card_test.dart` — 4 widget tests for `lib/widgets/recent_lifts_card.dart`: renders nothing on an empty list; header + View-all + a row per workout; caps at five most-recent; row tap fires `onOpenWorkout`, View-all fires `onViewAll`.
- `activity_timeline_test.dart` — 4 tests for `lib/activity_timeline.dart`: `groupActivitiesByDay` empty input, buckets by local calendar day preserving order, same-day-different-time stays one bucket, sequential (non-global-sort) grouping re-opens a day.
- `activity_timeline_list_test.dart` — 4 widget tests for `lib/widgets/activity_timeline_list.dart`: empty state, per-kind run/lift/meal summaries (distance+duration / title+sets+volume / item+kcal) + Today header, run/lift tappable + meal read-only (chevron count), lift volume in the user weight unit.
- `local_activities_test.dart` — 4 unit tests for `lib/local_activities.dart#buildLocalActivities`: merges run/lift/meal newest-first with the right `summary` keys (incl. set-count + Σ volume, incomplete sets counted but adding no volume); drops a workout with no `started_at` + a meal with an unparseable date; caps at the limit keeping the newest; per-source cap still surfaces the global newest across all three modalities (the bounded-build correctness pin).
- `runs_screen_timeline_test.dart` — widget tests for the History timeline on `runs_screen.dart` plus the run-surface peer strip (every peer renders as a label and the tapped one fires; the current peer is marked selected and does not navigate; no strip when a mount supplies none; no overflow at 320dp with the longest German labels), seeding the **local stores** (the timeline's source): chips appear once a second modality has data; **every chip (incl. Runs) stays on the unified timeline filtered by kind** (`ActivityTimelineList` stays mounted; Runs is not the inline run list); a single-modality tab shows a `View all` link while All does not; the **add FAB follows the active chip** (All → a "Log" FAB that opens the run/lift/meal picker; Runs → Add run; Lifts → Log lift; Meals → Log food) and a run-only mount keeps the plain Add-run FAB; no chips / inline run list when the gym store is absent (run-only).
- `period_summary_test.dart` — 26 tests: period boundary computation, stats aggregation, share text generation, formatting helpers
- `recap_screen_test.dart` — 5 widget tests for `lib/screens/recap_screen.dart`: a `month` arg opens the monthly card scoped to that month (a run in another month of the same year is excluded); the Year segment widens the same card to the whole year; a month with no activity renders the empty state instead of a zeroed hero; the previous-month chevron steps January back into the prior December; the next chevron is disabled on the current month. Dates are anchored on `DateTime.now().year - 1` so the assertions don't rot as the calendar moves; store seeding runs inside `tester.runAsync` (disk I/O).
- `goals_test.dart` — 32 tests: goal evaluation (distance/time/pace/run-count, weekly/monthly, multi-target)
- `route_simplify_test.dart` — 30 tests: Ramer-Douglas-Peucker track simplification, elevation gain/loss, the `simplifyToBudget` group (never exceeds the budget, endpoints exact, order preserved, budget spent on the corners not evenly, a degenerate track collapses, a budget below two is rejected), and the antimeridian group (a straight line across 180° collapses to its endpoints; a real 50 m deviation across it still survives)
- `training_test.dart` — 65 tests: VDOT, Riegel, pace derivation, plan generation (mirrors `apps/web/src/lib/training.test.ts`)
- `auth_gates_test.dart` — 17 tests for `lib/auth_gates.dart` (mirror of the `checkPasswordPair` half of `apps/web/src/lib/core/auth_gates.test.ts`, one case per web case): the floor re-exports the canonical kPasswordMinLength, the boundary is inclusive, too-short beats mismatch (precedence — telling someone with a 3-char password that the passwords don't match sends them round the loop again), case sensitivity, leading / trailing whitespace is significant, a transposition typo, non-ASCII, and the UTF-16 code-unit length quirk (Dart's `String.length` counts the same way as JS, so the twin agrees with web by construction).
- `password_change_test.dart` — 7 tests for `lib/password_change.dart` (mirror of `apps/web/src/lib/core/password_change.test.ts`, one case per web case): correct current password writes, wrong current password writes nothing, empty current password rejected before any verify call, a verification that throws fails closed (no write), a mismatched new pair rejected before the current password is sent, a too-short new password reports length not mismatch, and a failed update surfaces the provider message. The step-up wiring itself is covered by `settings_account_change_password_test.dart`.
- `plan_adherence_test.dart` — 11 tests for `lib/plan_adherence.dart` (mirror of `apps/web/src/lib/training/plan_adherence.test.ts`): `weeklyDrift` on-track / under / over / threshold edge / no-plan / negative-clamp, `missedWorkoutAdvice` make-up / taper / recovery-soon / precedence / non-long-run.
- `plan_replan_test.dart` — 15 tests for `lib/plan_replan.dart` (mirror of `plan_replan.test.ts`): on-track no-op, missed-long make-up capped at +15%, no-shrink of an already-longer next long, taper skip, over-running ease-off, frozen-past/taper guard, plus the multi-missed-long regression block (largest wins whether it's the earliest or latest week, under-cap make-up reaches the largest miss exactly, a taper miss is excluded from the make-up max, three misses still produce exactly one capped change), plus the explicit-skip block (a deliberately-skipped long run is never made up; a skipped long is excluded from the make-up max while a genuine miss still makes up).
- `relink_candidates_test.dart` — 10 tests for `lib/relink_candidates.dart` (mirror of web `relink_candidates.test.ts`): in-window newest-first, ±window exclusion + boundary inclusivity, excludes a run linked to another workout (double-count guard), keeps the workout's own current run even when linked / out of window, empty input, default 7-day window.
- `race_predictor_test.dart` — 11 tests for `lib/race_predictor.dart` (mirror of web `race_predictor.test.ts`): empty pool → null, non-finite/non-positive efforts filtered, one-effort full ladder, rungs match `riegelPredict`, pace = time/km, a recent effort out-anchors a faster-but-stale PR, a recent PR still wins when fastest, `qualifyingCount` reflects the filtered pool, a 10K anchor grades the marathon rung below the 10K rung, a stale-only pool still predicts (no null-out), future-dated (clock-skew) effort treated as weight 1.
- `workout_detail_screen_test.dart` — initial render (loading spinner, no AppBar yet) plus the **re-link picker** group: Re-link opens the picker and picking a non-current run calls `markCompleted` with that run id; the empty-candidate state renders; no Re-link button when the workout is only manually completed (run-link-only). Uses a fake `TrainingService` overriding `fetchWorkout` / `fetchRelinkCandidates` / `markCompleted`.
- `plan_detail_adherence_test.dart` — 6 widget tests for the owner surfaces on `lib/screens/plan_detail_screen.dart`: the under-running drift banner + the missed-long make-up callout render (owner only), hidden for a non-owner; the Re-plan flow proposes a make-up + applies it via `updateWorkout`; the per-week Duplicate action calls `duplicatePlanWeek` (owner only). Uses fake `TrainingService`/`SocialService` + `viewerIdOverride`.
- `nutrition_meal_detail_screen_test.dart` — 2 widget tests for `lib/screens/nutrition_meal_detail_screen.dart`: a seeded breakfast (two items) shows the slot title + macro total + items + 7-day trend (and excludes a same-day lunch item); the empty-items state renders for a slot with nothing logged.
- `ble_heart_rate_test.dart` — 14 tests: BLE HR characteristic 0x2A37 parser (8-bit/16-bit BPM, edge cases) + `bleReconnectDelay` backoff schedule (2/4/8/16→30 s cap, negative clamp) for the mid-run auto-reconnect loop (persona #13)
- `ble_treadmill_test.dart` — 20 tests: FTMS Treadmill Data 0x2ACD parser (speed-only, 0.01 km/h resolution, m/s conversion, total distance bit 2, average-speed skip keeping the distance offset correct, positive/negative sint16 inclination, distance+inclination together, zero speed, more-data flag → null, truncation edge cases) + `bleTreadmillReconnectDelay` backoff + connect-status/pairing helpers (C3)
- `integration_visibility_test.dart` — 12 tests over `lib/integration_visibility.dart`, the Dart half of the `integration_visibility` parity pair (mirror of web's `integration_visibility.test.ts`): a resolved `env`/`probe` gate reads its verdict; an UNRESOLVED one is `pending`, not `unconfigured`; `always`/`unsupported`/`unbuilt` ignore the verdict; visibility is fail-closed for an unconnected provider but a CONNECTED one stays visible whatever the gate says; a missing verdict key reads as pending rather than false; `visibleIntegrations` filters while preserving catalogue order; an unconfigured deployment offers nothing it cannot honour; and only an `unsupported`/`unbuilt` connected row is stranded (an `unconfigured` Strava still syncs).
- `settings_integrations_gate_test.dart` — 6 widget tests for the gate on `lib/screens/settings_integrations_screen.dart`: an unconfigured Strava with no row gets no tile (every widget test runs without a loaded `.env`, which is the shape a minimal deployment has); a CONNECTED Strava is still shown on that same deployment; an unreachable parkrun leg gets no tile and a connected parkrun does; the on-device tiles (watch relay) are never gated; and an offered tile's `(i)` opens its explanation and dismisses.
- `treadmill_tile_test.dart` — 2 widget tests for the `TreadmillTile` in `settings_integrations_screen.dart`: not-paired subtitle + chevron when no treadmill stored, paired-name subtitle + forget affordance when stored (C3)
- `pace_segments_test.dart` — 18 tests: pace-bucket clamping, activity-specific scaling, age-band partitioning, coalescing, vertex-sharing continuity, timestamp-less fallback — backs the NRC-style pace heatmap in `widgets/pace_segments.dart`
- `track_preview_test.dart` — 11 pure-helper tests for `widgets/track_preview.dart` (plus the map-backed render-path widget group): 6 for `isTrackRenderable` (rejects empty / single-point / sub-5 m jitter tracks — including a jitter cluster straddling the antimeridian, whose raw span used to read 359.99° and defeat the gate — accepts genuine tiny laps, multi-km loops + a run crossing the line), 5 for `projectTrack` (square 100 m loop at 51 °N renders square within 2 %, longitude scale matches `cos(midLat)` at high latitudes, horizontal segments stay on the x-axis, empty input, and a box straddling the antimeridian fits the same as the identical box a degree west instead of collapsing to a dot). Mirrors the `isTrackRenderable` + projection helpers on the web `track_projection.ts` module.
- `track_decorations_test.dart` — 10 tests for `widgets/track_decorations.dart`: 6 for `computeDistanceMarkers` (one pin per km / mile, empty short tracks, miles produces fewer pins, `totalDistanceM` rescales sparse-polyline runs, mid-segment interpolation), 4 for `computeChevrons` (right cadence, north-bound segments rotate to point up, empty for invalid stepMetres). Mirrors the `computeDistanceMarkers` + `trace-arrows` layer on web's `RunMap.svelte`.
- `track_segment_test.dart` — 9 tests for `widgets/track_segment.dart`: `buildCumulativeDistances` is monotonic, `nearestTrackIdx` finds the closest waypoint, `buildSegmentAt` expands ±150 m, computes pace + duration from timestamps, returns null pace under 10 m, aggregates avg BPM and elevation gain on the slice, widens past a degenerate single-point selection at the track edge. Mirrors `buildSegment` on web's `RunMap.svelte`.
- `hr_zones_test.dart` — 14 tests: time-weighted vs sample-count fallback, the 30 s pause cap, custom cutoffs, malformed-input rejection, `bpmStatsOf` aggregation
- `fitness_test.dart` — 43 tests: VDOT range checks (5k, marathon), `currentVdot` 90-day window + qualifying-source filter, `runTss` Coggan reference, EWMA training-load smoke, `recoveryAdvice` thresholds, `computeSnapshot` end-to-end
- `training_load_test.dart` — 28 tests for `lib/training_load.dart`: distance-fallback stress (~50 for an easy 5k), TRIMP path lights up with avg_bpm + resting + max, zero-input → 0, daily aggregation sums same-day runs, series emits exactly windowDays entries, TSB rises during a 14-day taper, all-zero with no runs, `hasTrimpSignal` gating (no avg_bpm / has avg_bpm + prefs / prefs missing). Mirrors `apps/web/src/lib/training_load.test.ts`.
- `mileage_trend_test.dart` — 17 tests for `lib/mileage_trend.dart`: weekly view groups same-week runs (Monday anchor), separates across week boundaries chronologically, caps to 12 buckets by default, respects `maxBuckets` override, returns empty on no runs; monthly view groups same-month runs and separates across boundaries; yearly view groups same-year runs and separates across boundaries.
- `mileage_trend_card_test.dart` — 6 widget tests for `lib/widgets/mileage_trend_card.dart`: hides when there are no runs; renders the header + SegmentedButton labels (Week / Month / Year); default view is weekly (week-start label visible); tapping Month switches the bucket labels; tapping Year switches to 4-digit year; numeric labels honour the user `DistanceUnit` (10000 m → "10.00" in km, "6.21" in mi).
- `run_intensity_test.dart` — 20 tests for `lib/run_intensity.dart`: 9 for `computeIntensityBreakdown` (empty when no runs, drops runs older than window, drops runs without avg_bpm or with sentinel zero, correct zone classification across the five bands, accumulates within-zone seconds, rejects non-ascending / wrong-length zones, rejects non-positive window, < semantics on exact boundary); 5 for `parseHrZones` (valid map → ascending list, rounds non-int numerics, null on non-Map / missing / non-numeric / non-ascending). Backs the dashboard `IntensityCard` + `run_detail_screen`'s per-run HR-zone panel.
- `intensity_card_test.dart` — 6 widget tests for `lib/widgets/intensity_card.dart`: hides when `hrZones` is null, hides when no runs in window carry `avg_bpm`, renders the `TRAINING INTENSITY` header + `last N days` window label when populated, renders five zone legend rows with rounded percentages (Z3 = 20% pinned for cleanliness), helper text reports HR-tracked-run count with correct singular / plural, uses the `<1%` marker for non-empty sub-1% slivers (not `0%`).
- `segments_test.dart` — 21 tests for `lib/segments.dart`: clean segment, run shorter than segment end, ≤2-point tracks, zero / negative segment windows, sparse-sampling guard (median step > segLen / 5), missing timestamps in the interpolation bracket, mid-segment interpolation, sample-aligned endpoints. Mirrors `apps/web/src/lib/segments.test.ts`.
- `privacy_test.dart` — 9 tests for `lib/privacy.dart`: `isInAnyZone` (empty / centre / far), `clipPointsToZones` (empty zones returns input, drops leading + trailing in-zone, keeps interior, every-point-in-zone returns empty, multi-zone union), plus the pin that a zone on the antimeridian still catches a point across it — the zone test is haversine, so it needed no §468 fix and that absence is evidence, not an omission. Mirrors `apps/web/src/lib/routes/privacy.test.ts`.
- `plan_calendar_test.dart` — 3 widget tests: month rendering with kind labels, chevron navigation, completed-tick presence
- `workout_execution_band_test.dart` — 6 widget tests: empty render, header / step counter / remaining-metres formatting, Skip + Abandon callbacks, complete + abandoned shells, em-dash for null pace
- `workout_review_section_test.dart` — 18 tests for `widgets/workout_review_section.dart`: hidden when metadata absent / empty / null, header + adherence pill rendered with one row per step, kind-specific labels (Warmup / Rep i/n / Recovery i/n / Cooldown), strikethrough + "skip" label for skipped steps, em-dash + neutral colour when actual pace is null, signed `+/−Ns` delta label, and tone classification thresholds (`on` / `amber` / `off` against the 10 s tolerance) plus `formatPace` formatting
- `race_predictor_card_test.dart` — 4 widget tests for `widgets/race_predictor_card.dart`: self-hides with no qualifying runs / no runs at all; renders the title + the four column headers (Distance/Time/Pace uppercased, Confidence in title case with more flex so it fits — issue #503) + the per-rung confidence chips when the gate passes; the confidence header + value stay on one line (maxLines 1, no overflow) at a narrow phone width.
- `fitness_card_test.dart` — 5 widget tests for `widgets/fitness_card.dart`: hidden with no runs, hidden when no runs qualify (sub-3 km / sub-5 min), populated card renders all six stat labels + the recovery-advice icon + the qualifying-run count, em-dash placeholder when VDOT can't be computed but qualifying runs exist, plus a smoke test for the `FitnessStat` value-over-label primitive
- `watch_settings_test.dart` — 38 tests for `lib/watch_settings.dart` (`WatchSettings.encode` — the phone→watch `watch_core::settings` `SET1` wire mirror, **v8** since decisions §376): the fully-populated 196-byte frame matches the frozen golden vector byte-for-byte, plus the empty/maxHr-only/pacer-only/gear-only/zone-only/seaLevel-only/fuel-only/pages-only/hideEmpty/tz-only/v4-arms/restingHr/ice/autoLap/storm partial frames, a null gear target encoding as 0.0, `zoneCeiling` 0 (clear) / 4 (top zone), the `WatchRacePhasePreset` + `WatchAutoLap` declaration orders as wire contracts, the storm threshold travelling as tenths of a hPa with 0 as the disarm and an armed value never rounding into that sentinel, bit-order layout across all three presence bytes, that every frame stamps the constant current version (so the phone cannot emit the v7-stamped-v8-bit frame the watch refuses whole), and that the four-byte trailer on each is the **derived** `crc32` of everything before it (reused from `sim_watch_sync.dart`, not a second implementation) rather than only a pinned literal. Seven vectors — the full frame plus the v4-arms / restingHr / ice / tz / autoLap / storm ones — are byte-identical to the firmware's own `settings.rs` goldens, and are named constants rather than inline literals so `scripts/check_watch_wire_vectors.mjs` can read both rails and fail when they part company (decisions §793). The `WatchSyncClient.pushSettings` orchestration is pinned in `sim_watch_sync_test.dart` (encoded frame written over the fake transport) and the dev-screen Push-settings action in `sim_watch_screen_test.dart`.
- `watch_course_test.dart` — 23 tests for `lib/watch_course.dart`: the `CRS1` v3 encoder against the frozen goldens shared with the firmware's own (with and without the elevation series), the derived CRC trailer, chunking/reassembly, `capCoursePoints`, and the `courseFromWaypoints` group — a short route passes through, a dense one is thinned to `kMaxCoursePoints` with its real endpoints intact (never cut at the cap), elevation rides along only when EVERY carried point has one, and a route with fewer than two positions is refused with a reason.
- `route_detail_watch_course_test.dart` — 13 widget tests for the Send-to-watch entry on `route_detail_screen.dart` (fake `WatchBleTransport` + a `devBackendUrl` on both sides of the dev gate). **Course half (7):** the action shows against a loopback backend and is absent against a production one; a short route pushes every point; an over-long route is thinned and the banner says by how much; a one-point route is refused with nothing written and no scan; a failed write surfaces the failure and still disconnects; and a non-owner's push carries the CLIPPED trace, not the stored one (§33). **Schedule half (6, decisions §413):** course markers become an `RBK1` schedule on the same action (the synthetic start dropped, so three markers + the finish = four checkpoints); a route with no markers pushes the course alone; an over-cap schedule is thinned and the banner says by how much; too many cut-offs refuses the schedule while the course still lands; a clock-only cut-off is disclosed rather than silently missing; a marker-fetch failure degrades to a course-only push. These use `_longLine(30)` (1 km legs) rather than `_loop`, whose ~450 m circle would clamp every marker onto the finish.
- `watch_roadbook_test.dart` — 31 tests for `lib/watch_roadbook.dart`: the `RBK1` v1 encoder against the frozen golden vector shared with the firmware's `roadbook_store`, the derived CRC trailer, the cap refusals, chunking/reassembly, plus the **`watchRoadbookFromRoadbook`** group (decisions §413) — the synthetic start is dropped and the finish always survives; a distance-less route is refused, not pushed empty; an over-cap route reduces to exactly the cap keeping the end; every cut-off survives a reduction even when clumped late; **a sparse mid-course stop outranks one of a dense clump** (the pin that distinguishes farthest-point sampling from even index-sampling — 40 stops in the first 2 km plus 4 lonely ones, and all four lonely ones must survive); more cut-offs than the wire carries is refused; a full 16 cut-offs plus a separate finish is refused too (17 checkpoint slots); two markers sharing a position collapse with the cut-off winning; the emitted series is strictly increasing in both wire distance and wire time (what `Pacer::set_schedule` silently requires); leg distances are recomputed against the surviving predecessor; a clock-only cut-off is counted not swallowed; an aid station is flagged a refill point and a plain note is not; a reduced schedule still chunks into at most two writes.
- `watch_screens_editor_screen_test.dart` — 20 tests for `lib/screens/watch_screens_editor_screen.dart`: 6 for the pure draft codec (round-trip; the stored form names layouts + metrics by `wireName`; an arity mismatch, an unknown name, a set past `kMaxWatchScreens`, and malformed JSON each reject the **whole set** rather than truncating) and 13 widget (empty state on a fresh store; add seeds a single-slot screen; the Add button is disabled at the cap so the encoder's fifth-screen `ArgumentError` is unreachable; widening a layout fills the new slots with *distinct* metrics; narrowing names the metrics it would drop and honours cancel / confirm; move-down reorders the persisted set because order is meaning; remove confirms and keeps the screen on cancel; an unparseable stored set lands on Start-over; the push writes the frozen SCR1 golden frame and disconnects; an empty push clears the watch and says so; a failed push surfaces the error). SharedPreferences round-trips need `tester.runAsync` (store I/O), and the dialog buttons are found dialog-scoped (`Cancel` appears once per dialog).
- `metadata_registry_test.dart` — 2 tests: (1) every `runs.metadata` key referenced in Dart source is registered in [docs/backend/metadata.md](../../docs/backend/metadata.md) — catches cross-client drift like `metadata.activityType` vs `metadata.activity_type` at CI time; (2) a soft "dead-key" info log surfacing registered keys that no Dart reader touches (may be web/watch/EF-only). Fails on unknown-key writes; purely informational on the reverse direction.
- `recurrence_test.dart` — 25 tests: weekly / biweekly / monthly `expandInstances`, hour/minute local-tz preservation, `count` cap, `until` cap, non-recurring single-instance
- `importer_external_id_test.dart` — 4 tests: `StravaImporter` ZIP import produces `strava:<id>` prefix; a second import of the same ZIP yields an identical, stable `id` (= `stableRunIdFromExternalId('strava:98765')`, the #361 re-import guard); source-text guards confirm `HealthConnectImporter` uses the `healthconnect:<uuid>` prefix AND derives its id via `stableRunIdFromExternalId` (no `_uuid.v4()`).
- `imported_run_id_test.dart` — 4 tests for `lib/imported_run_id.dart`: `stableRunIdFromExternalId` is deterministic per external_id, differs across external_ids + source namespaces, and emits a canonical v5 UUID (#361).
- `shared_file_import_test.dart` — 27 tests for `lib/shared_file_import.dart`: 5 for `detectRouteFormat` (known extension, case-insensitive, GPX/KML content-sniff when the extension is missing/unknown, non-route → null), 2 for `routeFromImportedFile` (parses a GPX / KML body), and 6 for `SharedFileImportService.importPath` (imports a `.gpx` / `.kml` file + saves to the store, imports a GPX body handed over with a wrong extension via content-sniff, fails a non-route file / an unparseable `.gpx` / a missing file). The `receive_sharing_intent` plugin surface is injected so no MethodChannel opens.
- `routing_test.dart` — 36 tests for `lib/routing.dart` (OSRM client used by the in-app route builder): snapToRoad parses + falls back to input on non-Ok / network error, builds /foot vs /car path correctly; fetchRouteThrough returns empty on <2 points, parses routes[0].geometry + distance, semicolon-joins lng,lat pairs, per-segment fallback + cancellation + batching; plus the `RouteSegmentCache` group (key order/profile sensitivity, incremental re-route fetches only the new segment, no-cache re-fetches all, failed segments aren't cached, clear() re-fetches).
- `elevation_test.dart` — 15 tests for `lib/elevation.dart`: fetchElevations returns empty for empty input, parses elevation array, returns zeros on fetcher error / length mismatch, batches inputs > 100 points across 2 requests, builds comma-joined lat/lng query params; calculateElevationGain sums positive deltas only, returns 0 for flat / descending / <2-sample series; sampleCoordinates returns input unchanged ≤ maxPoints, downsamples to exactly maxPoints, produces evenly-spaced indices.
- `geocoding_test.dart` — 32 tests for `lib/geocoding.dart` (MapTiler geocoding + the region-aware club-search helpers): 13 for `searchPlaces` (ok-empty on a <2-char query, empty apiKey routes through the Nominatim fallback, parses MapTiler features into PlaceResult, URL-encodes the query + includes limit/key params, skips malformed center coords, falls back to `text` when `place_name` absent — plus the outcome group that pins the distinction the empty-list contract used to erase: **unavailable** on a thrown fetcher, on an HTTP error (the Nominatim 429 case), on a non-list body, on a MapTiler response with no `features` array, and on a timeout, against **ok-empty** when the provider genuinely matched nothing); 2 for `haversineM` (zero distance for identical points, London↔Paris within 1 km of reference 344 km); 1 for `bboxRadius` (1° square at the equator → ~78.5 km half-diagonal); 8 for `geocodePlace` (null on <2-char / empty apiKey / no features / no center / fetcher error, parses top feature into `GeocodedPlace` with bbox-derived radius for "Virginia" in the 400–600 km range, defaults to 5000 m radius when bbox absent, falls back to `text`). Backs `SocialService.searchClubs`.
- `route_overlap_test.dart` — 8 tests for `lib/route_overlap.dart`: empty for <4 points, one-way / no-retracing returns no overlap, out-and-back marks return-half as overlap, tolerance gates parallel-but-separate streets, span covers contiguous indices, OverlapSpan.contains hits indices inside the span, long polylines down-sample (perf bound under 500 ms).
- `run_heatmap_test.dart` — 10 tests for `lib/run_heatmap.dart` (mirror of web `run_heatmap.test.ts`): grid-cell collapse, repeated-route weight accumulation, distinct-cell separation, `kMaxCellWeight` clamp, invalid/out-of-range point drop, empty/all-invalid → no cells + null bounds, `heatBounds` extent, `heatBounds` spanning the short way round the antimeridian (east passes 180 rather than the box wrapping the planet), invalid-gridDeg fallback.
- `run_heatmap_screen_test.dart` — 3 widget tests for `lib/screens/run_heatmap_screen.dart`: renders the AppBar chrome; aggregates the downloaded tracks (via the `fetchRunsFn`/`fetchTrackFn` seams) and shows the legend; the empty state renders when no run carries a track. flutter_map tile fetches fail under the test runner (the standard map-test constraint) — the test pins the data path + chrome, not tile load.
- `snap_to_start_test.dart` — 15 tests for `lib/widgets/snap_to_start.dart`: shouldSnapToStart is false with <3 existing waypoints, true within 30 m of start when 3+ waypoints, false outside tolerance, false at-start when route too short, respects a custom tolerance.
- `turn_cues_test.dart` — 14 tests for `lib/turn_cues.dart` (mirror of web `turn_cues.test.ts`): straight line → no cues, empty / single-point / two-point → no cues, 90° left → one left cue at the vertex, 90° right → one right cue, sub-threshold wiggle suppressed, slight bend just over threshold → slight, near-reversal → u-turn, coincident-vertex merge, plus the densely-sampled group — a 90° corner sampled every 10 m fires exactly ONE left cue AT the corner (turns-remaining 1, not 2), a 40° bend that used to vanish fires its slight cue, a corner rounded over three vertices fires one cue at its full angle, and the same corner reports once at five different samplings.
- `tile_pack_test.dart` — 10 tests for `lib/tile_pack.dart` (mirror of web `tile_pack.test.ts`): bbox → correct tile set at one zoom, multi-zoom union, count cap triggers, degenerate point bbox → one tile/zoom, swapped corners normalised, antimeridian/degenerate handling.
- `turn_cue_announcer_test.dart` — 17 tests for `lib/turn_cue_announcer.dart`: empty/straight cue lists announce nothing, the 300 m band fires first, each band fires once per turn near→far, the 0 band is the "now" announcement, a turn behind the runner is never announced, only the nearest upcoming turn is considered, the far turn fires only after the near one is passed, slight/u-turn directions announced.
- `offline_tile_pack_test.dart` — 9 tests for `lib/offline_tile_pack.dart` (injected `TileBytesFetcher`): happy path writes one file per tile → ready; a failing tile / a thrown fetch is isolated → partial; a retry only fetches the gaps; `deletePack` removes the dir; the count cap → `tooLarge` with no download; `cachedTile` hit/miss; `init` reconciles an already-downloaded pack to ready; **`deletePack` mid-download** (gated fetcher held open) removes the pack and leaves `_packs` absent — the unpin-during-download race guard (issue #401).
- `route_conditions_test.dart` — 5 widget tests for `lib/widgets/route_conditions.dart` (fake `ApiClient` subclass): empty state + Report affordance for a signed-in viewer, a seeded condition renders chip + severity + note, the composer writes via the api, `canReport: false` hides the composer, a signed-out viewer sees no composer. (The undo window arms a real Timer; `_drain` pumps past it.)
- `replay_dot_index_test.dart` — 6 tests for `replayDotIndex` in `lib/screens/run_detail_screen.dart`: null/degenerate inputs return null; a same-length displayed track (no map match) round-trips to the same index; a shorter or longer map-matched track maps proportionally and stays in range; the result is always a valid index into the displayed track. Pins the fix for the replay dot drifting off the map-matched line.
- `show_raw_track_test.dart` — 5 tests for `displayedRunTrack` in `lib/screens/run_detail_screen.dart` (the map-matching "Show raw GPS track" toggle): prefers the matched line when present + `showRaw` off (default); `showRaw` forces the raw track even when a matched line exists; falls back to raw with no match info; falls back to raw when the match isn't renderable (pending / sub-2-point); falls back to raw when a `matched` row couldn't download its gz (`trackUnreachable`, the offline-fallback L1 pin). The offline map-match selectors themselves (`isMatchUnreachableError` / `matchPillKind` / `shouldRetryMatchFetch`) are pure + live in `core_models/run_match_info.dart`, unit-tested in `packages/core_models/test/run_match_info_test.dart` (12 tests).
- `settings_email_notifications_test.dart` — 3 widget tests for the email-notifications picker on `settings_preferences_screen.dart`: the tile renders the default ("Important only") subtitle; picking "Everything" writes `{email_notifications: all}` and picking "Off" writes `{email_notifications: off}` through `SettingsSyncService.updateUniversal`.
- `settings_push_notifications_test.dart` — 3 widget tests for the push-notifications picker on `settings_preferences_screen.dart` (mirror of the email one): the tile renders; picking "Everything" writes `{push_notifications: all}` and picking "Off" writes `{push_notifications: off}` through `SettingsSyncService.updateUniversal`.
- `dim_screen_while_recording_test.dart` — 7 tests for the "dim screen while recording" battery-saver (issue #271): `Preferences.dimScreenWhileRecording` defaults false + round-trips through SharedPreferences + is cleared by `resetAccountScopedPrefs`; the pure `shouldDimRecordingMap` gate in `run_screen.dart` dims only while recording with keep-screen-on AND the toggle on (never during the countdown's own scrim, never with keep-screen-on off).
- `settings_dim_screen_test.dart` — 3 widget tests for the "Dim screen while recording" tile on `settings_preferences_screen.dart`: renders with its battery-saver subtitle + enabled by default; toggling flips `Preferences.dimScreenWhileRecording`; disabled (`onChanged == null`) while keep-screen-on is off.
- `push_messaging_bridge_test.dart` — 4 unit tests for `lib/push_messaging_bridge.dart` (native-push device-token registration) over a fake `PushMessaging` seam + fake `ApiClient`: an unavailable-messaging `attach()` is a complete no-op (no permission ask, no registration); `setNotificationsEnabled` is a guarded no-op when no token is registered (proves the null-token guard, not just a swallow) and routes nothing to the api; `attach()` exposes the bridge as the static `instance`. The auth-driven register-on-sign-in / re-register-on-refresh / remove-on-sign-out paths need a live Supabase session (mobile has no e2e by design — `docs/testing/testing.md`); they're exercised on-device.
- `report_sheet_test.dart` — 10 widget tests for `lib/widgets/report_sheet.dart`: title + reason radio count + Submit button render; title reflects targetKind (user / club / route / club_post / run); Submit forwards the picked reason + trimmed notes to `submitReport`, and forwards a `run` targetKind; empty notes pass `null` not `""`; PostgrestException 23505 surfaces "already have a pending report" copy and keeps the sheet open; Cancel pops without calling the API; default reason is `spam`.
- `strava_test.dart` — 16 tests for `lib/strava.dart`: `isStravaConfigured` is false when `STRAVA_CLIENT_ID` is absent / empty / the `12345` placeholder, true for a real client id; `stravaAuthUrl` throws when unconfigured, builds a `/oauth/authorize` URL with the right client_id/scope/approval_prompt/redirect_uri, URL-encodes the redirect.
- `revenuecat_test.dart` — 8 tests for `lib/revenuecat.dart` (mobile Pro purchase flow wrapper): `isRevenueCatConfigured` returns false when no platform key is in `dotenv.env`, true when `keyOverride` non-empty, false when `keyOverride` empty; `configureRevenueCat` returns false when no API key is provided; `keyOverride` is honoured (configure attempts but headless test surfaces MissingPluginException internally → wrapper catches and returns false — no crashes on host-test runner); `startProCheckout` returns `PurchaseResult.notConfigured` when SDK has no API key; `managementUrl` returns null when SDK has no API key; `proMonthlyPriceString` returns null when SDK has no API key (so the Subscribe tile falls back to the `$9.99` USD list price). The wrapper is the integration glue used by `settings_pro_screen`'s "Subscribe to Pro" tile.
- `pro_sellable_test.dart` — 7 tests for `lib/pro_sellable.dart`'s pure `parseProPerks`: both flags read off the manifest; `sellable` is true for either perk and false for neither; missing keys read as off; non-boolean truthy values (`"true"`, `1`, a nested object) read as off; malformed / empty / non-object bodies read as off; `routeGen` camelCase is NOT accepted (the wire contract is `route_gen` only, so a drifted server shape can't silently re-enable selling); `ProPerks.none` is the fail-closed default.
- `settings_pro_screen_test.dart` — 4 widget tests for `lib/screens/settings_pro_screen.dart`: no purchase CTA when the deploy has no live Pro perk (teaser tile is inert — `onTap` null — and the USD regional note is gone, while Restore purchases + Manage subscription stay reachable); the CTA returns when a single perk goes live; the storefront stays a teaser while the perk lookup is unresolved and flips when it resolves; and the original double-submit guard (tapping Subscribe disables the tile for the in-flight window). Drives both states through the `loadPerks` injection seam — no network.
- `route_builder_screen_test.dart` — 54 tests for `lib/screens/route_builder_screen.dart`: initial state shows "Tap the map" hint + Save disabled; AppBar renders the place-search field + Locate FAB; mode toggle has Trail / Road / Straight segments; `straightLineDistance` math; `overlapLatLngsFor` slices the polyline by span indices, clamps endIndex, skips out-of-range spans, drops <2-point slices.
- `live_broadcaster_test.dart` — 19 tests for `lib/live_broadcaster.dart`: 8 legacy `insertLivePing`-path tests (starts inactive; `pushPing` is a no-op when inactive; `attach` flips `isActive`; first ping after attach goes through with the full field set; back-to-back pings within the 5 s window are throttled; `detach` silences future pings; api errors are swallowed (L4); re-attach resets the throttle so the next run's first ping fires immediately) plus 4 new transport-selection tests (routes through the Go hub when `hubClient` is configured; falls back to `insertLivePing` when `hubClient` is null or `isConfigured=false`; hub HTTP failure stays swallowed per the L4 contract). Pure Dart, fake `ApiClient` + stub fetcher.
- `live_hub_client_test.dart` — 11 tests for `lib/live_hub_client.dart` (HTTP push wrapper for the Go live hub): `isConfigured` flag semantics; `pushPing` targets `/v1/live/{run_id}/push`, URL-encodes the run id, strips trailing slashes, omits null optional fields, includes set optional fields, returns the fetcher status, propagates fetcher errors.
- `services_integration_test.dart` — wire-level tests against a real local Supabase via the seed user (gated on `SUPABASE_TEST_URL`). Drives `SocialService.browseClubs` / `searchClubs` / `fetchMyClubs` / `fetchClubBySlug` / `fetchClubRoutes` / `fetchClubPosts` / `setRouteClub` / `createClub` + `leaveClub` / `joinClub` / `createPost` + `deletePost` / `fetchUpcomingEvents` / `fetchEventById` / `fetchAttendees` / `rsvpEvent` + `clearRsvp` / `createEvent` / `submitEventResult` + `fetchEventResults` + `removeEventResult` / `fetchRecentRuns`, plus `TrainingService.fetchMyPlans` / `fetchActiveOverview` / `fetchPlan` / `fetchWorkout` / `fetchPlanForWorkout` / `updateWorkout` / `fetchClubTemplates` / `createPlan` + `updateStatus` + `deletePlan` / `markCompleted` / `clonePlanTemplate` / `publishPlanAsTemplate` through the `withClient(SupabaseClient)` DI seam — the same shape as `ApiClient.withClient`. Region-aware `searchClubs` cases use a stub geocoder so the RPC's text branch + geographic branch can be driven independently of MapTiler. CI runs this file from the `api-client-integration` job.
- `run_detail_stats_grid_test.dart` — 4 widget tests for `StatGrid` on run detail: all eight secondary stats get a ≥72dp cell on a 360dp phone (they got 40 before), a four-cell secondary row shares its column grid with the primary row, the grid drops to two columns at 2.0x text scale rather than shrinking every cell, and the heart-rate summary row reflows instead of overflowing its `Row`.
- `app_bar_actions_test.dart` — 6 widget tests: a destructive action is never promoted to a visible slot and stays reachable in the overflow, no overflow button appears when everything fits, and on route detail an owner sees three toolbar slots (not six) with a title that keeps ≥120dp, the overflow holding star / visibility / club transfer / delete behind a divider, while a non-owner leads with the bookmark.
- `fab_clearance_test.dart` — 4 widget tests measuring `fabScrollClearance` against real `Scaffold` layout: it covers the button on a phone with a 3-button nav bar (120dp occluded), does not double-count an inset a `SafeArea` or a parent bottom nav bar already took (72dp), and clears a two-FAB column.
- `architecture_guards_test.dart` — 205 tests: static source-level assertions that pin in place the efficiency + layering optimizations (no `setState` in `_onSnapshot`, `markSynced` doesn't rewrite the run file, sync paths use `saveRunsBatch`, `ErrorWidget.builder` override present, RunNotificationBridge pins geolocator channel constants + its lock-screen action buttons must broadcast to `RunActionReceiver` (never a `getActivity` MainActivity launch, which trips the keyguard on a locked phone — #270), plus the `LocalRunStore` newer-wins guards — `save`/`update` must stamp `last_modified_at`, `saveFromRemote` must not — added in the Apr 2026 data-sync hardening pass; the May 2026 UI-freeze guards: `StravaImporter.importFromZip` must dispatch to `compute()`, `BackupService` must keep the `_encodeArchiveInIsolate` / `_decodeArchiveInIsolate` helpers, `routes_screen._importFile` must wrap `RouteParser` in `compute()`, and both stores' `_loadAll` must keep the directory walk on `listSync` (the async stream form deadlocks `RunsScreen` widget tests under flutter_test — see [decisions.md § 48](../../docs/architecture/decisions.md#48-heavy-import--backup-parsers-run-in-compute-isolates-never-on-the-ui-thread)); and the May 2026 notification-primitive guards: no direct `showSnackBar` or `ScaffoldMessenger.of(context)` calls anywhere under `lib/screens/` or `lib/widgets/` — the canonical primitive is `showTopBanner` from `widgets/top_banner.dart` (see [../../docs/architecture/conventions.md § Mobile in-app notifications](../../docs/architecture/conventions.md#mobile-in-app-notifications--showtopbanner))). Latest additions: the May 2026 iOS Info.plist guards — auto-skip on the Android twin (no `ios/` folder there) and assert on the iOS twin that every plugin-required usage-description key is set (`NSHealthShareUsageDescription`, `NSBluetoothAlwaysUsageDescription`, `NSMotionUsageDescription`, `NSPhotoLibraryUsageDescription`, plus the two location keys) and that the Workmanager `BGTaskScheduler` identifier `com.threkir.backgroundSync` is permitted with `UIBackgroundModes:processing`. The Jun 2026 crash-atomic write guards: no bare `writeAsString` in any of the five `local_*_store.dart` files (every record + sidecar write goes through `writeJsonAtomic` from core_models) and gym/food/gear `_rewriteAll` must write new files before deleting orphans (see [decisions.md § 123](../../docs/architecture/decisions.md)). The Jun 2026 at-rest / backup-exclusion guards: the phone manifest must set `android:allowBackup="false"` (keeps the GPS/HR cache + the SharedPreferences-stored Supabase session out of Android Auto-Backup) and `AppDelegate.swift` must mark the Documents dir `isExcludedFromBackup` (iOS-twin-only check, auto-skips on Android) — see [decisions.md § 127](../../docs/architecture/decisions.md). **When one of these fails, read the `reason:` before rubber-stamping a fix** — a failure means a recent change reversed an optimization we deliberately codified.
- plus `run_recorder`'s own tests in `packages/run_recorder/test/` — 18 behavioural in `run_recorder_test.dart` + 7 guards in `architecture_guards_test.dart` + 6 in `laps_serialiser_test.dart` (canonical `metadata.laps` round-trip) + 13 in `workout_runner_test.dart` (step expansion, auto-advance, halfway / last-50m progress, skip / abandon, pace-adherence wayBehind, results JSON shape)

Two stream-test gotchas from the sim-watch work: (1) never `await controller.close()` on a single-subscription `StreamController` whose listener was cancelled or never attached — the returned future only completes after a listener consumes the done event, and `testWidgets` has no default timeout, so the run hangs forever (use `unawaited(controller.close())`); (2) a real `Socket` reifies as `Stream<Uint8List>`, which `Stream.transform(Utf8Decoder)` rejects at runtime — decode byte streams with `decoder.bind(stream)` and pin any socket-consuming decoder with a `Uint8List`-typed stream test, because `List<int>` fakes will happily pass.

See [../../docs/testing/testing.md](../../docs/testing/testing.md) for how to run them and the patterns they use. Widget tests exist for every screen and widget, including `RunScreen` (`run_screen_test.dart`) and `LiveRunMap` (`live_run_map_test.dart`) (~1270 `testWidgets` calls across ~390 files as of 2026-08-03 — `grep -cE '^\s*testWidgets\(' test/*.dart`); see `test/` for the full list.
