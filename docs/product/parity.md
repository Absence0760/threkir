---
name: Cross-platform feature parity matrix
description: Single table listing every user-visible feature with a per-platform status cell (shipped / partial / not started / N/A).
---

# Cross-platform feature parity matrix

> **iOS + Apple Watch are deferred.** The landing page's platform strip marks both "In testing" — built, not in the stores (decisions § 1623) — and the team is not actively pushing them forward right now. Nothing in either column has been observed running on a Mac build, simulator or device. The Apple columns will be re-energised when the deferral is lifted; until then, prioritise Android / Wear OS / Web work first. How to read the **iOS** column is stated once, under [What an iOS cell means](#what-an-ios-cell-means); the Apple Watch column is a separate native Swift codebase and is not covered by that rule.

The app ships on five surfaces — **Android**, **iOS** *(deferred)*, **Web**, **Wear OS**, **Apple Watch** *(deferred)* — and features drift between them. This doc is the single place where that drift is visible. Every user-facing feature has a row, every platform has a column, and every cell uses one of the four symbols the Legend below defines.

> **Marketing / SEO surfaces are web-only by design and not tracked as drift.** The landing page, the legal pages (`/privacy`, `/terms`, `/cookie-notice`, `/health-data-notice` — decisions §243 — plus the store-facing `/delete-account` instructions page, decisions §247), `/compare`, and the public **Learn / guides** surface (`/learn`, shipped 2026-06-19 — [learn.md](../features/learn.md), [decisions.md § 161](../architecture/decisions.md#161-learn-guides-are-repo-committed-markdownmdsvex-prerendered-to-static-html-not-a-db-backed-cms)) are acquisition/SEO content, which is a web property ([decisions §24](../architecture/decisions.md)). They have **no mobile/watch twin** and a future session should not flag their absence on the device columns as parity drift.

See [roadmap § Cross-platform parity enforcement](roadmap.md#future--cross-platform-parity-enforcement) for why this exists, and [decisions.md § 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive) for the web-canonical rule that shapes how gaps get closed.

## Legend

| Symbol | Meaning |
|---|---|
| `✓` | Shipped and working end-to-end on that platform. |
| `Partial` | Scaffold, mock-data screen, wired for read-only but missing the full flow — or code that ships on that platform but whose runtime nobody has observed there. Expand in the **Notes** column, unless a column-level rule already accounts for it. |
| `✗` | Not started. May be a genuine gap (drift) or a planned future item. |
| `N/A` | Intentionally not applicable — the platform cannot reasonably provide the feature (e.g. pedometer in a browser). Expand in the **Notes** column so it isn't rediscovered as "missing". |
| 🔸 in Notes | This row is a **gap against the web-canonical principle** ([decisions.md § 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive)) — web is `✗` or `Partial` on a feature that is *not* a physical exception. Close these by building the web version, not by adding to the matrix's tail. |

<!-- parity-ios-rule -->
### What an iOS cell means

`apps/mobile_ios/lib/` and `test/` are byte-identical to `mobile_android` ([decisions.md § 39](../architecture/decisions.md#39-mobile_android-and-mobile_ios-share-a-byte-for-byte-dart-codebase)), and no row in this matrix has been run on a Mac build, simulator or device. An iOS cell is therefore not an observation. It is **derived from the Android cell**, and the only thing a row can add for itself is an obstruction on the iOS side of the shared Dart: a `Platform` gate, a method channel with no iOS handler, a plugin that does not declare iOS, or a missing `Info.plist` key or entitlement.

| Android cell | iOS cell |
|---|---|
| `✓` | `Partial` |
| `Partial` | `Partial` |
| `✗` | `✗` |
| `N/A` | `N/A` |

So the column carries no `✓` at all: shared Dart nobody has run is wired, not shipped. A cell that departs from the table states the obstruction in its own Notes as `**iOS <symbol>:** <why>`, and that marker is the only per-row iOS claim this document wants — everything the rule already says, a row does not repeat. The one owed device run is tracked once in [followups.md](followups.md), never per row.

`scripts/check_parity_ios_column.mjs` enforces the table above, the marker, and the fact that this block is the only place the column's vocabulary is stated.
<!-- /parity-ios-rule -->

**Columns:**

- **Android** — `apps/mobile_android` (Flutter). Most mature surface.
- **iOS** — `apps/mobile_ios` (Flutter). Dart codebase byte-identical to mobile_android ([decisions.md § 39](../architecture/decisions.md#39-mobile_android-and-mobile_ios-share-a-byte-for-byte-dart-codebase)); the column is derived, not observed — see [What an iOS cell means](#what-an-ios-cell-means). Also [apps/mobile_ios/CLAUDE.md](../../apps/mobile_ios/CLAUDE.md).
- **Web** — `apps/web` (SvelteKit 2 + Svelte 5).
- **Wear OS** — `apps/watch_wear` (native Kotlin + Compose-for-Wear). Recording-only by design.
- **Apple Watch** — `apps/watch_ios` (native SwiftUI). Recording-only by design.

## How to use this doc

- Reviewing a PR that adds or changes a user-visible feature? Check the matrix. If the PR ships on one platform only, the other cells should stay `✗` (or flip to a planned follow-up), not silently stay empty.
- Opening a new feature? Add a row here before you merge. It's much cheaper to promise `✗` for four platforms than to discover a year later that one of them half-implemented the feature from a different angle.
- Auditing for drift? Scan for rows where `Android` and `Web` disagree. That's the most common source of the silent desync bugs this matrix exists to prevent (`activity_type`, `surface`, `moving_time`, `source` have all burned us before).
- A mismatch is acceptable only if the **Notes** column explains the gap. If it doesn't, it is drift and should be either fixed or documented-as-intentional in the same PR.

## Intentional asymmetries (don't re-flag these)

A periodic audit greps this matrix for rows whose platform ticks aren't uniform and checks each against the code. The patterns below are **permanent / structural** asymmetries — a mismatched row matching one of these is *not* drift and should not be opened as a follow-up. Anything *not* covered here that has a non-uniform row is a candidate gap.

- **The iOS column differing from Android** — it is derived from Android rather than observed, per [What an iOS cell means](#what-an-ios-cell-means). A row where the two differ is that derivation, not drift, and the owed device run is one item in `followups.md` rather than a defect per row.
- **Web `N/A` on a recording / device-sensor / on-wrist feature** — browsers can't reliably record a run, read a pedometer/BLE strap, or run a foreground service ([decisions.md § 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive)). Live GPS recording, background location, wakelock, indoor mode, HR-via-sensor, offline tile cache, treadmill FTMS, etc. are correctly `N/A` on Web.
- **Web `✓` while mobile is `✗` and the Notes say "web-canonical per § 24"** — back-office + authoring + acquisition surfaces are deliberately web-first: admin moderation queue, bulk results CSV import, claim-an-imported-result, AI route request, route-design preferences, the `/learn` guides, marketing/SEO pages. Building the device twin is optional platform-additive work, not drift.
- **Watch `N/A` on a non-recording surface** — the watches are recording-only wrist complements, NOT pocket-app mirrors. History/analytics, social, training-plan authoring, settings editors, nutrition, gym, coaching, sharing, etc. are correctly `N/A` (or `✗` where a recording-adjacent feature is genuinely unbuilt) on Wear OS / Apple Watch.
- **Watch settings keys read-only / `✗`** — by design the wrist never hosts a settings editor ([`apps/watch_wear/CLAUDE.md`](../../apps/watch_wear/CLAUDE.md)); universal prefs are consumed read-only on Wear OS and edits stay on phone/web.
- **A genuine physical exception is already `N/A` with a Notes explainer** — e.g. pedometer on web, haptics on Wear OS ([decisions.md § 15](../architecture/decisions.md)), self-hosted Protomaps (a deploy-side infra decision, not a per-client feature).

**iOS column swept 2026-08-25** ([decisions.md § 739](../architecture/decisions.md)) — the whole column re-derived in one pass against [What an iOS cell means](#what-an-ios-cell-means). 268 of the 337 cells moved; five rows depart from the derivation and each names its obstruction in its own Notes. The per-row "pending Mac-runtime verification" clauses are gone with them, since the rule now says it once. The before-and-after tallies are in the ADR.

**Last audited: 2026-06-20.** Every non-uniform row was checked against the source. Two cells were factually wrong and corrected (race-time predictor cells inverted; matched-track display on mobile marked `✗` despite being wired) — see those rows' Notes. No genuine *unintentional* drift gaps were found beyond what `followups.md` already tracks (race-predictor mobile card #11; native-push mobile leg; DSAR export coverage). The roadmap audit checkbox under "Cross-platform parity enforcement → 2. Living feature parity matrix" records the same date.

## Auth and onboarding

See [features § Cloud sync and auth](features.md#cloud-sync-and-auth).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Email / password sign-in | ✓ | Partial | ✓ | ✓ | ✗ | **Wear OS was miscalled `✗`** — it ships a real native `SignInScreen` (email + password straight to GoTrue's password grant via `SupabaseClient.signIn`), and has since before this row was written. The phone handover is the ordinary path, not the only one: a watch separated from its phone would otherwise have no way to authenticate at all. Reached from the PreRun sign-in chip, and — since [decisions § 1545](../architecture/decisions.md) — from PostRun whenever there is no usable session, which is the screen the `SyncFault.SignInRequired` banner renders on. **Apple Watch `✗` stands**: `SupabaseService.signIn` exists but its only caller is `syncRunDirectDebug`, which hands it the seed account, and there is no `SecureField` anywhere in the target — it piggy-backs on the phone's session over Watch Connectivity. It is not a shipped-but-unreachable path: the whole of `SupabaseService.swift` sits inside `#if DEBUG` and `WatchApp.xcodeproj` defines `DEBUG` on the Debug configuration alone, so a Release build compiles neither the grant nor the credential. Both halves of that are now claim (14) of `scripts/check_watch_ios_source.mjs` ([decisions § 1596](../architecture/decisions.md)), because deleting one `#if DEBUG` line shipped both and nothing would have said so. |
| Password show/hide toggle | ✓ | Partial | ✓ | N/A | N/A | Mobile: every obscured field via `PasswordField` (sign-up, sign-in, change-password dialog). Web: every obscured field via the shared `PasswordInput` component — `/login` sign-in + sign-up, `/auth/reset`, and the `/settings/account` change-password fields (#487 closed the earlier `/login`-only gap). |
| Google OAuth | ✓ | Partial | ✓ | ✗ | ✗ | iOS: scaffolded, needs provider credentials. |
| Apple OAuth | ✓ | Partial | ✗ | ✗ | ✗ | Android: native `sign_in_with_apple` SDK via `sign_in_screen.dart`. iOS: behind `_kAppleSignInEnabled = false` pending Services ID setup. Web: button rendered behind a "Soon" pill, `handleAppleSignIn` surfaces a "coming soon" toast — Apple Services-ID configuration for the web OAuth flow is the unblocking step. |
| Auth callback / deep-link handler | ✓ | Partial | ✓ | N/A | N/A | Watches never handle the OAuth redirect directly. |
| Onboarding flow (first launch permissions) | ✓ | Partial | N/A | N/A | N/A | Web has no location permission pre-gate; the browser prompts per-feature. |
| Post-signup setup wizard (display name + goal + units + privacy + notifications) | ✓ | Partial | ✓ | N/A | N/A | Web: 7-step `/onboarding` wizard mounted by `apps/web/src/routes/onboarding/+page.svelte`, gated by `user_profiles.onboarded_at` (migration `20261016_001` adds the column + backfills existing rows with `now()`). Layout-level redirect catches signed-in users with `onboarded_at = null` on any protected route. The header-level Skip-onboarding link stamps the column so the gate never re-fires. The **per-step** Skip is a different thing and means "leave this one unanswered": on web it clears the step's answer before advancing and is offered only on the goal + about steps, where an answer can be left unset; on mobile it is the same single forward button relabelled, per [decisions § 1647](../architecture/decisions.md). Two shapes, one meaning ([decisions § 1650](../architecture/decisions.md)). Settings shows a "Finish setting up" nudge for users who left key fields unset. See `docs/architecture/decisions.md § 78`. **Mobile twin shipped** (`apps/mobile_android/lib/screens/setup_wizard_screen.dart`, byte-identical iOS twin): `home_screen.dart` fetches the profile after the first frame and pushes the wizard once when `onboarded_at == null`; `ApiClient.completeOnboarding` / `markOnboarded` write the same fields + stamp the column (Art 9 consent gates gender + the consent stamp; DOB is always written for the minor-exclusion floor, mirroring web). The notifications step writes the mobile `push_notifications` bag pref (no native OS prompt, unlike web's push step) — and writes it only when the runner taps a level, so that step reads as unanswered until they do; web's push step stores no answer at all and therefore offers no skip. Pure contract twin `onboarding.dart` ↔ web `onboarding.ts`. The units step seeds its default from the visitor/device locale on both platforms (`defaultUnitForLocale`, `locale_defaults` parity pair — mi for US/GB/LR/MM); an explicitly chosen unit still wins. |
| Offline-only mode (no auth required) | ✓ | Partial | N/A | N/A | N/A | Architectural exception for web: the web app is cloud-first by design — every view reads from Supabase, sign-in is the entry point, there is no local-first run database in the browser. Mobile keeps runs local first and syncs opportunistically; the watches inherit auth from their phone. |

## Route management

### Import

See [features § GPX / KML import](features.md#gpx--kml-import).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| GPX import | ✓ | Partial | ✓ | N/A | N/A | |
| KML import | ✓ | Partial | ✓ | N/A | N/A | |
| KMZ import | ✓ | Partial | ✓ | N/A | N/A | Web unzips with JSZip and hands the inner document to its KML path. Mobile reached parity 2026-09-02: the whole route-import read path is bytes now (`readAsBytes` through the `compute` request), a KMZ is unwrapped by `routeTextFromImportedBytes` before the format dispatch, and it is recognised by the zip magic number rather than by an extension an OS share often drops. Both entry points — the file picker and the OS "Open with" share — take it. iOS is `Partial` only because no Mac build has run it: the Dart is byte-identical to Android (decisions §39), so the cell flips with the rest of the iOS column when a device run lands. See [decisions § 1025](../architecture/decisions.md). |
| Route-file "Open with" + share-target | ✓ | Partial | N/A | N/A | N/A | Device-led (decisions §297). The OS hands a shared GPX / KML / KMZ / GeoJSON / TCX file to the app → imported as a route (a KMZ is unwrapped by `routeTextFromImportedBytes`, recognised by its zip magic number — which is what makes it work on this path at all, since an OS share routinely drops the extension; [decisions § 1025](../architecture/decisions.md)) (reuses `RouteParser` + `LocalRouteStore` via `lib/shared_file_import.dart`). Android: `ACTION_VIEW` "Open with" (incl. WhatsApp, which shares as `application/octet-stream`) + `ACTION_SEND` share intent-filters. iOS: `CFBundleDocumentTypes` document "Open with" wired; the iOS system *share sheet* needs a Share Extension target (deferred). Unit-tested (`shared_file_import_test.dart`); on-device end-to-end verification pending (mobile has no e2e by design). |
| GeoJSON import | ✓ | Partial | ✓ | N/A | N/A | Mobile reached parity 2026-09-02 ([decisions § 988](../architecture/decisions.md)) — the Routes picker and the share-sheet dispatch offered GPX and KML only, so a `.geojson` was parsed as GPX and failed. A GeoJSON saved as `.json` resolves through an extension alias, as web's `accept` list has always allowed. |
| TCX import | ✓ | Partial | ✓ | N/A | N/A | Web: `parseTcx` added to `lib/import.ts`; the import drop-zone accepts `.tcx` alongside GPX/KML/KMZ/GeoJSON. Preserves per-point elevation + timestamp. Mobile reached parity 2026-09-02, same change as GeoJSON ([decisions § 988](../architecture/decisions.md)). |
| Strava ZIP bulk import | ✓ | Partial | ✓ | N/A | N/A | Shipped on web: `/settings/integrations` → "Bulk import from a Strava export" — parses `activities.csv` + per-activity GPX/TCX files, dedupes against already-imported Strava IDs, reports live progress. |
| CSV summary re-import | ✓ | Partial | ✗ | N/A | N/A | Mobile-only counterpart to Settings → "Export runs as CSV". Surfaced as a card on the Import screen; accepts both the 5-column Settings export and the 17-column backend `/export-data` GDPR shape. Offline-first — works signed-out (rows queue in `LocalRunStore` for the next `SyncService` drain). Idempotent on re-import via a stable `external_id` (`csv:<iso>-<dist>-<dur>` or the original column). **Lossy** — no GPS track; the card copy warns explicitly. See `decisions.md § 65`. |
| Import failure report (what failed, and why) | ✓ | Partial | ✓ | N/A | N/A | A bulk import that drops activities names each one and classifies why, instead of reporting a bare count. Shared shaping is the `import_failures` TS↔Dart parity pair (`classifyImportFailure` → network / auth / rate_limited / too_large / unparseable / rejected / unknown, a 200-entry cap with the overflow counted, and a CSV report with English column headers so two reports merge). Web: `ImportFailureReport.svelte` under each import card on `/settings/integrations`, behind the Strava + Garmin ZIP importers. Mobile (Android + iOS byte-identical twin): `widgets/import_failure_report.dart` on the Import screen, behind Strava ZIP, Health Connect / Apple Health, and CSV — each importer records at the point it catches, so no reason is lost to a discarded error, and the report shares as a CSV via the OS share sheet rather than a browser download. The mobile screen additionally discloses a batch cloud push that failed after a successful local save, which previously left the import reading as a clean success. |
| Backup ZIP restore (lossless) | ✓ | Partial | ✓ | N/A | N/A | `BackupService.restore` on mobile / `lib/backup.ts` on web — runs + routes + per-run tracks + profile/prefs. Surfaced on the mobile Import screen alongside Strava + Health + CSV, and on Settings → "Restore from backup". Works **offline-first** on mobile — rows land in `LocalRunStore` + `LocalRouteStore` and ride the next `SyncService` cycle to Supabase. The Settings + Import wrappers thread `routeStore` through to the offline branch (previously a silent skip, fixed when CSV-import landed in May 2026). See `decisions.md § 65`. **iOS `Partial`** — the Full backup and Restore tiles are the same ungated `ListTile`s Android renders (`settings_account_screen.dart`), `BackupService` names no `Platform`, and every package on the path (`archive`, `file_picker`, `share_plus`, `path_provider`) declares iOS support; no method channel is involved and neither Info.plist nor the entitlements owe anything a document picker or share sheet needs. |

### Builder and library

See [features § Full-screen route builder](features.md#full-screen-route-builder-web) and [features § In-app route builder (mobile)](features.md#in-app-route-builder-mobile).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Full-screen click-to-place route builder | ✓ | Partial | ✓ | N/A | N/A | Mobile shipped on `route_builder_screen.dart`: tap-to-place + OSRM road/trail/straight modes + undo/clear/save + drag-to-reshape (long-press → tap-to-move) + snap-to-start (pulsing halo on start when route is loop-ready) + MapTiler place search + geolocate FAB + overlap detection (purple out-and-back stroke). Reached from the second "Build" FAB on `routes_screen`. Wrist screens too small by design. |
| Snap-to-road / trail (OSRM) | ✓ | Partial | ✓ | N/A | N/A | Mobile shipped via `lib/routing.dart` — the SOLE client-side OSRM implementation, and deliberately NOT a lockstep parity pair ([decisions § 1119](../architecture/decisions.md) deleted web `routes/routing.ts`'s `snapToRoad` / `fetchRoute` / `fetchFullRoute` because nothing imported them, leaving it the `/api/routes/osrm` proxy client alone, with `RouteBuilder.svelte` building its service paths inline). `snapToRoad` + `fetchRouteThrough` wrap the public OSRM endpoint with foot / car profiles. 36 unit tests in `test/routing_test.dart` on URL building, parsing, error fallback and the `RouteSegmentCache`. |
| Elevation preview while drawing | ✓ | Partial | ✓ | N/A | N/A | Mobile uses `lib/elevation.dart` against Open-Meteo (sample down to ≤100 points per re-route). Renders as `N m ↑` in the status pill while drawing. 12 unit tests cover the URL build, batch splitting, empty-on-error fallback, gain calculation, and sample-downsample shape. |
| Route library (list saved routes) | ✓ | Partial | ✓ | ✗ | ✗ | iOS code is identical to Android per [decisions.md § 39](../architecture/decisions.md#39-mobile_android-and-mobile_ios-share-a-byte-for-byte-dart-codebase) — `Partial` reflects "Mac-build runtime parity not yet verified end-to-end" rather than "screen is mock". |
| Route detail (map + stats) | ✓ | Partial | ✓ | ✗ | ✗ | Route `description` (migration `20260902_001`) is set in the route builder's Save modal and rendered under the title on `route_detail_screen.dart` + `public_route_screen.dart` (mobile parity with web `/routes/[id]` + `/share/route/[id]`). Both clients state the route row's stored `elevation_m` as its one climb; web derives descent from it and draws the profile from the waypoints (`routeElevation`, [decisions § 1637](../architecture/decisions.md)). |
| Route course markers (aid stations, cut-offs, …) | ✓ | Partial | ✓ | ✗ | ✗ | Owner annotates a saved route line with typed markers (aid station / cut-off / crew access / hazard / note / climb) carrying per-kind detail (aid services, cut-off clock). Markers follow route visibility and a non-owner read redacts in-privacy-zone pins (`route_markers_for_viewer` RPC, the marker analogue of `clip_route_for_viewer`). Web: `RouteMarkerEditor.svelte` + `RunMap` pins on `/routes/[id]` — click-to-place, **typed lat/lng coordinate entry** (labelled validated fields, the keyboard / screen-reader placement path — mobile matches with coordinate fields in the editor sheet + an "Enter location instead" button), **snap-to-route-line** (default-on toggle, `routes/route_snap.ts`), and **drag-to-move** draggable pins (drag is web-only; mobile is tap-to-place or typed coordinates). Mobile: `widgets/route_markers_panel.dart` + `LiveRunMap` pins on `route_detail_screen.dart`. Shared `route_markers.ts ↔ .dart` helper (kind catalogue + `sortMarkers` + `parseCutoff`). `position_m` derived server-side from `routes.geom`. **Viewer contributions (decisions §296):** any signed-in viewer can add their OWN markers to a public/club route (a private personal overlay — invisible to the owner and other viewers); the owner's OFFICIAL markers render read-only + badged, own-only edit/delete (RLS `20270428_001`). Markers are placeable **by distance along the route** (mi/km → `markerPointAtDistance`, `route_geometry` pair) as well as map-tap / lat-lng. Segments got the same viewer-add ungate (they were already community-contributable at the backend). See [route_markers.md](../features/route_markers.md) + [decisions.md § 152](../architecture/decisions.md#152-route-course-markers-are-their-own-table-with-server-derived-position-and-a-redacting-viewer-rpc). Club-defined custom kinds deferred. |
| Route condition reports (community) | ✓ | Partial | ✓ | ✗ | ✗ | Any signed-in viewer of a route can file a freshness-stamped, severity-graded condition report (clear / muddy / flooded / snow-ice / overgrown / closed / hazard / other × info / caution / impassable) with an optional note + anchor. Reports follow route visibility; a non-owner read **nulls the anchor of any report inside the owner's privacy zones** (`route_conditions_for_viewer` SECURITY DEFINER RPC, the condition analogue of `route_markers_for_viewer`; client read fails closed to `[]`). Author **or** route owner can delete (spam cleanup). Web: `RouteConditions.svelte` on `/routes/[id]` + `/share/route/[id]`. Mobile: `widgets/route_conditions.dart` on `route_detail_screen.dart` (full) + `public_route_screen.dart` (read-only). Migration `20270215_001`; `condition`/`severity` narrow-union CHECK + TS unions + `check_constraint_unions.mjs` guard. UI fades a report older than 30 days; never auto-expired. **Privacy-boundary surface — `/audit/privacy-zones` is a pre-deploy gate.** See [trail_navigation.md](../features/trail_navigation.md) + [decisions.md § 171](../architecture/decisions.md). |
| Offline map tile packs (route bbox) | ✓ | Partial | ✓ | N/A | N/A | "Save for offline" on a route now downloads the map tiles covering the route bbox (z12–z16, `tilesForBbox` with a 5000-tile per-pack cap) to a **sibling** on-disk cache (`offline_packs/<routeId>/`) that the LRU tile cache can't evict, so the live map renders with zero connectivity for a pinned route; un-pin deletes the pack. Read-through: the live map serves a pack tile from disk first, falls through to network/LRU. Mobile-only (web has no offline map; watches inherit starred routes via DataLayer). `offline_tile_pack.dart` + the `tile_pack.ts ↔ .dart` pure parity pair (8 mirror tests). L4: a failed tile is skipped (partial pack + retry), never breaks the online map. **Prod gate: MapTiler-ToS sign-off for bulk tile persistence** (else self-hosted Protomaps). See [trail_navigation.md](../features/trail_navigation.md) + [decisions.md § 170](../architecture/decisions.md). |
| Per-cue voice toggles + spoken corrections (#607) | ✓ | Partial | Partial | ✗ | ✗ | Every recording cue type is individually toggleable (Settings → "Spoken cues"): splits, start/finish, off-route, pace alerts, workout steps, cutoff catch-up, marker targets, race phases. Backed by the `voice_cue_types` map ([settings.md](../backend/settings.md)) — absent id = on, entry-by-entry merge on every apply. The pace alert now speaks the correction amount ("speed up by 15 seconds per kilometre", rounded to 5 s), and the new cutoff catch-up cue speaks distance + still-sufficient pace off `live_cutoff_eta.requiredPaceSecPerKm` when the projection is tight/behind (honest "limit has passed" when unreachable). **Web is `Partial` by design:** the eight toggles ship on `/settings/recording` (the key became `UD` so web's universal write reaches the phone — decisions §469), but web never *plays* a cue, because recording is mobile-only (decisions §235). Decisions §295. |
| Course-marker target times + crossing cues (#608) | ✓ | Partial | ✓ | ✗ | ✗ | `meta.target_elapsed_s` on any route marker (`parseTarget` in the `route_markers` pair): editable in the web RouteMarkerEditor + mobile markers panel (same h:mm:ss parsing), shown as a Target chip in both course-schedule lists, seedable one-click from the roadbook's projected arrivals (owner-only "Save as marker targets", web). While recording along the route, crossing a targeted marker speaks ahead/behind-plan (`marker_targets` cue). [race_roadbook.md](../features/race_roadbook.md). |
| Race pacing strategy — 10-10-10 / negative split / even (#609) | ✓ | Partial | ✗ | ✗ | ✗ | Pre-run strategy sheet (preset + distance prefilled from the route + optional goal time) builds a `race_phases` phase plan: live phase chip, spoken phase transitions with per-phase target pace, pace alerts re-anchored to the current phase's target, and the executed strategy stamped into `runs.metadata.pacing_strategy` ([metadata.md](../backend/metadata.md)). Web carries the twinned `race_phases` helper only (recording is mobile-only); per-phase post-run grading is the tracked follow-up on #609. Decisions §295. |
| Turn-by-turn voice cues (route follow) | ✓ | Partial | ✓ | ✗ | ✗ | While following a saved route, the recorder speaks "in 300 m, turn left" / "turn left" cues generated **purely from the route polyline geometry** (no routing engine, no network, no key — works offline). `turn_cues.ts ↔ .dart` parity pair (`generateTurnCues`, 14 mirror tests) + the mobile `TurnCueAnnouncer` decision core (announce at ~300/100/0 m, once per band per turn) spoken via the existing `audio_cues` TTS path. Gated on a `Preferences.turnByTurnCues` device toggle (default on with audio cues). L4: wrapped in the `_ttsCue` try/catch — a TTS failure never disturbs recording. Mobile-only (device-led). Watches render stats-only during recording (no nav). See [trail_navigation.md](../features/trail_navigation.md) + [decisions.md § 169](../architecture/decisions.md). |
| Race roadbook (crew sheet) | ✓ | Partial | ✓ | ✗ | ✗ | The crew sheet a route's course markers + a goal time imply: per-checkpoint cumulative distance, projected arrival (elapsed + wall clock), cutoff margin (green/amber/red), per-leg vert, aid services. Goal time is allocated by grade-adjusted **effort** (`gradeFactor`, Minetti) not even pace, degrading to even with no elevation (Open-Meteo backfill on demand). Web: `/routes/[id]/roadbook`, goal/start/model in URL params (shareable), print-friendly + copy-as-text. Mobile: `RoadbookScreen` + share-as-text. Both surfaces also state a per-leg **pace** — the number the effort model actually produces, and the only place its output is readable as a pace rather than as a cumulative arrival: a Leg pace column on web, a line per checkpoint on mobile, both in the viewer's km/mi unit and both present in copy/share-as-text. A leg the model cannot price (the start row, a zero-length leg between two markers on the same spot) reads as an em-dash on both rather than a confident number. A checkpoint carrying a target time (#608) also gets a **target verdict** — resolved time, signed margin, ahead/on-plan/behind in words — on both surfaces: a Target column on web, a chip above the cut-off chip on mobile. Grade is measured over a trusted-length window, not per point-pair, so the allocation can't collapse onto even pace on a densely-sampled course ([decisions.md § 600](../architecture/decisions.md)). Shared `roadbook.ts ↔ .dart` engine (15 mirror tests). See [race_roadbook.md](../features/race_roadbook.md) + [decisions.md § 153](../architecture/decisions.md#153-the-race-roadbook-allocates-goal-time-by-grade-adjusted-effort-and-shares-via-url-params-not-a-schema). Saved named plans deferred. |
| Race fueling plan (carbs/hr + fluids) | ✓ | Partial | ✓ | ✗ | ✗ | The fueling layer over the roadbook: a "Fueling" toggle adds per-leg **carbs** + **fluid** columns + a "carry to next aid" hint, scaling each figure with the leg's duration off the roadbook timeline. A "Heat" toggle bumps fluid only (non-persisted). Rates come from universal prefs `carbs_per_hour` (60 g/hr default) + `fluid_per_hour` (500 ml/hr) — flat per-hour, not weight-derived, so no Art 9 health gate. kcal is computed per leg but not shown (clutter). Web: URL-backed toggle on `/routes/[id]/roadbook`. Mobile: screen-local toggle on `RoadbookScreen`. Shared `fuel_plan.ts ↔ .dart` engine (9 mirror tests). In-run reminders deferred. See [race_fueling_plan.md](../features/race_fueling_plan.md) + [decisions.md § 155](../architecture/decisions.md#155-the-race-fueling-plan-extends-the-roadbook-engine-with-flat-per-hour-rate-prefs-not-weight-derived-sweat-estimates). |
| Race-director checkpoint check-in (offline aid-station → live results) | Partial | Partial | Partial | N/A | N/A | Offline aid-station volunteers stamp each runner in/out at each checkpoint, syncing to a live organiser results + cutoff board and a public results page. Account-optional identity (`user_id` OR bib+name); offline conflict resolution merges in/out inside the single `upsert_checkpoint_crossing` RPC; the P3 weigh-in / medical fields are Art 9, built **fail-closed** (`requires_weigh_in` + consent gate + column-lock, decisions §150 / §154). **Data foundation shipped** (migration `20270201_001`: `event_checkpoints` + `checkpoint_crossings`, the two RPCs, RLS + column-lock, 17-test pgtap, the shared `checkpoint_projection` helper twin). **Web (canonical):** organiser live board (P2) + public results (P4) building on top. **Android/iOS:** offline volunteer check-in screen (`local_crossings_store`, batched drain) building. **Watches N/A** (volunteer data-entry on a wrist is the wrong surface). See [race_director_ops.md](../features/race_director_ops.md) + [decisions.md § 154](../architecture/decisions.md). |
| Predictive live tracking (next cut-off ETA) | ✓ | Partial | ✓ | N/A | N/A | On the public spectator page, when the live run is linked to a **public route carrying cut-off markers**, a "Next cut-off" card answers *"will they make it?"* — checkpoint name, distance to go, projected arrival, and a green/amber/red margin chip (on / tight / behind). It projects the runner onto the planned line (`distanceAlongRoute`, the `route_geometry` inverse), reads the cut-off limits off the roadbook legs (`buildRoadbook`), and grades the projection (current elapsed + remaining ÷ recent flat pace) via the `live_cutoff_eta.ts ↔ .dart` pair (18 mirror tests). **Honest staleness:** when `live_freshness` flags the fix stale or pace is unknown, the verdict is `unknown` — the card names the checkpoint but shows "waiting for a fresh signal" and **suppresses the chip**, never a false ETA. Suppressing the verdict does not suppress what the card knows ([decisions.md § 601](../architecture/decisions.md)): the pace still required (independent of recent pace, so it survives a stale fix — labelled "from the last fix") and an already-expired limit are both stated outright, and the race clock feeding the projection keeps running through a dead zone so a deadline that passes in one registers — off the run's own `started_at` via `raceClockS` where it resolves, with `liveElapsedS`'s ping-age advance (a lower bound on the same clock) as the fallback ([decisions.md § 712](../architecture/decisions.md)). An unlocated runner names no checkpoint at all, and a concluded run projects none. Web: card on `/live/[id]`. Mobile: `live_spectator_screen.dart`. All reads, works logged-out. Watches N/A (no spectator surface). See [predictive_live_tracking.md](../features/predictive_live_tracking.md) + [decisions.md § 156](../architecture/decisions.md#156-predictive-live-tracking-reuses-the-roadbook-cut-off-legs-and-fails-to-unknown-when-the-fix-is-stale). Grade-adjusted remaining + EWMA pace deferred. |
| Describe this route (templated + AI) | ✓ | Partial | ✓ | N/A | N/A | Web: `/routes/[id]` "Describe this route" renders the localised, unit-aware templated sentence instantly (`localisedTemplate` over `describeRoute` — distance band, surface, m/km elevation character, loop-vs-point-to-point shape); Pro users' tap calls `/api/coach/route-describe` (`claude-opus-4-8`, adaptive thinking) for an AI paragraph, gated server-side on `is_pro()` fail-closed and falling back to the templated text on every failure (`decisions.md § 151`). Mobile route-detail wires the same affordance: the `describeRoute` twin helper feeds an in-screen localised template built from the ARB catalogue + `UnitFormat`, the tap then runs a Pro check (`isPro()`, fail-closed) and for Pro users calls the same endpoint via `route_describe_client.dart`; any non-Pro / failure path keeps the templated baseline. The endpoint now also requires the **v2 AI disclosure** (`decisions.md § 571`), and both platforms branch on the 403 body's `code` rather than the bare status the paywall shares: web shows `routeDetail.describeConsentRequired` pointing at Settings → Account, mobile shows the same line plus an inline **Review disclosure** action that opens the shared `AiDisclosureNotice` and re-runs the enhancement on acceptance (`§ 573`). Watches N/A (no route-detail surface). |
| Route-design preferences: quiet / scenic / cul-de-sac | N/A | N/A | ✓ | N/A | N/A | Web-only — the distance loop generator is a web-canonical server surface (`/api/routes/generate`), there's no mobile equivalent. A four-option single-choice control on `/routes/new` sets `preference` to `quiet`, `scenic` or `cul_de_sac`, which now rides the v3 chain — the graph-cycle sidecar honours it on its own weighted search (soft per-edge multipliers over a road-class + green-space attribution byte; opt-in cul-de-sac mode splices capped dead-end stubs), with the GraphHopper custom-model `round_trip` only as the fallback it already was. Before 2026-08-30 a preference SKIPPED the sidecar entirely and downgraded the runner onto `round_trip` ([decisions § 796](../architecture/decisions.md)). Both the sidecar and the 200 body report `preferenceApplied` from the serving engine's verdict, never echoed from the ask, and the page says so when an ask was not honoured. **Elevation-aware is deferred for want of a DEM, not code** — neither engine imports terrain ([§ 795](../architecture/decisions.md)). See [graph_cycle_loop_generation.md § Extension](../features/graph_cycle_loop_generation.md#extension-route-design-preferences-a-layer-on-the-graph). |
| AI route request (NL → constraints) | ✗ | ✗ | ✓ | N/A | N/A | Web-only. The "Describe the route you want" box on `/routes/new` (in the generate-by-distance panel) posts a plain-English request to `/api/coach/route-request` (`claude-opus-4-8`, adaptive thinking, **forced tool_choice** `extract_route_constraints`), which extracts a constraints object the deterministic generator already understands. The LLM never routes — `validateConstraints` (the `route_request/constraints.ts` trust boundary) clamps/whitelists every field (distance band, shape, surface, avoid-highways) before it reaches the form; a hallucinated value collapses to a safe default. Pro-gated server-side on `is_pro()`, **fail-closed** (unknown Pro status → 503 unavailable; non-Pro → 403 upsell). Purely additive: on any failure the manual generator form is untouched. The box populates distance + surface and surfaces shape / avoid-highways / assumptions as a review hint. Unit tests: `constraints.test.ts` (clamp/validate, 11) + `handler.test.ts` (pre-Supabase branches, 7); Playwright `route-request.spec.ts` (mocked endpoint — fill / Pro upsell / failure). Companion of the describe row above (`ai_route_assistant.md` § Component A). Mobile/watch not built (web-canonical, decisions §24). |
| Public / private toggle per route | ✓ | Partial | ✓ | N/A | N/A | Bidirectional on both: Android's globe icon on `route_detail_screen.dart`; web's owner-only `Public` / `Private` button on `/routes/[id]` (added in parallel with the explicit Share button; `setRoutePublic(id, bool)` in `data.ts` with optimistic UI + rollback). |
| Shareable route link | ✓ | Partial | ✓ | N/A | N/A | Web makes the route public and copies the link in one action. |
| Export route as GPX | ✓ | Partial | ✓ | N/A | N/A | Android: share button on `route_detail_screen.dart` writes the route's waypoints to a temp `.gpx` and hands it to the system share sheet. |
| Export route as KML | ✓ | Partial | ✓ | N/A | N/A | Android: share popup on `route_detail_screen` offers GPX or KML; KML emits a single `<Placemark>` `<LineString>` with `<coordinates>` lng,lat,ele. |
| Course waypoint export (GPX + markers) | ✓ | Partial | ✓ | ✗ | ✗ | The route line as a `<trk>` plus one `<wpt>` per course marker (name + raw kind in `<type>`, a Garmin `<sym>` where one maps, cutoff time + aid services in `<desc>`) so a watch surfaces "Aid 2 in 1.3 km" mid-race. Exported markers are privacy-clipped. Shown only when the route has ≥1 marker. Web: "GPX + markers" download on `/routes/[id]` + `/routes/[id]/roadbook` (`routeDetail.exportGpxMarkers`). Mobile: "Share as GPX + markers" on `route_detail_screen.dart` (`routeDetailShareAsGpxMarkers`). Shared `route_gpx.ts ↔ .dart` emitter (`toRouteGpxWithMarkers` / `routeGpxFromRoute`, 10 mirror tests). FIT Course (`CoursePoint`) export deferred to v2. See [course_waypoint_export.md](../features/course_waypoint_export.md). Watches have no route-detail surface. |
| Share route as link (send to a follower) | ✓ | Partial | ✓ | N/A | N/A | One-tap share of the public `/share/route/[id]` link. Web: copy-link share bar on `/routes/[id]` (`handleShare` — flips the route public first). Mobile: "Share link" in the route-detail share menu → OS share sheet (`routeShareUrl` + `shareTextFrom`); gated to public/owner so a non-owner never shares a dead link. iOS mirrored byte-identical; on-device verification pending. Decisions §298. |
| Send a route to a follower in-app (targeted DM) | ✗ | ✗ | ✓ | N/A | N/A | The *targeted* complement to the link share above: pick a person, and the route's public `/share/route/[id]` URL lands in your DM thread with them without leaving the app. Web: a "Send to a follower" button beside Share on `/routes/[id]` opening `SendRouteDialog.svelte`, which sends through the existing `sendDm`. **v2 shipped 2026-08-28** (decisions §772): migration `20270619_001` adds a nullable `direct_messages.route_id` FK — INSERT gated on `private.is_route_visible_to` so a sender can only attach a route they can see — and the thread draws it as a card through `DmRouteAttachment.svelte`, which resolves via the owner-aware `fetchRouteById` so a non-owner recipient gets the privacy-zone-clipped line and a route they may not see says so rather than showing a dead link. The body still carries the share URL (the inbox preview and the Art 20 export read the body and neither resolves an attachment); it is suppressed on screen once the card draws it. [route_direct_share.md](../features/route_direct_share.md). Picker is the DM-*eligible* set (`social/dm_recipients.ts`), i.e. the union of both follow directions, because the `direct_messages` INSERT policy admits a follow edge either way. A still-private route routes through the same public-flip confirm the copy-link share uses, so a targeted send can never widen exposure silently. Playwright `routes/send-to-follower.spec.ts`. **Mobile is `✗` because there is no mobile `/messages` twin at all** — see the "Direct messages (1:1)" row below, which is `✗` on both device columns; this leg is gated on that surface existing first, not on a route-detail button. Until then mobile's targeted send is the shipped Share link → OS share sheet. Decisions §734. |
| Start / save a non-owned public route | ✓ | Partial | N/A | N/A | N/A | A follower who opens a shared / public route can **Start a run** following it (privacy-clipped waypoints) and **Save it to their library** (bookmark), via `public_route_screen`'s Start FAB + bookmark action routed through the global `pendingStartRunWithRoute` handoff (also fixes the route-detail Start FAB doing nothing from a pusher that didn't handle the pop). Recording is device-only (§235) so Web is N/A — the web "save a public route" already exists via RouteExplorer. Decisions §298. |

### Discovery

See [features § Community route library](features.md#community-route-library).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Explore public routes (search + filters) | ✓ | Partial | ✓ | ✗ | ✗ | |
| Popular / near-me feed (PostGIS) | ✓ | Partial | ✓ | ✗ | ✗ | Uses `nearby_routes` RPC. |
| Community heatmap discovery browser (filters + clustering + list) | ✓ | Partial | ✓ | ✗ | ✗ | Web `/routes/heatmap`: a results sidebar (search + a **Filters** popover + scrollable route list) beside the map — nothing floats over the map. Filters = lens (`popular` / `friends` / `featured` / `hidden_gems` on `p_filter`, migration `20261113_001`) + multi-select **race-distance bands** (5K/10K/Half/Marathon/Ultra, any permutation, via `p_dist_min[]`/`p_dist_max[]`, migration `20261114_001`) + layer toggles. MapLibre route-pin clustering, distance-band badges on list rows, heat off by default (opt-in; not fetched until enabled), hover-to-preview (route lines hidden by default; hovering a dot or its list row reveals that one line, synchronized map↔list), a **cluster/overlap list popup** (overlapping start pins can't be zoomed apart → hover lists them to pick), and **keep-on-map / pin** (the violet line persists across pan + filter; Clear to reset). **Clicking a route never navigates** — a click on a list row, a map dot, a line, or an overlap-popup row *keeps* that route on the map; navigation is an explicit **"View route →"** link per sidebar / overlap row (decisions § 105), so a mis-click can't yank you off the discovery surface. `friends` = routes created by users you follow; `hidden_gems` = un-run routes past a 1 km floor. **Mobile (`routes_heatmap_screen.dart`, byte-identical twin):** mirrors the full browser — lens + race-distance band filters (Filters sheet), pure-Dart clustered pins (no new dep), results `DraggableScrollableSheet`, the cluster list (tap → sheet), and keep-on-map/pin. Mobile's tap model differs by design: a **tap selects** a route (preview + a route card whose button navigates), which already avoids the web mis-click trap, so the web click-keeps model (§ 105) is not mirrored. `distance_bands.dart` is the Dart twin of `distance_bands.ts`. |
| Cross-club activity discovery (Social Discover tab) | ✓ | Partial | ✓ | ✗ | ✗ | Web `/social?tab=discover` (`SocialDiscover.svelte`) + mobile `discover_screen.dart` (4th tab on `social_screen.dart`, byte-identical iOS twin) both run the `search_public_events` RPC (migrations `20270110_001` + `20270111_001`, `security invoker`, scoped to `clubs.is_public = true` so it works logged-out). Filters: free-text query (title + discipline ILIKE), category (run/cycle/class/social), cadence (one-off/weekly/biweekly/monthly), weekday, time-of-day (morning/afternoon/evening, resolved against each event's `events.timezone` so it's the organiser's LOCAL hour, UTC fallback for legacy rows), and free/paid (cheapest `event_pricing` row). Results open the club event detail. Mobile data layer: `ApiClient.searchPublicEvents` + `PublicEventResult` (core_models), mirror of web `core/data.ts#searchPublicEvents`. See `decisions.md § 147`. |
| Route ratings and comments | ✓ | Partial | ✓ | ✗ | ✗ | Shared `route_reviews` table; one review per user per route. **Deleting your own review is web-only** (2026-08-02, issue #664): author-only button on the route-detail review card, confirm-gated through `ConfirmDialog`, scoped by `(route_id, user_id)` rather than review id so a caller cannot aim it at another row — the `20260627_001` RLS DELETE policy is the hard gate either way. Mobile is still read + create + report only, so this row's Android cell covers creation, not deletion. |
| Club-owned routes | ✓ | Partial | ✓ | ✗ | ✗ | `routes.club_id` + Routes tab on `/clubs/[slug]`. Admins create / transfer / remove. See `docs/features/clubs.md § Club-owned routes`. Android: 4th "Routes" tab on `club_detail_screen.dart` lazy-loads via `SocialService.fetchClubRoutes`; rows open in `PublicRouteScreen` (read-only). The owner-side **transfer** + **detach** actions live on `route_detail_screen.dart`'s AppBar — opens `RouteTransferClubPicker` which lists the viewer's owner/admin clubs (filtered via `adminClubsForRouteTransfer`) plus a "Detach to personal" row when the route is currently club-owned. `Route.clubId` was added to the domain model so the picker can pre-mark the current club; both `_routeFromRow` codecs (ApiClient + SocialService) pass it through. |
| Bookmark public routes (saved_routes ref) | ✓ | Partial | ✓ | ✗ | ✗ | Replaces the old "save = clone the row" flow. `decisions.md § 30`. Android: `route_detail_screen.dart` shows a bookmark toggle in the AppBar for non-owner viewers (idempotent against `saved_routes` composite-PK). `routes_screen.dart` fetches bookmarks alongside the owned list and merges them with a "Saved" badge so they appear in the route library — taps open the detail with `isOwner: false`. |
| Star routes for the watch picker (`is_starred`) | ✓ | Partial | ✓ | ✓ | ✗ | Owner-curated flag drives the Wear OS route picker. Toggle from web (`/routes` cards + `/routes/[id]` header), mobile (`routes_screen.dart` trailing star, `route_detail_screen.dart` AppBar star). Wear OS is **read-only** by design — the watch fetches `is_starred=eq.true&order=updated_at.desc&limit=30`; when that returns empty (first-launch / un-curated) it falls back to the 10 most-recently-updated owned routes so the picker isn't empty. An empty picker now distinguishes its two causes (`decisions.md § 1019`): a watch that could not reach the list — no session pushed from the phone yet, or the fetch threw — says so, instead of telling a runner with fifty saved routes to go and build one. No toggle on the wrist. Schema: `routes.is_starred boolean` + partial index `idx_routes_user_starred (user_id, updated_at desc) WHERE is_starred`. See `decisions.md § 44`. Apple Watch has no picker and doesn't read `is_starred` — a route reaches that watch by an explicit per-route **Send to Apple Watch** push instead (see the phone → Apple Watch row below). |
| Save a route for offline (per-device pin) | ✓ | Partial | N/A | N/A | N/A | Per-device, local-only flag — never written to Supabase. `LocalRouteStore` persists pinned IDs to `offline_pinned_route_ids.json` alongside the existing synced-ids sidecar. Surfaces as the AppBar download icon + inline SwitchListTile on `route_detail_screen.dart`, with a `download_done` badge on the `routes_screen.dart` row. Web has no offline mode by design; watches inherit starred routes via the phone-DataLayer push instead. See `decisions.md § 64`. |
| Phone → Wear OS push of starred routes (DataLayer) | ✓ | N/A | N/A | ✓ | N/A | `WearRoutesBridge` (Dart + Kotlin) subscribes to `LocalRouteStore` on the paired phone and pushes the user's starred subset to the watch at `/saved_routes` whenever it changes. Watch's `RoutesBridge` updates DataStore-backed `LocalRouteStore` + the live picker — run-start works without watch connectivity as long as the phone has wifi/LTE. Supabase fetch in `refreshRoutes()` remains the canonical path when the watch has its own network. See `decisions.md § 64`. **iOS N/A:** Wear OS dropped iPhone pairing at Wear OS 3, so there is no phone half of this to build on the iOS twin. |
| Phone → Apple Watch push of the armed route (WCSession) | N/A | Partial | N/A | N/A | ✓ | The Apple-Watch counterpart of the Wear row above, but explicit rather than ambient: **Send to Apple Watch** on the route-detail share menu (`AppleWatchRouteBridge` in Dart → `WatchIngestBridge.swift` on the phone → `ArmedRoute` on the watch), shown only when a paired watch with the app installed reports in. Transport is `WCSession.transferUserInfo` — queued and durable across app launches and watch reboots, because the runner picks a route long before the watch app is on screen and `sendMessage` would fail in exactly that case. 512-point budget, the same number in Dart / phone Swift / watch Swift; a denser route is thinned by `simplifyToBudget` (never cut at the cap), a shorter-than-two-point one is refused with a reason, and the watch drops any mismatched / over-cap / non-finite payload whole. Reads the privacy-clipped polyline (decisions §33). **iOS Partial:** the phone half is iOS-only inside the byte-identical Dart twin, dispatched on `defaultTargetPlatform`, so Android is `N/A` (no Apple Watch pairing) and the derivation has no Android cell to read. |

## Run recording

See [features § Live GPS run recording](features.md#live-gps-run-recording), [features § Apple Watch standalone GPS recording](features.md#apple-watch-standalone-gps-recording), and [features § Wear OS standalone GPS recording](features.md#wear-os-standalone-gps-recording).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Live GPS recording | ✓ | Partial | N/A | ✓ | ✓ | iOS: identical Dart to Android via the byte-identical twin — `RunScreen` and `RunRecorder` are the same files. What the device run has to confirm on this row specifically: `Info.plist` `UIBackgroundModes: location`, the location entitlements, and a real-device soak. Web: browsers cannot record a run reliably — reviewing only. |
| Background location tracking | ✓ | Partial | N/A | ✓ | ✓ | iOS recorder has background mode wired in Info.plist but recording loop not end-to-end. |
| 3-second start countdown | ✓ | Partial | N/A | ✓ | ✗ | Wear OS shows a full-screen 3-2-1 overlay between permission grant and `vm.start()` (tap to cancel). Apple Watch still starts immediately. |
| Manual pause / resume | ✓ | Partial | N/A | ✓ | ✓ | |
| Hold-to-stop (prevents accidental stop) | ✓ | Partial | N/A | ✓ | ✗ | Wear OS requires an 800 ms press on the Stop button; a circular progress ring fills during the hold and releasing early cancels. |
| Lap markers | ✓ | Partial | N/A | ✓ | Partial | Both watches expose a Lap button during recording and persist the marks into `run.metadata.laps` on sync, in the registered per-lap shape (`{ index, start_offset_s, distance_m, duration_s }`, `start_offset_s` cumulative-BEFORE). Wear OS builds the splits (`buildFinishedLapsList`) but deliberately does **not** render them on the wrist — its post-run screen is an edge-anchored overlay on a full-bleed route preview a table would obscure. Apple Watch (2026-09-18) mirrors the shape in `RunLaps.splits`, including the trailing-partial gate, renders a compact split list on its `ScrollView` post-run summary, and carries the cumulative marks in the 15 s crash checkpoint so a recovered run lands the laps the runner took. The `laps` key reaches the row over `WCSession` only since the phone bridge's key list learned it in the same change. `Partial` on Apple Watch because no lap has been marked on a real wrist: `xcodebuild test` on the watchOS 26.4 simulator covers the split arithmetic, the envelope shape and the checkpoint round-trip, not the tap. |
| Wakelock during run | ✓ | Partial | N/A | N/A | N/A | watchOS / Wear OS handle their own wake policies. |
| Activity types (run / walk / cycle / hike) | ✓ | Partial | N/A | ✓ | ✗ | Wear OS has a CompactChip on the PreRun screen that cycles run → walk → hike → cycle; the choice stamps into `metadata.activity_type` on save. **Note:** the watch clients still write the jsonb key, not the promoted `activity_type` column — the Round-4 Tier-2 column switch (`20261207_001`, F3) is web + mobile only, so a non-run watch activity currently lands as `'run'` on the column with the real value in the bag until the watch bridge is switched. Apple Watch still records as "run". **Vocabulary:** the chip's five labels are the phone's and web's words verbatim in all six Wear locales (decisions § 713 — the abbreviated `Lauf` / `Correr` / `Caminar` / `ラン` / `ウォーク` / `サイクル` are gone; the 56 dp chip ellipsises either way and the `contentDescription` carries the whole word). There is no divergence left: `hike` read "Hike" / "Wandern" / "Senderismo" / "Rando" / "ハイク" / "Trilho" on the wrist against "Trail run" / "Traillauf" / "Trail" / "トレイルラン" / "Trail" elsewhere until decisions § 1155 took the owner call and the wrist adopted the phone's word in all six locales that differed (pt-BR already said "Trilha"). The guard's exemption is gone with it, so a future divergence fails the build. |
| TTS audio cues (splits + pace alerts) | ✓ | Partial | N/A | ✓ | ✗ | Wear OS: `recording/TtsAnnouncer.kt` wraps `android.speech.tts.TextToSpeech`; `RunRecordingService` announces "Run started", each completed unit of the runner's `preferred_unit` ("1 kilometre. Pace 5 minutes 30 seconds per kilometre" / "1 mile. Pace 8 minutes 2 seconds per mile"), pace-drift alerts, and a finish summary. **The cue is unit-aware on both halves** (`decisions.md § 467`): the cadence comes from `UnitFormat.completedSplits(distance, unit)` — a mi-mode runner is cued once per *mile*, not every kilometre under a mile label — and every spoken resource has a `_km` / `_mi` variant in all six locales, taking the phone's own `ttsSplitUnitMile` / `ttsPaceMi` wording. The unit is read once at run start (same stamping as the tile). Apple Watch has no spoken cue at all — its pace alert is haptic-only (row below) — so this row stays `✗` there rather than carrying the same defect. Gated on `BuildConfig.ENABLE_TTS` (defaults on; `DISABLE_TTS=true` in `.env.local` silences it for dev). **Audio-focus ducking (persona #12):** all three Android-stack TTS surfaces request transient ducking so a cue lowers the runner's music/podcast instead of talking over or hard-pausing it — mobile via `FlutterTts.setAudioAttributesForNavigation()` (Android) / `setIosAudioCategory(playback, [mixWithOthers, duckOthers], voicePrompt)` (iOS), Wear OS via `TextToSpeech.setAudioAttributes(USAGE_ASSISTANCE_NAVIGATION_GUIDANCE)`. |
| Audio-coached guided runs (cue playback during a run) | ✓ | Partial | N/A | ✗ | ✗ | Armed from the Run tab's idle pre-run options row (`_pickGuidedRun`, beside Choose route and Race strategy) and spoken by an L4 block on the recorder snapshot in `run_screen.dart` via `cuesDue` + `AudioCues.announceGuidedCue`. **`announceGuidedCue` is the QUEUEING path and is not the same call the preview button makes**: `speakGuidedCue` opens with `_tts.stop()`, which is right for tapping down a preview list and wrong during a recording, because a guided cue sits on a fixed time mark (0:00, 5:00, 10:00 ...) and therefore collides with splits, off-route warnings and cut-off catch-ups it must not flush mid-word. The clock is `snapshot.elapsed`, the recorder's monotonic stopwatch, so it STOPS on a manual pause — a guided script is a sequence of running minutes, not a timetable — and the cursor only ever advances, so a repeated tick cannot re-fire a mark and un-muting late cannot dump the marks already passed. Own `guided_run` per-cue toggle, so silencing structured-workout steps does not silence a coach script. Finish stamps the owner-only `runs.metadata.guided_run_id`. **Web `N/A`** — web is not a recording surface (see the library row under Training plans and workouts for what web does have). **Watches `✗`** — the custom watch's `guided_runs.rs` core and `SET1` slot exist and the phone now arms them behind the § 71 loopback gate, but that is research-tier and is not one of these columns; Wear OS and Apple Watch have no guided-run surface. Shipped 2026-08-30; between 2026-06 and that date `roadmap.md` claimed this row worked when `cuesDue` had no production caller at all ([decisions § 802](../architecture/decisions.md)). |
| Guided-run attribution on a recorded run | ✓ | Partial | ✓ | N/A | N/A | A run recorded under a coach script says so afterwards, resolved through the library rather than shown as the stored id. Web: a chip in `/runs/[id]`'s `.detail-meta` provenance row linking to `/guided/[id]`. Android + iOS: `_buildGuidedRun` on `run_detail_screen.dart`, tapping through to the script — which can then re-arm it. **An id the shipped library no longer answers to keeps the claim and drops the name** on both platforms: the run WAS guided, and self-hiding would present it as a silent one, while the raw slug is an English identifier no reader should see. Web additionally drops the link, since `/guided/<unknown>` is the library's own dead-end empty state. Both pin the no-slug rule with a test. Owner-only twice over — `20270627000001` strips `guided_run_id` from `public_runs`, and on web the chip sits inside the owner branch a non-owner never enters — so widening the projection later would not by itself expose it. **Watches `N/A`** per [decisions § 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive). |
| Arm a guided run from its script page | ✓ | Partial | N/A | N/A | N/A | `Use this run` on `GuidedRunDetailScreen` parks the library **id** (never the resolved object — the library is rebuilt per locale, so the recorder re-resolves against its own) in a `pendingArmGuidedRun` notifier beside the existing `pendingStartWorkout`, pops to the shell, and the Run tab arms it through the same `_armGuidedRun` the in-screen picker ends on — so the watch push, the cue-cursor reset and the metadata stamp cannot diverge between the two entry points. Consumed unconditionally on arrival, unlike `pendingStartWorkout` which parks on a busy screen: a `String` id makes a repeat request a silent no-op (a `ValueNotifier` does not fire on an unchanged value), and arming mid-recording rewinds the cue cursor so the next tick would replay every mark already passed. **Web `N/A`** — web is not a recording surface. |
| Haptic pace alerts | ✓ | Partial | N/A | ✓ | ✓ | All three clients: when live pace drifts >30 s off target, a rate-limited (1/30 s) haptic fires — two pulses for "speed up", one for "slow down". Wear OS: pre-run **Pace** chip sets the target (off / 4:00 / 4:30 / 5:00 / 5:30 / 6:00 / 6:30 / 7:00 per km); `RunRecordingService.firePaceAlert` uses `VibratorManager` with a matching `VibrationEffect.createWaveform` pattern and pairs it with a TTS nudge. |
| Step count via pedometer | ✓ | Partial | N/A | ✓ | Partial | Wear OS: `Pedometer.kt` wraps `Sensor.TYPE_STEP_COUNTER` with a per-run baseline; `RunRecordingService` forwards step counts into `RecordingRepository.Metrics.steps`; the save path writes `run.metadata.steps`. Apple Watch (2026-09-18): `Pedometer.swift` wraps Core Motion's `CMPedometer` with the twin baseline (`PedometerMath.stepsSinceBaseline`), the count rides the `WCSession` envelope as `steps` and `WatchIngestBridge` + `runFromWatchPayload` land it on the row. Both write only a POSITIVE count — absent means UNMEASURED (no sensor, a declined grant), never a run of no steps. The 15 s recovery `Checkpoint` snapshots `steps` on both watches, so a **crash-recovered** run keeps its step count instead of silently uploading with none (#389). Wear requires `ACTIVITY_RECOGNITION` at runtime (requested alongside `ACCESS_FINE_LOCATION` + `BODY_SENSORS` by `permissionLauncher`); watchOS requires `NSMotionUsageDescription` + the Motion & Fitness grant. `Partial` on Apple Watch because **a simulator has no pedometer**: what has run is the baseline arithmetic, the checkpoint round-trip and both ends of the envelope — `CMPedometer` itself has never delivered a sample to this code. Browsers have no pedometer sensor. |
| Heart rate via device sensor | ✓ | Partial | N/A | ✓ | ✓ | Android pairs with an external BLE chest strap (HR Service `0x180D` / characteristic `0x2A37`) via `lib/ble_heart_rate.dart`; live BPM appears on the run screen and `avg_bpm` writes to `run.metadata` on save. **Mid-run auto-reconnect (persona #13):** a dropped strap now retries with exponential backoff (2/4/8/16→30 s, capped at 20 attempts) and surfaces a "reconnecting… / reconnected / lost" disclosure on the run screen via `BleHeartRate.statusStream`; recording continues HR-free if the strap stays gone. iOS chest-strap pairing not wired. Watches use the built-in wrist sensor. **Wear OS caveat + fail-closed behaviour (2026-09-02, `decisions.md § 1015` + `§ 1017`):** the wrist sensor is read through Health Services `MeasureClient`, which Google documents as foreground-only and "not intended for background capture or workout tracking" — so HR past the ambient transition is not promised, and the `BODY_SENSORS_BACKGROUND` declaration that claimed to fix it was removed (it gates `PassiveMonitoringClient`, and was never requested so never granted). Continuous wrist HR needs an `ExerciseClient` migration, now **scoped and deliberately not started** (`decisions.md § 1054`): it is a recorder rewrite rather than a client swap — one app owns the active exercise per device (`ExerciseTrackedStatus.OTHER_APP_IN_PROGRESS`), the platform can end a run itself (`AUTO_ENDED`), and crash recovery inverts from `CheckpointStore` to a platform-owned exercise, none of which the emulator reproduces. What is fixed is the failure path: a declined `BODY_SENSORS` used to throw `SecurityException` out of an unhandled `launch` and kill the whole recording; acquisition now fails closed, the run records HR-free, and the pre-run notice names the loss before the run starts (`§ 1018`). **And since 2026-09-03 the wrist says so mid-run** (`decisions.md § 1052`): `HeartRateMonitor` emits the `DataTypeAvailability` alongside each sample instead of discarding it, so the running screen's HR slot renders `HR…` while acquiring, `HR off wrist` on `UNAVAILABLE_DEVICE_OFF_BODY` (kept distinct because it is the only cause a runner can act on mid-run) and `No HR` otherwise, in place of the reading rather than as an extra line. The same change stops a stale reading persisting: nothing used to clear `bpm`, so a watch that slipped off the wrist at minute three rendered that figure for the rest of the run. **What the saved average may claim, and the asymmetry between the two watches (`decisions.md § 1083` + `§ 1106`):** Wear records `metadata.hr_coverage` — the share of ACTIVE elapsed time the sensor was delivering — on every run where heart rate was enabled, and **suppresses `avg_bpm` below 0.5**, because a mean over less of the run than not is not the run's average and every reader treats it as one. So a Wear run can legitimately carry a coverage figure and no average. watchOS writes neither: `HKLiveWorkoutBuilder`'s average is scoped to the `HKWorkoutSession`, which holds the sensor for the life of the session rather than the life of the foreground app, so the coverage question that forced the Wear measurement does not arise the same way — and an assumed `1.0` would be a fabricated measurement rather than a missing one. The absence is not attributable by source, though: **both watch clients write `source = 'watch'`**, so a run with no `hr_coverage` is an Apple Watch, a Wear build predating the key, or a Wear checkpoint recovery. Measuring it honestly on watchOS is filed — it needs a matching lift in `apps/mobile_ios`' `WatchIngestBridge.swift`, because the WCSession envelope is guarded at both ends. |
| Live HTTP tile cache (offline revisits) | ✓ | Partial | N/A | N/A | N/A | Watches render pure stats during recording — no live map, so there's nothing to cache. Tracked as a follow-up of the *Route navigation on watch* roadmap row rather than a recording feature. |
| GPS self-heal retry | ✓ | Partial | N/A | ✓ | ✗ | Wear OS: `RunRecordingService.gpsRetryJob` mirrors Android's `_startGpsRetryLoop` in `packages/run_recorder/lib/src/run_recorder.dart` — polls every 10 s and re-subscribes when the job has died (same shape as Android's `_positionSub == null`). Additionally re-subscribes when the stream has been silent for >30 s mid-run, a Wear-specific trigger because `FusedLocationProviderClient` doesn't error when a callback goes silent. Initial no-fix is not a stall — that's indoor mode. |
| Indoor / no-GPS mode (time-only) | ✓ | Partial | N/A | ✓ | ✗ | Wear OS: the recorder's elapsed clock ticks unconditionally; `TrackWriter` produces a valid empty `[]` track when no points arrive, so upload works. The RunningScreen banner reads "No GPS — time only" when no fix has landed yet (vs. "GPS lost" mid-run). |
| Live lock-screen / ongoing notification | ✓ | ✗ | N/A | ✓ | ✓ | Watches post persistent workout notifications via their platform workout session APIs. **Wear OS reached API 33+ watches only from 2026-09-02** (`decisions.md § 881`): `RunRecordingService` built its `OngoingActivity` notification and `startForeground` succeeded, but `POST_NOTIFICATIONS` was declared in the manifest and requested nowhere, so the platform kept the notification out of the shade and the runner lost the ongoing-activity chip — their way back into a live run from the watch face — with no error on any surface. **iOS ✗:** the `run_app/run_notification` channel has an Android handler only (`RunNotificationBridge.kt`); no iOS side posts or updates the live notification, and `run_notification_bridge.dart` documents the channel as inert there. |
| Crash checkpoint recovery | ✓ | Partial | N/A | ✓ | ✓ | |
| Ultra-length (10h+) run support | Partial | Partial | N/A | ✓ | Partial | Streaming track writer + rolling HR shipped on Wear OS. **Apple Watch:** the whole recording→finish→sync path is now allocation-bounded — a rolling in-memory `track` window, a streaming NDJSON save, and (`decisions.md § 467`) a streaming *read* (`CheckpointStore.forEachTrackPoint`, 64 KiB at a time) feeding a `FinishedRun` that carries the track's file URL rather than a `[TrackPoint]`, so `writeTrackJSON` and the DEBUG upload stream too. The old `loadTrackPoints` peaked at ~73 MB at the 100 h / ~360k-fix target. Still `Partial`, not `✓`: nothing is stress-tested on device, the figures are derived from struct layouts rather than measured in Instruments, and the Swift is Mac-CI-compiled only per the banner at the top. **Mobile (Android/iOS phone recorder):** the 10 s incremental-save loop encodes the full track on every tick; pinned by a 25 000-waypoint stress test in `test/local_run_store_test.dart` that exercises encode → write → read → decode and confirms every field (lat/lng/ele/timestamp/bpm) survives a recovery roundtrip past the 1.5 MB file-size mark. `loadInProgress` now runs the read + decode in a background isolate via `compute` so the recovery doesn't stall app launch for ultra-length sessions. Still Partial because save jank under low-end Android hasn't been profiled. |
| Live race mode (Arm / Go / End + pings) | ✓ | Partial | ✓ | ✓ | ✗ | **Participant side:** Android (`RaceController` + run_screen), Wear OS (`RaceSessionClient` + `RunViewModel.maybePushRacePing`) — both stamp the recorded run with `event_id`, post `race_pings` at 10 s cadence, submit the finisher time to `event_results`. **Organiser Arm/Go/End:** web (`apps/web/src/lib/core/data.ts:armRace/startRace/endRace` → admin panel on event detail page) and Android (`SocialService.armRace/startRace/endRace` → admin-only Race control card on `event_detail_screen.dart`, gated on `ClubView.isRaceDirector`, with realtime updates via the existing `subscribeToEvent` channel). Wear OS and Apple Watch stay participant-only — typing admin actions on a wrist is a bad UX. **Web spectator surface:** `/live/event/{id}/{instance}` is a public Realtime page subscribed to `race_pings` / `race_sessions` / `event_results`, with a MapLibre map showing each runner as a coloured dot + recent trail (latest 30 samples) and a leaderboard sorted by distance. Web has no live recording (physical exception per `decisions.md § 24`), so participation flows through the mobile / watch clients. The unrelated per-run `/live/{run_id}` page is tracked separately under "Live spectator tracking". |

## Route overlay during run

See [features § Route overlay during run](features.md#route-overlay-during-run).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Live position marker on planned route | ✓ | Partial | N/A | ✓ | ✓ | Wear OS renders a polyline + position-dot mini-map on `RunningScreen` whenever there's anything to show (planned route, accumulated track-so-far, or just the current GPS fix) — free-form runs get the mini-map too as a "shape of my run" view. When `PUBLIC_MAPTILER_KEY` is set, `streets-v2-dark` raster tiles render under the polyline using the same Web Mercator projection (`ui/RouteMiniMap.kt`, math in `recording/MercatorTiles.kt` with 13 unit tests, fetcher in `ui/TileSource.kt`, draw layer in `ui/TileLayer.kt`). Same env-var name as the web app so a single key can be shared. The track-so-far is overlaid as a faded indigo polyline behind the route, fed by a rolling buffer (`recording/TrackOverlayBuffer.halveIfOverflowing`, cap 256, 7 unit tests) that geometrically halves on overflow so the run start stays anchored. Pre-fetching tiles for offline robustness is deferred. Apple Watch shipped its own on 2026-08-21 — `WatchApp/RunMiniMapView.swift`, maths in `WatchApp/MiniMap.swift` (31 unit tests) plus 5 recorder-seam tests. It is the second page of a now-paged `RunningView` (the stats and controls keep page one, so nothing moved the Stop button for an auxiliary surface) drawn in SwiftUI `Canvas` over a local planar metre frame with **no tiles at all**: MapKit was considered and refused ([decisions § 702](../architecture/decisions.md)) because a watch off its phone has no network, and a basemap that cannot load reads as a broken map rather than as an absent one. The track-so-far comes from `MiniMapTrail` — the map's own fixed-capacity, distance-decimated breadcrumb (cap 96, 20 m initial spacing, geometric halving ported from the firmware's `trackback`) — because `WorkoutManager.track` is a 600-fix rolling window that no longer holds the start of a long run. A run with no usable fix renders "Waiting for GPS" rather than a dot. **Build-verified + host-tested only** (`xcodebuild test`, Xcode 26.4, watchOS 26.4 simulator, 203 tests green); the on-wrist legibility check is still owed and is tracked in [followups.md](followups.md). |
| Off-route detection and alert | ✓ | Partial | N/A | ✓ | ✓ | Wear OS: the pre-run Route chip opens a picker; the selected route's waypoints flow through `ACTION_START` into `RunRecordingService`, which calls `RouteMath.offRouteDistanceM` per GPS sample. `RunningScreen` shows an "Off route · N m" banner above the distance readout; entering the off-route state fires a double `HapticFeedback.LongPress`. Hysteresis (alert >40 m, clear <20 m) prevents boundary chatter. Apple Watch: same engine (`RouteNavigator.swift` — the web `route_geometry.ts`/`route_snap.ts` projection math, the same 40 m / 20 m hysteresis, `WKInterfaceDevice` haptic on the transition), now reachable end to end — the phone push arms it (row above), `WorkoutManager.start()` builds the navigator from `ArmedRouteStore`, and `RouteGuidanceView` renders "Off route · N m" during the run. It is an L4 auxiliary effect fed at the very end of `didUpdateLocations`, after distance / track / pace are committed, so a route with no usable geometry cannot cost the run a GPS fix. Cell reads `✓` = wired but not Mac-verified, per the banner at the top. |
| Distance remaining to end of route | ✓ | Partial | N/A | ✓ | ✓ | Wear OS: `RouteMath.routeRemainingM` runs alongside off-route per GPS sample; `RunningScreen` renders "X.XX km to go" under the elapsed-distance line whenever a route is loaded. Hidden when `selectedRoute` is null. Apple Watch: `RouteGuidanceView` renders the same "X.XX km to go" off `RouteNavigator.remainingMetres`, in the user's `preferred_unit`, and shows an explicit "Route position unknown" rather than a false on-route reading when the geometry can't project the current fix. |

## Glanceable tiles and complications

See [roadmap § Glanceable tiles and complications](roadmap.md#glanceable-tiles-and-complications).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Active-run tile / complication (elapsed · distance · pace) | N/A | N/A | N/A | Partial | Partial | Wear OS: only the **tile** ships — `apps/watch_wear/.../tiles/ActiveRunTileService.kt` renders an idle "Tap to start" layout and an active 3-line stat layout (elapsed time, distance, pace) via ProtoLayout. `RunRecordingService` calls `requestUpdate` on every stage transition. 7 formatter tests + a wiring guard (≥4 `requestUpdate` call-sites pinned). A watch-**face complication** is NOT built (the `Streaks.kt` note flags it as a future add); on Wear OS tiles and complications are distinct surfaces, so this row is **Partial** until the complication ships. Apple Watch: source for the WidgetKit complication ships under `apps/watch_ios/Complications/` (TimelineProvider + four widgetFamily views + an App-Group-backed bridge); `WorkoutManager.publishComplicationSnapshot` is called on every state transition. Partial because the Widget Extension target itself is a one-time Xcode add — see `apps/watch_ios/Complications/README.md`. |

## Run history and analytics

See [features § Run history](features.md#run-history), [features § Analytics dashboard (web)](features.md#analytics-dashboard-web), and [features § Deep run analysis (web)](features.md#deep-run-analysis-web).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Run list | ✓ | Partial | ✓ | ✗ | ✗ | |
| Run detail (map + stats) | ✓ | Partial | ✓ | ✗ | ✗ | iOS code is byte-identical to Android per [decisions.md § 39](../architecture/decisions.md#39-mobile_android-and-mobile_ios-share-a-byte-for-byte-dart-codebase) — `Partial` reflects "Mac-build runtime parity not yet verified end-to-end" rather than a missing screen. **Secondary stats gate per cell on both platforms (2026-09-04, [decisions.md § 1124](../architecture/decisions.md)).** Mobile used to wrap the whole secondary-stat grid in a single `run.track.length >= 2 \|\| _hasElevation` test, so a run with no geometry — every manual entry, and every Health Connect import whose source app released a workout summary but no route — rendered no calories, steps, cadence, average heart rate, HR coverage or age grade either, none of which is derived from the track. Each cell now tests its own datum and the section self-hides when none has one, which is what web's `/runs/[id]` key-stats grid has always done. Calories additionally gained the `> 0` value test web already had, so an unusable estimate is an absent cell rather than a claimed `0 kcal`. Both clients state pace for a run and speed for a ride, never both (web `activityUsesSpeed` ↔ `ActivityType.usesSpeed`, [decisions § 1638](../architecture/decisions.md)); the web delete icon carries the danger colour at rest, as mobile's overflow-menu item does. |
| "Incomplete" badge on a boot-recovered run | ✓ | Partial | ✓ | N/A | N/A | Reads `runs.metadata.recovered_unfinished` (registered in [metadata.md](../backend/metadata.md), [decisions.md § 323](../architecture/decisions.md)). The custom watch checkpoints a run in progress to flash; a reset mid-run means boot reconciliation recovers that checkpoint and the phone ingests it, so its totals are its **totals-so-far**. Phone-side ingest (`watch_ingest_queue.dart` `runFromWatchPayload`) stamps the key off the `run_store` header's `FLAG_FINISHED` being clear; presence is the whole signal, a finished run carries no key. Web: an amber "Incomplete" chip in the run-detail header meta beside the DNF / discipline chips, with the explanation on hover. Android + iOS (byte-identical twin): the matching badge in the `run_detail_screen.dart` AppBar title, explanation on long-press. The key is **public-safe** so the share page shows it too. Only the custom-watch sync path can produce it (Wear OS / Apple Watch never send an uncommitted run), hence N/A on the watches. |
| Elevation chart on run detail | ✓ | Partial | ✓ | ✗ | ✗ | |
| Lap splits table | ✓ | Partial | ✓ | ✗ | ✗ | Manually-marked laps from `runs.metadata.laps`. Web: `/runs/[id]` renders a Laps section (Lap / Distance / Time / derived pace) when the key is present — `apps/web/src/routes/runs/[id]/+page.svelte`. Android: `run_detail_screen.dart` `_buildLaps`. Per-lap deltas, 1-based index; section hidden when there are no laps. |
| Heart-rate zone breakdown | ✓ | Partial | ✓ | ✗ | ✗ | Web's `/runs/[id]` computes a **time-weighted** zone distribution from per-point `track[].bpm` samples — each sample's slice is half the gap to the previous + half to the next (capped at 30 s either side so a paused recording can't dominate). Falls back to sample-count weighting if any sample is missing a timestamp. Cutoffs come from the user's `hr_zones` settings bag, defaulting to 60/70/80/90/100 % of HR-max when unset. The panel surfaces avg / min / max BPM, the stacked bar, and a legend with both **time-in-zone** and **% of run** per band. When no per-point BPM is present, the card renders an honest empty state (with scalar `metadata.avg_bpm` when available). **Inputs**: Strava live OAuth + ZIP imports both populate `track[].bpm` (live via the streams API; ZIP via GPX `<extensions>/<gpxtpx:hr>` and TCX `<HeartRateBpm><Value>`). Mobile/watch BLE recorders are an additional input path tracked separately under "Live HR via device sensor". **Android**: `lib/hr_zones.dart` is a Dart port of the same time-weighted algorithm (with the 30 s gap cap and sample-count fallback); `run_detail_screen.dart` renders min/max/avg, the stacked bar, and the per-zone legend whenever `track[].bpm` is non-empty. The Android phone recorder now writes per-point BPM too — `RunRecorder.setHeartRate(bpm)` stamps each strap reading onto the next constructed waypoint, and `run_screen.dart` pipes the `BleHeartRate` stream into it. Out-of-range samples (< 30 or > 230 BPM) are dropped at the recorder so a flaky strap can't poison the saved track. Runs imported from Strava / FIT / TCX or pushed from a watch continue to populate `track[].bpm` directly. Cutoffs are pulled from the user's `hr_zones` settings bag (z1..z5 BPM upper bounds) when present; the panel falls back to the default 60/70/80/90/100 % HR-max bands when the bag is unset or malformed. **Coverage disclosure (web, 2026-09-03, `decisions.md § 1088`):** the panel now reads `metadata.hr_coverage` — written by the Wear recorder, which suppresses `avg_bpm` below 0.5 — so a suppressed average states the share the sensor covered and that it was too little for an average, a coverage of exactly `0` says the sensor delivered nothing, and a reported average carries the share it was taken over whenever that is under 100 %. Before this, all three rendered the same "No heart-rate data on this run" a phone run with no strap gets. |
| Interactive elevation + pace chart | ✓ | Partial | ✓ | ✗ | ✗ | Android: `run_detail_screen.dart` renders a tap/drag crosshair chart with pace-zone colouring (matches the roadmap's Phase 3 spec). Web equivalent on `/runs/[id]`. |
| Grade-adjusted pace (run detail) | ✓ | Partial | ✓ | N/A | N/A | Effort-equivalent flat pace over hilly terrain via the Minetti 2002 cost model. Shared TS↔Dart parity pair `grade_adjusted_pace.ts` ↔ `grade_adjusted_pace.dart` (ported from the on-watch Connect IQ field `apps/watch_garmin/source/GradeAdjustedPaceView.mc`; this web-first surface is what unblocks shipping that field per decisions §107). Walks the track in ≥5 m segments, applies `C(grade)/C(0)` to each segment's horizontal distance, GAP = total time / total equivalent-flat distance. Web: `/runs/[id]` key-stat after Avg Pace. Android + iOS (byte-identical twin): secondary stat on `run_detail_screen.dart`. Both surface the cell only when GAP differs from raw average pace by ≥2 s/km (a flat run's GAP is the raw pace), and return null without elevation or timestamps. 10-test mirror suite each side. Wear OS / Apple Watch are recording-only (the run-detail analysis surface lives on web/mobile). |
| Pacing halves + grade-adjusted splits (run detail) | ✓ | Partial | ✓ | N/A | N/A | The interpretation layer over the splits table. `pace_analysis.ts` cuts the track at its own **distance** midpoint (never a time midpoint — that lands short of halfway on any run that faded) and reports first-half vs second-half pace, a negative / even / positive verdict inside a 2 % band, and the same comparison on grade-adjusted effort by running each half through `gradeAdjustedPaceSecPerKm`. The same slicing gives every split its own grade-adjusted pace, rendered as an extra column so a climb doesn't read as a blow-up. Both grade-adjusted reads are gated on the ≥ 2 s/km margin the key-stat GAP cell already uses — on flat ground GAP is the raw pace and restating it is noise. Web: `/runs/[id]`, above and inside the splits table. Android + iOS (byte-identical twin): `run_detail_screen.dart` renders the pacing card above the splits list and a grade-adjusted lane on each split row. `pace_analysis.ts` ↔ `pace_analysis.dart` is a registered TS↔Dart parity pair, 13 mirror tests each; `gradeAdjustedSplitPaces` takes the split LENGTHS on the Dart side because mobile's `RunSplit` carries only a tick and a duration where web's `Split` carries `distance_m`. Web adds `tests-e2e/runs/pacing.spec.ts`; mobile has no e2e equivalent by design. Wear OS / Apple Watch are recording-only. |
| Age grade (run detail) | ✓ | Partial | ✓ | N/A | N/A | Scores a race against the world-standard time for the runner's age + sex on one 0–100 % scale. Was parkrun-only (scraped `metadata.age_grade`); now computed for **any** standard-distance race when DOB + sex + a standard distance + duration are known and that key is absent. Shared TS↔Dart parity pair `age_grade.ts` ↔ `age_grade.dart` against the embedded **USATF-MLDR 2025** factor tables (CC0; generated from `scripts/age_grade/`); 22 standard distances 1 mile…200 km incl. ultras, single-year age factors 5–99, M/F. `agePct = openStandard / (duration × ageFactor) × 100`; nearest-standard match within ±2 %, sex-gated (binary only — null for unset/non-binary). Web: `/runs/[id]` key-stat tile. Android + iOS (byte-identical twin): secondary-stat tile on `run_detail_screen.dart`. 12-test mirror suite each side. Watches are recording-only. See [decisions.md § 132](../architecture/decisions.md) + [features/age_grade.md](../features/age_grade.md). |
| Trace animation replay | ✓ | Partial | ✓ | ✗ | ✗ | Android: FAB on the run-detail map drives an `AnimationController` that advances a pointer along `run.track`; a moving marker renders on `LiveRunMap` via the existing `currentPosition` param. Duration fixed at 15 s regardless of run length. |
| Best-effort auto-detection (1k / 5k / 10k / HM / FM within a run) | ✓ | Partial | ✓ | ✗ | ✗ | Android: dashboard `_bestEffortCache` + run-detail best-efforts section. |
| PB comparison on same route | ✓ | Partial | ✓ | ✗ | ✗ | Android: run-detail "Personal best on \{route name\}" section shows PB, time delta, attempt ranking when the run is stamped with a `route_id`. |
| Edit run title / notes | ✓ | Partial | ✓ | ✗ | ✗ | Web: `updateRunMetadata(id, { title, notes })` on `/runs/[id]`. |
| Manual run entry | ✓ | Partial | ✓ | ✗ | ✗ | Web: `/runs/new` form (date-time, activity type, distance km, duration min + sec, notes) → `createManualRun` inserts with `source='app'` + `metadata.manual_entry=true`. |
| Delete run | ✓ | Partial | ✓ | ✗ | ✗ | |
| Bulk delete / multi-select | ✓ | Partial | ✓ | ✗ | ✗ | Web: `Select` button on the runs list enters selection mode (checkbox cards, sticky "N selected / Delete" bar). Confirm dialog guards the destructive write; `deleteRuns(ids)` runs per-item deletes in parallel so a single 4xx doesn't kill the batch. **Mobile's entry point is a long press, not a button** — on both History (`runs_screen.dart`) and Routes (`routes_screen.dart`) — so each list captions itself with ui_kit's `SelectionHint` under the filters while something is selectable, and drops it once the selection app bar takes over. A visible mobile equivalent of web's `Select` button is deliberately NOT added: the app bar already changes on entry, and a second permanent affordance would compete with the two FABs. |
| History sort (newest / oldest / longest / fastest) | ✓ | Partial | ✓ | ✗ | ✗ | Web: `<select>` in the runs list header. Client-side sort on the already-filtered list so the chosen key persists through filter flips. |
| Date filter | ✓ | Partial | ✓ | ✗ | ✗ | Web: chip row on `/history` matching Android's buckets (All time / Today / This week / Last 30 days / This year / Custom), composes with source + activity-type + sort. Web's custom is a from/to date pair; Android's custom opens Material's `showDateRangePicker` and snaps the bounds to start-of-day / end-of-day so the comparator includes both endpoints. Both clients persist the active filter (range / sort / activity / source / custom bounds) — web in localStorage under `runs_filters_v1`, Android in SharedPreferences under the same key. |
| Activity-type filter | ✓ | Partial | ✓ | ✗ | ✗ | |
| Source filter (All / Recorded / Strava / parkrun / HealthKit) | ✓ | Partial | ✓ | ✗ | ✗ | Android: source chip row on `runs_screen.dart` (All / Recorded / Watch / Strava / parkrun / HealthKit / Health Connect), composes with the activity-type and date filters. |
| Share run as GPX | ✓ | Partial | ✓ | ✗ | ✗ | Web: Download button on run detail → `toRunGpx` builds GPX 1.1 with per-point `<time>` so the trace round-trips into Strava / Garmin / Komoot as a real activity, not just a route. |
| Share run as image card | ✓ | Partial | ✓ | ✗ | ✗ | Shipped on web (`/runs/[id]` → "Share as image") — uses `html-to-image` to render a 1080×1080 gradient card with stats and falls back to a PNG download when the Web Share API is unavailable. Android has its own share-sheet integration. 🔸 gap vs web-canonical — watch surfaces to follow if ever needed. |
| Gear tracking (shoes / bikes + per-run tagging + mileage) | ✓ | Partial | ✓ | N/A | N/A | `gear` + `run_gear` tables (migrations `20260827_001` + `20260901_001`), a `gear_with_distance` view rolling up assigned-run distance, and an auto-tag-default trigger. **Web:** `/settings/gear` CRUD + `RunGearChips` on run detail and the public share page (`RunShareView`). **Post-create gear backfill** now ships on web too (`GearBackfillModal` + the `gear_backfill.ts` ↔ `gear_backfill.dart` parity pair, decisions §598): registering an item with a past `purchased_at` offers the matching runs since then and attaches the gear to the picked ones via the additive `addGearToRuns`, so a pair registered three weeks after purchase doesn't start its retirement target at zero. Android has had this since the gear screen shipped — closing that inversion of [decisions § 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive). **Android:** `GearScreen` (Settings → Gear) backed by an offline-first `LocalGearStore` (client-minted UUIDs, decisions §73) + post-create gear-backfill prompt; `RunGearChips` on `run_detail_screen` and now `public_run_screen` (the read-only chip mounts for non-owner + anon viewers). Both the owner-management read (`fetchMyGearWithDistance`) and the public chip (`fetchRunGear` → `public_run_gear` SECURITY DEFINER RPC, migration `20261126_001`, projects only public columns) are wired. Both web and Android also surface a **wear signal** on the gear list (the `gear_wear.ts` ↔ `gear_wear.dart` parity pair): the progress bar recolours and a Replace-soon / Past-replacement badge appears as accrued distance nears the replacement target. **Per-shoe wear-pattern logging** ships on both (migration `20270225_001`, `gear_wear_logs` table): the gear edit surface carries a dated wear-observation log (free-text note + optional outsole/midsole/upper/other area) — the qualitative companion to the distance bar. Web is full CRUD inline on `/settings/gear`; mobile adds/lists/deletes via the api online from the gear edit form (online-only, mirroring the gear-backfill sheet — wear logs sit outside the offline `LocalGearStore` pipeline). **Multi-pair "rotation" tagging** ships on both (migration `20270227_001`, `gear_rotations` + `gear_rotation_members`, owner-only RLS with the run_gear double-gate, decisions §183): named many-to-many gear groupings ("Daily trainers", "Race day"), distinct from and additive to the single `is_default` current pair (a rotation never changes auto-tagging). Web manages rotations + a member-checkbox modal + a group-by-rotation filter on `/settings/gear`; mobile reaches `GearRotationsScreen` from the gear AppBar, online-only via the api (outside `LocalGearStore`, like gear-backfill). **Rotation next-up ships on both** (`gear/rotation_pick.ts` ↔ `gear_rotation_pick.dart`, a registered parity pair, 18 mirror tests each): the rotation row names the member with the most life left and offers a one-tap handoff of the `is_default` star to it — the bridge between a rotation and the flag that actually auto-tags runs, so a runner rotating three pairs stops banking every kilometre on one. The pick ranks on share of each pair's own replacement target, sorts worn pairs last, drops retired ones, and measures an untracked pair against the rotation's median target; it is computed per gear kind because the star is scoped to `(owner, kind)` — web within the tab it is showing, mobile once per kind the rotation holds (`GearRotationsScreen`, over the new `ApiClient.setDefaultGear`). Watches don't surface gear (N/A). |
| Save history run as a reusable route | ✓ | Partial | ✓ | ✗ | ✗ | Web: "Save as route" icon on run detail prompts for a name, runs the track through Douglas-Peucker (10 m ε, port of `apps/mobile_android/lib/route_simplify.dart` → `apps/web/src/lib/routes/route_simplify.ts`), writes a `routes` row, and back-links `runs.route_id` to the new route. |
| Auto-link unmatched run to a saved route | ✓ | Partial | ✓ | N/A | N/A | Web: when a run isn't linked to a route but its track overlaps one of the user's saved routes (closest-endpoints + length-similarity check against the `routes_intersecting_track` RPC), `/runs/[id]` surfaces a one-tap "Link to *X*?" banner. Backed by the `routes.geom` GIST index from migration `20260607_001`. **Android**: stronger flavour — the auto-link runs on **save** rather than at the detail screen, so the row hits the DB with `route_id` already populated. `run_screen.dart` calls `ApiClient.fetchRoutesIntersectingTrack` after the recorder stops, feeds the candidates through `bestStrongRouteMatch` (same `startOffsetM + endOffsetM < 200` + `lengthRatio < 0.2` thresholds the web suggest path uses), and stamps `runs.route_id` before the local + cloud save. Pure-Dart helper in `lib/route_match.dart` (10 unit tests). Network failure is L4-best-effort and never blocks the save. Watches don't browse run history. |
| Weekly mileage summary | ✓ | Partial | ✓ | ✗ | ✗ | |
| Vertical metres climbed (vert) | ✓ | Partial | ✓ | ✗ | ✗ | Persona-hunt Round 3 finding Ultra #4 — pro / ultra runners track vert as a first-class metric alongside distance and pace. Web: `formatElevation` helper in `units.svelte.ts` (m / ft based on user pref, integer-rounded), `This Week Vert` stat card on `/dashboard`, per-row Vert chip on `/history` when `metadata.elevation_m > 0`. Android + iOS (byte-identical twin): `UnitFormat.elevation` mirror in `lib/preferences.dart`, vert secondary line on each `_PeriodStatCard` (Week / Month / All time) on the dashboard, vert suffix on the shared `RunListTile` subtitle (`widgets/run_list_tile.dart`), so every run list carries it. Same `metadata.elevation_m` canonical key on both platforms; positive-only aggregation; hidden on rows with no elevation signal so layouts don't widen unnecessarily. 4-test unit suite in `preferences_helpers_test.dart` pins the formatter. |
| Calendar heatmap of runs | ✓ | Partial | ✗ | ✗ | ✗ | Android + iOS: `_RunHeatmap` on the dashboard — 7 × 20-week grid, intensity by per-day run count, levels from `ChartPalette.ramp` (ui_kit) rather than a `colorScheme.primary` alpha ladder, whose composited 1.164 / 1.952 / 4.038 / 11.005:1 against the light card put its zero and one-run tiles under WCAG 1.4.11's 3:1 (decisions § 505); zero tiles are the calendar frame, so they carry the 3:1 line token as a hairline. **Web ✗ deliberately** — `/dashboard` carried this card until `3c86b8fc3` (2026-05-16) replaced it with the Training intensity card, because presence-per-day duplicated what the Mileage chart and the Streak stat already convey; `page.spec.ts` pins the replacement *and the old card's absence*. Round 11 mistook the still-present unmounted component for a §24 inversion and mounted it; the spec caught that, and the component is now deleted (decisions § 515). Re-adding it is a product decision to reopen, not drift to close. |
| Personal run-track heatmap (geographic) | ✓ | Partial | ✓ | ✗ | ✗ | Persona-hunt finding #53 (privacy / strava-migration) — a Strava-style "everywhere you've run" map of the user's OWN tracks, distinct from the public "where people run" community heatmap on `/routes/heatmap`. Web: `/runs/heatmap` (linked from the runs-list toolbar) downloads the owner's own track blobs via the owner-only `fetchTrackByPath`, aggregates them with the pure `lib/run_heatmap.ts` (grid-quantised weighted cells, `MAX_TRACKS` cap + bounded concurrency, weight clamped so a daily commute doesn't flatten the gradient), and renders a MapLibre `heatmap` layer weighted by cell hit-count with fitBounds. Owner-only path so no privacy-zone clip needed (the runner already knows where they live). 9 unit tests in `run_heatmap.test.ts`, e2e in `tests-e2e/runs/heatmap.spec.ts`. **Mobile (`run_heatmap_screen.dart`, byte-identical twin):** sources the **union of local + synced tracks** — `LocalRunStore.runs` carry their track inline (no download, no session; signed-out recording is supported, and reading only the server told a signed-out runner with local tracks "No mapped runs yet" — issue #239), then `api.getRuns` → owner-only `fetchTrackByPath` for whatever isn't already local (same `_maxTracks` cap + bounded concurrency), skipped entirely when signed out; the empty state then offers sign-in instead of claiming the runner has never run. Aggregated by the pure `run_heatmap.dart` (Dart twin of `run_heatmap.ts` — `buildHeatCells`/`heatBounds`, 9 mirror tests). flutter_map has no GPU heatmap layer, so heat cells render as low-opacity `CircleLayer` dots that crossfade into the actual track `PolylineLayer` as you zoom past street level (the same fade band as web). Reached from the labelled "Run heatmap" button in the Routes screen's Discover strip. |
| Run streaks (current + best, daily) | ✓ | Partial | ✓ | ✗ | ✗ | Strava-style grace rule — missing today doesn't break the streak if yesterday is intact. Pure compute in `lib/streaks.ts` / `lib/streaks.dart`, twinned. Web: 5th `.stat-card` on `/dashboard`, flame-coloured when active. Android: "Streak" `_StreakRow` between All Time and Last 20 Weeks on `dashboard_screen.dart`. All-time figures on both platforms come from the `run_streaks_for_user` aggregate (migration `20270501_001`) through the `streak_card.ts` ↔ `streak_card.dart` pair — a missing answer suppresses the best/"all-time best" claim rather than presenting the windowed/resident figure as truth, and mobile folds the row with the local compute so an unsynced run is never walked back (decisions § 471 / § 475). Current-streak display stays derived live from `runs.started_at`. |
| Training intensity (time in HR zone) | ✓ | Partial | ✓ | ✗ | ✗ | Per-zone seconds breakdown across a rolling window, classified by `runs.metadata.avg_bpm` against the user's `hr_zones` (z1..z5 universal-bag map). Pure helper in `apps/web/src/routes/dashboard/+page.svelte` (inline) ↔ `apps/mobile_android/lib/run_intensity.dart`. Self-hides when no zones are configured or no HR-tracked runs land in the window. Web: 30/90/365 day toggle. Android: `widgets/intensity_card.dart` defaults to a 30-day window between Readiness and Training Load on the dashboard. |
| Personal records table (5k / 10k / HM / FM) | ✓ | Partial | ✓ | ✗ | ✗ | |
| Dashboard first-run state (runless account) | ✗ | ✗ | ✓ | N/A | N/A | Web (#905, 2026-09-16): an account with no runs all-time and no gym sessions gets `DashboardFirstRun.svelte` in place of the entire derived-metric block — one heading, one primary action (`/runs/new`), an import alternative (`/settings/integrations`), two quiet hints. Gated on `isNewAccount` in `routes/dashboard/+page.svelte`; the plan hero and upcoming-event card still render above it, since onboarding creates a plan before the first run exists. Not the same thing as the per-modality self-hiding the gym / nutrition / intensity cards already do (`multi_modal.md` § Home rule 4) — this is the case where all thirteen self-hide at once. **Mobile ✗ deliberately for now:** `dashboard_screen.dart` still composes its stack for a runless account; web-first per decisions § 24, tracked on #905. e2e `tests-e2e/dashboard/dashboard-first-run.spec.ts` pins both directions of the branch. |
| Progressive-disclosure level (`disclosure_level`, derived from the onboarding goal) | Partial | Partial | ✓ | N/A | N/A | Web (#905 workstream 1, [decisions § 1658](../architecture/decisions.md)): `disclosureLevel(primaryGoal, runCount)` → `simple` / `standard` / `full` is the FIRST read anything ever made of the `primary_goal` the setup wizard has stored since § 78 — until now its only apparent consumer, `planPresetForGoal`, was handed the goal as a `?goal=` URL parameter rather than out of the bag, on both platforms. The level is the max of a goal floor and a run-count floor (10 runs → `standard`, 50 → `full`); an absent or unrecognised `disclosure_level` derives rather than defaulting, so an account that predates the key is never shown less than its own history justifies. Overridable at Settings → Display (`/settings/display`, "How much detail"), where **Automatic** writes `null` and deletes the key. One consumer today: `/dashboard` at `simple` folds the VO₂ max / CTL / ATL / TSB card and the fitness-fatigue-form curve into a named `<details>` that remembers per account whether it was opened (`dashboard_training_load_expanded`, universal bag); at `standard` / `full` the page is unchanged, and nothing is removed at any level. **Mobile is Partial by construction**: the pure helper twin `apps/mobile_android/lib/disclosure.dart` (+ the byte-identical iOS twin) is a registered [parity pair](../architecture/parity_pairs.md) with a matching 10-case suite, but there is no mobile control and no mobile consumer — web is the canonical surface per [decisions § 24](../architecture/decisions.md), and the Dart half exists so the level cannot diverge on the day mobile grows one. |
| Derived-metric definitions (`<MetricLabel>` + registry + guard) | ✓ | Partial | ✓ | N/A | N/A | Web (#902 § 1 + § 2, decisions § 1639): every derived-metric name — VO₂ max, CTL / ATL / TSB, VDOT, 1RM, RPE, age grade, vert, TRIMP, Riegel, KOM/QOM, distance banked, the plan phases — renders through `<MetricLabel>`, a toggletip a tap or a keypress opens, from one registry that `metric_label_guard.test.ts` reads to fail a bare term or a `title=` definition. Mobile shipped 2026-09-17 (decisions § 1655): `lib/metrics.dart` registers ELEVEN of those — the five the fitness card already explained plus RPE, 1RM, age grade, vert, TRIMP and Riegel, carrying web's own sentences — and `widgets/metric_label.dart` renders them as `MetricStat` / `MetricSentence` / `MetricInfoButton`, a tap-to-open `AlertDialog` in the `info_tip.dart` idiom, adopted across the fitness card, run detail, dashboard, the roadbook, the training-load + race-predictor cards, the plan surfaces and the six gym surfaces. `test/metric_label_guard_test.dart` scans `lib/screens` + `lib/widgets` with the registry's own terms. **Three of web's fourteen have no mobile copy that names them** — measured against `app_en.arb`, no string carries KOM/QOM, "distance banked" or "training phases" — so the Dart registry is eleven entries, not fourteen, and adding one of those surfaces to mobile means adding its entry with it. Watches show no derived-metric labels. |
| Dashboard + plan legibility (readiness breakdown name, zone names, distance title, empty week, lift volume unit, under-plan copy) | ✓ | Partial | ✓ | N/A | N/A | #902 § 1.1 / 1.3–1.7. **Both:** the readiness breakdown names the form input "Training balance", not the TSB stat's name, through a `kind` on the `readiness` TS↔Dart parity pair that each UI localises; the distance chart is titled "Distance"; the under-running plan flag says "So far this week you've run {done} of the {planned} planned" instead of a percentage that read as praise; the training-intensity legend names each zone ("Z1 (recovery)") instead of a bare digit. **Web only, because mobile never had the defect:** the lift volume unit (mobile's recent-lifts card already formats it), and words instead of "0 m" / "--" for an empty week — mobile's week card pairs its distance with a run count ("0 runs"), hides vert at zero, and has no pace tile. |
| Achievements / badges | ✓ | Partial | ✓ | N/A | N/A | Badge catalogue defined as code (`apps/web/src/lib/social/badges.ts` ↔ `apps/mobile_android/lib/badges.dart`, the guided-runs pattern) — distance (single-run + lifetime), streak, PR-count, and plan-finisher families, each bronze→platinum. Awards persisted in the `achievements` table (migration `20270208_001`), derived by the SECURITY DEFINER `award_achievements_for_user` full-rebuild trigger mirroring `personal_records`; thresholds duplicated into the SQL function and pinned by `achievements_test.sql` + the `badges.test.ts` ↔ `badges_test.dart` parity pair (15 cases each). Web surfaces: the **Achievements** tab on `/u/[id]` (owner sees private + a per-badge public toggle, non-owner sees only public via RLS), a recent-badges strip in the `/social` feed (`fetchFollowingBadgeAwards`), and the public `/share/badge/[id]` page + `/og/badge/[id].png` card (+ the `share-badge` Lambda). **Mobile (read surface, 2026-06-20):** `BadgeGrid` mounted as the **Achievements** tab on `ProfileScreen` (owner sees private + public via RLS, non-owner public only — no owner visibility toggle / share controls, which stay web-only) + a recent-badge `ActionChip` strip on `FeedScreen` (`fetchFollowingBadgeAwards`, an L4 auxiliary load that degrades to no strip on failure). api_client: `fetchUserBadges` / `fetchMyBadges` / `fetchFollowingBadgeAwards` → the `BadgeAwardEntry` core_models view. Default `is_public = true`, owner-only visibility flip (web). See `decisions.md § 164` + `features/achievements.md`. |
| Multi-goal dashboard (distance / time / pace / count) | ✓ | Partial | ✓ | ✗ | ✗ | Web: dashboard Goals section with inline editor; `lib/goals.ts` ports the Android `RunGoal` model end-to-end — distance / time / **avg pace** / run count, week or month period. Pace targets are distance-weighted, exclude `activity_type === 'cycle'` (the promoted column, `20261207_001` — web `goals.ts` reads the column post-Tier-2), and use the lower-is-better progress fraction (`target / current`, clamped to 1.0) so a sub-target avg fills the bar. Editor accepts `mm:ss/km` or `mm:ss/mi` per the user's display unit; canonical storage is seconds-per-km. Stored in localStorage. Scalar `weekly_mileage_goal_m` from the settings bag still drives the separate "Weekly goal" progress card above. |
| Week / month / year mileage toggle | ✓ | Partial | ✓ | ✗ | ✗ | Web: bar chart with a Week/Month/Year SegmentedButton on `/dashboard`. Android: `widgets/mileage_trend_card.dart` mounts above the Last 20 Weeks heatmap with the same toggle; pure aggregation in `lib/mileage_trend.dart` groups runs by Monday-anchored ISO week / calendar month / calendar year (capped to the most-recent 12 buckets). Bar values honour the user's `DistanceUnit` at render time. |
| Browsable period summary (prev / next + share) | ✓ | Partial | ✓ | ✗ | ✗ | Web: clicking the This-Week stat card on the dashboard opens a `PeriodSummary` modal; prev/next arrows shift by week or month, type toggle switches period, share button uses Web Share API with clipboard fallback. The same component also backs the standalone `/dashboard/period/[type]/[date]` route for deep links. |
| This-week activity strip (calendar-week 7-day ribbon) | ✓ | Partial | ✓ | ✗ | ✗ | Day-by-day shape behind the This-Week stat. Pure `current_week.ts` ↔ `current_week.dart` parity pair (`currentWeek` buckets activities onto the seven days of the REAL calendar week containing today, honours the `week_start` pref, local-day bucketing). Web: `ThisWeekStrip.svelte` below the stat grid on `/dashboard`, busiest-day-scaled bars, today highlight + future dimming, follows the source filter. Android + iOS (byte-identical twin): `widgets/this_week_strip.dart` in a card below the period-stat row on `dashboard_screen.dart`, honours `DistanceUnit`. Distinct from the plan-anchored `CurrentWeekStrip` on `/plans/[id]`. 10 helper mirror tests each + web Playwright + 6 mobile widget tests each. |
| Gym workout module (log / list / detail + PRs) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4 (in progress).** Web: `/gym` list + PR badges + `GymEditor` composer modal + `/gym/[id]` detail with per-exercise PR chips + edit/delete (decisions §63). Android/iOS (byte-identical twin): `screens/gym_screen.dart` (list + PR badges + log), `widgets/gym_compose_sheet.dart` (free-text exercise + history autocomplete + inline reps/weight/RPE sets), `screens/gym_detail_screen.dart` (PR chips + edit/delete) — all offline-first through `LocalGymStore` (client-minted UUIDs, sets stored inline; decisions §122), hydrated by `api_client.fetchGymWorkoutsWithSets`. PR engine twin `gym_prs.ts` ↔ `gym_prs.dart`. Weight in kg. **Mobile nav entry landed (G5):** the bottom-nav Run→Log reshape makes `GymScreen` reachable via the centre Log sheet ("Log lift") and the Home today's-lift card. **`/gym/records`** (per-exercise current-bests — est. 1RM / heaviest / top volume + last-performed + session count) is now served **web-side by the `gym_exercise_records` RPC** (`fetchExerciseRecords`, migration `20261224_001`; all-time bests can't be windowed, so the aggregation is server-side, mirroring run PRs) → each card links to **`/gym/exercise?name=`** (per-exercise progression over time — `exercise_history.ts`; est.-1RM delta vs first session + session-by-session list with new-e1RM PR badges). **Mobile keeps its own client-side records screen** (`gym_records_screen.dart` over `exercise_records.dart`): the two compute the same bests via different paths (web RPC ↔ mobile Dart), no longer a byte-identical pair, each pinned by its own tests. `exercise_history` stays a byte-identical pair (`exercise_history.ts`↔`.dart`), backing the progression screen + the per-exercise "vs last time" hint (`previousExerciseSession`) on `gym_detail_screen.dart`. |
| Nutrition module (rings / meal log / food search / water) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4 (in progress).** Web: `/nutrition` (Mifflin-St Jeor macro rings vs targets — hidden when body metrics absent, anti-clutter — with a calories-remaining/over-budget chip + ceiling-aware ring state, meal-slot daily view, water tracker with a bodyweight + exercise-aware daily hydration goal, 7-day calorie trend with a goal reference line + week over/under-goal summary) + `/nutrition/log` (Open Food Facts search → confirm-portion modal → log, with manual macro entry as the no-match fallback). Data layer in `core/data.ts`; `food_search.ts` (injectable-fetcher seam, parse/scale pure-tested); `nutrition_targets.ts`↔`.dart` parity pair (BMR); `nutrition_totals.ts` (day aggregation). Body metrics (height on `user_profiles`, weight in `body_metrics`, migration `20261216_001`) + activity-level/goal prefs entered in Settings → Preferences under the Art 9 health-consent gate. Nutrition sidebar item always present (§63 amendment ungated web). e2e `tests-e2e/nutrition/nutrition.spec.ts`. Android/iOS (byte-identical twin): `screens/nutrition_screen.dart` (rings/meal-slots/water/trend, offline-first via `LocalFoodStore`), `widgets/nutrition_log_sheet.dart` (search → confirm portion; manual fallback), `food_search.dart` + `nutrition_totals.dart` (mirror web). **Budget/hydration/weekly-trend + dynamic-TDEE now mirror on mobile (2026-06-10):** the `nutrition_budget.ts`↔`.dart`, `hydration.ts`↔`.dart`, and `nutrition_week.ts`↔`.dart` parity pairs drive the calories-remaining/over-budget chip + ceiling-aware ring recolour, the bodyweight+exercise-aware water goal readout/chip, and the 7-day trend goal line + over/under-goal summary; today's runs + gym sessions feed `exerciseCaloriesForDay` into `loadNutritionTargets` for a base+exercise calorie goal (ARB activity labels relabelled to non-exercise baseline in the same change). **Both former mobile gaps closed (G5):** `NutritionScreen` is reachable via the centre Log sheet ("Log food") + the Home nutrition rings card; and the **Settings → Body metrics** entry (`settings_body_metrics_screen.dart`, Art 9 consent-gated height/weight + activity/goal prefs) now feeds real targets through the shared `loadNutritionTargets` resolver (height/sex from profile, weight from `body_metrics`, age from the DOB pref, activity/goal from the bag). **Barcode scan (camera) shipped on mobile (2026-06-20, mobile-only — web has no camera-record surface):** a Scan-barcode action in `nutrition_log_sheet.dart` scans an EAN/UPC via `mobile_scanner` → OFF product-by-barcode lookup (`lookupBarcode`/`parseOffProduct` in the `food_search` parity pair) → confirm-portion; L4 fallback to search / manual on no-match / scan failure; camera-permission denial handled; iOS `NSCameraUsageDescription` + Android `CAMERA` added (decisions §180). **Second food source added (2026-06-20, web + mobile):** USDA FoodData Central joins Open Food Facts behind the `food_search` TS↔Dart parity pair — `searchFoodSources` merges both (OFF first, deduped by case-insensitive name+brand), each result labelled by a source tag/chip; USDA gated fail-closed on `PUBLIC_USDA_FDC_API_KEY` (web) / `USDA_FDC_API_KEY` (mobile dotenv), so unset → OFF-only, and a partial-failure of one source degrades to the other. kcal keyed on USDA nutrient number 208 (never kJ). See [decisions.md § 188](../architecture/decisions.md). |
| Per-meal detail (slot items + macro breakdown + 7-day trend) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4 (2026-06-12).** Web canonical: `/nutrition/[date]/[slot]` shows one meal slot's items + macro breakdown + a trailing 7-day calorie trend for that slot, linked from each day-view meal-group header. Pure shaping (`entriesForSlotOnDay` / `slotCalorieTrend`) in `nutrition/meal_detail.ts` (vitest); Playwright happy-path in `tests-e2e/nutrition/nutrition.spec.ts`. Mobile (byte-identical twin): `screens/nutrition_meal_detail_screen.dart` mirrors it from the offline-first `LocalFoodStore`, reached by tapping a meal-group header on `nutrition_screen.dart`. Web-only `meal_detail.ts` (no Dart twin — the mobile screen reimplements the shaping against its local store). |
| Extended-nutrient day roll-up (fibre / sodium / sat fat / sugar / cholesterol) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4 (web 2026-08-15).** The five nullable `food_log` nutrients (issue #492, migration `20270426_001`) were captured per item and surfaced only on the portion preview + `/nutrition/[date]/[slot]`; nothing rolled them up to a day or measured them against anything. **Web ✓:** a self-hiding **Nutrients** section on `/nutrition` (rows, not rings — the four headline macros keep the rings) over the web-only `nutrition/extended_nutrients.ts`. The load-bearing part is **coverage**: both food sources carry these fields unevenly, so a null-counts-as-zero sum would be fail-open — eight items of which two report sodium sum to a number that reads as the day and sits under a ceiling it may have blown. A total therefore carries how many entries reported it, and only the **monotone** claims are offered: `exceeded` (ceiling past target) and `reached` (floor at/past target) hold under partial coverage because the reported items alone already clear the line, while `remaining` ("600 mg left") is **withheld** and the row is marked "at least"; a nutrient nothing reported is omitted, never a zero row. Reference intakes: fibre floor 14 g/1000 kcal of the **base** goal (a long-run day's extra carbohydrate is deliberately low-residue — scaling by workout calories would prescribe 50 g+ of fibre on exactly the wrong day); sodium ceiling 2300 mg rising 6 mg per logged exercise minute on the same "base + exercise" model as the calorie (§134) and water goals, capped at `hydration.ts`'s `MAX_EXERCISE_MINUTES`, and the one target that resolves with **no body metrics**; saturated fat 10 % of the full goal. Sugar + cholesterol are deliberately **ungraded** (the stored sugar field is *total* sugars while every published ceiling is about free/added sugars; the 300 mg cholesterol cap left the Dietary Guidelines in 2015). `nutrition_targets` / `nutrition_budget` untouched. 22 unit tests + e2e `tests-e2e/nutrition/extended-nutrients.spec.ts`. **Android + iOS (2026-08-18):** mirrored on `nutrition_screen.dart` as the same self-hiding Nutrients section, over the new `extended_nutrients.dart` — now a registered TS↔Dart parity pair (both registries per §604), 22 mirror tests each, carrying the coverage contract intact: the "at least" prefix, the withheld `remaining`, and — because a withheld `remaining` is not the same claim as no target — no untargeted chip in its place. Two idiomatic shape differences, neither a divergence: TS's structural `keyof` row index reads as a nominal `ExtendedNutrientRow.valueOf(column)`, and web's dotted `MessageKey` as mobile's camelCase ARB identifier resolved at the render layer (the `badges` convention — key strings are outside the lockstep). |
| Nutrition diary day navigation (review + back-fill a past day) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4 (web 2026-08-13, [decisions § 591](../architecture/decisions.md)).** `/nutrition` was today-only — every window came from `new Date()` — so a forgotten yesterday could never be corrected and no past day could be reviewed, even though `/nutrition/[date]/[slot]` was already date-parameterised and every write path already took a `started_at`. **Web ✓:** a `?date=` day stepper above the rings (prev / label / next, next disabled on today, a Today button that returns to the bare `/nutrition` URL); the viewed day drives the food window, that day's runs + gym sessions behind the exercise add-on, the seven trend buckets, the water counter's storage key and the per-meal deep link; the composer, saved meals and recipes all stamp the viewed day, resolved at **save** time so a composer left open across midnight cannot file an entry where its host can no longer show it. A malformed or future `?date=` resolves to today (a future `started_at` would sit outside every "today" window the app has). Pure `nutrition/diary_day.ts` (30 tests, incl. a `TZ=America/New_York` DST file pinning the 25-hour fall-back day § 589 records). e2e `tests-e2e/nutrition/diary-date-navigation.spec.ts`. **Android + iOS (2026-08-18):** `diary_day.ts` ↔ `diary_day.dart` is now a **registered** parity pair (both registries, 26 mirror tests each — the web file's 30 counts its separate DST file, which the Dart suite does not have). `nutrition_screen.dart` holds the viewed day as a local `YYYY-MM-DD` string and derives from it the food window, the exercise add-on's day, the seven trend buckets, the water `SharedPreferences` key and the `NutritionMealDetailScreen` day; a stepper above the rings walks it (prev / label / next disabled on today / a Today reset / an off-today back-fill hint). All three write paths the screen owns — the `nutrition_log_sheet` composer (via a new `diaryDate`), a logged meal template and a logged recipe — stamp `entryTimestampFor(viewedDay, now)` at **save** time. The day is only ever stepped through `DateTime(y, m, d + n)`, so a 23- or 25-hour DST day cannot repeat or skip one, and nothing writes a naive local string (`LocalFoodStore.createLocal` converts to UTC at its boundary, `wall_clock_writes_test.dart` green). Web's `DIARY_DATE_PARAM` has no mobile analogue (no URL). |
| Meal templates (save a meal once, log it with one tap) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4, Nutrition mid tier (2026-06-20, migration `20270219_001`).** `meal_templates` + `meal_template_items` (owner-scoped RLS, deny-by-default, cascade from `auth.users`; items mirror the `food_log` row shape so a template instantiates straight into entries). Web canonical: a "Save as meal" action on `/nutrition` promotes the day's logged entries into a named template (`templateFromEntries`), a self-hiding Meal-templates section (mirrors the gym Routines section) logs the whole meal with one tap (`entriesFromTemplate` → `food_log` rows) or deletes it behind `ConfirmDialog`. Data helpers in `core/data.ts`. The `meal_template.ts`↔`.dart` parity pair holds the pure plan↔log shaping (22 mirror tests each). Mobile (byte-identical twin): `LocalMealTemplateStore` (offline-first, items inline, client-minted UUID = server id) + the `nutrition_screen.dart` "Save as meal" action + templates section. In the Art 20 DSAR export (Go `dataexport` + `export-data` EF, items nested). e2e `tests-e2e/nutrition/meal-templates.spec.ts`; pgtap `meal_templates_rls_test.sql`; mobile `local_meal_template_store_test.dart` + `meal_template_test.dart`. |
| Recipe builder (N ingredients → one summed logged meal) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4, Nutrition mid tier (2026-06-20, migration `20270221_001`).** `recipes` + `recipe_ingredients` (owner-scoped RLS, deny-by-default, cascade from `auth.users`). Sibling of meal templates, but where a template logs each item as its own entry, a recipe **sums** its ingredients (each × `quantity`) into ONE `food_log` entry, divided by a `servings` count. Web canonical: a "Save as recipe" action on `/nutrition` (with a servings field) promotes the day's logged entries (`recipeFromEntries`), a self-hiding Recipes section logs one summed serving with one tap (`logInputFromRecipe`) or deletes behind `ConfirmDialog`. The `recipe.ts`↔`.dart` parity pair holds the sum-and-log shaping — `recipeFromEntries` + `sumRecipe` (missing macro contributes 0, never poisons the sum; rounds to 0.1) + `logInputFromRecipe` (25 mirror tests each). Mobile (byte-identical twin): `LocalRecipeStore` (offline-first, ingredients inline) + the `nutrition_screen.dart` action + Recipes section. In the Art 20 DSAR export (ingredients nested). e2e `tests-e2e/nutrition/recipes.spec.ts`; pgtap `recipes_rls_test.sql`; mobile `local_recipe_store_test.dart` + `recipe_test.dart`. |
| Unified multi-modal History timeline + kind chips | Partial | Partial | Partial | ✗ | ✗ | **Phase 4 (in progress).** Web: `/history` is the pure unified timeline (the run-list management surface is its own dedicated `/runs` page, un-redirected — §63 amendment); All/Runs/Lifts/Meals chips + a day-grouped timeline over the `activities` view (`fetchActivities`) once a second modality has data (data-gated, no flag); **every chip — incl. Runs — is the timeline**, and each single-modality tab has a `View all` link → that modality's page (`/runs`, `/gym`, `/nutrition`). Rows link to `/runs/[id]` + `/gym/[id]` (meal rows read-only in the timeline; per-meal detail lives on the dedicated `/nutrition/[date]/[slot]` route reached from the nutrition day view). **Mobile (byte-identical twin):** `runs_screen.dart` hosts the same chips + a day-grouped timeline (`widgets/activity_timeline_list.dart`), assembled from the **local stores** via `lib/local_activities.dart` (offline-first, not `fetchActivities`); History hydrates the gym + food stores on mount. Same per-tab `View all` (Runs → `RunsScreen` without a `gymStore` = the run list; Lifts → `GymScreen`; Meals → `NutritionScreen`). Run rows open run-detail (local lookup, else `fetchRunById`), lift rows open `GymDetailScreen`, meals read-only. The History add FAB mirrors web's per-tab Log action (converged 2026-06-08): the All view opens a run/lift/meal picker → that modality's one-shot composer; a single-modality tab adds straight into it (Add run / Log lift / Log food). Separately, the home Log FAB navigates to each modality's dwell-in surface (run → the keep-alive recorder page, lift → the Fitness hub's Gym tab, food → its Nutrition tab — one instance of each, decisions § 1654), the surface's own composer one tap away. `Partial` everywhere = runs + lifts shipped; meal rows are summary-only (no per-meal detail route). e2e `tests-e2e/gym/multimodal_home_history.spec.ts` (web); mobile `activity_timeline_test.dart` / `activity_timeline_list_test.dart` / `runs_screen_timeline_test.dart` / `local_activities_test.dart`. |
| Home today-modality cards (today's lift + nutrition rings) | ✓ | Partial | ✓ | ✗ | ✗ | **Phase 4 (in progress).** Web `/dashboard`: a Today's-lift card, a Recent-lifts trend card, a first-run "log a lift" footer, and a **Today's-nutrition rings card** (`NutritionRingsCard.svelte` — four macro rings vs targets, links to `/nutrition`; self-hides when no food logged today, renders unfilled when body metrics absent per the Art 9 health-consent gate; reuses the `/nutrition` `computeNutritionTargets` + dynamic-TDEE `exerciseCaloriesForDay` math so the rings agree across surfaces) — all gated on data presence (anti-clutter: no empty card / zeroed stat; data-gated, no flag — §63 amendment). e2e `tests-e2e/dashboard/nutrition-rings.spec.ts`. Mobile (G5, byte-identical twin): `dashboard_screen.dart` composes a self-hiding today's-lift card (`widgets/gym_summary_card.dart`) + today's nutrition rings card (`widgets/nutrition_rings_card.dart`) + a **recent-lifts trend** card (`widgets/recent_lifts_card.dart`, below the load chart, self-hiding when the gym store is empty) — each omitted when that modality has no data, 2-up on phones ≥360 dp. |
| Lift sessions in fitness/fatigue/form curve | ✓ | Partial | ✓ | ✗ | ✗ | **Phase 4 (in progress).** Web `/dashboard` folds logged gym sessions into `computeTrainingLoadSeries` via the pure tested `gym/lift_load.ts` (`liftsFromSetHistory`), so CTL/ATL/TSB + recovery advice + the readiness ring reflect lift fatigue. `source='lift'` keeps run-only readiness recoverable; the chart shows a "gym sessions included" hint. **Mobile (byte-identical twin):** `dashboard_screen.dart` feeds the gym store through the Dart twin `lift_load.dart` into `computeTrainingLoadSeries`; `TrainingLoadChart` shows the same hint. A pure runner's empty gym store leaves the curve run-only. |
| Exercise catalogue (`gym_sets.exercise_id`, additive) | ✓ | Partial | ✓ | N/A | N/A | migration `20270222_001`, decisions §176. Gym-mid "exercise database" bullet, built **additively**: a new `exercises` catalogue (seeded globals, `author_id` null, read-only for all; + owner-created customs) and a **nullable** `gym_sets.exercise_id` FK *alongside* free-text `exercise_name` (`on delete set null`; PR computation stays keyed on the normalised name). **Web ✓:** `fetchExerciseCatalogue` + `createCustomExercise` in `core/data.ts`; `GymEditor` merges catalogue names into the autocomplete + binds `exercise_id` on a normalised-name match; `Exercise`/`ExerciseCategory` overlays in `types.ts` (both in `check_constraint_unions.mjs`). **Android + iOS:** `gym_compose_sheet.dart` catalogue merge + bind, `gym_screen.dart` best-effort fetch; offline path logs free-text (`exercise_id` null), byte-identical twin. Owner-customs-only DSAR export (Deno + Go worker, `author_id`-filtered). pgtap `exercises_rls_test.sql`, Playwright `gym/gym.spec.ts`, mobile `gym_compose_sheet_test.dart`. **Watch N/A.** **Browse/picker UI ✓ (2026-06-20, web + mobile twin):** a "Browse catalogue" affordance per exercise block opens `ExerciseCataloguePicker.svelte` / `ExerciseCataloguePickerScreen` — searchable, `category`-filterable list of globals + customs; picking fills the block name (binds via the same normalised-key path, never directly), and a no-match search offers create-custom (`createCustomExercise`, merged in locally). Self-hides offline / signed-out. **The phone's third state landed 2026-09-07** ([decisions § 1513](../architecture/decisions.md), [§ 1514](../architecture/decisions.md)): `gym_screen.dart` used to catch the catalogue read, `debugPrint`, and leave `_catalogue` at `const []` — the one state in which every typed name looks free, so the picker offered to create a name the catalogue already held. It now carries `catalogueUnavailable` (true until a read ANSWERS) through the composer to the picker, which renders `gymCatalogueUnavailable` and refuses the create while unknown, keeps the browse affordance reachable rather than letting the feature vanish on a transient error, and keeps whatever rows were last known — matching web's `catalogueUnavailable` exactly. In the same change the composer stopped snapshotting its catalogue prop in `initState` (a prop the host fills asynchronously) and stopped merging a created custom by `id`, a test that cannot see a shadow: it now reduces prop ∪ created customs through the registered `dedupeShadowedExercises` pair, which is generic on both platforms for exactly this second call site. |
| Logged-set RPE + set-type (`gym_sets.set_type`) | ✓ | Partial | ✓ | N/A | N/A | Gym-mid "RPE / set-type metadata" bullet, migration `20270228_001`, decisions §189. RPE was already a `gym_sets` column (`20261204_001`); this added `gym_sets.set_type` (NOT NULL, default `working`, CHECK over the `gym_routine_sets` vocabulary: warmup / working / dropset / amrap / failure / backoff). **Web ✓:** `GymEditor` per-set type `<select>` + RPE input, `GymSet`/`GymSetInput` carry `set_type: GymSetType` (`gym_sets.set_type ↔ GymSetType` in `check_constraint_unions.mjs`), `/gym/[id]` chips non-working roles. **Android + iOS:** `gym_compose_sheet.dart` per-set type dropdown (`GymSetInput.setType`, `GymSet.setType`, `LocalGymStore` inline maps), `gym_detail_screen.dart` chips non-working roles; the gym session runner threads each step's `set_type` onto the logged set; byte-identical twin. DSAR rides the existing `gym_sets(*)` wildcard. pgtap `gym_sets_set_type_test.sql`, Playwright `gym/set_type.spec.ts`, mobile `gym_compose_sheet_test.dart`. **Watch N/A.** |
| AI Coach context: recent lifts + 7-day nutrition | ✗ | ✗ | ✓ | N/A | N/A | **Phase 4 (in progress).** `coach/context.ts` projects a bounded `recent_lifts` array + a `nutrition_7d` daily-average rollup (pure `summarizeRecentLifts` / `summarizeNutrition`, unit-tested). Nutrition gated on the Art 9 health-consent (like DOB/HR); lifts ungated. Both self-hide when nothing is logged. Mobile coach just renders the richer answers (no mobile work). |
| Multi-modal nav: Fitness hub (mobile) + Routes under the run surface | ✓ | Partial | ✓ | N/A | N/A | **Phase 4, Fitness-hub redesign (decisions §139).** **Mobile** (`home_screen.dart`): bottom nav is `Home · Fitness · [+]Log · Social · You`. **Fitness** (`fitness_hub_screen.dart`) is a modality hub — sub-tabs All / Runs / Gym / Nutrition — where **All absorbs the former standalone History tab**; Runs is the run list + the relocated Routes entry; Gym/Nutrition host their screens — and since 2026-09-17 ([decisions § 1654](../architecture/decisions.md)) they host the app's **only** instances of them: `Log lift` / `Log food` select this hub's Gym / Nutrition tab through a shared `ValueNotifier<FitnessTab>` instead of landing on a second keep-alive copy with its own diary day, filters and routine store. Settings folds into a **You** tab (`you_screen.dart`); Coach keeps no nav slot and is a pinned "Ask your coach" entry on Home. The centre **Log** FAB, the keep-alive Run recorder page and the `prefsKeepRunPrimary` protect-the-runner toggle are unchanged; the foreground-service recording survives navigating away (code-reviewer-verified). **Web** (no bottom nav): Routes relocated off the top-level sidebar to under the run surface — `RunSurfaceTabs` strip (`Runs · Routes`) on `/runs` + `/routes`, the `/routes` URL preserved. Training-Plans nesting deferred. |
| Body metrics + nutrition prefs entry (Art 9 consent-gated) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4.** Web: the demographics card on `/settings/body` (height on `user_profiles`, weight in `body_metrics`, consent via `grant_health_data_consent()`; activity-level/goal auto-save). Mobile (G5, byte-identical twin): `screens/settings_body_metrics_screen.dart` — an explicit Art 9 consent toggle gates height + weight (height → `user_profiles.height_cm`, weight → `body_metrics`; withdrawal nulls consent + height, clears the weight series, clears the Art 9 `date_of_birth` prefs mirror and leaves the `user_profiles` age record alone, which the RPC no longer nulls — [decisions § 718](../architecture/decisions.md) + [§ 721](../architecture/decisions.md)), while activity-level + goal auto-save to the universal bag. Reached from a tile in `settings_preferences_screen.dart`, which owns the DOB row itself (the age-record write is ungated; only its mirror follows consent). api_client adds `grantHealthDataConsent` / `withdrawHealthDataConsent` / `setMyHeightCm` / `setMyDateOfBirth` / `recordBodyWeightKg` / `clearBodyWeightHistory`. |
| Nutrition targets peer (derivation + levers at its own URL) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4, issue #666 M1 (web 2026-08-05, decisions § 527).** The calorie + macro target was computed on `/nutrition` as a ring denominator but reachable nowhere — its only editor a card partway down `/settings/preferences`, and the untargeted rings state naming Settings in prose without linking it. **Web ✓:** `/nutrition/targets` is a peer of the Nutrition surface, linked **ungated** from the `/nutrition` header (the user with no targets is the one who needs it), showing the derivation — BMR → activity factor → goal delta → base → today's workout add-on → macro split — composed only from what `nutrition_targets.ts` already exports, so **no new arithmetic and no new `.dart` twin obligation**. Activity level + weight goal edit inline (non-sensitive, already auto-saving); height / weight / DOB / sex stay in Settings behind the Art 9 consent gate, deep-linked as `/settings/preferences#body-metrics` from both surfaces (redirected to `/settings/body#body-metrics` since the #905 split). Not an always-on card — `/nutrition` keeps one primary action (anti-clutter checklist). Guards: `routes/nutrition/targets_peer_guard.test.ts` (ungated link, no Art 9 inputs on the peer, engine reused not re-derived, every referenced key in all six catalogues); e2e `tests-e2e/nutrition/targets-peer.spec.ts`. **Android + iOS (2026-08-19, [decisions § 695](../architecture/decisions.md)):** `screens/nutrition_targets_screen.dart` mirrors the peer — the same derivation rows and macro split, composed only from what `nutrition_targets.dart` already exported (**no new pure logic, no parity-pair change**), with the two non-sensitive levers editing inline through `SettingsSyncService.updateUniversal` on the same bag keys the consent-gated editor writes. The rings card on `nutrition_screen.dart` carries the entry **ungated** — it used to reach `SettingsBodyMetricsScreen` from its untargeted **empty state only** (§ 490), so the route vanished the moment the number became real. Height / weight / DOB / sex are read-only here and keep their single Art 9 entry point; the activity / goal label switches were promoted to shared `activityLevelLabel` / `weightGoalLabel` so the two surfaces cannot name one stored value differently. Guards mirror web's `targets_peer_guard.test.ts` (ungated entry, no Art 9 input or consent call on the peer, engine reused not re-derived); 12 widget/source tests; 24 ARB keys across all seven catalogues; iOS twin byte-identical. |

## Sync and backup

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Pull remote runs from Supabase | ✓ | Partial | ✓ | N/A | N/A | Watches hand off to the phone rather than maintaining their own synced history. |
| Bulk sync button (manual re-push) | ✓ | Partial | N/A | N/A | N/A | Web has no unsynced-queue concept — it's always online. |
| Parked run (permanently-refused push) + drop-track offer | ✓ | Partial | N/A | N/A | N/A | Mobile-only **by construction**, not by drift: parking is a verdict on a run sitting in the phone's local queue, and web has no queue to park from — the row above records why. `blocked_runs.json` + `RunPushOutcome` + the run-detail parked card ([decisions § 1070](../architecture/decisions.md), re-verified [§ 1096](../architecture/decisions.md)). A missing web surface here is correct, not a § 24 inversion. |
| Auto-sync on connectivity change | ✓ | Partial | N/A | ✓ | ✓ | Both watches sync on reconnect; Wear OS via `RunViewModel.observeConnectivity` collecting from `system/NetworkWatcher.kt` (offline → online edge fires `drainQueue`), in addition to app-open + post-stop drains. |
| Background periodic sync (WorkManager etc.) | ✓ | Partial | N/A | ✗ | ✗ | |
| Conflict resolution (newer-wins) | ✓ | Partial | ✓ | N/A | N/A | |
| Backup all runs as JSON | ✓ | Partial | ✓ | N/A | N/A | Two surfaces on web. (1) **Full backup ZIP** on `/settings/account` (`createBackup()` in `lib/backup.ts`) — same `run-app-backup` v1 wire format as Android, contains `runs.json` + `routes.json` + `profile.json` + `manifest.json` + per-run gzipped tracks; round-trip restores via the same UI. (2) **Single-file `runs-{ts}.json` quick export** in the same Data Export card as the existing CSV — identical row shape to the `runs.json` inside the ZIP (so scripts consume them interchangeably), no tracks, `user_id` stripped. **Scale story (May 2026):** mobile streams to disk via `ZipFileEncoder` + `InputFileStream`, downloads tracks in bounded-concurrency batches (peak heap ~5 MB at any size), and tries the **Go service's `/v1/export?format=backup`** path first when `LIVE_HUB_URL` is configured — server does the fan-out, mobile streams the signed-URL response straight to disk. Falls back to the local writer on any failure or for libraries > 5 000 runs. Web uses `@zip.js/zip.js`'s `BlobWriter` + `ZipWriter` for per-entry streaming + parallel downloads. **Both local writers declare their own completeness** — `manifest.json` carries the `complete` / `incomplete` pair in the Go writer's vocabulary and Settings → Account discloses a shortfall on download and at restore; web's `runs` / `routes` reads page through `readAllRows` (an unranged select was clamped to 1000 rows and answered 200). See `decisions.md § 66` + [§ 668 / § 675](../architecture/decisions.md). **iOS `Partial`** — the Full backup and Restore tiles are the same ungated `ListTile`s Android renders (`settings_account_screen.dart`), `BackupService` names no `Platform`, and every package on the path (`archive`, `file_picker`, `share_plus`, `path_provider`) declares iOS support; no method channel is involved and neither Info.plist nor the entitlements owe anything a document picker or share sheet needs. **The server rail is asynchronous on BOTH clients since 2026-08-24** ([decisions § 717](../architecture/decisions.md) web, [§ 724](../architecture/decisions.md) mobile): each enqueues a `data_export` job (`POST /v1/export/jobs`) and watches it, so a closed tab, a killed app or a client timeout no longer ends an export, and a reload / relaunch mid-build resumes from `GET /v1/export/jobs/latest` with nothing stored locally. The synchronous `POST /v1/export` is **deleted**. Mobile now splits the two archives into two tiles — Account export (server, complete) and Full backup (on-device, no account-record set, and it says so) — because `BackupService.tryServerBackup`'s silent fall-back between them hid a refused Art 20 request behind a narrower file. The queued rail is driven end to end in CI by the `e2e-web-exporthub` lane against a real Go worker. **The subject is now told when the build finishes** ([decisions § 729](../architecture/decisions.md), migration `20270607_001`): a `data_export_ready` notification lands in the inbox and rides the existing email / web-push / native-push siblings, carrying a link to the export page and never a signed URL — which is the whole reason this was buildable at last, since a URL minted at build time would already be expiring. Idempotent against an at-least-once redelivery via `data_export_jobs.notified_at`, and a failed announcement never fails the export. This is what § 724 deliberately did NOT promise on mobile, and it is server-side precisely because a client cannot promise to notice (iOS background refresh is opportunistic). |
| Download all data as CSV (GDPR) | ✓ | Partial | ✓ | N/A | N/A | Android: "Export runs as CSV" tile in Settings → Account writes the same `date,distance_m,duration_s,pace_s_per_km,source` shape as the web's GDPR export and hands the file to the system share sheet. |

## Integrations

See [features § External platform sync (OAuth)](features.md#external-platform-sync-oauth).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Connect / disconnect integrations UI | ✓ | Partial | ✓ | N/A | N/A | iOS Settings screen is the same Dart file as Android; `Partial` is pending real-device verification of the OAuth callback round-trip on iOS Safari / native browser. |
| Integration availability gate + per-card info tips | ✓ | Partial | ✓ | N/A | N/A | Settings → Integrations offers only what the deployment can honour, through the `integration_visibility` TS↔Dart parity pair (decisions § 1621). A provider declares a gate — `env` (Strava's client ID), `probe` (parkrun + the three race-results legs, fail-closed on their Edge Function), `always` (the client-side bulk importers on web, the BLE / watch tiles on mobile), `unsupported` (Apple HealthKit on web — an on-device iOS API a browser cannot reach) or `unbuilt` (Garmin Connect OAuth — blocked on Garmin's developer programme). An unresolved probe hides like a refused one but carries no explainer. A CONNECTED row stays visible whatever its gate says, so it can still be disconnected, and is marked stranded only when it is `unsupported` / `unbuilt` — an `unconfigured` Strava keeps Sync now, because the env var gates a new OAuth grant and not a sync on the existing one. `parkrun-import` gained a `{probe: true}` reachability answer on its own rate-limit bucket. **Every offered card / tile carries an `(i)`** explaining what the provider is and how to use it (`InfoTip.svelte` ↔ `widgets/info_tip.dart`, decisions § 1622): a disclosure rather than a `title=` tooltip, which never appears on touch, named "About {name}" so several on one screen stay distinguishable. Copy is per-platform where the instruction differs — web's parkrun tip points at Settings → Account, mobile's at the tile itself. **Amends § 488**: the race tiles no longer render with an explainer where their action would be; the race calendar keeps its own entry point. |
| Strava OAuth live sync | ✓ | Partial | ✓ | N/A | N/A | Shipped on web: connect button on `/settings/integrations` redirects to Strava's `/oauth/authorize`, the callback POSTs the code to the `strava-import` Edge Function, and the function exchanges it for tokens + backfills the last 90 days of run/walk/hike activities (GPS streams included). A "Sync now" button re-triggers the backfill. **Realtime sync via webhook is wired:** `strava-webhook` accepts Strava's signed POSTs, looks up the integration, refreshes the access token if it's within 5 min of expiry, fetches the activity detail + streams via the shared `_shared/strava.ts` helpers, dedupes against `runs.metadata.strava_id`, and ingests with the same row shape as the backfill path. The EF always returns 200 to ack so Strava doesn't retry. Android: Strava tile in Settings → Integrations shows connected status + last-sync; "Connect Strava" hands the user off to the web `/settings/integrations` page via `url_launcher` (web handles OAuth, the integrations row syncs back). Once connected, "Sync now" calls the `strava-import` Edge Function directly via `ApiClient.syncStrava`; "Disconnect" deletes the integration row. Native in-app OAuth via `flutter_web_auth_2` (Chrome Custom Tabs / `ASWebAuthenticationSession`, `threkir://strava-callback`) has since shipped — the hand-off is now only the unconfigured-client-ID fallback. **A sync that stops short of the end of the lookback window says so on both platforms** (decisions § 766): the walk has five truncating exits — Strava throttling us, an upstream error, a transport failure, a malformed page, the 20-page cap — of which only the first ever carried a field, and neither client declared even that one, so all of them rendered as a finished sync. `backfill` now returns `complete`, stamps `last_sync_at` only when it is true, and both clients grade the response through the `strava_sync_result` parity pair rather than casting it. Mobile also reports `failed`, which it had dropped entirely. **And it can now be recovered from** (decisions § 768): a truncated walk records the window still to walk on `integrations.sync_cursor` so a re-sync resumes rather than re-walking the same pages, the response carries `resumable`, and both clients can ask for a wider window than the 90-day default — a select beside "Sync now" on web, a "Sync older history…" picker in the tile menu on mobile — where neither could raise it above 90 before. The partial state persists on the card / tile rather than living only in a dismissed toast. |
| parkrun athlete-number import | ✓ | Partial | ✓ | N/A | N/A | Web: "Pull latest parkrun results" button on `/settings/account` shows once the athlete number is set. Calls the existing `parkrun-import` Edge Function, surfaces an imported-count toast. Android: "parkrun" tile in Settings → Integrations opens a dialog pre-filled from `user_profiles.parkrun_number`; on confirm, `ApiClient.setParkrunAthleteNumber` stores the number and `ApiClient.importParkrunResults` invokes the same Edge Function. Reports the imported count via SnackBar. |
| Race calendar (discover races near me + entry links) | ✓ | Partial | ✓ | N/A | N/A | Public, crowd-submittable race calendar over `search_race_listings` (proximity + soonest-first; pg_trgm name search + distance-band filter + geocoded "near a place"). Web: top-level `/races`. Mobile (Android + iOS byte-identical twin): `RacesScreen`, reached from the **Races** peer on the run surface's labelled strip (Fitness → Runs), with the Settings → Integrations RunSignUp / ChronoTrack tiles kept as secondary deep links (no new bottom-nav slot — the 5-slot ceiling holds). Reaching it is **ungated**: the search is a public RPC, and the provider probe governs only the per-race import (decisions §488; until then both Settings tiles were `onTap`-gated on the probe, so a deployment with neither key had no reachable entry point at all). Register link via `entry_url`; "Add a race" submits a `manual`, unverified listing (RLS forces `is_verified=false`). Migration `20270214_001` (`race_listings` table + RLS + RPC + `runs.race_listing_id`). |
| Race-result import from a timing provider | ✓ | Partial | ✓ | N/A | N/A | The `race-results-import` Edge Function has four legs — `runsignup`, `ultrasignup`, `chronotrack`, `paste` — each fail-closed behind its own credentials. **Until 2026-08-30 both clients branched on `runsignup` alone**, so the other two built legs were unreachable and the roadmap called the remainder a pure credential gate ([decisions § 806](../architecture/decisions.md)). Both platforms now render from one provider catalogue — web `integrations/race_import_providers.ts` (`RACE_IMPORT_LEGS`, a `Record` over a type DERIVED from the import call and the `RaceProvider` union, so `tsc` makes it exhaustive), mobile `race_service.dart` (`raceImportProviders` plus a `RaceImportScope` enum switched exhaustively). Each leg carries its own scope field, hint, testids and **its own unavailable explainer** — a runner told "RunSignUp import isn't available" about a ChronoTrack race learns nothing true. Both surfaces are covered: the calendar's import sheet and the run-detail auto-match prompt, which had the same single-provider defect. Guarded on both platforms by a test that reads the Edge Function's own accepted-provider literal, so a leg built server-side cannot stay unreachable on a client. **The two vocabularies are deliberately different overlapping sets**: the import set is 4 values, the listing set (`race_listings.provider`) is 6 — `paste` is a leg with no listing, and `parkrun` / `manual` / `raceresult` are listings with no leg, which fall through to manual paste as the correct answer rather than a gap. Every leg now has a pre-fetch scope gate; UltraSignup's was added in the same round, replacing a fallback that read the listing's `provider_race_id` as an athlete account id. **Watches `N/A`.** |
| Race results import (RunSignUp + manual paste) | ✓ | Partial | ✓ | N/A | N/A | The `race-results-import` Edge Function maps a RunSignUp finisher (or a pasted result row) onto a `source='race'` run carrying the owner-only race metadata (`race_name`/`bib`/`chip_time`/`gun_time`/`overall_place`/`age_group_place`/`age_group`), deduped per-user on `external_id = race:{name}:{date}:{bib}`. **RunSignUp leg is prod-gated, fail-closed:** with `RUNSIGNUP_API_KEY` unset it returns `503 provider_not_configured` and the UI shows the unavailable explainer; parkrun + manual paste work without it. Web: paste form on `/races` + run-detail. Mobile: paste form in `RacesScreen` + run-detail. |
| Auto-match-on-record (inform-tier) | ✓ | Partial | ✓ | N/A | N/A | After a run, if a same-day race listing matches by distance band (+ proximity when known), the run-detail surface shows a non-blocking "Was this the {race}? Import your official result." prompt — **never writes without a tap**. Confirming enriches the existing run row in place (no duplicate `race:` run) and links `runs.race_listing_id`. Scoring is the pure `race_match` TS↔Dart parity pair (`raceMatchScore` / `isRaceMatchCandidate`, threshold 0.5). Web: `RunRaceResult.svelte` on `/runs/[id]`. Mobile: `RunRaceSection` on `run_detail_screen.dart`. Recording platforms only (the watches are recording-only). |
| HealthKit (iOS / Apple Watch) | ✗ | ✗ | N/A | N/A | ✓ | Apple Watch reads HR and forwards `avg_bpm`. Phone HealthKit importer not started. |
| Health Connect (Android) | ✓ | N/A | N/A | N/A | N/A | Imports a workout's summary always, plus its **GPS route** (`READ_EXERCISE_ROUTES`, asked for only on an explicit tap via `HealthRoutePermissionBridge` — the `health` plugin maps route reads onto the session grant alone, so it is otherwise unreachable; a withheld route arrives as a zero-location point and is offered a backfill, a read that threw is reported rather than shown as "no routes") and its **step count** (`READ_STEPS` — the plugin sums every `StepsRecord` in the session window, so `stepsForWorkout` drops `cycle` and any count implying a cadence past 300 spm; writes `metadata.steps` only, the cadence coming from the shared `steps / moving_time_minutes` derivation). **Laps are not reachable** — the `health` package surfaces `ExerciseSessionRecord.laps` only on the write path, so a multi-lap session flattens to one interval. Route + steps are host-tested against fixtures; both grants remain device-gated. **iOS N/A:** Health Connect is an Android platform service; the Apple equivalent is the HealthKit row above. |
| Garmin Connect | ✗ | ✗ | Partial | N/A | N/A | **Live OAuth + webhook sync** is still blocked on Garmin's developer-program approval (NDA, multi-week review, integration call) — neither side can be implemented client-side. **Bulk-import path shipped on web** (`/settings/integrations` → "Bulk import from a Garmin export"): accepts a single `.fit` from "Export Original" or the full `.zip` from `Garmin → Account Management → Request Your Data`. Parses FIT via `fit-file-parser` (lazy-loaded so it only ships when triggered), routes user-uploaded `.gpx` / `.tcx` originals through the existing `parseRouteFile`, dedupes on `metadata.garmin_id` (`<file_id.time_created>-<file_id.serial>`) with a `started_at,distance_m` composite fallback. 🔸 OAuth flow remains the gap; flip to ✓ when Garmin grants developer-program access. |

## Social — clubs and events

See [docs/features/clubs.md](../features/clubs.md). No features.md section yet — update this row-block when one is added.

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Browse clubs | ✓ | Partial | ✓ | N/A | N/A | |
| Create club | ✓ | Partial | ✓ | N/A | N/A | Android: FAB on `clubs_screen.dart` opens `widgets/club_form_sheet.dart` (name, description, location, public/private, join policy with the same visibility ↔ policy linking as web `ClubEditor`). Auto-slugifies the name; the `enroll_club_owner` trigger inserts the owner's `club_members` row server-side. |
| Club detail (feed / events / members tabs) | ✓ | Partial | ✓ | N/A | N/A | |
| Club website + social links | ✓ | Partial | ✓ | N/A | N/A | A club publishes a website + Instagram/Strava/Facebook link row ("Visit our website"). Migration `20270131_001`: four `*_url` columns with an http(s)-scheme CHECK (XSS backstop) + explicit `grant select`. Admin-edited via dual-mode `ClubEditor` (web) / `club_form_sheet` (mobile); rendered in the club header with `rel="noopener noreferrer nofollow"`. `normaliseClubLink` (web `data.ts` ↔ mobile `social_service.dart`) drops non-http(s) input before write. See [clubs.md § Club links](../features/clubs.md). |
| Club posts with threaded replies | ✓ | Partial | ✓ | N/A | N/A | |
| Create event | ✓ | Partial | ✓ | N/A | N/A | Android: admin-only "Create event" button on the Events tab of `club_detail_screen.dart` opens `widgets/event_form_sheet.dart` (title, starts_at picker, description, meet label, distance/duration, recurrence none/weekly/biweekly/monthly with auto-byday from the picked DOW). |
| Event detail + RSVP | ✓ | Partial | ✓ | N/A | N/A | |
| Recurring events (per-instance RSVP) | ✓ | Partial | ✓ | N/A | N/A | |
| Add event to calendar | ✓ | Partial | ✓ | N/A | N/A | Web downloads an RFC 5545 `.ics` for the chosen occurrence, and — for a recurring event — the whole series as one VEVENT carrying an `RRULE` + one `EXDATE` per cancelled occurrence (`buildEventSeriesIcs`, decisions §599). A series the RRULE grammar cannot state faithfully (a monthly one anchored past the 28th, where the app clamps and `BYMONTHDAY` skips) withholds the button rather than emitting dates that disagree with the page. **Android + iOS (byte-identical twin) mirror it natively 2026-08-19** (decisions §692): `event_detail_screen.dart` offers the same two actions and hands the event to the OS calendar's own editor — `ACTION_INSERT` on `CalendarContract.Events` / `EKEventEditViewController` — with the series expressed as an RRULE value that `calendar_intent.dart#buildRrule` shapes under web's exact refusals. The `.ics` **text builder** stays web-only and owes no Dart twin (a download is a browser concern, §24). The one thing the hand-off cannot carry is `EXDATE`, so mobile **names** the called-off occurrences ahead under the series button rather than dropping them silently. 38 mobile tests. See [clubs.md § Event detail capabilities](../features/clubs.md). |
| Typed events (run/cycle/class/social) — type-first create + category-gated detail | ✓ | Partial | ✓ | N/A | N/A | Slice E ([club_events.md](../features/club_events.md)). Schema + data-layer athletic guards in migration `20261227_001`; the `isAthleticCategory` predicate is a web↔Dart parity pair. **Web ✓:** `EventEditor.svelte` picks `category` as its first control + free-text `discipline` for a `class`, athletic fields (route/distance/pace) self-hide + clear off-category, `createEvent` persists `category`/`discipline`; `/clubs/[slug]/events/[id]` gates distance/pace/route + race panel/banner + the whole results/leaderboard/Submit-my-time/certificate/claims section on `isAthleticCategory(event.category)` and surfaces the discipline in the hero for class/social. `EVENT_SELECT_COLS` now reads `category`+`discipline`; migration `20261228_001` grants cross-user SELECT on them. **Android + iOS** (byte-identical twin): `event_form_sheet.dart` picks `category` as its first control + a free-text `discipline` for a `class`, athletic fields (distance/route/pace) self-hide + clear off-category via `isAthleticEventCategory`, and `SocialService.createEvent` / `buildCreateEventBody` persist `category`/`discipline`/`gym_template`; read-only `event_detail_screen.dart` is category-gated (athletic affordances behind `isAthleticEventCategory`, discipline label shown). Slice P1 (paid registration) has now shipped on web — see the row below. |
| Class → gym seam (host templates a class → attendee one-tap logs a workout) | ✓ | Partial | ✓ | N/A | N/A | [club_events.md § The class → gym seam](../features/club_events.md#the-class--gym-seam-is-designed-not-a-footnote). Inform-tier. A `class` host sets an optional `gym_template` (`{discipline, duration_min}`) in `EventEditor`; a signed-in attendee on the event detail gets a "Log this as a workout" button (gated on `category==='class'` + non-null template + signed in — never on a run/cycle/social event) that **pre-fills the canonical composer** (web `GymEditor`, mobile `gym_compose_sheet`) from the template and writes to **their own** gym log only after they confirm. Typed contract = parity pair `event_gym_template.ts` ↔ `event_gym_template.dart` (`parseGymTemplate` / `gymTemplateFromInputs` / `workoutDraftFromTemplate`). Read path needs the `SELECT (gym_template)` grant in migration `20261230_001` (the column-locked `events` table). **Web ✓** (`event_class_gym_seam.spec.ts`), **Android + iOS** (byte-identical twin; `event_detail_screen.dart` opens `showGymComposeSheet` through a screen-owned `LocalGymStore` — flat `prefillTitle` normally, and when the `class` carries a `session_plan_id` it loads + expands the plan and passes `seedSets`/`seedTitle` from `workoutDraftFromSession`, mirroring web's `logWorkoutPrefill`, 2026-07-11). **Watch N/A.** |
| Session planner (yoga/pilates content) — P1 build/read | ✓ | Partial | Partial | N/A | N/A | P1 ([session_planner.md](../features/session_planner.md), [decisions.md § 140](../architecture/decisions.md)). A reusable `session_plans` engine (blocks → hold/reps/flow items, per-side, cue/tempo). **Web ✓:** `SessionPlanEditor` builds + saves; `/sessions` list (self-hiding empty) + `/sessions/[id]` expanded read view; class-event detail surfaces the attached sequence read-only with an organiser attach control. **Android/iOS Partial:** the `expandSessionSteps` TS↔Dart parity pair + a read/list (`SessionsScreen` / `SessionDetailScreen`) — **no editor** (web-first). `SessionsScreen` is now reachable from the **Sessions** peer on the gym surface's labelled strip (decisions §488, mirroring web's `/gym` → `/sessions` link); before that it had no instantiation anywhere in the app. **No execution/logging** (P2). Schema `20270103_001`; `SessionItemKind` narrow union; pgtap `session_plans_rls_test.sql`. **Watch N/A.** |
| Session planner — P2 follow-along + log | ✓ | Partial | ✓ | N/A | N/A | [session_planner.md § P2](../features/session_planner.md), 2026-06-12. The planned-vs-actual layer is merged on all platforms via the byte-identical pairs: `computeSessionAdherence` on the `session_steps` pair (`session_steps.ts` ↔ `session_steps.dart`), the `workoutDraftFromSession` seam helper on `event_gym_template.ts` ↔ `.dart`, and the `gym_workouts.metadata` session trio (`session_plan_id` / `session_step_results` / `session_adherence`, registered in [metadata.md](../backend/metadata.md) + `MetadataKeys`, owner-only). **Web ✓ (2026-06-12):** `/sessions/[id]` Start control mounts the `SessionRunner` overlay (`SessionRunner.svelte` + `SessionExecutionBand.svelte`) — timed auto-advance off the cumulative axis, reps wait on Done, Pause/Resume/Skip/Abandon; on finish `workoutDraftFromSession` → `createGymWorkout` with the metadata trio; `EventEditor` gains a session-plan picker. **Android + iOS:** the on-screen follow-along **runner** lives on `session_detail_screen.dart` (Start → timed auto-advance, Done on reps, Pause/Resume/Skip/Abandon, finish → `workoutDraftFromSession` → gym log with the metadata trio), reading the server rather than an offline cache — the unwired `LocalSessionStore` was deleted ([decisions § 707](../architecture/decisions.md)); byte-identical iOS twin. **Watch N/A** (wrist follow-along deferred). |
| Session planner — P3 sharing (public share · autocomplete · club templates) | ✓ | Partial | Partial | N/A | N/A | [session_planner.md § P3](../features/session_planner.md), 2026-06-12. **Web ✓:** the logged-out share page `/share/session/[id]` (`lookupSharedSession`) + an `is_public` toggle + copy-share-link on `/sessions/[id]`; movement-name autocomplete in `SessionPlanEditor` (`fetchSessionMovementNames`); publish-to-club on `/sessions/[id]` (`publishSessionAsTemplate`) + a Session-templates surface on the club-detail Templates tab with per-row Adopt (`cloneSessionTemplate` → the `clone_session_template` RPC, migration `20270104_001`). **Android/iOS Partial:** the owner `is_public` toggle on `session_detail_screen` (`ApiClient.setSessionPlanPublic`) + the club Session-templates list + Adopt on `club_detail_screen` (`ApiClient.cloneSessionTemplate`); the editor autocomplete + the share page stay web-first (mobile session editing is web-canonical per P1). **Watch N/A.** Public read gated on `is_public` + RLS; pgtap `clone_session_template_test.sql`; Playwright `share/session.spec.ts` + `sessions/movement-autocomplete.spec.ts` + `sessions/club-template.spec.ts`; Flutter `session_detail_visibility_test.dart`. |
| Paid event registration (Stripe Connect marketplace) — Slice P1 | ✓ | Partial | ✗ | N/A | N/A | Slice P1 ([club_events.md § Slice P](../features/club_events.md#slice-p--paid-registration)). **Web ✓:** `/settings/payouts` Connect onboarding + status, the EventEditor "Charge" toggle (disabled until `host_can_take_payment`), and a "Register · $X" → Stripe Checkout flow on `/clubs/[slug]/events/[id]` with a `?paid=1` success poll. Destination charges → the money lands in the **host's** connected account (`events.host_user_id`) and the platform takes an application fee; the **platform**, not the host, is the merchant of record, because `on_behalf_of` is not sent ([club_events.md § Money movement](../features/club_events.md#money-movement--to-the-host-not-the-club)). Migration `20261229_001` + EFs `events-connect-onboard` / `events-checkout` / `stripe-events-webhook` (the sole, idempotent, service-role-only order-status writer). Pure `paid_registration.ts` (fee/sales-window/refund) is **web-only** for now; the Dart twin lands with mobile register (P3). **In-person only; web checkout only; manual refunds via the Stripe dashboard.** **Mobile ✗ until P3** (read-only price + "Register on the web" handoff). **Watch ✗** (no purchase/creation). **Live charge path UNVERIFIED** — needs operator Stripe test-mode keys. |
| Paid event registration — buyer self-cancel + refund (Slice P2) | ✓ | Partial | ✗ | N/A | N/A | Slice P2 ([club_events.md § Slice P](../features/club_events.md#slice-p--paid-registration)), migration `20270303_001`, ADR §187. **Web ✓:** a "Cancel registration" affordance under the registered badge on `/clubs/[slug]/events/[id]` → `cancelEventOrder` → the `events-cancel` EF, which **initiates** the cancel (refund-eligible `paid` **or `partially_refunded`** order → Stripe refund with **both** `reverse_transfer: true` and `refund_application_fee: true`, which Stripe couples on a destination charge — see [decisions § 769](../architecture/decisions.md); `pending` → expire the Checkout Session) but never writes status; the `stripe-events-webhook` (still the sole, idempotent order-status writer) handles `charge.refunded` to CAS `paid->refunded` + release the seat — and, since `20270624000001`, the `refund.failed` / `refund.updated` / `charge.refund.updated` trio to CAS `refunded->refund_failed` when the bank reverses the refund, leaving the seat alone ([decisions § 789](../architecture/decisions.md)). `event_orders.refund_initiated_at` (a non-status buyer "refund requested" stamp, gated by the status-lock→column-lock + a buyer UPDATE policy) drives a "Refund in progress" badge during the async gap; ConfirmDialog shows refundable / no-refund / pending copy per `refund_policy`. Since `20270701000001` a **reversed refund** (`refund_failed`) also renders: a banner outside the registered block — outside it because § 789 does not restore the seat, so `regState` is never `already_registered` and the block cannot draw, and outside the `!isPast` gate too — naming what happened, that the money is still with us, and `billing@threkir.com` ([decisions § 825](../architecture/decisions.md)). **Mobile ✗** (P2 is web-only — mobile register itself lands in P3; the reversed-refund news reaches mobile through the `refund_failed` notification kind instead, which is the row below). **Watch ✗.** **Live refund round-trip UNVERIFIED** — fail-closed 503 without operator Stripe test keys; pure libs (Deno), the column-lock + buyer policy (pgtap `event_order_self_cancel_test.sql`), and the UI states (Playwright `event-self-cancel.spec.ts`) are tested. |
| Charity fundraising pages (run/event) — gated | Partial | Partial | ✓ | N/A | N/A | [fundraising.md](../features/fundraising.md), migration `20270213_001`, ADR §167. A public charity page (goal thermometer + donation feed + share) on a run **or** club event, reusing the paid-events Stripe Connect destination-charge rail (same payout account + the **one** `stripe-events-webhook`, extended with a `metadata.kind==='donation'` branch). **Web ✓:** public `/fundraisers/[id]` (thermometer + feed + amount-picker → Stripe-hosted Checkout, `?donated=1` success poll), `FundraiserEditor` create/edit/close, attach affordance on run-detail + event-detail. The `fundraiser_progress` TS↔Dart parity pair (11 mirror tests each). **Android + iOS read + web-handoff:** read-only `FundraiserSection` card (thermometer + feed) on run-detail + event-detail; **Donate routes to the web page** (no in-app checkout, mirrors paid-events P3). `donations` is service-role-only (webhook is the sole idempotent writer); donor identity / Stripe ids revoked from client roles; public feed via the `fundraiser_feed`/`fundraiser_totals` RPCs. **Watch N/A.** **Live charge path prod-gated + UNVERIFIED** — fail-closed behind live Stripe keys + owner/CISO/counsel sign-off (test-mode only in P1). pgtap (`fundraisers_rls_test` / `donations_status_lock_test` / `fundraiser_pricing_requires_charges_test`), Deno (`donations-checkout/lib.test` + the webhook donation-branch tests), Playwright `fundraising/fundraiser.spec.ts`, Flutter `fundraiser_progress_test.dart`. |
| Attendance marking (host-only, distinct from RSVP) | ✓ | Partial | ✓ | N/A | N/A | instructor_business.md M6. A `class`-event organiser marks who actually showed up (`attended`/`no_show`, NULL until marked) — orthogonal to RSVP `status` (paid ≠ attended). Migration `20270102_001`: a nullable `event_attendees.attendance` column (`EventAttendance` check↔union pair) + the organiser-only `mark_attendance` SECURITY DEFINER RPC; the self-only RSVP UPDATE policy stays intact but column UPDATE on `attendance` is revoked so the RPC is the sole write path. **Web ✓:** per-attendee attended/no-show controls on `/clubs/[slug]/events/[id]` for the host (`markAttendance` in `data.ts`); non-hosts see the state read-only. **Android + iOS:** the same host-only controls + read-only badge on `event_detail_screen.dart` (`SocialService.markAttendance`); host-gating via `canMarkEventAttendance`. pgtap `event_attendance_test.sql` (organiser-can / member-cannot / orthogonal RSVP / column-lock), Playwright `event-attendance.spec.ts`, Flutter `event_detail_screen_test.dart`. **Watch N/A.** |
| Gym routine engine — P1 (reusable routines + repeat-last) | ✓ | Partial | ✓ | N/A | N/A | [gym_programming.md](../features/gym_programming.md) P1, migration `20270101_001`. Reusable `gym_routines`/`_exercises`/`_sets` (+ `gym_workouts.metadata`); **prefill-only, no execution**. The `gym_routine` parity pair (`routineFromWorkout` + `prefillFromRoutine`, reuses `gym_prs.normaliseExerciseName`). **Web ✓:** `/gym/routines` library (self-hiding) + builder + `[id]` detail; "Save as routine" + "Repeat last" on `/gym/[id]`. **Android + iOS:** `LocalRoutineStore` (offline, exercises+sets inline) + routine library/detail/builder + the Routines AppBar entry + save-as-routine / repeat-last on gym detail; byte-identical twin. DSAR export extended (Deno **and** the production Go worker). Four narrow-union↔CHECK pairs (periodisation / modality / progression / set_type). Playwright `routines.spec.ts` + pgtap `gym_routines_rls_test.sql`. **Watch N/A.** P2/P3 pure-logic + runner landing now (see the next row); P4 (progression-engine wiring) gated on the P1 repeat-rate signal. |
| Gym routine engine — P2/P3 execution + adherence | ✓ | Partial | ✓ | N/A | N/A | [gym_programming.md § P2/P3](../features/gym_programming.md), 2026-06-12 — pure-logic + runner + execution UI merged. Shared across platforms via the byte-identical pairs + the shared runner: `expandRoutineSteps` on the `gym_routine` pair, the `gym_adherence` pair (`computeRoutineAdherence`, per-axis 80%-of-target cutoff), the `gym_progression` pair (`nextPrescription`), the `GymWorkoutRunner` (`packages/run_recorder/lib/src/gym_workout_runner.dart`, `gym_workout_runner_test.dart`), and the `gym_workouts.metadata` execution trio (`routine_id` / `gym_step_results` / `gym_adherence`, registered in [metadata.md](../backend/metadata.md) + `MetadataKeys`, owner-only). **Web ✓:** the `GymSessionRunner` (`/gym/session/[routineId]`) drives the run via `GymExecutionBand.svelte` + `RestTimer.svelte`, finishing into `createGymWorkout` with the metadata trio. **Android + iOS:** `gym_session_screen.dart` + `gym_execution_band.dart` (the follow-along runner) backed by `LocalGymStore`; byte-identical iOS twin. Both platforms guard navigation out of a live session and resume a persisted `gym_session_draft` (issue #666 U5 on mobile, [decisions §483](../architecture/decisions.md); the web half landed [§493](../architecture/decisions.md), issue #666 round 7) — mobile from a gym-screen resume card after a three-way keep / leave-with-draft / discard dialog, web from the same card on `/gym` after a `beforeNavigate` + `beforeunload` guard. The draft shape is identical on both ([metadata.md](../backend/metadata.md)), so a session interrupted on one platform resumes on the other. **Watch N/A.** P4 progression-engine wiring still gated on the P1 repeat-rate signal. |
| Gym routine engine — P2 authoring + P4 progression UI | ✓ | Partial | ✓ | N/A | N/A | [gym_programming.md](../features/gym_programming.md), 2026-06-12 — the routine editor can now AUTHOR what the runner already respected. **P2 authoring (web + Android/iOS):** per-set set-type (warmup/working/dropset/amrap/failure/backoff) + rest seconds, a superset toggle that brackets adjacent exercises into a group (the `routine_editor_build` `assignSupersetGroups` TS↔Dart parity pair), a per-exercise modality picker (weight_reps / time / distance / bodyweight_reps) that swaps the target field, and a progression-scheme + params selector — all persisted to the existing `gym_routine_exercises` / `gym_routine_sets` columns. Routine detail renders the authored structure (set-type, modality-aware target, rest, superset + progression chips); the session runner threads superset/set_type/rest through `expandRoutineSteps`. **P4 progression UI (web + Android/iOS):** session-start prefill of the planned targets from `nextPrescription` over the exercise's logged history (the `progression_prefill` `lastSessionSets` TS↔Dart parity pair; still editable in the band), and a neutral "next target" chip on the workout review ("+5 kg" / "rep climb 8→9" + reason) from this session's logged sets. `nextPrescription` itself (the `gym_progression` pair) is unchanged — only called. **Watch N/A.** Web Playwright + Dart widget/unit tests; byte-identical iOS twin. **Prescriber input parity closed 2026-08-13** (migration `20270525_001`, [decisions § 605](../architecture/decisions.md)): the two set-history RPCs now return `gym_sets.set_type` and all three `GymSetWithDate` producers map it, so web excludes an explicitly-typed warmup logged AT the working weight — the one case the working-weight narrowing cannot separate, and which mobile (reading its local store) already excluded. |
| Gym routine engine — public template library (publish + adopt) | ✓ | Partial | ✓ | N/A | N/A | [gym_programming.md](../features/gym_programming.md), migration `20270226_001` (2026-06-20). The public, adoptable counterpart to the build-your-own routine library — the gym-routine analogue of the public PLAN library (`clone_public_plan`, `20270126_001`) and orthogonal to the club-scoped templates (`20270109_001`). `gym_routines.is_public_template` (+ a `gym_routines_public_not_club` CHECK keeping public and club visibility separable) + additive public-read RLS on routine/exercises/sets + a public-template branch in `clone_gym_routine_template` (rate-limited deep-copy → a personal, club-less, non-public clone) + a `set_gym_routine_public` author-gated publish/unpublish toggle. A public routine carries no private fitness data (targets ARE the prescription), so nothing is stripped on publish/clone. **Web ✓:** `/gym/routines/library` (search + author handle), `[id]` preview + Adopt, an owner publish/unpublish toggle + badge on the routine detail, a Library link on `/gym/routines`. **Android + iOS** (byte-identical twin): `RoutinePublicLibraryScreen` + preview/adopt, a Library AppBar action on the routine library, and the publish toggle + badge on `routine_detail_screen` (`StoredRoutine.isPublicTemplate` + `LocalRoutineStore.setPublicLocal`). pgtap `public_gym_routine_library_test.sql`; Playwright `public-routine-library.spec.ts`; Flutter widget tests. **Watch N/A.** |
| Gym routine engine — per-routine history read-back | ✓ | Partial | ✓ | N/A | N/A | [decisions § 617](../architecture/decisions.md). The read-back half of the guided session's execution trio (`routine_id` / `gym_step_results` / `gym_adherence`) — every guided session stamps the routine link, and this is what reads it back per routine: sessions performed, days since the routine was last done, a completed-of-graded ratio, and the five most recent sessions as links into their gym workout. An in-flight `gym_session_draft` row is not a performed session; a "save as is" row counts as one but is graded as neither hit nor miss. Author-gated on both platforms, self-hiding at zero sessions but **never** on a failed read — "couldn't load" and "you have never run this" are different claims, so the panel owns its own fetch and offers a retry. **Web ✓:** `GymRoutineHistory.svelte` on `/gym/routines/[id]` over `fetchGymRoutineHistory`. **Android + iOS** (byte-identical twin, 2026-08-18): the same panel on `routine_detail_screen.dart` over `ApiClient.fetchGymRoutineHistory`. Read server-side on mobile rather than from `LocalGymStore`, which holds only the most recent page of workouts — a routine last run months ago would otherwise read as never run. Both sides share the `routine_history` TS↔Dart parity pair (16 mirror tests each) and both read the **`gym_routine_history` aggregate RPC** (migration `20270528_001`, decisions § 658): the session count is complete on both platforms and the five listed rows are an explicitly bounded page of the same snapshot. The earlier 500-row client window silently under-reported a routine run weekly for years, on both platforms at once. **Watch N/A.** |
| Timed gym work (`gym_sets.duration_s`) | ✓ | Partial | ✓ | N/A | N/A | [instructor_business.md § M2](../features/instructor_business.md), migration `20261231_001`. A nullable `duration_s` (seconds) so planks / holds / intervals are first-class. **Web ✓:** duration input in `GymEditor` (persist + hydrate) + read-side surfacing. **Android + iOS:** the same in `gym_compose_sheet.dart` + set rendering; byte-identical twin. The `gym_exercise_set_history` RPC returns `duration_s`. pgtap `gym_sets_duration_s_test.sql` + Flutter widget tests. Optional on every set. **Watch N/A** (gym is not a watch surface). |
| Event photo gallery (multi-attendee) | ✓ | Partial | ✓ | N/A | N/A | Persona-hunt finding #49 (social-group). `run_photos` gained `event_id` + `event_instance_start` (migration `20261025_001`) so a photo can be tagged to an event occurrence; a new SELECT policy makes event-tagged photos readable by anyone who can see the event — even when the underlying run is private — so the gallery aggregates every attendee's shots. Web: a Photos section on `/clubs/[slug]/events/[id]` shows the grid (thumbnail + caption + uploader); any signed-in user gets an "Add photo" control (persona round-5 runner-social-group relaxed the original finisher-only gate that hid it on non-race group runs) — finishers attach to their own `event_results.run_id` with zero extra clicks, everyone else picks which of their recent runs (`fetchRecentRunsForPicker`) the photo belongs to. `fetchEventPhotos` + `addRunPhoto({event_id})` in `data.ts`; 4-assertion pgtap (`event_photo_gallery_test.sql`) pins the private-run-but-visible-gallery RLS; `event-photos-nonrace.spec.ts` pins the non-race picker path. **Android ✓** (2026-07-12): `widgets/event_photos.dart` mounts a Photos section on `event_detail_screen.dart` — the same gallery grid + any-signed-in-viewer add flow (a finisher attaches to their own event run `_myEventRunId`, everyone else picks a recent run via a bottom-sheet), reusing the `run_photos.dart` EXIF-strip + signed-URL path and the shared `photo_lightbox.dart` viewer. `ApiClient.fetchEventPhotos` (run_id NOT selected, so a private run's id can't leak; `EventPhotoView` DTO + batched uploader-name join) + `addRunPhoto`'s new `eventId`/`eventInstanceStart` params. Widget tests in `event_photos_test.dart`. |
| Finisher certificate (downloadable) | ✓ | Partial | ✓ | N/A | N/A | Persona-hunt finding #44 (event organiser). Web: a "Certificate" button on each finished + organiser-approved leaderboard row on `/clubs/[slug]/events/[id]` builds a certificate SVG (`lib/finisher_certificate.ts`, pure + 8 unit tests) and downloads it as a PNG via the shared client-side rasteriser (`lib/svg_raster.ts`). No server PDF service. **Android + iOS** (byte-identical twin): each finished + organiser-approved row on `event_detail_screen.dart` shows a certificate action that opens `widgets/finisher_certificate_card.dart` — a native landscape card mirroring the web SVG, captured via `RepaintBoundary.toImage` and handed to the OS share sheet (`share_plus`, the run-share-card idiom). `lib/finisher_certificate.dart` is the pure TS↔Dart parity twin of web's shaping (eligibility, time/distance/ordinal formatting); `event_results_redacted.organiser_approved` is now read into `EventResultView`. |
| Bulk results import (chip-timing CSV) | ✗ | ✗ | ✓ | N/A | N/A | Persona-hunt finding #43 (event organiser). Web: an organiser-gated "Import results CSV" panel on `/clubs/[slug]/events/[id]` parses a bib/name/time CSV (`lib/event_results_csv.ts`, pure + 10 unit tests — header aliases, HH:MM:SS/MM:SS/seconds, DNF/DNS, km-vs-m, per-row errors, in-file dup-bib) and upserts via `bulkImportEventResults` keyed on `(event, instance, bib)`. Finishers without an account are stored on the now account-optional `event_results` (nullable `user_id` + `bib` + `finisher_name`, migration `20261028_001`). Mobile / Android get **leaderboard read parity** — bib-only finishers render by name — but the import itself is web-canonical per `decisions.md § 24`; no mobile import UI. |
| Claim an imported result | ✗ | ✗ | ✓ | N/A | N/A | Persona-hunt finding #43 follow-up (migration `20261030_001`, organiser-approve per `decisions.md § 95`). Web: a logged-in runner taps "This is me" on a bib-only leaderboard row (`claim_event_result` RPC); the event-organiser approves from a "Result claims" queue (`decide_event_result_claim`), attaching the account to the row and auto-rejecting competing claims. Backed by `event_result_claims`. Mobile shows imported finishers (read parity) but the claim action is web-only per `decisions.md § 24`. Claim/approval notifications + an auto-approve-on-attendance refinement are deferred. |
| Invite tokens / join links | ✓ | Partial | ✓ | N/A | N/A | |
| Join-request approval flow | ✓ | Partial | ✓ | N/A | N/A | Android: pending-requests panel on the Members tab of `club_detail_screen.dart` (admin-only — RLS filters non-admins to []). Per-row Approve / Deny buttons call `SocialService.approveJoinRequest` / `denyJoinRequest` and remove the row optimistically. |
| Upcoming-event card on home (within 48h) | ✓ | Partial | ✓ | N/A | N/A | Web: `fetchNextRsvpedEvent(48)` on dashboard mount; card renders above stats when a matching RSVP exists, links to the event detail page. |
| Realtime subscriptions (posts / RSVPs / members) | ✓ | Partial | ✓ | N/A | N/A | |
| Push notifications (event reminders, admin updates) | Partial | Partial | Partial | ✗ | ✗ | **Web subscribe path shipped.** Service worker at `apps/web/static/sw.js` (handles `push` + `notificationclick`); `lib/push.ts` registers the SW, requests permission, calls `pushManager.subscribe`, persists the subscription onto `user_device_settings.prefs.push_subscription` keyed by the per-browser device id. UI on `/settings/account` → "Notifications" card with Enable / Disable toggle and a clear "blocked in browser" / "build not configured" fallback; **also mirrored on the current-device row of `/settings/devices`** so the per-browser scope is visually obvious (non-current rows show a read-only "Push on" badge when their saved prefs carry a `push_subscription`). Gated on `PUBLIC_VAPID_PUBLIC_KEY` being set at build time — when absent the UI renders an honest "not configured" hint instead of a broken button. **Email delivery now ships as the first server-side channel** (migration `20261130_001`, `decisions.md § 117`): event-day reminders (`enqueue_event_reminders()` hourly pg_cron over `going` RSVPs in the next 24 h) + a per-recipient `notification_email` job drained by the Go worker, gated on `user_settings.prefs.email_notifications` (`all \| important \| off`, default `important`; the toggle lives in web `/settings/notifications` and mobile Settings → Preferences → Notifications, i18n'd across all six locales on both), sent over SMTP (local Mailpit; Resend/SES in prod). Credential-free — testable end-to-end on the local Docker stack. **Web-push server-side delivery now ships** (migration `20261219_001`): a `web_push` Go-worker job — the sibling of `notification_email` over the same notifications rows — signs + POSTs encrypted Web Push messages (RFC 8291/8292) to each stored browser subscription, gated on a registered `push_subscription` + the separate `push_notifications` preference (`all \| important \| off`, default `important`); a dead endpoint is pruned, a 429/5xx defers. Gated on the operator-generated `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` (self-generated, not a third-party credential); unset → jobs finish done, rows stay pending. Remaining web gap is only the `push_notifications` category UI toggle (works on the default without it). **Native-push server-side delivery now ships too** (migration `20270212_001`, `decisions.md § 166`): a `native_push` Go-worker job — the THIRD sibling over the same notifications rows — fans out over the user's enabled `device_tokens`, routing `android` → FCM HTTP v1 and `ios` → APNs HTTP/2 (`internal/nativepush/`, stdlib + `golang-jwt`), gated on the same `push_notifications` pref; a dead token is pruned (`clear_device_token`), a 429/5xx defers. The mobile client leg (`push_messaging_bridge.dart` + `firebase_push_messaging.dart`, byte-identical iOS twin) registers the FCM/APNs device token on sign-in, re-registers on `onTokenRefresh`, removes it on sign-out, and mirrors the `push_notifications` choice to the per-device `is_notifications_enabled` flag — the first writer of the `device_tokens` table (shipped table `20260506_001`). Android/iOS are `Partial` because going live is gated on user-supplied Firebase/APNs credentials on the worker (`FCM_*` / `APNS_*`) + the per-app config files (`google-services.json` / `GoogleService-Info.plist`); unset → jobs finish done, rows stay pending, and the mobile bridge no-ops (compiles + runs without the config). **Tap-to-open shipped 2026-08-05 (issue #666):** the bridge's `onOpenNotification` seam existed but `main.dart` never supplied one, so a tap opened the app wherever it already was and discarded the deep link. A pure `pushTargetFromUrl` (`push_target.dart`) maps the worker's stamped URL onto a typed target over the whole `pathForKind` set, `main.dart` parks it in `pendingPushTarget`, and `HomeScreen` drains it — via a listener **and** a first-frame drain, because a tap that cold-starts the app arrives before any Navigator exists and is delivered once through `getInitialMessage()`, never replayed on `onMessageOpenedApp`. Club and event targets carry a UUID against slug-addressed screens, so `SocialService.fetchClubSlugById` / `fetchClubSlugForEvent` resolve it and degrade to the clubs hub; `/messages` (web-only) and anything unrecognised degrade to the inbox. **Web side closed in the same round:** `/events/{id}` and `/notifications` — two of the targets `pathForKind` stamps — had no route on web and 404'd; both now forward to their canonical URL (`/clubs/{slug}/events/{id}`, `/u/{id}?tab=notifications`), as does `/clubs/{id}` → `/clubs/{slug}`. Pinned anonymously by `tests-e2e/clubs/id-deep-links.spec.ts`, which asserts the resolved page's title so a forward that lands on the right URL with no data behind it fails (see native_push.md). |

## Social — following + activity feed

Strava-style follow graph; layered on top of (but independent of) the clubs/events social model. See [`decisions.md § 31`](../architecture/decisions.md#31-following-graph-is-its-own-social-layer-profiles-are-public-by-default), and roadmap § Competitor-parity backlog.

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Follow / unfollow another user | ✓ | Partial | ✓ | N/A | N/A | `user_follows` join table; Follow button on `/u/[id]` (web) and the profile screen header (android). |
| Public user profile page | ✓ | Partial | ✓ | N/A | N/A | `/u/[id]` (web) and `ProfileScreen` (android) show display_name, avatar, recent public runs, follower/following counts. Reachable from Settings → View profile on android. Every mobile avatar renders through ui_kit's `IdentityAvatar`, which layers the uploaded picture over the hue-derived initial so a failed load degrades to the letter rather than an empty disc (decisions §492). |
| Profile avatar upload | ✓ | Partial | ✓ | N/A | N/A | Web: `/settings/account` avatar control → `uploadAvatar`/`removeAvatar` (`data.ts`). Mobile: the Profile-photo tile on `settings_account_screen.dart` → `ApiClient.uploadAvatar`/`removeAvatar` (byte-identical twin). Both pick → strip EXIF/GPS client-side → remove-then-insert into the public `avatars` bucket (`{user_id}/avatar.{ext}`) → public URL written to `user_profiles.avatar_url`. Before this, avatars were OAuth-only. See `decisions.md § 157`. |
| Activity feed (followed users' runs) | ✓ | Partial | ✓ | N/A | N/A | Web: lives as the **Feed** tab under `/social` (the top-level Social hub) per `decisions.md § 61` — `SocialFeed.svelte` fetches via `fetchFollowingFeed`, 14-day window, cursor-paginated on `(runs.started_at desc, id desc)` 20-per-page. Activity-segmented filter + 14-day-window hint; cards link to `/runs/[id]`. The legacy `/feed` URL redirects to `/social?tab=feed`. Android: `screens/feed_screen.dart` reachable from the Dashboard AppBar (feed icon); same 14-day window via `ApiClient.fetchFollowingFeed`. |
| Activity feed — lift cards (public gym workouts) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4, shipped 2026-06-12.** The feed merges public runs + public gym workouts via `fetchFollowingActivityFeed` (runs through the redacted `public_runs` view, lifts through the redacted `public_gym_workouts` view — both base tables are owner-only, so only `title` / `set_count` / `volume_kg` are projected, never notes / metadata / RPE; a client that reads `gym_workouts` directly gets an empty page, not an error, which is what made #527 silent), one cursor-paged window. Web `SocialFeed.svelte` renders a distinct lift card (title + set count + total volume + handle + date) + a `Lift` filter chip; the card links to `/share/workout/[id]`. Mobile `feed_screen.dart` mirrors it (`_LiftEntryCard` + `Lift` chip over the `ActivityFeedEntry` sealed union). Engagement (kudos / comments) stays run-only. |
| Gym workout sharing (public toggle + share page) | ✓ | Partial | ✓ | N/A | N/A | **Phase 4, shipped 2026-06-12.** Bidirectional public/private toggle (`setGymWorkoutPublic`, stamps `last_modified_at`) + a copy-share-link action on web `/gym/[id]`; a read-only, logged-out-friendly public detail at `/share/workout/[id]` (anon reads via the `gym_workouts` owner-or-public RLS; only headline + per-set columns fetched). Mobile mirrors the toggle on `gym_detail_screen.dart` (offline-first via `LocalGymStore.updateLocal`). |
| Find other runners (search + suggested) | ✓ | Partial | ✓ | N/A | N/A | Web: **People** tab under `/social` runs the `search_user_profiles(p_query, p_limit)` SECURITY DEFINER RPC (migration `20261015_001`) so name search can honour the per-user `discoverable_in_search` opt-out pref — `user_settings.prefs` has owner-only RLS so the join can't run client-side. Plus a Suggested-for-you list (members of viewer's clubs they don't already follow, ranked by shared-club count). Inline Follow toggle with optimistic flip. See `decisions.md § 61` + `data.ts#searchPeople` / `fetchSuggestedPeople`. Android: `screens/people_screen.dart` mirrors the web component; `ApiClient.searchPeople` calls the same RPC. **Opt-out toggle ("Show me in name search") on web `/settings/privacy` + matching SwitchListTile on mobile `settings_preferences_screen.dart` (byte-identical iOS twin)** — persona-hunt Round 3 finding Woman #2 closed. iOS shares the byte-identical Dart codebase per decisions §39. |
| Runners nearby (opt-in coarse-location discovery) | Partial | Partial | Partial | N/A | N/A | **Built on every client, gated OFF everywhere pending owner + CISO/counsel sign-off** — person-location is Art 9-adjacent, so the flag flip is a pre-deploy gate, not missing code (`decisions.md § 270` + `§ 701`). Backend: `discoverable_nearby` pref + the coarse `user_settings.discoverable_area` centroid (a geocoded city/area point rounded server-side to ~1 km, never live GPS) + the `discoverable_runners_near` SECURITY DEFINER reader, which centres on the CALLER's own area, applies every person-discovery filter, and returns only a coarse distance BUCKET (migration `20270424000005`). Web: the nearby list on `SocialPeople.svelte` + the area setter on `/settings/privacy`, behind `PUBLIC_ENABLE_NEARBY_RUNNERS`. Android/iOS (byte-identical twin): the nearby section on `people_screen.dart` + the `discoverable_nearby` switch and the `Your area` row on `settings_preferences_screen.dart` → `nearby_area_screen.dart` (place-search picker + "Forget my area"), behind `ENABLE_NEARBY_RUNNERS` (`nearby_flag.dart`, same accepted-affirmative set as web's `isTruthyFlagValue`). Both platforms' empty state links into Preferences — web an `<a>` to `/settings/privacy`, mobile an "Open Preferences" button through the `SettingsDestination` seam (`decisions.md § 710`), because the embedded People tab cannot construct that screen itself; the affordance is inside the gated section, so it is absent with the flag off like the rest of the surface. Every cell stays `Partial` until the flag is on in prod. Distances are ONLY the shared bucket bounds (`nearby.ts` ↔ `nearby.dart`, third copy in the SQL CASE) — never metres, never a pin. Watches N/A (no person-discovery surface). |
| Kudos on runs | ✓ | Partial | ✓ | N/A | N/A | `run_kudos` composite-PK join; one-tap toggle. RLS inherits from runs (`decisions.md § 32`). Web mounts `RunSocial` on three surfaces: the owner's `/runs/[id]`, the public `/share/run/[id]`, and — since issue #666 — the **non-owner branch of `/runs/[id]`**, which mounts the same `RunShareView` the share page uses when the viewer doesn't own a publicly-readable run. Android: `widgets/run_social_section.dart` mounts the kudos pill on `run_detail_screen.dart` (and `feed_screen.dart` cards already render their own kudos toggle); another runner's run opens `PublicRunScreen`. Optimistic + rollback on error. |
| Comments on runs (one-level threading) | ✓ | Partial | ✓ | N/A | N/A | `run_comments` with `parent_comment_id`; author + run-owner can delete; INSERT policy enforces shallow nesting (write gate is `private.is_run_visible_to` + the block filter, so owner / public / active-coach only — there is no follower-or-club run visibility). Web: the composer reaches a non-owner on `/share/run/[id]` and, since issue #666, on `/runs/[id]` itself — before that the canonical signed-in surface 404'd every run the viewer didn't own, so several e2e specs routed around it. Android: `widgets/run_social_section.dart` renders the thread on `run_detail_screen.dart` — top-level + replies bucketed client-side, "Reply" / "Delete" affordances gated by author/owner, composer + reply composer wired to `addRunComment`. |
| Privacy zones (clip start/end of public tracks) | ✓ | Partial | ✓ | ✗ | ✗ | `user_settings.prefs.privacy_zones` + `clip_track_for_user` SECURITY DEFINER RPC. Settings UI on `/settings/privacy` with MapLibre map picker. v1 known gap: `routes.start_point` (nearby search) still uses unclipped first waypoint — see `decisions.md § 33`. Android: `lib/privacy.dart` is a pure-Dart port of `privacy.ts` (`PrivacyZone`, `clipPointsToZones`, `isInAnyZone`); `screens/privacy_zones_screen.dart` is a `flutter_map` picker — tap-to-add with a radius slider, tap-marker to remove, Save persists to `user_settings.prefs.privacy_zones` via `SettingsService.updateUniversal`. Reachable from Settings → **Preferences → Privacy & sharing → "Privacy zones"**, matching web's `/settings/preferences`; it sat under Settings → Account until issue #666 I11. |
| Plan templates (club-owned, clone-on-adopt) | ✓ | Partial | ✓ | N/A | N/A | `training_plans.is_template + parent_template_id + club_id` + `clone_plan_template` RPC. Templates tab on `/clubs/[slug]`; "Start from a template" picker on `/plans/new`; Publish-as-template control on `/plans/[id]`. See `decisions.md § 35`. Android: 4th "Templates" tab on `club_detail_screen.dart` lists templates owned by the club; per-row Adopt button calls `clone_plan_template` and pushes the new personal plan's detail screen. A **"Start from a club template" picker** also sits atop the new-plan wizard (`plan_new_screen.dart` — fetches every adoptable template across the viewer's clubs, bottom-sheet picker → `clonePlanTemplate`), mirroring web's `/plans/new` picker. **Publish-as-template** action lives on `plan_detail_screen.dart`'s AppBar — opens a picker of the viewer's admin-able clubs (filtered via `adminClubsForPublish`), then drives `TrainingService.publishPlanAsTemplate` which mirrors the web `data.ts#publishPlanAsTemplate` multi-INSERT path (no server-side `publish_plan_as_template` RPC exists; the Dart RPC reference + the parameter-prefix bug on `clonePlanTemplate` were both fixed in the same change). |
| Public plan library (anyone-can-clone) | ✓ | Partial | ✓ | N/A | N/A | `training_plans.is_public_template` (orthogonal to the club-scoped `is_template + club_id`) + `clone_public_plan` RPC (authorises on public visibility, not club membership; strips publisher fitness data + auto-completes the caller's active plan). Migration `20270126_001`. **Web ✓:** `/plans/library` (search + author handle + preview) → `/plans/library/[id]` clone; owner-only Publish-to-library / Unpublish on `/plans/[id]` (copy semantics; source plan untouched). **Android + iOS** (byte-identical twin): `PlanLibraryScreen` + `PlanLibraryPreviewScreen` from the plans-list Browse-library action; publish/unpublish AppBar action on `plan_detail_screen.dart`. Data layer `fetchPublicPlanLibrary` / `clonePublicPlan` / `publishPlanToLibrary` / `unpublishFromLibrary` / `fetchMyPublishedPlans` (web `data.ts` ↔ mobile `training_service.dart`). Additive RLS lets any authenticated user read a public template's weeks/workouts; non-public plans stay private. Tests: pgTAP `clone_public_plan_test.sql`, Playwright `public-library.spec.ts`, Flutter `plan_library_screen_test.dart`. **Watches N/A** (no plan-authoring surface). |
| Training-load curves (Fitness / Fatigue / Form) | ✓ | Partial | ✓ | N/A | N/A | `lib/training_load.ts` (TRIMP when HR available, distance fallback) + `TrainingLoadChart` mounted on `/dashboard`. Server-side recompute job still pending. See `decisions.md § 34`. Android: `lib/training_load.dart` is a pure-Dart port (same TRIMP / distance-proxy ladder, same EWMA halflives 7 / 42); `widgets/training_load_chart.dart` is a `CustomPaint` chart mirroring the SVG layout (Fitness / Fatigue / Form lines + zero line + reading). Mounts on the dashboard between the Fitness card and the All-Time stats; HR prefs sourced from the synced `SettingsService` (`resting_hr_bpm` + `max_hr_bpm`). |
| Training-load ramp (own ACWR + injury-risk band) | ✓ | Partial | ✓ | N/A | N/A | `lib/training/self_load.ts` composes the existing `coach_load.ts` ACWR policy (`acwr` / `injuryRiskBand` / `loadTrend`) over the runner's OWN recent runs, surfaced as `LoadRampCard` on `/dashboard`. Until now the band existed only on the coach roster (`/coaching`, about other people's athletes) and inside `plan_ramp`'s hypothetical-plan check — the runner it describes could not see it, and most runners have no coach. Distinct from the readiness ring (today: form + sleep + resting HR) and from the Fitness/Fatigue/Form trio (EWMA curves): ACWR is the last month's ramp. Graded on distance rather than the roster's `km×10` stress proxy — the proxy is linear so the quotient is identical, pinned by a unit test so a non-linear stress change fails loudly. Counts DNF distance (diverging from the roster, per `decisions.md § 592`); self-hides below `MIN_ACTIVE_WEEKS` rather than dividing by a base that isn't there. **Mirrored onto the phone 2026-08-18**: `self_load.dart` (a registered parity pair, 15 mirror tests each) grading through the existing `coach_load.dart` twin, rendered by `widgets/load_ramp_card.dart` on the mobile dashboard; the reduction is imported from `plan_ramp.dart` rather than copied, so the wizard's ramp check and the dashboard card cannot read the same runs differently. See `decisions.md § 609` + `§ 670`. |
| Comeback load (first weeks back vs pre-break base) | ✓ | Partial | ✓ | N/A | N/A | `lib/training/comeback.ts` grades a runner returning from a layoff against their OWN weekly average before the break, surfaced as `ComebackCard` on `/dashboard`. Exists because the ACWR row above deliberately refuses to grade a near-empty chronic month — correct, but silent in the case that most warrants a signal. Fires only where `selfLoad` is silent (same `MIN_ACTIVE_WEEKS` gate read the other way), so the two cards are mutually exclusive by construction; pinned by a unit test and by a Playwright assertion that `load-ramp` has count 0 on a comeback dashboard. A break is `training_load.ts`'s `kLayoffResetDays` (28) reused, not a second constant; a base older than 365 days grades nothing rather than reassuring off a stale average; a week with no running is not graded at all. **Mirrored onto the phone 2026-08-18** alongside the ACWR row it is the complement of: `comeback.dart` (a registered parity pair, 21 mirror tests each) + `widgets/comeback_card.dart`, both cards fed one conversion of the runner's runs so the two windows can never come from different run sets, and a widget test pins that neither card renders where the other does. See `decisions.md § 612` + `§ 670`. |
| Multi-distance race-time predictor | ✓ | Partial | ✓ | N/A | N/A | Web ✓: `lib/training/race_predictor.ts` (`predictRaceLadder` — projects the 5K/10K/Half/Marathon ladder from recency-weighted qualifying efforts; anchor by recency-weighted Riegel equivalence, per-rung confidence via `predictionConfidence`) + `RacePredictorCard.svelte` mounted on `/dashboard` (self-hiding on the `qualifyingRuns` gate). Distinct from the single-distance plan-detail `RaceDayPanel`. **Android ✓** (drift correction 2026-07-12 — the mobile card had shipped but the cell was never flipped): `widgets/race_predictor_card.dart` mounts on `dashboard_screen.dart` below `FitnessCard`, feeding the `race_predictor.dart` twin (TS↔Dart parity pair, 11 mirror tests each) the qualifying efforts; widget-tested in `race_predictor_card_test.dart`. Watches N/A. |
| Photos on runs | ✓ | Partial | ✓ | N/A | N/A | `run_photos` table + public-read `run-photos` Storage bucket. RunPhotos.svelte gallery on `/runs/[id]` (owner: upload + caption + delete) and `/share/run/[id]` (read-only). Android: `widgets/run_photos.dart` mirrors web — owner-gated upload via `image_picker`, lightbox view via the shared `widgets/photo_lightbox.dart` (loader while the full-size bytes are in flight, an explicit error affordance on an expired signed URL), caption edit, delete. Mounted on run_detail + public_run screens. RLS inherits from runs via EXISTS. See `decisions.md § 36`. |
| Photos on routes | ✓ | Partial | ✓ | N/A | N/A | `route_photos` table + private `route-photos` Storage bucket (migration `20270114_001`, backlog C1). RoutePhotos.svelte gallery on `/routes/[id]` (owner: upload + caption + delete) and `/share/route/[id]` (read-only). Android: `widgets/route_photos.dart` mirrors web — owner-gated upload via `image_picker`, EXIF stripped client-side before upload, the shared `photo_lightbox.dart` viewer, caption edit, delete; mounted on route_detail + public_route screens. SELECT gated on `private.is_route_visible_to`; `deleteRoute` sweeps photo blobs before the row delete (web + mobile). Deferred: server-side EXIF/thumbnail worker (no route-photo job handler yet — client strip is the active protection). |
| Photos on clubs (gallery) | ✓ | Partial | ✓ | N/A | N/A | `club_photos` table + private `club-photos` Storage bucket + the `club_photo_process` EXIF/thumbnail worker (migration `20270301_001`, roadmap row 8, decisions §190). A Photos tab on the club detail page: web `ClubPhotos.svelte` (`/clubs/[slug]?tab=photos`), Android `widgets/club_photos.dart` mounted on `club_detail_screen` (shared `photo_lightbox.dart` viewer). **Any active member uploads** (client-side EXIF strip → private bucket), **photo owner edits caption**, **owner-or-admin deletes** (moderation). SELECT gated on club visibility (public club → anyone incl. anon; private → owner/active-member). pgtap `rls_club_photos_test.sql` (17). **Privacy audit is a pre-prod gate.** |
| Treadmill (BLE FTMS indoor source) | ✓ | Partial | N/A | N/A | N/A | FTMS service `0x1826` / Treadmill Data `0x2ACD` reader (`ble_treadmill.dart`) → `TreadmillSample` stream + status/reconnect mirroring `ble_heart_rate.dart`; pairing tile in Settings → Integrations; an **additive opt-in** distance seam in `packages/run_recorder` (`setTreadmillSample` / `clearTreadmillMode` — belt distance overrides GPS only in treadmill mode, the GPS L0/L1 + auto-pause path is untouched, paused belt-advance excluded, lap splits use belt distance); `stop()` tags `metadata.indoor_source='treadmill'`. **Live run-screen mode toggle shipped** (`run_screen.dart` `_toggleTreadmillMode`, gated on a paired belt, L4 opt-in — the belt is the app-owned `BleTreadmill` singleton paired in Settings, read by the run screen, mirroring the HR strap). Web N/A — not a recording surface (decisions §24). Backlog C3. |
| Segments + leaderboards (route-anchored, v1) | ✓ | Partial | ✓ | N/A | N/A | `segments (route_id, name, start_distance_m, end_distance_m, …)` + `segment_efforts` with `unique(segment_id, run_id)`. SegmentsPanel on `/routes/[id]`; RunSegmentEfforts on `/runs/[id]`. Auto-effort generation is client-side: `lib/segments.ts#computeEffortFromTrack` walks the run track on the run-detail page. Arbitrary-geometry segments deferred to v2. See `decisions.md § 37`. Android: `widgets/run_segment_efforts.dart` on `run_detail_screen.dart` (per-run effort chips with rank pills, owner-side auto-compute via `lib/segments.dart`); `widgets/segments_panel.dart` on `route_detail_screen.dart` (segments list with expand-on-tap leaderboards, owner-only create form + delete). **The rank pill is nullable on both platforms** ([decisions § 746](../architecture/decisions.md)): the rank arrives from `segment_effort_ranks` separately from the effort row, so an effort the RPC did not answer for renders a neutral placeholder with a localized accessible name rather than `#1` — which both clients used to draw, turning an unanswered call into a crown. No cell moves; the capability is unchanged on every platform. |
| Segments v2 — tiered leaderboards (gender / age band) + KOM/QOM crowns | ✓ | Partial | ✓ | N/A | N/A | `user_profiles.gender` + `date_of_birth` (nullable, opt-in via Settings → Demographics) + `segment_leaderboard_tiered(segment_id, gender, age_band, limit)` SECURITY INVOKER RPC that joins profiles server-side and filters with Strava's 5-year bins (`SEGMENT_AGE_BANDS` — `18-19`…`75+`). Web: dropdowns in `SegmentsPanel.svelte` re-fire the RPC; demographics fields land on `/settings/body`; gold trophy renders on the rank-1 row with a `crownLabel(gender, age)` tooltip plus a "You hold this crown" banner when the viewer leads the active tier. Android: same dropdowns + crown row in `widgets/segments_panel.dart` (mobile twin); demographics writeback from `settings_screen.dart` is the next chunk. Arbitrary-geometry HMM/Hausdorff segment matching + live-effort-during-run still deferred. Migration `20260829_001_segments_v2_tiered_leaderboards.sql`. |
| Global / famous-segment catalogue (route-less) | ✓ | Partial | ✓ | N/A | N/A | Persona-hunt finding (runner-strava-migration). Free-standing `global_segments` (own polyline geometry, world-readable, `app_admins`-curated) + `global_segment_efforts` under the same run-privacy rules as `segment_efforts` (`private.is_run_visible_to` in RLS; `is_blocked_either_way` in the leaderboard RPC). Matches a run END-TO-END against curated geometry — `computeGlobalSegmentEffort` (the `segments` TS↔Dart parity pair) reusing `computeEffortFromTrack`, 35 m tolerance + 25 %-of-length guard; no route needed, so imported (`route_id` null) runs finally match. Web: run-detail "Famous segments" chips in `RunSegmentEfforts.svelte` (owner-only backfill on mount; now mounted even for route-less runs) linking to a new `/segments/[id]` detail page (public metadata + geometry map + block-guarded gender/age-filterable leaderboard — times/ranks only, never another athlete's GPS track). Migration `20270411_001`; `global-segment-catalogue-journey.spec.ts` + `global_segments_test.sql`. Mobile carried the `computeGlobalSegmentEffort` twin in `segments.dart` with no catalogue UI until 2026-08-19. **Browse surface shipped 2026-08-13** ([decisions § 593](../architecture/decisions.md)): `/segments` lists the active catalogue with accent-folded search, region + surface filters and four sorts over the web-only `segments/catalogue_browse.ts`; the run-detail block and `/segments/[id]`'s back-link (was `/dashboard`) both route into it. Reachable from the run-surface sub-tab strip (Runs · Routes · Segments · Plans · Races) and listed in `sitemap.xml`; the page is anon-reachable, matching `global_segments`' own anon select grant. Individual `/segments/[id]` pages stay out of the sitemap (a leaderboard of named runners is the people-directory objection that keeps profiles out) — see [decisions § 677](../architecture/decisions.md). See `decisions.md § 232`. **Android + iOS (byte-identical twin) mirrored the browse surface 2026-08-19** ([decisions § 693](../architecture/decisions.md)): `screens/global_segments_screen.dart` lists the catalogue with the same accent-folded search, region + surface filters and four sorts over the new `catalogue_browse.dart` parity pair, reachable from the Fitness hub and from the run-detail famous-segment chips (which previously linked nowhere on the phone), plus a `GlobalSegmentDetailScreen` carrying the curated geometry as a `TrackPreview` (public catalogue geometry, not an athlete track, so nothing for `clipTrackForUser` to clip) and the same block-guarded gender / age-band leaderboard with its crown label. **The catalogue chips' ranks were unreadable logged-out until 2026-08-26** ([decisions § 746](../architecture/decisions.md)): `global_segment_effort_ranks` was granted to `authenticated` only while both catalogue tables carry an `anon` select grant, so an anonymous reader of a public run got the effort rows and a 42501 on the ranks — and the `?? 1` degrade drew a crown on every one. Fixed by `20270609_001` plus the nullable rank. |
| Direct messages (1:1) | ✗ | ✗ | ✓ | N/A | N/A | Persona-hunt finding #55 (very-social). `direct_messages` table (migration `20261026_001`) — a thread is the unordered participant pair. RLS gates sending on no-block-either-way (`is_blocked_either_way` SECURITY DEFINER helper, since a plain `user_blocks` subquery is hidden from the sender by owner-read RLS) **and** an existing follow relationship in either direction, so cold strangers can't DM. A `message` notification fires to the recipient on the first unread message of a burst. Web: `/messages/[[id]]` two-pane (thread list + conversation + composer), a "Message" button on `/u/[id]` profiles, and the kind renders in the bell + inbox (links to `/messages/{actor}`). `fetchDmThreads` / `fetchDmThread` / `sendDm` / `markDmThreadRead` in `data.ts`. 6-assertion pgtap (`direct_messages_test.sql`) + Playwright (`messages/dm.spec.ts`). A nullable `route_id` FK carries a typed route attachment (migration `20270619_001`, decisions §772) — see the route-send row above; `direct_message_route_attachment_test.sql` pins its INSERT gate. Realtime delivery + a non-follower "message requests" inbox deferred. The send throttle (30/min + 250/h) is enforced by a DB trigger, so it already covers any future client and earns no per-platform cell — the device columns are `✗` for the surface, not for the rail (decisions § 737). Mobile deferred. |
| Notifications inbox (kudos / comments / replies / follows / event RSVPs / cancels / plan updates / messages / club posts / completed runs) | ✓ | Partial | ✓ | N/A | N/A | `notifications` table populated by SECURITY DEFINER triggers on `run_kudos`, `run_comments`, `user_follows`, `event_attendees`, `event_exceptions`, `plan_workouts`, `direct_messages`, `club_posts`, and `runs`. The two community fan-outs (migration `20261101_001`, persona #38): `club_post` notifies every active club member except the author; `run_completed` notifies a runner's followers for a public run started in the last 24 h (recency gate stops a bulk import from exploding inboxes — `decisions.md § 96`). Web: sidebar bell with unread badge + popover; full inbox lives under the Notifications tab on `/u/[me]`; `club_post` links to `/clubs/[slug]`, `run_completed` to `/share/run/[id]`. Android: same Notifications tab on `ProfileScreen` (own profile only) with All / Unread filter, mark-all-read, per-row dismiss; tapping `event_rsvp` / `club_post` / `run_completed` rows navigates into `EventDetailScreen` / `ClubDetailScreen` / `PublicRunScreen`. Email delivery of these rows ships (migration `20261130_001`, `decisions.md § 117`) — gated on `email_notifications` (default `important`); native push (FCM/APNs) deferred per roadmap Phase 4b. **`data_export_ready` joined the kind set 2026-08-25** (migration `20270607_001`, `decisions.md § 729`) — the one kind with no source trigger and no FK: the Go worker's `data_export` handler calls `notify_data_export_ready()` once a queued Art 20 export is `ready`, and every surface deep-links to `/settings/account` rather than to a signed URL, because the URL is minted when the subject arrives. Both clients render it (web bell + inbox, mobile Notifications tab, whose tap parks `SettingsDestination.account`); the per-kind **mute** (`notify_data_export_ready`, opt-OUT, default on) is web-only per § 24 — mobile's Preferences screen enumerates channels and engagement streams, not kinds. See `decisions.md § 38`. **`refund_failed` joined the kind set 2026-08-31** (migration `20270701000001`, `decisions.md § 825`) — the first kind fired from a **money ledger**: one `after update of status` trigger on `event_orders` and one on `donations`, on the transition only. Both clients render it (web bell + inbox, mobile Notifications tab); the order arm carries `event_id` and taps through to the event page, the donation arm carries **no FK** — `donations` has no client SELECT policy, so there is no donor-readable row on any platform — and renders as plain text with the outbound CTA falling back to the inbox. No per-kind mute, deliberately. This kind is the **only** surface a donor has, and on mobile the only one a buyer has: there is no paid-registration surface there at all (club_events.md P3). See `decisions.md § 38`. |
| Safety-contact finish alerts | ✓ | Partial | ✓ | N/A | N/A | Opt-in safety contacts emailed when their owner finishes a run — **even a private one** (migration `20261218_001`, `decisions.md § 131`). Distinct from `run_completed` (which stays gated on `is_public` + followers). `safety_contacts` table + double opt-in: owner adds by email; the contact confirms in-app (`my_pending_safety_requests` + `confirm_safety_contact`) or via an emailed link (`confirm_safety_contact_by_token`). A `runs` AFTER INSERT trigger enqueues a `safety_email` `finish` job per **confirmed** contact regardless of `is_public` (24h recency guard; **not** gated on the runner's `email_notifications` pref). Web: Settings → Safety (`/settings/safety`) add/confirm/remove + the logged-out `/safety/confirm` page; pgtap (`safety_contacts_test.sql`) + Playwright (`settings/safety.spec.ts`). Mobile: Settings → Safety contacts (`settings_safety_screen.dart`, byte-identical twin) add/confirm/remove + incoming-request confirm/decline; `ApiClient` safety methods + `SafetyContact`/`PendingSafetyRequest` DTOs; widget tests (`settings_safety_screen_test.dart`) + DTO tests (`safety_contact_dto_test.dart`). Both clients scope the list read AND the remove to `owner_id` explicitly rather than leaning on RLS (ADR §720) — the table's linked-contact SELECT/DELETE policies are permissive, so an unfiltered read returned the contact-of union and an id-only delete destroyed the other party's row. The email-link `/safety/confirm` page stays web-only (no mobile deep-link route). Native push to a locked phone still waits on the FCM/APNs leg. |
| Safety net — auto live share + overdue escalation | ✓ | Partial | ✓ | N/A | N/A | The mid-run half of the safety story ([safety.md](../features/safety.md), decisions §207; persona-woman CRITICAL). Mobile: `auto_live_share` device pref starts the live broadcast at `_begin()` (L4, banner disclosure, shares the manual path's `_startLiveBroadcast`). Backend: `enqueue_safety_overdue_emails()` pg_cron (migration `20270401_001`) emails every confirmed safety contact once when a live-broadcast run goes silent past the runner's `safety_overdue_minutes` pref — `safety_email` `overdue` template (six locales), times + live link only, `metadata.safety_escalated_at` idempotency stamp; also fixed the live-stub INSERT mis-firing the finish/run_completed notifiers (they now fire on the stub→saved transition). Settings: web `/settings/safety` overdue select + mobile Settings → Safety contacts (overdue dropdown + auto-share switch). The old Account-card trusted-contacts scaffold is deleted on both platforms (key deprecated in settings.md). Web column = the settings surface + spectator page (web doesn't record). pgtap `safety_overdue_test.sql` (12) + Go handler tests + widget tests + Playwright.
| Solo-run safety nudge (after-dark, no live share) | ✓ | Partial | N/A | N/A | N/A | Closes the "off-route / no-live-share solo run has no safety net" gap ([safety.md](../features/safety.md), decisions §235; persona-woman HIGH). When a run starts after dark — the fixed 20:00–06:00 window OR a seasonal latitude+date sunrise/sunset test (`isSunDown`) that catches dark pre-dawn winter runs at higher latitudes, issue #265 — with `auto_live_share` off and no manual broadcast, the run screen surfaces a one-time, throttled (`safety_nudge_dismissed_at`, 30-day), dismissible top-banner nudging a live-link share (action shares the current run's link). Pure decision is the twinned `safety_nudge.ts`↔`.dart` (`shouldNudgeSoloSafety` + `isDarkOutside` + `isNightWindow` + `isSunDown` + `nudgeThrottled`, 24 mirror tests each); the banner is the L4 mobile-only surface (a failed daylight/pref read never touches recording). N/A on web (web doesn't record) and the watches. |
| Safety net — off-route → auto-notify trusted contact | ✓ | Partial | ✓ | N/A | N/A | Feature D ([safety.md](../features/safety.md) § feature D, decisions §241; persona-woman Medium). The off-route leg of the safety net: a sustained departure from the planned route on a live-shared run escalates ONCE to confirmed contacts via the existing notify path. Detection is the twinned pure `OffRouteAlertDetector` (`off_route_alert.ts`↔`.dart`, debounce > 75 m for ≥ 90 s, latches once-per-run, 10 mirror tests each); the run screen fires `escalate_run_off_route` (migration `20270414_001`) which enqueues the `safety_email` `off_route` template (six locales) + additive `safety_sms`, sharing the once-per-run `metadata.safety_escalated_at` stamp. **Fail-closed / prod-gated:** the client only calls when the `OFF_ROUTE_ESCALATION_ENABLED` deploy flag + the `safety_off_route_alerts` opt-in are on, a route is selected, and a live broadcast is active; the RPC re-checks every gate server-side. Android/iOS: detector + run-screen wiring + banner + the flag-gated toggle on Settings → Safety contacts (iOS byte-identical twin). Web ✓: the same opt-in toggle on `/settings/safety` (web doesn't record, so no trigger). pgtap `safety_off_route_test.sql` + Go handler/i18n tests + Flutter widget + mirror tests. |
| Safety net — trusted-contact SMS + per-run "not back by X" | ✓ | Partial | ✓ | N/A | N/A | Feature C ([safety.md](../features/safety.md) § SMS escalation, decisions §240 + §719). Backend (cross-platform): `safety_contacts.contact_phone` (E.164 CHECK) + a contact-controlled `sms_opt_in_at`, a `safety_sms` `jobs` kind with a **fail-closed** Twilio transport (nil unless `SMS_PROVIDER=twilio` + all `TWILIO_*` set; handler no-ops when nil), and `set_run_expected_return` writing `runs.metadata.expected_return_at` (migration `20270410_001`). The overdue scan sends SMS **only** to a confirmed contact who opted in — additive to the always-sent email. Web ✓: optional phone + "SMS on" badge + contact-side opt-in on `/settings/safety`, the prompt-then-confirm `/safety/confirm` token page, and an owner-only "Not back by X" control on `/live/[id]`; pgtap + Go handler tests + Playwright (`settings/safety.spec.ts`). **Android ✓ (2026-08-24, §719):** the optional phone on Settings → Safety contacts (normalised through the new `e164` parity pair, so a pasted `+44 (0) 7700 900123` is repaired rather than refused), the three-state SMS rendering — mobile says outright when a stored number has no contact opt-in, which web renders as silence — the confirm-time opt-in offered only when the requester stored a number, and the "Not back by X" ladder on the live-share sheet (recording is mobile-only, so this is the platform the control belongs on). Flutter widget tests + the mirror `e164` suites. Changing an SMS consent after confirming shipped 2026-08-25 on web + Android (§726) — see the row below. Native-push leg still pending. |
| Submit content reports (moderation) | ✓ | Partial | ✓ | N/A | N/A | `reports` table + `submit_report(target_kind, target_id, reason, notes)` SECURITY DEFINER RPC. Fixed reason set: `spam` / `harassment` / `inappropriate` / `impersonation` / `other`. Unique `(reporter_id, target_kind, target_id)` constraint prevents stacking — second attempt raises 23505. P0001 raised when the per-user rate-limit bucket trips. Web: report buttons on `/u/[id]`, `/clubs/[slug]`, `/share/route/[id]` open the same modal. Android: `lib/widgets/report_sheet.dart` exposes `showReportSheet(context, api:, targetKind:, targetId:)` — flag IconButton in the AppBar of `profile_screen.dart` (when not self), `club_detail_screen.dart`, and `route_detail_screen.dart` (when not owner). Same fixed reason set, same friendly copy for 23505 / P0001 paths. Notes are trimmed; empty notes pass `null` (not `""`) so the moderator dashboard's "had context" filter stays accurate. **Backlog E2 widened the reportable target kinds** to {user, club, route, comment, club_post, run} (migration `20270115_001` — `target_kind` CHECK + the `ReportTargetKind` TS union, now a CHECK↔union guard pair; `submit_report` validates the new targets). Web added Report on club-feed posts + runs (run-detail + share-run); mobile added it on club posts + the public-run screen (byte-identical iOS twin). **`route_review` added as a reportable kind** (migration `20270402_001`): a flag on each route-review card, gated non-author + signed-in, on web `/routes/[id]` (`ReportDialog targetKind="route_review"`) + mobile `route_detail_screen.dart` (`showReportSheet targetKind: 'route_review'`); `submit_report` blocks a self-report (reporter == `route_reviews.user_id`). |
| Admin moderation queue (back-office) | ✗ | ✗ | ✓ | N/A | N/A | **Web-only** back-office tooling ([decisions §142](../architecture/decisions.md#142-admin-authorization-is-db-enforced-via-app_admins--a-privateis_admin-oracle-never-client-side-route-gating)) — no device twin by design (§24). `/admin/reports`: a moderator triage queue over the `reports` table. `app_admins (user_id)` allow-list + `private.is_admin(uid)` oracle back four admin-gated RPCs (`am_i_admin`, `fetch_pending_reports`, `fetch_reports_for_target`, `resolve_target_reports`); every report RPC hard-denies a non-admin with `42501` server-side (the web SPA can't gate on the client). Queue = one row per reported target (kind + link, reason chips, reporter count, latest-reported time) → detail modal listing every report → resolution note + Mark reviewed / Dismiss (ConfirmDialog-gated). Triage-only; no content takedown in v1. Migration `20270104_001_admin_moderation.sql`; pgtap `admin_moderation_test.sql` (admin-allowed vs non-admin/anon DENIED) + Playwright `tests-e2e/admin/reports.spec.ts`. |
| Block another runner (self-defence) | ✓ | Partial | ✓ | N/A | N/A | `user_blocks` table + `block_user(p_target, p_reason?)` / `unblock_user(p_target)` SECURITY DEFINER RPCs + `is_blocked_either_way(a, b)` predicate. Symmetric: a block in either direction suppresses follows / kudos / comments / share-page unfurls for both. `block_user` also drains existing follow rows in both directions in the same transaction so the block subsumes unfollow. Web: icon-only Block / Unblock IconButton next to the Report flag on `/u/[id]` (`btn-block` class, aria-pressed reflects state, ConfirmDialog gates the destructive direction, unblock is one-tap). Android + iOS: matching IconButton on `profile_screen.dart`'s AppBar with an AlertDialog confirm; tinted red when active. Backend: migration `20261012_001_user_blocks.sql` + pgtap suite in `apps/backend/supabase/tests/user_blocks_test.sql`. |
| Region-aware club search (geocode + ST_DWithin) | ✓ | Partial | ✓ | N/A | N/A | `clubs.location_point` + `search_clubs(p_query, p_center_lng, p_center_lat, p_radius_m, p_limit)` SECURITY INVOKER RPC (migrations `20260905_001`, `20260906_001`). Combines ILIKE on name + location_label with a PostGIS ST_DWithin filter on `location_point`. Web: `searchClubs` in `data.ts` geocodes via MapTiler then calls the RPC, so a query like "Virginia" pulls clubs in the state even when their `location_label` is "Richmond, VA" without the state name. Android: `lib/geocoding.dart` adds `geocodePlace(query, apiKey)` + `bboxRadius` + `haversineM` (mirrors web `geocoding.ts` + `geocoding_math.ts`); `SocialService.searchClubs(query, mapTilerKey)` runs the same geocode-then-RPC flow with a graceful ILIKE fallback when the RPC errors. `clubs_screen.dart`'s Browse tab routes non-empty queries through `searchClubs` and keeps `browseClubs()` for the empty / default path. |
| Create a challenge | ✓ | Partial | ✓ | N/A | N/A | Authoring surface, web-canonical per [§ 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive) — the mobile twin is additive, not drift. **Web ✓:** `ChallengeEditor.svelte` (title / metric / scope / goal / activity type / club / date window) reached from the modal on `/challenges` and from the standalone `/challenges/new`; it is also the only surface that can EDIT an existing challenge. **Android + iOS ✓ (2026-08-26, [decisions § 754](../architecture/decisions.md)):** `widgets/challenge_form_sheet.dart` carries the same create field set behind a "Create challenge" FAB — hoisted by `SocialScreen` for the Challenges sub-tab exactly as it hoists Clubs' "New club", and rendered by `challenges_screen.dart` itself on the standalone mount. `SocialService.createChallenge` existed in both twins with no caller anywhere until then, so the phone could browse and join a challenge but never author one. **Both clients now ask for the goal in the reader's OWN unit** and convert at the boundary through the `social/challenge_goal.ts` ↔ `challenge_goal.dart` parity pair (2026-08-27, [decisions § 758](../architecture/decisions.md)) — until then web asked for the stored number with no unit named, so a typed `100` on a distance challenge was a 100 **metre** goal. Both also echo the converted figure back through the leaderboard's own formatter (only where a conversion happened), state the window's active-day ceiling for a `streak_days` goal, clear a typed number when the metric changes, and name both `challenges_goal_ck` refusals plus `challenges_window_ck`'s end-after-start rule inline, because those 23514s name neither the goal nor a bound. One departure remains, mobile-only: the create throttle's P0001 goes through `rateLimitErrorMessage` before `friendlyError`, whose rate-limited branch would collapse the bucket and the wait into a generic sentence ([§ 747](../architecture/decisions.md)). Editing an existing challenge is still web-only. No backend work — the RLS insert policy, the throttle and the `rateLimitCreateChallenge` sentence in all seven ARBs already shipped. |
| Join / leave a challenge | ✓ | Partial | ✓ | N/A | N/A | **Web:** `doJoin` / `doLeave` on `/challenges/[id]`, the leave path gated by `ConfirmDialog`. **Android + iOS** (byte-identical twin): `_join()` / `_leave()` on `challenge_detail_screen.dart`, the leave path gated by `confirmDestructive`. Both write `challenge_participants`; the row is what later identifies the viewer's team on a club-vs-club board. |
| Challenge goal progress | ✓ | Partial | ✓ | N/A | N/A | Progress bar + fraction against the challenge's goal, off the `challenge_progress.ts` ↔ `challenge_progress.dart` parity pair (`progressFraction` clamps 0..1 and returns null for a goal-less challenge; `isComplete`). A DNF'd run contributes 0 on both clients, matching the server aggregate's `is_dnf = false` filter, so an optimistic client estimate can't drift from the board. **Web:** `ChallengeProgressBar.svelte`. **Android + iOS:** the shared `widgets/challenge_progress_bar.dart`, rendered by both `challenge_detail_screen.dart` and `challenges_screen.dart`. The detail screen takes the caller's value off the board it already holds rather than collapsing a null `myValue` to zero, matching web's `myRow?.value ?? challenge.my_value ?? 0` ([decisions.md § 694](../architecture/decisions.md)). |
| Challenge list — per-row progress signal | ✓ | Partial | ✓ | N/A | N/A | The joined-challenge rows on the challenges list carry the caller's own value against the goal, the bar, their rank and the earned badge — fed by the `my_active_challenges` aggregate, because the `challenges` read carries no per-caller value at all. Engine is the `social/challenge_list.ts` ↔ `challenge_list.dart` parity pair (`mergeMyProgress` + `myProgressView`, 18 web / 17 Dart mirror tests), registered in both parity registries. A joined challenge **outside** that RPC's live-plus-7-day window has no known value, and both surfaces say "Progress unavailable — open for your result" rather than drawing an unearned 0 % beside the goal; the one *known* zero is a window that has not opened yet ([decisions.md § 694](../architecture/decisions.md)). **Web:** the `myProgressView` branch in the My-challenges list on `/challenges`. **Android + iOS** (byte-identical twin): `_progressRow()` on `challenges_screen.dart`. Both render through the one shared progress-bar component. |
| Challenge on-pace hint | ✓ | Partial | ✓ | N/A | N/A | Ahead / on-track / behind against the even-pace line for a time-boxed goal challenge, plus the daily rate still needed to finish — `challengePace` from the same `challenge_progress` parity pair, a read-time re-shape of the leaderboard value with no extra query. **Web:** `ChallengeProgressBar.svelte`. **Android + iOS:** `_paceHint()` inside the shared `widgets/challenge_progress_bar.dart`, so the list rows carry the hint too. |
| Challenge leaderboard — individual board | ✓ | Partial | ✓ | N/A | N/A | The `challenge_leaderboard` RPC (value desc, dense rank); `rankParticipants` in the `challenge_progress` pair mirrors the SQL window so an offline client orders identically. **Web:** `ChallengeLeaderboard.svelte` on `/challenges/[id]`. **Android + iOS:** `_leaderboard()` on `challenge_detail_screen.dart`, fetched with `byTeam: false`. |
| Challenge leaderboard — club-vs-club board | ✓ | Partial | ✓ | N/A | N/A | A distinct board mode, not a filter: both clients derive it from `scope == 'club_vs_club'` and re-fetch the same RPC with the team flag set, then resolve club names for the rows through `teamLabel` (the `challenge_list` pair), which reports an unaffiliated group and a club RLS refused as distinct localised labels and never as the raw club uuid ([decisions.md § 694](../architecture/decisions.md)). The viewer's own entrant is the club they **joined under** — web's `my_team_club_id` (`joinedRow?.team_club_id`), mobile's `ChallengeView.myTeamClubId` — never whichever of their clubs happens to appear on the board, or a runner in two competing clubs is told the trailing team is leading ([decisions.md § 615](../architecture/decisions.md)). |
| Challenge completion badge | ✓ | Partial | ✓ | N/A | N/A | The challenge's own server-stamped `completed_at` (set by `recompute_challenge_completion`), **not** an entry in the `badges` achievement catalogue — so the two are separate surfaces and a challenge finish does not mint a trophy. **Web:** a `military_tech` badge chip on `/challenges/[id]`, gated on `completed_at`. **Android + iOS:** the `challengesBadgeEarned` line beside the bar on `challenge_detail_screen.dart` and beside the rank on `challenges_screen.dart`. |
| Challenge leaderboard — "Your standing" card | ✓ | Partial | ✓ | N/A | N/A | The viewer's own line above a challenge board: rank, board size, tie count, and the metric gap to the entrant immediately above and below ([decisions.md § 615](../architecture/decisions.md)). A pure re-shape of the rows the page already fetched — no extra query. Engine is the `social/leaderboard_standing.ts` ↔ `leaderboard_standing.dart` parity pair (`standingFor` + `entryKey`, 16 mirror tests each), registered in both parity registries. **Web:** the card above the board in `ChallengeLeaderboard.svelte` on `/challenges/[id]`. **Android + iOS** (byte-identical twin): the same card above the board on `challenge_detail_screen.dart`, keyed on `ChallengeView.myTeamClubId` for a club-vs-club board (the club the viewer JOINED under, off their own `challenge_participants` row — not whichever of their clubs tops the board). Both surfaces suppress the card entirely when the viewer isn't on the board and on a board of one, so neither ever renders "#1 of 1"; a tied leader reports the tie rather than claiming the lead. |

## Training plans and workouts

See [docs/features/training.md](../features/training.md) and [docs/features/workout_execution.md](../features/workout_execution.md). No features.md section yet.

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Guided audio runs — scripted cue library (browse + preview) | ✓ | Partial | ✓ | N/A | N/A | The library is a TS↔Dart parity pair — `lib/training/guided_runs.ts` ↔ `lib/guided_runs.dart`, 19 mirror tests each — holding three scripted workouts, each an id, a duration and an ordered list of cue second marks, plus `cuesDue` (the cues falling in `(prev, now]`, idempotent on a repeated tick) and `isGuidedRunValid`. Web browses at `/guided` and previews one script at `/guided/[id]`; Android mirrors both in `screens/guided_runs_screen.dart`, whose preview button speaks a single cue. **The pair is data, not an algorithm, so the two mirror suites cannot police it** — each tests its own rail against itself ("ids are unique", "a finish cue at exactly duration") and neither reads the other, so a mark moved or a workout renamed on one side passes both. `check_shared_constants.mjs`'s `guided-run cue library` entry is the rail that does: it reads the ids in order, each duration and each ordered cue list out of both files and fails the PR on any disagreement ([decisions § 801](../architecture/decisions.md)). The cue *text* is deliberately outside the lockstep — 37 library keys per web catalogue against 37 per ARB, key identifiers differing by convention (`guidedRuns.easy30.cue0` vs `guidedEasy30Cue0`), the `badges` carve-out; the 7 further `guidedRun*` ARB keys are mobile screen chrome with no web twin. Playing the cues during a recording is a separate row under Run recording. **Watches `N/A`** per [decisions § 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive). |
| Plans list | ✓ | Partial | ✓ | ✗ | ✗ | |
| Create plan (goal race, date, target time) | ✓ | Partial | ✓ | ✗ | ✗ | |
| Train for a race on the calendar (seed a plan from a race listing) | ✓ | Partial | ✓ | N/A | N/A | `racePlanPreset` (`apps/web/src/lib/training/race_plan_preset.ts`) sizes a plan so its final `race` week contains race day: a Sunday-aligned start (the generator anchors day 0 to the long run), weeks capped at `defaultPlanWeeks(goal)` rather than filling the gap, and a refusal (`past` / `too_soon` / `invalid`) instead of a squeezed plan. `RaceCalendarCard` shows the CTA only when a plan can be built; `/plans/new?raceDate=&raceDistance=&raceName=` re-derives against today and seeds goal + name + start date + week count. A distance matching no standard rung within 2 % preselects no goal event — the wizard has no custom-distance input, so a 50k shown as a marathon would be a plan for a different race. **Mirrored onto the phone 2026-08-18**: `race_plan_preset.dart` (a registered parity pair, 17 mirror tests each), a "Train for this race" action on the races screen shown under the same refusal gate, and `PlanNewScreen`'s `raceDateIso` / `raceName` / `raceDistanceM` props, which carry the listing's facts and re-derive against today exactly as the web URL does. See `decisions.md § 606` + `§ 671`. |
| Plan detail with weekly grid | ✓ | Partial | ✓ | ✗ | ✗ | |
| Plan detail with month-by-month calendar | ✓ | Partial | ✓ | ✗ | ✗ | Web `/plans/[id]` renders `PlanCalendar.svelte` above the weekly grid — projects every workout onto its real date, shades completed days, color-codes by kind. Android: `lib/widgets/plan_calendar.dart` is a Dart port (Monday-first grid, prev/next month chevrons, kind-coloured left border, target-distance row, completed tick), wired into `plan_detail_screen.dart` between the today card and the weekly cards. |
| Plan detail leads with today + this week, rest behind named expanders | ✗ | ✗ | ✓ | ✗ | ✗ | Web `/plans/[id]` only, 2026-09-18, [decisions § 1660](../architecture/decisions.md). Today's session card and the `CurrentWeekStrip` follow the hero directly (today's card used to be the eighth block on the page), with the current-week adherence flags and the race-week `RaceDayPanel` beside them; **Plan progress**, **Plan rules**, **Calendar**, **Week by week** and **Share & publish** are each a named `<details>` region whose open/closed state is remembered per account (`lib/util/disclosure_state.ts`, an account-keyed `localStorage` blob). All default open. **Mobile is the gap:** `plan_detail_screen.dart` still leads with the hero + progress ring and has no disclosure memory — issue #905 workstream 3 scoped the web half only, and the phone has no equivalent of the account-keyed blob today. Watches ✗ as with every other plan row — no plan-management surface on the wrist. |
| Edit per-day workouts | ✓ | Partial | ✓ | ✗ | ✗ | Android: each row in the weekly grid on `plan_detail_screen.dart` exposes an edit pencil (and accepts long-press) that opens `widgets/workout_edit_sheet.dart` — a modal sheet for kind, target distance (km), target pace (mm:ss /km), and notes. `TrainingService.updateWorkout` patches the row; the screen reloads on save. |
| Workout detail screen | ✓ | Partial | ✓ | ✗ | ✗ | |
| "Today's workout" card on home / dashboard | ✓ | Partial | ✓ | ✗ | ✗ | |
| Structured-workout execution loop (live rep targets) | ✓ | Partial | Partial | ✗ | ✗ | **Live execution loop shipped on Android.** `packages/run_recorder/lib/src/workout_runner.dart` is a pure state machine + step expander (13 unit tests in `workout_runner_test.dart`); `apps/mobile_android/lib/widgets/workout_execution_band.dart` mounts at the top of the run screen with progress bar, pace pip, skip / abandon controls. `audio_cues.dart` adds step-transition / halfway / last-50 m / pace-drift / complete utterances. Tap today's-workout card → "Start workout" loads the runner; on stop, `_stop` writes `plan_workout_id` + `workout_step_results` + `workout_adherence` to `runs.metadata`. Both `apps/mobile_android/lib/screens/run_detail_screen.dart` and the web `/runs/[id]` render a "Workout" review section (adherence pill + per-step planned-vs-actual rows with colour-coded pace delta) when those keys are set. **Planner shipped on web** (`/plans/new`, `/plans/[id]`, `/plans/[id]/workouts/[wid]`); the web execution loop itself is a physical exception (no record button). |
| Auto-link completed run to planned workout | ✓ | Partial | ✓ | ✗ | ✗ | Web: `autoMatchRunToPlanWorkout` runs inside `saveRun` / `createManualRun` so a freshly-saved run lights up the same-date plan workout (±25% target-distance window). Android: `ApiClient.saveRun` + `saveRunsBatch` run the same client-side match as web — a direct `plan_weeks`/`plan_workouts` query, no RPC — after the upsert, L4-best-effort wrapped (debugPrint on failure — never blocks the save). Covers manual entry (`AddRunScreen`), live recording (`RunScreen`'s `_stop` → `saveRun`), Strava ZIP / Health Connect ingest (`saveRunsBatch`), and watch hand-off replay (`WatchIngestQueue.replay` → `saveRun`). |
| Re-link a completed workout to a different run | ✓ | Partial | ✓ | ✗ | ✗ | **Shipped 2026-06-12 (Training Phase-3).** A workout-detail **Re-link** picker (run-linked workouts only) re-points `completed_run_id` at a different owner-scoped run. Candidates are scoped to the owner and within ±7 days of the scheduled date, EXCLUDING any run already linked to another workout so `plan_progress` can't double-count; the current pick stays visible. Eligibility is the `relink_candidates` TS↔Dart parity pair (`filterRelinkCandidates`); web `fetchRelinkCandidateRuns` (`core/data.ts`) + mobile `TrainingService.fetchRelinkCandidates` do the owner-scoped fetch, both calling `markWorkoutCompleted`/`markCompleted` with the new run id. Web `/plans/[id]/workouts/[wid]` (Modal + Playwright); mobile `workout_detail_screen` (AlertDialog picker, errors via `showTopBanner`). |
| Adherence % + weekly summary | ✓ | Partial | ✓ | ✗ | ✗ | |
| Plan adherence banner (weekly drift + missed-long advice) | ✓ | Partial | ✓ | ✗ | ✗ | Pure engine `plan_adherence.ts` ↔ `plan_adherence.dart` parity pair (`weeklyDrift` > ±20%, `missedWorkoutAdvice` make-up/skip). Web `/plans/[id]` + Android `plan_detail_screen.dart` render an owner-only banner from the current week's planned-vs-actual mileage (actual from a recent-runs fetch) + a past, uncompleted long run. |
| Re-plan around missed sessions | ✓ | Partial | ✓ | ✗ | ✗ | Pure engine `plan_replan.ts` ↔ `plan_replan.dart` parity pair (`replanRemaining` — make-up a missed long run ≤+15%, ease-off after over-running; past + taper frozen). Owner-only "Re-plan remaining weeks" → preview modal of proposed distance changes → apply via the per-workout update path. Web `/plans/[id]` + Android `plan_detail_screen.dart`; iOS byte-identical twin. Both platforms group it with the other whole-plan changes in one explained **Adjust plan** dialog (decisions § 1635 web, § 1653 mobile) — each choice named with a sentence on what it does and when to use it. Web's dialog also carries **Shift dates** and the cycle/pregnancy adjust; mobile carries neither (no shift write path, and no cycle surface on the phone) — remaining drift. |
| Adaptive re-plan (trend-based) — gen v2 P1 | ✓ | Partial | ✓ | ✗ | ✗ | [decisions.md §144], 2026-06-12. Pure `plan_adaptive_replan.ts` ↔ `plan_adaptive_replan.dart` parity pair (`adaptiveReplanRemaining`): gates a re-plan on a sustained multi-week adherence trend (2-of-3 trailing completed weeks flagged under/over) then delegates the deltas to `replanRemaining`, suppressing the single-week noise the manual re-plan acts on. Owner-only "Adaptive re-plan", an option in the Adjust plan dialog on both platforms (decisions § 1635 web, § 1653 mobile) + reason/confidence badge reusing the existing preview-and-apply; no schema. Web `/plans/[id]` + Android `plan_detail_screen.dart`; iOS byte-identical twin. 7+7 unit parity tests + Playwright `adaptive-replan.spec.ts`. **P2 (fitness-gated direction) CISO-gated; P3/P4 deferred.** |
| Duplicate a plan week | ✓ | Partial | ✓ | ✗ | ✗ | Atomic `duplicate_plan_week` RPC (migration `20261205_001`) — inserts a copy after the chosen week, pushes later weeks + the end date back 7 days. Owner-only per-week action: web's week-grid Duplicate menu + Android's per-week trailing copy icon (`TrainingService.duplicatePlanWeek`). |
| Pause / resume a plan | ✓ | Partial | ✓ | ✗ | ✗ | A reversible plan-lifecycle primitive distinct from abandon and complete: `status = 'paused'` frees the one-active slot, `resume` claims it back. Both platforms pre-check that slot before resuming and say which plan is in the way, rather than letting the `training_plans_one_active` partial unique index surface as a bare 23505 — web `pausePlan` / `resumePlan` + `ActivePlanExistsError` in `core/data.ts`, mobile the same three in `training_service.dart`. Offered as options in the **Adjust plan** dialog on both surfaces. Web guards each with a `ConfirmDialog`; mobile applies them straight from the chooser, because the action is reversed by its own sibling option in the same dialog and `confirmDestructive` documents that shape as the one it is not for (decisions § 1653). Mobile shipped 2026-09-17; before that the phone's only lifecycle exit was **Abandon** on the plans list. Watches ✗ — no plan-management surface on the wrist. |
| VDOT / Riegel pace derivation | ✓ | Partial | ✓ | ✗ | ✗ | Derivation engine shared via `core_models`. |
| Adaptive plan generator (phase-banded) | ✓ | Partial | ✓ | ✗ | ✗ | Shipped on web at `/plans/new` — `lib/training.ts` holds the generator (VDOT + Riegel derivations, phase-banded workout templates) and persists via `training_plans` + `plan_weeks` + `plan_workouts`. Android: `screens/plan_new_screen.dart` is the matching wizard (goal event + date + recent 5k + days/wk → live `generatePlan` preview → `TrainingService.createPlan`). The Dart `lib/training.dart` is the canonical port of the web engine. |
| VO2 max estimate | ✓ | Partial | ✓ | ✗ | ✗ | Web: `lib/fitness.ts` computes VDOT/VO2 max from the user's best recent qualifying run. Android: `lib/fitness.dart` is a 1:1 port (Daniels' "%VO2max at race pace" curve, same qualifying filter, same 90-day window) and the dashboard renders VDOT + VO₂ max alongside the qualifying-run count. Web also draws a sparkline trend from `fitness_snapshots` — Android shows the latest scalar only. |
| Recovery advisor (ATL / CTL / TSB) | ✓ | Partial | ✓ | ✗ | ✗ | Web: `lib/fitness.ts` exposes EWMA-based ATL (7-day) / CTL (42-day) / TSB plus `recoveryAdvice`. Android: `lib/fitness.dart` is the matching Dart port (same EWMAs, same TSB thresholds, same advice strings); the dashboard's Fitness card renders all three numbers plus the rule-based advice line below the VO₂-max row. |
| Opening-week ramp check in the plan wizard | ✓ | Partial | ✓ | N/A | N/A | `lib/training/plan_ramp.ts` ↔ `lib/plan_ramp.dart` (parity pair, 34 mirror tests each) grades the generated plan against the runner's trailing-28-day average, reusing `coach_load`'s ACWR bands so the plan wizard and the coach roster classify load the same way — too-big off the **opening** week, too-light off the **peak** week, since every plan opens below its own peak. `PlanEditor.svelte` renders the verdict above the week outline with both numbers, recomputing as the wizard's inputs change; Android mirrors it in `plan_new_screen.dart`'s preview, above the week outline, off a guarded `TrainingService.fetchRecentRunVolume` read whose failure leaves the wizard untouched. Silent when the plan matches the runner and below three active weeks of history (a beginner is never told their C25K is a high injury risk), and an under-cooked verdict is withheld from a beginner walk-run plan. Watches N/A (no plan-authoring surface). See [training.md § Opening-week ramp check](../features/training.md) + [decisions.md § 592](../architecture/decisions.md#592-a-plan-wizard-that-never-looks-at-what-the-runner-already-runs-and-the-two-directions-of-wrong-volume-are-not-one-question). |

## Coaching (human coach-athlete roster)

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Coach-athlete roster (invite/accept) | ✓ | Partial | ✓ | N/A | N/A | `coach_athletes` + `redeem_coach_invite` RPC (migration `20261102_001`). Web `/coaching` hub + mobile `coaching_screen.dart` (reached from the Settings → Coaching tile): mint a shareable `/coaching/accept/<token>` link (mobile copies + shares it via the OS share sheet), athlete redeems → active link; coach roster + "my coaches" list; either party can end (mobile gates end/revoke behind an `AlertDialog`). MVP = invite/accept + roster (persona #46, decisions §97). Watch not built (web-canonical, decisions §24). |
| Coach run-review surface | ✓ | Partial | ✓ | N/A | N/A | `/coaching/athletes/[id]` + mobile `coaching_athlete_screen.dart`: a coach reviews a linked athlete's recent runs (public **and** private, via the `active coach reads athlete runs` RLS policy — decisions §98) and active-plan compliance (planned-vs-done workouts window + a completion bar, via the coach plan-read policies, migration `20261116_001`). Read-only — no coach write path into the athlete's runs; the raw GPS track stays owner-only (column-narrowed read on both platforms). `fetchAthleteRuns` / `fetchAthletePlanOverview`; Playwright `tests-e2e/coaching/invite.spec.ts` + Flutter `coaching_athlete_screen_test.dart`. |
| Coach roster dashboard | ✓ | Partial | ✓ | N/A | N/A | The multi-athlete roster (coach_roster.md). Web `/coaching` renders a sortable roster table (last-run recency · 7-day load + trend · plan % · ACWR injury-risk chip) above the athlete list; mobile `coaching_screen.dart` renders the same as a roster card. Each row drills into the existing review surface. Data comes from one consent-gated `coach_roster_summary` SECURITY DEFINER RPC (migration `20270206_001`) that re-checks the active link per athlete inside its body (fail-closed; non-coach → zero rows, unauthenticated → raises) and returns run-stat aggregates only — **no track bytes** (decisions §98 holds). The ACWR risk band + load trend live in the shared `coach_load` parity helper (web `training/coach_load.ts` ↔ mobile `coach_load.dart`), not the SQL. Sort defaults to risk-then-staleness. pgtap `coach_roster_summary_test.sql` (auth boundary) + Playwright `tests-e2e/coaching/roster.spec.ts` + `coach_load.test.ts` / `coach_load_test.dart` (band edges). Watch N/A (web-canonical, decisions §24). |
| Coach plan assignment | ✓ | Partial | ✓ | N/A | N/A | On `/coaching/athletes/[id]` (web) + `coaching_athlete_screen.dart` (mobile), when the athlete has no active plan, the coach picks one of their own plans and assigns it: `assign_plan_to_athlete` deep-clones it into an athlete-**owned** active plan (clone-not-subscribe, provenance in `training_plans.assigned_by_coach_id`), gated on the active link + caller-can-read-source, raising if the athlete already has an active plan (migration `20270106_001`, decisions §143). The RPC RAISE is surfaced as a banner on mobile, not swallowed. The athlete sees it as their active plan on every plan surface. `assignPlanToAthlete` / `fetchMyPlans`; pgtap `assign_plan_to_athlete_test.sql` + Playwright `tests-e2e/coaching/assign-plan.spec.ts` + Flutter `coaching_athlete_screen_test.dart`. Watch not built (web-canonical, decisions §24). |

## AI Coach

See [features § AI Coach](features.md#ai-coach).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| AI-disclosure consent (present · accept · withdraw) | ✓ | Partial | ✓ | N/A | N/A | The GDPR Art 6(1)(a) record is **versioned** — `user_profiles.ai_disclosure_version` beside `coach_consent_at`, v1 = Coach only, v2 = all AI features including the route assistant (migration `20270511_001`, `decisions.md § 571`). Each platform renders the copy from ONE component: web `AiDisclosureNotice.svelte` (the `/coach` first-use dialog + Settings → Account), mobile `widgets/ai_disclosure_notice.dart` (the Coach gate, a Settings → Account tile, and the route-detail fallback notice — `§ 573`). Both gate the Coach at the **v1** rung so an older acceptance is not re-prompted, record the **current** version on accept, and take the server's returned record rather than a local stamp (`§ 560`). Withdrawal clears the whole record on both (`withdraw_ai_disclosure_consent()`). Mobile offers the acceptance whenever the record is not current (missing **or** stale); web offers it when stale. Watches N/A (no AI surface). |
| Chat UI | ✓ | Partial | ✓ | N/A | N/A | Web: top-level `/coach` page (sidebar nav) plus a deep-link card on the dashboard and `/plans/[id]`. Android: `screens/coach_screen.dart` ports the same surface — Scaffold with archives Drawer + chat list + composer. Reached via the Coach icon in the Dashboard AppBar. |
| Plan switcher (multi-plan users) | ✓ | Partial | ✓ | N/A | N/A | `/coach` exposes a `<select>` listing every plan; choice is round-tripped via `?plan=<id>` in the URL. Android: `DropdownButton` in the AppBar appears when the user has more than one plan. |
| Configurable runs window (10 / 20 / 50 / 100) | ✓ | Partial | ✓ | N/A | N/A | Chip selector in the "Grounded in:" strip; sent as `recent_runs_limit` and clamped to `[1, 100]` server-side. Android mirrors with the same four-option dropdown chip. |
| Grounded-in context strip | ✓ | Partial | ✓ | N/A | N/A | Shows the plan, run count, HR-zones-loaded indicator, and weekly goal that `buildContext()` actually loaded. Android: horizontal chip row above the chat list. |
| Personality tones (supportive / drill / analytical) | ✓ | Partial | ✓ | N/A | N/A | Tone is read server-side from `user_settings.prefs.coach_personality`. Both web and Android already write the pref from their respective Settings screens, so the chat surface itself doesn't need a per-message control. |
| Daily usage cap (free 2 / day, pro 10 / day) | ✓ | Partial | ✓ | N/A | N/A | Server-side cap; client mirrors the count via `get_coach_usage` RPC + 429 fallback. Both tiers share the `increment_coach_usage` + `usedToday > TIER_LIMITS[tier].dailyLimit` gate. Android shows a "$N messages left today" banner when remaining ≤ 3 and disables the composer when reached. |
| Cross-device chat history | ✓ | Partial | ✓ | N/A | N/A | `coach_messages` table, RLS owner-only, scoped per (user × plan). One-time client migration from the prior localStorage shape on first read. Android shares the table — same RLS, same per-plan scope. |
| Streaming responses (SSE) | ✓ | Partial | ✓ | N/A | N/A | Tokens render as they arrive; bouncing-dot indicator until the first token. Persists across reload mid-stream via Realtime subscription on `coach_messages`. Android: `dart:io` `HttpClient` POST + UTF-8 stream + `\n\n`-delimited event parser; the same Realtime subscription stitches in messages persisted by other tabs / devices. |
| Markdown rendering + run-link autoformatting | ✓ | Partial | ✓ | N/A | N/A | Web: `marked` + `DOMPurify`. Android: `flutter_markdown`'s `MarkdownBody` with selectable text. System prompt instructs Claude to format references to specific runs as `[Apr 25 long run](../../../../../../runs/<id>)`. |
| Conversation history sidebar | ✓ | Partial | ✓ | N/A | N/A | "New chat" archives the current thread (flips `archived_at` rather than deleting). Per-archive view (read-only) and delete. Web: collapsible sidebar. Android: end-drawer (swipe from the AppBar menu) with a confirmation dialog before archiving + a swipe-to-delete on each archive row. |
| Inline bubble actions | ✓ | Partial | ✓ | N/A | N/A | Web: hover-revealed icon set. Android: same actions, always-visible under the bubble — copy (both roles), regenerate (assistant), edit-and-resend (user — inline `TextField`), thumbs-up / thumbs-down (assistant). Reactions persist via column-level UPDATE on `coach_messages.reaction`. |
| Multi-line composer (Enter sends, Shift+Enter newline) | ✓ | Partial | ✓ | N/A | N/A | Auto-growing textarea up to ~10 lines before internal scroll; resets to single row after send. Android: `TextField(minLines: 1, maxLines: 6)` with a circular Send button; submit on `onSubmitted` mirrors the web Enter-to-send behaviour. |
| OpenAI-compatible provider switch (local Ollama, etc.) | N/A | N/A | ✓ | N/A | N/A | Dev-only convenience: `COACH_PROVIDER=openai` + `OPENAI_BASE_URL` plumbing in `/api/coach/+server.ts` for testing without Anthropic tokens. Production stays on Claude with prompt caching. |

## Spectating and public sharing

See [features § Deep run analysis (web)](features.md#deep-run-analysis-web) and [roadmap § Live spectator tracking](roadmap.md#live-spectator-tracking).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Public run share page (`/share/run/{id}`) | ✓ | Partial | ✓ | N/A | N/A | Link generation is web-only; the page renders anywhere. Android: `screens/public_run_screen.dart` reads through `ApiClient.fetchRunById` (RLS gates non-public) and reuses the `LiveRunMap` + segment-effort + gear-chip + social section widgets to render the same content read-only. The gear chip mounts unconditionally (anon viewers included) via `RunGearChips` → the `public_run_gear` SECURITY DEFINER RPC (migration `20261126_001`), so a non-owner reads only the public gear columns; the owner-only edit affordance stays gated on viewer == owner. Wired up from the feed: tapping a feed-card body now pushes this screen. |
| Public route share page (`/share/route/{id}`) | ✓ | Partial | ✓ | N/A | N/A | Android: `screens/public_route_screen.dart` reads through `ApiClient.fetchRouteById` and renders the planned-route polyline + stats + segments panel (read-only — `canCreate: false`). |
| Year-in-running / "Wrapped" recap (annual) | ✓ | Partial | ✓ | N/A | N/A | Web `/recap/[year]` (hero + stat cards + trophy grid + monthly bar chart + share-card image via `recap_share_image.ts`); mobile `RecapScreen` from the dashboard. Engine = the `recap.ts`↔`recap.dart` parity pair (`buildYearInRunningRecap` + `computeRecapBadges`). iOS shares the byte-identical Dart codebase. |
| Monthly recap (`/recap/[year]/[month]`) | ✓ | Partial | ✓ | N/A | N/A | Web reuses the same engine via `buildMonthInRunningRecap` + the shared `RecapView.svelte` (the month card hides the 12-month strip). Mobile: web's two routes collapse into one `RecapScreen` with a Year / Month `SegmentedButton` — the chevrons then step by year or by calendar month (rolling over the year boundary), the next chevron disables on the current period, and publishing upserts `period_kind='month'` / `period_key='YYYY-MM'` so a month published from phone or web refreshes the same share link. iOS shares the byte-identical Dart codebase. |
| Public recap share link + OG unfurl (`/recap/share/{id}`, `/og/recap/{id}.png`) | ✓ | Partial | ✓ | N/A | N/A | Opt-in, fail-closed-private, revocable: the owner publishes a FROZEN aggregate snapshot into `public_recaps` (migration `20270207_001`); anyone may read it by id (the uuid is the capability token). Web has the public share page + og:image route + the `share-recap` prod Lambda (SSR + og:image, mirrors `share-run`). Mobile (Android + iOS twin) adds a "Publish & share link" action: `ApiClient.publishRecap` freezes the snapshot via `recapSnapshotJson` and the OS share sheet shares the `/recap/share/[id]` web URL. Only aggregate non-track numbers are exposed — no GPS, no per-run rows. |
| Safety net — change / withdraw SMS consent after confirming ("you are a safety contact for") | ✓ | Partial | ✓ | N/A | N/A | The withdrawal half of the [safety.md](../features/safety.md) SMS leg ([decisions §726](../architecture/decisions.md); persona `runner-woman`). `set_safety_sms_opt_in` shipped with migration `20270410_001` and worked, but no surface listed the relationships a user is the **contact of** — only the ones they own — so the consent could be given exactly once, at confirm time, and never withdrawn in-app. A new `fetchSafetyContactOf` reads that half of `safety_contacts` scoped on `contact_user_id` (the linked-contact SELECT policy from `20261218_001` already permitted it, so **no migration**), and both platforms render a "you are a safety contact for" section: one row per confirmed relationship, an SMS toggle over `set_safety_sms_opt_in`, and a withdraw over **`decline_safety_contact`** — never `removeSafetyContact`, which is owner-scoped since §720 and would match nothing here while reporting success. The owner's stored number is reported as a fact (`has_phone`), never as digits, so a relationship carrying none renders an honest note instead of a toggle that would consent to nothing. Web: `/settings/safety` + `ConfirmDialog`; two Playwright legs in `settings/safety.spec.ts` (the toggle round-trip, and that withdraw issues the RPC and no table DELETE) + a `data.test.ts` source guard. Android: `settings_safety_screen.dart` + the `AlertDialog` idiom, five widget tests, ARB keys across all seven catalogues. Backend: five pgtap assertions in `safety_contacts_test.sql`. Watches N/A — consent management is a settings surface, not a wrist one. |
| Live spectator page (`/live/{run_id}`) | ✓ | Partial | ✓ | N/A | N/A | Shipped on web: `live_run_pings` table (migration `20260509_001`) streams GPS samples via Supabase Realtime; `/live/{run_id}` subscribes, hydrates any backlog on join, and renders the trace + stats in real time. Falls back to a "demo" animation after 5 s of silence so the page is still useful for previews. Mobile recorder writes are now wired (see "Runner shares a live-tracking link before start"). Android (read view): `screens/live_spectator_screen.dart` hydrates existing `live_run_pings` for the run, then subscribes via `Supabase.channel.onPostgresChanges(insert)` filtered by run_id; renders the trace on `LiveRunMap` (follow-runner mode) with a Live / Idle / Connecting badge in the AppBar. **Privacy-conscious viewer split (persona-hunt Round 3 finding Privacy #5):** anon spectators + signed-in strangers see an anonymous `Runner #ABCD` handle (first 4 hex chars of the runner's uuid, upper-cased) instead of the `display_name`. Friends (one-way follow in either direction, or the runner viewing their own broadcast) see the real name. Pure helper `runnerHandle` + `shouldRevealDisplayName` in `apps/web/src/lib/social/runner_handle.ts` ↔ `apps/mobile_android/lib/runner_handle.dart` (TS↔Dart parity); mobile screen doesn't surface a runner identifier today so the helper sits ready for when it does. 10-test mirror suite on both sides + Playwright pin on anon (handle visible) and friend (display_name visible) branches in `tests-e2e/live/spectator.spec.ts`. **Coarse last-seen label:** the privacy-zone carve-out (migration `20270121_001`) keeps a single ~1 km-coarsened in-zone last-seen ping flagged `coarse=true` for SAR; when the latest ping carries it, both clients render the current-position marker as a distinct hollow amber ring (web `.runner-dot.coarse`, mobile `LiveRunMap.coarsePosition` → `_CoarseDot`) plus an "Approximate / last seen near here" badge + sub-line so a SAR watcher can't read it as a precise current position. Gated on a live (non-terminal) fix; pairs with the `live_freshness` staleness UI. Web Playwright + mobile widget test pin the badge. **Race clock vs runner's timer (decisions §712):** the strip used to render one "Elapsed" tile that was two quantities — the last ping's `elapsed_s` under a label a spectator reads as the race clock, so an 18 h dead zone left a crew doing cut-off arithmetic 18 h short. They are now separate tiles. **Race time** is wall time since the run's `started_at` (which `public_runs` already serves anon), ticking on the page clock, measured to `concluded_at` once terminal and **withheld** for a run that concluded before that marker existed rather than counting on forever. **Timer** is the runner's own recording stopwatch as of the last fix — not moving time, since the recorder has had no auto-pause layer since moving time became a track-derived post-run metric — muted and relabelled "Timer, last fix" under staleness, the same qualify-don't-withhold treatment the recent-pace tile gets. Web `/live/[id]` + mobile `live_spectator_screen.dart`; the `raceClockS` half of the `live_freshness` pair (16 web / 13 Dart mirror tests). The sibling `race_pings` board has no `coarse` column yet (carve-out scoped to `live_run_pings` pending the `/audit/privacy-zones` review — see `followups.md`). **Run conclusion + enrichment (issue #613, migration `20270427_001`):** the recorder now stamps `runs.concluded_at` on stop (via `concludeLiveBroadcast`, exposed through `public_runs`) instead of wiping the pings — both surfaces read that positive marker (on-load + a ~15 s live poll, transport-agnostic) and render a conclusion card ("Run complete" + a "view the full run" CTA → web `/share/run/[id]`, mobile `PublicRunScreen`; web also frames the whole trace). While live the page adds a **recent-pace** tile + a **course-progress** bar (when the run follows a known route). **Entry points (issue #666):** web's `/live/[id]` URL is itself the entry (the runner shares it; a browser opens it), but on mobile the shared link opens the *web* page and nothing in the app pushed `live_spectator_screen.dart` — it was an orphan reachable only from its own tests. Both platforms now surface a **Watch live** CTA on the public run surface (web `/share/run/[id]`, mobile `PublicRunScreen`) whenever the run is still being broadcast, which is also the mobile tracker's first in-app entry. "Still being broadcast" is the shared `isLiveBroadcast` predicate (`runs/live_broadcast.ts` ↔ `live_broadcast.dart`) — `concluded_at` first, then a zero `duration_s`, fail-closed on an absent duration — so an in-progress stub no longer reads as a finished 0 km run. iOS carries the byte-identical twin (`live_spectator_screen.dart`). **Stopped-runner readout (decisions §621):** staleness covers the runner we cannot see; a runner still pinging from the same spot rendered as a fresh LIVE dot with the Recent tile silently gone (a zero pace delta returns null), so "not moving" and "no data" looked identical. `safety/live_motion.ts` grades the recent-ping window into moving / stopped / unknown — 180 s minimum observation, 25 m stopped radius, `unknown` on a stale fix (a stationary claim off pre-dropout pings is the same lie in a new place) and `unknown` across a telemetry gap over 30 s (an outage is exactly where a runner could have left and come back, so only the contiguous run ending at the newest ping is evidence; no gap length is perfectly safe, so the bound is a proportion — at most a sixth of the shortest claim — not a guarantee) — and the page states the duration neutrally, as a floor ("at least N min") when the stop fills the held buffer. The Recent tile also relabels to "When last seen" under staleness (web). Mirrored to mobile as a registered `live_motion` parity pair (18 mirror tests each): `live_spectator_screen.dart` renders the same neutral chip, fed by a buffer stamped from each ping's own `at` clock so a hydrate backlog cannot collapse an hour of history into a burst of "now" ([safety.md](../features/safety.md) § Stopped-runner readout). |
| Runner shares a live-tracking link before start | ✓ | Partial | N/A | ✗ | ✗ | **Web is the landing page, not the trigger.** The "share my live link before I start" affordance lives on the recorder. Mobile (Android + iOS twin): "Share live link" button on the pre-run idle screen pre-mints the run id, calls `ApiClient.beginLiveBroadcast` to insert a stub `runs` row with `is_public=true` so the FK + RLS on `live_run_pings` accept later pings, attaches a throttled `LiveBroadcaster` to `_onSnapshot` (one ping every 5 s — `lat`, `lng`, `distance_m`, `elapsed_s`, optional `bpm` from the BLE strap, optional `ele`), and hands the `/live/{run_id}` URL to the system share sheet. On stop, `saveRun` upserts the real row with the runner's default visibility, `concludeLiveBroadcast` stamps `concluded_at` (pings left for the 48 h retention cron), and the post-stop dialog resolves the rest — keeping the finished run public is an explicit choice, never a side effect of the live share (issue #664, decisions §434). Failures are L4 (per `docs/architecture/conventions.md § Layered resilience`) so a network blip during a ping doesn't disturb the recorder's L0 clock or L1 distance. |
| Live-share active indicator on the recorder | ✓ | Partial | N/A | N/A | N/A | Issue #613: while a live broadcast is active, the mobile recording screen carries a persistent **`LiveShareIndicator`** "Live" pill (top-left chrome) driven by the `_liveShareActive` `ValueNotifier`. Tapping it opens `showLiveShareSheet` with "Share link again" + "Stop sharing" — stop concludes the broadcast mid-run (`concludeLiveBroadcast`) without ending the run. The share persists until the run finishes: the run screen is a keep-alive tab, so switching tabs / minimizing never tears the broadcast down; the only teardowns are the stop path and an explicit stop-share. Android + iOS twin (`widgets/live_share_indicator.dart`). **Web/watches N/A — recording is a mobile-only capability (a physical exception under [decisions.md § 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive)).** |
| Make a shared run private again (revoke the share / live link) | ✓ | Partial | ✗ | N/A | N/A | Issue #251 (persona-woman, Critical privacy): a Share tap, a `public` privacy default, or an explicit "Keep public" at the end of a live-shared run (the stop path itself no longer persists `is_public` — issue #664, decisions §434) can leave a run public, but there was no way back to private on mobile. Android + iOS twin: "Make private" in `run_detail_screen.dart`'s overflow menu — `AlertDialog` confirm (spells out that the share link + spectator page stop working) → `ApiClient.makeRunPrivate` (`is_public=false`, owner-only via RLS, idempotent) → banner. Offered whenever signed in — the mobile Run model doesn't surface `is_public`, matching the always-prompt Share idiom. Widget tests pin confirm / cancel / failure-banner / signed-out-hidden. Web `✗`: `/runs/[id]` has `makeRunPublic` only — the reverse toggle on the visibility chip is an open gap to close web-side. Watches N/A (no run-detail surface). |
| In-app update to the latest version (Settings → About & updates) | ✓ | N/A | N/A | N/A | N/A | Settings → About & updates shows the real installed build (`package_info_plus`) and, on Android, a **Play In-App Updates** check (`in_app_update`): when the internal/closed/open testing track has a newer build, an "Update available → Update" row runs Play's immediate update flow. The check is fired by an explicit "Check for updates" action, not from `initState` (issue #666 I1 — it previously ran on every visit to a legal page). Guarded by `Platform.isAndroid` + fail-closed via injectable seams — a non-Play build (dev / sideload) reports `unavailable` and hides the update row. Android + iOS twin (`settings_about_screen.dart`). **iOS N/A:** Play In-App Updates is an Android platform service with no iOS counterpart — TestFlight and the App Store own iOS updates, and the screen shows the real version only. Web `N/A` — a web build is always the latest deployment on reload, so there is no app-update concept. |

## Paywall and funding

See [docs/features/paywall.md](../features/paywall.md), [features § Pro tier](features.md#pro-tier), and [features § One-off donations](features.md#one-off-donations).

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Pro tier ($9.99 / mo) — server enforcement | N/A | N/A | ✓ | N/A | N/A | `is_pro()` RPC + `subscription_tier` column are shared; all clients read `user_profiles` the same way. Server rule lives on the web because the coach endpoint is web-owned. |
| Storefront gated on a live perk (no hollow subscription) | ✓ | Partial | ✓ | N/A | N/A | Pro is only offered where a Pro perk is actually live — `coachEnabled() \|\| routeGenEnabled()`, i.e. `PUBLIC_COACH_ENABLED` or `PUBLIC_ROUTE_GEN_ENABLED`. Web computes `proSellable` inline on `/settings/upgrade` and renders the `upgrade.proComingSoon` teaser with Donate as the primary CTA otherwise (pinned by `paywall_guards.test.ts`). Mobile reads the same two flags from the web build's prerendered `/app-capabilities.json` via `pro_sellable.dart` (`fetchProPerks` → `ProPerks.sellable`, twin-mirrored) and swaps the "Subscribe to Pro" tile for the `proComingSoonTitle` / `proComingSoon` teaser — fail-closed, so offline / timeout / non-200 / malformed all read as "no perk live" and the teaser holds until the manifest lands. Restore purchases + Manage subscription stay reachable in both states (Apple 3.1.1 / Play policy). Watches N/A (no purchase surface). See [decisions.md § 466](../architecture/decisions.md) + [paywall.md](../features/paywall.md). Pinned by `settings_pro_screen_test.dart` (4) + `pro_sellable_test.dart` (7). |
| Pro "Get Pro" checkout UI | Partial | Partial | ✓ | N/A | N/A | Shipped on web: `/settings/upgrade` "Get Pro" button redirects to RevenueCat's **hosted Web Paywall Link** (`pay.rev.cat/<token>/<userId>`) when `PUBLIC_REVENUECAT_WEB_CHECKOUT_URL` is set; unconfigured builds fall back to the placeholder toast. The embedded `@revenuecat/purchases-js` SDK was removed (2026-06-20) to drop ~178 KB gzipped from the bundle. Mobile: the "Subscribe to Pro" tile in Settings → Account drives `apps/mobile_android/lib/revenuecat.dart#startProCheckout` which presents the native RC sheet (`purchases_flutter`) when `REVENUECAT_API_KEY_ANDROID` / `_IOS` is in `dotenv.env`, and falls back to the web `/settings/upgrade` URL via `url_launcher` when unconfigured — native IAP is mandatory on iOS/Play, so mobile keeps the SDK by design. Twin-mirrored. **Partial** until the RC dashboard / product / Web Paywall Link are provisioned on the operator side. |
| Higher AI Coach daily cap for Pro users | ✓ | Partial | ✓ | N/A | N/A | `/api/coach/+server.ts` resolves the cap from `TIER_LIMITS[tier].dailyLimit` (free: 2, pro: 10) and runs the same `increment_coach_usage` + `usedToday > dailyLimit` gate for both tiers. The same endpoint serves the Android Coach screen, so the higher Pro cap is automatic on both. |
| Priority processing — coach context budget | N/A | N/A | ✓ | N/A | N/A | Concrete tier-aware budget on `/api/coach/+server.ts`: **free** = 2 messages/day · 768 max-tokens per response · up to 30 runs of context; **pro** = 10 messages/day · 2048 max-tokens (richer / longer answers) · up to 75 runs of context. Every coach response carries `X-Coach-Tier`, `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-MaxTokens`, `X-RateLimit-MaxRuns` headers and echoes `tier` + `limits` in the JSON body. The chat footer reads it back: free users see "N of 2 messages remaining today" with a Free badge; Pro users see "N of 10 messages remaining today · priority context window" with a Pro badge. `BYPASS_PAYWALL=true` reports as `pro` and skips the daily-cap increment so dev sees the Pro shape without burning quota. Mobile / watch / iOS clients hit the same endpoint, so they pick up the same budget — `N/A` on those columns reflects "no separate per-client surface to wire". |
| Priority processing — map-matching queue position | N/A | N/A | N/A | N/A | N/A | DB-level priority via the `job_scheduled_at_for_user(uuid)` helper (migration `20260730_001_tier_aware_job_scheduling.sql`). Both `map_match` enqueue sites — the auto-trigger `runs_enqueue_match_job` and the manual-rematch RPC `enqueue_run_rematch` — return `now()` for `pro` / `lifetime` callers and `now() + 30 s` for `free`. The Go worker's `claim_next_job` orders by `(scheduled_at, id)` so Pro jobs are always claimable strictly before Free jobs enqueued at the same wall-clock instant. The feature itself (clean snapped track) is delivered to every user — Pro pays for queue position, not for access. **Future job kinds must call the same helper** at enqueue time so the tier-priority semantic stays consistent across the queue (don't inline `case ... subscription_tier ...` at enqueue sites). All five client columns are `N/A` because there is no per-platform UI surface — the priority is invisible at the wire and falls out of the data model. See [decisions.md § 57](../architecture/decisions.md#57-map-matching-is-free-queue-priority-is-the-pro-perk). Pinned by `cross-cutting/job-tier-priority.spec.ts` (3 e2e tests) + the run-to-completion + chunked-jobs convention in [decisions.md § 58](../architecture/decisions.md#58-job-queue-priority-is-scheduled_at-offset-not-preemption--and-every-kind-must-be-short). |
| BillingIssueBanner — payment-failed grace-period warning | ✓ | Partial | ✓ | N/A | N/A | When RevenueCat fires `BILLING_ISSUE` (a renewal payment failed but the entitlement is still active during the store's grace period — ~16 days App Store, up to 30 days Play), the `revenuecat-webhook` EF stamps `user_profiles.billing_issue_at = now()` (decoupled from `subscription_tier` so we don't strip Pro mid-grace). Web mounts a persistent banner on the root layout; mobile mirrors it via `apps/mobile_android/lib/widgets/billing_issue_banner.dart` (twin-mirrored to `apps/mobile_ios/`), wired into `HomeScreen`'s Scaffold body so it surfaces above every authed tab. Mobile copy: "Pro renewal failed N days ago. Update your card or you'll be downgraded to Free." with a "Manage" CTA that opens `https://threkir.com/settings/upgrade` via `url_launcher`. Mobile re-fetches `get_my_profile` on app foreground (`WidgetsBindingObserver.didChangeAppLifecycleState` → `AppLifecycleState.resumed`) so a webhook delivery while the app was backgrounded surfaces the next time the user opens the app. Cleared automatically across all clients when RC fires `RENEWAL` / `UNCANCELLATION` (recovered) or `EXPIRATION` / `CANCELLATION` (access ended). Pinned by `apps/web/tests-e2e/cross-cutting/billing-issue-banner.spec.ts` (4 e2e tests for the web side — visibility, copy, and the `lock_subscription_columns` defence-in-depth that prevents client-side suppression of the banner) plus `apps/mobile_android/test/billing_issue_banner_test.dart` (10 pure-logic tests for `shouldShowBillingIssueBanner` + `relativeDaysSince`, byte-mirrored on iOS). See [paywall.md § Pro perks](../features/paywall.md#pro-gates-and-perks). |
| One-off Donate button | Partial | Partial | ✓ | N/A | N/A | Web `/settings/upgrade` has a single Donate button linking to an external provider. Android: "Support the app" tile in Settings → Account opens the same URL via `url_launcher` in the system browser. Native one-tap donate (Play Billing) is still pending. |
| Paywall feature gate (registry-driven) | ✓ | Partial | ✓ | N/A | N/A | `isLocked()` still returns `false` for every key — no feature is hidden behind Pro today. Infra kept so a future Pro-only feature can flip one return. |
| RevenueCat subscription wiring (web) | N/A | N/A | ✓ | N/A | N/A | Shipped on web via RevenueCat's **hosted-checkout redirect** (no embedded SDK — the ~178 KB `@revenuecat/purchases-js` dep was removed 2026-06-20). `$lib/billing/revenuecat.ts` is a thin env shell over the pure `revenuecat_links.ts`: "Get Pro" redirects to `pay.rev.cat/<token>/<userId>?redirect_url=…` (user id = app user id); "Manage subscription" opens the no-code customer portal (`PUBLIC_REVENUECAT_WEB_PORTAL_URL`, email-authenticated). Env-gated by `PUBLIC_REVENUECAT_WEB_CHECKOUT_URL`; builds without it compile and fall back to the placeholder toast, so local dev / previews work end-to-end. |
| RevenueCat subscription wiring (mobile) | Partial | Partial | N/A | N/A | N/A | `purchases_flutter ^10.0.0` shipped on both platforms behind `apps/mobile_android/lib/revenuecat.dart` (twin-mirrored). The wrapper exposes `isRevenueCatConfigured` / `configureRevenueCat` / `startProCheckout` / `managementUrl` and gates every call on `dotenv.env['REVENUECAT_API_KEY_ANDROID']` / `_IOS` so unconfigured builds compile + fall back to the existing web `/settings/upgrade` URL. The Settings → "Subscribe to Pro" tile presents the native sheet when configured, falls through to the web URL otherwise. **Partial** because the RC dashboard / `pro_monthly` product / API keys still need provisioning on the operator side; once the keys land, the same code paths drive a real checkout. 7 unit tests pin the unconfigured behaviour. |

## Settings and preferences

See [docs/backend/settings.md](../backend/settings.md) for the full registry of known keys (`user_settings.prefs` + `user_device_settings.prefs`). A cell is `✓` only if the client actually exposes an editor — not if the key merely survives a round-trip through the settings bag.

### Account and identity

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Display name | ✓ | Partial | ✓ | N/A | N/A | Web edits it on `/settings/account`. **Mobile twin shipped**: `settings_account_screen.dart` carries a display-name tile (dialog editor, blank clears to the "Runner" fallback) backed by `ApiClient.updateDisplayName` — a row-count-verified update with an insert fallback that throws when signed out. Previously the setup wizard was the only mobile writer, so a user who skipped it was stuck as "Runner". |
| Email address (view) | ✓ | Partial | ✓ | N/A | N/A | |
| Change email | ✓ | Partial | ✓ | N/A | N/A | All three call `supabase.auth.updateUser({ email })` (mobile via `ApiClient.updateEmail`), starting GoTrue's secure double-confirmation (link to both the old + new address). Web: a Change-email affordance on `/settings/account` reveals a new-address input + a persistent confirmation-pending banner naming both inboxes. Mobile: a Change-email tile → dialog → pending subtitle. Backend mail is the `auth-email` EF's `email_change` + `email_change_current` templates (all six locales, already present). See `web_app_auth.md § Change email`; issue #245. |
| Change password | ✓ | Partial | ✓ | N/A | N/A | All three require the current password before `supabase.auth.updateUser`. The step-up is the `password_change` parity pair (web `core/password_change.ts` ↔ mobile `password_change.dart`): `checkPasswordPair` first, then a positive `signInWithPassword` on the current password (`ApiClient.verifyCurrentPassword` on mobile), fail-closed — a rejection, a throw, or a session with no email all fail identically. Both surfaces also carry an "email me a reset link" path for OAuth-only accounts that have no password to prove (mobile's link lands on web `/auth/reset`). Issue #381, [decisions §278](../architecture/decisions.md), `web_app_auth.md § Change password requires the current password`. |
| Delete account | ✓ | Partial | ✓ | N/A | N/A | All three call the `delete-account` Edge Function. |
| Sign out | ✓ | Partial | ✓ | N/A | N/A | Watches sign out when the paired phone does. |

### Universal preferences (U / UD scope)

Written to `user_settings.prefs` and propagate across devices.

| Key | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| [`preferred_unit`](../backend/settings.md#keys) (km / mi) | ✓ | Partial | ✓ | Partial | N/A | Android dual-writes the legacy `profiles.preferred_unit` column through `SettingsSyncService`. **Wear OS (read-only)**: `SupabaseClient.fetchUniversalSettings()` reads `user_settings.prefs.preferred_unit` (validated against `{km, mi}`); `RunViewModel.applyUniversalPrefsAsync` resolves it to a `DistanceUnit` on the UI state (same path as `body_weight_kg`), and `recording/UnitFormat.kt` (`formatDistance` / `paceSecPerUnit`) renders distance + pace in the runner's unit on the running screen, the route "to go" badge, the route picker, the PostRun summary, the crash-recovery prompt, and the active-run tile (the tile carries the unit on `RecordingRepository.Metrics`). **The recovery prompt was km-only until 2026-09-02** (`decisions.md § 882`): `distance_km_recorded` was the one unit-bearing string with no `_mi` sibling and `PreRunScreen` the one screen never handed `preferredUnit`, so a miles runner was told "6.44 km recorded" about their four-mile run on the screen whose whole job is letting them recognise which run survived; `StringResourceContractTest` now fails on any km-bearing key without its mi sibling, and on a pair only half-wired. No editor on the wrist — edits stay on phone / web. On-face visual flip awaits a device check. Apple Watch honours the unit only via its own `RunFormat`; the bag isn't read there yet. |
| [`default_activity_type`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✓ | ✗ | Editor shipped on all three pocket clients. Android: `Preferences.defaultActivityType` mirrors the universal bag (overlay on sign-in via `SettingsSyncService._applyUniversal`, dual-write on settings change). `run_screen` reads it in `initState` so the activity picker opens on the user's preferred activity. **Wear OS (read-only)**: `SupabaseClient.fetchUniversalSettings()` pulls `user_settings.prefs.default_activity_type` after every session restore (cached + phone-handoff + direct sign-in), and `RunViewModel.applyUniversalPrefsAsync` primes the pre-run picker chip to that value — only when the user is on the PreRun stage AND hasn't already moved off the hardcoded default, so a manual chip-tap or an in-progress run is never clobbered. Edits stay on the phone / web (per `apps/watch_wear/CLAUDE.md` "don't build settings panels on the wrist"). iOS still picks via the run screen's local toggle, not the bag. |
| [`hr_zones`](../backend/settings.md#keys) (5-band editor) | ✓ | Partial | ✓ | ✓ | ✗ | Editor shipped on the three pocket clients. **Wear OS (read-only consumer)**: `parseUniversalSettings` decodes the 5-band object with strict-ascending + range gates; `resolveZoneCutoffs` picks `hr_zones` first in the precedence chain. The live `RunningScreen` shows "146 bpm · Z3" next to the BPM reading via `hrZoneOf(bpm, cutoffs)`. Falls back through `max_hr_bpm` then Tanaka `208 − 0.7 × age (date_of_birth)`; the badge stays hidden when none of the three signals are set. No editor on the wrist by design. |
| [`resting_hr_bpm`](../backend/settings.md#keys) | ✓ | Partial | ✓ | Partial | ✗ | Read-only on Wear OS, and **cached but not yet consumed** — `parseUniversalSettings` pulls `resting_hr_bpm` into `UniversalSettings.restingHrBpm` with [25, 120] range validation, but no live wrist UI reads it today. The plumbing is in place for the eventual TRIMP / Karvonen pace-adjusted zone work. No editor on the wrist. |
| [`max_hr_bpm`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✓ | ✗ | Read-only on Wear OS — `parseUniversalSettings` pulls `max_hr_bpm` into `UniversalSettings.maxHrBpm` and validates the [100, 240] range. Drives the second-priority cutoff derivation in `resolveZoneCutoffs` when `hr_zones` is unset (60/70/80/90/100% of max). |
| [`date_of_birth`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✓ | ✗ | Read-only on Wear OS — `parseUniversalSettings` pulls `date_of_birth` after a shape gate (YYYY-MM-DD). `ageBasedMaxHr` converts to Tanaka `208 − 0.7 × age` via `java.time.Period.between` and feeds `resolveZoneCutoffs` as the third-priority signal when both `hr_zones` and `max_hr_bpm` are unset. |
| [`privacy_default`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✓ | ✗ | Editor shipped on the three pocket clients. **Wear OS (read-only consumer)**: `parseUniversalSettings` pulls `prefs.privacy_default` alongside `default_activity_type` and `RunViewModel.applyUniversalPrefsAsync` caches it on the VM. `handleFinishedRun` reads `snapshotIsPublic()` at stop time and stamps `QueuedRun.isPublic` so an in-flight queued run keeps its visibility even if the user toggles the pref before the upload drains. **Crash recovery honours it too (#389)**: the pref is passed into `RunRecordingService` and snapshotted onto every 15 s `Checkpoint`; `recoverCheckpoint` maps `Checkpoint.privacyDefault` through the SAME `isPublicFromPrivacyDefault` `snapshotIsPublic` uses, so a recovered run can't drift to always-non-public. `SupabaseClient.saveRun` writes `is_public` only when the snapshot was non-null — runs queued before this slice ship continue to use the DB default (`false`). Mapping: `public → true`, `followers / private → false` (the `followers` nuance lives in the phone/web social layer; the wrist's `runs.is_public` is a boolean). The phone editor still wins for per-run overrides; no editor on the wrist by design (per `apps/watch_wear/CLAUDE.md`). **Web applies the pref at write time** — `createManualRun` + `saveRun` (and therefore every importer) map `privacy_default` → `is_public` via `run_visibility.ts#privacyDefaultToIsPublic` (`public → true`, else private); previously web ignored the pref entirely and every run landed at the `is_public` default `false` (comeback persona #27). Web's manual-run editor (`RunEditor`) also has a per-run "Make this run public" toggle seeded from the pref. No per-run visibility selector is wired on mobile yet, so on mobile the universal value is set-and-roam only. |
| [`strava_auto_share`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✗ | ✗ | Toggle shipped on all three; enforcement lands when Strava OAuth sync ships. |
| [`coach_personality`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✗ | ✗ | Only consumed by the web coach; mobile edits it for cross-device roaming. |
| [`weekly_mileage_goal_m`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✗ | ✗ | Both clients ask for the goal in the account's km/mi and store metres through the `weekly_goal` parity pair (issue #902 § 5; web `/settings/training`, mobile Settings → Preferences, whose coach context chip now also reads in miles). Settings editor and the dashboard goal editor both write the bag value and the local `RunGoal` list in tandem. On sign-in, `SettingsSyncService._applyUniversal` seeds a weekly-distance `RunGoal` from the bag when one isn't already present locally; `pushWeeklyDistanceGoal` mirrors local edits back to the bag. Multi-target / monthly / pace goals stay client-only — the bag scalar can't represent them. |
| [`week_start_day`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✗ | ✗ | |
| [`undo_window_s`](../backend/settings.md#keys) | ✓ | Partial | ✓ | N/A | N/A | How long the undo bar keeps a destructive action reversible: `8` (default), `30`, or `0` = **no time limit**. The `0` choice is the WCAG 2.2.1 *Timing Adjustable* ("Turn off") route — a countdown must not be the only way to reach the only undo affordance, which is why the mobile half shipped the **editor** as well as the read: a phone consumer with no adjust route would leave the user racing a timer they cannot switch off. Universal so the accessibility choice roams. Mobile mirrors the bag value into `Preferences.undoWindowS` (so the next deletion honours a pick that is still queued, and it survives a cold start offline) and the host reads it at defer time. A present-but-corrupt value normalises to `8` and never to `0` on both platforms; an ABSENT key leaves a local choice alone. Watches N/A — no undo surface on the wrist. |
| [`map_style`](../backend/settings.md#keys) (UD) | ✓ | Partial | ✓ | ✗ | ✗ | Editor **and consumer** shipped on all three. Mobile's tile layer swaps on the value since the issue #666 round-6 fix (decisions §489): `resolveTileUrl` ports web's `buildMapStyleUrl` slug switch, `streets` follows the app theme on both platforms, and the overlay casing / marker rings / track gradient derive from the RESOLVED basemap rather than assuming dark. Watches keep a fixed style by design. Every mobile map also credits the basemap it actually resolved to (round 7, decisions §491) — MapTiler + OpenStreetMap on the keyed path, OpenStreetMap alone on the keyless fallback, Protomaps + OpenStreetMap under a `TILE_URL_TEMPLATE` override — where web gets the same credit for free from MapLibre's attribution control reading the style.json. |
| [`units_pace_format`](../backend/settings.md#keys) (UD) | ✓ | Partial | ✓ | ✗ | ✗ | Editor shipped; mobile still derives pace format from `preferred_unit` at render time. |
| [`auto_pause_enabled`](../backend/settings.md#keys) (UD) | ✓ | Partial | ✓ | ✗ | ✗ | Editor shipped. Android removed live auto-pause (derived post-run); the key is still valid for iOS run recording and future watch use. |
| [`auto_pause_speed_mps`](../backend/settings.md#keys) (UD) | ✓ | Partial | ✓ | ✗ | ✗ | |
| [`voice_cue_types`](../backend/settings.md#keys) (UD) | ✓ | Partial | ✓ | ✗ | ✗ | Per-cue on/off for the eight spoken cues (issue #607). Mobile owns playback (recording is mobile-only) and its cue switches write the **device** bag; the web editor on `/settings/recording` writes the **universal** bag — a browser is its own `device_id` and never records, so a device write from web would be unreadable. `SettingsSyncService` applies universal then device, so a per-phone override still wins per cue and an absent id stays audible. Cue ids are a wire contract pinned by `voice_cues.test.ts` against the Dart `VoiceCue` class. |

### Device-scoped preferences (D)

Written to `user_device_settings.prefs`; a dedicated per-device editor is not wired on any client yet.

| Key | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| [`voice_feedback_enabled`](../backend/settings.md#keys) (UD) | ✓ | Partial | ✓ | ✗ | ✗ | The master spoken-cues gate. Web's editor on `/settings/recording` writes the **universal** bag; mobile's "Spoken split announcements" toggle writes the **device** bag (overlay-on-signin + push-on-change in `SettingsSyncService`), and the overlay applies universal then device, so a per-phone choice wins whenever the device bag names the key. This row previously claimed the web toggle reached the recorders while the key was registered `D` and mobile read only the device bag — it never did until the 2026-08-03 `UD` promotion (decisions § 472). The untouched default is **on** on every surface (decisions § 474 — registry + web load fallback aligned to the phone's since-v1 local default; `voice_cues.test.ts` pins the agreement). No watch surface reads the key (Wear's `parseUniversalSettings` deliberately doesn't consume it). |
| [`voice_feedback_interval_km`](../backend/settings.md#keys) | ✓ | Partial | ✓ | ✗ | ✗ | Web editor on `/settings/recording`. Mobile's "Split interval" control dual-writes; unit conversion metres ↔ km happens in `SettingsSyncService`. |
| [`haptic_feedback_enabled`](../backend/settings.md#keys) | N/A | N/A | N/A | N/A | Partial | Apple Watch ships haptic pace alerts but the on/off toggle isn't surfaced yet. Wear OS has no haptics ([decisions.md § 15](../architecture/decisions.md)). |
| [`keep_screen_on`](../backend/settings.md#keys) | ✓ | Partial | N/A | N/A | N/A | Android: SwitchListTile in `settings_screen.dart` writes `Preferences.keepScreenOn` (default true) and `SettingsSyncService.pushKeepScreenOn` mirrors it to the device bag. `run_screen.dart` only calls `WakelockPlus.enable()` when the pref is true. The tile subtitle discloses the battery cost (issue #271). |
| [`dim_screen_while_recording`](../backend/settings.md#keys) | ✓ | Partial | N/A | N/A | N/A | Battery-saver companion to `keep_screen_on` (issue #271): SwitchListTile in `settings_preferences_screen.dart` writes `Preferences.dimScreenWhileRecording` (default false), `SettingsSyncService.pushDimScreenWhileRecording` mirrors it to the device bag. `run_screen.dart` darkens the live map via `shouldDimRecordingMap` while recording; disabled in Settings while `keep_screen_on` is off. |

### Device management

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Device ID mint + `user_device_settings` row on first launch | ✓ | Partial | ✓ | ✗ | ✗ | Both mobile clients mint a UUID on first launch via `Preferences` and upsert the device row on sign-in through `SettingsService.load`. See [settings.md § Client responsibilities](../backend/settings.md#client-responsibilities). |
| Device list / labels screen | ✓ | Partial | ✓ | N/A | N/A | Titled **"Signed-in devices"** on both platforms since issue #666 I10 — the screen is sessions plus their per-device overrides, while heart-rate strap / treadmill / watch pairing lives under Integrations. Web: `/settings/devices`. Android: `screens/devices_screen.dart` lazy-loads via `ApiClient.fetchMyDevices`; per-row "This device" badge, last-seen relative time, override count, and a popup menu with Rename / Edit overrides… / Remove. |
| Per-device override editor UI | Partial | Partial | ✓ | ✗ | ✗ | Web: expanding a device row on `/settings/devices` lists its current `prefs` key/value pairs with a per-row Clear button. An "+ Add override" row opens a typed editor (key picker → bool toggle / number input / enum select depending on the chosen key's shape; catalogue covers the D + UD keys from `settings.md`, U-only keys are deliberately excluded). Mobile clients stay read-only on the device-override surface — they can edit their own device's settings via the regular preferences screen, but cross-device override management is a desktop affordance. Android: tapping "Edit overrides…" on a device row opens a sheet listing the existing keys with a per-row clear; "+ Add override" with the typed key catalogue is still pending — `ApiClient.setDeviceOverride` already supports the write-path. |
| Remove a device / wipe local settings | ✓ | Partial | ✓ | ✗ | ✗ | Web: resetting the current device deletes its `user_device_settings` row, clears the cached `run_app.device_id` localStorage key, and reloads so the stores reinitialise with a fresh device id. Removing another device just deletes the server row. Android: Devices screen popup → Remove deletes the server row via `ApiClient.removeDevice`; the device stays signed in (matches the web "removing another device" semantics). |

### App-level settings (not in the registry)

Controls that live on a settings screen but aren't part of `user_settings.prefs` — they're either platform-only or stored in a client-local key-value store.

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Preferences grouping (settings information architecture) | ✓ | Partial | ✓ | N/A | N/A | **Diverges by design (issue #905).** Web splits preferences into six topical pages under a Preferences nav section — Units & display, Recording & voice, Training, Privacy & sharing, Notifications, with Body metrics beside Account — and keeps `/settings/preferences` as a landing page whose old anchors redirect ([decisions § 1640](../architecture/decisions.md)). Mobile keeps one Settings → Preferences screen carrying the same groups as pinned sections (Units & display, Activity & recording, Training & demographics, Race fueling, Privacy & sharing, AI coach, Notifications) with Body metrics as a sub-screen: one scrolling list with sticky group headers is the phone idiom, while web has the settings nav to spend. The groups differ at the edges (week start is a display setting on web and a training one on mobile; fueling and the coach tone sit inside Training on web). Every key stays reachable on both; web's reachability is pinned by `settings_ia_guard.test.ts`. |
| Dark mode / theme toggle | ✓ | Partial | ✓ | N/A | N/A | Web: three-way `Auto / Light / Dark` toggle in `/settings/display`. Applied via `html[data-theme]` (CSS duplicates the dark block for the explicit override; media query still fires for auto). Persisted to localStorage, per-browser (intentional — a dark laptop + light tablet is common). |
| Multi-language / localization (UI) | Partial | Partial | ✓ | ✓ | ✓ | **All five surfaces ship SEVEN locales as of 2026-08-27** — `en/de/fr/es/ja/pt-BR/pt-PT` everywhere, the Apple Watch gap closing (decisions § 761) the day after § 755 opened it. European Portuguese reached the phone first — 3,793 translated keys, **1,164 genuinely divergent from Brazilian, 30.7%** ("partilhar" not "compartilhar", "em direto" not "ao vivo", "ecrã" not "tela", "palavra-passe" not "senha"). It was unreachable twice: by device until 2026-08-06, when `supportedLocales` learned `Locale('pt')` (decisions § 547), and by explicit choice until 2026-08-25, when the language picker stopped listing its options by hand (decisions § 740). Now reachable three ways — `pt-PT` / `pt-AO` / bare `pt` negotiate onto it, the in-app picker offers it, and both platform manifests advertise it (`CFBundleLocalizations` → `pt-PT`; Android `locales_config.xml`, added in the same round, → `pt-PT` since § 760). **Web and Wear OS caught up 2026-08-27** (decisions § 755): `locales/pt-PT.ts` (5,049 keys, 1,288 of them — 25.5% — divergent from Brazilian) and `values-b+pt+PT/strings.xml` (100 named resources) both ship, `BASE_TO_LOCALE.pt` points at European on web as it already did on the phone, and the wrist's `unmirroredPhoneCatalogues` is empty for the first time. **The phone caught up and passed them 2026-08-27** (decisions § 760): 822 strings derived through the same corpus, taking `app_pt.arb` from 9.5% divergent to **30.7%** against web's 27.7%, and **one tag now means one thing on every surface**. The phone had spelled it bare `pt` — CLDR's Brazilian default content — while iOS's plist, web and the wrist all said `pt-PT`; renaming the ARB is impossible (gen-l10n refuses to generate without a bare `pt` base beside `pt_BR`), so the FILE stays `app_pt.arb` and every tag a reader, the OS or the runtime sees is `pt-PT`, held together by `arbTagOverride` in the locale-reach guard. A **tag-vs-content guard** now ships on both platforms — a catalogue tagged for one Portuguese variant may not contain words only the other uses — which is the first guard here that asks what a catalogue SAYS rather than whether it is reachable; it flags 319 strings in the pre-derivation ARB and 0 today. `locale.ts` ↔ `locale_support.dart` is a registered parity pair in the same round. All pt-PT copy is model-derived and awaits native review. Every locale-declaration site on mobile and web is now held to the catalogue directory by a source guard, so these mobile cells are guard-enforced rather than asserted (decisions § 740); **Wear OS's five declaration sites got the same treatment 2026-08-26** (`LocaleReachTest` + `WearLocales`, decisions § 748), so all three platforms are now derived rather than listed. Per-device choice defaulting to device locale, never DB-synced. Web: full — hand-rolled runtime, 5,049 keys × 7 locales, picker in `/settings/display` derived from `SUPPORTED_LOCALES` ([decisions.md § 108](../architecture/decisions.md#108-web-i18n-is-detected-client-side-with-a-lazy-loaded-message-catalogue--not-an-accept-language-ssr-framework)). Mobile (Android + iOS twin): **framework landed** — `flutter_localizations` + `intl` + gen-l10n ARB catalogues, `localeNotifier` + per-device `Preferences.locale`, language picker in Settings → Preferences; **bulk string migration + `intl` date/number formatting + locale-aware TTS (engine language + spoken phrases + guided-run scripts) all landed; remaining-string cleanup in progress** ([decisions.md § 113](../architecture/decisions.md#113-mobile-i18n-uses-flutter-gen-l10n--arb-with-committed-non-synthetic-output-and-a-per-device-locale)). **Wear OS: landed** — standard Android string resources (`values/` + `values-de/fr/es/ja/b+pt+BR/b+pt+PT`, 100 named resources each), `locales_config.xml` referenced from the manifest, device-locale-follow with no in-app picker (platform norm on a tiny screen); locale-aware distance decimal separator + km/mi unit honoured (`NumberFormat`/`formatDistance` + `DistanceUnit`, reading `preferred_unit` from the universal bag), TTS engine set to `Locale.getDefault()` and spoken phrases drawn from resources (same dialect as the mobile ARB catalogues), resource-parity unit test mirrors the mobile `l10n_parity_test`, and `LocaleReachTest` holds `locales_config.xml`, the manifest's `localeConfig` reference and both test-side locale lists to the `values-*` directories on disk (decisions § 748), which is what carried the pt-PT set into every declaration site when it landed. **Apple Watch: landed** — modern String Catalog (`WatchApp/Localizable.xcstrings`, **59 strings across seven locales** — six explicit plus an `en` that only the one plural entry declares, since the source string is the key), SwiftUI `Text` auto-localises against it, device-locale-follow with no in-app picker; locale-aware distance via `RunFormat` (`NumberFormatter` decimal separator + `MeasurementFormatter` unit word, km/mi honoured), all seven in `CFBundleLocalizations` + `knownRegions`; script parity check `apps/watch_ios/scripts/check_xcstrings_parity.sh` runs without Xcode, and since 2026-09-02 `scripts/check_watch_ios_source.mjs` additionally holds the SOURCE against the catalog in both directions — a `Text("…")` with no entry renders English in every locale and throws nothing, which no Swift test and no `xcodebuild` run can see (decisions § 884). **The four `NS*UsageDescription` consent prompts are localized too as of 2026-09-18** — `WatchApp/InfoPlist.xcstrings`, four keys × seven locales with an explicit `en` (the key is a plist key name, not the English text), wired into the `WatchApp` target the way `Localizable.xcstrings` is; `check_xcstrings_parity.sh` now derives the shipped locale set across BOTH catalogs and holds every `NS*UsageDescription` key in `Info.plist` to a catalog entry and back, with the source value held against the plist string watchOS falls back to (decisions § 1675). Same dialect as the mobile ARB / Wear OS catalogues; the pt-PT copy diverges from pt-BR in the ways § 755 / § 760 established (`registar`/`registrar`, `percurso`/`rota`, `ecrã`/`tela`, `guarda`/`salva`) and, like all pt-PT copy here, is model-derived and awaits native review. **Compiled and rendered on a Mac, 2026-09-18** (Xcode 26.4 / watchOS 26.4 simulator): the whole `WatchAppTests` suite is green at 225 tests, the built `WatchApp.app` carries an `InfoPlist.strings` in each of the seven `.lproj` directories, and the simulator booted in `pt-PT` and in `ja` renders the app's HealthKit consent sheet in that locale — the first non-English wrist this project has rendered. The purpose paragraph itself sits below the 40 mm fold and `simctl` exposes no touch or crown input, so our own sentence is *build*-verified (read out of the installed bundle with `plutil -p`) rather than read off the screen. The phone (`apps/mobile_ios/ios/Runner/Info.plist`, ten usage descriptions) still has no `InfoPlist.xcstrings` — same shape, still open. |
| Offline-only mode switch | ✓ | Partial | N/A | N/A | N/A | Mirror of the offline-only sync behaviour. |
| HR monitor pairing (BLE) | ✓ | Partial | N/A | N/A | N/A | External chest-strap pairing; watches use their built-in sensor instead. |
| Advanced GPS filter tuning | ✓ | Partial | N/A | ✗ | ✗ | Per-activity-type speed / accuracy thresholds. |
| Licenses / open-source notices | ✓ | Partial | ✓ | ✗ | ✗ | Web: `/settings/licenses` with dep + licence list and map-data / OSM attribution. Linked from the settings sidebar. Mobile: an Open-source licenses row on Settings → About & updates pushing `showLicensePage` (issue #666 I1 — the tile used to be *named* Licenses and hid the version + update path behind it). |
| Legal documents reachable in-app (Privacy, Terms, Cookie, health-data) | ✓ | Partial | ✓ | ✗ | ✗ | Web lists all four in the settings nav footer ([decisions §243](../architecture/decisions.md)). Mobile reaches the same four from Settings → About & updates via the shared `legal_links.dart` (`LegalDoc` + `openLegalDoc`), which also backs the sign-up consent gate's inline links — one URL definition, two surfaces. Before issue #666 I4 a signed-in mobile user had **no** route to any of them. Watches `✗` by design: reading a policy on a wrist screen is not a supported surface. |
| App version display | ✓ | Partial | ✓ | ✗ | ✗ | |
| Unsaved-changes guard on create/edit forms | ✓ | Partial | ✓ | N/A | N/A | Navigating away from a dirty create/edit form prompts before discarding, on both surfaces. **Android + iOS:** the shared `DiscardGuard` on all 10 form surfaces, probing dirtiness at **pop time** — a build-time `canPop` goes stale because `TextEditingController` edits never rebuild the route ([decisions §478](../architecture/decisions.md), issue #666 U1). **Web ✓:** `UnsavedChangesGuard.svelte` over the `form_dirty` tracker, arming SvelteKit's `beforeNavigate` for in-app navigation and `beforeunload` for tab close/reload — two mechanisms, because `beforeunload` cannot carry custom copy and must stay disarmed while the form is clean or browsers penalise it ([decisions §493](../architecture/decisions.md), issue #666 round 7, closing the §24 inversion where the canonical surface lagged the additive one). **Watches N/A** — no create/edit forms. |
| Undo a destructive action | ✓ | Partial | ✓ | N/A | N/A | **Web ✓** (issue #666 U8, rounds 11–12, decisions § 514 + § 516): `UndoBar.svelte` over `core/undo_queue.ts` — the delete is **deferred**, not compensated, so Undo cancels a pending mutation and can never fail. Adopted on **seven** deletes, each of which **dropped its `ConfirmDialog`** (confirm and undo are alternatives — [conventions.md § Destructive actions](../architecture/conventions.md)): round 11's `/nutrition` food entry, route condition report and run comment/reply; round 12's **notification dismiss** (single row *and* a collapsed group — one intent, one slot, one batched delete), **route review** (issue #664), **route marker**, and the **gear wear-log** observation, which lives inside a `Modal` and works because the dialog's Tab ring now admits the bar's `[data-modal-trap-include]` region (WCAG 2.1.1; the order is dialog-first per 2.4.3). **Mobile ✓** (round 13): the SAME seven surfaces, over a Dart twin of the queue (`undo_queue.dart`, 14 mirror tests) hosted by `widgets/undo_bar.dart`. The host is a **root `Overlay` entry**, not a `SnackBar` — measured: a snack bar raised while a dialog is up carries no semantics node at all (a modal barrier's `BlockSemantics` drops the whole route beneath it, Scaffold included), so TalkBack/VoiceOver could neither announce nor reach it; a root overlay entry lands ABOVE the barrier and stays in the tree, which is why the in-sheet gear delete needs no exemption here and why Flutter needed no analogue of web's Tab-ring widening. Because the queue is a top-level singleton, a route pop cannot discard a pending commit — mobile therefore does NOT flush on navigation the way web's `beforeNavigate` does; the offer rides over the new screen and every `restore` is mount-guarded (guarded in `architecture_guards_test.dart`). Deletes that cascade or that destroy an authored reusable artefact still keep the confirm on both platforms; **being inside a modal is no longer a reason to** on either. **Watches N/A** — no destructive list actions on the wrist. |
| Manage premium subscription | Partial | Partial | ✓ | N/A | N/A | Web: "Manage subscription" button on `/settings/upgrade` when the user is Pro. Calls `managementUrl()` (`$lib/billing/revenuecat.ts`) which opens RevenueCat's no-code customer portal (`PUBLIC_REVENUECAT_WEB_PORTAL_URL`, email-authenticated) in a new tab — no per-user SDK `CustomerInfo` call. Falls back to a "manage where you started it" toast when the build is unconfigured (preview / dev) or the subscription was started in App Store / Play Store. Mobile: the same "Subscribe to Pro" tile drives `revenuecat.dart#startProCheckout` (native `purchases_flutter` sheet when configured, web URL fallback when not). Twin-mirrored. Native `managementUrl` accessor shipped in `revenuecat.dart`; surfacing it as an "Already Pro? Manage" tile is deferred until the live RC keys land. |
| Funding / donation surface | Partial | Partial | ✓ | N/A | N/A | Web-only; see row under *Paywall and funding*. Android: "Support the app" tile in Settings → Account opens the donate URL via `url_launcher` in the system browser. |

## Map and tile layer

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| MapLibre (MapTiler) base tiles | ✓ | Partial | ✓ | ✗ | ✗ | Watches render pre-downloaded tiles only. |
| Offline tile cache (HTTP replay) | ✓ | Partial | N/A | ✗ | ✗ | Architectural exception for web: browsers handle tile caching natively via HTTP cache headers + (optionally) the service worker, not via a custom HTTP-replay layer. The web's existing `static/sw.js` could grow a tile-cache strategy if needed; tracked separately if so, not as a parity gap with the mobile recorder's bespoke cache. |
| Self-hosted Protomaps tiles | N/A | N/A | N/A | N/A | N/A | Infrastructure decision, not a per-client feature — the basemap source is set deploy-side and every client picks it up uniformly. Tracked under [roadmap § Future — Protomaps](roadmap.md), not the per-platform matrix. |

## Map matching (post-run clean-up)

| Feature | Android | iOS | Web | Wear OS | Apple Watch | Notes |
|---|---|---|---|---|---|---|
| Server-side HMM map matching | N/A | N/A | N/A | N/A | N/A | Server-side feature — every client picks up the snapped trace uniformly. Schema + auto-enqueue trigger + Go worker (`apps/job_worker/`) shipped. Engine pick is **OSRM** (`/match/v1/foot`) — `OSRMMatcher` in `apps/job_worker/internal/matcher_osrm.go` is selected when `OSRM_URL` is set; otherwise the worker stays on the passthrough shim so the rest of the pipeline can be exercised without the engine. Local dev stack at `apps/job_worker/osrm/`. Production deploy of the engine still pending. Tracked under [roadmap § Future — Map matching](roadmap.md). |
| Display matched track on run detail | ✓ | Partial | ✓ | N/A | N/A | **Audit 2026-06-20 corrected Android `✗` → `✓`** (the "Mobile not yet wired" note was stale). Web: `/runs/[id]` fetches `run_matched_tracks` on mount via `fetchRunMatchedTrack`; when `status='matched'` the matched line replaces the raw track on `RunMap`. Stats below the map (distance, splits, elevation, pace zones) continue to derive from the raw track — those are properties of what the runner actually did, independent of how the line is projected. A `match-pill` overlay surfaces `pending` / `failed` / `skipped` so users know whether the worker has run yet. **Android:** `apps/mobile_android/lib/screens/run_detail_screen.dart` background-fetches `run_matched_tracks` via `ApiClient.fetchRunMatchedTrack` (`_maybeFetchMatchedTrack`, L4-best-effort), prefers the matched line on `LiveRunMap` when `status='matched'` (~L751-775), renders the same match-status pill, and adds an owner-only **re-match** action (`_handleRematch` → `enqueueRunRematch`, mirrors web's `handleRematch`). |
| Show-raw-vs-matched debug toggle | ✓ | Partial | N/A | N/A | N/A | Mobile **Settings → Preferences → "Show raw GPS track"** (`Preferences.showRawTrack`, per-device, off by default). When on, `run_detail_screen` forces the raw recorded line via the pure `displayedRunTrack(run.track, _matchInfo, showRaw:)` selector even when a matched track exists; stats still derive from `run.track`. Verification aid. Web N/A — not a GPS-recording surface. |
| Graceful offline fallback + retry-on-reconnect | ✓ | Partial | N/A | N/A | N/A | Mobile `run_detail_screen.dart`. When the matched-track read can't reach the backend, the raw track keeps rendering (L1) and the status pill shows an honest "Offline — will retry" (`MatchPillKind.offline`) instead of a hard error; a transport/unreachable failure is distinguished from a server verdict via `isMatchUnreachableError` (`core_models/run_match_info.dart`). A `matched` row whose gz can't download carries `RunMatchInfo.trackUnreachable` → same state. The screen re-fetches on connectivity return, bounded + idempotent via `shouldRetryMatchFetch` (terminal `failed`/`skipped`/clean-`matched` never re-hit the backend). iOS shares the byte-identical Dart twin. Web N/A — not a GPS-recording surface. See [roadmap § Future — Map matching](roadmap.md). |

---

## Updating this matrix

1. **When adding a feature**, add a row in the most relevant section (or create a new section). Set every platform cell explicitly — don't leave blanks.
2. **Intentional gaps** (e.g. pedometer on web) go in as `N/A` with a one-line Notes entry. The point is that a reviewer can tell the difference between "we haven't built it" and "we can't build it".
3. **Partial** cells should either link to the follow-up ticket in Notes, or state what's missing, so the next visitor can act on it.
4. **When removing a feature**, delete the row — don't mark it `✗` everywhere. The matrix is a living reflection of the product, not a graveyard.
5. **PR hygiene**: the repo's PR template asks you to confirm this doc is up to date when the PR is user-visible. If you skipped the update because the change was internal only, say so in the PR description.
