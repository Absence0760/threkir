# Settings registry

Two jsonb bags drive every user preference in the app:

- **`user_settings.prefs`** — one row per user. Universal settings that should
  follow the user across every device they sign into. Edited from the web and
  mobile settings screens.
- **`user_device_settings.prefs`** — one row per (user, device). Per-device
  overrides of universal settings, plus settings that only make sense on one
  device. Edited from the per-device settings screen.

**Effective value lookup**: device override → universal → client default.
Absent keys fall through; explicit `null` is treated as "unset" and also
falls through. Clients that want "device explicitly opts out of the
universal value" must use a sentinel like `"off"` rather than `null`.

The DB stores an opaque jsonb bag. This file is the registry of known
keys. Adding a new key is a client change + an entry below — no migration.

## Placement rule — bag vs column (F4)

When you add a **user preference**, decide where it lives before reaching for
this registry:

1. **A `prefs` bag key (here)** — the default for a preference. Use it when the
   value is read by *clients* to shape the UI / behaviour and is **never
   filtered, joined, or aggregated in SQL**. No migration, no type regen — just
   a row in the table below and a client default. Almost every preference is
   this.
2. **A real column on a domain table** (`user_profiles`, `runs`, …) — only when
   the value must be queried server-side: a `WHERE` / `ORDER BY` / `GROUP BY`,
   a `CHECK`, an index, or a read by a view / RPC / trigger / RLS policy. This
   is the same bar the [activity-table column checklist](../architecture/conventions.md#activity-table-column-checklist--column-vs-jsonb-bag-f6--decision-d4)
   applies to activity attributes. A column costs a migration + both type
   regens, so only pay it when SQL genuinely needs the value. (`preferred_unit`
   is the live example of the migrate-away-from-a-column direction — it began
   as `profiles.preferred_unit`, is dual-read today, and the column is slated to
   drop once every client prefers the bag.)

Then, **within the bag**, pick the scope (U / D / UD below): default to `UD`,
but ask whether a per-device override is actually meaningful — if it isn't,
make it `U`. A pref that only makes sense on one physical device is `D`.

## Scope shorthand

| Symbol | Meaning |
|---|---|
| U | Universal-only. Stored on `user_settings`; per-device overrides ignored. |
| D | Device-only. Stored on `user_device_settings`; never overlaid from universal. |
| UD | Overridable. Written to `user_settings` by default; a device may override. |

## Keys

| Key | Type | Scope | Default | Description |
|---|---|---|---|---|
| `preferred_unit` | `'km' \| 'mi'` | UD | `km` | Distance unit for all displays. Mirrors `profiles.preferred_unit` today; migrate away from the column in a follow-up. **As a UD key, a per-device override (set on `/settings/devices`) wins over the universal bag, which wins over the profile column.** The web app-wide `setUnit` signal is seeded from the profile column by the auth store, then **re-resolved through the full device→universal→column overlay** in the root `+layout.svelte` (`effectivePreferredUnit` in `settings_overlay.ts`) — otherwise a per-device `mi` override would be silently dead (column-only) and the browser would render km. **The region default is seeded at ACCOUNT CREATION, not just in the onboarding toggle** (issue #488): the client is the only party that knows the browser locale (the server has none at insert time), so the web signup/OAuth callers pass `defaultUnitForLocale(navigator.language)` (mi for US/GB/LR/MM, km elsewhere; `format/locale_defaults.ts` ↔ `locale_defaults.dart`) into the `confirm_age_and_terms(p_preferred_unit)` RPC (migration `20270424000006`), which writes it on the **brand-new-row insert only** — a returning user's `on conflict` update never touches `preferred_unit`, so an explicit choice is never overwritten. The auth-store bootstrap create-branch (`auth.svelte.ts`) applies the same locale default as a belt-and-suspenders for any path that provisions the profile client-side. Because the column now carries the region default, it flows through the whole resolution chain — the app-wide `setUnit` signal, the onboarding units step (`onMount` prefill), the skip-onboarding path, and the `/settings/display` load (which resolves via `effectivePreferredUnit(settings, profile.preferred_unit)`, not a hard-coded km). An explicitly chosen unit (the bag key) still wins over the seeded column. Previously the RPC hard-coded `'km'` here, so region units effectively never applied. |
| `default_activity_type` | `'run' \| 'walk' \| 'hike' \| 'cycle'` | UD | `run` | Pre-selected activity on the watch/phone start screen. |
| `hr_zones` | `{ z1: int, z2: int, z3: int, z4: int, z5: int }` | U | — | HR zone upper bounds in bpm. Used by training plan pace derivation + post-run zone split. |
| `resting_hr_bpm` | `int` | U | — | Resting heart rate. Feeds VDOT estimate if a recent race isn't available. |
| `max_hr_bpm` | `int` | U | — | Max heart rate override. If absent, we fall back to Tanaka `208 − 0.7 × age` (from `date_of_birth`), then a 190-bpm default. See `apps/web/src/lib/training/hr_zones.ts` / `apps/mobile_android/lib/hr_zones.dart`. |
| `date_of_birth` | `YYYY-MM-DD` | U | — | **This bag key is the Art 9 HEALTH-USE MIRROR, not the record of the runner's age.** The two stores carry two different rules ([decisions § 718](../architecture/decisions.md)). This mirror feeds the Art 9 uses — the coach context (`coach/context.ts`, which re-gates on `health_data_consent_at` at read time) and the age-based HR max — so it is written ONLY while consent is granted and is explicitly nulled on withdrawal, by every surface that writes it (`/onboarding`, `/settings/account`, `/settings/body`, mobile Preferences, and the mobile body-metrics withdrawal). The canonical `user_profiles.date_of_birth` column is the **age record**: its consumer is the under-18 exclusion in `search_user_profiles` (migration `20261017_001`, reading the column per `20261104_001`) and `discoverable_runners_near` — a child-protection purpose that must survive a runner declining or withdrawing Art 9 consent — so every entry point writes it whenever a date is supplied, with no consent term, and `withdraw_health_data_consent()` leaves it standing (migration `20270605_001`, [decisions § 721](../architecture/decisions.md)). Erasure is the separate right that does clear it: `delete-account` drops the `auth.users` row and `user_profiles` cascades away. A DOB read that must not go blank after a withdrawal reads the column, not this key. **The converse is the rule the column's readers follow ([decisions § 722](../architecture/decisions.md)): reading the column and feeding it to a HEALTH inference — VO2max, HR max, a calorie target, an age grade, the masters recovery calibration — spends the ungated child-safety record on the gated purpose, so those reads resolve the date through `healthUseDob` (`apps/web/src/lib/core/health_consent.ts` ↔ `apps/mobile_android/lib/health_consent.dart`) and emit null without `health_data_consent_at`.** A surface reading THIS key needs no such gate — the mirror already follows consent in both directions. Both platforms carry a source guard that the health-use surfaces never read the column directly. |
| `cycle_tracking_mode` | `'cycle' \| 'pregnancy'` or absent | U | — (absent = off) | **Art 9 reproductive-health data.** Opt-in mode for cycle/pregnancy-aware plan adjustment (persona runner-woman, decisions §231). Written from `/settings/account` **only when `PUBLIC_CYCLE_PLANS_ENABLED` is on AND `health_data_consent_at` is granted** (fail-closed, same gate + consent as `date_of_birth`); nulled on consent withdrawal or if the flag is off. Read by `/plans/[id]` to offer the "Adjust remaining weeks" action. Absent / unknown → no adjustment. Never queried in SQL (client-read only), so it lives in the bag per the placement rule. |
| `cycle_length_days` | `int` (21–40) | U | — | **Art 9.** Average menstrual-cycle length; anchors the `cycleDayInfo` phase derivation (`training/cycle_plan.ts` ↔ `cycle_plan.dart`). Same consent + flag gate as `cycle_tracking_mode`; only meaningful when `cycle_tracking_mode === 'cycle'`. Out-of-band values are refused by the helper (no adjustment). |
| `cycle_last_period_start` | `YYYY-MM-DD` | U | — | **Art 9.** First day of the last period — the cycle-phase anchor date. Same consent + flag gate; only used in `cycle` mode. |
| `pregnancy_due_date` | `YYYY-MM-DD` | U | — | **Art 9.** Estimated due date; the trimester math (`trimesterForDate`) counts back 40 weeks from it. Same consent + flag gate; only used in `pregnancy` mode. Weeks outside the pregnancy window are left unadjusted. |
| `privacy_default` | `'public' \| 'followers' \| 'private'` | U | `followers` (unset fallback) / `private` (wizard pre-selection) | Default visibility of new runs; per-run override still wins. The onboarding wizard pre-selects `private`; honoured on every run-creation + import path, failing closed to private. Full behaviour in [§ privacy_default detail](#privacy_default-detail). |
| `strava_auto_share` | `bool` | U | `false` | Auto-push every new run to Strava (requires connected integration). |
| `email_notifications` | `'all' \| 'important' \| 'off'` | U | `important` | Which notification kinds are delivered by email. `important` (default) emails event reminders/cancellations, coach plan updates, and direct messages; `all` adds the social loop (kudos, comments, follows, club posts, run-completed); `off` suppresses every email. The in-app bell is unaffected — this gates only the email channel. Read server-side by the Go worker's `notification_email` handler (migration `20261130_001`); the `List-Unsubscribe` header + email footer both link to `/settings/notifications` (`notificationPrefsPath` in `mailer.go`; older mail carries `/settings/preferences`, which is now the landing page that links there). Unknown/non-string values fall back to `important` (fail toward the smaller set). Editable in web Settings → Notifications (`/settings/notifications`, "Email and push" card) and mobile Settings → Preferences ("Notifications" section), both i18n'd across all six locales. |
| `push_notifications` | `'all' \| 'important' \| 'off'` | U | `important` | Which notification kinds are delivered by **push** — **both web push** (system notification to a subscribed browser) **and native push** (FCM/APNs to a locked phone). One "push" channel covers browser + native; there is no separate native pref. Same three-mode shape + `important`-by-default classification as `email_notifications`, but an **independent** key — muting email must not mute push, and vice-versa. Read server-side by the Go worker's `web_push` handler (migration `20261219_001`, fires for users with a `push_subscription` registered on a device, encrypted Web Push per browser) **and the `native_push` handler** (migration `20270212_001`, fires for users with an enabled `device_tokens` row, FCM/APNs per device — `decisions.md § 166`). The mobile Settings toggle also mirrors this choice to the current device's `device_tokens.is_notifications_enabled` flag (the per-device fan-out filter). Unknown/non-string values fall back to `important`, **and so does an absent key** — the mobile setup wizard writes it only when the runner actually taps a level on its notifications step, so an untouched step leaves the key unset rather than recording the registered default as a choice ([decisions § 1650](../architecture/decisions.md)). Editable in web Settings → Notifications (`/settings/notifications`, "Email and push" card) and mobile Settings → Preferences ("Notifications" section), both i18n'd across all six web locales / seven mobile ARBs; the browser subscribe toggle also lives in web Settings. |
| `email_weekly_digest` | `'on' \| 'off'` (opt-IN) | U | `off` | **Opt-IN** consent for the weekly engagement digest (promotional/marketing mail — migration `20270108_001`). Default `off`; ONLY the literal `'on'` opts a recipient in (absent / `'off'` / non-string → skip). Deliberately a **separate** key from `email_notifications` — marketing consent cannot be inferred from a transactional-email setting (CAN-SPAM + GDPR/ePrivacy). Read server-side by the Go worker's `weekly_digest` handler, which **also** hard-blocks any address on `email_suppressions` before sending. The digest carries an RFC 8058 one-click unsubscribe (stateless keyed-HMAC token → `/unsubscribe/weekly-digest`) that flips this key to `off` AND inserts a suppression row; the provider bounce/complaint webhook (`POST /v1/email/bounce`, worker `internal/bouncehook`) writes `bounce`/`complaint` suppression rows. The opt-in toggle ships on web Settings → Notifications (`/settings/notifications`, `email-weekly-digest` checkbox) + mobile Settings → Preferences (`prefsEmailWeeklyDigest` switch, default off). **SEND GATED:** the per-recipient builder is built but UNSCHEDULED — no `pg_cron` ships; enabling an actual digest send is a CISO/counsel-gated step. |
| `email_lifecycle_drip` | `'on' \| 'off'` (opt-IN) | U | `off` | **Opt-IN** consent for the lifecycle drip — staged onboarding / re-engagement / streak nudges (promotional/marketing mail — migration `20270223_001`). Default `off`; ONLY the literal `'on'` opts a recipient in (absent / `'off'` / non-string → skip). A **separate** key from BOTH `email_notifications` (transactional) AND `email_weekly_digest` (the other engagement stream) — opting into one engagement stream is never consent to the other (CAN-SPAM + GDPR/ePrivacy). Read server-side by the Go worker's `lifecycle_drip` handler AND filtered in the `enqueue_lifecycle_drip()` cohort-selection SQL; the handler **also** hard-blocks any address on `email_suppressions` before sending. The drip carries an RFC 8058 one-click unsubscribe (stateless keyed-HMAC token scoped to the drip stream → `/unsubscribe/lifecycle-drip`) that flips this key to `off` AND inserts a suppression row. **SEND GATED:** fail-closed on the unset SMTP credential + CISO/counsel sign-off. The opt-in toggle ships on web Settings → Notifications (`/settings/notifications`, `email-lifecycle-drip` checkbox) + mobile Settings → Preferences (`prefsEmailLifecycleDrip` switch, default off), i18n'd across all six locales — mirroring `email_weekly_digest`. |
| `notify_data_export_ready` | `'on' \| 'off'` (opt-OUT) | U | `on` | Per-KIND mute for the `data_export_ready` notification — the notice that a queued Art 20 export has finished building (migration `20270607_001`, [decisions § 729](../architecture/decisions.md)). Gates the **outbound** channels only (email + both pushes), exactly the scope `email_notifications` / `push_notifications` have; the in-app bell row is never withheld. **Opt-OUT, deliberately the opposite direction from `email_weekly_digest` / `email_lifecycle_drip`**: those are marketing, where an absent choice is an absent consent, whereas this is transactional — the subject requested the export minutes earlier, so an opt-IN default would mean the feature does nothing at all for everyone who has never opened this page, which is the gap it was built to close. It exists because the three-mode channel setting is too blunt an instrument: muting `email_notifications` to stop this one notice also stops direct messages and event-day reminders. **It can only ever subtract** — the worker's `kindMuted` (`mailer.go`) is consulted inside `shouldEmail` / `shouldPush` alongside the channel mode, never instead of it, so `email_notifications='off'` still silences the kind and a per-kind `'on'` can never promote a kind past a channel mute. Only the literal `'off'` mutes: absent / unknown / non-string all read as unmuted, because a corrupt bag is not a decision to stop being told about your own data-rights request. Editable in web Settings → Notifications (`/settings/notifications`, "Email and push" card, `notify-data-export-ready` checkbox), i18n'd across all six locales. **Web-only** per [decisions § 24](../architecture/decisions.md): mobile's Preferences screen enumerates channels and engagement streams, not notification kinds. |
| `locale` | BCP-47 tag (`en` \| `de` \| `fr` \| `es` \| `ja` \| `pt-BR` \| `pt-PT`) | U | — (clients write the active UI locale) | The user's preferred language for **server-sent communications** (email). Web + mobile write the active UI locale into the universal bag so the Go worker can localize emails — see `decisions.md § 120`. **Distinct** from the per-device UI locale that governs what the app shows on a given device (mobile `Preferences.locale`; web client-side detection — `decisions.md § 113`); this key exists only because the server can't read those. The email renderer normalizes unknown/region tags to the seven supported locales and falls back to English — exact tag first, then base language, so `pt-BR` resolves by its own tag while bare `pt` / `pt-AO` / `pt-MZ` / `pt-CV` resolve to `pt-PT`, matching web's `EXACT` / `BASE_TO_LOCALE` ([decisions § 761](../architecture/decisions.md)). The same normalizer decides the language of web push and native push, not only email. Not a per-control toggle — it's written as a side effect of the existing language picker. |
| `discoverable_in_search` | `bool` | U | `true` | When `false`, this user is excluded from People-tab name search (`search_user_profiles` RPC — migration `20261015_001`). The user remains reachable by direct profile URL and via share-page unfurls; this only gates name-string discovery. Persona-hunt Round 3 finding Woman #2. A declared minor (`date_of_birth` under 18) is excluded regardless of this pref — migration `20261017_001`, Round 4 finding family-club #1. |
| `discoverable_nearby` | `bool` | U | `false` | Opt-in to "runners nearby" coarse-location discovery (issue #466, migration `20270424000005`). When the string `'true'`, other opted-in runners near the area the user set (`user_settings.discoverable_area`, a geocoded city/area centroid rounded to ~1 km, NOT live GPS, written via the `set_discoverable_area` RPC) can find them through the `discoverable_runners_near` SECURITY DEFINER reader. Default false / fail-closed: no one appears unless they flipped this AND set an area. The reader also honours minor-exclusion, `shadow_hidden`, `discoverable_in_search` (a search opt-out also removes you from nearby), and blocks; it returns only a coarse distance bucket, never a coordinate. The whole surface is additionally gated behind a default-off feature flag on every client pending owner + CISO/counsel sign-off — web `PUBLIC_ENABLE_NEARBY_RUNNERS` (`social/nearby_flag.ts`), mobile `ENABLE_NEARBY_RUNNERS` (`nearby_flag.dart`, same accepted-affirmative set, forwarded as a `--dart-define` so a release build can be flipped). With the flag off the mobile Settings rows are absent and no `my_discoverable_area` / `discoverable_runners_near` read happens at all. Edited on mobile from the Privacy group of `settings_preferences_screen.dart`; the area itself lives on `nearby_area_screen.dart` (place-search picker + a confirmed "Forget my area"). (decisions §270 + §701). |
| `trusted_contacts` | `TrustedContact[]` | U | `[]` | **Deprecated / dormant (2026-07-06, [safety.md](../features/safety.md)) — no surface reads or writes it any more.** Was the inert Settings→Account "Safety" scaffold (`{ name, phone?, email?, relationship? }` rows, max 5) whose copy implied overdue-run alerts that never existed — the persona-woman CRITICAL conflation finding. The real safety net is the `safety_contacts` table (double opt-in, finish + overdue delivery) plus `safety_overdue_minutes` above. This bag's `phone` was dropped deliberately in 2026-07; an SMS channel later shipped on its own column `safety_contacts.contact_phone` + a contact-controlled `sms_opt_in_at` (feature C / ADR §240, [safety.md](../features/safety.md) § SMS escalation) — NOT on this dormant prefs key. The key stays registered so existing bag data remains readable in DSAR exports; don't add new reads or writes. |
| `primary_goal` | `'general_fitness' \| 'weight_loss' \| '5k' \| '10k' \| 'half_marathon' \| 'marathon'` | U | — | Set by the post-signup `/onboarding` wizard (step 3); web's per-step Skip clears the selection, so a skipped goal step writes nothing ([decisions § 1650](../architecture/decisions.md)). Read back by the finish-step "create my training plan" CTA (`planPresetForGoal` in the `onboarding` twin) to deep-link into the plan wizard preselected — the three beginner-leaning goals seed the walk-run 5K, the distances map 1:1 to `training.ts#GoalEvent`. See `docs/architecture/decisions.md § 78`. |
| `disclosure_level` | `'simple' \| 'standard' \| 'full'` | U | — (absent = derived, see below) | How much of a metric-dense surface renders expanded rather than folded away (issue #905 workstream 1). **Absent is not a default — it means "derive it"**, and the derivation is the first read anything ever made of `primary_goal`: `disclosureLevel(primaryGoal, runCount)` (`apps/web/src/lib/settings/disclosure.ts` ↔ `apps/mobile_android/lib/disclosure.dart`, a [parity pair](../architecture/parity_pairs.md)) takes the higher of two floors — one from the stated goal (the three beginner-leaning answers ask for `simple`, 10K / half for `standard`, marathon for `full`), one from the account's run count (10 runs reaches `standard`, 50 reaches `full`). So an account that predates the key is never shown less than its history already justifies. A value that is not one of the three — a stale spelling, a number, a corrupt bag — falls through to the SAME derivation rather than to a hard-coded default. Edited on web at Settings → Display (`/settings/display`, "How much detail"), where a fourth **Automatic** choice writes `null` and hands the level back to the derivation. Read today by `/dashboard`, which folds the training-load block into a collapsed expander at `simple` and is unchanged at `standard` / `full` — nothing is removed at any level, which is what makes the override enough. Web-only per [decisions § 24](../architecture/decisions.md); the Dart half of the helper exists so the level cannot diverge when mobile grows a surface for it. |
| `dashboard_training_load_expanded` | `bool` | U | `false` | Whether `/dashboard`'s "Training load" expander is open. Only rendered, and only written, while the effective `disclosure_level` is `simple` — at `standard` / `full` the block is not inside an expander at all, so the key sits unread. Universal rather than per-device so opening it once on a laptop is not asked again on a phone: the runner has said they want the numbers, and that is a fact about them, not about the browser. Absent / non-bool reads as closed — the collapsed state is the one the level asked for, so an unreadable bag must not silently re-expand it. Not a settings control; written by the expander itself. |
| `coach_personality` | `'supportive' \| 'drill_sergeant' \| 'analytical'` | U | `supportive` | Tone preset for the Claude coach chat. |
| `multi_modal_nav` | `bool` | U | `false` | **Deprecated / dormant (2026-06-04, decisions §63 amendment) — no surface reads it any more.** Originally the web-only Phase 4 multi-modal feature flag. Web has since been **ungated to match mobile**: the Gym + Nutrition sidebar items are always present, and the `/dashboard` lift cards + lift→load contribution and the `/history` kind chips + unified `activities` timeline self-hide purely on **data presence** (a pure runner sees no clutter on either platform). The key is kept registered (not deleted) so a kill-switch could be reintroduced without a migration, but it is no longer consulted anywhere — do **not** add new reads of it. Mobile never had this flag (it ships gym/nutrition ungated with the `keep_run_primary` "protect the pure runner" toggle). |
| `voice_feedback_enabled` | `bool` | UD | `true` | Speak pace/distance callouts during a run — the MASTER gate the per-cue `voice_cue_types` map sits under. **Default ON** (aligned 2026-08-03, decisions § 474): the phone's local `Preferences.audioCues` has defaulted on since v1, and this row previously said `false` while web's preferences page loaded the same `false` — so web showed the switch off for a runner whose phone was speaking cues. Web now loads through `VOICE_FEEDBACK_ENABLED_DEFAULT` (`settings/voice_cues.ts`); `voice_cues.test.ts` reads the Dart source, the web page source, and this row, and fails on any drift. (Web's pre-2026-06-01 snapshot-save era persisted un-chosen `false` values into some universal bags; those stand — a stored value always beats this default — see § 474.) **`UD`, not `D`** (promoted 2026-08-03, decisions § 472 — the gap § 469's closing note named): web's settings page (`/settings/recording` since the #905 split) has always written this key to the **universal** bag, but while it was registered `D` the phone read only the **device** bag, so the web toggle never reached a device that records. Mobile's "Spoken split announcements" switch keeps writing the device bag (a race phone and a training phone can differ); `SettingsSyncService` applies universal first then device, so the per-phone value wins whenever the device bag names the key, and a key absent from both bags keeps the phone's local default (on). A non-bool value is dropped, never coerced — a corrupt bag must neither silence cues nobody turned off nor un-mute an explicit off. |
| `voice_feedback_verbosity` | `string` | U | `full` | `full` speaks every cue; `minimal` suppresses the chatty in-rep progress + pace-drift nudges. Universal so the choice roams across devices. |
| `voice_feedback_interval_km` | `double` | D | `1.0` | Interval in km between spoken callouts. |
| `voice_cue_types` | `object` | UD | `{}` | Per-cue voice toggles as a map of cue id → bool (`splits`, `start_finish`, `off_route`, `pace_alerts`, `workout_steps`, `cutoff_catch_up`, `marker_targets`, `phase_transitions`; issue #607; `guided_run`, the audio-coached guided-run script — deliberately its own id rather than riding `workout_steps`, so silencing a structured workout's step calls does not also silence a guided run). An **absent id is ON**, so a new cue type defaults audible without a migration, and every apply path merges entry-by-entry so a bag written by an older build can't erase toggles it didn't know about. Turn-by-turn cues keep their own older local-only pref and are not in this map. **`UD`, not `D`** (promoted 2026-08-03): recording is mobile-only, but *choosing which cues speak* is a preference that should roam, and web is the canonical settings surface — a browser is its own `device_id` and never records, so a web-written device value would be unreadable by the phone. Web Settings → Recording & voice (`/settings/recording`, inside the voice-feedback card, hidden until `voice_feedback_enabled` is on) therefore writes the **universal** bag like every other control on that page; the mobile cue switches keep writing the **device** bag, and `SettingsSyncService` applies universal first then device, so a per-phone override still wins per cue. Ids are a wire contract shared between web `settings/voice_cues.ts` and the Dart `VoiceCue` class; `voice_cues.test.ts` reads `preferences.dart` and fails on drift. |
| `haptic_feedback_enabled` | `bool` | D | `true` | Vibration on lap + pace-zone changes. Watches only. |
| `keep_screen_on` | `bool` | D | `true` | Disable OS auto-dim while the running screen is visible. Phones only; watches use ambient mode. The mobile preferences tile now discloses the battery cost of holding the wakelock for a whole run (issue #271). |
| `dim_screen_while_recording` | `bool` | D | `false` | Battery-saver companion to `keep_screen_on` (issue #271): when both are on, the mobile run screen darkens the live map (the dominant bright element) while recording, leaving the live stats + controls readable. Off by default — the historical behaviour is a full-brightness screen. Device-scoped like `keep_screen_on`; only takes effect while `keep_screen_on` is on (with the display already free to sleep there is nothing to dim), so the settings tile is disabled when `keep_screen_on` is off. Phones only (recording is mobile-only); no web control. |
| `push_subscription` | `{ endpoint, keys: { p256dh, auth }, registered_at }` | D | — | One browser's Web Push registration, minted by `apps/web/src/lib/util/push.ts` on subscribe — written via the key-targeted `set_push_subscription` RPC (migration `20270419_001`, issue #235), a single atomic `jsonb_set`/minus so persisting it can never clobber the device's other prefs the way a whole-bag read-merge-write could, with the result checked so a failed registration surfaces instead of leaving the toggle lying. Device-scoped because each browser/device has its own push endpoint. Read server-side by the Go worker's `web_push` handler (migration `20261219_001`) to deliver an encrypted push per device; the worker prunes the key (via the `clear_push_subscription` RPC) when the push service reports the endpoint dead (404/410). Cleared locally on unsubscribe + when the device row is revoked. Keep the shape in lockstep with the handler's `FetchPushSubscriptions` projection. |
| `map_style` | `'streets' \| 'satellite' \| 'outdoors' \| 'dark'` | UD | `streets` | Basemap for every map surface. **Consumed on both clients.** Web resolves it in `buildMapStyleUrl` (`routes/map-style-url.ts`) to a MapLibre `style.json`; mobile ports the same slug switch into `resolveTileUrl` (`widgets/live_run_map.dart`) for its raster `{z}/{x}/{y}` template — `satellite` → `satellite`, `outdoors` → `outdoor-v2`, `dark` → `streets-v2-dark`, and `streets` → `streets-v2` or `streets-v2-dark` **by app theme**. Mobile mirrors the bag value into `Preferences.mapStyle` (SharedPreferences) so the choice survives a cold start offline, and every map surface reads it context-free through `activeMapStyle`; an unknown value normalises to `streets` rather than blanking the map. The `TILE_URL_TEMPLATE` dev override still outranks the preference on both platforms (decisions §68). Share cards are pinned to the dark basemap regardless — they rasterise a fixed dark PNG. See decisions §489. |
| `units_pace_format` | `'min_per_km' \| 'min_per_mi' \| 'kph' \| 'mph'` | UD | `min_per_km` | Display format for pace. Independent of `preferred_unit` so users can keep km distances but pace in mph if they want — but the web preferences page will auto-snap `min_per_km` ↔ `min_per_mi` when the user flips `preferred_unit` so they don't have to update both. Speed formats (`kph`/`mph`) are treated as deliberate choices and are left alone. |
| `undo_window_s` | `8 \| 30 \| 0` (seconds) | U | `8` | How long the undo bar keeps a destructive action reversible. The action's server mutation is **deferred** for this long rather than performed and later compensated, so "Undo" only cancels a pending timer and can never fail (decisions.md § 514). `0` means **no time limit** — the bar waits for an explicit dismiss — which is how the surface meets WCAG 2.2.1 *Timing Adjustable* ("Turn off"), since a countdown must not be the only route to the only undo affordance. An absent, non-numeric, or unrecognised value falls back to `8`, **never to `0`** — a corrupt bag must not silently pin every deletion open until the user navigates. **Web consumers**: read at defer time by `stores/undo.svelte.ts`, hydrated in the root `+layout.svelte`, written from `/settings/display`; normalised by `undoWindowSFromPref` in `core/undo_queue.ts`. Seven adopting deletes as of round 12 — including one inside a `Modal`, which works because the dialog's Tab ring admits the bar's `[data-modal-trap-include]` region (decisions.md § 521). **Mobile consumers** (round 13, decisions.md § 523): the same seven deletes. `SettingsSyncService._applyUniversal` normalises the bag value through the Dart twin `undoWindowSFromPref` (`undo_queue.dart`) into `Preferences.undoWindowS`, which is also persisted locally so the choice survives a cold start offline; `_RunAppState` mirrors it into `widgets/undo_bar.dart`'s module-level window, read at defer time. Settings → Preferences carries the editor beside the dark-mode row — a phone that could only READ this key would leave a user unable to turn the timer off at all. A **present-but-corrupt** value normalises to `8`; an **absent** key leaves the local choice alone rather than resetting it. Universal, not `UD`: it is an accessibility preference and should follow the user to every browser and device they sign into. Never queried in SQL, so it lives in the bag per the placement rule. |
| `weight_unit` | `'kg' \| 'lbs'` | U | `kg` | Display + entry unit for body weight and lift weights (Phase 4 gym/nutrition). **Storage stays canonical kg** — `gym_sets.weight_kg` (and the future `body_metrics`) are always kilograms; this key only changes how the number is shown and parsed on input, the same display-only split as `preferred_unit`. Universal (not `UD`): a per-device weight unit isn't meaningful. Placed in the bag per the [placement rule](#placement-rule--bag-vs-column-f4) — never queried in SQL. **Consumed on web (F19, 2026-06-04):** the Settings → Units & display toggle (`/settings/display`) persists it; the app shell hydrates the `weightUnit` signal in `format/units.svelte.ts` on sign-in; `/gym` + `/gym/[id]` render via `formatWeight` and `GymEditor` parses entry through `parseWeight` (round-trip kg↔lbs in `format/weight.test.ts`). **Also seeded by onboarding:** the `/onboarding` wizard's body-weight step (step 4) derives the unit from the distance choice (`mi` → `lbs`, else `kg`), labels + parses the field in that unit through `parseWeightToKg`, and writes `weight_unit` into the bag on finish — so an imperial runner isn't asked for kg and a typed lbs value can't be stored verbatim as kg. **When the key is UNSET (e.g. a US user who skipped onboarding), the app-wide fallback follows the resolved distance unit** — `lbs` for imperial — via `defaultWeightUnitForDistanceUnit` in `format/weight.ts`, applied in the root `+layout.svelte` and the preferences load, rather than a hard-coded kg (issue #488). Only an explicitly stored `weight_unit` overrides this. Mobile twin (`Preferences` + `gym_compose_sheet.dart`) is the remaining half, tracked in followups.md. |
| `body_weight_kg` | `number` (kg) | U | — | Body weight in canonical kilograms. Drives the run-detail + gym calorie estimate (the calorie path falls through to a documented 70 kg default when unset). Written by the Health Connect importer (`health_connect_importer.dart` seeds it, filtering samples through `withinColumnLimit('body_metrics.weight_kg', …)` so an import cannot seed a weight the body-metrics field would then refuse to re-save — it carried its own wider 30–300 kg until decisions § 819; `import_screen.dart` only fills it when still null), mirrored into mobile `Preferences.bodyWeightKg`, and synced as a universal key by `settings_sync.dart`; web reads it on `/runs/[id]` and writes it from `/onboarding`. Constant is `SettingsKeys.bodyWeightKg` (`packages/api_client/lib/src/settings_service.dart`). **Note the tension with the health-data framing:** weight is special-category data whose *canonical* store is the consent-gated `body_metrics` table (migration `20261216_001`), yet this prefs-bag copy exists and is synced through the ordinary universal-settings path (no `health_data_consent_at` gate on the bag mirror) so the calorie estimate has a value to read on every device. Non-positive / null clears the local cache. |
| `weekly_mileage_goal_m` | `int` | U | — | Target weekly distance in metres. Displayed on the dashboard progress bar and the coach context chip. **Stored in metres, never typed in them** (issue #902 § 5): every editor asks in the account's km/mi and converts at the boundary through the `weekly_goal` parity pair (web `settings/weekly_goal.ts` ↔ `weekly_goal.dart`) — one decimal in the typed unit, whole metres stored, range 0.1–500 in the typed unit, and re-saving the figure the field already shows hands back the stored metres unchanged so an untouched goal never drifts by the display rounding. Web editor: `/settings/training` (`#weekly-distance-goal`, labelled "Weekly distance goal (km)"); mobile: Settings → Preferences → Weekly distance goal, which also seeds the local weekly `RunGoal`. The key name keeps its historical `mileage` spelling — renaming a stored key is a migration of every bag for a label. |
| `nutrition_activity_level` | `'sedentary' \| 'light' \| 'moderate' \| 'active' \| 'very_active'` | U | `moderate` | **Baseline (non-exercise) activity multiplier** for the Mifflin-St Jeor nutrition target (`nutrition_targets.ts`/`.dart`). Since the dynamic-TDEE "base + exercise" model (decisions §63 amendment), this means daily lifestyle EXCLUDING logged workouts — measured run/gym calories are added on top as `exerciseKcal`, so it should reflect a desk-vs-active *day*, not training volume (the labels were reworded accordingly). Not health-special-category (an effort label, not a body measurement), so it lives in the prefs bag and auto-saves like other prefs — unlike height/weight which are consent-gated on `health_data_consent_at`. Phase 4 nutrition (`multi_modal.md`). **Consumed on mobile (G5):** set in Settings → Body metrics (`settings_body_metrics_screen.dart`), read by `loadNutritionTargets`. |
| `nutrition_goal` | `'lose' \| 'maintain' \| 'gain'` | U | `maintain` | Weight-goal calorie delta applied after base TDEE for the nutrition target (lose −500 / maintain 0 / gain +300 kcal). Same placement rationale as `nutrition_activity_level`. Phase 4 nutrition. **Consumed on mobile (G5)** alongside `nutrition_activity_level`. |
| `carbs_per_hour` | `number` (g/hr) | U | `60` | Target carbohydrate intake per hour, backing the roadbook fueling plan (`fuel_plan.ts`/`.dart` `buildFuelPlan` scales it onto each leg's duration). A plain effort/strategy pref, not health-special-category (like `nutrition_activity_level`, not a body measurement), so it lives in the prefs bag and auto-saves — no `health_data_consent_at` gate. Editable in web Settings → Training (`/settings/training`, "Race fuelling" card) and mobile Settings → Preferences. See [race_fueling_plan.md](../features/race_fueling_plan.md). |
| `fluid_per_hour` | `number` (ml/hr) | U | `500` | Target fluid intake per hour, backing the roadbook fueling plan (`fuel_plan.ts`/`.dart`). A non-persisted "Heat" toggle on the roadbook multiplies this for fluid only; the stored value stays the moderate-conditions baseline. Plain pref, not health-special-category — same placement rationale as `carbs_per_hour`. Editable in web Settings → Training (`/settings/training`, "Race fuelling" card) and mobile Settings → Preferences. See [race_fueling_plan.md](../features/race_fueling_plan.md). |
| `show_calories` | `bool` | U | `true` | When `false`, the run-detail calorie-estimate cell is hidden (the estimate silently assumes a 70 kg default when no body weight is set — a weight-conscious runner can opt out entirely; hunt-newrunner 2026-07-02). Read by web `/runs/[id]` and mobile `run_detail_screen` (calorie tile hidden when `false`); editable in web Settings → Units & display (`/settings/display`, "Maps & pages" card) and mobile Settings → Preferences → Units & Display ("Show calorie estimates"). |
| `exclude_gym_from_readiness` | `bool` | U | `false` | When `true`, gym sessions are dropped from the dashboard fitness/fatigue/form (CTL/ATL/TSB) curve so run readiness + recovery advice stay run-only. Default `false` = lifts add to fatigue like a run (the `lift_load.ts`/`.dart` → `training_load` contribution). Read by `/dashboard` on **both web and mobile**; the run-only curve is always recoverable by construction (`training_load.ts`/`.dart` separable provenance), so this is a display-side opt-out, not a data change. Editable in web Settings → Training (`/settings/training`, "Heart Rate Zones" card) and mobile Settings → Preferences. Mobile honours it: the mobile training-load wiring drops lifts from the readiness curve when set, and the dashboard shows a gym-in-fatigue note when lifts are factored in. |
| `week_start_day` | `'monday' \| 'sunday'` | U | `monday` | First day of the week for mileage + plan rollups. The stored/bag default stays `monday`, but the **preferences screens on web and mobile show a locale-derived default when the key is unset** (`defaultWeekStartForLocale` in `format/locale_defaults.ts` ↔ `locale_defaults.dart` — Sunday-first regions like US/CA, Monday elsewhere); a value is only written when the user picks. Other readers (dashboard, plan rollups) still fall back to `monday` until a value is stored. |
| `privacy_zones` | `{ lat: number, lng: number, radius_m: number }[]` | U | `[]` | Geofences clipped from the start and end of any track rendered on a public surface (`/share/run/[id]`, `/share/route/[id]`). The list itself is private to the owner via `user_settings` RLS; the clipped output is what the public sees. See `decisions.md § 33` for the algorithm and known v1 gaps. |
| `safety_overdue_minutes` | `int` (minutes) or absent | U | — (absent = escalation off) | The overdue-escalation silence window ([safety.md](../features/safety.md)). When set (UI presets 15/30/60/120; the SQL scan also requires ≥ 10), a live-broadcast run whose latest ping — or start, if none landed — is older than this many minutes triggers ONE `safety_email` `overdue` job per confirmed safety contact (migration `20270401_001`, pg_cron every 5 min). **Fail-closed:** absent/malformed → the scan never matches, whatever contacts exist. Read **server-side only** (`enqueue_safety_overdue_emails()` reads the bag in SQL — the documented exception to the never-queried placement rule, same class as the worker-read email prefs). Editable on web `/settings/safety` + mobile Settings → Safety contacts. |
| `safety_off_route_alerts` | `bool` | U | `false` | Opt-in for the off-route → auto-notify-contact escalation ([safety.md](../features/safety.md) § feature D). When true AND the runner is on a live-shared run AND has ≥ 1 confirmed safety contact, a sustained departure from the planned route (debounced by `OffRouteAlertDetector`: > 75 m for ≥ 90 s) calls `escalate_run_off_route`, which alerts confirmed contacts via the same `safety_email` (+ additive `safety_sms`) path as the overdue scan, with an `off_route` template. Read **server-side** by the RPC (same never-queried-placement exception as `safety_overdue_minutes`). **Fail-closed:** absent/false → the RPC never enqueues; also gated behind the `OFF_ROUTE_ESCALATION_ENABLED` deploy flag on the client. Editable on web `/settings/safety` + mobile Settings → Safety contacts (both hidden until the flag flips). |
| `auto_live_share` | `bool` | D | `false` | Start a live broadcast automatically when a run recording starts on THIS device ([safety.md](../features/safety.md)). Device-scoped like the other recording prefs (`voice_feedback_interval_km`): it's a property of the phone that records. **Default off and deliberately opt-in** — a live share makes the in-progress run publicly viewable by link (`beginLiveBroadcast` stub, `is_public=true`). The mobile run screen reads it at `_begin()`; failure to start the share is L4 (never blocks the recording). No web control (web doesn't record). |
| `safety_nudge_dismissed_at` | ISO-8601 `string` or absent | D | — (absent = never surfaced) | Throttle anchor for the solo-run safety nudge ([safety.md](../features/safety.md)). The run screen stamps it (UTC ISO-8601, this device only) the moment it surfaces the "recording solo after dark — share a live link" banner, so the nudge is one-time / throttled (`safetyNudgeThrottleMs` = 30 days in `safety_nudge.dart`); a stamp within the window suppresses the nudge. Device-scoped like `auto_live_share` — the nudge is a property of the recording phone. Read + written **client-side only** by the mobile run screen; L4 (a read/write failure never touches the recording, just risks re-surfacing next run — the safe direction). No web/watch use (they don't record). |

### Where each key is edited on web

`/settings/preferences` carried 32 of these on one page until issue #905 split it by what a setting changes ([decisions § 1640](../architecture/decisions.md)). `/settings/preferences` is now a landing page that edits nothing: it lists the groups below, and its old section anchors redirect (`#heart-rate-zones` and `#weekly-mileage-goal` to `/settings/training`, `#body-metrics` to `/settings/body`; `LEGACY_PREFERENCES_ANCHORS` in `lib/settings/preferences_ia.ts`). Every preference page loads and auto-saves through the shared `createPrefsPage` (`lib/settings/prefs_page.svelte.ts`) and renders through `PrefsPage.svelte`, so the coalesced save and the fail-closed load (an alert with Retry, never defaults) are one implementation.

| Page | Keys |
|---|---|
| `/settings/display` — Units & display | `locale` (as a side effect of the language picker), `preferred_unit`, `weight_unit`, `units_pace_format`, `week_start_day`, `map_style`, `undo_window_s`, `show_calories` (theme is per-browser `localStorage`, not a bag key) |
| `/settings/recording` — Recording & voice | `default_activity_type`, `voice_feedback_enabled`, `voice_feedback_verbosity`, `voice_feedback_interval_km`, `voice_cue_types` |
| `/settings/training` — Training | `weekly_mileage_goal_m`, `resting_hr_bpm`, `max_hr_bpm`, `hr_zones`, `exclude_gym_from_readiness`, `carbs_per_hour`, `fluid_per_hour`, `coach_personality` |
| `/settings/privacy` — Privacy & sharing | `privacy_default`, `strava_auto_share`, `discoverable_in_search`, `discoverable_nearby` (flag-gated), `privacy_zones` (plus the per-browser error-report consent) |
| `/settings/notifications` — Notifications | `email_notifications`, `push_notifications`, `notify_data_export_ready`, `email_weekly_digest`, `email_lifecycle_drip` |
| `/settings/body` — Body metrics | `date_of_birth` (mirror), `nutrition_activity_level`, `nutrition_goal` (plus the consent-gated `user_profiles` / `body_metrics` columns) |
| `/settings/account` | `cycle_tracking_mode`, `cycle_length_days`, `cycle_last_period_start`, `pregnancy_due_date`; a second editor of `date_of_birth`, `resting_hr_bpm`, `max_hr_bpm` on the profile card |
| `/settings/safety` | `safety_overdue_minutes`, `safety_off_route_alerts` |
| `/settings/devices` | `haptic_feedback_enabled`, `keep_screen_on`, `push_subscription`; the per-device override of every `UD` key above |

`apps/web/src/routes/settings/settings_ia_guard.test.ts` holds the same map and fails the PR when a registry key has no declared home, when a page stops naming a key it is declared to edit, or when a settings page starts editing a key somewhere undeclared. Adding a key to the table above means adding its home there too. Every control on the six preference pages also carries a one-line explanation (`aria-describedby`, or a `.hint` inside a checkbox's label), pinned by `apps/web/src/lib/control_hints_guard.test.ts` — whose `SURFACES` list has since grown well past them (decisions § 1651, § 1659); read the list rather than assuming this page's scope.

### privacy_default detail

Default visibility of new runs. Per-run override still wins. **Onboarding wizards on both web and mobile pre-select `private`** (privacy-by-default for a new runner — persona #56) and write it as an explicit bag value on finish, so a fresh account starts private and that choice roams + isn't overridden by another device's default. The legacy unset-fallback (for accounts that never set the pref and never ran the wizard) stays `followers` to avoid retroactively hiding existing users' runs. **Honoured on every run-creation path (persona #27):** web `createManualRun`/`saveRun` + the Strava/Garmin ZIP importers, and the server-side imports — the `parkrun-import` + `strava-import` Edge Functions and the Go worker's Strava ingest (`InsertStravaRun`). Only an explicit `public` default publishes; `followers`/`private`/unset and any settings-read error fall closed to private, so importing history never silently publishes it. (The deprecated `strava-webhook` EF rollback path defaults private regardless.)

## Device-local pairing prefs (not in the synced bag)

Some per-device prefs are **never** synced to `user_settings` / `user_device_settings` — they're hardware pairings meaningful only on the device that did the pairing, so they live in raw `SharedPreferences` on mobile (and would live in `localStorage` on web, were any of these surfaces web-relevant). They are listed here so the registry stays the single coordination point even though they don't carry a U/D/UD scope.

| Pref | Owner | Keys | Notes |
|---|---|---|---|
| HR strap | `apps/mobile_*/lib/ble_heart_rate.dart` | `ble_hr_device_id`, `ble_hr_device_name` | Last-paired BLE chest strap. Auto-reconnected at app start. |
| `treadmill_device` | `apps/mobile_*/lib/ble_treadmill.dart` | `treadmill_device_id`, `treadmill_device_name` | Last-paired BLE FTMS treadmill (C3). Device-local, per-device: a treadmill paired on a phone is irrelevant to the user's other devices, so it is deliberately **not** roamed through the settings bags. Paired from Settings → Integrations; auto-reconnected via `connectCached`. When a run is recorded against it, the recorder's treadmill seam tags the run `metadata.indoor_source = 'treadmill'` (see [metadata.md](metadata.md)). |
| Custom-watch `SET1` config | `apps/custom_watch/core/src/settings.rs` (+ the phone's `watch_settings.dart` encoder) | — (a binary frame, not a prefs key) | Every research-tier custom-watch setting the phone pushes — max HR, pacer goal, gear baseline, alert cadences, page mask, timezone offset, ICE card, since [decisions § 374](../architecture/decisions.md) the **auto-lap trigger** (off / 1 or 5 km / 1 or 5 mi / 5, 10 or 30 min of moving time), and since [§ 376](../architecture/decisions.md) the **storm-alert threshold** (hPa of sea-level-reduced fall over the trend window; 0 disarms the banner — the wire field exists, the runner-facing control § 376 refused does not). Deliberately **not** in either settings bag and not a registry key: the values are firmware config for one paired device, they roam via the watch's own `CFG1` flash record rather than via Supabase, and the watch is research-tier ([§ 71](../architecture/decisions.md)) with no product surface. Listed so a future session looking for "where does the watch's auto-lap preference live" finds the answer here rather than adding a bag key for it. The **unit** each rung names is explicit (1 KM vs 1 MI) rather than derived from `preferred_unit` — the watch has no unit preference of its own, so the phone, which does, picks the rung. |

## Client responsibilities

- **On sign-in**: fetch the user's `user_settings` row. If none exists, insert
  one with `prefs = '{}'`.
- **On first launch per device**: mint a stable `device_id` (UUID, stored in
  device-local storage) and upsert a `user_device_settings` row with
  `platform` + a human `label` (e.g. "iPhone 15", "Pixel Watch 2").
- **On settings edit**: write to whichever table matches the key's scope. A
  device that edits a `UD` key writes to `user_device_settings.prefs`
  unless the user is in the universal-settings UI.
- **On read**: merge `user_device_settings.prefs` on top of
  `user_settings.prefs`, fall back to the default in this doc.

## Where it's wired today

- **Dart clients** (`mobile_android`, `mobile_ios`):
  `SettingsService` in [`packages/api_client/lib/src/settings_service.dart`](../../packages/api_client/lib/src/settings_service.dart),
  with string-constant key names in `SettingsKeys` so clients can't drift on
  spellings. Device ID is minted and cached in `Preferences`
  (`mobile_*/lib/preferences.dart`, key `device_id`).
  `SettingsSyncService` lives as a verbatim twin in
  `mobile_android/lib/settings_sync.dart` and `mobile_ios/lib/settings_sync.dart`
  — it pulls both bags on sign-in, overlays `preferred_unit`,
  `voice_feedback_enabled`, and `voice_feedback_interval_km` onto local
  `Preferences`, and exposes `updateUniversal` / `updateDevice`
  passthroughs the settings screen uses for bag-only keys.
  `voice_cue_types` and `voice_feedback_enabled` are read from **both**
  bags (universal first, device second) since they became `UD` — that is
  what lets a choice made on the web settings page reach the phone that
  actually speaks the cues.

  **Offline behaviour**: a `SharedPrefsSettingsCache`
  ([`mobile_*/lib/settings_cache.dart`](../../apps/mobile_android/lib/settings_cache.dart))
  is wired into `SettingsService` in production main.dart. Both bags are
  persisted to `SharedPreferences` after every successful server load
  and after every optimistic local write. On the next cold start the
  cache hydrates `SettingsService` before the network fetch, so the
  Preferences screen renders every bag-backed tile with the correct
  value instantly. A signed-in offline user editing a bag pref applies
  the change to the cache + in-memory state, then if the server push
  fails the change is queued in `PendingSettingsChange` form and
  replayed on the next successful `SettingsService.load()` — the queue
  drain runs the same `applyPrefsChanges` merge on top of the live
  server bag so a concurrent write from another device isn't clobbered.
  Cache keys are user-scoped (and device-scoped for the device bag)
  so a sign-out + sign-in as a different user can't read another
  user's rows. Both mobile
  settings screens edit the full universal + device registry (profile,
  HR, pace, privacy, coach, map style, auto-pause, weekly goal, coach
  personality, Strava auto-share).
- **Web**: [`apps/web/src/lib/settings/settings.ts`](../../apps/web/src/lib/settings/settings.ts).
  Device ID is minted once in `localStorage` (key `run_app.device_id`). The
  Units & display page at `/settings/display` dual-writes `preferred_unit` to
  both `user_profiles.preferred_unit` (legacy column) and the universal bag;
  the page that edits every other key is listed in
  [§ Where each key is edited on web](#where-each-key-is-edited-on-web).

  **Offline behaviour**: a `LocalStoragePrefsCache`
  ([`apps/web/src/lib/settings/settings_cache.ts`](../../apps/web/src/lib/settings/settings_cache.ts))
  fronts every `loadSettings` / `updateUniversal` / `updateDevice` call —
  the web mirror of the mobile pattern above. `loadSettings` returns the
  cached bags synchronously when both are populated and fires a
  background refresh that drains any queued offline writes; the cold
  path (no cache) blocks on the server fetch as before, with a soft
  fall-through to empty bags on network failure so a brand-new offline
  visit still loads. Writes apply to the cache first; on push failure
  the change is queued under
  `settings_cache_pending_<userId>_<deviceId>` and replayed against a
  fresh server bag on the next successful refresh. **Only a transport
  failure queues** ([`settings_write.ts`](../../apps/web/src/lib/settings/settings_write.ts),
  decisions §559): a write PostgREST *refused* — an RLS denial, a CHECK
  violation, an expired JWT — rolls the optimistic cache entry back,
  rejects out of `updateUniversal` / `updateDevice`, and never enters the
  queue, so the page shows the failure instead of "Saved" and the drain
  can't retry a doomed write forever. Cache keys are
  user- + device-scoped, and the auth store's `logout()` calls
  `dropUserCache(userId)` so a subsequent sign-in as a different user
  on the same browser can't read or replay against another account.
  This lifts the dashboard's Fitness / Intensity / weekly-goal /
  unit-preference reads off the critical network path on every visit
  after the first.
- **`profiles.preferred_unit`** is dual-read during the transition — newer
  clients prefer the bag and fall back to the column. A follow-up migration
  drops the column once every client has cut over.
- **Not yet wired**: `watch_ios` (Swift), `watch_wear` (Kotlin), and the
  per-device settings editor UI on any client (the DB holds the device
  rows, but no phone-side UI lets the user override a universal value on
  a specific device yet). The DB + registry are ready; adding surfaces on
  those is ~30 min each.

## Adding a new key

1. Add the row to the table above. Pick the scope deliberately — defaulting to
   `UD` is fine but ask whether a per-device override is actually meaningful.
2. Add the default to a shared constants file in each client (TBD — today
   defaults are scattered).
3. Expose the control in the universal settings screen on web + mobile.
4. For `D`/`UD` keys, expose the override in the per-device settings screen.

No DB migration is required. If you ever need server-side validation of a
specific key (e.g. a function that reads `hr_zones` and must reject malformed
shapes), add the check in that function — not as a DB constraint — so the
registry stays the one source of truth.
