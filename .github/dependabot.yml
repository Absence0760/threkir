# Dependabot configuration
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
#
# Strategy:
# - One update schedule per ecosystem × workspace combo. Weekly on
#   Monday at 06:00 UTC — low-noise, predictable, lands before the
#   week's work starts.
# - Aggressive grouping. Each ecosystem opens at most a couple of PRs
#   per week — one for the related-package family (e.g. svelte-kit
#   ecosystem, supabase clients, AGP / Kotlin) and one catch-all
#   `minor-and-patch` for everything else. Major bumps stay
#   ungrouped so they get individual review (decisions.md §39 — mobile
#   dep bumps frequently break the Android build, e.g. flutter_reactive_ble
#   pinning compileSdk 33 forced an `afterEvaluate` hook in
#   apps/mobile_android/android/build.gradle.kts).
# - GitHub Actions are SHA-pinned per the /audit/all GHA pinning pass;
#   Dependabot keeps those SHAs current so the pinning doesn't go
#   stale.
# - Mobile twin caveat: apps/mobile_android/pubspec.yaml is the
#   tracked Flutter target. apps/mobile_ios/pubspec.yaml is a manual
#   mirror per the lib/+test/ byte-identical-twin invariant
#   (decisions.md §39). When merging a Dependabot PR for mobile_android,
#   mirror the version bump into apps/mobile_ios/pubspec.yaml in the
#   same commit. The shared-library-syncer agent helps catch drift.
# - Major version bumps for ungrouped packages open as individual PRs
#   and require review — see audit logs for the sentry_flutter 9.20
#   K2 incompatibility (apps/mobile_android/android AGP 8.13 fix) and
#   the flutter_blue_plus → flutter_reactive_ble migration that
#   wasn't caught by version bumps alone.

version: 2
updates:
  # ---------------------------------------------------------------------------
  # npm — root workspace (covers apps/web, apps/backend, apps/web/lambda/coach)
  # ---------------------------------------------------------------------------
  # The repo uses npm workspaces with a single root lockfile, so one
  # entry covers every package.json under it. Updates surface as
  # `chore(deps)` and the actual workspace is in the file paths.
  #
  # Run `pnpm sync:deno-lock` on the branch before merging — deno.lock's
  # `workspace` section transcribes these package.json specs, Dependabot
  # doesn't know about it, and the parity-types job fails on the drift
  # (decisions.md § 706). Same shape as the mobile-twin caveat in the
  # header above: auto-merge only queues on the required checks, so the
  # PR waits for the human step rather than landing without it. Having
  # CI push the sync onto this branch was refused, not deferred —
  # decisions.md § 711.
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(deps)"
      include: "scope"
    groups:
      sveltekit-ecosystem:
        patterns:
          - "svelte"
          - "svelte-*"
          - "@sveltejs/*"
          - "vite"
          - "@vite/*"
      supabase:
        patterns:
          - "@supabase/*"
          - "supabase"
      anthropic-and-llms:
        patterns:
          - "@anthropic-ai/*"
          - "openai"
      playwright:
        patterns:
          - "@playwright/*"
          - "playwright"
      types:
        patterns:
          - "@types/*"
      eslint-prettier:
        patterns:
          - "eslint"
          - "eslint-*"
          - "@typescript-eslint/*"
          - "prettier"
          - "prettier-*"
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  # ---------------------------------------------------------------------------
  # pub — Flutter (apps/mobile_android, the canonical target)
  # ---------------------------------------------------------------------------
  # Mirror to apps/mobile_ios/pubspec.yaml manually before merging —
  # see header comment.
  - package-ecosystem: "pub"
    directory: "/apps/mobile_android"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 4
    commit-message:
      prefix: "chore(mobile/deps)"
      include: "scope"
    groups:
      flutter-core:
        patterns:
          - "flutter"
          - "flutter_*"
          - "cupertino_*"
      supabase:
        patterns:
          - "supabase_*"
          - "postgrest"
          - "gotrue"
          - "realtime_*"
          - "storage_client"
      sentry:
        patterns:
          - "sentry"
          - "sentry_*"
      ble:
        patterns:
          - "flutter_reactive_ble"
          - "reactive_ble_*"
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  # ---------------------------------------------------------------------------
  # pub — shared Dart packages: NOT supported by Dependabot
  # ---------------------------------------------------------------------------
  # The packages/ subdirectories are members of a Melos / pub workspace
  # rooted at the repo root. Dependabot's pub ecosystem can only run
  # `pub dependency_services` against the workspace root and fails on
  # any sub-package with: `dependency_services failed: Only apply
  # dependency_services to the root of the workspace.` Removed the
  # five per-package entries (api_client / core_models / gpx_parser /
  # run_recorder / ui_kit) — they're guaranteed-fail every Monday.
  #
  # Coverage isn't lost: every dep in those packages is also pulled
  # transitively by `apps/mobile_android` (which imports all five
  # via path: refs), and the apps/mobile_android pub entry above does
  # update those transitive constraints. If pub workspaces ever land
  # in Dependabot, restore one entry rooted at /, not per-package.

  # ---------------------------------------------------------------------------
  # gomod — apps/job_worker (the map_match background worker)
  # ---------------------------------------------------------------------------
  - package-ecosystem: "gomod"
    directory: "/apps/job_worker"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "chore(job_worker/deps)"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  # ---------------------------------------------------------------------------
  # docker — apps/job_worker (the only Dockerfile in the repo)
  # ---------------------------------------------------------------------------
  # Picks up base-image bumps (currently the Go builder + the distroless
  # final stage). Surfaces via the Security workflow's Trivy scan when a
  # known CVE hits an in-use base image; Dependabot here closes the
  # loop by opening a PR to bump the FROM line. Low PR volume — base
  # image churn is slow.
  - package-ecosystem: "docker"
    directory: "/apps/job_worker"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "chore(job_worker/docker)"
      include: "scope"

  # ---------------------------------------------------------------------------
  # gradle — apps/watch_wear (native Kotlin + Compose-for-Wear)
  # ---------------------------------------------------------------------------
  # AGP 9.x + Kotlin 2.3.x + Gradle 9.4.x as of the last bump. AGP /
  # Kotlin / Compose ride together — group them tightly so a major
  # bump on one doesn't ship without the others.
  - package-ecosystem: "gradle"
    directory: "/apps/watch_wear/android"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "chore(watch_wear/deps)"
      include: "scope"
    groups:
      androidx:
        patterns:
          - "androidx.*"
      kotlin-and-compose:
        patterns:
          - "org.jetbrains.kotlin*"
          - "androidx.compose*"
      health-services:
        patterns:
          - "androidx.health*"
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"
    ignore:
      # Hold the Kotlin Gradle plugins below CodeQL's extractor ceiling: a
      # bump past it does not degrade the scan, it fails the codeql-kotlin
      # Security job from inside a compile task and loses the whole
      # java-kotlin analysis (this bit us when the minor-and-patch group
      # shipped #68 → Kotlin 2.4.0). The ceiling has MOVED since: bundle
      # 2.27.0, which the pinned github/codeql-action SHA ships, refuses
      # only >= 2.4.20 (measured against the CLI in decisions § 1656), so
      # these two entries are now tighter than the fact. Left that way
      # deliberately — this app's Kotlin moves together with its
      # compose-compiler plugin against AGP 9.4.0, which is its own
      # decision; followups.md carries it. Relax to >= 2.4.20 when taking
      # it, in lockstep with the pin + comment in
      # apps/watch_wear/android/settings.gradle.kts and the CLAUDE.md note.
      - dependency-name: "org.jetbrains.kotlin.plugin.compose"
        versions: [">=2.4.0"]
      - dependency-name: "org.jetbrains.kotlin.plugin.serialization"
        versions: [">=2.4.0"]

  # ---------------------------------------------------------------------------
  # gradle — apps/mobile_android/android (Flutter Android shell)
  # ---------------------------------------------------------------------------
  # Most plugin deps come via Flutter's pub side; this stack tracks
  # AGP, Kotlin, and the Gradle plugin pins in settings.gradle.kts.
  # AGP and Kotlin must move together — Kotlin 2.2 needs AGP ≥ 8.13
  # (the bump documented in commit 4a5eb1f), so group them.
  - package-ecosystem: "gradle"
    directory: "/apps/mobile_android/android"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "chore(mobile_android/deps)"
      include: "scope"
    groups:
      agp-and-kotlin:
        patterns:
          - "com.android.*"
          - "org.jetbrains.kotlin*"
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"
    ignore:
      # The Gradle wrapper is capped at the version AGP 8.13.0 tolerates.
      # Gradle 9.6.0 broke AGP 8.13's AndroidProblemReporterProvider at
      # config time (the APK smoke build failed, run 28027750381), and
      # this group auto-merges minor/patch — so an unpinned wrapper bump
      # silently lands a broken toolchain on main. Hold it at 9.5.x;
      # lift this when the agp-and-kotlin group moves AGP to a version
      # that supports a newer Gradle (keep gradle-wrapper.properties in
      # lockstep with whatever ceiling that AGP documents).
      - dependency-name: "gradle-wrapper"
        versions: [">= 9.6"]
      # AGP is held at 8.x until the Flutter plugin ecosystem finishes its
      # built-in-Kotlin migration (flutter/flutter#181557, open P1 — Flutter's
      # own guidance is still "don't adopt AGP 9 yet"). AGP 9 turns built-in
      # Kotlin on and rejects any module that applies KGP, so the Flutter tool
      # auto-writes `android.builtInKotlin=false` + `android.newDsl=false` into
      # gradle.properties (Flutter 3.44's DisableBuiltInKotlinMigration /
      # DisableNewDslMigration) to keep the legacy KGP path alive for the ~15
      # unmigrated plugins in this app's graph. file_picker 11.x bets the other
      # way — it skips `apply plugin: 'org.jetbrains.kotlin.android'` when it
      # detects AGP >= 9 and relies on built-in Kotlin, which Flutter has just
      # switched off — so nothing compiles its Kotlin and the app's
      # GeneratedPluginRegistrant fails with "cannot find symbol
      # FilePickerPlugin" (PR #563, run 29721601600). Setting either flag by
      # hand is a no-op (the migrator already wrote them) and flipping
      # builtInKotlin on just moves the failure onto the other 15 plugins —
      # this needs the plugin authors, not a build-file change here. Lift once
      # flutter/flutter#181557 closes; keep settings.gradle.kts in lockstep.
      - dependency-name: "com.android.application"
        versions: [">= 9"]
      # Held behind the SAME wall as the AGP pin above, and it has to be, or
      # the two pins contradict each other. connect-client 1.2.0-alpha04
      # declares `requires AGP >= 9.1.0` and `compile against API >= 37` in its
      # AAR metadata, so `:app:checkReleaseAarMetadata` fails the APK smoke
      # build outright (PR #684, run 30789381005) — this app is on AGP 8.13.2
      # with `compileSdk = flutter.compileSdkVersion` (36), and the entry above
      # is the reason it cannot simply move. Nothing in this repo can satisfy
      # it: bumping AGP is exactly what flutter/flutter#181557 blocks.
      # Pinned at the working alpha02 rather than at alpha03 because alpha03's
      # own floor is untested here and the lift condition is the same either
      # way — when the AGP pin above lifts, re-evaluate this one WITH it and
      # take the newest version whose metadata the new toolchain satisfies.
      - dependency-name: "androidx.health.connect:connect-client"
        versions: ["> 1.2.0-alpha02"]
      # The Kotlin Gradle plugin is held below CodeQL 2.27.0's extractor
      # ceiling, which is what lets `security.yml`'s `codeql-kotlin` job
      # scan this host at all. 2.4.20 is refused outright — "Kotlin version
      # 2.4.20 is too recent. CodeQL currently supports versions below
      # 2.4.20" — from inside a plugin subproject's compile task, so the
      # bump does not degrade the scan, it fails the job and takes the
      # whole java-kotlin analysis with it. `apps/watch_wear/android` is
      # held the same way for the same reason. Raise this only when the
      # bundle the pinned `github/codeql-action` SHA ships raises the
      # ceiling; keep settings.gradle.kts in lockstep.
      - dependency-name: "org.jetbrains.kotlin.android"
        versions: [">= 2.4.20"]

  # ---------------------------------------------------------------------------
  # terraform — infra stacks
  # ---------------------------------------------------------------------------
  # Six directories under infra/, one entry each. Provider major
  # bumps (hashicorp/aws v5 → v6 etc.) need careful review since the
  # state schema can shift; ungrouped majors get individual PRs.
  - package-ecosystem: "terraform"
    directory: "/infra/bootstrap"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "chore(infra/deps)"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "terraform"
    directory: "/infra/dns"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "chore(infra/deps)"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "terraform"
    directory: "/infra/github-oidc"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "chore(infra/deps)"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "terraform"
    directory: "/infra/modules/web-stack"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "chore(infra/deps)"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "terraform"
    directory: "/infra/envs/prod"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "chore(infra/deps)"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "terraform"
    directory: "/infra/envs/preview"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "chore(infra/deps)"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  # ---------------------------------------------------------------------------
  # GitHub Actions workflows
  # ---------------------------------------------------------------------------
  # The /audit/all GHA pinning pass replaced every `@vN` ref with a
  # commit SHA. Dependabot keeps those SHAs current — without it the
  # pinning is fire-and-forget and rots silently.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "chore(ci)"
      include: "scope"
    groups:
      all-actions:
        patterns:
          - "*"
