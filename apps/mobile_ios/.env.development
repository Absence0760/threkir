SUPABASE_URL=http://10.0.2.2:54321
SUPABASE_ANON_KEY=
MAPTILER_KEY=
# USDA FoodData Central — second nutrition food-search source, queried from
# the device (api.nal.usda.gov) alongside the keyless Open Food Facts source.
# Gated FAIL-CLOSED: unset → the USDA source is simply not queried and Open
# Food Facts still works, no error. USDA = a new outbound personal-data flow
# (the search term leaves the device) — see the nutrition privacy checklist +
# decisions §188 before going live.
USDA_FDC_API_KEY=
# Optional dev override that points the live-run / run-detail map
# at a different raster-tile URL template (typically a local
# Protomaps tileserver-gl). Set, the override wins outright;
# empty, the app falls back to the MapTiler URL above. Use
# `http://10.0.2.2:8080/...` (not `localhost`) on the Android
# emulator. Boot the local server with `bin/protomaps-dev.sh start`
# from the repo root. See docs/ops/protomaps_local_setup.md +
# decisions.md § 68.
TILE_URL_TEMPLATE=
# Dev-only seed auto-login: set these to the seed user (runner@test.com /
# testtest) and the app signs in automatically on launch so you skip the
# sign-in screen. Only fires when SUPABASE_URL is a loopback address
# (dev_auto_login.dart) — never against production. `pnpm dev:run:android:seed`
# passes them via --dart-define so you don't even need to set them here.
DEV_USER_EMAIL=
DEV_USER_PASSWORD=
# Apple sign-in on Android runs through Apple's web flow, which requires
# an Apple Developer "Services ID" + a return URL allow-listed on that
# Services ID (use the Supabase project's auth callback,
# https://<project-ref>.supabase.co/auth/v1/callback). Gated FAIL-CLOSED:
# with either unset the Apple button shows a friendly "coming soon"
# notice instead of the guaranteed plugin error — mirrors the
# GOOGLE_WEB_CLIENT_ID gate. iOS uses the native flow and ignores both.
# See local_testing.md § Apple Sign-In.
APPLE_SERVICE_CLIENT_ID=
APPLE_REDIRECT_URI=
# Base URL the "Share live link" action stitches the run id onto so the
# resulting `/live/{run_id}` URL points at the right spectator host.
# Defaults to the production host when unset.
WEB_BASE_URL=https://app.runapp.com
# OSRM base URL — e.g. https://osrm.threkir.com. When set, the
# in-app route builder issues `/route` + `/nearest` requests against
# this host. Empty → dev falls back to `https://router.project-osrm.org`
# (the community demo). **Release builds throw on first routing call
# when this is empty** — the public demo has no DPA and must not
# receive production GPS waypoints. See
# `apps/job_worker/osrm/` for the self-hosted instance + decisions
# §53 / audit/third-party-data-flows (2026-05-25).
OSRM_URL=
# Go live-hub base URL — e.g. https://live.threkir.com. When set,
# the recorder pushes live spectator pings to
# `${LIVE_HUB_URL}/v1/live/{run_id}/push` (via `LiveHubClient`)
# instead of inserting into Supabase `live_run_pings`. Empty →
# falls back to the legacy Supabase path. See
# `apps/mobile_android/lib/live_hub_client.dart` + the server side
# at `apps/job_worker/internal/livehub/`.
LIVE_HUB_URL=
# BYPASS_PAYWALL is kept out of THIS template, but IS set in the committed
# `.env.local` (the working local-dev config) so the free seed user can
# exercise Pro features. The Android client reads it only in DEBUG flutter
# builds; release builds ignore the flag, so it can't ship in an APK.

# ── Fail-closed feature gates ────────────────────────────────────────────
# Every one of these parses through `isTruthyFlagValue` (lib/env_flag.dart),
# so `1` / `true` / `yes` / `on` all enable — trimmed, case-insensitive — and
# anything else, including unset, leaves the feature unreachable. The same
# four values enable the matching `PUBLIC_*` flag on web (decisions §709).
#
# Each is ALSO a `--dart-define`, which is the only path that reaches a
# release build (`.env.development` is debug-only). Flipping one on in
# production is the sign-off-gated action; the code path itself ships either
# way.
OFF_ROUTE_ESCALATION_ENABLED=
ADAPTIVE_FITNESS_GATE=
WEIGH_IN_GATE=
ENABLE_NEARBY_RUNNERS=
