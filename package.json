{
  "name": "run-app-root",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/backend"
  ],
  "scripts": {
    "//-- setup --": "One-time workspace bootstrap: full setup, or just the npm / Flutter+Melos halves.",
    "setup": "bash scripts/setup.sh",
    "setup:install": "pnpm install",
    "setup:flutter": "dart pub get && melos bootstrap",

    "//-- dev --": "Backend-stack orchestrators: dev:core = Supabase + seed + tiles + adb reverse + Ollama check; dev:full adds the job worker + OSRM/GraphHopper. Apps still start via dev:run:*.",
    "dev:core": "bin/dev-core.sh",
    "dev:full": "bin/dev-full.sh up",
    "dev:full:down": "bin/dev-full.sh down",
    "dev:full:status": "bin/dev-full.sh status",
    "dev:full:logs": "bin/dev-full.sh logs",

    "//-- db --": "Local Supabase stack. Ports: API 54321, DB 54322, Studio 54323, Mailpit 54324.",
    "dev:db:up": "cd apps/backend && supabase start",
    "dev:db:down": "cd apps/backend && supabase stop",
    "dev:db:reset": "cd apps/backend && supabase db reset && cd ../.. && node scripts/seed-run-tracks.mjs",
    "dev:db:status": "cd apps/backend && supabase status",
    "dev:db:studio": "xdg-open http://127.0.0.1:54323",
    "dev:db:mailpit": "xdg-open http://127.0.0.1:54324",
    "dev:db:psql": "psql postgresql://postgres:postgres@127.0.0.1:54322/postgres",
    "dev:db:logs": "docker logs -f supabase_db_backend",
    "dev:db:seed-tracks": "node scripts/seed-run-tracks.mjs",

    "//-- run --": "Start one app against the local stack, each in its own terminal. Web on :7777; :seed variants auto-sign-in runner@test.com.",
    "dev:run:web": "pnpm -C apps/web dev",
    "dev:run:web:preview": "pnpm -C apps/web preview",
    "dev:run:fns": "cd apps/backend && supabase functions serve --env-file .env.local",
    "dev:run:android": "cd apps/mobile_android && flutter run",
    "dev:run:android:seed": "cd apps/mobile_android && flutter run --dart-define=DEV_USER_EMAIL=runner@test.com --dart-define=DEV_USER_PASSWORD=testtest",
    "dev:run:ios": "cd apps/mobile_ios && flutter run",
    "dev:run:ios:seed": "cd apps/mobile_ios && flutter run --dart-define=DEV_USER_EMAIL=runner@test.com --dart-define=DEV_USER_PASSWORD=testtest",
    "dev:run:worker": "cd apps/job_worker && GOTOOLCHAIN=auto go run .",

    "//-- routing --": "OSRM (:5000) + GraphHopper (:8989) routing engines. dev:setup:* builds the graphs once; dev:run:* serves them.",
    "dev:setup:osrm": "cd apps/job_worker/osrm && make download && make build",
    "dev:run:osrm": "node scripts/dev_run_osrm.mjs",
    "dev:setup:graphhopper": "cd apps/job_worker/graphhopper && make seed && make build",
    "dev:run:graphhopper": "node scripts/dev_run_graphhopper.mjs",

    "//-- payments --": "Stripe test-mode dev loop: functions serve + stripe listen webhook forwarding (bin/payments-dev.sh).",
    "dev:payments": "bin/payments-dev.sh start",
    "dev:payments:replay": "bin/payments-dev.sh replay",
    "dev:payments:status": "bin/payments-dev.sh status",
    "dev:payments:stop": "bin/payments-dev.sh stop",

    "//-- tiles --": "Self-hosted Protomaps tile server in Docker on :8080 — no MapTiler quota burn (bin/protomaps-dev.sh).",
    "dev:tiles:up": "bin/protomaps-dev.sh start",
    "dev:tiles:down": "bin/protomaps-dev.sh stop",
    "dev:tiles:restart": "bin/protomaps-dev.sh restart",
    "dev:tiles:status": "bin/protomaps-dev.sh status",
    "dev:tiles:logs": "bin/protomaps-dev.sh logs",
    "dev:tiles:fetch": "bin/protomaps-dev.sh fetch",
    "dev:tiles:env": "bin/protomaps-dev.sh env",

    "//-- emu --": "Android emulator + iOS simulator helpers. emu:ios:* need macOS — xcrun and the iOS simulator don't exist on Linux.",
    "emu:android:list": "flutter emulators",
    "emu:android:launch": "flutter emulators --launch",
    "emu:ios:list": "xcrun simctl list devices available",
    "emu:ios:launch": "flutter emulators --launch apple_ios_simulator",

    "//-- watch --": "Custom-watch firmware (research-tier, decisions §71/§80): thin aliases for bin/watch-*.sh — cargo, probe-rs, Renode. watch:test, watch:sim and watch:shots need no board.",
    "watch:doctor": "bin/watch-doctor.sh",
    "watch:build": "bin/watch-build.sh",
    "watch:test": "bin/watch-test.sh",
    "watch:flash": "bin/watch-flash.sh",
    "watch:logs": "bin/watch-logs.sh",
    "watch:sim": "bin/watch-sim.sh",
    "watch:sim:gui": "bin/watch-sim.sh --gui",
    "watch:sim:gui:idle": "bin/watch-sim.sh --gui --no-autostart",
    "watch:sim:idle": "bin/watch-sim.sh --no-autostart",
    "watch:view": "bin/watch-view.sh",
    "watch:shots": "bin/watch-shots.sh",
    "watch:preview": "bin/watch-preview.sh",
    "watch:monitor": "bin/watch-monitor.sh",

    "//-- gen --": "Committed generated artifacts. Schema codegen: run gen:all after every migration; both outputs are committed and CI's parity-types job fails on drift (docs/architecture/schema_codegen.md). gen:icon-font re-cuts the web icon font when an icon is added; the build-web job fails on drift (decisions § 780).",
    "gen:all": "pnpm gen:types && pnpm gen:dart",
    "gen:types": "pnpm -C apps/backend gen:types",
    "gen:types:check": "pnpm -C apps/backend gen:types:check",
    "gen:dart": "dart run scripts/gen_dart_models.dart",
		"gen:icon-font": "node scripts/gen_web_icon_font.mjs",
		"gen:exercise-fold-table": "node scripts/gen_exercise_fold_table.mjs",

    "//-- sync --": "deno.lock's `workspace` section is generated from the npm workspace's package.json files — re-sync it after any npm dep bump. Needs deno, but no network, no node_modules and no Docker. CI's parity-types job runs the :check half.",
    "sync:deno-lock": "node scripts/sync_deno_lock.mjs",
    "sync:deno-lock:check": "node scripts/sync_deno_lock.mjs --check",

    "//-- build --": "Production builds + install-to-device.",
    "build:web": "pnpm -C apps/web build",
    "build:android": "cd apps/mobile_android && flutter build apk",
    "build:ios": "cd apps/mobile_ios && flutter build ios --no-codesign",
    "build:worker": "cd apps/job_worker && go build .",
    "install:android": "cd apps/mobile_android && adb install build/app/outputs/flutter-apk/app-release.apk",
    "install:android:debug": "cd apps/mobile_android && adb install build/app/outputs/flutter-apk/app-debug.apk",

    "//-- check --": "Static analysis. check:flutter exits non-zero on info-level lints — only warning/error matter (see CLAUDE.md). check:pnpm-overrides verifies the security pins are declared in both lockfiles AND actually resolved to; it never rewrites a lockfile — fix drift with 'pnpm install --lockfile-only'. CI's parity-types job runs it. check:script-types and check:cloudfront-types are the two tsc roots at this level — this repo has no root tsconfig.json, so until decisions §757 every guard script here was in no tsc program at all; check:tsconfig-coverage is what stops a new tree slipping back outside them.",
    "check:web": "pnpm -C apps/web check",
    "check:flutter": "melos exec -- dart analyze",
    "check:pnpm-overrides": "node scripts/check_pnpm_overrides.mjs",
    "check:script-types": "tsc -p tsconfig.scripts.json",
    "check:cloudfront-types": "tsc -p tsconfig.cloudfront.json",
    "check:tsconfig-coverage": "node --test scripts/tsconfig_coverage.test.mjs",
    "check:doc-links": "node scripts/check_doc_links.mjs",
    "check:test-inventory": "node scripts/check_test_inventory_counts.mjs",
    "check:conflict-markers": "node scripts/check_conflict_markers.mjs",
    "check:adr-numbers": "node scripts/check_adr_numbers.mjs",

    "//-- test --": "Test suites per surface; test:scripts validates this file's script targets.",
    "test:web:unit": "pnpm -C apps/web test:unit",
    "test:web:e2e": "pnpm -C apps/web test:e2e",
    "test:web:e2e:ui": "pnpm -C apps/web test:e2e:ui",
    "test:flutter": "melos exec -- flutter test",
    "test:worker": "cd apps/job_worker && go test ./...",
    "test:scripts": "node scripts/check_root_scripts.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "svelte": "5.57.0"
  },
  "overrides": {
    "cookie": "^1.0.2",
    "devalue": "^5.8.1",
    "undici": "^7.28.0",
    "brace-expansion": "^5.0.8",
    "@sveltejs/kit": {
      "cookie": "^1.0.2",
      "devalue": "^5.8.1"
    }
  },
  "pnpm": {
    "overrides": {
      "cookie": "^1.0.2",
      "devalue": "^5.8.1",
      "undici": "^7.28.0",
      "brace-expansion": "^5.0.8",
      "@sveltejs/kit>cookie": "^1.0.2",
      "@sveltejs/kit>devalue": "^5.8.1"
    }
  },
  "_overrides_rationale": "/audit/all High (deps): @sveltejs/kit@2.59.x pulls cookie@^0.6.0, which has GHSA-pxg6-pf52-xh8x (out-of-bounds chars in name/path/domain). Upstream hasn't bumped yet. Top-level override deduplicates direct copies but npm leaves SvelteKit's nested node_modules/@sveltejs/kit/node_modules/cookie@0.6.0 in place because the v0.x → v1.x range jump is outside SvelteKit's declared dependency range. The nested @sveltejs/kit > cookie override forces that transitive copy too. The API surface SvelteKit uses is the v0.x compatible subset, so the bump is safe in practice. Drop both entries once @sveltejs/kit ships its own bump. devalue override: @sveltejs/kit@2.59.1 declares devalue ^5.6.4 which still resolves to 5.8.0 (GHSA-77vg-94rm-hx3p — sparse-array DoS). Patch fix is in 5.8.1, inside Kit's declared range, so the override just nudges resolution to the patched version. Drop once Kit's lockfile picks it up upstream. undici override: the only undici in the tree is transitive via isomorphic-dompurify > jsdom > undici, pinned by jsdom@29.1.1 to ^7.25.0 which resolved to 7.27.2 — vulnerable to GHSA-vmh5-mc38-953g / GHSA-p88m-4jfj-68fv / GHSA-vxpw-j846-p89q / GHSA-hm92-r4w5-c3mj / GHSA-pr7r-676h-xcf6 (issue #103, all the SOCKS5/cookie/websocket/cache CVEs fixed in 7.28.0). ^7.28.0 sits inside jsdom's ^7.25.0 range so it's a safe nudge to the patched line. Drop once jsdom ships a lockfile past 7.28.0 upstream. brace-expansion override: apps/web > @sentry/sveltekit > @sentry/vite-plugin > @sentry/bundler-plugins > glob > minimatch pins brace-expansion@5.0.7 (GHSA-mh99-v99m-4gvg / issue #633 — unbounded expand() result length in chained brace groups causes an uncatchable OOM crash from a ~7.5KB input). Patched in 5.0.8. ^5.0.8 sits inside minimatch's declared brace-expansion range, so this is a safe nudge to the patched line for a build-time-only (Sentry source-map upload) dependency. Drop once @sentry/bundler-plugins ships a lockfile past 5.0.8 upstream.",
  "_svelte_root_pin_rationale": "svelte is declared here at the workspace root (not only in apps/web) so the copy @sveltejs/vite-plugin-svelte resolves from the hoisted root node_modules is pinned. apps/web's own svelte dep lands nested in apps/web/node_modules, invisible to the hoisted build tooling, so the plugin would otherwise resolve a separate root peer copy that npm settled on 5.56.0. 5.56.0 has a parser regression that misparses a text-expression tag starting with the bare identifier `type` (`{type === ...}`) as a declaration tag — it broke the web build in run 26844715187; 5.56.1 fixes it. An `overrides` entry can't do this because npm overrides don't rewrite peerDependencies edges, which is what the tooling's svelte is. Keep this in lockstep with apps/web's svelte pin; drop once both move past 5.56.x."
}
