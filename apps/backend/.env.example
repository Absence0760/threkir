STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
# Verify-token returned in the GET handshake when Strava creates the
# webhook subscription (sent by us in POST /push_subscriptions and
# echoed back to us via hub.verify_token). Generate with
# `openssl rand -hex 32` and set the same value here and on Strava.
STRAVA_VERIFY_TOKEN=
# Shared secret embedded in the webhook callback URL configured in
# Strava (e.g. https://<host>/strava-webhook?secret=<this>). Strava
# preserves the URL's query string on both GET and POST, so the same
# value guards both methods. Required: the function fails closed
# without it. Generate with `openssl rand -hex 32`.
STRAVA_WEBHOOK_SECRET=
PARKRUN_USER_AGENT=RunApp/1.0 (contact@runapp.com)
# RunSignUp REST API credentials for race-results-import + race-listings-sync.
# Both leave the RunSignUp leg INERT (503 provider_not_configured) until set —
# fail-closed gate per the missing-credential rule. Provision from the
# RunSignUp partner dashboard. The race calendar's parkrun + manual-paste paths
# work without these.
RUNSIGNUP_API_KEY=
RUNSIGNUP_API_SECRET=
# UltraSignup partner credential (trail/ultra results). Same fail-closed gate as
# RunSignUp: unset → the UltraSignup leg returns 503 provider_not_configured.
ULTRASIGNUP_API_KEY=
ULTRASIGNUP_API_SECRET=
# ChronoTrack Live (CTLive) results import (race calendar). All three are
# REQUIRED together; any unset leaves the ChronoTrack leg fail-closed (503
# provider_not_configured). Provision a CTLive API account. The race calendar's
# parkrun + manual-paste paths work without these.
CHRONOTRACK_CLIENT_ID=
CHRONOTRACK_USER_ID=
CHRONOTRACK_PASSWORD=
# Polite identifier for the RunSignUp / UltraSignup / ChronoTrack results fetch (defaults to RunApp/1.0).
RACE_IMPORT_USER_AGENT=RunApp/1.0 (contact@runapp.com)
# RevenueCat webhook HMAC verification secret.
# Set this on the RevenueCat dashboard at Integrations → Webhooks → Signing Secret.
REVENUECAT_WEBHOOK_SECRET=
# ─── Stripe Connect — paid events (club_events.md slice P1) ───
# TEST MODE ONLY in P1. Use sk_test_ / ca_ / whsec_ keys; NEVER a live
# key. All three are server-only (read via Deno.env in the EFs) and must
# NEVER be exposed to the client bundle. The functions fail closed (503)
# when STRIPE_SECRET_KEY / STRIPE_EVENTS_WEBHOOK_SECRET are unset.
#
# Platform secret key (destination-charge Checkout + Connect account
# create). Stripe dashboard → Developers → API keys (test mode).
STRIPE_SECRET_KEY=
# Connect application id (ca_...). The platform's Connect-app identifier;
# configured on the platform account for Express + Account Links and kept
# here for any future Standard-OAuth path (open question #4). Stripe
# dashboard → Connect → Settings.
STRIPE_CONNECT_CLIENT_ID=
# Stripe-events webhook signing secret (whsec_...). SEPARATE from the
# RevenueCat webhook secret and a SEPARATE endpoint. Locally this is the
# value `stripe listen` prints; in prod it's the endpoint's signing
# secret. stripe-events-webhook verifies the Stripe-Signature HMAC over
# the raw body against it and fails closed (503) when unset.
STRIPE_EVENTS_WEBHOOK_SECRET=
# Comma-separated allow-list of origins the events EFs will accept for
# Account Link return/refresh URLs and Checkout success/cancel URLs (the
# strava-import open-redirect defence). Required: both events EFs return
# 503 when empty. The local-dev value covers the web dev server (7777).
STRIPE_EVENTS_ALLOWED_REDIRECTS=http://localhost:7777
# Service-role key for the Supabase project. INJECTED AUTOMATICALLY in
# Supabase Cloud and `supabase functions serve --env-file` — do NOT
# set it manually against a Cloud project. Listed here because the
# Edge Functions read it from Deno.env at runtime and a contributor
# debugging a local function-host invocation may need to confirm the
# variable is in scope.
SUPABASE_SERVICE_ROLE_KEY=
# Shared secret the pg_cron job passes to the refresh-tokens Edge
# Function in `Authorization: Bearer ${CRON_SECRET}`. Required —
# without it refresh-tokens is publicly invokable and an attacker can
# spin every Strava integration's refresh-token loop on demand.
# Generate with `openssl rand -hex 32`. Set the same value on the EF
# secret store and on the cron schedule's request headers.
CRON_SECRET=
# Comma-separated allow-list of redirect URIs strava-import:connect
# will accept. The web client sends `redirect_uri` along with the
# auth code; the EF rejects mismatches. Strava's /authorize already
# pins callbacks to the registered domain, but only at path-prefix
# granularity — this allow-list pins the exact URI shape so a code
# captured against an unrelated path under our domain can't be
# token-exchanged through this function. **Required**: the EF returns
# 503 when this is empty (matches the other secret-gated webhooks).
# Set every prod URI plus any preview deploy hosts. The local-dev
# value below covers `npm run dev --workspace=apps/web`; the second
# entry is the custom-scheme callback the mobile clients send
# (kStravaCallbackUri in mobile_android/lib/strava.dart), without which
# an in-app connect is refused with 400 invalid_redirect_uri.
STRAVA_ALLOWED_REDIRECTS=http://localhost:7777/settings/integrations,threkir://strava-callback
# Sentry DSN — read by every Edge Function via _shared/sentry.ts.
# Optional in local dev (functions still run; errors just don't
# upload). On Supabase Cloud set via `supabase secrets set
# SENTRY_DSN=...`. Get the DSN from the Sentry project's
# `Settings → Client Keys`.
SENTRY_DSN=
# Release tag attached to every Sentry event. CI sets this from the
# git tag on production deploys; for local dev leaving it unset
# tags errors as "dev".
APP_RELEASE=
# RevenueCat REST secret key. Read by delete-account to DELETE the
# subscriber server-side (GDPR Art 17). Optional — when unset the
# delete still completes but records `revenuecat_delete: 'skipped'`
# in deletion_audit_log instead of issuing the upstream delete. Get
# it from the RevenueCat dashboard → Project → API keys (Secret).
REVENUECAT_SECRET_API_KEY=
# Firebase Cloud Messaging server key. Read by delete-account to
# batch-invalidate Android push tokens at Google's routing layer on
# account deletion. Optional — when unset the delete still completes
# (the DB cascade removes the rows) but records `fcm_remove: 'skipped'`.
FCM_SERVER_KEY=
# HMAC key for the deletion_audit_log pseudonymous user-id hash.
# When set (≥ 32 bytes; `openssl rand -hex 32`), delete-account writes
# HMAC-SHA256(key, userId) so a holder of a UUID can't confirm a user
# was deleted. When unset it falls back to the legacy salted SHA-256
# (service-role-only table, but UUID-reconstructable). Optional but
# recommended in production. See delete-account/lib.ts.
DELETION_AUDIT_KEY=
# Web-push VAPID private key. The PUBLIC half lives in the web bundle
# (apps/web/.env.example PUBLIC_VAPID_PUBLIC_KEY) to subscribe with;
# the PRIVATE half belongs here because only a server-side push sender
# may sign payloads with it. No consuming Edge Function ships yet — the
# push-send path is planned (see apps/web/src/lib/util/push.ts for the
# browser subscribe side that persists the subscription). Generate the
# pair with `npx web-push generate-vapid-keys`.
VAPID_PRIVATE_KEY=
# GoTrue send-email auth hook (auth-email function). The Standard
# Webhooks signing secret GoTrue is configured with — config.toml's
# [auth.hook.send_email].secrets locally, the Dashboard → Auth → Hooks
# value in production. `v1,whsec_<base64>` (or bare `whsec_<base64>`);
# multiple secrets for rotation are `|`-separated. Required: the
# function 503s (fail-closed) without it.
SEND_EMAIL_HOOK_SECRET=
# SMTP transport for auth-email — the same variable names the Go
# worker's mailer uses. Local dev points at the Mailpit catcher as seen
# from inside the edge runtime container (host.docker.internal:24325);
# production points at the real relay (Resend / SES SMTP, port 465 =
# implicit TLS, 587 = STARTTLS) with SMTP_USERNAME + SMTP_PASSWORD set.
# auth-email 503s (fail-closed) when SMTP_HOST or SMTP_FROM is unset.
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
# Host-reachable API origin auth-email builds verify links from. REQUIRED
# locally (http://127.0.0.1:24321): the local runtime's injected
# SUPABASE_URL is the Docker-internal http://kong:8000, which a browser
# can't resolve. Leave unset in prod — the hosted runtime's SUPABASE_URL
# is already the public project URL. (SUPABASE_-prefixed names are
# reserved by the CLI, hence the GoTrue-style name.)
API_EXTERNAL_URL=
SMTP_FROM=
