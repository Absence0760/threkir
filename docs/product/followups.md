---
name: Gap-closure follow-ups
description: Open follow-ups only. Shipped items are pruned as they land — their cutover recipes live in apps/*/deployment.md and the code is in git history. Mirrors roadmap.md in spirit.
---

# Open follow-ups

Every item below is one of: (a) blocked on an external credential / account, (b) an operator deploy step on code that's already merged, or (c) a sized-but-unstarted feature / a deferred-with-reason finding awaiting a product or architecture call.

> **Completed work was pruned from this file on 2026-06-14.** The full pre-prune snapshot — every shipped `[x]` item and closed section with its implementation context — is preserved in [`followups_archive.md`](followups_archive.md). Pair this file with [`docs/testing/testing.md`](../testing/testing.md) "What's *not* covered" for the test view. Operator cutover recipes for the Go-service migrations live in [`apps/job_worker/deployment.md`](../../apps/job_worker/deployment.md) and [`apps/web/deployment.md`](../../apps/web/deployment.md).

## Deploys (code merged, operator steps remain)

- [ ] **share-run / share-route Lambda og:image PNG — verify the arm64 @resvg binary ships in the zip.** Both Lambdas serve their og:image PNG at request time and `build.mjs` copies the `@resvg/resvg-js` loader + `@resvg/resvg-js-linux-arm64-gnu` native package into the zip. The Lambdas run on arm64, so that native package must resolve on the build host. If `build.mjs` fails fast with the missing-binary error, run `npm install --workspace=apps/web --cpu=arm64 --os=linux @resvg/resvg-js-linux-arm64-gnu` before the zip step. Operator: confirm the first prod/preview deploy renders `/og/run/<id>.png` + `/og/route/<id>.png` (200, valid PNG) and that a private/deleted id returns the generic branded card at 200, not a 404. The new `/og/run/*` + `/og/route/*` + `/share/route/*` CloudFront behaviours are a `terraform apply` on `infra/envs/{prod,preview}`. See `apps/web/lambda/share-run/README.md` + `apps/web/lambda/share-route/README.md`.
- [ ] **Live spectator hub → Fly.io — cutover only.** The worker is already deployed (2026-07-21, Fly app `threkir-worker`, region `ord`) and the `live.threkir.com` CNAME has landed. What remains: the `flyctl certs add` TLS check, then set `PUBLIC_LIVE_HUB_URL` (web sops blob) + `LIVE_HUB_URL` (mobile release builds). Client wiring already switches transport on the env flip; no code change. Recipe: `apps/job_worker/deployment.md § Live spectator hub`.
- [ ] **Map matching deploy** — OSRM alongside Supabase, OSM-extract refresh pipeline, auth endpoint, matched-geom return + raw-vs-matched toggle, offline fallback. Engine choice + trigger wiring shipped; the deploy is what remains.
- [ ] **Route builder needs a reachable OSRM — design decision first, not just a deploy.** The shipped internal-track build (`mobile_android@1.0.5`, 2026-07-14) has `OSRM_URL` unset, so the route Build button throws on a phone. **Do NOT "just" expose `osrm.threkir.com`** — the previous wording of this item said that, and it contradicts a documented decision: `osrm-routed` is the upstream `osrm/osrm-backend` image with **no auth of any kind**, which is exactly why `apps/job_worker/osrm/fly.toml` attaches no public IP and `apps/job_worker/deployment.md` § DR states "OSRM has no auth; that's why it can never have a public route" (it declines to expose even `/health` directly). A naked public OSRM is an open, abusable `performance-2x`/8 GB compute service.

  The real blocker is **cross-cloud reachability + no auth**, and it has three parts:
  1. **OSRM is on Fly 6PN; its consumer is an AWS Lambda.** The `osrm-proxy` Lambda (`apps/web/lambda/osrm-proxy/`, issue #198 / decisions §242) is built, Terraformed (`infra/envs/{prod,preview}`) and bundled by `release-web.yml` — but it cannot reach `osrm.internal`. `var.osrm_url` defaults to `""`, and an empty value leaves the proxy at 501 with the **web** route builder degrading to straight-line segments. Confirm whether prod actually has it set before assuming web routing works.
  2. **Auth has to be added in front of OSRM, not to it.** The house precedent is `graph_cycle`: `[http_service]` + `force_https` + a required `GRAPH_CYCLE_API_KEY` header — but that works because `graph_cycle` is *our* Go code (`apps/graph_cycle/main.go`). OSRM is a third-party binary, so the equivalent needs a small auth reverse-proxy in front of it on Fly (or moving OSRM into AWS beside the Lambda, removing the cross-cloud hop entirely).
  3. **Mobile must go through the Lambda proxy, not straight at OSRM.** `routing.dart` currently builds `$OSRM_URL/nearest/v1/...` and `$OSRM_URL/route/v1/...` and calls them directly from the device — the precise exposure web *removed* in §242, where a user's pin coordinates (routinely their home) left the client with no server boundary. Pointing `MOBILE_OSRM_URL` at a public OSRM would re-open it on mobile. The proxy deliberately mirrors OSRM's own GET path shape, so mobile's URL building needs **no change** — set `MOBILE_OSRM_URL=https://threkir.com/api/routes/osrm` and send the Supabase JWT in the `x-supabase-authorization` header on the two fetch helpers — the standard `Authorization` header is owned by CloudFront's OAC sigv4 signing and never reaches the Lambda (`apps/web/lambda/osrm-proxy/src/index.ts`); the proxy 401s without a signed-in user.

  Until (1) and (2) are decided and provisioned, mobile route-building cannot ship past Internal, and the mobile code change in (3) is not worth landing (it would only move the failure). Operator-gated: needs `flyctl` + Fly/AWS credentials.
- [ ] **Graph-cycle loop generator (v3) — prod deploy only.** The Go graph sidecar (`apps/graph_cycle/`) + its integration as the first link in the generator chain (`apps/web/src/lib/routes/generate/graph_cycle.ts`) are shipped and gated on `GRAPH_CYCLE_URL`/`GRAPH_CYCLE_API_KEY` (falls back to `round_trip` then polygon). Operator: `flyctl deploy` the service per its `fly.toml`/`deployment.md`, then set `GRAPH_CYCLE_URL` + `GRAPH_CYCLE_API_KEY` in the web env. No further code required. Design in [graph_cycle_loop_generation.md](../features/graph_cycle_loop_generation.md).
- [ ] **Protomaps self-hosted tiles** — all 4 items from `roadmap.md` "Future — Protomaps self-hosted tiles".
- [ ] **Live-hub Redis — provisioning, and the Bridge that provisioning would switch off.** Surfaced 2026-08-26 by the partial-box reconciliation ([decisions § 756](../architecture/decisions.md)); it was the one `- [~]` in the tree tracked in no other place. `apps/job_worker/internal/livehub/redis_hub.go` implements the multi-replica backend and `main.go:556` selects it on `REDIS_URL`, so the deployed hub runs `backend=in-process (single-replica)` today. Two things are owed, and only the first is an operator step. **(a) Provisioning:** stand up Upstash Redis, set `REDIS_URL`, then lift `--ha=false` (`apps/job_worker/deployment.md`) — the in-process hub shares no state, so a second machine silently breaks fan-out. Live pings are GPS coordinates, so hosted Upstash is a cross-border processor transfer whose DPA is a pre-cutover item in [`sub-processors.md`](../compliance/sub-processors.md). **(b) Unwritten code:** `main.go:671` wires the Realtime→hub Bridge inside `if inProc, ok := hub.(*livehub.Hub); ok`, so setting `REDIS_URL` **silently drops the Bridge** — with no log line — and a hub spectator stops seeing runners still on the legacy transport, which is precisely the population that exists during the cutover above. `bridge.go:37-40` names the cause: the cursor and the hub-native guard are in-memory and moving them into Redis is "not built". Do (b) before (a), or the two cutovers interfere.
- [ ] **Engagement email send gate — provision worker SMTP.** The weekly digest and the lifecycle drip are code-complete including their `pg_cron` schedulers, and both drain to `done` without `SMTP_HOST` (see the `[x]` entry under **Email / lifecycle** below for what shipped). Filed here so the open half is reachable from the operator's own list rather than only from the roadmap: the full recipe is `roadmap.md` § **Email — production ops** — provision SMTP (Resend/SES) + `APP_BASE_URL` on the worker, confirm the pg_cron schedules in prod Supabase, set SPF/DKIM/DMARC. Sending also needs the CISO/counsel sign-off recorded in [`email.md`](../features/email.md), which gates the deploy and not the code.
- [ ] **Auth email — localized templates (Send Email Hook).** The signup/reset redirect fix + custom-SMTP sender are done (project custom SMTP → Resend fixes the `noreply@threkir.com` From). The `auth-email` function's six-locale templates are **not** live: prod uses GoTrue's built-in **English-only** templates until the send-email hook is enabled. Operator: deploy `auth-email` to prod, `supabase secrets set` its `SEND_EMAIL_HOOK_SECRET` + Resend `SMTP_*`, then enable the hook (Dashboard → Auth → Hooks → Send Email) — in that order (an enabled hook pointed at an absent function 404s all auth mail). Also set the hosted **Site URL** = `https://threkir.com` + add `/auth/callback`, `/auth/reset`, `com.threkir.app://login-callback` to the Redirect-URLs allow-list. Recipe: [email.md § Production ops 2d](../features/email.md).

## Blocked on external credentials / accounts

- [ ] **Push notifications (FCM + APNs + web Push)** — operator: create a Firebase project, drop `google-services.json` (Android) / `GoogleService-Info.plist` (iOS), enable an APNs auth key + upload to Firebase, generate `VAPID_PRIVATE_KEY` for web. The client legs are built, not owed: `firebase_messaging` registration ships on both mobile twins and writes to `device_tokens` (never `user_devices.push_token`, a column that has never existed), and the server-side **web-push** leg ships too. What is left after the credentials is the workout-reminder + kudos-receive handlers and `apps/web/src/lib/util/push.ts`. The delivery rail itself is **Native push delivery** below.
- [ ] **Garmin Connect** — blocked on the multi-day Garmin Developer Program application + OAuth client provisioning. Then follow the Strava pattern: a `garmin.dart` helper + a `garmin-import` Edge Function + a Settings tile wired to OAuth. (Distinct from the on-watch Connect IQ data field at `apps/watch_garmin/`, which needs no Garmin approval — see [decisions.md § 107](../architecture/decisions.md#107-vector-1-starts-as-a-connect-iq-data-field-grade-adjusted-pace-not-a-full-watch-app).)
- [ ] **RunSignUp race-results** — needs a runsignup.com API key (free for non-commercial use). Then follow the parkrun pattern: a `runsignup-import` Edge Function + a Settings tile that ingests into `runs.metadata.event` / `position`.
- [ ] **iOS verification** — Mac-only. The byte-identical Dart codebase already supports every Android feature ([decisions §39](../architecture/decisions.md#39-mobile_android-and-mobile_ios-share-a-byte-for-byte-dart-codebase)); `parity.md` cells stay ✗/Partial *by design* until simulator/device-verified. Gates: `Runner.entitlements` (HealthKit + Sign-in-with-Apple), `pod install`, the Apple Developer Sign-in-with-Apple Services ID + APNs setup. Info.plist is already complete.
- [ ] **Apple Watch** — Xcode / watchOS device required: route-nav visuals, **in-run mini-map on a wrist**, ultra-length stress test, live race participant, complication target (Widget Extension), activity types, lap markers, hold-to-stop, TTS cues, pedometer, GPS self-heal, indoor mode, route picker, BLE pairing UI — each wired in Xcode + verified on a simulator or paired device. The mini-map is the one item already **built and host-tested** (2026-08-21, [decisions § 702](../architecture/decisions.md)): `xcodebuild test` is green on the watchOS 26.4 simulator, so what remains for it is strictly *device* verification — line weights and the position dot read on a real panel in sunlight, the paged swipe between stats and map is usable with a wet glove, and the trail's shape survives the thinning passes a multi-hour run puts it through.

## Mobile

- [x] **Sign-up takes the password in a single field, so a typo locks the account out permanently.** Closed 2026-07-16 alongside the web fix: `auth_gates.dart` (the `checkPasswordPair` Dart twin, 17 mirror tests) + a confirm field on `sign_up_screen.dart` checked before `signUp`, ARB keys across all seven catalogues, four widget tests, mirrored to the iOS twin. Sign-up is the only password-minting surface on mobile, so the class is closed there. See [web_app_auth.md § Password confirmation](../features/web_app_auth.md#password-confirmation).
- [x] **Change password now has a current-password step-up (the mobile half of issue #381).** Landed 2026-07-20 in the same PR as the web fix: `settings_account_screen.dart`'s `_changePassword` dialog carries a current-password field and drives the pure `password_change.dart` twin of `lib/core/password_change.ts` (now a parity pair — `checkPasswordPair`, then `ApiClient.verifyCurrentPassword` over `signInWithPassword`, fail-closed, before `updatePassword`), plus an "email me a reset link" fallback for OAuth-only accounts (lands on web `/auth/reset`). ARB keys across all catalogues, unit + widget tests, and the byte-identical iOS mirror shipped alongside. See [decisions §278](../architecture/decisions.md).
- [x] **Undo has no mobile equivalent** — closed in round 13 (decisions § 523). `apps/mobile_android/lib/undo_queue.dart` is the Dart twin of web `core/undo_queue.ts` (a new parity pair, 14 mirror tests each); `widgets/undo_bar.dart` hosts the offer in a **root `Overlay` entry** rather than the `SnackBar` this entry originally proposed — a snack bar under a modal barrier is dropped from the semantics tree entirely, and `showSnackBar` was already banned in `lib/screens`/`lib/widgets` by an architecture guard. All seven of web's adopting deletes adopted, `undo_window_s` is read AND editable on the phone, and the deferred-commit contract is guarded per call site.
- [x] **Undo offered on every eligible web surface** — round 12 closed the four the U8 sweep enumerated ([decisions § 516](../architecture/decisions.md)): the **notification** dismiss (single row and a collapsed group — one intent, one undo slot, one batched delete), the **route review** delete (issue #664), the **route marker** delete, and the **gear wear-log** delete, which was the one blocked on design. That block is resolved rather than routed around: `Modal.svelte`'s Tab ring now admits a designated `[data-modal-trap-include]` host, so the bar is keyboard-reachable inside a dialog, guarded by `src/lib/undo_modal_trap_guard.test.ts` + a keyboard-only leg in `tests-e2e/settings/gear.spec.ts`. Everything still destructive keeps its confirm on purpose — see [conventions.md § Destructive actions](../architecture/conventions.md).
- [x] **Mirror "train for this race" onto the phone.** Closed 2026-08-18 ([decisions § 671](../architecture/decisions.md)): `race_plan_preset.dart` is a registered parity pair (17 mirror tests each), `RacesScreen`'s card shows a "Train for this race" action under the same refusal gate the web card uses, and `PlanNewScreen` takes `raceDateIso` / `raceName` / `raceDistanceM` — the listing's facts, re-derived against today exactly as the web URL is — seeding goal, name, start date and week count, and rendering the anchored note or the refusal reason. ARB keys across all seven catalogues; iOS twin mirrored. Original entry: Web shipped the race-calendar → plan-generator bridge on 2026-08-14 ([decisions § 606](../architecture/decisions.md)): `apps/web/src/lib/training/race_plan_preset.ts` sizes a plan so its final `race` week contains race day, and `RaceCalendarCard` → `/plans/new` carries the race's facts. Mobile has both ends already — `screens/races_screen.dart` and `screens/plan_new_screen.dart` (which holds `_startDate` / `_weekOverride` / `_goalEvent` and already drives `generatePlan`) — but nothing between them. The mirror is a Dart twin of `race_plan_preset.ts` (a new parity pair: register it in `CLAUDE.md` **and** `.claude/agents/shared-library-syncer.md`, or `check_parity_pair_registry.mjs` fails the PR), a preset argument on `PlanNewScreen`, a "train for this race" row action on the races screen, ARB keys across all seven catalogues, and the byte-identical iOS mirror. Deliberately not written on web's schedule — a Dart twin with no caller is dead code the parity guard would then police forever.
- [x] **The comeback runner gets no load warning in the case that most warrants one.** Closed 2026-08-15 by `apps/web/src/lib/training/comeback.ts` + the dashboard `ComebackCard` ([decisions § 612](../architecture/decisions.md)): the week is graded against the runner's own pre-break weekly average rather than a near-empty chronic window, firing only where `selfLoad` is silent so the two cards are mutually exclusive by construction. Original entry: `self_load` (and `plan_ramp` before it) refuse to grade below `MIN_ACTIVE_WEEKS` of the chronic window, so a runner returning from a layoff who logs one big week after three empty ones sees `activeWeeks = 1` → `insufficient` → no card. The gate is right and should NOT be lowered: dividing a comeback week by a near-zero base manufactures a terrifying ratio out of thin air, and a safety signal that cries wolf gets ignored (decisions § 609). The gap is that ACWR is the wrong *instrument* for this runner, not that its threshold is wrong — the honest signal is a different claim ("you have not run in N weeks; this is a big first week back"), derived from the layoff itself rather than from a ratio. `training_load.ts` already knows what a layoff is (`kLayoffResetDays`, 28), so the input exists. Surfaced by the round-3 audit; sized as its own change because it needs new copy and a new rule, not a tweak to this one. Relevant persona: `runner-comeback`.
- [x] **Mirror the runner's own load-ramp band onto the phone.** Closed 2026-08-18 ([decisions § 670](../architecture/decisions.md)) together with the comeback mirror below, because the two share the reduction and the gate that keeps them mutually exclusive: `self_load.dart` + `comeback.dart` are registered parity pairs (15 and 21 mirror tests each) importing `recentRunVolume` / `volumeSample` from `plan_ramp.dart` rather than copying them, and `widgets/load_ramp_card.dart` + `widgets/comeback_card.dart` render them on the dashboard off one conversion of the runner's runs. The DNF divergence from the coach roster (decisions § 592) is carried over and pinned by a test. Original entry: Web shipped it 2026-08-14 ([decisions § 609](../architecture/decisions.md)): `apps/web/src/lib/training/self_load.ts` composes the existing ACWR policy over the runner's own runs and `LoadRampCard` renders it on `/dashboard`. Mobile already carries the `coach_load.dart` twin (the coaching screen uses it for the roster) but has no self-facing surface, so the mirror is a Dart twin of the `selfLoad` composition plus the acute half of the volume reducer, a dashboard card, ARB keys across all seven catalogues, and the byte-identical iOS mirror. Deliberately not written on web's schedule — a Dart twin with no caller is dead code the parity guard would then police forever. Note the DNF divergence from the coach roster (decisions § 592) is intentional and must be carried over, not "fixed" to match the SQL.
- [x] **Nobody can change an SMS consent after confirming, on either platform.** Closed 2026-08-25 ([decisions § 726](../architecture/decisions.md)). `fetchSafetyContactOf` reads the contact-of half of `safety_contacts` scoped on `contact_user_id` — the predicate the linked-contact SELECT policy already permitted, so no migration — and both platforms render a "you are a safety contact for" section: an SMS toggle over `set_safety_sms_opt_in` and a withdraw over `decline_safety_contact`. The withdraw is the correctness point: `removeSafetyContact` became owner-scoped in § 720, so wiring the button to it would match nothing here and toast success while the relationship stood; a Playwright leg pins that the RPC fires and no table DELETE does. One correction to the entry below: the number is reported as a FACT (`has_phone`), not shown, mirroring `my_pending_safety_requests` — a contact cannot correct a wrong number from there anyway (no owner UPDATE policy), so a relationship with none renders a note rather than a toggle that would consent to nothing. Web + Android + the iOS twin, all six web locales + all seven ARBs, two Playwright legs, five Flutter widget tests, source guards on both clients, five pgtap assertions. Original entry: `set_safety_sms_opt_in` (migration `20270410_001`) lets a confirmed contact turn SMS alerts on or off later, and works — but no surface on web or mobile lists the relationships a user is the *contact* of, only the ones they own. So the consent can be given exactly once, at confirm time, and never withdrawn in-app; a contact who changes their mind has to withdraw from the relationship entirely. The Dart binding was removed in round 11 rather than left as an unused method ([decisions § 719](../architecture/decisions.md)). The fix is a small "you are a safety contact for…" section on both `/settings/safety` and Settings → Safety contacts — the linked-contact RLS read policy on `safety_contacts` (`20261218_001`) already permits the query, so it needs no backend change. **Read the item below first.** That query's accidental union used to be where these rows surfaced; as of decisions § 720 it is scoped, so this section has to fetch the contact-of direction deliberately (`.eq('contact_user_id', uid)`) — which is what it always should have done, and which the linked-contact read policy already permits. Relevant persona: `runner-woman`.
- [x] **`fetchMySafetyContacts` claims to be owner-scoped on both platforms and is not.** Closed 2026-08-24 ([decisions § 720](../architecture/decisions.md)). Verified as described, with one correction worth recording: the table carries **four** permissive policies, not two — `safety_contacts linked contact read` **and** `safety_contacts linked contact delete` (`20261218_001`, both still present after `20270416_001`'s initplan wrap) sit alongside the owner pair. So the Remove button was not the benign "withdrawal" this entry read as: `removeSafetyContact` deleted by `id` alone, the linked-contact DELETE policy permitted it, and pressing it therefore **deleted the other person's emergency-contact row** while toasting the presser that their own list had been tidied. RLS neither refused it nor no-op'd it. Fixed at the root on both clients — `.eq('owner_id', uid)` on the read *and* the delete, both refusing outright when signed out rather than issuing an unscoped query, and the false doc comments replaced with what the policies actually do. Deliberately **not** a policy change: the contact-side policies are correct, and the follow-up above needs exactly those rows. Pinned by source guards on both clients (`data.test.ts`, `packages/api_client/test/safety_contacts_scope_test.dart`) plus four pgtap assertions in `safety_contacts_test.sql` that demonstrate the union, the scoped read, the destructive unscoped delete and the inert scoped one. Original entry: Web's `fetchMySafetyContacts` (`core/data.ts`) and Dart's (`api_client.dart`) both select from `safety_contacts` with no `owner_id` filter, and both carry a doc comment saying "RLS scopes to `owner_id = me`". That was true when the table shipped, but `20261218_001` added a second SELECT policy — `safety_contacts linked contact read`, `using (contact_user_id = auth.uid())` — so the query now returns the union: the contacts you added, **plus** every relationship where someone else added you and you confirmed. Both surfaces render that union as "your safety contacts" with a Remove button (which does work — the sibling delete policy permits withdrawal), so a runner sees people they never added listed as their own emergency contacts, and the SMS state shown beside such a row describes a number they do not own. Not a data leak (RLS is doing what it was told; the columns are ones the contact may read) and not a new regression — it predates the SMS work, which is why round 11 did not fix it inside a mobile-mirror slice. The fix is one `.eq('owner_id', uid)` on each side plus a corrected comment, and it should land **with** the follow-up above, since the rows it removes from the owner list are exactly the ones the contact-side section needs. Web + mobile, same change.
- [ ] **Pro native RevenueCat purchase sheet + native donate** — scaffolding shipped (`revenuecat.dart`); the live sheet needs a RevenueCat project + a `pro_monthly` package + `REVENUECAT_API_KEY_ANDROID` / `_IOS` provisioned. Until then the Subscribe tile falls through to the web URL. (The store-localised Pro price is already wired via `getOfferings()`; it just needs the RC project provisioned for the localised amount to appear.)
- [x] **Health Connect import brings tracks (garmin/android, mobile + device)** — Closed 2026-08-28 ([decisions § 783](../architecture/decisions.md)). **Three of this entry's four claims were false when it was read.** `health_connect_importer.dart` has not hard-coded `track: []` since #664: it reads `WORKOUT_ROUTE`, joins on the `ExerciseSessionRecord` id, screens each fix for finite in-range coordinates, orders by time, and passes a real track into `Run` — with `READ_EXERCISE_ROUTES` in both the manifest and `res/xml/health_permissions.xml`, a `HealthRoutePermissionBridge` method channel to ask for it, a `withheldSessionIds` set driving an after-the-fact backfill, a `readFailure` field so a throw is not reported as an absence, and 60-odd host tests. "Lapless" is true but was filed as though it were the same problem: `ExerciseSessionRecord.laps` is surfaced by the `health` package **only on the write path**, so it is not reachable at all (see the entry below). What was left was the **cadence** half, and its cause was neither the route nor a sample series: `fetchWorkouts` read `value.totalDistance` off the `WorkoutHealthValue` and dropped `value.totalSteps` — a field already in the object in hand — so no imported run ever carried `metadata.steps` and the run-detail Steps and Cadence tiles could not render for one. `READ_STEPS` was also declared nowhere, and the plugin builds `totalSteps` by summing a `StepsRecord` read over the session window, so the field would have stayed null even had the Dart side asked. Both fixed, with `stepsForWorkout` screening `cycle` and physiologically impossible counts, and no `cadence_spm` written — steps is the fact, the shared `steps / moving_time_minutes` derivation is the cadence. 7 host tests, iOS twin mirrored.
- [ ] **Health Connect: verify both grants on hardware, and reach the laps that are not reachable today** (needs an Android device with Health Connect + a routed session written by another app). Two separate things, filed together because one device session settles both. **(a) The device gate.** Everything about the route and step paths is host-tested against fixtures; nothing has run against a real Health Connect. What must be checked on hardware: that the `READ_EXERCISE_ROUTES` sheet raised by `HealthRoutePermissionBridge` actually appears and returns `true`; that a granted route read yields locations rather than the zero-location `ConsentRequired` shape (and that a *refused* one yields exactly that shape, which the withheld-set logic depends on); that `READ_STEPS` makes `WorkoutHealthValue.totalSteps` non-null for a foot-powered session; and — the one with teeth — **what Health Connect does with the plugin's ungranted sub-reads**. `handleWorkoutData` reads `DistanceRecord`, `TotalCaloriesBurnedRecord` and `StepsRecord` inside the session loop with no permission check (the `authorizedTypeMap` filter guards only the top-level type), and `getData` swallows anything thrown into `result.success(emptyList())`. We hold `READ_DISTANCE` and now `READ_STEPS` but **not** `READ_TOTAL_CALORIES_BURNED`. If `readRecords` throws `SecurityException` for an ungranted type rather than returning empty, the entire workout batch comes back empty and *no* activity imports — a state the app would report as "no workouts found". That permission was deliberately not added speculatively: we consume no calorie value, and an undeclared-but-unused health permission is a Play Data Safety liability (the bridge's own comment states the principle). Settle it on a device; if it throws, the durable fix is our own `ExerciseSessionRecord` read alongside the route bridge rather than one more permission to satisfy a plugin's unguarded aggregation. **(b) Laps.** `ExerciseSessionRecord` carries `laps` and `segments`, but `health` 13.3.1 binds them only in `HealthDataWriter.kt` (preserving them across an update); there is no read binding and no `HealthDataType`, so a multi-lap imported session flattens to one interval and `metadata.laps` is never written. Closing it needs a lap read of our own beside `HealthRoutePermissionBridge`, or an upstream change — either way it is a build, not a wiring fix, and it shares the device session above.
- [ ] **Background sync doesn't execute on an iOS device (BGAppRefreshTask vs `fetch`)** — `background_sync.dart`'s `registerPeriodicTask` maps to a `BGAppRefreshTaskRequest` on iOS, which requires the `fetch` `UIBackgroundMode`; Info.plist only declares `processing`. `AppDelegate.swift` registers the identifier so launch no longer crashes, but the app-refresh submit is rejected at runtime so background sync never runs in the background. Fix: add `fetch` to `UIBackgroundModes` and keep app-refresh, OR switch the Dart side to a `BGProcessingTask`. Needs on-device testing. iOS-only — Android uses `WorkManager` and is unaffected. See `apps/mobile_ios/CLAUDE.md § Catch-up status`.
- [x] **Retire the vestigial `onStartRun` route-carrying callback plumbing** — DONE, and this box was stale rather than open: the cleanup landed with #791 and was re-verified 2026-08-26. No `void Function(cm.Route)` remains anywhere under `apps/mobile_android/lib/`, and `routes_screen` / `explore_routes` / `fitness_hub` — three of the four screens this entry named — no longer mention `onStartRun` at all. What survives is a *different, live* member that the entry's wording invites a future reader to delete by mistake: a `VoidCallback?` on `dashboard_screen`, carrying no route and reached from one `home_screen` call site, which is the dashboard's "start a run" affordance. Issue #789 already recorded this as closed; the drift was doc-side only.
- [ ] **Course-marker drag-to-move on mobile** (needs device) — **snap-on-tap-placement SHIPPED 2026-07-02**: `route_markers_panel.dart` gained a default-on "Snap to route line" toggle that projects the tapped point onto the route polyline via `snapToPolyline` before placing (render-only; `position_m` still server-derived), mirroring web's `RouteMarkerEditor`, twin-mirrored to iOS, pinned by two widget tests. **Remaining:** the draggable-symbol affordance on `LiveRunMap` (maplibre-flutter `SymbolManager` drag) — needs on-device verification, deferred. See [route_markers.md § Surfaces](../features/route_markers.md#surfaces).
- [x] **`activity_type` has no shared localized vocabulary, and the coach surface renders raw English.** Closed. The mobile + web halves were already done by `d129a55bb` (`activity_type_labels.dart#activityTypeLabel`; web's `runs/activity_type.svelte.ts` behind the broader `enum_vocabulary` registry guard) — **verified, not assumed**, including that `feed_screen._activityLabel` correctly keeps `lift` and its "All" default, which are feed-filter scopes over the `activities` view and NOT members of the CHECK domain. The bug was still live on a **fourth** platform: Wear OS carried its own resolver covering four of five values with a `replaceFirstChar { it.uppercase() }` fallback, and `stroller` was reachable there without ever being cycled to, because `default_activity_type` primes the pre-run chip straight off the phone's settings bag with no allowlist — so the one value the wrist could not name was the one the phone could hand it. Closed 2026-08-18 ([decisions § 654](../architecture/decisions.md)) with the label in all six Wear catalogues, the capitalising fallback replaced by the raw token (matching web's documented rationale), and a third guard deriving its value set from the `runs_activity_type_check` migration exactly as the mobile and web ones do.
- [x] **Nutrition targets are not a peer of the mobile Nutrition surface** — closed 2026-08-19 ([decisions § 695](../architecture/decisions.md)). `screens/nutrition_targets_screen.dart` mirrors web's `/nutrition/targets`: the derivation (resting metabolism → activity factor → goal delta → base → today's workout add-on) then the macro split, composed only from what `nutrition_targets.dart` already exported — no new pure logic and **no parity-pair change**, exactly as the entry asked. The two non-sensitive levers edit inline through the same `SettingsSyncService.updateUniversal` path the consent-gated editor writes them on (the label switches were promoted to `activityLevelLabel` / `weightGoalLabel` rather than copied, so the two surfaces cannot name one stored value differently); height / weight / DOB / sex are **read-only** here and keep their single Art 9 entry point in `SettingsBodyMetricsScreen`, pinned by a source guard mirroring web's `targets_peer_guard.test.ts`. The rings card carries the entry **ungated** — it renders whether or not targets exist, which the widget tests pin in both states, because the runner with no targets is the one who needs the derivation. 24 keys across all seven ARBs; 12 tests; iOS twin byte-identical. Original entry: **Nutrition targets are not a peer of the mobile Nutrition surface** — web closed issue #666 M1's structural half on 2026-08-05 with `/nutrition/targets` (derivation + the two non-sensitive levers at its own URL, ungated from the `/nutrition` header; height/weight/DOB/sex stay in the Art 9 consent-gated Settings editor). On mobile, `nutrition_screen.dart` reaches `SettingsBodyMetricsScreen` from its **empty state only** ([decisions § 490](../architecture/decisions.md)) — once targets exist there is no route to the number the rings are measured against, and no derivation view exists on either twin. The mobile shape is a peer entry beside the rings card (or a row on the § 488 peer strip if Nutrition gains one), pushing a new `NutritionTargetsScreen` that composes the **existing** `nutrition_targets.dart` exports — the Dart twin already has every constant and `mifflinStJeorBmr`, so this needs no new pure logic and no parity-pair change. Web is canonical per § 24, so this is a follow-up, not drift to close first.
- [x] **Nutrition diary date navigation is web-only** — closed 2026-08-18 ([decisions § 656](../architecture/decisions.md)). `nutrition_screen.dart` holds the viewed day as a local `YYYY-MM-DD` over the new registered `diary_day` parity pair (26 mirror tests each). A stepper above the rings walks the day, and every read (food window, exercise add-on, trend buckets, water key, meal-detail deep link) and **every write path the screen owns** (composer via the log sheet's new `diaryDate`, logged meal template, logged recipe) follows it, stamping `entryTimestampFor(viewedDay, now)` at SAVE time — a surface that lets a runner review yesterday but silently logs to today would be worse than no stepper. Steps go through `DateTime(y, m, d + n)` so a 23/25-hour DST day cannot repeat or skip, and the code emits no `toIso8601String()` at all. 10 keys across all seven ARBs; `parity.md` flipped.
- [x] **`fetchGymWorkouts` has no date window, so a deep diary day under-reads its lift calories** — `fetchRuns` takes `startedAtFrom` / `startedAtBefore` and the nutrition day view uses them to fetch exactly the viewed day's runs, but its gym sibling only took a `limit`, so the page pulled the newest 50 sessions and filtered. A diary day older than the caller's 50 most recent lifts therefore contributed no gym calories to that day's `exerciseKcal` — an under-read, identical in effect to "the lift wasn't logged yet", not a wrong kind of number, but avoidable. **Done 2026-08-13** ([decisions § 597](../architecture/decisions.md)): `fetchGymWorkoutsWithError` / `fetchGymWorkouts` now take a `FetchGymWorkoutsOptions` bag carrying `limit` plus the same two bounds `fetchRuns` has, applied as PostgREST `.gte()` / `.lt()` on `started_at`; `/nutrition` passes the viewed day's window and its `GYM_FETCH_LIMIT` + client-side filter are gone. The three other call sites (`/gym`, `/dashboard`, `/nutrition/targets`) moved to `{ limit: n }` with no behaviour change. Pinned by a source guard in `lib/core/data.test.ts`.
- [x] **A long-lived `/nutrition` tab does not roll over at local midnight** — `todayIso` / `yesterdayIso` (`routes/nutrition/+page.svelte`) were recomputed only inside the `$effect`, which fires on a `$page.url` or `authReady` change and never on a timer, so a tab left open past local midnight kept labelling the previous day "Today" and kept the Next-day button wrongly disabled. A stale-label bug, not a data bug: every write path resolves `entryTimestampFor(viewDate, new Date())` at save time with a fresh clock ([decisions § 591](../architecture/decisions.md)), so nothing was ever filed onto the wrong day. **Done 2026-08-13** ([decisions § 597](../architecture/decisions.md)): a `syncDay` re-check on **two** triggers, each costing one wakeup a day — a single `setTimeout` armed for `msUntilNextLocalMidnight(new Date())` (the new calendar-stepped helper in `diary_day.ts`, DST-tested), which is the only trigger that reaches a tab that stays *visible* across midnight, plus the proposed `visibilitychange` check for the backgrounded tab whose timer the browser throttled or whose machine slept. Still no polling interval. The day is re-resolved from the URL, so a bare `/nutrition` follows the clock while an explicit past `?date=` only changes its label. Pinned by `nutrition/diary_rollover.test.ts` + the `msUntilNextLocalMidnight` cases in `diary_day.test.ts` / `diary_day.dst.test.ts`.

- [x] **Mobile mirror: the plan wizard's opening-week ramp check** — closed 2026-08-18 ([decisions § 655](../architecture/decisions.md)). `plan_ramp.dart` is a faithful twin (34 mirror tests each — the docs' claim of 32 was stale and is corrected), banding through the existing `coach_load.dart` twin's `injuryRiskBand` so the wizard and the coach roster cannot drift; `plan_new_screen.dart` renders the note in the preview card off a guarded `TrainingService.fetchRecentRunVolume` whose failure leaves the wizard untouched. Silent below three active weeks, and `under` still withheld from a beginner walk-run plan. Registered in both registries. It did **not** duplicate the shared reduction: `recentRunVolume` / `volumeSample` / the two constants live in `plan_ramp.ts` on web (both `self_load.ts` and `comeback.ts` import them from it), so the faithful port lands them at the identical module boundary in `plan_ramp.dart`.
- [x] **Mobile mirror: famous-segment catalogue UI** — closed 2026-08-19 ([decisions § 693](../architecture/decisions.md)). `screens/global_segments_screen.dart` carries both halves: a browse list with accent-folded search, region + surface filters and the four sorts, and a `GlobalSegmentDetailScreen` with the curated geometry as a `TrackPreview` plus the block-guarded gender / age-band leaderboard and its crown label. The run-detail famous-segment chips, which linked nowhere on the phone, now open the detail screen, and the Fitness hub gained the catalogue entry. `catalogue_browse.dart` is a registered parity pair (28 mirror tests web / 27 Dart — the web extra covers an NFD case Dart reaches through an explicit table instead), registered in **both** registries as § 604 requires. ARB keys across all seven catalogues; iOS twin byte-identical. Original entry: **Mobile mirror: famous-segment catalogue UI** — web shipped `/segments`, the browse half of the §233 catalogue ([decisions § 593](../architecture/decisions.md)), on top of the new web-only `segments/catalogue_browse.ts` (search with accent folding, region / surface filters, four sorts). Mobile carries the `computeGlobalSegmentEffort` twin in `segments.dart` but **no catalogue UI at all** — neither a browse list nor a detail screen — so the run-detail effort chips there link nowhere. The mirror is a `GlobalSegmentsScreen` + a catalogue detail screen; the Dart twin of the four shaping helpers lands with them, not before (a twin with no consumer is dead code). Web is canonical per §24.
- [x] **Mobile mirror: add a club event to the phone's calendar** — closed 2026-08-19 ([decisions § 692](../architecture/decisions.md)). `event_detail_screen.dart` gained an "Add to calendar" action for the occurrence on screen and, for a recurring event, "Add whole series" beside it; both are withheld for a past or called-off occurrence. The mirror is the native one as this entry asked: `lib/calendar_intent.dart` hands the event over the `run_app/calendar` channel to `ACTION_INSERT` on `CalendarContract.Events` (`CalendarBridge.kt`) and to `EKEventEditViewController` (`CalendarBridge.swift`), so the runner confirms the save in their own calendar app — no permission on Android, write-only access on iOS. **No Dart port of the `.ics` builder and no parity pair**, per § 24. The series crosses the channel as an RFC 5545 RRULE (the one grammar both platforms already speak) built by `buildRrule`, which mirrors web's refusals — a monthly series anchored past the 28th is withheld, an `until` + `count` pair resolves to whichever binds first, BYDAY is read off the expansion in UTC. Neither hand-off carries `EXDATE`, so cancelled occurrences ahead are **named under the button** (over `event_occurrence.dart`'s `isOccurrenceCancelled`, reused not re-derived) rather than silently kept. 5 ARB keys across all seven catalogues; 38 tests (30 unit + 8 widget); `parity.md` + `clubs.md` corrected.

## Watch (Wear OS) — sized features awaiting hardware + a product green-light

Most need the Wear build toolchain plus on-watch verification (Galaxy Watch / Pixel Watch) — the recording stack isn't runtime-testable without a device.

- [x] **Honour the mi distance preference on the watch** — `preferredUnit` added to `UniversalSettings` (parsed from `prefs.preferred_unit`, validated against `{km, mi}`) and threaded onto `RunViewModel.UiState` as a `DistanceUnit` (same path as `bodyWeightKg`). `recording/UnitFormat.kt` gained `DistanceUnit` + `formatDistance` + `paceSecPerUnit`; `distance_mi` / `distance_mi_to_go` / `pace_per_mi` added to `values/strings.xml` + all five `values-*`. Applied at the running screen (distance + pace + ambient), the route "to go" badge, the route picker, the PostRun summary, and the active-run tile (unit carried on `RecordingRepository.Metrics`, set at `startRecording` via `EXTRA_PREFERRED_UNIT`). Pure parse/format pinned in `UnitFormatTest` + `ActiveRunTileFormattersTest` + `UniversalSettingsTest`. **TTS closed 2026-08-03** (issue #664, [decisions.md § 467](../architecture/decisions.md)): `UnitFormat.kt` gained `splitIntervalMetres` / `completedSplits` so the cue *fires* once per mile for a mi-mode runner rather than every kilometre, and every spoken resource split into a `_km` / `_mi` pair across all six locales with the pace scaled through `paceSecPerUnit`. Pinned in `UnitFormatTest` / `TtsPhrasesTest` plus a `TtsSplitUnitWiringTest` source guard (the announcer's resource lookup needs Robolectric, which this module doesn't carry). **Remaining:** the on-face visual flip and the audible read-out both need a device check.
- [ ] **BLE chest-strap HR paired directly to the watch** (~1-1.5 wk + device) — today watch HR is optical (Health Services `MeasureClient`) only. A strap paired to the *watch* means an on-watch Wear OS BLE GATT client: scan/pair UI, HR Service (`0x180D`)/HR Measurement (`0x2A37`) subscription, `BLUETOOTH_SCAN`+`BLUETOOTH_CONNECT` perms, source-preference override into `metadata.avg_bpm`. Reconnect/dropout on the wrist is the fiddly part. Minority feature; needs hardware to validate.
- [ ] **Real body weight → calorie estimate** (~2-4 d + device) — `recording/RunCalories.kt` defaults to 70 kg when `body_weight_kg` is unset. Pipe the actual weight in. Source options: (a) Samsung Health BIA (most accurate, heaviest — Samsung SDK/partner approval); (b) Health Services/Health Connect `WeightRecord` (vendor-neutral, lighter); (c) the existing universal-setting sync from web/mobile (cheapest, manual). Watch still won't apply the female calibration (reads `user_settings.prefs` only). Needs a device to confirm the read path returns a value.
- [ ] **Watch-face complication** (~1-1.5 wk + device) — only an active-run *tile* ships today; a true watch-face complication is unbuilt. Needs a `ComplicationDataSourceService` (`wear-watchface-complications-data-source`), `SHORT_TEXT`/`RANGED_VALUE`/`MONOCHROMATIC_IMAGE` types, a tap `PendingIntent` into `MainActivity`, the same `requestUpdate`-on-stage-transition wiring the tile uses, + preview/picker label. Pure formatters unit-testable; binding + render need a watch face that hosts the slot.

## Internationalisation

RTL *layout* is complete web-wide; web + mobile string extraction is complete (5,049 web keys ×7 locales — the ~2,682 figure predated several rounds of copy; the mobile ARB pass is done at 3,772 keys ×7). Remaining:

- [x] **The rate-limit refusal was English on every platform, for every bucket.** **CLOSED 2026-08-26** ([decisions § 744](../architecture/decisions.md)). `rateLimitErrorMessage` was a TS↔Dart pair that returned a finished English sentence, so the prose sat inside the one module the lockstep forbids either platform from diverging on and no locale could get anything else. It is parse-only now — both halves return `{bucket, seconds}` — with nine translated sentences plus three wait forms added to all six web catalogues and all seven ARBs (including a real European/Brazilian Portuguese split) and assembled by per-platform render glue outside the lockstep. Filed as a `direct_messages` copy item; it was the whole helper, ten buckets across six surfaces, and **web fell through to the generic `'doing that'` sentence for the two send buckets exactly as Dart did** — a trade-off § 737 made deliberately, and one the split makes free. English is unchanged character for character (three Playwright specs match on it), both parsers carry a source guard that no user-facing phrase may return to them, and the render suites pin every bucket in every locale.
- [x] **`create_challenge` throttles through its own trigger, so a refused challenge create still shows the raw postgres exception.** **CLOSED 2026-08-26** ([decisions § 747](../architecture/decisions.md), migration `20270610_001`). The bucket count was right — exactly ten reach `enforce_create_rate_limit`, and `create_challenge` is the only non-conforming rate-limit raise in the whole migration set (the other `raise exception` lines naming a rate limit are `check_rate_limit` / `check_rate_limit_tiered` argument-validation and caller-guard errors, not throttle refusals). **The user-facing half was wrong in the direction that mattered.** Nobody read a raw postgres exception: mobile has no challenge-create surface at all (`SocialService.createChallenge` has zero callers under `apps/mobile_*/lib`), and web's `ChallengeEditor` discarded the error and toasted the generic "Couldn't create the challenge." So the defect was a *swallowed* refusal naming neither cause nor wait, not a leaked one — worse, because the raw string at least told a reader to come back later. "A migration plus both type regenerations, not a client change" was wrong both ways: the migration alone reaches no reader (the editor drops `e.message`), and there is nothing to regenerate — a trigger function never enters `database.types.ts` and the Dart generator reads only `create table` / `alter table`; both generators re-ran to a zero diff. Fixed at the producer (one `perform enforce_create_rate_limit('create_challenge', new.creator_id, 30, 3600)`, which also moves the forged-`creator_id` case from a mis-classified P0001 to RLS's 42501, skips `service_role`, and drops a fail-open wrapper guarding against nothing) plus the client wiring, `rateLimit.createChallenge` in six catalogues and seven ARBs, a class-level source guard that no trigger body may call `check_rate_limit` directly, and `challenge_create_rate_limit_test.sql`. Original filing below.
  - Surfaced counting the buckets for § 744 (2026-08-26). Ten buckets go through `enforce_create_rate_limit`, which raises the `rate limit exceeded for <bucket>, retry in Ns` shape the client parser reads; `create_challenge` (migration `20270308_001`) instead raises the bare string `challenge_create_rate_limited` with no bucket and no retry figure, and nothing on either platform handles it. The durable fix is to route that trigger through `enforce_create_rate_limit` like every other bucket so it inherits the parser, the localized sentence and the cap accounting for free — a migration plus both type regenerations, not a client change. Adding a second parser branch for the odd shape would be the band-aid.
- [ ] **Native review of machine-extracted translations** — the de/es/fr/ja/pt-BR/**pt-PT** strings (web catalogue + mobile ARB + **the Wear OS `values-*` sets** + the nutrition/consent copy) are best-effort machine output. Agents flagged fitness-domain tooltips (CTL/ATL/TSB/VO₂max) and a few register / article-agreement / fragment-split spots. **European Portuguese joined this list 2026-08-27** ([decisions § 755](../architecture/decisions.md)) and is the least reviewed of the set: web's 5,049 keys and the wrist's 102 resources were derived from Brazilian through a substitution corpus mined from `app_pt.arb`'s own 359 curated strings, not translated by a human. **`app_pt.arb` itself joined the list the same day** ([§ 760](../architecture/decisions.md)), when 822 of its strings were derived through that same corpus, so the phone is now model-authored on the same terms as the browser and the wrist rather than being the curated source they were mined from — all three want the one pass. **The same day it widened again to the server-sent and watchOS catalogues** ([decisions § 761](../architecture/decisions.md)): 293 further model-authored strings across `apps/job_worker/internal/{email_i18n.go,sms_i18n.go}` (181 — every notification, lifecycle, drip, digest, safety and both push channels), `apps/backend/supabase/functions/auth-email/lib.ts` (55 — sign-up, magic link, password reset, e-mail change, reauthentication) and `apps/watch_ios/WatchApp/Localizable.xcstrings` (57). Those are the highest-stakes strings in the set — a password-reset mail and an account-erasure receipt are read once, under pressure, by someone who cannot ask us what a word meant — so they want the native pass **first**, ahead of the UI catalogues. Known-weak spots named at the time: the register choice (third-person elision throughout, where `app_pt.arb` mixes `tu` and third person), `password` → "Palavra-passe", which § 760 propagated to the phone and the browser so all five surfaces now agree (they did not when this list was written), the Wear battery-settings strings that quote real Android menu paths ("Apps" vs pt-PT's "Aplicações"), and `hike` reading "Trilho" on the wrist against "Trail" on phone and web. **Widened again 2026-08-27** ([decisions § 767](../architecture/decisions.md)) with two things measured and left rather than guessed, both wanting a native call specifically. **(a) Register: ~~the phone still addresses its reader two ways~~ — CLOSED 2026-08-28** ([decisions § 782](../architecture/decisions.md)), and *not* as a native-speaker call: web's `pt-PT.ts` had already decided the possessive unanimously (335 `seu`/`sua`, **zero** `teu`/`tua`) and `app_pt_BR.arb` carries zero as well, so aligning the phone was a consistency repair rather than a linguistic judgement. The count was **44 occurrences over 39 keys**, not 36, and the possessive was one axis of four — 7 tu-only finite verbs, 6 `-te` enclitics, one `contigo` and 9 tu imperatives no token can see brought it to **54 strings**, plus **20 spoken cues**. Half the possessive strings carried a tu verb in the same sentence, so the filed scope could not have been executed on its own. Guarded on both axes by `architecture_guards_test.dart § locale reach`, proved to fail against the pre-fix catalogue and on a single planted string. **(b) `longão`**, Brazilian running slang for the long run, in **7** web keys and **5** phone keys. It is community vocabulary rather than dictionary vocabulary — `o longo` and `a corrida longa` are both plausible in Portugal — and getting the register wrong across twelve sites is worse than leaving one word Brazilian. Wants a native-speaker pass before advertising full localisation. **Widened again 2026-09-04** ([decisions § 1121](../architecture/decisions.md)): `runDetail.hrAvgOnly` was rewritten in all seven catalogues to drop a roadmap promise, and six of the seven new strings are model-authored — de, es, fr, ja, pt-BR, pt-PT. The de/es/fr/pt wordings were checked against the existing sibling keys in the same panel (`hrCoverageNone` / `hrCoverageOnly` / `hrNoData`) for register and vocabulary and are believed sound; **`ja` is the one to look at first** — it was recast from a two-clause promise into a contrastive `〜ですが` and nobody on this side reads Japanese. One line each, all seven at `runDetail.hrAvgOnly`.
- [x] **`app_pt.arb` is not reachable at runtime, so its European-Portuguese content ships to nobody — decide whether pt-PT is a locale.** **Settled 2026-08-25 by shipping it** ([decisions § 740](../architecture/decisions.md)). Half of the 2026-08-05 measurement was already stale when this was re-measured on `b103c9388`: [§ 547](../architecture/decisions.md) put `Locale('pt')` into `supportedLocales` on 2026-08-06, so a `pt-PT` / `pt-AO` / bare-`pt` device does reach the European catalogue. The other half was live — `_editLanguage` listed its options as `['', 'en', 'de', 'fr', 'es', 'ja', 'pt-BR']`, so a Portuguese speaker on any other device could never select it. The picker now derives from `supportedLocales`. Option (b) was refused on the numbers: all seven catalogues carry 3,758 of 3,758 keys with zero missing and zero orphan, and `pt` differs from `pt_BR` on 349 of them — a complete catalogue that already resolves for real devices, not a mechanical copy (the filed 3,251-of-3,477 figure is superseded). Guarded on both platforms so the class cannot recur: `apps/mobile_android/test/architecture_guards_test.dart § locale reach` and `apps/web/src/lib/i18n/locale_reach.test.ts`.
- [x] **Web ships no European-Portuguese catalogue, so the phone and the browser answer a `pt-PT` reader differently.** **CLOSED 2026-08-27** ([decisions § 755](../architecture/decisions.md)). The filing was accurate — the first in this issue whose premise survived measurement — but the assumption under it was not: `app_pt.arb` is **9.5% Europeanised**, 359 of 3,772 keys, with the whole auth block and much else left as byte-identical Brazilian, so web was behind a catalogue a tenth finished. Lifting from it would have imported those leftovers. `locales/pt-PT.ts` ships instead, 5,049 keys derived from `pt-BR.ts` through a substitution corpus mined from those 359 curated strings: 133 lifted, 1,165 rule-transformed, 5 pinned to the activity vocabulary the wrist cross-checks, differing from Brazilian on **1,288 keys (25.5%)** — which makes the browser the most European surface we ship. `BASE_TO_LOCALE.pt` now points at `pt-PT` (matching the phone; Brazil reports its region on every client, and `pt-AO`/`pt-MZ`/`pt-CV` share Portugal's orthography), giving `BASE_FALLBACK_EXEMPT` the `pt-BR` entry its own comment said it was shaped for. Two things the filing did not anticipate: making pt-PT real would have **dropped Lisbon readers from Brazilian Learn guides to English**, since `getGuide` fell back to English alone — it now falls back within the reader's language first, so the still-unwritten pt-PT prose costs nothing; and six further suites hand-listed the locale set and would have skipped the new catalogue silently (see the new box below). Not natively reviewed — see the native-review item above. Original filing below.
  - Surfaced re-measuring the above (2026-08-25). Mobile carries `app_pt.arb`; web's `BASE_TO_LOCALE.pt` points at `pt-BR`, so the same reader gets European Portuguese on the phone and Brazilian on the web, and the eight localized Learn guides have no `pt` variant either. This is a *missing* catalogue, not an unreachable one — `locale_reach.test.ts` passes today because everything on disk is reachable — but it is drift from [§ 24](../architecture/decisions.md)'s web-is-canonical rule in the one direction that rule does not anticipate. Fixing it is Europeanising a copy of `locales/pt-BR.ts` plus eight guides, which is translation work and wants the native review above, not a mechanical port. Adding it also gives `locale_reach.test.ts`'s `BASE_FALLBACK_EXEMPT` its first entry.
- [x] **Wear OS ships no European-Portuguese string set, and its own locale declarations are unguarded.** **FULLY CLOSED 2026-08-27** ([decisions § 755](../architecture/decisions.md)); the declaration half closed 2026-08-26 ([§ 748](../architecture/decisions.md)). `values-b+pt+PT/strings.xml` ships the remaining **102 named resources** (99 `<string>` + 3 `<plurals>`), which reconciles this entry's "99 strings" with section S's "102 resources" — the same file counted two ways, both right. Both declaration sites moved with it: `locales_config.xml` gained `pt-PT`, `localeCatalogues` gained the `values-b+pt+PT` → (`app_pt.arb`, `pt-PT.ts`) row, and **`unmirroredPhoneCatalogues` is now empty — the wrist mirrors every phone locale for the first time.** Verified the way § 748 verified its own work: with the string set on disk and both sites reverted, `testDebugUnitTest --rerun-tasks` fails exactly two tests, one per site; restored, `assembleDebug testDebugUnitTest` is green at 638 tests, 0 failures. Not natively reviewed. Original filing below.
  - Surfaced re-measuring the pt-PT item (2026-08-25). `apps/watch_wear/android/app/src/main/res/` carries `values/` plus `values-{de,es,fr,ja}` and `values-b+pt+BR`, 99 strings each and all reachable — no `resConfigs` filter strips them, and `res/xml/locales_config.xml` lists exactly those six. A `pt-PT` watch falls back to the Brazilian set through Android's own same-language region matching, which is the right degradation, but it means the wrist disagrees with the phone for the one locale the phone ships and web does not. 99 strings; do it with the web catalogue. **The declaration seam is CLOSED 2026-08-26** ([decisions § 748](../architecture/decisions.md)) — the string set itself is still owed. Counting the sites found **five**, not the two filed: `locales_config.xml`, the manifest's `android:localeConfig` reference (without which that file is inert and the OS picker never lists the app), `L10nResourceParityTest.localeDirs`, `ActivityTypeVocabularyTest.localeCatalogues`, and its `unmirroredPhoneCatalogues` record; a sixth is absent rather than wrong — no Gradle `resConfigs` / `localeFilters` exists, confirming nothing strips a set from the APK. The counts also want one correction: 99 `<string>` plus 3 `<plurals>` is **102 named resources** per directory, not 99. The defect was reproduced before it was fixed: a seventh key-incomplete `values-pt/strings.xml` on unmodified `main` left the suite at 627 tests, 0 failures. `WearLocales` now reads the resource set and every site derives from or is held to it, and `testDebugUnitTest` was found to take `src/main/res` as no input at all — so the guard reported green on a resource-only change until that was declared. **Still open: the 102-resource pt-PT translation itself**, which is translation work and wants the native review above; do it with the web catalogue.
- [x] **Android does not declare `localeConfig`, so the OS-level per-app language picker never lists the app.** Shipped 2026-08-25 ([decisions § 740](../architecture/decisions.md)). `apps/mobile_android/android/app/src/main/res/xml/locales_config.xml` lists all seven and the manifest's `<application>` references it, mirroring what `apps/watch_wear` has carried since its own i18n landed. Held to `lib/l10n/` by the locale-reach guard, so it is a declaration site that cannot fall behind rather than a second list to remember.
- [x] **Supabase auth email templates (SRV-1)** — closed as filed. This entry said localised auth email was "platform-limited, not a quick edit" because GoTrue serves one template set per project; the Send Email Hook removes that limit and `apps/backend/supabase/functions/auth-email/` ships all six locales (`AUTH_EMAIL_LOCALES` in `lib.ts`, hook enabled in `config.toml`). The remaining work is the prod deploy of that function, tracked once, above, under **Auth email — localized templates (Send Email Hook)** — not twice.
- [ ] **RTL on mobile** — `EdgeInsetsDirectional`/`AlignmentDirectional` sweep, deferred until an RTL locale catalogue actually ships. (Web RTL is done; the legal pages stay English-by-design pending counsel.)
- [x] **`app_pt.arb` is a tenth of a European catalogue, so the phone is now the LEAST European of the three Portuguese surfaces.** **CLOSED 2026-08-27** ([decisions § 760](../architecture/decisions.md)). The counts were right and one round stale: **359 of 3,793** keys, **9.46%**, with **3,434** byte-identical Brazilian rather than 3,413 — twenty-one keys landed between the filing and the fix. **822 strings changed**; the catalogue now differs from Brazilian on **1,164 of 3,793 keys, 30.69%**, above web's own 27.70%. The 359 curated strings are preserved verbatim. The corpus was re-derived rather than reused: word-diffing BOTH aligned pairs (`pt-BR.ts` ↔ `pt-PT.ts`, 1,289 keys, and this ARB pair's 359) yields 717 distinct substitutions, of which the ones that fire here are kept and four are DROPPED after reading their call sites — `meta`, `endereço`, `download` and `recursos` are correct European Portuguese in every place this catalogue uses them. **Half-converted strings: 17, not the 2 filed.** Beyond the two named, the whole voice-cue block spoke two registers (`ttsPaceAlertFast` third-person against `ttsPaceAlertSpeedUpByKm`'s `tu`, and the same split across `ttsPaceDrift*`, `ttsTurn*`, `ttsSlight*`, `ttsUturn`), and the curated set contradicted ITSELF on the most-used word of the lot — nine keys called a shareable link a `ligação` where four called it a `link`. **A defect the filing did not reach: `ttsLanguageTag` mapped bare `pt` to `pt-BR`**, so a European reader's spoken cues were Brazilian over European text. Not natively reviewed — see the native-review item above. Original filing below.
  - Surfaced building web + Wear pt-PT (2026-08-27, [decisions § 755](../architecture/decisions.md)). Measured: it differs from `app_pt_BR.arb` on **359 of 3,772 keys, 9.5%** — against web's new 25.5%. The remaining 3,413 are byte-identical Brazilian, and not at random: the entire auth block (`authPasswordLabel` "Senha", `signInForgotPassword` "Esqueceu a senha?", `authErrorOffline` "Você parece estar offline. Verifique sua conexão"), `historyTimelineEmpty` "Nada registrado", `gymOfflineCached` "treinos salvos", `runDetailSuggestRanRoute` "Parece que você correu", `settingsAccountRestoreTileSubtitle` "backup .zip salvo". Two curated strings are half-converted: `safetyAutoLiveShareSubtitle` still reads "compartilhamento ao vivo … em andamento … visibilidade padrão" inside a sentence that was otherwise rewritten, and `ttsPaceAlertFast`/`Slow` keep "Acelere"/"Diminua" where the neighbouring `ttsPaceAlertSpeedUpByKm`/`SlowDownByKm` were moved to "Acelera"/"Abranda" — so the phone says two different things for the one cue. The durable fix is to run the same substitution corpus over the ARB that built `pt-PT.ts`, in one pass, with the iOS twin mirrored; it is a mobile change and was deliberately out of this slice's scope. Wants the native review above either way.
- [x] **`app_pt.arb`'s locale tag disagrees with every other platform's, and nothing can detect it.** **CLOSED 2026-08-27** ([decisions § 760](../architecture/decisions.md)), though **not by the rename the filing proposed — that one is not available.** `flutter gen-l10n` refuses outright when a country-coded catalogue exists without a bare base of the same language ("Arb file for a fallback, pt, does not exist, even though the following locale(s) exist: [pt_BR, pt_PT]") and generates nothing at all, so `app_pt_PT.arb` cannot be the only Portuguese base. Reproduced before it was worked around. The filename is therefore the tool's and **the tag is ours**: `supportedLocales`, `localeLabels`, `_exact`, `_baseToLocale` and `locales_config.xml` all carry `pt-PT`, the generated delegate resolves `Locale('pt', 'PT')` through its `pt` base, and `arbTagOverride` in the locale-reach guard holds the two spellings together with a companion assertion that each entry is still an override — the shape [§ 740](../architecture/decisions.md) already used for the plist. `plistTagOverride` is **gone**: Apple names a locale by region, which is now what every other declaration does too. The parity pair is registered in both registries, and registering it found a real divergence — **the order of `supportedLocales` is load-bearing on the phone and not on web.** Following the device locale leaves `MaterialApp.locale` null and Flutter resolves against the list itself, first language-only match winning; measured against Flutter's own resolver, reversing `pt-PT` and `pt-BR` sends a bare-`pt` device to Brazilian while `_baseToLocale` still says European. Both are now pinned against each other. The durable half is a **tag-vs-content guard on both platforms**: a catalogue tagged for one Portuguese variant may not contain words only the other uses. It flags **319 strings** in the pre-derivation ARB and 0 in this one; no existing guard could see the difference, because they all ask whether a catalogue is REACHABLE and never whether it says what its tag claims. Original filing below.
  - Same round. The phone spells European Portuguese bare `pt` (`@@locale: pt`, `Locale('pt')`), while iOS's `Info.plist` already overrides it to `pt-PT` through `plistTagOverride`, web now ships `pt-PT.ts` and the wrist `values-b+pt+PT`. Bare `pt` is also CLDR's Brazilian default content, so the phone's tag means the opposite of what the file contains. Renaming `app_pt.arb` → `app_pt_PT.arb` (plus `supportedLocales`, `localeLabels`, `_exact`, `_baseToLocale`, the gen-l10n output and the byte-identical iOS twin) would make one tag mean one thing everywhere. Related and worth doing in the same change: `apps/web/src/lib/i18n/locale.ts` and `apps/mobile_android/lib/l10n/locale_support.dart` **describe each other as twins in their own doc comments but are not a registered TS↔Dart parity pair**, so `shared-library-syncer` never checks them — which is exactly why the two drifted to different Portuguese answers for eighteen days without anything noticing. Register the pair once the tags agree.
- [x] **A `pt-PT` reader gets European Portuguese in the browser and on the wrist, and Brazilian in their inbox.** **CLOSED 2026-08-27** ([decisions § 761](../architecture/decisions.md)). All three server-side normalizers and the watchOS String Catalog now answer `pt-PT` in European Portuguese, using web's `EXACT` / `BASE_TO_LOCALE` tables verbatim (`pt-BR` by its own tag; bare `pt`, `pt-AO`, `pt-MZ`, `pt-CV` → European). 293 new strings: 181 in the worker, 55 in `auth-email`, 57 on watchOS. **The filing's "four sites" was the right count for the wrong reason.** Measured: **three** collapse functions, not four — `apps/watch_ios` has no normalizer at all (Apple's bundle lookup decides, from `CFBundleLocalizations`), and the two worker files filed as one site share **one** function, which is also `push_render.go`'s, so that single line decided the language of **eight** job handlers. Meanwhile `delete-account`, filed with the same weight as the others, carries **zero** catalogue keys — it normalizes a tag and the worker renders it — so the translation work was three files, not four. Two counts were off: watchOS is 56 entries × **five** explicit locales plus an implicit `en` (281 localizations, not 336 — the source string is the key in 55 of 56 entries), and `knownRegions` holds **seven** rows, because Xcode's `Base` marker sits among the locales and the derived guard has to skip it by name. **The unwired parity script was exactly as filed — and running it found nothing hiding**: green on `3204ff84d`, exit 0, so the wiring was owed for the future rather than covering a live defect. The § 748 / § 755 hand-listed-set shape existed **twice**, not four times: the Go `emailLocales` slice (ranged by four separate test loops, so a catalogue added to the three maps and missed there was skipped by every guard in the package) and `check_xcstrings_parity.sh`'s `LOCALES`, which additionally read neither declaration site. `AUTH_EMAIL_LOCALES` looked identical and **was already correct** — its test compared the const against the catalogue's keys in both directions; its real gap was `authEmailShared`, never key-set-checked at all. Three test files asserted the defect (the filing named one); all three now pin the rule, plus the three unshipped European regions nothing had covered. Each surface gained a guard proved to trip by deleting a row, including a cross-tier one: `delete-account` renders nothing itself, so its test parses the worker's Go catalogue and fails when the receipt can name a locale the worker has no copy for. `check_xcstrings_parity.sh` now runs in a Linux `watch-ios-locale-parity` job. Not natively reviewed, and **not Mac-verified** — see the two boxes below. Original filing below.
  - Same round; newly sharper *because* pt-PT now exists. Three surfaces still collapse every `pt*` tag to Brazilian and were out of this slice's scope: `apps/backend/supabase/functions/auth-email/lib.ts` (`AUTH_EMAIL_LOCALES` is the six, and `normalizeEmailLocale` does `if (t.startsWith('pt')) return 'pt-BR'`, pinned by its own test), `apps/backend/supabase/functions/delete-account/lib.ts` (same line, deletion receipts), and `apps/job_worker/internal/{email_i18n.go,sms_i18n.go}` (`emailLocales` is the six; the `strings.HasPrefix(t,"pt")` branch returns `"pt-BR"`, and it drives web-push + native-push copy through `push_render.go` as well). `apps/watch_ios` is the fourth: 56 keys × 6 locales in `Localizable.xcstrings`, six entries in `CFBundleLocalizations` and `knownRegions`, and its parity script `check_xcstrings_parity.sh` **is not wired into CI at all**. Each is a catalogue's worth of translation plus a tag row, and the web pt-PT catalogue is now a source to derive from.
- [x] **The web pt-PT catalogue addresses its reader two ways, on the axes its derivation's rules did not reach.** **CLOSED 2026-08-28** ([decisions § 784](../architecture/decisions.md)). The possessive half of the filing was exact — **335 singular `seu`/`sua` as filed, 458 counting the plurals, zero of the `teu` family** — so this was a consistency repair and not a native-speaker call, as § 782 argued. **The residue was 42 strings, not 15**, and the shortfall was three axes nobody had counted rather than one undercounted: § 782's marker list was built from what `app_pt.arb` happened to contain, so it missed `usas`, `digitas`, `corres`, `participas` and `registares`; it had no **second-person preterite** at all (`Ganhaste`, `Completaste`, `confirmaste`, `selecionaste` — scanned here as an `-aste`/`-este`/`-iste` suffix with the nine non-verbs that end the same way named beside it); it never had to consider the **proclitic** `te` (4 strings, scanned in `pt-PT` only because Brazilian writes it beside `você`); and the **imperative** axis added 7 more (`Importa` ×3, `Ativa`, `descarta`, and `Corre`/`visualiza` in the coach block). **The `precisas` trap is live on web too** (`runDetail.hrDisclaimerSuffix`) **and there is a second the phone did not have**: `aceitas` is `aceitar` in the tu form and a feminine plural participle, live in both catalogues at `settingsAccount.aiConsentUpdatedToast`. Both are excluded at the list. **`pt-BR.ts` was fixed first, as this entry said it must be** — 13 strings, every one under `coachPage.*`, a consent disclosure reading in two registers — and doing so proved § 782's blindness claim rather than assuming it: four of those strings were byte-identical to pt-PT's, so the derived scan saw no difference until the Brazilian side moved, at which point it named three tu imperatives that had been invisible. **Reading the derived scan the other way found a defect nobody had filed**: four pt-PT strings ran the same rule backwards and turned a third-person indicative into a subjunctive — "ele **confirme** e passa a receber", "Isto **adicione** ou sobrescreve" — which is broken Portuguese rather than the wrong register. Two further closed classes fell out of the measurement and were fixed with their own derived guards (see the two boxes below for what was left). Not natively reviewed. Original filing below.
  - **The web pt-PT catalogue addresses its reader two ways, on the axes its derivation's rules did not reach.** Surfaced closing the phone's half (2026-08-28, [decisions § 782](../architecture/decisions.md)). `locales/pt-PT.ts` is unanimous on the **possessive** — 335 `seu`/`sua`, zero `teu`/`tua`, which is exactly why the phone could be aligned to it without a native-speaker call — and carries a **15-string tu residue** on every other axis: 8 tu-only finite verbs (`coachPage.consentBulletProfile` `tiveres`, `routeDetail.shareConfirmBody` `podes`, `settingsAccount.avatarRemoveConfirmMessage` `podes`, `notificationBell.eventReminder` + `notificationsList.verbEventReminder` `vais`, `notificationsList.allCaughtUp` `estás`, `nutrition.targets.defaultsHint` `alteras`, `gym.session.leaveSaveFailed` `estás`), 6 `-te` enclitics (`notificationBell.{follow,message,planAssigned}` and their `notificationsList.verb*` twins), and `coachPage.guidedEyebrow` `ti`. **They are largely the same keys the phone carried**, because § 755 derived web by lifting the 133 strings that "already had a curated European counterpart" and those counterparts were the curated `tu` ones — so the derivation converted the possessive axis completely and the verb axis only where its rules happened to reach. The sharpest case is `gym.session.leaveSaveFailed`: "guardar o **seu** rascunho — ainda **estás** aqui … **Tente** novamente ou **descarta** a sessão" is two registers, twice each, in one sentence. Not touched in § 782 because web is a separate deployment unit whose suite that round's constraints did not allow running, and a drive-by half-fix of a catalogue is the defect being closed rather than a smaller version of it. The phone's fix is the model: ~15 strings plus the two-test guard mirrored into `apps/web/src/lib/i18n/locale_reach.test.ts` beside its variant-vocabulary and sense-split siblings. **Note the derived half needs a `você` reference to measure against** — on the phone that is `app_pt_BR.arb`, and web's `pt-BR.ts` does *not* qualify unmodified: it carries **6 `teu`/`tua` in the `coachPage.consent*` block**, half-Europeanised strings that also kept `tiveres`, `digitas` and `corres` beside Brazilian `gênero` and `por padrão`. Fix that block first or the guard's second half is blind.
- [x] **`pt-PT.ts` was sentence-cased as it was derived, and 248 values now start with a capital where Brazilian starts lowercase.** **CLOSED 2026-09-01** ([decisions § 872](../architecture/decisions.md) + [§ 873](../architecture/decisions.md)). The 229 left after § 784's nineteen were read one call site at a time: **208 lowercased, 21 left capital**, and the residual set is now exactly those 21. The filing's own example was the smallest part of it — `coachPage.consentPrivacyLink` is one of 16 assembled-sentence fragments, and the worst class was ten route-description parts interpolated straight into `descSentence`, which rendered “é uma rota Em circuito De asfalto de 10 km” on every pt-PT route page. Two subclasses turned out not to be style calls at all: eleven **unit symbols** (`M` is mega, `H` is henry, `S` is siemens, `min` is never `Min`) and seven **weekday abbreviations**, which Portuguese does not capitalise — es and pt-BR agree and only en and de differ, because their own orthographies require it. And the filing's premise that pt-BR is the target was wrong three times: `dash.allTime` is the third toggle button beside “Semana” and “Mês”, `live.cutoffToGo` sits beside “Chegada {t}” in one metric row, and `coachChat.recentRunsOnly` / `readOnly` / `errorStreamFailed` share slots whose other values are capitalised **in pt-BR too** — so following Brazilian would have introduced the defect rather than closed it. A guard ships for the one subset a catalogue can see — a value interpolated INTO another value, where the outer value's own text says where the slot sits (§ 873); the markup-attached majority stays unguarded and the reason is written down.
  - **`pt-PT.ts` was sentence-cased as it was derived, and 248 values now start with a capital where Brazilian starts lowercase.** Surfaced closing the box above (2026-08-28, [decisions § 784](../architecture/decisions.md)). Nineteen of them were fragments whose leading space had been eaten as well, and those are fixed and guarded — the guard measures the **whitespace** edge, which is unambiguous, and deliberately not the case, because nearly all of the remaining 229 are standalone labels (`dash.average` "Média" against "média", `coachChat.readOnly` "Apenas leitura" against "somente leitura") where either case is defensible and a guard would ship as an allowlist of 248 entries rather than as a decision. At least one is not defensible: `coachPage.consentPrivacyLink` is "Política de privacidade" where all six other catalogues are lowercase, and it renders mid-sentence inside "estão na nossa …". Settling this is a per-site pass over 248 strings, which is the native review below rather than a mechanical fix — a capital is right for a button and wrong for a clause, and only reading the call site says which.
- [x] **`reportDialog.title` carries a gendered demonstrative against a `{noun}` slot that holds both genders, in five catalogues.** **CLOSED 2026-09-03** ([decisions § 1060](../architecture/decisions.md)). Done in the shape the filing named — seven title keys, the determiner written into the same string as the noun — and it is a **net reduction** of one key per catalogue, because the seven bare nouns had no other caller and are gone with it. The English rendering is byte-identical to what the slot produced, so the six Playwright specs asserting "Report this profile" / "club" / "post" / "run" / "comment" needed no change; every locale that was wrong is one no e2e spec reads. `GENDER_FLIPPED_NOUNS` now reads it, keyed by locale rather than pt-PT-only, and widening it took two measurements the filing could not have known: French PLURAL determiners carry no gender, and `les`/`ces`/`aux` in the first draft reported **31 correct strings** — so the French rows are singular-only; and German needed its own treatment entirely (three genders, four cases), listing only the demonstratives no case licenses and matching exact singulars, because the genitive puts an `-s` on the noun and `dieses Clubs` is correct. Zero offenders across all five, proved to trip on all three families. A second guard pins the shape: every kind has a title, none holds a `{noun}` slot, and `ReportDialog.svelte` names all seven. Original filing below. Surfaced closing the box above (2026-09-01, [decisions § 872](../architecture/decisions.md)). The dialog's title is one string — pt-PT and pt-BR “Denunciar este {noun}”, es “Denunciar este {noun}”, fr “Signaler ce {noun}”, de “Diese {noun} melden” — and the seven nouns it fills the slot with are not one gender: `rota`, `corrida`, `avaliação` and `publicação` are feminine where `perfil`, `clube` and `comentário` are masculine, so four of the seven target kinds render “Denunciar **este** rota”. French has the mirror problem (`ce` against feminine `course` / `critique`) and German the reverse (`Diese` against masculine `Klub` / `Kommentar` / `Beitrag` / `Lauf`). Only en (“Report this {noun}”) and ja are gender-free. This is not a case defect and was deliberately left by the case pass: closing it means a **title key per target kind** — seven strings × six locales — so the determiner is written into the same string as the noun it governs, which is the shape the existing agreement guard (`GENDER_FLIPPED_NOUNS`) can then read. A slot-based fix (a per-noun determiner param) would be a second vocabulary to keep in lockstep and is the worse of the two.
- [ ] **The watchOS permission prompts are English on every wrist, in an app that ships seven locales.** Measured 2026-09-02 ([decisions § 888](../architecture/decisions.md)). `WatchApp/Info.plist` carries four `NS*UsageDescription` strings — `NSHealthShareUsageDescription`, `NSHealthUpdateUsageDescription`, `NSLocationWhenInUseUsageDescription`, `NSLocationAlwaysAndWhenInUseUsageDescription` — and there is **no `InfoPlist.xcstrings` anywhere in the repo** (`find apps -iname 'InfoPlist*'` returns nothing; the only `.lproj` in the tree is `apps/mobile_ios/ios/Runner/Base.lproj`, which holds two storyboards). So all 56 UI strings localize into de / fr / es / ja / pt-BR / pt-PT and the four consent dialogs do not. These are the most privacy-sensitive strings the watch shows and the only ones a reader is *required* to act on — a German or Japanese runner is asked to grant heart-rate and background-location access in a language the rest of the app has already agreed not to use. **The phone has the identical shape**, which is why this was not fixed on one tier alone: `apps/mobile_ios/ios/Runner/Info.plist` carries ten usage descriptions and no `InfoPlist.strings` either, so closing it on the watch would leave the two tiers inconsistent and the larger half untouched. Closing it needs a new `InfoPlist.xcstrings` per app **and** four `project.pbxproj` insertions (PBXFileReference, PBXBuildFile, the group's children, the Resources build phase) that no build on this workstation can validate — `Localizable.xcstrings` is wired exactly that way and is the template. When it lands, `scripts/check_watch_ios_source.mjs` should gain a seventh claim: every `NS*UsageDescription` key in `Info.plist` carries a non-empty value for every locale the String Catalog derives, which is the same both-directions shape claim (3) already applies to the keys themselves.
- [ ] **Three watchOS edits from the § 884 – § 888 round have been read but never built, signed or run.** Filed 2026-09-02 ([decisions § 888](../architecture/decisions.md)). Wants one pass on a Mac, in this order, because each is cheap to confirm and silent if wrong. **(a) The Swift.** `Complications/ActiveRunComplication.swift` gained a `private func statLine(_:)` that both `InlineView` and `RectangularView` now pass to `Label(_:systemImage:)` / `Text(_:)`, replacing a literal-with-interpolations that was a `LocalizedStringKey` lookup. The claim that a `String` argument selects Swift's non-localizing `StringProtocol` overload rests on the same construction already shipping four lines away (`Text(formatElapsed(entry.elapsedSeconds))`) — a reading of existing code, not a type-checker's answer. Note the file is in **no target**, so `xcodebuild` will not compile it until the Widget Extension exists; confirming it needs the Xcode wiring in `Complications/README.md` first. **(b) The entitlements.** `com.apple.developer.healthkit.background-delivery` was removed (nothing calls `enableBackgroundDelivery`; the background HealthKit work is an `HKWorkoutSession` the `workout-processing` mode authorises) and `com.apple.security.application-groups` was added for `group.com.threkir.app.activerun`, which `ActiveRunBridge` binds and had no entitlement for. `test-watch-ios` passes `CODE_SIGNING_ALLOWED=NO`, so **CI cannot regress on an entitlement in either direction** — it neither validated the removal nor will it catch a provisioning profile that lacks the new App Group. The device check is: heart rate keeps arriving with the display asleep on a real run, and `ActiveRunBridge.read()` returns a non-empty snapshot after a `start()`. **(c) The locale spot-check § 761 already owes** is unaffected and still outstanding — see the box below.
- [ ] **The watchOS pt-PT catalogue has never been compiled, and the Apple Watch column cannot be verified from this workstation.** Surfaced closing the box above (2026-08-27, [decisions § 761](../architecture/decisions.md)). The String Catalog edit is JSON that `python3` re-parses and the `Info.plist` / `project.pbxproj` edits are one line each, and the new `watch-ios-locale-parity` job holds all three to each other on Linux — but **no Xcode has built the catalog and no simulator has rendered a pt-PT wrist.** The `test-watch-ios` job (macOS, `xcodebuild test`) is the only thing that would catch a catalog Xcode rejects, and it does not run here. Contrast § 755's Wear OS half, which was verified by a real Gradle run at 638 tests. Wants the same simulator spot-check the rest of `apps/watch_ios/CLAUDE.md § Localization` already lists as outstanding — do it in the same pass, per locale.
- [x] **The web pt-PT catalogue still says `senha` 29 times, and `morada` where it means an e-mail address.** **CLOSED 2026-08-27** ([decisions § 760](../architecture/decisions.md)), in the same round that filed it — the slice holding `apps/web/src/lib/i18n/` picked it up. **Both counts were low.** It was `senha` in **33 keys / 36 occurrences**, not 29, and **all six** `morada` uses were wrong rather than the one named — four of them also disagreeing with their own article ("envia **seu morada** IP"). Fixed with the same corpus pass that closed `app_pt.arb`, so the phone, the browser, the wrist and the three server catalogues now render one word for a password. Original filing below. Surfaced deriving the server-side catalogues from it (2026-08-27, [decisions § 761](../architecture/decisions.md)). `locales/pt-PT.ts` carries **29** occurrences of Brazilian `senha` against European `palavra-passe` — including `login.resetEmailSent` ("um link para redefinir a senha") — where `values-b+pt+PT/strings.xml`, curated in the same round, says "Palavra-passe". `settingsAccount.emailChangeInvalid` says "Introduza um morada de e-mail válido", which is both a postal address in European Portuguese and disagrees with its own article. These are the § 755 leftover class, one layer along: the substitution corpus had no `senha` → `palavra-passe` rule because `app_pt.arb`'s auth block is byte-identical Brazilian and so produced no evidence for one. The new server catalogues use `palavra-passe`, so web is now the odd one out on the single most security-relevant word we render. Fixing it is a `apps/web/src/lib/i18n/` change and was another slice's scope this round; it wants the native review above either way.
  - **The web pt-PT catalogue still says `senha` 29 times, and `morada` where it means an e-mail address.** Surfaced deriving the server-side catalogues from it (2026-08-27, [decisions § 761](../architecture/decisions.md)). `locales/pt-PT.ts` carries **29** occurrences of Brazilian `senha` against European `palavra-passe` — including `login.resetEmailSent` ("um link para redefinir a senha") — where `values-b+pt+PT/strings.xml`, curated in the same round, says "Palavra-passe". `settingsAccount.emailChangeInvalid` says "Introduza um morada de e-mail válido", which is both a postal address in European Portuguese and disagrees with its own article. These are the § 755 leftover class, one layer along: the substitution corpus had no `senha` → `palavra-passe` rule because `app_pt.arb`'s auth block is byte-identical Brazilian and so produced no evidence for one. The new server catalogues use `palavra-passe`, so web is now the odd one out on the single most security-relevant word we render. Fixing it is a `apps/web/src/lib/i18n/` change and was another slice's scope this round; it wants the native review above either way.
- [x] **`padrão` means both *default* and *pattern*, and the European catalogues use it for the first.** **CLOSED 2026-08-27** ([decisions § 767](../architecture/decisions.md)). The judgement was right and every number was low. **47 occurrences, not 42** — web **26** across 25 lines (`onboarding.step5Hint` says it twice), the phone **21** — because the filed figures counted matching LINES in lower case; the two a case-sensitive grep walks past (`live.statTimer` / `liveSpectatorStatTimer`, `Cronômetro`) were missed by this round's manual pass too and named by the guard. The wrist and watchOS hold **zero**, so "do it with the wrist in the same pass" needed no wrist pass. **44 mean the pre-set value; three do not, and "at least two" was short by a whole third sense** — two are *pattern* (`loadRamp.meaning_high` / `loadRampMeaningHigh`) and `dash.prAgeGradeTitle` is "the world **standard**", which reads correctly as `padrão` in Portugal exactly as *pattern* does. Every site was classified against `en.ts` / `app_en.arb`; `predefinição` was chosen over `por omissão` because § 760 had already named it and a second vocabulary is worse than an imperfect one. Four strings carry pronouns bound to the now-feminine noun (`substituí-lo` → `substituí-la`), the gender class § 760 recorded for `tela` → `ecrã`. **The guard is the inverse of a deny-list**: `padrão` fails the tag-vs-content scan's own "a word the other variant does not use at all" test, which is why § 760 declined it — so the word is banned and the other-sense sites are named with their English source, and the allowlist fails loudly when a named key goes stale. **Reading the 47 found 30 more Brazilian forms**: the ô/ê-for-ó/é proparoxytones (`cronómetro`, `oxigénio`, `autónomo` — the class the deny-list already held `quilômetro` and `gênero` from, one word at a time), postposed `demais`, `planilha`, `todo mundo`, `liberar`, one `Pulou`, and a third shape of § 755's elision defect: `planNewRecent5kWarning` read "Até **si** confirmar", a stressed oblique standing as a subject, against a web twin that is correct. The wrist and watchOS catalogues had **no lexical guard at all** and now share the web one cross-tier. Not natively reviewed, and two things left for it deliberately — see the native-review item above. Original filing below.
  - **`padrão` means both *default* and *pattern*, and the European catalogues use it for the first.** Measured 2026-08-27 while deriving `app_pt.arb` ([decisions § 760](../architecture/decisions.md)) and left alone deliberately. European Portuguese prefers `predefinição` / `predefinido` where Brazilian says `padrão`, and the phone says `padrão` **20 times**, web **22** — but `padrão` is a less-preferred synonym rather than a Brazilianism, so it is not something the tag-vs-content guard can or should catch, and **one of the phone's twenty means *pattern*** (`loadRampMeaningHigh`, "o padrão mais associado a lesões"), as does at least one of web's. A blanket rule breaks those; converting them wants reading each of the 42 call sites. Doing it needs the wrist's `values-b+pt+PT` in the same pass, and it wants the native review above rather than a second rule. Low value on its own — a Lisbon reader understands `padrão` — so it is worth folding into that review, not a round of its own.
- [ ] **The eight Learn guides have no pt-PT prose.** Same round. `getGuide` now falls back within the reader's language before English ([decisions § 755](../architecture/decisions.md)), so a Lisbon reader is served the Brazilian guide silently and correctly rather than being dropped to English — the absence costs nothing today. Authoring them is 8 long-form MDsvex articles, prose rather than UI strings, and `locale_reach.test.ts` already accepts a `<slug>.pt-PT.md` the moment one lands.

## Email / lifecycle

The notification-email, welcome, Pro-receipt/dunning, and server-side web-push legs all ship (see archive). Remaining:

- [ ] **Native push delivery (FCM/APNs leg) — operator credentials only.** Native push to a locked phone. **All of the code ships** (2026-06-19, migration `20270212_001`): the FCM HTTP v1 + APNs HTTP/2 sender (`apps/job_worker/internal/nativepush/`), the `native_push` handler dispatched from `worker.go`, the device-token-gated enqueue trigger + `clear_device_token` RPC, and the mobile `firebase_messaging` registration (`push_messaging_bridge.dart`, both twins) writing through `ApiClient.registerDeviceToken`. This entry previously read "no `firebase_messaging` wiring exists" and named a `user_devices.push_token` column that has never existed — corrected 2026-08-26 ([decisions § 756](../architecture/decisions.md)); the table is `device_tokens`. What remains is operator-only: a Firebase project, `google-services.json` / `GoogleService-Info.plist`, an APNs auth key, and `FCM_*` / `APNS_*` on the worker. Unset is fail-closed — `handler_native_push.go` leaves the row pending **without stamping it**, so a later credentialed deploy delivers the backlog. Also unblocks safety-contact finish alerts to a locked phone.
- [x] **Account-deletion receipt** — SHIPPED 2026-06-20 (migration `20270217_001`, `account_deleted` template). Built as enqueue-with-inline-address + a non-cascading send-once record (not a second SMTP transport in the EF): `delete-account` captures `user.email` + locale before the cascade, enqueues a `lifecycle_email` job carrying `{email, locale}` and **no `user_id`** AFTER `admin.deleteUser` succeeds (so the `payload->>user_id` drain leaves it untouched), and the worker dedups on a hash of the address via the non-cascading `account_deletion_receipts` table. GDPR Art 17 confirmation mail — no per-feature compliance gate; goes live with the shared `SMTP_HOST` worker gate (see `docs/features/email.md` § Production ops) and a CISO eyeball as a pre-prod deploy check, no new credential. `docs/features/email.md`, `decisions.md § 121`.
- [ ] **Security emails (password-changed, new-device sign-in)** — blocked on absent infrastructure: no GoTrue auth hooks configured and no sign-in/device tracking (`device_tokens` now has a write path — `ApiClient.registerDeviceToken` — but it records push tokens, not sign-ins, so it is not the device/session history this needs). Needs a password-change auth hook + a device/session table with new-device detection. `decisions.md § 121`.
- [x] **Weekly digest + lifecycle drip (engagement email)** — BUILT (digest foundation `20270108_001` + scheduler `20270220_001`; drip `20270223_001` + first-week template `20270331_001`; decisions §174 + §177). Shipped as dedicated `weekly_digest` / `lifecycle_drip` job kinds (not extra `lifecycle_email` templates — §177 records why): per-stream opt-IN prefs (`email_weekly_digest`, `email_lifecycle_drip`, both default off, toggles on web + mobile Preferences), stream-aware RFC 8058 one-click unsubscribe (`/unsubscribe/{weekly-digest,lifecycle-drip}` off one HMAC secret), the `email_suppressions` hard-block + bounce/complaint webhook (`POST /v1/email/bounce`), four localized drip templates (`drip_onboarding`, `drip_first_week`, `drip_reengagement`, `drip_streak`) with cohort selection in SQL (`enqueue_lifecycle_drip()` daily pg_cron). **Send is the fail-closed gate, not the code**: nothing mails until an operator provisions SMTP + the unsub/bounce secrets AND the CISO/counsel sign-off lands (bulk mail under CAN-SPAM + GDPR/ePrivacy) — pre-deploy checklist in `docs/features/email.md § Production ops` items 4-5.
- [x] **Data-export-ready notification (email or native push)** — SHIPPED 2026-08-25 ([decisions § 729](../architecture/decisions.md), migration `20270607_001`). Built as ONE `notifications` row of a new `data_export_ready` kind, so the three existing transports (`notification_email` / `web_push` / `native_push`) pick it up as siblings with no new rail. **The staleness objection is answered by read-time URL minting, not waived**: the message links to `/settings/account` and carries no URL and no expiry of its own, because § 717 stores the object PATH and signs when the subject asks. Idempotency is one statement — `notify_data_export_ready()` inserts the inbox row and stamps `data_export_jobs.notified_at` under a row lock — which makes a redelivery silent AND lets the handler's already-built early return re-ask, repairing a crash between the finish write and the announcement; a failed announcement never fails the export. The pref is an opt-OUT per-kind mute (`notify_data_export_ready`, default on, web Settings → Preferences) that can only ever subtract from the channel modes. Mobile renders the inbox row and routes the tap to Settings → Account; the mute toggle is web-only per § 24. Goes live on the shared `SMTP_HOST` worker gate, no new credential. Original filing below.
  - The precondition this entry ruled itself out on now holds. It read "NOT planned: the export endpoint is synchronous and returns a 10-minute signed URL inline, so an async 'your export is ready' email would arrive stale. Revisit only if export moves to an async/job model." The export IS an async job model since [decisions § 717](../architecture/decisions.md), and since [§ 724](../architecture/decisions.md) it is the only rail on either client — and the staleness objection is answered by the same design that closed it: the signed URL is minted when the subject asks, not when the worker finishes, so a message can carry a LINK TO THE PAGE rather than a URL with a clock already running on it. Both clients currently make the weaker, true promise instead ("you can close this — the download will be waiting"), which is honest but means a runner with a deep history has to remember to come back. The work is a `data_export` completion hook on `FinishDataExportJob` fanning out an existing job kind (`notification_email` or `native_push`), an opt-in pref, the i18n catalogue entry, and the same suppression/unsubscribe rails every other stream rides. Mobile deliberately does **not** poll in the background for this (§ 724: iOS background refresh is opportunistic, so a client that promised to notice would be promising what the OS does not allow), which is exactly why the notification is the right shape rather than a client-side fix.

## Feature-scale / product-gated

- [x] **In-app "Send a route to a follower" (targeted DM send)** — **v1 SHIPPED on web 2026-08-25** ([decisions § 734](../architecture/decisions.md), [route_direct_share.md](../features/route_direct_share.md)). A "Send to a follower" button beside Share on `/routes/[id]` opens `SendRouteDialog.svelte` and sends the public `/share/route/[id]` URL as a plain `direct_messages` body through the existing `sendDm` — no migration, no column, no new RLS. **The picker is the DM-*eligible* set, not the follower list** (`social/dm_recipients.ts`): the INSERT policy admits a follow edge in EITHER direction, so a followers-only picker would hide people the send accepts and an open search would offer sends RLS refuses; the block half is deliberately not mirrored client-side because `user_blocks` is owner-read only and a "can't be messaged" marker would leak the block. Privacy widens nothing the copy-link share didn't — the body is a link whose track still resolves through `clip_route_for_viewer`, and a private route routes through the SAME `share-confirm-dialog` public-flip confirm, cancel leaving it private and the picker closed. Pinned by `tests-e2e/routes/send-to-follower.spec.ts` (5 specs) + 13 unit tests. **v2 CLOSED 2026-08-28** ([decisions § 772](../architecture/decisions.md)), and the filing was wrong in both halves. "A raw URL" undersold it: the thread rendered `{m.body}` as escaped text with no linkification anywhere on the surface, so the recipient received an **unclickable** string to copy by hand. And `TrackPreview` is the one route renderer with no clip in it — `security_guards.test.ts` already pins three surfaces OFF it for exactly that reason, because a bare `<TrackPreview points={route.waypoints} />` was the pre-audit shape that leaked non-owner polylines (§ 33); a DM thread is a non-owner surface by construction, so built literally the filing is a privacy regression. Shipped instead: migration `20270619_001` adds a nullable `route_id` FK (the plain FK the spec's own v2 section preferred over a jsonb bag, `on delete set null` so a tidied-up route cannot delete someone's correspondence) with the INSERT policy gated on `private.is_route_visible_to` — sender-side, because a recipient-side check would accept or refuse the same insert depending on who it is addressed to. `DmRouteAttachment.svelte` resolves through the owner-aware `fetchRouteById` (`public_routes` + `clip_route_for_viewer` for a non-owner) and only then draws the trace, pinned by two new guards. The body **keeps** the share URL — it is the disclosure `routeDetail.sendDm.linkNote` makes, and `dm_threads()` and the Art 20 export both read the body and neither resolves an attachment — and is suppressed on screen only, and only when it points at this route's own path. 12 unit tests, 6 pgtap assertions, `tests-e2e/messages/dm-route-attachment.spec.ts`. **Still owed: the mobile leg**, which remains gated — re-verified 2026-08-28 that no mobile `/messages` twin exists (nothing under `apps/mobile_android/lib/` or `packages/` references `direct_messages`, `sendDm` or `DirectMessage`; the only filename matches are `ble_treadmill.dart` and `rate_limit_message.dart`), so mobile stays on the Share link → OS share sheet and `dm_attachment.ts` is owed no Dart twin.
- [x] **Rate-limit the `direct_messages` rail** — CLOSED 2026-08-25 ([decisions § 737](../architecture/decisions.md)). Migration `20270608_001` puts a `before insert` trigger on the table itself, debiting **two** buckets through the existing `enforce_create_rate_limit` helper: `send_direct_message_burst` at 30/60 s and `send_direct_message` at 250/3600 s. Two windows because `check_rate_limit` is fixed-window, so one "N per hour" either admits the whole allowance in a single instant or trips a real back-and-forth; the burst bucket bounds how *fast* messages arrive, the hour bucket how *many*, and together they cap a flood at 250 messages over at least ~8 minutes instead of 250 in two seconds. Table-level, so `/messages`, the § 734 route-send dialog and every future affordance are covered by one mechanism — verified by a sweep confirming `direct_messages` has exactly one write path (a PostgREST INSERT; no RPC, no Edge Function, no Go worker). **Three things the filed entry had wrong or unstated.** The bucket name it proposed, `'direct-messages'`, cannot be matched by the `rate_limit_errors` pair's `(\w+)` regex, so a hyphenated bucket would have surfaced the raw postgres string at the sender — the buckets are snake_case for that reason. `check_rate_limit`'s documented "denied calls still count" property is **false through a trigger**: the raise aborts the statement and rolls the increment back with it (measured — a refused send left the burst bucket on 30, not 31), so on this rail the counter counts *successful* sends. And the sender saw nothing comprehensible before this: `sendDm` re-threw the `PostgrestError` verbatim for anything but a 42501, so the P0001 now goes through `rateLimitErrorMessage` after the follow-graph branch, with a source guard pinning that order. New pgtap `direct_message_rate_limit_test.sql` (9 tests: burst admitted, burst refused in the client-parser format, nothing written, a batched multi-row insert refused per row, per-sender isolation, the hour bucket refusing on its own, a forged sender still 42501 rather than mis-reported as a throttle, and the roll-back-on-refusal accounting) plus a Playwright spec pinning the composer's alert and the surviving draft.
- [ ] **Strava community segment import (~2-3 wk)** — Strava's segment API requires per-segment OAuth scopes we don't request, and segment-leaderboard data has Strava ToS redistribution limits. Needs a legal + API-scope decision before building.
- [ ] **Family / household Pro tier (~1 wk + pricing decision)** — a household plan (shared subscription across linked accounts) is a pricing/product decision (RevenueCat entitlement model + a household link table), not a code-only change.
- [ ] **Segment-KOM "crowns" in the recap (deferred)** — `segment_efforts` has no stored rank, so a per-segment global-min aggregation across all users would be needed — heavier than a recap card warrants. Personal records stand in as the achievement metric for now.
- [ ] **Auto-follow on club join — product decision (decided: NOT a silent fan-out)** — `join_club_by_token` deliberately does not create `user_follows` edges; club invite tokens are generic so there's no specific person to follow, and auto-following every member is presumptuous + a consent concern. The right shape, if wanted, is an opt-in "Follow members" suggestion list on the club page — tracked here so it isn't re-raised as a silent trigger.
- [x] **A run's segment rank and the segment's leaderboard rank are computed from different populations** — CLOSED 2026-08-19 ([decisions § 696](../architecture/decisions.md)). **The server half had already shipped when this entry was written** and the box was never ticked: `20270523_001` (2026-08-13, [§ 594](../architecture/decisions.md)) gave both `segment_effort_ranks` and `global_segment_effort_ranks` the per-athlete population — counted as distinct strictly-faster rival athletes, which IS the boards' `distinct on (se.user_id)` reduction — plus the block filter and a self-exclusion, pinned by `segment_effort_ranks_per_athlete_test.sql` (8 tests, including the A 60+65 / B 70 → B #2 shape asserted against the board itself) and `global_segment_effort_ranks_per_athlete_test.sql`. So no new migration was needed, and two claims in this entry were wrong: the visibility predicates were **not** divergent — the board's `private.is_run_visible_to` is verbatim the `segment_efforts` SELECT policy (`20260812_001`) and route visibility arrives through the segments EXISTS, so SECURITY INVOKER supplies both; and the block filter, the one predicate RLS cannot carry, was added by `20270523_001` too.
  What the re-audit did find is a **third** population on the phone: `ApiClient.fetchEffortsForRunWithSegments` never moved to the RPC and still counted strictly-faster effort ROWS client-side, so the fixed defect kept shipping to mobile users — and contradicted mobile's own `segment_leaderboard_tiered` board. Now on the RPC (which also retires an N+1), pinned by `packages/api_client/test/segment_effort_ranks_source_test.dart`. The two stale client/test contract comments the entry flagged are corrected. Remaining, deliberately: the boards inner-join `user_profiles` where the chip does not (unreachable — the Art 8 consent gate makes a profile row a precondition of owning a run — and dropping the rival would restate every slower athlete's rank, which § 596 refused on the same grounds), and the demographic tiers / `p_limit` / the anon caller stay unmirrored. Noted while there: both clients degrade a missing rank row to **#1**, so an RPC failure paints a false crown — the RPC is written to always return a row precisely because of it, but the degrade itself deserves its own round. **CLOSED 2026-08-26** ([decisions § 746](../architecture/decisions.md)) — and the premise it rested on was wrong twice over. There were **four** call sites, not three: `core/data.ts` carries the degrade in `fetchGlobalEffortsForRun` as well as in `fetchEffortsForRunWithError`. And the RPC does **not** always return a row: `20270523_001` added the block filter as `not is_blocked_either_way(auth.uid(), …)` inside two SECURITY INVOKER bodies whose calling role may be `anon`, whose EXECUTE on that function `20261108_001` had revoked — so since 2026-08-13 an anonymous reader of a public run was admitted into the body and 42501'd inside it, and only once the rival subquery yielded a row. The RPC therefore succeeded exactly when the crown was real and failed exactly when it was not. Rank is now nullable end to end on both platforms with a neutral placeholder; `20270609_001` routes both bodies through `private.viewer_blocks` and grants `anon` EXECUTE on the catalogue twin, which `20270411_001` had never granted at all.
- [x] **The segment catalogue is in no global nav slot and no sitemap** — DONE 2026-08-18 ([decisions § 677](../architecture/decisions.md)). `/segments` is the fifth tab on the shared `RunSurfaceTabs` strip (Runs · Routes · Segments · Plans · Races), the catalogue index is in `sitemap.xml`, and `/segments` joined `anonExtraExact` so the URL the manifest advertises answers with the catalogue rather than a login form (`global_segments` grants select to anon).
- [ ] **Make an individual `/segments/[id]` page indexable** — the catalogue INDEX is in the sitemap; the per-segment pages deliberately are not (§ 677): each is a leaderboard of named runners, the people-directory objection that keeps profiles out, and they are CSR, so a crawler gets the empty shell. Doing it properly is the step-2 path in [seo.md](../features/seo.md) § Adding a new indexable surface — a `share_segment_lookup` returning anon-safe metadata only (name, geometry, region — never a leaderboard row), a `share_segment_meta` builder, a `/share/segment/[id]` route, a `share-entity` dispatcher branch + CloudFront behavior, and the `share_log_groups` alarm registration. Needs the product call on whether a curated segment is public marketing surface at all before the Lambda work.
- [x] **Consent-flow consistency for health-data fields (legal-adjacent)** — CLOSED on the clients 2026-08-24 ([decisions § 718](../architecture/decisions.md)). **The filed premise was backwards.** Onboarding was the only surface applying a coherent rule; the two settings pages were aligned with neither it nor each other, and they wrote to *different stores*. `/settings/preferences` wrote the `user_profiles` column and never the prefs mirror, and **refused the save entirely** when a DOB was present without consent — so a minor who declined the Art 9 checkbox could not record a birth date at all and kept a NULL age record, the exact fail-open the floor exists to close. `/settings/account` wrote the mirror and never the column, and aborted every save on a DOB whose input the same condition disabled — a deadlock for anyone whose stored DOB outlived their consent. Mobile Preferences wrote the mirror **ungated** and never the column, while `nutrition_targets_screen.dart` and § 695 both claimed DOB was editable only behind the body-metrics consent gate, where there is no DOB field. The decision is now stated rather than implied: the column is the **age record** (child protection, no consent term, written by every entry point and re-asserted after the withdrawal RPC), the bag key is the **Art 9 health-use mirror** (written under consent, cleared on withdrawal, everywhere). Shipped web + mobile + iOS twin with guards on all four surfaces.
- [x] **`withdraw_health_data_consent()` still nulls the age record** — CLOSED 2026-08-24 ([§ 718](../architecture/decisions.md) open item 1, now [§ 721](../architecture/decisions.md)). Migration `20270605_001` is the bare-body `create or replace` dropping `date_of_birth` from the RPC's null list; `height_cm`, `gender` and the `body_metrics` series still go in the same transaction. Both generators re-ran with no diff, confirming the function-only shape. `withdraw_health_data_consent_test.sql` splits into "nulled the consent stamp + height + gender" plus "kept the age record the under-18 floor reads" (10 tests → 11), and the compensation § 718 added on both platforms is gone: web's two settings pages no longer order their profile write after the RPC to undo it, and the phone's body-metrics screen no longer loads the DOB just to write it back (a new withdrawal test pins that it doesn't). Lawfulness is stated in § 721 — the surviving column does child-protection work on a different lawful basis, and Art 17 erasure still clears it through `delete-account`'s `auth.users` cascade.
- [x] **Client surfaces derive age from the ungated DOB column** — CLOSED web + mobile + iOS twin 2026-08-24 ([decisions § 722](../architecture/decisions.md), [§ 718](../architecture/decisions.md) open item 3). **The filed count and the mitigation were both wrong.** Seven sites were named; the sweep found nine reads of the age record across the two platforms, of which **four** were already fail-closed for a second reason, **two** were genuine defects, and **three** were never on the column at all. Already fail-closed: web `dashboard/+page.svelte`'s PR age-grade and mobile `dashboard_screen.dart`'s (both need `gender`, which `ageGradeForRun` refuses without and which `withdraw_health_data_consent()` nulls), and web `nutrition/+page.svelte` + mobile `nutrition_screen.dart` (both need `height_cm` and the `body_metrics` weight series, nulled and erased by the same RPC). Genuine: web `PlanEditor.svelte` ↔ mobile `training_service.dart`'s `fetchViewerAge`, where age ALONE reshapes the generated plan through the masters recovery calibration (50+ → wider hard-day spacing + a 3-week build/recover cycle), and the nutrition-targets metrics card on both platforms, which rendered the runner's age outright on a health surface. Never on the column: web `runs/[id]`, mobile `run_detail_screen.dart` and `intensity_card.dart` all read the settings-bag mirror, which already follows consent. **The filed mitigation — "the open-category age-grade on `/dashboard` is the only claim age alone still produces" — is false in both halves**: `ageGradeForRun` has no open category and returns null without a binary sex, and the claim age alone actually produced was a differently-shaped training plan. Fixed with a `health_consent` parity pair (`healthUseDob` / `healthUseDobState`) rather than seven remembered checks, plus a source guard on each platform. Two things the sweep turned up beyond the filed scope: `training_service`'s `fetchViewerAge` AND `fetchViewerGender` both went through a direct column select the lockdown refuses with 42501, so neither calibration has ever fired on the phone — both now share one `get_my_profile()` read; and the metrics card now says "Needs health-data consent" rather than "Not set" about a date the runner did set.
- [x] **The age-banded segment leaderboards read DOB with no consent term** — CLOSED 2026-08-25 ([§ 718](../architecture/decisions.md) open item 2, now [§ 727](../architecture/decisions.md)). Migration `20270606_001` puts both boards' age band behind `health_data_consent_at`, at **two** sites per function: the band-membership filter inside `best_per_athlete` and the caller's own `age` echo in the outer select. An unconsented runner falls out of the age tier entirely rather than into a wrong one — the same outcome the adjacent `date_of_birth is not null` branch already produced for a runner with no date, and the suite pins the two as indistinguishable — while staying untouched on the unfiltered all-comers board. Gender tiers gained no term and needed none: `gender` is only ever populated under consent. **Two things the filed item had wrong.** The catalogue RPC is `global_segment_leaderboard`, not `global_segment_leaderboard_tiered`. And both bodies it named (`20270424000003` / `20270513_001`) had been superseded by `20270524_001`, the §206 shadow-hidden definer backstop — a bare-body `create or replace` built on the text the item pointed at would have silently reverted that fix, so the migration re-emits `20270524_001`'s bodies with the consent term as the only edit (diffed to confirm). New pgtap suite `segment_leaderboard_age_band_consent_test.sql` (10 tests, both boards) plus a `health_data_consent_at` stamp on the DOB-carrying fixtures in `segment_leaderboard_tiered_test.sql`, whose age bands would otherwise have gone dark. Both generators re-ran with a zero diff, confirming the function-only shape.

## Compliance — DSAR export coverage (GDPR Art 20)

- [x] **DSAR export omits four personal-data tables — and the guard that should catch this is blind to them.** (Closed 2026-06-20.) `session_plans` (`author_id`), `event_orders` (`buyer_user_id`/`host_user_id`), `route_photos` (`owner_id`), and `event_pricing` (host data) were missing from **both** export paths — the Go worker `exportPersonalDataSpecs` (`apps/job_worker/internal/supabase.go`) and the `export-data` EF `buildBackupSpecs` (`apps/backend/supabase/functions/export-data/backup_spec.ts`). Erasure was always fine (all four cascade-delete from `auth.users`, so Art 17 held); the gap was the Art 20 **export**. **Durable fix shipped:** the export-completeness guard (`apps/job_worker/internal/personal_data_export_guard_test.go`) no longer keys on a literal `user_id` column — it now flags any table with *any* owner-style FK to `auth.users` (`user_id` / `author_id` / `owner_id` / `buyer_user_id` / `host_user_id` / `contact_user_id`) that isn't in the export specs or the reasoned-exclusion list (the `user_id` form by name; the other owner names only when the column declaration carries an inline `references auth.users`, so a non-user `owner_id` can't false-positive). All four tables are wired into both export paths (`event_orders` × 2 legs, `event_pricing` via embedded `events!inner` host-id join), the Deno export test asserts each, and widening the guard surfaced five more pre-existing plain-`user_id` gaps now wired in too (`achievements`, `challenge_participants`, `challenge_badges`, `public_recaps`, `route_conditions`). Documented in [`docs/compliance/data-subject-rights.md`](../compliance/data-subject-rights.md).

From `roadmap.md § Competitor-parity backlog`; sizes are rough estimates carried from the roadmap table:

- [ ] **Heatmap / popular-route discovery** (#4, ~2 wk) — materialised tile table or a Go tile service; anonymised aggregation. Open decision: opt-in vs opt-out privacy default.
- [ ] **Trail / offline navigation** (#5, ~3-4 wk) — turn-by-turn on a loaded route + offline tile packs + condition reports. Needs a routing-engine choice (Valhalla vs GraphHopper).
- [x] **Audio-coached runs** (#9) — **the TTS half is now genuinely shipped; closed 2026-08-30** ([decisions § 801](../architecture/decisions.md) + [§ 802](../architecture/decisions.md)). The entry as filed had the scope right and the status wrong in both directions. The pre-recorded-voice half was not blocked, it was **descoped** — roadmap row 9 resolved it to "TTS-only v1, no audio CDN" in 2026-06, so no audio CDN strategy or voice-talent budget was ever owed. And the TTS half that row called shipped **was not built**: `cuesDue` had no production caller on either platform, `run_screen.dart` contained zero occurrences of "guided", the only callers of `speakGuidedCue` were the library's preview button and an unrelated gym timer, and `guided_runs_screen.dart`'s own doc comment called the L0–L4 integration a follow-up. Both surfaces sat at browse-and-preview; nobody could run a guided run. **What landed:** the arm-from-idle affordance + the L4 tick dispatch, its own `guided_run` voice-cue id across all four rails, the owner-only `runs.metadata.guided_run_id` stamp, and the first product-driven `SET1` push to the custom watch. **Three defects found beneath the filing**, each recorded where it belongs: `speakGuidedCue` interrupts (`_tts.stop()`), which `run_recording.md` row 18 names as the deliberate exception for the *preview* — wiring the recorder to it would have truncated a split or off-route **safety** cue mid-word on both platforms, so the recording path got its own queueing `announceGuidedCue`; the `guided_runs.ts` ↔ `guided_runs.dart` pair asserted in both doc comments that the shared-library-syncer watched it while being registered in **neither** registry (§ 801); and `runs.metadata.guided_run_id` was first classified public-safe, which `public_runs`' denylist projection would have published to anonymous readers as a legible `first-timer-15` beginner label (§ 802).
- [ ] **Pre-recorded voice talent for guided runs** — reviewed 2026-08-30 and deliberately left open. The remaining half of #9 is a budget and vendor question (voice talent, an audio CDN, per-locale re-recording across seven catalogues), not a keyboard-ready item; v1 is TTS by decision, not by omission.
- [x] **No handoff from the Coach tab's guided-run detail screen to the recorder.** — **CLOSED 2026-08-30** ([decisions § 805](../architecture/decisions.md)). `Use this run` on `GuidedRunDetailScreen` parks the library id in a `pendingArmGuidedRun` notifier beside `pendingStartWorkout` and pops to the shell; the Run tab arms it through the same `_armGuidedRun` the picker uses, so the watch push, cue-cursor reset and metadata stamp are single-sourced. The estimate held (~40 lines, 4 files). One deliberate divergence from the pattern it copied, and the reason is a latent defect in that pattern: `pendingStartWorkout` PARKS its value when the screen is busy, which for a `String` id would make a second tap on the same run a silent no-op, since a `ValueNotifier` does not fire on an unchanged value.
- [x] **`runs.metadata.guided_run_id` has no reader.** — **CLOSED 2026-08-30** ([decisions § 805](../architecture/decisions.md)). Both platforms now name the script a run was recorded under, resolved through the library for the active locale and never shown as the stored id: a chip in `/runs/[id]`'s provenance row on web, `_buildGuidedRun` on `run_detail_screen.dart` on mobile. An id the shipped library no longer answers to keeps the claim and drops the name rather than self-hiding — the run WAS guided — and both suites pin that the slug never reaches a reader. The projection stays owner-only; a public share-page reader would still be a deliberate re-classification.
- [ ] **`WatchSettings` still has no non-dev product push besides the guided-run arm.** Reviewed 2026-08-30 and deliberately left open: a settings-push surface is its own feature (max HR, pacer, gear, ICE), the device is research-tier with no unit in anyone's hands ([§ 71](../architecture/decisions.md)), and nothing in the app is gated on it. Not a residue of the guided-run work.
- [x] **Race calendar + results import** (#10) — **the client half is closed 2026-08-30** ([decisions § 806](../architecture/decisions.md)); the entry's stated blocker was wrong. "Gated on the RunSignUp key" described neither of the two things actually missing. The `race-results-import` function has had four built legs since 2026-06, and **both clients' import UI branched on `runsignup` alone** — so a ChronoTrack or UltraSignup listing fell through to manual paste, and provisioning `CHRONOTRACK_*` would have lit up a Settings card linking to a page that could not reach the leg it advertised. **UltraSignup did not exist on the phone at all**: absent from the provider union, no probe, no tile, zero ARB keys, against a full web surface in seven catalogues — a straight § 24 inversion. Both platforms now render every leg from one provider catalogue, each with its own scope field and its own unavailable explainer, on both the calendar sheet and the run-detail auto-match prompt (which carried the same defect on a second surface), guarded against the Edge Function's own dispatch set. **Two defects found beneath the filing:** the UltraSignup leg had no pre-fetch scope gate where both its siblings did, and fell back to reading the listing's `provider_race_id` — a RACE id by name and by every other provider's use — as an athlete account, which would have pulled a race's finishers and stamped the caller's `user_id` onto one of them; and the `RACE_IMPORT_UNAVAILABLE` fall-through gave ChronoTrack RunSignUp's error string.
- [x] **`race-listings-sync` is a stub, so no provider race ever enters the calendar** — the real remaining work on #10, and **not** a credential gate. It validates its credentials and returns `{ synced: 0 }`; the upcoming-races fetch + upsert is unwritten (the function's own comment has said so since it shipped). Fully provisioned, the calendar would still hold only parkrun, crowd submissions and hand inserts. Deliberately not written speculatively on 2026-08-30: the response shape cannot be observed without a key, and guessing it is the same fragility the per-site scrapers were deferred for. Needs one provider's credentials in hand, then the parser is small. **Closed 2026-09-02 in round 33** — [decisions § 977](../architecture/decisions.md). The deferral's reasoning was sound about the payload and wrong about the balance: with the leg stubbed, no provider race can enter the calendar at all, which is the whole of the remaining #10 work. The path is written behind the unchanged fail-closed credential gate, and the unverifiable half is handled explicitly rather than hidden — every field reading is optional-with-drop and COUNTED, so a differently-shaped payload answers `synced: 0, unusable: <row count>` on the first call, and a wrong endpoint answers a non-2xx which is refused `502` under the same fail-loud rule the sibling importers follow. Writes as the service role (the `force_unverified` trigger and the `submitted_by = auth.uid()` INSERT policy make a caller-scoped write land as an unverified crowd submission); reads-then-splits rather than upserting (the unique index is partial and PostgREST cannot arbitrate it, 42P10); rewrites only changed rows. Two defects found in the new code by its own tests: an unreachable `month > 12` guard, and `distanceMetresFrom` folding US `'M'` (miles) onto SI `'m'` (metres), which would have filed a 100-mile race as a 100-metre one.
- [x] **`isProviderConfigured` fails open on anything that is not a `FunctionException`** — offline, or Supabase not initialised, both read as configured. Worth a decision: fail open (a probe failure should not hide a working provider) or fail closed (the house default). Same question on web. **Decided 2026-09-03 — fail closed** ([decisions § 1067](../architecture/decisions.md)), and the web half is done. The reasoning does not turn on `FunctionException` at all: a probe asks one question and only a 200 answers it, so nothing a failure carries can prove the credential gate was cleared. Web's own grader turned out to fail open on a readable 4xx too — `race-results-import` answers 401 before it reads an env var and 400 `unknown_provider` for a leg it does not dispatch, so a signed-out session and a nonexistent provider both reported the card as live — and now reports through `core/provider_probe.ts#probeSaysConfigured`, which is available iff there was no error. The import path keeps its own grader under the name `isProviderGateRefusal`, because an import can fail for reasons that say nothing about a credential. **Two lanes read this entry in the same round and reached opposite verdicts; the reconciliation is that both were half right** ([§ 1073](../architecture/decisions.md)). The mobile lane is correct that the two cases this entry NAMES were already closed by [§ 1007](../architecture/decisions.md) two days after it was written — `raceProbeUnavailable` answers unavailable for a `SocketException`, for a `StateError('not initialized')` and for a 429 / 5xx, and both call sites initialise `ok = false` before their try. What neither § 1007 nor the entry considered is the case the web lane found by reading the probe's own endpoint: a readable non-429 4xx returned `false` from `raceProbeUnavailable`, so mobile reported the card as LIVE for a signed-out session (401) and for a provider the function does not dispatch (400 `unknown_provider`). Mobile now grades through the same rule as web. **Both halves are now done — closed 2026-09-03** ([§ 1073](../architecture/decisions.md)). `raceProbeUnavailable` is deleted (it had one `lib/` call site, and the import path's own grader `_isProviderNotConfigured` asks a different question and never referenced it), and `isProviderConfigured` now reports configured only when the invoke did not throw. That is exactly "a 2xx" rather than an approximation of one: `functions_client` 2.5.0 returns a response for 200-299 and throws for everything else. No Dart twin of web's `provider_probe.ts` was created — a `try -> true / catch -> false` states the same rule natively, and a one-call-site wrapper would be a new parity pair needing rows in both registries, which is [§ 641](../architecture/decisions.md)'s mistake if only half-registered.
- [ ] **Advanced analytics polish** (#11, ~2 wk) — no new tables; richer dashboard breakdowns + race-time predictor over what VDOT / training-load already ship. **Race-time predictor shipped** (2026-06-20): the multi-distance `race_predictor` TS↔Dart parity pair (`predictRaceLadder` — recency-weighted Riegel anchor, per-rung confidence reusing `predictionConfidence`) + the `RacePredictorCard` on web `/dashboard` AND the mobile `widgets/race_predictor_card.dart` on the Home dashboard (5K/10K/Half/Marathon ladder, self-hiding on the qualifying-run gate, both platforms). **Consistency card shipped** (2026-07-11): the web-only `training/consistency.ts` pure helper (`computeConsistency` — active weeks out of the last 12, current + best active-week streak with an in-progress-week grace, and a steady/variable weekly-volume CoV) + the self-hiding `ConsistencyCard` on web `/dashboard` (< 2 active weeks → hidden), scoped to `filteredRuns` so the source/activity-type filter carries through. 16 unit tests + a Playwright surface test. Web-only (no Dart twin, deliberately off the parity list). Remaining ideas: easy/hard intensity distribution, week/month trend deltas.
- [x] **Dashboard best-streak sub-label is windowed, not all-time.** Closed 2026-08-03 ([decisions § 471](../architecture/decisions.md)) — the last surviving half of the #332 follow-up (§ 470 closed the PeriodSummary drilldown). The `run_streaks_for_user(p_tz, p_source)` gaps-and-islands aggregate (migration `20270501_001`) serves `(current, best)` in one row on the paint path, bucketed by the runner's **local** day (client passes its IANA zone, matching `computeRunStreaks` rather than the awarder's UTC shortcut) and taking the dashboard's source filter so the sub-label's all-time claim stays true under a filter. Web `fetchRunStreaks` returns null — never zeros — on failure, and `streak_card.ts#streakCardState` then suppresses the numeric best claim instead of presenting the windowed number as all-time. 12 pgtap tests (pre-window island, 23:30-local/UTC-date edge, grace day, source filter, owner scoping) + 8 helper unit tests + source guards. **Mobile closed 2026-08-03 ([decisions § 475](../architecture/decisions.md))** — the § 471 note's open store-population question resolved the other way: the dashboard card reads the SAME RPC (`ApiClient.fetchRunStreaks`, device IANA zone via `flutter_timezone`, explicit `'UTC'` degrade) through the new `streak_card.ts` ↔ `streak_card.dart` parity pair, with a mobile-only pointwise-max fold so an unsynced local run is never walked back; no full-history backfill onto the device.
- [ ] **Premium billing extensions** (#12, ~1-2 wk) — Stripe Checkout + customer portal + paywall enforcement across web + mobile.
- [x] **Treadmill BLE FTMS — live run-screen wiring** (#13, mobile-only) — **the code all shipped**; this entry was stale. The parser, model, pairing tile and `RunRecorder.setTreadmillSample` seam were already in; the run-screen wiring landed 2026-06-19 ([treadmill_live_mode.md](../features/treadmill_live_mode.md)) — `_toggleTreadmillMode` in `run_screen.dart` (paired-belt-gated `SwitchListTile`, own `_treadmillSub` / `_treadmillStatusSub`, L4 try/catch + banner-revert) against the app-owned `BleTreadmill` singleton threaded `main.dart` → `HomeScreen` → `RunScreen` and → `TreadmillTile`. `parity.md`, `roadmap.md` item 13 and `integrations.md` were all flipped at landing; only this line was not. **Closed 2026-08-21** after an audit that found the toggle was not honest about the belt's state and closed the gap ([decisions § 704](../architecture/decisions.md)): the subtitle now switches on `BleTreadmillStatus`, `connected` is the only arm that can format a speed, and the `connecting` arm gained the `setState` it never had. 12 widget tests now drive the real screen through the real toggle + status listener via the `BleTreadmill` emit seams (the five states, the subscribe/unsubscribe lifecycle across start/pause/resume/stop, a mid-run disconnect leaving L0/L1 intact, the connect-failed path, the sample-fault guard) — the three that existed before replicated the screen's wiring in unit tests rather than driving it, so a screen-side regression could not fail them. **Still owed: hardware-in-the-loop verification against a real FTMS belt** — a device gate on the FTMS decode + reconnect behaviour of actual hardware, not unwritten code. Nothing in the app is gated on it; the belt is opt-in and off by default.
- [x] **Route-design preferences (scenic / cul-de-sac / multi-objective ranking)** — **closed 2026-08-30** ([decisions §§ 795-798](../architecture/decisions.md)). Three of the filing's four remaining items shipped; the fourth was blocked on data, not on code, and the filing did not name the defect underneath any of them. **The defect:** `handleGenerate` skipped the graph-cycle sidecar entirely whenever a preference was set (`config.graphCycleUrl && !req.preference`), so ticking "Quiet roads" silently moved the runner OFF the durable v3 graph search onto a custom-model `round_trip` — the preference was in practice a switch trading route quality for road class. It had to be that way, because the sidecar's `decodeBody` uses `DisallowUnknownFields` and a request carrying `preference` got a 400. **What shipped:** the sidecar retains a packed per-edge attribution byte (road-class bucket + a green flag rasterised from `leisure=park` / `natural=water` / `landuse=forest,grass` ways onto the spatial index's own 200 m cells) where `build.go` previously discarded every way tag — unbuilt prerequisite work the filing did not name, since there was nothing to weight; soft preference multipliers on the search (never 0, never an edge removal, so a hard filter cannot disconnect a buildable neighbourhood) with a never-deny unweighted retry; opt-in cul-de-sac mode splicing capped structural dead-end stubs (4 / 250 m one way / 20 % of target, only onto a loop running short) and crediting them at a weight sized to cancel the isoperimetric penalty they incur; and one multi-objective score replacing the two-branch selection, pinned against a verbatim copy of the old selector over 800,000 random candidate sets spanning the whole accepted target range — which caught a real divergence, a 1e-9 tolerance on the leading objective that made the old comparator **non-transitive**, so no scalar score could reproduce it until the objective was snapped to that same grid. **The first draft of that score was itself a live regression in the default generator** and its pin could not see it, because the corpus stopped at a 4.5 km target and the failure began there; the review that caught it is recorded in [§ 795](../architecture/decisions.md). The preference now rides the chain (sidecar first carrying it, `round_trip` under a custom model only as the fallback it already was), and both the 200 body and the sidecar report `preferenceApplied` from the **serving engine's verdict, never echoed from the ask** — every path can serve a loop that ignores the preference, so a client repeating the ask would tell the runner their quiet route was quiet on no evidence. `/routes/new`'s checkbox became a four-option native radio group (the values are mutually exclusive on the wire), which reports when an ask was not honoured; 7 i18n keys × 7 catalogues; and the AI route assistant extracts `preference` as a whitelisted vocabulary beside `shape`/`surface`. **A second unfiled defect, found twice independently:** the assistant listed "Avoid main roads." in its "Filled in from your request" summary while `handleNlRequest` never set `quietRoads` — the summary claimed a constraint the generate request did not carry. **Deferred, for want of data rather than code:** elevation-aware. GraphHopper boots with no `graph.elevation.provider` (`apps/job_worker/graphhopper/config.yml` lines 5, 13-15) and the sidecar's PBF parse keeps only `lat`/`lng` because an OSM extract carries no terrain, so a grade-weighted cost would have nothing behind it; `elevation` is absent from the wire vocabulary rather than accepted and ignored. Unblocking it costs **a DEM as a deploy input** (SRTM/Copernicus tiles on the sidecar's Fly volume beside the PBF, sampled per node at build time), which is a deploy-shaped change, not a code-shaped one. Web-only (route generation is web-canonical). Design in [graph_cycle_loop_generation.md § Extension](../features/graph_cycle_loop_generation.md).
- [ ] **Elevation-aware route-design preference — needs a DEM on the sidecar's deploy** — new 2026-08-30, split out of the entry above so the one blocked item is not buried inside a closed one. Not code: neither engine imports terrain. See § 795 for the sourced reason and the cost.

- [x] **Mobile mirror: session-derived Log-as-workout prefill on event detail** — done 2026-07-11. `event_detail_screen._logAsWorkout` now loads the attached session plan (`_maybeLoadSessionPlan`, best-effort L4) when a `class` event carries a `session_plan_id`, expands it via `expandSessionSteps`, and passes `seedSets` + `seedTitle` to `showGymComposeSheet` through the pure `logWorkoutSeed` helper (mirrors web's `logWorkoutPrefill`: discipline/plan title → flat title fallback, one seeded set per expanded step). Added `session_plan_id` to `_eventSelectCols` and carried `duration_s` through the composer's `seedSets` map so timed poses survive. Twin-mirrored; unit tests in `event_detail_screen_test.dart`.

- [x] **Mobile mirror: honour `show_calories` on run detail** — done 2026-07-11. `run_detail_screen`'s calorie tile now gates on `SettingsSyncService.service.effective<bool>('show_calories') != false` (default on), and Settings → Preferences → Units & Display carries a "Show calorie estimates" `SwitchListTile` writing the universal bag via `_putUniversal`. `SettingsKeys.showCalories` registered; ARB keys `prefsShowCalories`/`prefsShowCaloriesHint` in all six locales; widget tests in `run_detail_screen_test.dart` (tile hidden when false / shown by default) + `settings_show_calories_test.dart` (toggle write path); mirrored to the iOS twin.

## CI reliability

- [x] **`playwright install-deps` hard-failed a test shard when the runner's apt mirrors did not answer.** Five shard failures across two pushes on 2026-08-18 (round 4: shards 1, 7, 13; round 5: shards 6, 7), every one identical: three 120 s attempts, all timing out, then a hard fail, with no test ever run and a clean re-run every time. Closed 2026-08-18 ([decisions § 627 amendment](../architecture/decisions.md)) by making the apt half **conditional**: the step only ever verified — every library it installs is already the newest version on the hosted image — so `./.github/actions/install-playwright` now verifies directly by **launching Chromium** (the same headless-shell binary `devices['Desktop Chrome']` drives, and strictly stronger than reading `ldd`, which cannot see what Chromium dlopens) and reaches for apt only when that launch fails. The verdict afterwards is another launch rather than apt's exit code, because install-deps can succeed with the launch still broken and can fail on a mirror having already installed what was missing. No retries were added. The check cannot pass vacuously: any launch failure for any reason sends the job to apt, and a launch that still fails there fails the step with the browser's own error naming the library, plus the mirror list.
- [x] **Two Renode sim flakes, neither of them the firmware — closed 2026-08-24 ([decisions § 716](../architecture/decisions.md)).** Both surfaced on the same afternoon on PRs touching no firmware (a Terraform lockfile bump, an npm dep bump) and both passed on re-run, so neither is the `pages` wedge below: that one was deterministic per binary and lived in the RTC model, these two live in the harness. **(1) `alerts` sampled a repaint.** It waited for `record: alert <Kind>` — the RECORD task's line, emitted *before* the alert is published to the composer — then dumped the panel and retried four times. The flush lands ~11 ms of firmware time later, so which side of it the dump arrives on is decided by the runner's timing: **3 of 8 identical local runs missed on the first attempt**, and a contended two-core hosted runner missed all four and reported "+340 ink" against the 500 an inverse-video band adds. The screen task now states its own post-flush condition (`ui: banner <Kind>` / `ui: banner none`, the same contract `ui: page` has carried since § 361), every dump waits for it and re-reads it once the frame is on disk, and the quiet baseline's lowest-of-three sampling is gone. Eight consecutive post-fix runs landed the banner on the first attempt. **(2) `bin/watch-sim.sh` failed silently.** Its EXIT trap ended `exit 0` unconditionally — hence "exited early with **status 0**", a failure detectable only by a missing side effect — and ran under `set -e`, so `pkill -P $$` returning 1 (nothing left to sweep) abandoned the rest of the teardown including the `wait` and the latest-run pointer. Both fixed at the trap, with SIGINT the one deliberate exception. The phone-port TOCTOU the old message speculated about is real and was reproduced by racing two sims: the fixed port 7788 is now **claimed** with a per-port `flock` held on a descriptor Renode inherits, the monitor port (previously a bare unchecked draw) likewise, and the 30 s pty deadline — short enough to fail a healthy cold boot — is now a 180 s failure bound whose message names the last `watch.resc stage:` marker reached instead of guessing. **Not reproduced locally:** all four attempts missing in one run; what was reproduced is the per-attempt coin flip it is made of.
- [x] **14 fixed real-clock delays remain in the mobile widget suite, each with a stated reason; 44 were converted 2026-08-24 ([decisions § 723](../architecture/decisions.md)).** The round-11 estimate of "~50" measured 58 delay sites sitting next to `tester.runAsync`. Converting them was a classification exercise, not a rewrite, and the conversions were graded by temporarily instrumenting `pumpUntil` to print the iterations it actually spent: a predicate that never spins has converted nothing. Two findings are worth carrying forward. **A store's row list is not its write** — `OfflineSyncStore.persist` populates `rowsById` before `writeJsonAtomic` and notifies only once the row file and the index are both down, so a row-count predicate returns mid-write and lets an `addTearDown` temp-dir delete race it; wait on the store's `notifyListeners` (via `addListener`, no production seam) or on a UI state ordered after `persist()` returns. And **`pumpUntil` can never wait for a route transition, animation, or dialog dismissal** (it deliberately never advances the fake clock) and **cannot be called from inside a `tester.runAsync` block** (no nesting). **A third member of the same family was found on `main` on 2026-09-02 and closed** ([decisions § 991](../architecture/decisions.md)): the residue above is about a store's row list standing in for its write, and `sync_service_test.dart` had the outer version of it — a bare `pumpEventQueue()` standing in for a whole sync cycle whose store writes run AFTER the assertion's `saveBatchCallCount`, so the cycle outlived the test and raced the group's `tearDown` into `errno 39` (CI run 33690185523). The layer at fault was the service, not the test: `didChangeAppLifecycleState` and `start()` are `void` and discarded the cycle's future, so nothing could know one had finished. `SyncService` now holds it (`debugInFlightSync`). Survey of the class: 86 mobile test files create a temp directory, 9 bare `pumpEventQueue()` calls exist across 2 files (the other pumps inside a bounded loop over an observable predicate, which is correct), and 28 files pair a temp directory with widget taps and no `pumpUntil` at all — see the round-33 entry below.
  **What is left, all deliberate.** *Models elapsed time (1):* the 11 s GPS blackout in `run_screen_recording_flow_test.dart:848`. *No condition that is ever false (7):* absence assertions in `nutrition_log_sheet_test.dart:351` (cancelled scan), `import_screen_paths_test.dart:153` (pick with no readable path), `checkpoint_checkin_screen_test.dart:181` (empty bib), `run_screen_test.dart:318` + `run_screen_recording_flow_test.dart:1247` (the treadmill pairing read, invisible at idle), plus `watch_screens_editor_screen_test.dart:81` and `plan_new_screen_test.dart:265`, which drain a mocked in-memory read the assertions then consult directly. *No observable outcome (6):* teardown drains at `gym_screen_test.dart:602` and `routes_screen_fab_double_tap_test.dart:96`; `watch_live_screen_test.dart:183`, which exists to let a stream-cancel finish; `roadbook_screen_test.dart:64` + `route_detail_watch_course_test.dart:326` (21 call sites over a stored-race-plan read that resolves to `null` in 13 of 14 tests, so it changes nothing to poll for); and `dashboard_streak_card_test.dart:98`, whose test bodies are themselves inside `runAsync`. **Do not add a test seam to production code to make any of these convertible.** The remaining live question is whether the two existing `pumpUntil` call sites at `nutrition_screen_test.dart` that wait on `f.store.rows.length == 2` should move to the `persisted` probe — they are the same before-the-write proxy, in a file whose `finally` deletes the temp dir.
  **Closed 2026-09-03** — [decisions § 1072](../architecture/decisions.md). The remaining live question is answered and **three of this entry's four counts were stale**, which matters because a later lane sizes the work from them: the "9 bare `pumpEventQueue()` calls across 2 files" is **8 call sites across 3 files** (the ninth occurrence is inside a COMMENT in `sync_service_test.dart` describing § 991's own fix, so the grep counted the evidence as more of the problem), "28 files" is **26**, "86 files create a temp directory" is **88**, and the 14-item enumeration is short by three: `route_review_report_test.dart:74`, `routine_public_library_screen_test.dart:188` and `run_screen_recording_flow_test.dart:1626`. **That last one matters most, because this entry is a hand-transcribed copy of a register that is already machine-checked** — `test_timing_guard_test.dart`'s `_documentedDelays` lists every surviving wait with its reason, fails on an unlisted site AND on an exemption whose sites have all been converted, and carried all three of the "missing" ones. It proved that on this change: converting the two made their exemptions stale and the suite failed naming both. Size this work from the map, not from this paragraph. Two of the three were convertible and are converted (the review helper now waits on the seeded review all three of its callers assert, with the comment text hoisted to one named constant; the adopt test's fixed 8 x 20 ms poll now waits on the store row the next line asserts); the third is byte-identical to a case this entry already lists as deliberate and got the stated reason it was missing. **The `nutrition_screen_test` question resolves to a lever this entry does not cite** — `OfflineSyncStore.debugWritesSettled()`, `@visibleForTesting`, whose doc comment describes this exact race almost verbatim — and using it in a second file surfaced a precondition nobody had stated: **it is only awaitable when the write it waits on was queued from inside `tester.runAsync`.** `serialiseStoreWrite` links its chain with `.then`, so a write queued from a fake-zone tap has its continuation scheduled as a fake-zone microtask, which only `pump` drains and `runAsync` does not pump — so awaiting the chain deadlocks rather than failing. Measured three ways: fake-zone tap + the call hung past a 240 s bound twice, the same test without the call passed in 1 s, and the same test with the tap moved inside `runAsync` and the call restored passed in 1 s.
- [x] **The `pages` sim scenario wedged the firmware before its first log line — closed 2026-08-18 ([decisions § 666](../architecture/decisions.md), [#788](https://github.com/Absence0760/threkir/issues/788)).** The first CI failure carrying § 661's probe named `pc=0x1a000` with 3.9 M instructions retiring across two samples — a guest busy-wait — and the ELF the job stages beside it disassembles there to `ldr`/`cmp`/`bne` against `0x40011504`, RTC1's COUNTER: embassy-nrf's `RtcDriver::init` spin, confirming § 661's finding. **Two claims in the old entry were wrong**, both blaming the environment over the artifact: booting the ELF *CI itself uploaded* reproduces the wedge on arm64 macOS at the same PC, and the outcome is deterministic per **binary**, not per host — never a race, which is why `PerformanceInMips` 1/1000, `SetGlobalQuantum` 1 us and `CyclesPerInstruction` 200 all "still booted". Measured cause: Renode folds a CPU's executed slice into `BaseClockSource` only when the CPU reports, and `Update` charges that accumulated time to every *enabled* entry, so an RTC enabled part-way through absorbs the part that ran before the guest started it — `TASKS_START` at virtual time `0.001000000 s`, the COUNTER read one instruction later at `0.001065330 s`, 65.33 us, two ticks, and the spin then waits out the 512 s wrap. Fixed in the model, not the firmware: `sim/NRF52840_RTC_Overflow.cs` syncs the CPU's unreported time into the clock source before START / STOP / CLEAR / TRIGOVRFLW touch the timers, pinned by `sim/test_rtc_model.py` (a scenario cannot guard it — dropping the sync only fails for builds landing the wrong side of a tick). No budget widened, no retry, no skip. Verified on the exact CI ELF: 3/3 `pages` at `--budget 300`, reverting only that file wedges the same command at the same PC, and all nine scenarios green in one sweep.
- [x] **The local Supabase edge runtime intermittently never boots, failing a job in setup.** Closed 2026-08-19 ([decisions § 697](../architecture/decisions.md)) for both halves the entry asked about. **(2) The error string was the one lying.** The loop counted 45 attempts and called that "90s", which charges the `sleep 2`s and nothing else, so an attempt that spends curl's whole `--max-time` is free: 45 x (10 s + 2 s) = 540 s, i.e. the ~9 minutes actually observed. The budget is now a wall-clock deadline, the last attempt's `--max-time` is clamped to what is left of it, and the failure reports the elapsed seconds and attempt count it really spent. The budget itself was **not** widened, no retry was added, nothing was skipped — a probe whose upstream answers fast still gets the same ~45 tries. **The 90 s deadline was measured, not inherited:** across 277 jobs carrying this step in 14 workflow runs (2026-08-13 to 2026-08-19, every stack-using job type), the 275 green ones took a median of 1.21 s and a max of 12.15 s — 274 under 2 s, exactly one needing a single retry (shard 10/14, run 32150279557) — so the worst healthy boot on record uses 13 % of the deadline, and 90 s still affords eight full-`--max-time` attempts against the one ever observed. The sampling also found a **third occurrence** the entry did not know about: run 31834502152 (2026-08-14, e2e shard 12/14), 530.04 s over 45 attempts, reported as "within 90s", with attempts 2-45 exactly 12.013 s apart — 10 s of curl timeout plus the 2 s sleep, which is the measured proof that `000` here is a hung upstream, not a refused connection (attempt 1 got kong's instant `502`). The same arithmetic corrects this entry's own reading of `000`: 12 s per attempt means kong was accepting the connection and hanging for the full `--max-time`, not refusing it. `(last status 000000)` was a third small lie — `$(curl … || echo 000)` appended a second `000` to the one curl writes itself. **(1) Forensics now exist:** giving up dumps the stack's container list, every container's state + exit code, and the failing sidecar's plus kong's `docker logs` tail, so the next occurrence is diagnosable from the run log alone. The loop moved out of `action.yml` into `.github/actions/start-supabase/wait_for_sidecars.sh` so it could be pinned — `wait_for_sidecars.test.mjs` drives it with stub `curl`/`docker` on `PATH` under `workflow-lint`, and every accounting assertion was verified to fail against the old arithmetic. Original text: Seen twice on 2026-08-14 in one session — `Web e2e (Playwright) shard 8/14` on PR #764 and `Web e2e — Go live-hub (WebSocket)` on PR #766 — both dying in the *Start local Supabase stack* step with `edge runtime (clip-public-track) never became ready within 90s (last status 000000)` after ~45 consecutive curl attempts, ~9 minutes apart from the stated 90 s. No test ever runs; a re-run of the failed job passes clean both times. Two things to look at: (1) why the edge runtime container never serves, which the step's own forensics do not currently capture (it logs the wait loop but not the container's logs or exit state); (2) the message says "within 90s" while the loop actually ran ~9 minutes, so the wait helper's timeout accounting and its error string disagree — whichever is right, one of them is lying to whoever reads the failure. **Do not "fix" this by widening the timeout**: the runtime is not slow, it is not coming up at all (status `000000` is connection-refused, not a slow 200), so a longer wait would only make each failure more expensive. Worth catching because the failure is indistinguishable from a real break at a glance and trains people to re-run reflexively.

- [x] **A per-function edge worker could not boot without reaching `registry.npmjs.org`, which is what "the edge runtime never boots" has been all along.** Diagnosed and fixed 2026-08-19 ([decisions § 699](../architecture/decisions.md)) — the diagnosis came from the forensics the entry above added, on their first real firing (PR #793's own `Edge Function tests (Deno)` run). The runtime itself starts normally; the **worker** dies: `failed to create the graph: Failed loading https://registry.npmjs.org/@types%2fnode for package "@types/node" … Network is unreachable (os error 101)`. Cause: esm.sh's default (denonext) build of supabase-js imports `node:buffer` / `node:process`, and the runtime resolves `node:` builtin **types** from npm before a worker may boot. Every probe then hangs to curl's `--max-time` and reports `000`, which is the measured signature above. Reproduced deterministically outside CI — `edge-runtime start --main-service` with `--add-host registry.npmjs.org:127.0.0.1` reproduces the CI error byte for byte — and that harness is what graded the candidates:

  | candidate | offline worker boot |
  |---|---|
  | esm.sh default (denonext), i.e. before this change | **fails** |
  | `?no-dts` | **fails** — it is the `node:` specifier, not the declarations |
  | `?target=deno` | **boots** (no `node:` specifiers at all) |
  | `?target=deno` on stripe | **fails** — the stripe SDK imports `node:http` itself |
  | edge-runtime v1.73.15 / v1.74.3 (latest), default build | **fails** — a CLI bump does not fix it |
  | import map redirecting `npm:@types/node` to a local stub | not intercepted |

  Fixed where it is load-bearing rather than everywhere: `clip-public-track/index.ts` (the function every stack-using job's setup probes for its 405) and `_shared/rate_limit.ts`'s **type-only** import, which measurably re-arms the failure on its own even though it is erased at emit. Verified by booting the real repo files against a blackholed npm registry and getting the probe's own `Method not allowed`. **Deliberately NOT applied to the other 16 functions**: `?target=deno` swaps the `node:` imports for esm.sh's `deno.land/std@0.177.1` polyfill tree, which grows one function's bundle from **228 KB to 2.4 MB** (measured with `edge-runtime bundle`), so blanket-applying it would pay a 10x cold-boot cost on every function to convert a test-time failure that names itself into a slightly earlier one. Residual exposure, stated rather than hidden: the four stripe functions (`donations-checkout`, `events-cancel`, `events-checkout`, `events-connect-onboard`) and the other supabase-js functions still resolve `@types/node` at boot, so an npm outage can still fail a test that calls one — but it can no longer kill a job in setup before any test runs. Closing that residue needs a vendored/pre-seeded runtime module cache, not an import parameter. Guarded by `_shared/offline_worker_boot_guard.test.ts`, which fails the PR if either probe-path import loses `?target=deno` (mutation-checked).

## Testing gaps

- [ ] **Device-instrumented `integration_test` harness** — none today; would cover tile-cache / foreground-service / background-sync on real Android primitives. New infrastructure.
- [ ] **OSRM smoke test in CI** — blocked on free-runner capacity (OSM PBF extract + osrm-extract memory). Options: a self-hosted runner, or a pre-built OSRM cache in S3 the workflow downloads.
- [x] **`tests-e2e/routes/segment-catalogue-browse.spec.ts`** — written alongside the `/segments` browse page ([decisions § 593](../architecture/decisions.md)) by a round that shared a local Supabase stack it could not reset, so it landed unexecuted; **run on the integration branch 2026-08-13 and it passed.** No longer an open gap.
- [x] **Positive-path Edge Function tests** — **the filing was wrong in both halves; closed 2026-09-03** ([decisions § 1079](../architecture/decisions.md)). `_shared/handler_envelope.test.ts` holds 23 cases and **10 are positive paths**, covering every class named: `revenuecat-webhook: 200 on valid HMAC + fresh anonymous event`, `400 event_outside_freshness_window on stale event`, the `pro → dedupe → free → lifetime` `user_profiles` write, two `stripe-events-webhook` cases that each dedupe a replay and assert the written row, the event-order `checkout.session.expired` CAS, and two `strava-webhook` handshake / non-create cases. Verified by booting `supabase functions serve --env-file .env.local` exactly as `ci.yml` does and running the file: **23 passed, 0 failed**. Nor did any of it need a real secret: for an HMAC webhook the secret is *ours*, and `ci.yml` already writes `REVENUECAT_WEBHOOK_SECRET=ci-revenuecat-secret` and friends into `.env.local` and exports the same values to the test process. A third-party credential is only needed where the success branch calls the third party. The genuine residue is two functions and is filed narrowly at the end of this file: `refresh-tokens` (its success branch POSTs to strava.com — credential- or mock-blocked) and `auth-email` (NOT credential-blocked; blocked on adding SMTP vars to `.env.local` in `.github/workflows/ci.yml`).

- [x] **The `Schema / type drift` job reports "database.types.ts is out of sync with the Supabase schema" for any failure in the job, including a plain unit-test failure.** Closed 2026-08-24 ([decisions § 711](../architecture/decisions.md)) — the trailing `if: failure()` step is gone and each of the five checks now diagnoses itself in the `if ! cmd; then echo "::error::…"; exit 1; fi` form the `twin-parity` and `schema-codegen-drift` jobs already used, with its own remediation; each migration guard now runs its own unit suite FIRST, because a broken guard's verdict on the migration set means nothing. The job was renamed `Schema / type drift + web unit tests` rather than split in two — splitting is the only thing that makes the job NAME say which check broke, and it costs a second `npm ci` plus a hosted-runner slot on every PR against a budget the concurrency block is already rationing, while a per-step `::error::` reaches the checks summary for free. `scripts/check_ci_diagnostics.mjs` (in `workflow-lint`) fails the build on any `failure()`-conditioned step that prints a diagnosis without naming a step outcome, and on a check step added to that job without one; verified against the pre-fix file, which it reports five ways. A failed web source guard in that job printed the type-drift error and the "run `npm run gen:types`" instruction, which is a confident statement about the wrong thing — the first look went to the migration and the generated types, neither of which was involved. Same class as the CI wait loop's "within 90s" ([§ 697](../architecture/decisions.md)): the message should name what actually failed, or the step should be conditioned on the drift check specifically.

## Web bundle weight

- [x] **Externalise the RevenueCat web SDK behind hosted checkout** (done 2026-06-20) — `@revenuecat/purchases-js` was a top-level static import in `apps/web/src/lib/billing/revenuecat.ts`, shipping the full SDK (~178 KB gzipped) into the `/settings/upgrade` bundle. The web Pro flow now drives both checkout and management through RevenueCat's **hosted-checkout redirect**: `proCheckoutUrl()` redirects to the Web Paywall Link `https://pay.rev.cat/<token>/<userId>?redirect_url=…`, and `managementUrl()` opens the no-code customer portal — no embedded SDK, no API key in the bundle. The dependency was dropped from `apps/web/package.json` + the root lockfile, removing the ~178 KB from the summed bundle metric (no `purchases-js` import remains in `apps/web/src`). Env config renamed `PUBLIC_REVENUECAT_WEB_API_KEY` → `PUBLIC_REVENUECAT_WEB_CHECKOUT_URL` (+ optional `PUBLIC_REVENUECAT_WEB_PORTAL_URL`), with the production-env guard, CI workflows, and security_guards test updated in lockstep; the fail-closed configured-vs-unconfigured fallback is preserved. Pure URL builder unit-tested (`revenuecat_links.test.ts`, 6 cases); the unconfigured-fallback e2e covers the redirect-fail-closed path. **Mobile parity:** N/A by design — `apps/mobile_android/lib/revenuecat.dart` keeps `purchases_flutter` because iOS Guideline 3.1.1 / Play policy require native IAP for digital goods; the web hosted-redirect is a web-only optimisation, not a parity gap.

- [x] **The bundle ceiling now climbs once per language, because the metric counts catalogues nobody but that language's readers download.** Closed 2026-08-28 ([decisions § 771](../architecture/decisions.md)). The premise held — a single total over every emitted chunk is right for a dep and wrong for a translation — but three of the filed numbers did not, and one of them decided the design. **Measured on a production build:** the total is exactly **2462 KB** as filed; the largest chunk is **245 KB**, not 248; and the catalogues are **six chunks totalling 522 KB** (de 88, es 85, fr 88, ja 91, pt-BR 85, pt-PT 85), a **fifth** of the bundle rather than a quarter, not seven totalling ~609 KB. **The seventh is not a chunk and "every reader downloads exactly one" is false**: `i18n/store.svelte.ts` imports English statically as the synchronous fallback dict, so rollup emits it inside the shared store chunk (80 KB gzipped) that every reader downloads before a locale is negotiated — every non-English reader downloads **two** catalogues, and the English 80 KB is an unconditional cost that belongs in the code budget rather than the catalogue one. The measurement is now `scripts/check_web_bundle_budget.mjs` (+ its `node --test` sibling, both run from `web-bundle-budget.yml`), classifying chunks from vite's client manifest because client chunks are content-hashed and carry no name a pattern could match. Three ceilings replace the one: **code 2120 KB** (measured 1934, ~9.6% headroom), **largest single code chunk 350 KB** unchanged (measured 245, catalogues now excluded), and **100 KB per catalogue, never summed** (measured max 91, ja), so a new language moves nothing. Proved on the real build in both directions: a 200 KB rogue dep totals 2656 and **passes** the retired 2700 ceiling while failing the code one; three more languages total 2720 and **fail** the retired ceiling while leaving the code figure untouched. Slack against a rogue dep goes 244 KB → 186 KB, and — the point — stays 186 KB however many languages ship.

- [x] **The bundle budget measures no HTML, so a per-locale prerendered surface would be invisible to all three ceilings.** Closed 2026-08-28 ([decisions § 775](../architecture/decisions.md)). **The premise about `/learn` is false and the file the same walk was hiding is 54x the whole population asked about.** The build emits **16** HTML files, not the 8 guides x 6 languages the filing assumed: 8 guides, 6 category pages, `/learn` and `/`, in **one** language — prose localization has not shipped, so the second per-language cost does not exist yet. All 16 total **71 KB gzipped**, largest 5 KB. Widening the walk to see them found what else `*.js`/`*.css` was hiding: **33 files and 4058 KB gzipped**, of which one file is **3866 KB** — `material-symbols-outlined.woff2`, the full unsubsetted variable font, `@font-face`d from the ROOT layout's stylesheet with `font-display: block`, i.e. **1.8x the entire 2120 KB code ceiling** and blocking for every reader who renders an icon. Decided: the population is **every emitted non-JS/CSS file**, not HTML's alone, budgeted at `MAX_ASSET_KB = 100` **per file and never summed** — the catalogues' rule, for the catalogues' reason, since a reader loads one prerendered page and one favicon. So prerendering `/learn` once per language multiplies the file count and moves no ceiling, which is the filed question answered. The font is exempted **by name** with its own 3900 KB ceiling (pattern skipping vite's content hash, so a version bump keeps it and growth does not); an exemption matching no emitted file fails, and so does one whose asset has shrunk back under 100 KB. Six new tests; subsetting the font is `apps/web` source work and is filed separately below. Original filing below. **The bundle budget measures no HTML, so a per-locale prerendered surface would be invisible to all three ceilings.** Inherited from the retired rule and unchanged by [decisions § 771](../architecture/decisions.md): the walk matches `*.js` and `*.css` only. Today that costs nothing measurable — the 8 Learn guides × 6 languages prerender to static HTML and emit no JS chunk, so the second per-language cost in the tree is outside the metric entirely — but it means the guard cannot see prerendered weight at all, and the `/learn` tree is exactly the surface that would grow it. Decide whether prerendered HTML gets a population and a ceiling of its own, or is deliberately out of scope and said to be.

- [x] **A red web bundle budget does not block a merge.** Closed 2026-08-28 ([decisions § 775](../architecture/decisions.md)). **The premise held exactly; the costing of the fix did not.** The filing priced moving the job into `ci.yml` as "a second `npm ci` + a `vite build` on the gate's critical path" — but `build-web` in `ci.yml` **already runs that exact install and that exact build**, with the same four `PUBLIC_*` placeholders, and is already in the gate's `needs:`. The second install was the one `web-bundle-budget.yml` was paying: **47 s** repeating what a **69 s** job had just done, and only on `pull_request`, so a direct push to `main` measured nothing at all. The measurement now runs against `build-web`'s own output at a measured **0.21 s**, plus 9 s of stdlib-only guard tests, and the duplicate job is deleted. **The gate's critical path is unchanged either way**: on run 33175862509 it is `test-packages` at **17 min** against an 18 min 01 s wall clock, so any candidate here is two orders of magnitude below it and the decision was about runner-minutes, not wall clock. Bundling rather than splitting follows `parity-matrix`'s own written rationale; `build-web` now derives as a bundled job and each of its named steps diagnoses itself per § 764. Original filing below. **A red web bundle budget does not block a merge.** `web-bundle-budget.yml` is its own workflow and its jobs are not in the `CI gate` aggregator's `needs:` list, which is the single required status check — so the budget is advisory in practice however tight the ceilings are ([decisions § 771](../architecture/decisions.md), § 764 for the aggregator rule). Unchanged by the split and not fixed there because it is a `ci.yml` decision, not a budget one: either the budget job moves into `ci.yml` (paying a second `npm ci` + a `vite build` on the gate's critical path, against a runner budget the concurrency block already rations) or the aggregator learns to wait on another workflow. Worth deciding rather than leaving implicit.

- [x] **A single 3866 KB icon font is 1.8x the whole web code budget, and it blocks first paint.** Closed 2026-08-28 ([decisions § 780](../architecture/decisions.md)). **The filing is right in every particular, and the fix is a subset built from the tree rather than a smaller exemption.** Re-measured: the upstream file is 3,960,036 bytes, 3866 KB gzipped, `font-display: block` on the root layout, and the non-JS/CSS population was 33 files / 4058 KB. The glyph set is DERIVED in two rules — element text at every render site, plus every quoted `[a-z0-9_]+` literal under `apps/web/src` that the upstream font can render, which is what finds the ~190 names that only ever reach a span through `{item.icon}` or a `*_ICON` map. That needs a vocabulary and the package does not ship a usable one: `index.d.ts` lists 3899 names against the font's 4271 and omits every alias whose ligature resolves to a differently-named glyph (`terrain` → `landscape`, `emoji_events` → `trophy`), twelve of which this app renders — so the vocabulary is read out of the font and committed at `scripts/material_symbols_ligatures.txt`. 347 icons over 332 glyphs; `GRAD`/`opsz` pinned at the values the source already asks for, `FILL`/`wght` kept variable because components vary both. **3866 KB → 74 KB (−98.1%)**, assets 4058 → 266 KB, and the exemption is deleted rather than resized — `ASSET_EXEMPTIONS` is now empty. `font-display: block` STAYS: for a ligature icon font every other value paints the icon's own NAME in the fallback face, and blocking was only ever a problem because the face was 3.9 MB. Guarded by `scripts/web_icon_font.test.mjs` in `build-web`, which re-runs the selection and compares with the manifest in both directions, so an icon added to the tree and an icon that left it each fail with the name in the message. Original filing below. **A single 3866 KB icon font is 1.8x the whole web code budget, and it blocks first paint.** Found measuring the asset population for [decisions § 775](../architecture/decisions.md). `apps/web/src/app.css` imports `material-symbols/outlined.css`, so the **full unsubsetted** Material Symbols Outlined variable font lands in the root layout's stylesheet as an `@font-face` with `font-display: block` — every reader who renders any icon (which is every page: the nav alone uses several) waits on a **3,960,036-byte** download before that text paints, and 3866 KB gzipped is **1.8x the entire 2120 KB `MAX_CODE_KB` ceiling** and **42x** the largest catalogue (ja, 91 KB). It was invisible to the bundle budget until the walk was widened, and it is now held at a named 3900 KB exemption in `scripts/check_web_bundle_budget.mjs` — ceilinged so it cannot grow, not fixed. The durable fix is to subset to the glyphs the app actually uses: the icon names are literal ligature strings in `.svelte` markup and in `BADGE_CATALOGUE` / `ROUTE_MARKER_KINDS`, so the used set is derivable from source rather than guessed, and a build-time subset (or self-hosting only that subset) should land the font in tens of KB. That is `apps/web` source work plus a guard that the derived glyph set still covers every ligature the tree names — deliberately out of scope for the CI slice that measured it. Deleting the exemption entry is part of closing this: the budget fails on an exemption whose asset has shrunk back under the general ceiling.
- [x] **`unplugin-icons` is configured for `apps/web` with `autoInstall: true` and nothing imports it.** Closed 2026-08-28 ([decisions § 786](../architecture/decisions.md)). **The filing is right in every particular, and three more of the same class sat beside it.** Verified independently: no `~icons/` specifier exists in any extension the build compiles, and the only two occurrences of the name under `apps/web/src` are a licence row and its spec mirror, both strings. Deleting the plugin and `@iconify-json/material-symbols` took **21 entries out of the npm tree (423 → 402)** — the transitives are the `autoInstall` machinery itself (`@antfu/install-pkg`, `package-manager-detector`, `tinyexec`, `local-pkg`), so the thing that could have installed an unlocked package at build time is gone rather than merely unused. Sweeping the class found **`normalize.css`** (declared in the scaffold commit `943f6af6e`, imported by nothing ever, and called the styling stack by `apps/web/CLAUDE.md` and `README.md` for the life of the repo), a stray root **`html-to-image`** (only `apps/web` imports it and only `apps/web` needs to declare it; `10b5ec519` added both at once), and **`apps/web/vercel.json`** (a deploy config for the platform [§ 53](../architecture/decisions.md) decided against, read by nothing). Everything else is genuinely indirect and stays: `material-symbols` is read out of `node_modules` by `scripts/web_icon_font.mjs`, the three `@types/*` back modules the tree uses, `tsx`/`typescript`/`svelte-check` are binaries whose names differ from their packages, and root `svelte` is the documented hoist pin. `scripts/check_dead_dependencies.mjs` (new, in `parity-types`) makes the class non-returnable: derived where it can be, four falsifiable `INDIRECT` entries where it cannot. Run against the pre-change tree it fails on `@iconify-json/material-symbols` rather than on the plugin — the honest limit, recorded in the ADR. Production build unregressed: largest asset still 74 KB, `ASSET_EXEMPTIONS` still empty. Original filing below. **`unplugin-icons` is configured for `apps/web` with `autoInstall: true` and nothing imports it.** Found while subsetting the icon font ([decisions § 780](../architecture/decisions.md)). `vite.config.ts` registers the plugin and `@iconify-json/material-symbols` is a devDependency, but there is no `~icons/` import anywhere under `apps/web/src` — every rendered icon comes from the Material Symbols webfont via a ligature. Two docs described the plugin as how web icons work, which was never true of a single icon; those are corrected, the dead configuration is not. `autoInstall: true` is the part that argues for removing rather than leaving it: it lets a build install a package that was not in the lockfile. Delete the plugin and the `@iconify-json` devDependency, or adopt it deliberately for the icons the webfont cannot serve.

## watchOS deferred target

- [x] **CheckpointStore.loadTrackPoints buffers the whole track file at finish** — SHIPPED 2026-08-03 (issue #664, [decisions.md § 467](../architecture/decisions.md)). The read now mirrors the streaming save: `forEachTrackPoint` walks the NDJSON 64 KiB at a time, carries at most one partial line and yields a point at a time, still skipping an undecodable tail. `FinishedRun` stopped carrying `[TrackPoint]` — it holds the file URL + point count — so `writeTrackJSON` streams into a staged file it renames and the DEBUG upload maps that artefact rather than re-encoding; streaming the read alone would have moved the peak, not removed it. **The ~60 MB figure here was an undercount**: recomputed at the 100 h / ~360k-fix target it is ~73 MB *inside `loadTrackPoints` alone* (~27 MB file + ~11.5 MB `Substring` headers + ~17.3 MB records + ~17.3 MB of heap `ts` strings — the 20-byte ISO stamp is past Swift's 15-byte small-string cap, the term the old estimate missed), plus the resident array and its encoding. Pinned by new XCTest cases (chunk-boundary walk, truncated vs whole final line, flush-boundary JSON validity, file lifetime); the Swift is **Mac-CI-compiled only**, and an Instruments trace over a long recording is still a bench item.

## Live-tracking residual (wave-1 follow-ons, 2026-06-13)

The headline live-tracking bugs (staleness honesty, name-leak on the event leaderboard, retention, reconnect JWT, tie-order/cap, in-zone last-seen carve-out) all shipped. Residual scope edges:

- [x] **Label the coarse last-seen ping in the spectator/SAR UI (web + mobile).** Done 2026-06-20. The web `/live/[id]` page + mobile `live_spectator_screen.dart` (+ iOS twin) now read the `coarse` flag on the latest ping and render a distinct marker (web `.runner-dot.coarse` hollow amber ring; mobile `LiveRunMap.coarsePosition` → `_CoarseDot`) plus an "Approximate / last seen near here" badge + sub-line, gated on a live (non-terminal) `coarse=true` fix. i18n added to all six web locales + all mobile ARBs; Playwright (`tests-e2e/live/spectator.spec.ts`) + a Flutter widget test pin the badge. Note: the **event live pages** were NOT in scope after reading the code — they render `race_pings`, which has no `coarse` column (the `20270121_001` carve-out covers `live_run_pings` only). Extending the carve-out to `race_pings` is the separate follow-up below.
- [x] **Extend the privacy-zone last-seen carve-out to `race_pings`.** SHIPPED 2026-07-02 (migration `20270309_001`, [decisions.md § 196](../architecture/decisions.md)). The sibling `race_pings` BEFORE-INSERT trigger now retains an in-zone ping coarsened to a ~2-dp grid + flagged `coarse=true` (at most one per `event_id/instance_start/user_id`) instead of dropping it; only lat/lng are coarsened (distance/elapsed/bpm kept for ranking). The competitive-board-semantics concern resolved in favour of the same last-seen honesty the solo feed provides — the web event live page renders the coarse "last seen near here" chip + hollow-amber marker. Web-only (no mobile event-live spectator reads `race_pings`); pgtap + Playwright pinned.
- [x] **Mirror the next-cut-off "signal lost" vs "still connecting" copy split to mobile.** Done 2026-07-11. `live_spectator_screen.dart`'s `_CutoffCard` now takes the screen's `stale` flag and branches the suppressed (`unknown`) verdict like web `/live/[id]`: a stale fix reads the amber w700 `liveCutoffSignalLost` ("Signal lost — can't project arrival", matching the screen's existing Delayed/amber freshness treatment) while a fresh still-connecting fix keeps the neutral italic `liveCutoffWaitingSignal`. ARB key added to all seven mobile catalogues (six locales + base `pt`); pure UI, no helper change (the `live_freshness` + `live_cutoff_eta` twins already exposed the flag). Both branches pinned in `test/live_spectator_cutoff_test.dart`; mirrored to the iOS twin. See [predictive_live_tracking.md § Design](../features/predictive_live_tracking.md).
- [ ] **Validate the Strava-CSV-UTC + FIT-compressed-timestamp fixes against real files pre-prod.** Both landed (Strava `Activity Date` parsed as UTC; FIT compressed-timestamp records decoded) but only against the documented format + a synthetic fixture. Get a real Strava bulk export + a real older-Garmin/Wahoo compressed-timestamp FIT and confirm the format string(s) + field layout before relying on them in prod (the parse is fail-safe, so the risk is "no improvement", not corruption).

## Pure-logic-helper hunt deferrals (2026-06-14)

Low-reach TS↔Dart twin divergences found by the 2026-06-14 helper hunt; the higher-impact bugs from that round were fixed + pinned. Pick these up the next time the file is touched:

- [x] **`macroBudget` negative-tie rounding diverges TS↔Dart.** `nutrition_budget.ts` uses `Math.round` (rounds negative `.5` toward +∞); the Dart twin uses `.round()` (half away from zero). Latent in production (inputs are exact integers, so `.round()` is a no-op) — only reachable via direct fractional calls. Pick one convention and align both twins. **Done:** the Dart twin now rounds via `_roundHalfUp(value) => (value + 0.5).floor()` (matches JS `Math.round`); negative-half-tie parity test added both sides + mirrored to the iOS twin.
- [x] **`computeSessionAdherence` can report >100% adherence.** `session_steps.ts` (+ `.dart`) computed `completedSteps / totalSteps` without clamping or matching results to steps by `itemId`; a results array longer than the steps array yielded `>1` and still verdicted `'completed'`. Fixed: completed-result counting is keyed to the set of step `itemId`s (extra/duplicate/unmatched results no longer inflate), the count is capped at `totalSteps`, and the fraction is clamped to `[0,1]`. Mirror tests added on both sides (more-results-than-steps → 1.0; unmatched itemIds ignored).
- [x] **`gym_routine.prefillFromRoutine` RPE string diverges for whole-number doubles.** Done 2026-06-20. Web is canonical (`String(s.targetRpe)` → `"8"`); the Dart twin (`apps/mobile_android/lib/gym_routine.dart` + iOS) now formats RPE via a `_rpeString` helper that drops the trailing `.0` on a whole-number double (8.0 → `"8"`) and keeps the decimal on a half-step (7.5 → `"7.5"`), matching JS `String(number)` semantics. The TS file was not touched. Pinned by a mirror test on both sides (`prefillFromRoutine: whole-number RPE drops the trailing .0, half-step keeps it`); test counts stay in lockstep (23 each).

## Pure-logic-helper hunt deferrals (2026-06-15)

Verified-but-deferred findings from the 2026-06-15 five-worker bug hunt (the three clear bugs that round — checkpoint-board tie-break jitter, roadbook equal-clock 0s cutoff, plan-progress zero-distance long run — were fixed + pinned). Both parked on a product call were resolved + shipped 2026-07-02:

- [x] **`gym_adherence` fails a weighted-duration set on an unlogged weight even when the full duration was held.** RESOLVED 2026-07-02 ([decisions.md § 194](../architecture/decisions.md)). Product call: duration is the primary axis while the weight is unrecorded. The weight gate now fails a set only when a weight was actually logged and fell short, or when the set has no duration axis (weight-only, unchanged); a both-axes set with the weight unlogged is graded on duration. Twin-pinned (`gym_adherence.ts` ↔ `.dart`, 20 tests each).
- [x] **`gym_progression` percent_cycle labels a real prescription `'hold'` when there's no prior top weight.** RESOLVED 2026-07-02 ([decisions.md § 195](../architecture/decisions.md)). Product call: added a dedicated `establish_baseline` `ProgressionReason` for the first/bodyweight session (concrete 1RM-percentage weight, no prior top weight) instead of the misleading `'hold'`; the suggested weight is unchanged. Localized in all six web locales + all mobile ARBs, exhaustive `gym_detail_screen` switch extended, twin-pinned (20 tests each).

## Mobile bug hunt deferrals (2026-07-24)

Findings from the 2026-07-24 three-worker mobile hunt that were parked for a
product call, a schema change, or a coordinated web+mobile change. The nine bugs
fixed that round (route-progress floor poisoned by a rejected fix, treadmill
console reset, pace across a pause, silent turn cues on a second run, negative
splits, run-index ordering, DST week boundaries, backup-restore abort,
route-sidecar fail-open) are in git history on the same PR.

**All twelve were resolved on 2026-07-25**, on the same PR, each with the
decision it needed recorded in [decisions.md](../architecture/decisions.md)
§§ 299-305 and a pinning test confirmed to fail against the old behaviour. A
follow-up hunt the same day found that the § 303 route-store half had been
claimed but not shipped, plus seven further defects in the same subsystem; those
are folded into the list below and recorded as §§ 308-313 (and an amendment to
§ 301). Two
carry a judgement the owner may want to revisit rather than a defect: the
hydration ceiling's specific value (§ 300) and the run-store's
re-upload-rather-than-presume-synced asymmetry (§ 299). Kept here rather than
pruned so the reasoning stays next to the finding it answers.

- [x] **`OfflineSyncStore.markSynced` re-reads the live row instead of the pushed snapshot.** RESOLVED 2026-07-25 ([decisions.md § 302](../architecture/decisions.md)). The compare key is **identity**, not `lastModifiedAt`: entries are immutable and every mutation installs a new instance, so `identical` is exact, while a clock comparison would miss two mutations inside one tick. `markSynced` now takes the pushed entry and marks only while it is still resident; the `pendingDelete` branch applies the same test before `dropRow`, so a tombstone raised mid-push is no longer flipped to synced and left with a live server row. `syncWithServer` also gained the drain guard (a second concurrent call returns 0). All eight subclasses inherit both. Three pinning tests, each confirmed failing against the old behaviour.
- [x] **Two `LocalRunStore` / `LocalRouteStore` instances write the same whole-file sidecars from independent snapshots.** RESOLVED 2026-07-25 ([decisions.md § 303](../architecture/decisions.md)). **This entry was wrong when first written and is corrected here: only `LocalRunStore` was changed that day.** The merge and `_withSidecarLock` never reached `local_route_store.dart`, whose three sidecars (`synced_route_ids.json`, `route_owner_tags.json`, `offline_pinned_route_ids.json`) stayed unlocked whole-file replaces even though `background_sync.dart` builds a second `LocalRouteStore` over the same directory and calls `markManyRoutesSynced` + `tagRoutesOwner` from it. Both stores are now covered. Run sidecars: `synced_ids` keeps an unknown id only while its run file exists (the process-independent ghost prune); `pending_remote_deletes` lets additions win and carries removals in a cleared-ledger. Route sidecars: all three resolve a shared id the `pending_remote_deletes` way, because for routes the losing direction is a cross-account transfer (an un-tagged route is drainable by any account on the device) rather than a re-upload — § 299's asymmetry applied to the other store. Pinned by three two-stores-over-one-directory tests per store, each confirmed failing against the whole-file write.
- [x] **`LocalRunStore._persistIndex` is an unlocked whole-file replace, and the cold-load gate only compares the id set.** RESOLVED 2026-07-25 ([decisions.md § 309](../architecture/decisions.md)). Per-id content drift from the background isolate's stale snapshot passed the membership-only gate, loaded verbatim, and was re-persisted from memory forever — so every all-history consumer read a stale distance while run-detail (which reads the file) read the corrected one, and the stale `last_modified_at` let a delta fetch clobber the local row. `_persistIndex` now merges per id under `_withSidecarLock`, newest-wins on `_lastModifiedOf` with ties to this process, pruning by run-file existence rather than against `_summaries`.
- [x] **`LocalRunStore.update()` never persists the unsynced transition and never restores residency.** RESOLVED 2026-07-25 ([decisions.md § 308](../architecture/decisions.md)). `synced_ids.json` outranks the index on cold load (§ 299), so an offline edit was silently re-marked synced on the next launch and could never drain; separately, an edit to a windowed-out run was never inserted into `_runs`, which `unsyncedRuns` reads, so it was invisible to the drain even in-session. Both fixed, plus the same durability hole in `save()`'s re-save case. Run-detail now marks the run synced after a successful `updateRunFields`, or the newly-durable un-sync would schedule a full-track re-upload on every title edit.
- [x] **`LocalCrossingsStore.replaceFromServer` treats a single event+instance fetch as a complete set.** RESOLVED 2026-07-25 ([decisions.md § 310](../architecture/decisions.md)). The method takes the fetch's scope and preserves synced rows outside it, mirroring the gym / food window guard; retention still sweeps the preserved rows so the 90-day Art 9 mirror keeps working for other events.
- [x] **`createBackup` archives only server rows, so an undrained run is silently absent.** RESOLVED 2026-07-25 ([decisions.md § 311](../architecture/decisions.md)). The archive now carries the device's unsynced runs and their in-memory tracks, and the server-first path is skipped while anything is local-only. The offline restore also became genuinely additive (it was overwriting a same-id local run — including replacing a full GPS trace with the empty one a failed track download leaves in the archive). The documented-but-uncalled `iterateAllRuns` was deleted.
- [x] **`clearInProgress` does not await an in-flight `saveInProgress`.** RESOLVED 2026-07-25 ([decisions.md § 312](../architecture/decisions.md)). Reported as a suspected timing window; it reproduced on 32 of 60 iterations, so it is live. The append opens in `writeOnlyAppend` and recreated `in_progress.json` after the delete, offering Resume/Finish for a run already saved — either of which overwrites the completed run's file with the ~10-point partial. `clearInProgress` now drains the in-flight append first.
- [x] **`LocalRunStore` / `LocalRouteStore` / `WatchIngestQueue` never sweep `.tmp` orphans.** RESOLVED 2026-07-25 ([decisions.md § 313](../architecture/decisions.md)). The `OfflineSyncStore` sweep moved into `core_models/atomic_io.dart` next to the writer that creates the orphans, and all four cold-load paths now call it. The one-hour age gate stays — it is what keeps the sweep off a concurrent writer's in-flight temp file.
- [x] **A watch payload with no `id` decodes cleanly but can never upload.** RESOLVED 2026-07-25 ([decisions.md § 301](../architecture/decisions.md), amended). `raw['id'] as String? ?? ''` put a permanent failure on the transient side of § 301's split, so the entry retried on every sign-in forever. A missing or blank id is now a parse failure and lands in the existing quarantine. Neither shipped sender omits `id`, so no live sender changes behaviour. `setLastKnownOwner` also became atomic (a truncated stamp reads as "no owner" and lets the next account adopt the previous user's run), and `watch_ingest_queue.dart` joined the `noBareWriteStores` guard.

### Open follow-up from this round

- [x] **One shaper for Run → raw `runs` row.** Closed 2026-08-18 ([decisions § 653](../architecture/decisions.md)). `core_models`' `runRowFromRun` is the only place a `Run` becomes a `runs` row; `saveRun`, `saveRunsBatch`, `updateRunFields` and `BackupService.rawRunRowForBackup` all call it and the private copies are deleted. **The drift was already real**: `20270302_001` promoted elevation and the wire writers learned to fill `elevation_gain_m`, the archive copy never did, so a restored device-only run banked no vert on the challenge board. The anti-drift guard derives the promoted-column set by parsing `RunRow`'s generated constants against `MetadataKeys`, so promoting a metadata key into a same-named column fails a test until someone teaches the shaper the lift.
- [x] **`WatchIngestQueue` writes queue files non-atomically and retries an unparseable entry forever.** RESOLVED 2026-07-25 ([decisions.md § 301](../architecture/decisions.md)). `enqueue` now uses `writeStringAtomic`, and `drain` splits the parse class from the upload class: a decode or `runFromWatchPayload` failure quarantines the entry as `<uuid>.json.rejected` (out of the `.json` glob, so no retry loop and no phantom `pendingCount`), while an upload failure still retries. Rejected entries are swept at init past 30 days — renamed rather than deleted so a decoder bug cannot silently destroy a real run. Five new on-disk tests including both failure classes.
- [x] **`clear()` and `rewriteAll()` only delete `*.json`, so a `<id>.json.N.tmp` orphan survives sign-out.** RESOLVED 2026-07-25. `clear()` now deletes EVERY file in the store's directory rather than filtering by name — the store owns the directory and the contract is "nothing of the prior user survives", which a filename filter can only approximate. Orphans are also swept at cold load, behind a one-hour age gate so the sweep can't delete the temp file of a genuinely concurrent writer (the background-sync isolate holds its own instance over the same directory) — that is a safer home for the sweep than `rewriteAll`, which runs while writes are in flight. Guarded by a planted orphan in `offline_store_wipe_test.dart` plus a stale-vs-fresh pair in `local_gym_store_test.dart`.
- [x] **`generateNewIds` is not plumbed into the gym/food restore.** RESOLVED 2026-07-25. The flag now reaches `restoreFromBackup`, which re-mints the record's id before parsing it. Re-keying is generic — every subclass keeps its id at `row['id']` inside the same `{row, sync_state, last_modified_at}` envelope — and a record whose shape exposes no id is **refused with a log rather than queued**, which is the decision the item asked for: a foreign id becomes a `pendingCreate` whose INSERT can never succeed, and the swallowed failure means `hasPending` never clears and every refresh re-runs the drain. Pinned by two backup round-trip tests (a foreign archive lands under fresh ids; the same archive restores twice side by side).
- [x] **`saveInProgress`'s waypoint cursor is read before an `await` and written after.** RESOLVED 2026-07-25. An in-flight guard drops an overlapping call rather than queuing it: the cursor has not advanced, so the skipped waypoints go out with the next tick and nothing is lost. Pinned two ways — overlapping saves append each waypoint exactly once (confirmed failing without the guard), and a dropped save's backlog still reaches disk on the following tick.
- [x] **`gym_adherence` collapses two planned refs for the same exercise onto one logged set.** RESOLVED 2026-07-25 ([decisions.md § 304](../architecture/decisions.md)). Matching moved from the per-block `setIndex` to an explicit `stepIndex` — the ref's ordinal position in the expanded step list — on both twins, both runners, and the persisted `gym_step_results` row (which `gym_programming.md` had always specced as carrying `step_index`; only the implementation omitted it). An ordinal also survives a skipped set, which a consume-in-order match would not. `setIndex` stays on the ref for display. Mirror test each (27 apiece), confirmed failing under the old key.
- [x] **`computeElevationGain` has two different algorithms across the twin, and each suite pins the opposite answer.** RESOLVED 2026-07-25 ([decisions.md § 305](../architecture/decisions.md)). One contract on both platforms: gain over the RAW track, a dropout carries the last reading rather than breaking the chain, and a change counts only once it clears a 3 m hysteresis band. Web's `summarizeRouteFromTrack` no longer grades the 2-D-simplified polyline (a straight road over a summit collapsed to its endpoints and saved 50 m of climb as 0), and the third inline copy in `run_detail_screen` now calls the shared helper. `computeElevationLoss` moved into the same module with the same gate — mobile-only, but it had to move or a flat road would report 0 m of climb beside hundreds of metres of descent. `route_simplify` is now a declared parity pair in `CLAUDE.md` and in `shared-library-syncer`, which is what both doc comments had been claiming without enforcement. **Amended same day:** a fourth ungated copy — the live counter in `run_screen._onSnapshot`, which read ~1,200 m on a flat 10K against run-detail's ~38 m — now feeds the Dart-only `ElevationGainAccumulator`, the streaming form of the same gate that `computeElevationGain` itself is now built on.
- [x] **`mileage_trend` weekly back-fill drifts a week on a DST transition, and anchors on the last bucket *with data*.** RESOLVED 2026-07-25. Both halves. `_mondayOf` and `_previousBucketStart` now step days with the year/month/day constructor (the calendar arithmetic `goals.dart` adopted this round), so a transition week's bars keep their real labels and stop vanishing from the chart. The back-fill window now always ENDS at the bucket containing `now` — the card labels that bucket "this week", so an idle runner used to read an eight-day-old total as the current one. Pinned behaviourally in `mileage_trend_test.dart` (verified failing under `TZ=America/New_York`) and at the source in `architecture_guards_test.dart`, since a DST test cannot fail under CI's UTC.
- [x] **`exercise_history` truncates reps before the Epley estimate.** RESOLVED 2026-07-25. The Dart twin now passes `reps` through unmodified, matching the TS twin and the non-truncation contract `gym_prs` declares (`estimatedOneRepMax` already takes a `num`). A 5.5-rep set at 100 kg scores 118.3 on both sides instead of 116.7 on one. Pinned by a mirror test each (14 apiece).
- [x] **`LocalRunStore._readSyncedIdsSidecar` fails open to "everything unsynced".** RESOLVED 2026-07-25 ([decisions.md § 299](../architecture/decisions.md)). A damaged sidecar is now distinguished from an absent one and resolves through a fixed authority order (sidecar → `index.json`'s cached flag → the legacy per-run flag, the last only when no sidecar ever existed). Deliberately **not** the route store's presumed-synced default: for runs a skipped upload can strand the only copy of a recorded run, so they re-upload instead. Fixing it surfaced the ordering bug underneath — the slow-path repair called `_persistSyncedIds()` before `_summaries` was rebuilt, and that prunes against `_summaries`, so every recovered id was dropped and an empty sidecar written; the legacy migration path had therefore never worked. Pinned across five corruption shapes + the no-index and legacy-migration paths.
- [x] **`hydration`'s exercise add-on has no upper clamp.** RESOLVED 2026-07-25 ([decisions.md § 300](../architecture/decisions.md)). The add-on now stops scaling at 240 exercise minutes (1.92 L), so a 12 h ultra no longer asks for ~8.2 L and a 24 h effort ~14 L. Shipped rather than parked because a ceiling is the conservative direction — the unbounded term extrapolated a daily-baseline heuristic far past its range and printed a hyponatremia risk as a goal, and long-effort fluid is already modelled per leg by `fuel_plan`. **The specific cap is a product call the owner can tune**; a per-athlete sweat rate is the better long-term answer. Twin-pinned (11 tests each).

## Antimeridian planar frames outside the parity pairs (2026-08-03)

[decisions.md § 468](../architecture/decisions.md) routed every planar frame in
the five TS↔Dart route helpers named by issue #664 through the shared
`geo` longitude normalisation. Four more sites still differenced two raw
longitudes to produce a planar value. **ALL FOUR RESOLVED 2026-08-03**
([decisions.md § 473](../architecture/decisions.md)), each as its own
behaviour change with its own pinning tests, and — per § 468's bar — with
no existing pinned value moved:

- [x] **`apps/web/src/lib/routes/routing_quality.ts`** — the perpendicular-offset
  frame now takes its deltas through `lonDeltaDeg`. The raw frame read a
  waypoint just across the line as ~40,000 km off the snapped path (a spurious
  deviation warning on every antimeridian route) and a point ON a crossing
  segment as ~87 m off it. Web-only, as surveyed.
- [x] **`apps/web/src/lib/routes/nearest_track_point.ts`** — the spatial grid
  now unwraps longitudes onto the first coordinate's side. Worse than the
  degenerate-column the survey described: the broken frame also made the
  ring-termination bound unsound, so a tap beside the line could return the
  WRONG vertex (a ~430 m same-side decoy over the true ~53 m neighbour across
  the line), pinned by a deterministic test. Web-only, as surveyed.
- [x] **`apps/web/src/lib/routes/route_loop.ts`** — the synthetic-curve delta is
  folded and every emitted longitude wrapped back into range (the loop branch
  too — a radial ring seeded beside the line used to emit lng > 180). The
  survey's "web-only" was WRONG: `route_loop.dart` declares lockstep in its own
  header and carried the same defect — on mobile a debug build crashed outright
  on latlong2's longitude-range assert. Fixed on web + both mobile twins.
- [x] **`RunTrackPreview.svelte`'s 5 m span** — extracted to `isTrackRenderable`
  in `routes/track_projection.ts` (the Dart twin's name and placement beside
  `projectTrack`) and unwrapped on both platforms alongside the Dart
  `isTrackRenderable`. The survey's "benign" was backwards in the one case the
  gate exists for: ~2 m of stationary jitter AT the line read as a 359.99° span
  and PASSED the gate, drawing exactly the meaningless dot it suppresses.

Everything trigonometric is unaffected and needed no work: `sin`/`cos` are
periodic, so haversine distances and great-circle bearings were always right.

## Bug-hunt 2026-08-10 — verified but deferred

Found during the multi-agent bug hunt that landed the fixes on
`fix/bug-hunt-2026-08-10`. Each was reproduced by running the real module;
they are deferred because the correct fix is a design decision, not a
mechanical change. Do not "fix" any of these without deciding the rule first.

- [x] **`turn_cues.ts` suppresses real turns on a densely-sampled route.** Already closed by the 2026-08-18 three-rail `turn_cues` fix ([decisions § 641](../architecture/decisions.md)) — this entry described the same `prev.wp = waypoints[i]` collapse and was never ticked. Verified stale by inspection: that assignment no longer exists in any of the three implementations. Original text:
  `generateTurnCues`' collapse pass advances the surviving vertex onto each
  merged point (`prev.wp = waypoints[i]`) while leaving `prev.cumM` behind, so
  on a corner sampled finer than `mergeWithinM` (15 m) it walks the kept vertex
  *through* the corner and measures the bearing pair across a chord that cuts
  it off. Measured at 5 m spacing: a 90° corner reports 71.6° and fires 20 m
  early; 45° reports 34.2°; **35° and below produce no cue at all**. At 40 m
  spacing the same bend is announced correctly, so cue behaviour depends on
  source-polyline density — and dense is the normal case (`route_save_polyline`
  persists the full OSRM polyline; GPX imports are 1 Hz; a power-hiked climb is
  ~1 m spacing). The live consumer is mobile: `run_screen.dart` builds
  `TurnCueAnnouncer` from `route.waypoints` and speaks these cues mid-run.
  **Deleting the offending line is NOT sufficient** — verified: it fixes the
  on-grid case but a 35° bend at 5 m spacing is still suppressed and an
  off-grid corner still reports 63.4° for a 90° turn, because the distance-based
  collapse discards the corner vertex itself. The real fix is choosing a
  corner-preserving rule (keep-max-turn within a cluster, or RDP — which
  preserves corners by construction — before cue generation) and porting it to
  all three implementations: `turn_cues.ts`, `turn_cues.dart`,
  `apps/custom_watch/core/src/turn_cues.rs`. Existing suites all sample at
  ~2.2 km spacing, so none of them exercises the merge branch at all.
  Route through `/safe-edit`.

- [x] **`roadbook.ts` silently degrades `model: 'effort'` to even pace.**
  RESOLVED 2026-08-13 ([decisions.md § 600](../architecture/decisions.md)).
  `walk()` now grades over an anchored window that accumulates horizontal
  distance until it clears `MIN_SEGMENT_M`, exactly as
  `gradeAdjustedPaceSecPerKm` walks a track; a trailing window that never
  clears it stays flat rather than amplifying the noise the constant guards
  against. All three rails moved together (`roadbook.ts`, `roadbook.dart`,
  `watch_core::roadbook`), 15 tests each, with the direction of the reported
  flip re-verified: at 20 m spacing the gate reads `miss` by 445 s, and at 3 m
  it now reads the same instead of `tight` with 896 s in hand. The page also
  gained the per-leg pace column that makes the model's output legible — the
  absence of which is why this could hide. Original report follows.
  `walk()` zeroes the grade on any segment under `MIN_SEGMENT_M` (5 m) instead
  of accumulating horizontal distance until the segment clears the threshold —
  which is what the sibling `gradeAdjustedPaceSecPerKm` does with the same
  constant. On a densely-sampled course every segment is sub-5 m, so effort
  allocation becomes byte-identical to `'even'` while `hasElevation` and
  `totalGainM` still report the full climb, so nothing signals the degradation.
  Reported flip on a 4 km leg with a 25 % climb, goal 5400 s: at 20 m spacing
  the cut-off reads `tight` with +1043 s in hand; at 3 m spacing the same gate
  reads `miss` by 300 s. Blast radius includes `fuel_plan.ts` (scales
  carbs/fluid by leg duration) and `live_cutoff_eta.ts`. Twins share it:
  `roadbook.dart`, `apps/custom_watch/core/src/roadbook.rs`. The fix is
  mechanical but changes projected arrival times on every existing roadbook, so
  it wants its own change with the crew-sheet numbers re-verified.

- [x] **Mirror the roadbook's per-leg pace column to mobile.** Closed 2026-08-18. `roadbook_screen.dart` renders a `Leg pace` line per checkpoint and carries the same field into share-as-text, in the viewer's km/mi unit. Presented as a plain labelled line rather than reusing the target/cut-off `_verdictChip`: a pace is not a verdict, and colouring a neutral number would dilute the two rows that do mean "look here". The engine was untouched; web's two em-dash guards are reproduced verbatim, so the start row and a zero-length leg read as `—` on both platforms rather than as a confident number. Original text:
  `/routes/[id]/roadbook` gained a "Leg pace" column (and the same field in
  copy-as-text) alongside the § 600 effort fix — the target pace each leg has
  to be run at to hold the goal, which is the only place the effort model's
  output is visible as a pace rather than as a cumulative arrival. Mobile's
  `roadbook_screen.dart` still shows cumulative distance + arrival only. Pure
  render work: `RoadbookLeg` already carries `legDistM`, and the leg's seconds
  are the difference of consecutive `projectedElapsedS`. Web-canonical per
  [decisions § 24](../architecture/decisions.md).

- [x] **Should `backoff` and `dropset` sets also be excluded from progression
  judging?** Decided yes and shipped in lockstep 2026-08-18 ([decisions
  § 680](../architecture/decisions.md)): `gym_progression.ts` /
  `gym_progression.dart` + the iOS twin drop all three deliberately-submaximal
  types before judging, 4 mirror tests each. `gym_adherence` is deliberately
  NOT changed — it grades against the target the routine author wrote for that
  set, and a planned back-off set's target already IS the back-off target. The
  bite is bodyweight work, where `workingSets` has no positive weight to narrow
  on: an assisted set logged after three clean ones stalled the exercise
  permanently. `amrap` / `failure` stay judged; both happen at the working
  weight.

## Bug-hunt round 2, 2026-08-10 — verified but deferred

Found by the second multi-agent sweep (recording engine, Edge Functions, SQL,
Rust firmware, native watch apps), landed alongside the fixes on
`fix/bug-hunt-2026-08-10-r2`. Each was reproduced against the real module or
the live catalog. Deferred because the fix needs a schema decision, a product
call, or a three-way lockstep port — not because it is uncertain.

### Blocks a whole feature

- [x] **`event_pricing` upsert can never succeed — 42P10 on every call.** Already fixed by migration `20270518_001` (#753, [decisions § 580](../architecture/decisions.md)) before this entry was written — one non-partial `unique (event_id, instance_start) nulls not distinct` replaces the two partial indexes, and `setEventPricing` drops its two-branch shape; the entry was simply never ticked. Verified empirically 2026-08-18 rather than taken on trust: on the pre-fix schema both branches raise 42P10, and after the migration all four writes succeed as the organiser through RLS — series create + re-price, per-occurrence create + re-price — with the series row and the override coexisting. The round did close a real gap the original suite left: every assertion ran as the *authorised* caller through `ON CONFLICT`, so nothing pinned that the unique still constrains a plain `INSERT` (the service role and seed both write that table with no conflict target) or that the write policy refuses a non-organiser. Both are now pinned and both were mutation-verified — `event_pricing_upsert_test.sql` 9 -> 12 tests, plus a `data.test.ts` source guard on the conflict target. See the § 580 amendment.
### Wrong data, fix needs a decision

- [x] **Enabling treadmill mode mid-run zeroes the accumulated distance.** Fixed 2026-08-18 ([decisions § 634](../architecture/decisions.md)): the run distance is ONE total handed between sources in both directions, so switching into belt mode carries the GPS metres onto the belt accumulator and `clearTreadmillMode` hands the belt total back. Five treadmill tests re-pinned, including the one that asserted the discard from the other side; new tests cover 60 m GPS + 100 m belt = 160 m, the lap split that used to clamp to 0 m, and the speed-only belt.

### Three-way lockstep port

- [x] **`turn_cues` collapse deletes the corner vertex it exists to preserve.** Fixed 2026-08-18 web-first as a lockstep triple ([decisions § 641](../architecture/decisions.md)) — `turn_cues.ts` / `turn_cues.dart` + iOS twin / `apps/custom_watch/core/src/turn_cues.rs`. The distance-only collapse is replaced by a windowed accumulator: bends are measured on the two segments that TOUCH each pivot (never one spanning a corner), and a turn is the net direction change accumulated within `mergeWithinM` metres of where the turning starts, classified on the sum and reported at the halfway-of-turning vertex. 14 mirror tests each pin the reproduced numbers. The round-1 finding that deleting the offending line is insufficient is now measured rather than asserted: with only that line removed, the rounded-corner and 4 m-sampling cases still split into two cues. **The pair was in neither parity registry** — which is how one bug came to live in three implementations — and is now registered in both.

### Firmware (bench-gated trigger, host-tested logic)

- [x] **Barometric altitude bypassed the `plausible_gps` gate.** Fixed 2026-08-18 ([decisions § 644](../architecture/decisions.md)). The window was receiver-only while the barometric altitude is PREFERRED over it at every altitude-fed site, so the trusted source was the ungated one: a stuck-high I2C burst reduces to -8789.741 m, banked 10,413 m of false descent, survived every later profile thinning, and its ~3,190 hPa reduction could raise a Storm banner. `elevation::plausible_alt` is now source-blind and applied where the altitude is PRODUCED and again at every core entry point, including `record_cadence::track_point` — representability is not plausibility, and the decimetre field alone stored an altitude 3 km below the sea floor. `storm::on_sample` requires the reduction to pass the same ~870-1080 hPa window a pushed QNH does. Six tests, each verified failing against the pre-fix source. **Host-tested, not bench-verified** — Renode's BMP581 model serves the value a scenario scripts, so it models the driver's own belief and can never produce a bus fault. Bench items owed to #597.
- [x] **`FixGate` had no re-anchor escape.** Fixed 2026-08-18 ([decisions § 644](../architecture/decisions.md)). The gate counts a rejection streak and carries the recorder's escape hatch: a run of rejections lasting `GPS_REANCHOR_AFTER_S` AND covering at least `REANCHOR_MIN_REJECTS` (3) fixes adopts the fresh position. Both halves matter — time alone would adopt the first outlier in a throttled GNSS mode where one fix already spans a minute. Pinned by a 40 km drive that was refused 1,800 times out of 1,800 and now re-anchors at t=10 s, plus two guards that a lone outlier and a pair still fail closed. Host-tested.
- [x] **`backyard` read any backward clock step as a corral bell.** Fixed 2026-08-18 ([decisions § 644](../architecture/decisions.md)). A rollover now needs a drop of at least half a bell window, derived from the 1 Hz fold cadence, so the ~1 s retreats a 20 ppm extrapolation makes about seven times over a 100-hour race no longer clear `closed_this_window` and let the real bell double-count; a sub-threshold step also holds `since_bell_s` monotonic so the countdown cannot gain a second back. Separately `BANK_CONSUME_WINDOW_S` moved 300 s to 900 s: it was JUSTIFIED as "the fastest a loop could be run" and SIZED as "how long a walk-in takes", and the ten minutes between the two counted a bell-banked loop a second time. Deliberately not a hard one-count-per-window latch — the bell's bank belongs to the previous window and the next loop's own return must still count. Six existing tests folded the clock sparsely, which the module's contract says never happens; they now roll at 1 Hz through a shared helper. Host-tested.

### Watch apps

- [x] **Wear `drainQueue` had no re-entrancy guard.** Resolved 2026-08-18 ([decisions § 640](../architecture/decisions.md)). `drainMutex` serialises the pass with the backoff check and the queue snapshot inside it and `awaitAuth()` outside it, so the second cold-start drain re-reads a drained queue instead of re-uploading a multi-MB ultra track. The `FileNotFoundException` arm `classifyDrainError` was missing is now unreachable rather than handled. The refresh-token half needed a different fix: `refreshIfExpired` sits outside `drainQueue` and GoTrue *rotates* the token, so serialising would only make the second caller POST later with a spent one — `refreshAccessToken` is single-flighted through the new `SingleFlight.kt`.
- [x] **Wear `LocalRunStore.save`/`remove` read-modify-write outside the DataStore transaction.** Resolved 2026-08-18 ([decisions § 640](../architecture/decisions.md)). All three mutations (`clear()` had the same shape) now reduce against the transaction's own `Preferences` snapshot inside one `edit` lambda, so a concurrent save can no longer resurrect a run the drain just removed.
- [x] **iOS pace look-back spans a pause.** Already fixed when re-checked
  2026-08-18 — `WorkoutManager.sealPaceWindow()` landed in `028bb5e41` and is
  called from both `resume()` and the GPS re-anchor escape, and
  `WorkoutManagerPaceTests` pins the 12-minute aid stop and the withheld pace
  until the resumed window refills. The box was simply never ticked. Verified
  by running the suite on a watchOS 26.4 simulator: 2/2 passing.
- [x] **Both watch apps kept the only copy of an unsynced run's GPS track in the OS-purgeable cache directory.** Resolved 2026-08-18 ([decisions § 638](../architecture/decisions.md)). Wear writes to `filesDir/tracks` via the new `recording/TrackStorage.kt`; watchOS to `Application Support/run_checkpoint` via the new `RunPayloadStorage.swift`, marked `isExcludedFromBackup`. Both carry an existing install's queued payloads across at cold start and both sweep the new home against a keep-set that can never include a live recording. The watchOS **export** moved too, not just the NDJSON — WCSession reads it off disk for the life of the transfer, by which time `reset()` has deleted the NDJSON it was built from. Honesty: Wear posts a queued run whose payload is gone with a null `track_url` rather than promising a sync forever ([§ 639](../architecture/decisions.md)); watchOS reports "GPS track unavailable — synced without it" instead of shipping an empty array indistinguishable from an indoor run.
- [x] **iOS `HKWorkoutSession` failure is swallowed** — already fixed when
  re-checked 2026-08-18: `didFailWithError` drives `handleSessionFailure()`,
  which nils the session and the reading, raises `heartRateUnavailable` (a
  notice on the run screen and the summary) and withholds the partial average
  through `summaryAverageBPM`, leaving GPS, distance and the timers untouched.
  `HealthKitFailureTests` pins all of it — 7/7 passing on a watchOS 26.4
  simulator. The one genuine residue closed in this round: the `error` value
  itself was still dropped, so nothing named the reason; it is now logged like
  `CheckpointStore`'s and `RunPayloadStorage`'s failures.

### Smaller, still real

- [x] **A partial Stripe refund is processed as a full refund.** STALE the day it was filed — already fixed by PR #758 (`refundScopeOfCharge` + the `partially_refunded` transition), repaired in `582fc1b4f`. What was genuinely missing was a guard on the *wiring*: every assertion lived in `lib.test.ts`, which cannot see whether `index.ts` asks the lib anything, so dropping the `scope` argument or hoisting the seat delete above the partial short-circuit would have restored the bug with a green suite. Closed 2026-08-18 by `stripe-events-webhook/wiring.test.ts` (3 source-level guards) plus three `lib.test.ts` cases driving a real-shaped `charge.refunded` envelope, including the instalment sequence.
- [x] **`events-checkout`'s stable idempotency key + now-derived `expires_at`** made every retry a Stripe `idempotency_error`. Fixed 2026-08-18 ([decisions § 632](../architecture/decisions.md)): the key is now the pending `event_orders.id` and `expires_at` derives from that order's persisted `created_at`, so a re-click inside the live hold replays byte-identically and Stripe returns the session already open; a lapsed hold is superseded with its session expired at Stripe first. `reserved_until` is no longer extended on reuse (it must not outlive the session backing it), and the capacity precheck runs only when opening a new hold. Pinned by a byte-identity test over two attempts eight minutes apart, verified failing against the pre-fix source.
- [x] **`export-data` / `/v1/export` truncated every table at PostgREST's 1000-row cap.** Fixed 2026-08-18 ([decisions § 637](../architecture/decisions.md)). Both paths (Deno EF + Go worker) page in 1000-row chunks ordered by each table's primary key, and `manifest.json`'s per-table count is now the database's own total with `complete` / `incomplete` naming any short section. The runs cap was never 5000 either: `limit=5000` was clamped to 1000 too, so a 3000-run runner received 1000 runs. Guard tests on both sides (`paging.test.ts`, `supabase_export_paging_test.go`) plus a migration-parsing build guard that fails when a new exported table has neither an `id` column nor an explicit order — it caught `event_results` while being written.
- [x] **The export builds the whole archive in memory**, so it carries a 5000-run cap and a 50,000-row-per-section ceiling. **Fixed on the Edge-Function rail 2026-08-21** ([decisions § 703](../architecture/decisions.md)). `export-data` now streams: `ZipWriter` targets a chunked tus Storage sink (`/storage/v1/upload/resumable`, 6 MiB chunks) and each section is a page-by-page byte stream, so peak memory is one chunk plus the fetch-concurrency blob window — flat in the size of the history. `MAX_RUNS` and `EXPORT_ROW_CEILING` are **deleted**, not raised. The surviving bound is the platform's 150 s request clock, now an explicit `ExportBudget` (120 s) that every paged walk and blob sweep checks; a section it cuts short is named in `manifest.json`'s `incomplete` and folded into the response's `complete`, and a section opened past the deadline is shed rather than half-read. Fail-closed got *stronger*: tus materialises the object only once the declared length arrives, so any build failure aborts the session and answers 500 with no artifact — where the old single-shot upload could ship a short-but-real file. 33 new Deno tests (a real zip round-tripped across a 64-byte chunk boundary; 120,000 rows through what used to be a 50,000-row ceiling; mid-stream chunk failure; offset mismatch) plus `wiring.test.ts` source guards (no `BlobWriter`, no cap constants, every sweep budgeted, abort before signing, `manifest.json` still last, full personal-data entry set still wired).
- [ ] **Operator: raise the Supabase project-level Storage upload limit.** The `exports` bucket allows 5 GiB (migration `20270602_001`) but Supabase also enforces a project-wide upload size limit (Dashboard → Storage → Settings, 50 MB by default) and the effective ceiling is the **lower** of the two. So until that setting is raised, 50 MB is the export's real bound however much streaming the code does. Not code — a dashboard step per environment, to be done as part of deploying [decisions § 703](../architecture/decisions.md) + [§ 708](../architecture/decisions.md). It now gates **both** rails: since § 708 the Go worker writes its artifact to the same bucket, so this is the single remaining size ceiling on any export. The local stack has the same bound from a different place: `apps/backend/supabase/config.toml` has no `[storage]` block, so the CLI default (50 MiB) applies there too — add `[storage] file_size_limit = "5GiB"` if you need to exercise a large export locally.
- [x] **The Go worker's `/v1/export` buffered the archive into one `bytes.Buffer`**, so the *production* rail kept `MaxRunsPerExport = 5000` + `exportRowCeiling = 50_000` after the EF rail lost both. **Fixed 2026-08-24** ([decisions § 708](../architecture/decisions.md)). Every section now streams: `StreamExportRuns` / `StreamExportRoutes` / `StreamExportPersonalDataTables` hand pages to the builder and drop them, and `WriteCSV` / `WriteGpxZip` / `WriteBackupZip` write into a chunked tus Storage sink (the same 6 MiB wire contract § 703 built). Both constants are **deleted**, not raised. Measured against 500 runs carrying 512 KiB tracks — 250 MiB of archive, a tenth of the deleted cap — peak heap went 521.6 MiB → 4.6 MiB (~11 MiB in production with the sink's fixed chunk); a 120,000-row history peaks at 17.9 MiB and holds ~128 bytes per run. No `io.Pipe`: `archive/zip` writes into an `io.Writer` already, so a pipe would add a goroutine and no overlap. The artifact moved from the 25 MB-capped `runs` bucket to `exports`, without which removing the row caps would have meant nothing. Fail-closed got stronger the same way the EF's did: any read, write, chunk or offset failure aborts the tus session before the signing path, so nothing materialises. The response's `complete` now folds in every section rather than runs alone, agreeing with `manifest.json`. Guards: `wiring_test.go` source greps (no `bytes.Buffer`, no cap constants, no collecting pager, abort before signing, `manifest.json` still last, full entry set wired, artifact in `exports`) plus a real zip across a 64-byte chunk boundary, 120,000 rows, a mid-stream chunk failure leaving no artifact, an offset mismatch, and bytes reaching the sink before the walk ends.
- [x] **The Art 20 export still holds the client's connection open for the whole build.** Fixed on the web rail 2026-08-24 ([decisions § 717](../architecture/decisions.md)). It is a `data_export` job now: `POST /v1/export/jobs` enqueues and answers with a job id, the worker builds off the queue with nothing attached, and `GET /v1/export/jobs/latest` reports the outcome. Two rows per export — the generic `jobs` queue entry plus a `data_export_jobs` state row (migration `20270603_001`), inserted together by `enqueue_data_export` so a state row nothing will build cannot exist. The signed URL is **not stored**: only the object path is, and the status endpoint mints the URL at read time, so its 10-minute clock starts when the subject asks rather than when the worker happened to finish; the web page re-reads at half the URL's own lifetime so the download link on screen cannot expire under someone looking at it. Status is an endpoint rather than an RLS read because a Storage key is not a download — the `exports` bucket carries no `storage.objects` policies at all (§ 703) and only the service role can sign — so `data_export_jobs` ships RLS-on-with-no-policies and no `authenticated` grant. Failure modes closed with it: a redelivered job whose artifact already landed rebuilds nothing (`max_attempts` is 2, not 5 — every attempt past the tus Finish is a whole archive); a build being retried still reads as `running` while a crashed one reads as `stalled` past a window DERIVED from the retry budget and pinned by a test; `expire_stale_export_jobs` flips a `ready` row to `expired` against the same 7-day window that deletes its artifact; and `data_export` takes a 15-minute per-attempt clock, because the generic 5-minute one would have made the queued rail worse than the endpoint it replaces. 19 web unit tests, 13 endpoint tests, 11 handler tests, plus a pgtap suite on the enqueue/dedupe/expiry contract. **The kind-allowlist pgtap grew three rows, two of them overdue**: `club_photo_process` and `safety_sms` had shipped their migration and their handler without ever being added, so the guard had been asserting nothing about them.
- [x] **Move mobile onto the export job rail, then delete `POST /v1/export`.** Closed 2026-08-24 ([decisions § 724](../architecture/decisions.md)). `backup_server_client.dart` speaks the queued rail (`enqueueExport` / `fetchLatestExportJob` / `downloadToFile`) and the Account screen orchestrates it: enqueue, watch with the shared backoff, and — the point of the whole thing — **resume from `GET /v1/export/jobs/latest` on mount**, so an app the OS killed between asking and finishing finds its export waiting with nothing persisted on the device. `BackupService.tryServerBackup` is **deleted** with its silent fall-back: it swallowed every non-200 and quietly built the narrower on-device archive, so a refused Art 20 request reached the subject as a smaller file nobody told them was smaller. Every refusal is surfaced now, and a 429 names its own retry window. The two archives became two tiles because they are two archives — "Account export" (server, complete) and "Full backup" (on-device: runs, routes, profile, prefs, gym, food, but **not** the account-record set), with a persistent notice under the second once it has produced one so the runner can tell which they got, and a standing notice under the first when undrained runs mean the server cannot see everything. The signed URL is minted at the Download tap rather than held on the card, so its ten minutes start when the runner acts. New parity pair `cloud_export_helpers` ↔ `export_job.dart` registered in both registries. `POST /v1/export` is gone from the Go worker, from web's `cloudExport`, and from the docs; its endpoint tests split into the queued rail's gates (four of which the rail had never had) and direct `BuildArtifact` tests, plus a `wiring_test.go` guard that it cannot come back.

- [x] **Drive the queued export rail end to end in CI.** Closed 2026-08-24 ([decisions § 724](../architecture/decisions.md)). The shape this entry named is the shape that shipped: `apps/web/tests-e2e/playwright.exporthub.config.ts` boots the real Go worker (`tests-e2e/scripts/start-exporthub.sh`) plus a dev server with `PUBLIC_EXPORT_HUB_URL` set, and `tests-e2e/settings/export_job.spec.ts` — `testIgnore`d by the sharded config so it never runs without the worker — drives the real enqueue, the worker's own claim off the `jobs` queue, the tus upload into the `exports` bucket, and the read-time signed URL, then fetches the artifact and checks its zip magic bytes. The reload-mid-build resume is a stage of that same journey rather than a second enqueue (each one spends a quota token, and the lane signs in as the PRO seed user so a CI retry has headroom); a second test asserts `POST /v1/export` answers 404 **against the running binary**, because a deleted route is worth checking where it was mounted rather than only where it was called. The lane runs auth **on**, unlike the live-hub lane which disables it, and `start-exporthub.sh` verifies the JWT signing secret it resolved by re-deriving the local stack's own anon-key signature from it before booting — a wrong secret would otherwise land as a 401 in the middle of a spec. CI job `e2e-web-exporthub` (unsharded, in the gate's `needs:`); locally `pnpm test:e2e:exporthub`.
- [x] **Neither web nor mobile surfaced a truncated export.** Fixed 2026-08-18 ([decisions § 643](../architecture/decisions.md)). Both clients gate on an explicit `complete: false` and disclose persistently rather than only in a toast — web swaps the success toast for "Export ready, but partial — N runs of M" and leaves a notice under the export buttons; mobile banners after the share sheet closes and keeps a warning row under the Full backup tile.
- [x] **The mobile LOCAL backup writer was silently capped at 1000 runs.** Fixed 2026-08-18 ([decisions § 668](../architecture/decisions.md)). `ApiClient.fetchRunRowsRaw` (and `createBackup`'s routes read) page through `readAllPages` with an explicit total order, and carry **no ceiling** — the server's 5000 exists because it buffers the whole zip, which the streaming mobile writer does not (~15 MB peak at 5,000 runs, flat in run count), and a local cap would leave the deepest histories with no complete path at all since mobile skips the server export past that ceiling. With the row reads exhaustive the only remaining shortfall is a swallowed per-blob download, so `writeBackupZipStreaming` returns a `LocalArchiveSummary` and the manifest carries `complete` / `incomplete` in the Go writer's own vocabulary; the settings screen banners it and keeps a notice under the Full backup tile, and restore reads it back into `RestoreResult.archiveIncomplete` with a notice under the Restore tile. Only an explicit `complete: false` claims a shortfall. **A data-loss path was found and fixed in the same pass**: the online restore assigned `track_url = null` whenever the archive carried no track, so restoring a track-short archive into the account it came from orphaned the Storage object and cost the run its trace — the column is now omitted from the upsert instead, which also preserves the § 116 rule that the archived `hr_series_url` must never survive as-is. 14 new tests, including one that genuinely crosses the 1000-row boundary at 1500.
- [x] **`search_public_events` derived `p_byday` in the session timezone** while the sibling `p_time` used the event's. Fixed 2026-08-18 by migration `20270527_002`: the one-off weekday branch goes through `at time zone coalesce(e.timezone, 'UTC')`. Pinned by `search_public_events_byday_timezone_test.sql` (5 assertions straddling UTC in both directions, plus a non-UTC session timezone), verified 5/5 failing pre-migration.
- [x] **`clubs_member_count_trigger` double-counts** a combined `status` + `club_id` UPDATE. Fixed 2026-08-18 by migration `20270526_001` ([decisions § 633](../architecture/decisions.md)): both count triggers now recompute the affected parent from the authoritative query instead of applying deltas. `clubs_member_count_test.sql` grew 6 -> 12 assertions covering move-only, move-and-approve and move-and-demote, each combined case on its own club — a double increment and a double decrement on one club cancel out and let the broken trigger pass. `derived_state.md`'s claim that the suite "guards every branch" was false and is corrected.
- [x] **`routes_run_count_trigger` skips the decrement when the route's visibility changed**, permanently overcounting. Fixed 2026-08-18 by the same migration. Root cause: the OLD-side gate re-evaluated `is_route_visible_to` at trigger time, reading the route's *current* visibility rather than the visibility in force at increment — a fact about the past that no delta form can recover. The new `routes_run_count_test.sql` (8 assertions) is this cache's first pgtap and supplies the "Pinned by" line it never had.
- [x] **Recorder `dispose()` is not terminal** the way `stop()` is. Fixed 2026-08-18 ([decisions § 636](../architecture/decisions.md)): a latched `_disposed` flag blocks `_openPositionStream` outright and `prepare()` after `dispose()` throws a `StateError`, so a retry callback parked on its async precheck can no longer resume into a subscription nothing can cancel. Pinned with a completer-gated fake platform.
- [x] **`resumeSession` resets the monotonic route floor**. Fixed 2026-08-18 ([decisions § 635](../architecture/decisions.md)): `_seedResumeState` rebuilds the floor by replaying the seeded track through the same closest-segment search, within a fixed projection budget so a multi-day track against a dense route is not a burst of the whole run's route maths on the UI isolate. Pinned by an out-and-back that read 699.2 m where a fresh run reads 299.7 m.

## Bug-hunt round 3, 2026-08-10 — verified but deferred

Two hunters (Dart data layer, production Lambdas). Fixes landed on
`fix/bug-hunt-2026-08-10-r3`; these are the verified remainder.

**Observability — a real outage currently pages nobody, and geography pages
everyone.** These are the inverse of the share-id bug fixed in this branch,
and worth doing together as one alarm-hygiene pass:

- [x] `share_session_lookup` / `share_workout_lookup` now inspect `error` on the
  parent AND child queries and log `[share-<x>] upstream_unreachable`; `session`
  + `workout` are registered in `share_log_groups` in `alarms.tf`, which drives
  both the metric filter and the alarm. `share_upstream_alarm_guard.test.ts`
  derives the surface set from the lookup files themselves and fails when either
  half is missing, so the next share surface cannot ship unalarmed.
- [x] `osrm-proxy` returns `422 no_route_for_coordinates` on an OSRM 4xx (the
  engine ANSWERED and rejected the coordinates) and reserves 502 — the only
  status the Lambda's `engine_unreachable` line is gated on — for a 5xx, timeout,
  or connect failure.
- [x] `generate-route` split FOUR outcomes, not the three filed: graph-cycle
  loop-poor with no fallback, every seed raising `no_route`, and a declined
  candidate set are all 422; only a transport failure is 502. The graph-cycle
  branch now tracks whether the call THREW rather than inferring geography from a
  missing fallback engine — a sidecar outage and a cul-de-sac had been one signal.
- [x] `lambda/share-entity` decodes a path segment through `decodeKey`, so a
  malformed percent-escape takes the branded `noindex` 404 like any other entity
  we cannot find, instead of a 503 that tells crawlers to retry.

**Mobile data layer** (all `packages/api_client/lib/src/api_client.dart`) — CLOSED 2026-08-18, [decisions § 646](../architecture/decisions.md)

- [x] **Five reads whose contract is "every row" truncated silently at 1000.**
  `fetchRunRowsRaw` is consumed by `backup.dart` as the COMPLETE history, so a
  1,400-run account archived 1,000 and the restore lost the other 400 with no
  error on either side of the round trip. Fixed by `readAllPages`
  (`paged_read.dart`), wired into `fetchRunRowsRaw`, `fetchCheckpointCrossings`,
  `fetchEventAttendees`, `getRouteReviews`, `fetchExerciseCatalogue` and the
  `routes` read inside `createBackup` (same bug, same function). Each gained a
  unique tiebreaker order — a range over an ambiguous order duplicates or drops
  the page-boundary row. The "local backup writer capped at 1000 runs" carried
  into this round **was this cap and not a separate policy**, so the local path is
  now genuinely complete and correctly still makes no completeness claim. 13 tests.
- [x] **`_hydratePeopleSuggestions` counted public runs off a relation RLS
  empties.** Every People suggestion read "0 public runs" and `comparePeopleRank`'s
  PRIMARY sort key had been a constant since `20260701_001`. Now reads the
  SECURITY DEFINER `public_run_counts` RPC. Guarded as source rather than wire
  because the failure is static — the old query is well-formed and succeeds; the
  guard also parses the migration and pins the RPC name, parameter name and
  returned columns against what the client unpacks. 5 tests.
- [x] **Six writes stamped local wall-clock into a `timestamptz`.** The four filed
  plus `markCompleted` and `setSkipped` in `training_service.dart`, which had
  diverged from the identical `completed_at` write `api_client` already had right.
  Two jsonb `metadata.imported_at` writes went with them. The tree-wide guard
  allowlists by exact source line the four occurrences that are genuinely not
  instants, so a new wall-clock write even inside an allowlisted file still fails.

## People-search hydrate exceeds the project's own `in`-filter chunk (2026-08-18) — CLOSED

- [x] Closed 2026-08-18 ([decisions § 652](../architecture/decisions.md)). Fixed by chunking at the shared consumer rather than by lowering the search cap — a cap would have fixed one of `_hydratePeopleSuggestions`' two callers and left the unbounded one (`fetchSuggestedPeople`, whose id list is the co-member set of every club the viewer belongs to) untouched. The sweep found **17** `.inFilter` sites in `packages/api_client` that can outgrow `kInFilterChunk`, one of which exceeds it in production today: `fetchFollowing`, which `feed_screen.dart` already calls with `limit: 200`. The run and lift feeds were handing PostgREST the viewer's entire followee set, where web has chunked since `feed_merge.ts`. `test/in_filter_bound_test.dart` fails a new unchunked `.inFilter` unless the line is allowlisted **with its occurrence count** (so a second unbounded read cannot hide behind an identically-written bounded one), and separately asserts the four `limit` defaults that allowlist leans on stay at or under the chunk.

### New, surfaced by the same sweep

- [x] **A partially-failed import push is still silent.** Closed 2026-08-18 ([decisions § 659](../architecture/decisions.md)) together with the seam item below — they were one problem. `saveRunsBatch` signals a partial failure by *returning* the ids whose track upload failed, not by throwing, and both `_saveImportedRuns` and `_backfillHealthConnectTracks` set the deferred flag only from their catch, so a half-landed batch reported a clean import. Both now count the returned set as well as the throw, in one commit so they cannot diverge, and the disclosure carries the number: `importStatusCloudPushDeferred` became an ICU plural, because "some are pending" makes 3-of-400 indistinguishable from 400-of-400. Still that one key and no sibling — its claim (on this device, upload did not go through, SyncService retries) is true verbatim for a partial failure. Deliberately **not** routed into `ImportFailureReport`: those entries name an activity that did **not** import and exist to decide whether re-running is worth it, whereas these runs did import and retry on their own — and the batch returns only ids, so an entry would carry a synthesised reason and an empty detail in a report whose whole purpose is saying why.
- [x] **`ImportScreen` has no seam for the Health Connect route path.** Closed 2026-08-18 with the item above. The four Health Connect platform calls (`requestPermission`, `fetchWorkouts`, `requestHealthRoutePermission`, `fetchRoutes`) became nullable `...Fn` constructor seams defaulting to the real statics — the pattern `run_heatmap_screen` / `route_builder_screen` / `settings_about_screen` already use, with no test-mode flag and no production branch. `ApiClient` needed no new seam: subclass-and-override is what `sync_service_test.dart` has always used, so one fake covers both the route path and the batch-push disclosure. `import_route_backfill_deferred_test.dart` and its three source assertions are **deleted**, replaced by six behavioural tests in `import_cloud_push_deferred_test.dart` that drive the real widget on the host with no Android device. Verified: 6 tests on each twin, `diff -r` clean across `lib/` + `test/`, and both partial-failure tests confirmed failing against the pre-fix accounting while the other four stayed green.
- [x] **`NutritionScreen` never inits its template / recipe stores.** Fixed 2026-08-18 ([decisions § 660](../architecture/decisions.md)). `initState` called `loadAll()` without `init()`, so `OfflineSyncStore.dir` was null and `persist()`'s `dir!` threw on every "Save as meal" / "Save as recipe" — the row landed in memory and looked saved while nothing reached disk or the server, and the widget test that printed the error passed because it asserted on that row. The screen now `init()`s both stores; `OfflineSyncStore.requireInitialised` raises a named `StateError` from `persist` / `dropRow` **before** any in-memory mutation, so an undurable write leaves nothing behind claiming success; and a new `architecture_guards_test.dart` group fails any screen that constructs one of these stores without `init()`ing it. `nutrition_screen` was the only offender — every other owning screen already called `init()`. The pinning widget test polls the file on disk. Still open in the same shape but deliberately out of scope: `rewriteAll()` returns silently on a null `dir`, which only drops a server-to-local cache fill rather than a user write.
- [x] **Wear OS activity words drift from web and mobile.** Translation half closed 2026-08-24 ([decisions § 713](../architecture/decisions.md)); the `hike` rename half is now a decision-ready question of its own, below. The "is it deliberate for the chip?" question was settled by measuring the chip rather than by argument: `CompactChip` puts 12 dp of content padding on each side, so `widthIn(max = 56.dp)` leaves the label **32 dp**, and at `caption3` (10 sp Roboto Medium, 0.1 sp tracking, not overridden by `DuskTypography`) the wrist already ships `Radfahren` 47 dp, `Poussette` 46, `Caminhada` 52, `ベビーカー` 50 and `Kinderwagen` 60 — while the *abbreviations* miss the box too (`Caminar` 38, `Course` 32, `ウォーク` and `サイクル` 40). Shortening bought a fourth vocabulary and no fit, so the phone's words won; the chip ellipsises and the `contentDescription` carries the whole word. **Six** labels changed, not the four filed: `de` run Lauf→Laufen, `es` run Correr→Carrera, `es` walk Caminar→Caminata, `ja` run/walk/cycle ラン→ランニング / ウォーク→ウォーキング / サイクル→サイクリング — the last three being the same within-locale disagreements § 547 resolved on the other platforms, which the entry missed. `fr` and `pt-BR` needed nothing. `ActivityTypeVocabularyTest` now asserts every wrist label equals the ARB's **and** the web catalogue's, locale for locale, so a fifth vocabulary cannot grow back.
- [x] **`deleteNotifications` is now N statements, not one.** Closed 2026-08-18 ([decisions § 667](../architecture/decisions.md)). Migration `20270529_001` adds `delete_notifications(p_ids uuid[])` and both clients call it instead of chunking: the array rides the RPC's POST body, which carries none of the request-line bound that forced the split, so one dismiss is one statement in one transaction. SECURITY INVOKER with **no** owner predicate of its own — the `notifications` DELETE policy is already exactly `auth.uid() = user_id` with no public-row leg, so a hand-written filter would only be a second copy to drift; `anon` is revoked alongside `public`, which the pgTAP grant assertion caught (Supabase's default privileges hand every new public function to `anon`). Past **1000 ids** the call raises `22023` and deletes no prefix, because a silent cap would re-open the same class of bug in a new place. pgTAP `delete_notifications_rpc_test.sql` (14 tests) covers a 150-id dismiss, the cross-user RLS case, the empty/null no-op, the cap refusal, and all-or-nothing under a row trigger that fails mid-batch — that last assertion was confirmed to discriminate by running the same fixture against a swallow-and-continue loop, which returns a success count and leaves a prefix deleted. **Correction surfaced while closing this:** § 653 says an over-long `in` filter returns an empty result rather than an error; measured against the local stack the DELETE is refused with a **414** past roughly 200 ids. Either failure loses the whole dismiss, but the mechanism is a property of the gateway in front of PostgREST, not of the code — the new comments and `api_database.md` say that rather than restating either as fact.
## Owner call — does the wrist take the `hike` -> "Trail run" rename? (2026-08-24) — CLOSED

Split out of the Wear-OS vocabulary entry above, whose translation half is
closed. This half is a product decision, not a translation fix, and it is the
only reason the wrist's activity vocabulary is not identical to the phone's.
Facts first, so it can be decided in one read.

- [x] **Decide: align the wrist's `hike` label with web and mobile, or record the wrist as deliberately different.** CLOSED 2026-09-04 ([decisions § 1155](../architecture/decisions.md)). Aligned, on the filing's own recommendation and its reasoning: the rename was made on recorded user evidence about the CONCEPT, not about the screen it is read on, and one product showing two names for one activity is the failure § 547 exists to prevent. **The filing's enumeration was one locale short and that mattered** — it listed six wrist labels for seven locales and concluded pt-BR alone already matched. European Portuguese was the omitted one: it said `Trilho` against the phone's and web's `Trail`, so it diverged too, and pt-BR's `Trilha` is the entry that already matched. Six values changed, not five: `Trail run` / `Traillauf` / `Trail` (es, fr, pt-PT) / `トレイルラン`. Acting on the filing's count would have left one locale behind with the guard's exemption already deleted and nothing left to catch it. Display-only exactly as the filing established — no migration, no regenerated types, no wire change. `renameAwaitingOwner` and the test that kept it non-stale are DELETED rather than emptied, so the vocabulary is now compared with nothing exempt; the deletion was confirmed to discriminate by reverting the English label and watching `ActivityTypeVocabularyTest` fail. `parity.md` and `apps/watch_wear/CLAUDE.md` updated. Verified: `./gradlew assembleDebug testDebugUnitTest --no-daemon` from `apps/watch_wear/android`, 733 tests, BUILD SUCCESSFUL.
  - Original filing follows.
  - **Who says what today.** Web + mobile: `Trail run` (en) / `Traillauf` (de) / `Trail` (es, fr, pt) / `トレイルラン` (ja) / `Trilha` (pt-BR). Wear OS: `Hike` / `Wandern` / `Senderismo` / `Rando` / `ハイク` / `Trilha` — so **pt-BR already matches** and the other five do not. Apple Watch and the Garmin data field have no activity picker at all (watchOS hardcodes `"run"`), so neither is affected. `apps/custom_watch` has a `Profile` ladder reading `RUN` / `TRAIL` / `ULTRA` / `HIKE`, but that is the firmware's GNSS/recording profile, a different value domain from `runs.activity_type` — do not sweep it in with this.
  - **Where the strings live.** Web `apps/web/src/lib/i18n/locales/*.ts`, key `activityType.hike`. Mobile `apps/mobile_android/lib/l10n/app_*.arb`, key `activityTypeHike` (plus the byte-identical iOS twin). Wrist `apps/watch_wear/android/app/src/main/res/values*/strings.xml`, key `activity_hike`. One key and one resolver per platform.
  - **The rename is display-only — confirmed, explicitly.** The stored identifier is untouched everywhere. `'hike'` is still a member of `runs_activity_type_check` (`20261207_001`) and of `challenges.activity_type`'s CHECK (`20270209_001`); still the token in the watch→phone/Supabase run payload and in `UNIVERSAL_ACTIVITY_TYPES`; still the pre-run chip's cycle-order entry, the `RunCalories` / `calories.dart` coefficient key, a `pace_segments` `ActivityKind` member, and a Health Connect exporter case; and still the *ingest* target of the Strava (`strava.ts`, `strava-zip.ts`) and Garmin FIT importers, which map the vendors' own "Hike" / "hiking" onto it. The gear auto-tag trigger reads it only as "not `cycle`, therefore shoe". **A display rename moves none of that.**
  - **Blast radius of aligning the wrist.** Five `<string>` values in five `values*/` files, plus deleting `renameAwaitingOwner` from `ActivityTypeVocabularyTest` — which fails on purpose the moment the wrist matches, so the exemption cannot outlive the decision — plus closing this box and the notes in `parity.md` and `apps/watch_wear/CLAUDE.md`. No migration, no regenerated types, no wire change, no backfill. The only non-mechanical part is fit: `Traillauf` measures 38 dp and `トレイルラン` 61 dp in a 32 dp chip box, longer than what they replace, though `Wandern` (40 dp) and `ハイク` (30 dp) already sit at or over that line and the chip ellipsises with the full word in its `contentDescription` either way.
  - **French belongs to this half, not the translation half.** The original entry filed `Rando` -> `Trail` as translation drift. It is not: `Rando` is the pre-rename French word for hiking (mobile's old `feedActivityHike` said exactly that), so changing it *is* making the rename call for French. It was left alone with the other four.
  - **Recommendation: align the wrist.** The rename was made on recorded user evidence — a user gave "Hike" as the reason trail runners did not see themselves in the picker — and that evidence is about the concept, not about the screen it is read on. A runner who picks "Trail run" on the phone and finds "Hike" on the wrist is looking at one product with two names for one activity, which is the failure § 547 exists to prevent, and the cost of fixing it is five string values. The one real argument against is that a watch picker is used at the trailhead by someone who already knows what they chose, where "Hike" is the plainer word; if that is the call, record it as a deliberate divergence in `parity.md` and `apps/watch_wear/CLAUDE.md` and keep the guard's exemption, rather than leaving it as a silent difference.

## Gear backfill vs the auto-tag trigger's activity mapping (2026-08-13) — CLOSED

Surfaced while porting the post-create gear backfill to web
([decisions § 598](../architecture/decisions.md)), deferred one round because the
fix had to land on both platforms at once, and **closed the same day** as a
matched web + `mobile_android` + `mobile_ios` change in a single commit.

- [x] **A `stroller` run is auto-tagged with the current pair but never offered
  for backfill.** `auto_tag_default_gear` (re-emitted by migration
  `20261207_001`) maps the run's `activity_type` to a gear kind as
  `cycle -> bike, everything else -> shoe`. `gearBackfillCandidates` instead
  enumerated the shoe set as `{run, walk, hike}` — so `stroller`, a real value
  in `runs_activity_type_check`, fell out of the offer while the trigger
  happily stamped a shoe on it. The durable fix was to invert the helper's rule
  to mirror the trigger (`bike` takes `cycle`, every other activity takes
  shoes), which also stops a future foot-powered `activity_type` from silently
  dropping out of backfill the day it is added to the CHECK.

  **Fixed** by deriving the shoe set as "not `cycle`" — the trigger's mapping
  verbatim — on both sides in one commit, with two mirror tests each (the
  `stroller` case, and one pinning that an unrecognised activity is treated as
  foot-powered, which fails against any re-enumerated shoe set even if
  `stroller` is remembered). Verified: 15 tests web, 11 Dart on each twin,
  `diff -r` clean across `lib/` + `test/`, and both new tests confirmed failing
  against the pre-fix predicate on both platforms.

  The related `activity_type` label-vocabulary gap above (line 48 — two partial
  mobile vocabularies, neither covering `stroller`) is the same class and stays
  open; it needs the `activityTypeLabel` consolidation, not a patch here.

## Segment leaderboards vs the §206 shadow-hidden backstop (2026-08-13)

Raised while fixing the segment-rank divergence ([decisions § 594](../architecture/decisions.md),
migration `20270523_001`) and **closed the same day** by migration
`20270524_001` ([decisions § 596](../architecture/decisions.md)). Both entries
below are fixed; the sweep that closed them turned two reported leaks into
five, so they are kept here as the record of what the class looked like.

- [x] **`segment_leaderboard_tiered` served a moderation-hidden route's board.**
  Its route branch was `r.is_public = true or r.user_id = caller or (r.club_id
  is not null and is_club_member(r.club_id))`, where `private.is_route_visible_to`
  additionally demands `shadow_hidden = false` (§206) — so a caller holding a
  segment id on a hidden route read the whole board, after `20270329_001` had
  closed that route's waypoints, photos, reviews, segments and markers. Now
  delegates to `private.is_route_visible_to(s.route_id, caller)`, resolved once
  before the CTE (every effort on a segment shares one route), which removed
  the `routes` join outright.
- [x] **The boards disclosed a shadow-hidden athlete's name + avatar.** Fixed
  on all three: `segment_leaderboard_tiered`, `global_segment_leaderboard`, and
  `challenge_leaderboard`, which the sweep added. A hidden athlete is
  **redacted, not dropped** — the row and rank stand, `display_name` +
  `avatar_url` go null for everyone but themselves — because dropping the row
  would restate every slower athlete's position and would reopen the § 594
  chip-versus-board divergence.
- [x] **Sweep extras: `is_event_visible` + `claim_event_result`.** Both pasted
  the events club-visibility predicate and never received the shadow gate the
  events policy got in `20270328_001`; `is_event_visible` backs the
  `event_pricing` / `event_checkpoints` / `checkpoint_crossings` SELECT
  policies, so a hidden club's pricing, checkpoints and runner crossing times
  stayed readable. Both now delegate to `is_public_club_by_id`.

Left deliberately untouched, with reasoning in § 596: `coach_roster_summary`
(hiding an athlete from their own consented coach is a narrowing that breaks a
legitimate viewer) and `join_club_by_token` (never consults `is_public`;
whether a hidden club still accepts an invite is a product question about
freezing hidden entities, not a leak).

## CI toolchain pins (2026-08-13)

## CI toolchain pins (2026-08-13) — CLOSED

Every toolchain the build installs is now pinned to an exact version, and
`scripts/check_toolchain_pins.mjs` (the `workflow-lint` job) fails CI when any
of them drifts. Rationale and the rejected alternatives: decisions.md § 595.

- [x] **Flutter SDK** — an explicit `FLUTTER_VERSION` in `ci.yml`,
  `release-android.yml` and `release-ios.yml` (8 `flutter-action` steps). A
  workflow-level `env` is per-file, so the three declarations are checked
  against each other; there is no in-repo file that resolves the SDK.
- [x] **melos** — `dart pub global activate melos 7.8.2` at all six sites
  (`ci.yml` ×4, both release workflows), checked against `pubspec.lock` rather
  than against each other, because the lockfile is the source of truth and
  `pub global activate` ignores the workspace entirely.
- [x] **defmt-print** — `cargo install defmt-print --locked --version 1.1.0`
  in the two firmware-sim jobs (a bare version is exact in `cargo install`,
  not a caret), with the exact version now in the `actions/cache` key. The key
  mattered as much as the version: the install is `command -v defmt-print ||
  cargo install ...`, and the old key `defmt-print-1.1-<os>` never changed, so
  a bump would have restored the old binary and the pin would never have run —
  CI run 31623789083 shows the cache hit winning and `cargo install` never
  executing. Checked by self-consistency plus that cache-key coupling, not by
  derivation: defmt-print is a host CLI, not a firmware dependency, so it is
  absent from `apps/custom_watch/Cargo.lock` and nothing in-repo resolves it.
- [x] **`flutter_lints: ^6.0.0` needed no change — the caret is not the pin.**
  The root `pubspec.lock` is COMMITTED (see the rationale in `.gitignore`,
  added after a third-party publish turned main red on 2026-08-05 — the same
  failure class § 595 is about, with a different upstream). All seven Flutter
  packages are members of the one pub workspace, so that single lockfile
  resolves every one of them, and it holds `flutter_lints` at exactly `6.0.0`.
  `melos bootstrap` runs `pub get`, which honours a lockfile; only `pub
  upgrade` moves it. Verified directly: a `flutter pub get` in a fresh
  worktree left `pubspec.lock` unmodified. The caret in `pubspec.yaml` is a
  resolution *constraint*; the lockfile is the pin, and Dependabot's `pub`
  entry means a bump arrives as a reviewable PR rather than silently.

One item remains open, deliberately (the TestFlight pin below was closed 2026-08-24):

- [x] The `node-version: 24` inputs track a major line rather than an exact **CLOSED 2026-09-07** ([decisions § 1502](../architecture/decisions.md)) — the tree has been exact since § 1214; what was open is that nothing read it. `EXACT_NODE` enforces what three comments already claimed, mutation-verified by sweeping every step to a bare major.
  patch. Left as-is: a Node minor/patch break is loud and local to the job
  that runs it, not a silent change to what gets built or shipped.
- [x] `apple-actions/upload-testflight-build@v1` in `release-ios.yml` is
  tag-pinned, not SHA-pinned — the only such reference in the repo. It sits
  inside the commented-out TestFlight block, so it is not live; SHA-pin it
  when that block is uncommented. **Closed 2026-08-24**
  ([decisions § 711](../architecture/decisions.md)) — pinned now rather than
  on the day someone uncomments it. Pinned to **v5.3.0**
  (`5e75ff58276689011512ba87a381d93dc67dbcf8`), not to v1: `@v1` resolves to a
  2020 commit declaring `runs.using: node12`, a runtime GitHub has removed, so
  SHA-pinning the tag verbatim would have frozen a reference that cannot
  execute. v5 takes the same five inputs the step passes and adds only
  optional ones. No repo-wide guard asserted SHA pinning at all — the sweep
  that pinned everything else was a one-time audit — so
  `scripts/check_toolchain_pins.mjs` grew a fourth check that reads **every**
  `uses:` in `.github/workflows` **and** `.github/actions/*/action.yml`,
  **including commented-out lines**, which is precisely why this one drifted;
  it also fails a SHA pin carrying no `# vN` comment, which caught four bare
  `actions/checkout` lines. 150 references, verified green, and verified to
  fail on the pre-fix file.

## Challenges: two things this round deliberately did not ship (2026-08-13)

- [x] **CLOSED 2026-08-19 — the mobile list grew the progress signal, and
  `challenge_list.ts` is a registered parity pair.** `challenge_list.dart`
  mirrors all three exports (`mergeMyProgress` / `myProgressView` / `teamLabel`)
  with a case-for-case suite — 17 Dart against 18 web, the odd one out being the
  unparseable-`starts_at` guard a typed `DateTime` cannot reach — and the pair is
  registered in **both** registries (`CLAUDE.md` + the syncer table), verified
  with `node scripts/check_parity_pair_registry.mjs`. `myProgressView`'s contract
  is mirrored exactly: `SocialService.fetchChallenges` folds
  `my_active_challenges` onto the joined rows (best-effort — a failed enrichment
  leaves `myValue` null), a challenge outside the RPC's live-plus-7-day window
  renders "Progress unavailable — open for your result" instead of a 0 % bar, and
  the one *known* zero is a window that has not opened yet. Two shape differences
  are documented rather than smoothed over: Dart has no object spread, so
  `mergeMyProgress` returns each row paired with its resolved values for
  `ChallengeView.withProgress` to apply, and the structurally-satisfied
  `MyProgressRow` bound is a declared abstract interface.

  **One correction to the original claim.** "It does not carry the web list's
  false-zero bug" was true of the list and false of the screen behind it: the
  detail screen's bar read `c.myValue ?? 0` while `fetchChallengeById` never
  fills `myValue`, so every participant was shown a confident zero. It now reads
  the caller's own row off the board it already holds, like web. Porting
  `teamLabel` closed a second one the entry did not mention — the club-vs-club
  board rendered `_clubNames[id] ?? id`, i.e. a raw uuid for any club RLS did not
  let the viewer read. Both are pinned by widget tests; see
  [decisions.md § 694](../architecture/decisions.md).
- [x] **CLOSED 2026-08-13 — `rankParticipants` brokeaks ties in the opposite
  direction to the SQL when
  the tie-break key is null.** `challenge_leaderboard`'s
  team branch ends
  `order by rank, pt.team_club_id nulls last` (both branches,
  unchanged across all four definitions since `20270210_001`); the TS/Dart/Rust
  helper mapped a
  null `team_club_id` to `''`, which sorts *first*. The null
  bucket is reachable three ways —
  `challenge_participants.team_club_id` is
  nullable, the join RLS policy explicitly permits `team_club_id is null`, and
  the FK is `on delete set
  null`, so deleting a club leaves its members as an
  unaffiliated group the
  aggregate still sums. Fixed on all four rails in
  lockstep (`challenge_progress.ts` + `.dart` + the byte-identical iOS twin +
  `watch_core::challenge_progress`), each with two pinning tests: the nulls-last
  ordering itself, and a guard that a keyless entry still outranks a *lower*
  value, so the next editor cannot "simplify" nulls-last into an unconditional
  last place. Still latent in the sense that `rankParticipants` has no production
  caller
  on any of the three rails (web, mobile, watch) — every board is renders in the
  order the server returned. — so
 is a three-rail parity edit (TS +
  `challenge_progress.dart` + iOS twin + `watch_core::challenge_progress`) and
  belongs in its own changes not rendered output today; it makes not return `set_type` (2026-08-13)

The batched history RPC (migration `20270323_001`) selects seven columns and
`set_type` is not one of them, so every set the helper corogression surfact from history arrives with the
  first caller's warmup
exclusion, which reads exactly that column, is inert on that path. The
judgement no longer *rests* on it: `workingSets` narrows to the session's top

  belongs in its own change, not tacked onto a UI round.

## The gym set-history RPCs did not return `set_type` (2026-08-13) — CLOSED

The batched history RPC (migration `20270323_001`) selected seven columns and
`set_type` was not one of them, so every set the web gym progression surfaces
read from history arrived with the column absent — the prescriber's warmup
exclusion, which reads exactly that column, was inert on that path. The
judgement did not *rest* on it: `workingSets` narrows to the session's top
completed weight, which drops a lighter ramp-up with or without a label
(decisions § 602). But an explicitly-typed warmup logged AT the working weight
still counted on web, where mobile (reading its local store, which carries the
column) excluded it.

- See ADR § 603.

- [x] **Closed 2026-08-13** ([decisions § 605](../architecture/decisions.md)),
  migration `20270525_001`. Scope grew by one on reading the code: the singular
  sibling `gym_exercise_set_history` had the same gap, and
  `fetchGymSetHistoryWithError` was already *selecting* `set_type` from
  PostgREST and dropping it in its row mapping — so all three `GymSetWithDate`
  producers were fixed and the field is required on that interface rather than
  optional. Both RPCs were dropped and recreated (a `returns table` change is
  42P13 under `create or replace`) with their grants re-issued. pgTAP grew to
  6 + 8 tests; `gym/set_history_set_type.test.ts` pins the mapping and the
  warmup-at-working-weight case.

## Mirrors owed from the 2026-08-15 improvement batch

Five parallel `/improve-round` passes landed web-first in one PR ([decisions § 612](../architecture/decisions.md)–§ 616). Each deferred its mobile half deliberately rather than shipping a Dart twin with no caller — a twin with no consumer is dead code the parity guard then polices forever.

- [x] **Mobile mirror: gear rotation next-up.** Shipped 2026-08-18 ([decisions § 649](../architecture/decisions.md)) — `gear_rotation_pick.dart` is a registered parity pair (18 mirror tests each) and `GearRotationsScreen` carries the Next up / Make current affordance over a new `ApiClient.setDefaultGear`; mobile had **no** `is_default` writer at all before this, which is why the star could not follow a rotation on the phone. Original text: Web ships `gear/rotation_pick.ts` + the "Next up / Make current" affordance on each rotation row at `/settings/gear` ([decisions § 613](../architecture/decisions.md)) — the bridge between a rotation and the `is_default` star the `runs`-insert trigger auto-tags with, without which a runner rotating three pairs banks every kilometre on one. Mobile already has the parallel `GearRotationsScreen` (§ 183) but no equivalent, so the mileage-accuracy problem persists on the phone. Port `rotation_pick.ts` to `apps/mobile_android/lib/gear_rotation_pick.dart` (+ iOS twin + mirror suite), register the new pair in **both** the `CLAUDE.md` parity list and `.claude/agents/shared-library-syncer.md` in the same change (`scripts/check_parity_pair_registry.mjs` fails on either-direction drift), and surface it on the rotations screen.
- [x] **Mobile roadbook screen does not render the target verdict.** Shipped 2026-08-18 — `roadbook_screen.dart` renders the resolved target time, signed margin and ahead/on-plan/behind verdict as a chip above the cut-off chip, and carries it into share-as-text. On-plan is neutral rather than a third alert colour. 4 ARB keys x 7 locales, 3 widget tests. Original text: The Dart `roadbook.dart` twin computes `RoadbookLeg.target` in lockstep with web ([decisions § 614](../architecture/decisions.md)), but `apps/mobile_android/lib/screens/roadbook_screen.dart` still shows only the cutoff column. Web-first per § 24, so this is a mirror task, not a gap — the engine half is already done and tested on both sides.
- [x] **Mobile mirror: the challenge leaderboard standing.** Shipped 2026-08-18 ([decisions § 615 amendment](../architecture/decisions.md)). `leaderboard_standing` is the 86th registered parity pair (16 mirror tests each) and `challenge_detail_screen.dart` renders the card above the board, gated exactly as web is. Two things came with the port: mobile had no analogue of `my_team_club_id`, so a club-vs-club standing had nothing correct to key on and would have had to use the derivation § 615 explicitly rules out — `ChallengeView.myTeamClubId` now reads the viewer's own participant row; and the suppression rule proved as load-bearing as the card itself, since rendering whenever a standing exists says "#1 of 1" to the first person who joins. Both are pinned by widget tests. `challenge_list.ts` stays web-only — still no mobile caller.
- [x] **Mobile: mirror the extended-nutrient day roll-up on `nutrition_screen.dart`.** Shipped 2026-08-18. `apps/mobile_android/lib/extended_nutrients.dart` is a faithful twin of the web helper (22 mirror tests each) and `extended_nutrients` is now a registered TS<->Dart parity pair in both registries per § 604. The coverage contract survives the port whole: the "at least" prefix, the withheld `remaining`, the monotone `exceeded`/`reached`, and a nutrient nothing reported omitted rather than zeroed. The port surfaced one thing worth recording — a targeted nutrient whose `remaining` is withheld must draw **no** chip, not the untargeted one, because "we can't tell you how much is left" and "this nutrient has no daily target" are different claims. Two idiomatic shape differences, neither a divergence: TS's structural `keyof` row index reads as a nominal `ExtendedNutrientRow.valueOf(column)`, and web's dotted `MessageKey` as mobile's camelCase ARB identifier resolved at the render layer (the `badges` convention).
- [x] **`comeback.ts` has no Dart twin.** Closed 2026-08-18 — ported together with `self_load` exactly as this entry asked ([decisions § 670](../architecture/decisions.md)); `plan_ramp` was already twinned (§ 655), so the reduction and the `activeWeeks` gate have one home on both platforms. Original entry: Like `self_load.ts` and `plan_ramp.ts` before it, the comeback load card is web-only ([decisions § 612](../architecture/decisions.md)): mobile carries the `coach_load` Dart twin but has no self-facing load surface at all, so a Dart port would be dead code today. If a mobile dashboard ever grows a load card, port `self_load`, `plan_ramp` and `comeback` **together** — they share `recentRunVolume` / `volumeSample` and the `activeWeeks` gate that makes the ratio card and the comeback card mutually exclusive, so porting one without the others would either duplicate the reduction or let a phone show both cards at once.

### Test-hygiene items surfaced by the same batch

- [x] **Flaky: `apps/web/tests-e2e/challenges/list-load-error.spec.ts:43`** Fixed 2026-08-18. The cause was not two competing endpoints — Browse goes through an RPC and never matches the interceptor — but `loadMine` running inside an `$effect` that reads `auth.user`, so it re-runs when auth hydrates and the one-shot `failNext` was consumed by whichever load won. The handler now fails every GET until the retry is armed. ("Retry re-fetches and clears the error on a healthy response") fails ~50 % of runs — its `page.route` handler arms `failNext` once, but `/challenges` issues more than one `GET /rest/v1/challenges` (My challenges + Browse), so which request receives the simulated 500 is a race. Scope the interceptor to the My-challenges request rather than adding a retry.
- [x] **`tests-e2e/dashboard/load-ramp.spec.ts:111` passes vacuously.** Fixed 2026-08-18 — the no-card assertion is anchored on `dash-total-runs`, so the absence only counts once the dashboard has rendered. Its "no card" assertion would also pass on a page that never loaded. The `dash-total-runs` testid added on `/dashboard` in this batch exists to anchor it; the comeback spec already uses that pattern.
- [x] **`RouteMarkerEditor.detailLine` drops a clock-only target.** Fixed 2026-08-18 — the chip prefers elapsed and falls back to `target.clock`, matching the `cutoff` branch above it and the editor's own read-back precedence. Pinned by `tests-e2e/routes/markers.spec.ts`. The mobile twin (`route_markers_panel.dart`) already had the fallback, so this was a web-only divergence. It renders the target chip under `if (target?.elapsedS != null)`, so a marker saved with `target_clock` and no `target_elapsed_s` shows no chip in the marker list — even though the editor can author that form and the roadbook now resolves it correctly ([decisions § 614](../architecture/decisions.md)). A one-line fix plus a test.
- [x] **Playwright reuses a stale dev server.** Fixed 2026-08-18 ([decisions § 631](../architecture/decisions.md)). `globalSetup` probes the server through Vite's `/@fs/` route before signing anyone in; a `403` proves it is rooted in another checkout and fails the run with that reason. `docs/testing/testing.md` now names the connection-refused symptom too. `playwright.config.ts` sets `reuseExistingServer: !CI`, so a long-lived `vite` process on `:7777` from an older checkout serves a bundle that predates a new component — a new-surface spec then fails with no console error while every pre-existing spec passes. Two rounds in this batch lost cycles to it. Worth a harness guard that asserts the served build matches the working tree, and a line in `docs/testing/testing.md`.

## Mirrors and fixes owed from the 2026-08-16 improvement batch

Five parallel `/improve-round` passes landed web-first in one PR ([decisions § 617](../architecture/decisions.md)–§ 621). Each deferred its mobile half deliberately rather than shipping a Dart twin with no caller.

- [x] **Mobile mirror: the gym routine-history panel.** Closed 2026-08-18 ([decisions § 617 amendment](../architecture/decisions.md)). `routine_history` is a registered TS<->Dart parity pair (`apps/mobile_android/lib/routine_history.dart`, 13 mirror tests each) and `routine_detail_screen.dart` carries the author-gated panel over a new `ApiClient.fetchGymRoutineSessions`. Read from the SERVER, not `LocalGymStore` — that store holds only the most recent page of workouts, so a routine last run months ago would have read as never run, and a "you have never done this" claim made from a cache that does not reach back far enough is worse than no panel. The Dart sort carries an input-index tiebreak the TypeScript does not need, because `Array.sort` has been stable since ES2019 and `List.sort` is not. `parity.md` row flipped.
- [x] **Mobile bulk importers report a bare failure count.** Closed 2026-08-18 ([decisions § 650](../architecture/decisions.md)). `import_failures` is a registered parity pair (22 mirror tests each) and `widgets/import_failure_report.dart` renders it behind all three importers. Every swallow was fixed AT ITS CATCH, never reclassified downstream of a discarded error — and the filed counter was the least of it: Health Connect dropped an unmappable workout into a `debugPrint` with **no counter at all**, so those activities never appeared anywhere; its `fetchRoutes` returned exactly the shape a source holding no routes returns, so a failed read was indistinguishable from an absence and the import asserted "these runs have no map" without knowing it; `csv_run_importer.dart` used `catch (_)` and discarded the error object outright; and the shared save loop named a failed run by opaque UUID. One mobile-only disclosure has no web twin — mobile saves locally then batch-pushes, so a push that throws is one deferred upload of runs safely on disk, reported as a status note rather than as N per-item failures. **`_backfillHealthConnectTracks`'s push swallow closed 2026-08-18** — it reports `pushDeferred` through the same `_cloudPushDeferred` flag and the same `importStatusCloudPushDeferred` string, because that string's claim (on this device, upload did not go through, SyncService retries) is true verbatim for the backfill. No new key, no second vocabulary.
- [x] **Mobile mirror: run-detail pacing halves + the grade-adjusted split column.** Shipped 2026-08-18 ([decisions § 648](../architecture/decisions.md)) — `pace_analysis.dart` is a registered parity pair (13 mirror tests each); `run_detail_screen.dart` renders the pacing card above the splits list and a grade-adjusted lane on each split row, both gated on the same 2 s/km margin the key-stat GAP tile uses. `parity.md` row flipped. Original text: Web ships `apps/web/src/lib/runs/pace_analysis.ts` behind the Pacing card and the extra splits column on `/runs/[id]` ([decisions § 619](../architecture/decisions.md)). `run_detail_screen.dart` already computes splits (`_computeSplits`) and whole-run GAP, so the Dart port is additive. Doing it makes `pace_analysis` a registered parity pair — add it to BOTH registries in the same change, and flip the `docs/product/parity.md` row when it lands.
- [x] **Mobile: the club event listings still advertise a cancelled occurrence.** Closed 2026-08-18 ([decisions § 629](../architecture/decisions.md)). `event_occurrence` is now a registered TS<->Dart parity pair carrying `isOccurrenceCancelled` + `nextLiveInstance`; `upcomingCancelledOccurrences` stays web-only because it backs an organiser reinstate picker mobile does not have. `SocialService._enrichEvents` and `fetchNextRsvpedEvent` both resolve through it off a new batched, chunked `fetchCancelledInstancesForEvents` (one `in`-filtered read per list, `gte now`, fail-closed to none-cancelled), and the RSVP fetcher walks 10 candidates like web's `RSVP_CANDIDATE_LIMIT`. `EventView.hasLiveOccurrence` (default `true`) is the non-nullable encoding of web's null `next_instance_start`. **Still open:** those three fetchers have no unit-test reach — `SocialService` resolves its client inline — so only the decision logic they delegate to is pinned. `SocialService._enrichEvents` and `SocialService.fetchNextRsvpedEvent` both resolve an event's next instance with a bare `nextInstanceAfter` and never consult `event_exceptions`, so the mobile club Events tab and the Run-tab `widgets/upcoming_event_card.dart` keep pointing at an occurrence the organiser called off — the leak web closed in [§ 620](../architecture/decisions.md). Mobile already has the primitive (`fetchCancelledInstances`, used today only by `event_detail_screen.dart`), so the fix is to feed it into both fetchers. If the Dart side takes the shaping helpers too, `social/event_occurrence.ts` becomes a registered parity pair.
- [x] **Mirror the spectator stopped-runner readout to mobile.** Shipped 2026-08-18 ([decisions § 647](../architecture/decisions.md)) — `live_motion.dart` ported test-for-test (18/18) with all three fail-closed branches and the `atLeast` floor, registered in both registries; `live_spectator_screen.dart` renders the neutral chip off a 1 h ping buffer stamped from each ping's own `at` clock, not the device clock. 4 widget tests. Original text: `apps/web/src/lib/safety/live_motion.ts` + the `/live/[id]` chip shipped web-only ([decisions § 621](../architecture/decisions.md)); mobile's `live_spectator_screen.dart` renders the same `live_run_pings` stream but has no motion readout, so a runner still pinging from one spot reads there as a plain live dot with the pace row simply absent. Carry all three fail-closed branches and the `atLeast` floor; the screen already holds the `stale` flag it needs from the `live_freshness` twin. Relevant personas: `moab240-spectator`, `ws100-spectator`, `utmb-spectator`.

### Bugs and hygiene surfaced by the same batch

- [x] **`expandInstances` silently drops the first occurrence of a weekly / biweekly series when `starts_at` carries non-zero milliseconds.** Closed 2026-08-18 on web + Dart + the `mobile_ios` twin ([decisions § 628](../architecture/decisions.md)). The not-before-start check compares against the start truncated to the second, the resolution the stamp itself has, rather than against the raw instant. One mirror test each, both verified to fail against the old comparison. Both halves of the registered `recurrence` parity pair stamp each candidate with the start's h/m/s and a **zero** millisecond (web `recurrence.ts` `stamp()`, Dart `recurrence.dart`), then reject it with `stamped < start`. A `starts_at` of `…T02:49:18.132Z` therefore yields a first candidate of `…:18.000`, which loses the comparison, and the series' own first occurrence never appears — in the picker, the listings, or any RSVP key. Reproduced on web (`ms=0` → first 2026-08-23; `ms=132` → first 2026-08-30). Production `starts_at` values come from a `datetime-local` input (whole minutes), so this bites API-written and fixture-written rows rather than user-created ones — but it silently swallows a real occurrence. The fix is a millisecond-tolerant comparison applied to web + Dart + the `mobile_ios` twin, with a mirror test on both sides.
- [x] **`fetchGymRoutineSessions` under-reports past ~500 sessions, now on two platforms.** Closed 2026-08-18 ([decisions § 658](../architecture/decisions.md), migration `20270528_001`). The durable fix, not the mobile-only one the entry warned against: `gym_routine_history(p_routine_id, p_recent_limit)` aggregates server-side — `session_count` / `last_performed_at` / `graded_count` / `completed_count` — and returns an explicitly bounded page of recent sessions (`p_recent_limit`, clamped to `[0, 50]`) in the SAME row, so the five rows the panel lists and the count above them come from one snapshot and one `WHERE`. SECURITY INVOKER with a load-bearing `auth.uid()` filter, because the `gym_workouts` select policy also admits other users' `is_public` rows. Both § 617 exclusions are reproduced in SQL — an in-flight `gym_session_draft` row is dropped from tallies AND page, a "save as is" row counts as a session but stays outside the graded denominator. Days-since-last deliberately stayed client-side (it is floored against the READER's clock and clamped, a display rule not a stored fact), so the `routine_history` parity pair keeps a real job: `summariseRoutineHistory` became `routineHistoryFromAggregate` and `RoutineHistory.sessions` became `recentSessions`, named so no surface can read the page as everything — 16 mirror tests each, web + Dart + the `mobile_ios` twin landed together. pgTAP `gym_routine_history_test.sql` (12 tests) pins a 1200-session routine past the PostgREST cap, both exclusions, and the cross-user RLS case against a deliberately-public row. **Left open deliberately:** no `(user_id, metadata->>'routine_id')` index — the aggregate walks the same `gym_workouts_user` index rows the old query did, so it is not a regression, and a non-concurrent `CREATE INDEX` takes `ACCESS EXCLUSIVE` while `CONCURRENTLY` cannot run in Supabase's wrapped transaction; it belongs in its own change.
- [x] **The displayed "Elapsed" stat on `/live/[id]` froze at the last ping while the page internally knew the true race clock.** Closed 2026-08-24 on web + Dart + the `mobile_ios` twin ([decisions § 712](../architecture/decisions.md)). **The framing was the defect**: one tile was being asked for two quantities, which is why neither answer to "should it advance?" was right. The **race clock** (wall time since the start) and the **runner's timer** (`elapsed_s`, the recorder's stopwatch as of the last fix) are now separate tiles under their own labels on both surfaces. No estimate was needed for the first: `beginLiveBroadcast` pre-creates the `runs` row with the recorder's real `started_at` and `is_public = true`, `public_runs` projects `started_at` with `grant select … to anon`, and **both surfaces were already reading it** (web `visibleRun.started_at`, mobile `run.startedAt`) — for a `cutoff_clock` marker's time of day and the `isFinishedStale` / `runIsFinished` end inference, but never subtracted from now — so the race clock is a subtraction of two values the page already holds. `raceClockS` joined the `live_freshness` parity pair (16 web / 13 Dart mirror tests, the pre-existing 3-test asymmetry unchanged); `liveElapsedS` stays as the fallback and both suites pin that it can only be a **lower bound** on the same clock. Cut-off maths runs on the race clock wherever the start resolves, which also cancels recorder clock skew (a `cutoff_clock` limit is `cutoffClockMin - startClockMin` off that same stamp, so both sides of the comparison shift together). **The gate the entry named no longer exists**: `elapsed` does not exclude auto-paused time, because there has been no auto-pause layer since moving time became a track-derived post-run metric — `run_screen.dart` says so and `RunRecorder.pause()` has exactly one caller, `_toggleManualPause`. So no product call about auto-pause semantics was needed, and labelling the tile "Moving" would have been a fresh lie; it is labelled **Timer**, muted and relabelled "Timer, last fix" under staleness — qualify, not withhold, matching the recent-pace tile and distinct from the projections (cut-off verdict, motion) that withhold because they extrapolate. A terminal run measures its race clock to `concluded_at`; one that concluded before that marker existed **withholds** the tile rather than ticking on. **The watch rail does not share the bug**: `watch_core::record`'s `elapsed_s()` is `now_s - start_s` with `moving_s` a separate accumulator, and `cutoff_snapshot` already feeds the cut-off the race clock — no port owed.
- [x] **Local Playwright is unusable when several agents share the checkout, and the failure is worse than the documented one.** Closed 2026-08-19 ([decisions § 697](../architecture/decisions.md)) — the build-matches-tree assertion § 631 left owed. `/@fs/` only proved the reused server's `fs.allow` admits our path; a server rooted in an ancestor (or with a widened allow list) answers `200` for our file while serving its own copy of everything the app imports. `dev_server_guard.ts` now compares CONTENT through Vite's `?raw` route — the file by absolute path and the file at the same path relative to the server's own root must both match disk — over this tree's own uncommitted `apps/web/src` sources, falling back to a fixed module on a clean tree. A mismatch names the file; anything inconclusive (404 on both routes, a non-Vite server, an unreachable port) still only warns. Pinned by `dev_server_guard.test.ts` (10 cases: foreign root, stale same-path copy, a file the served tree lacks, a differing copy, each inconclusive branch, the git probe-set derivation), which runs under the web unit suite because every Playwright config ignores `tests-e2e/fixtures/**`. The `ERR_CONNECTION_REFUSED` half of the symptom list was already named in `docs/testing/testing.md` by § 631. Original text: `playwright.config.ts` sets `reuseExistingServer: !CI`, so a dev server started from another worktree serves *its* bundle — and when that server dies mid-suite the result is `ERR_CONNECTION_REFUSED`, so unrelated specs fail in ways indistinguishable from real breaks. Three rounds in this batch lost cycles to it. The harness guard should assert the served build matches the working tree, and the symptom list in `docs/testing/testing.md` should name the connection-refused case, not just the stale bundle.

## New, surfaced by round 7 (2026-08-18)

Each of these was found while closing something else and was deliberately left
rather than tacked onto an unrelated change.

- [x] **`hasSessionDraft` disagreed between web and Dart on a JSON array.** Closed 2026-08-18 ([decisions § 662](../architecture/decisions.md)) — one `isJsonObject` predicate on web, matching Dart's `is Map` and the RPC's `jsonb_typeof(...) = 'object'`. It was worse than a latent divergence: a **fourth, looser copy** lived in `fetchGymSessionDraft`, whose PostgREST `.not(..., 'is', null)` filter also matched JSON null and arrays (a jsonb `null` under a key is not SQL NULL), so the row it returned was adopted as the draft and `replaceGymSets` ran over a workout the other two rails counted as performed; that filter is now a prefilter and the helper decides. `stripSessionDraft` separately spread an array bag into an index-keyed object that `saveDraftAsIs` wrote back. Pinned on all three rails, each verified to discriminate by temporarily loosening the other two implementations. **The "unreachable in production" note in the original entry was half wrong**: every app writer emits an object, but `metadata` is unconstrained jsonb and `backup.dart`'s restore passes a user-supplied archive's bag through to the server verbatim, as can any row owner via PostgREST.
- [x] **`OfflineSyncStore.rewriteAll()` still returns silently on a null `dir`.** Closed 2026-08-18 ([decisions § 663](../architecture/decisions.md)). Filed as "one severity down" because it drops a cache fill rather than a user write — true of the data, wrong about the state: all eight `replaceFromServer` overrides rebuild `rowsById` from the fetch *before* calling down, so a silent return left the screen showing rows nothing on disk agreed with. The gate is therefore the literal first statement of every override as well as of `rewriteAll`. **The predicted ripple through ~19 test files did not exist** — all of them already `init()` with an `overrideDirectory`, so no existing test changed and none swallows the new `StateError`. Auditing the callers found one real reach: `gym_screen._initRoutines` set `_routineStoreReady = true` after *catching* a failed `init()`, offering the Routines destination over a directoryless store where every write refuses; it reads `_routineStore.dir` now. New `architecture_guards_test.dart` group asserts `requireInitialised` is the FIRST statement of each override and that at least 8 exist (7 since [§ 707](../architecture/decisions.md) deleted `LocalSessionStore`), so a re-ordering or a new store fails the PR.
- [x] **A failed `markManySynced` still over-reports the deferral.** Closed 2026-08-18 ([decisions § 664](../architecture/decisions.md)). The catch is split, and the second branch gets **no disclosure at all** — verified rather than assumed: `markManySynced` runs only after `saveRunsBatch` returned, so the runs are already on the server with the `failed` set excluded; `LocalRunStore` mutates `_syncedIds` in memory before awaiting the disk write, so a persist failure costs only this device's record, the next cold start re-reads them as unsynced, and the re-push upserts on `id`/`external_id` onto the same rows. `importStatusCloudPushDeferred` claims the upload did not go through, which is false here, so reusing it would have been the wrong reuse and a sibling key would have been inventing a message to fill a branch.
- [x] **The Strava / CSV / backup-ZIP import paths still cannot be driven end-to-end in a widget test.** Closed 2026-08-18 ([decisions § 664](../architecture/decisions.md)). `pickFilesFn` is the fourth `...Fn` constructor seam on `ImportScreen`, defaulting to the real `FilePicker.pickFiles`, backed by 15 behavioural tests covering each path's own parsing and error handling. **It found the data-loss bug it existed to find**: Strava leaves `Filename` empty for every manually-entered or indoor activity, and `_parseStravaZipBytes` `continue`d past those rows before any counter — so a migrating runner's whole treadmill history vanished from an import that reported "0 runs" with no failures and no explanation. Web already imports those rows trackless off the CSV's own numbers (`importOne` guards inside), so this was a parity gap, not a product choice. Fixed at the root; the existing test asserting the drop was named `row with empty filename is silently skipped` — encoding the bug in its own title — and was replaced rather than relaxed. Also fixed: `_importStrava` dereferenced the picked path with a bare `!` where both siblings guarded, showing a null-check crash on a path-less pick.
- [x] **No index on `gym_workouts (user_id, metadata->>'routine_id')`** — measured and **rejected**; no index added ([decisions § 665](../architecture/decisions.md)). The RPC is `security invoker`, so the query runs under RLS, and the planner may not evaluate a non-leakproof qual below an RLS security qual: `jsonb_object_field_text` is not `LEAKPROOF`, so `(metadata ->> 'routine_id') = $1` stays a heap `Filter` and only `user_id` reaches `Index Cond` whatever the index is on. Over 60,000 synthetic rows the existing `gym_workouts_user` index reads 1,278 heap blocks / 1,296 buffers in 1.7 ms; the proposed partial expression index reads 1,277 / 1,300 in 2.5 ms — strictly worse, plus a write cost on every gym workout. A GIN `@>` index is ignored outright (`jsonb_contains` is not leakproof either). The only usable shape is a promoted plain `routine_id uuid` column (154 heap blocks / 158 buffers / 0.37 ms) — a column-vs-bag call, not an index one, and not taken yet since 1.7 ms on a page-open path does not earn a `runs`-style promotion plus its batched backfill. Revisit when a typical account reaches five figures of `gym_workouts`. The general rule is now in [conventions.md § Activity-table column checklist](../architecture/conventions.md).

### Residues from the round-7 fixes (2026-08-18)

- [x] **The mobile gym resume-card scan still tests the draft marker for mere presence.** Fixed 2026-08-18 ([decisions § 681](../architecture/decisions.md)). The three Dart copies of the predicate are now one `hasGymSessionDraft` in `lib/gym_session_draft.dart`, asking the JSON-object question the RPC, web and `routine_history.dart` all ask. "Harmless today" was true of the data and false of the offer: the card advertised "Workout in progress · Resume" for a row the routine history counts as performed, and the resume then landed in the fresh-start fallback. Pinned by a widget case over an array / string / number / null marker (verified failing against the old condition) plus a helper suite; mirrored to the iOS twin.
- [x] **Web's Strava importer is lenient where mobile is now strict.** — DONE 2026-08-18 ([decisions § 676](../architecture/decisions.md)). The decision moved into the pure `integrations/strava_track_member.ts`: a `Filename` naming a member the archive does not hold, or one in a format neither parser reads, throws into the `ImportFailureReport`; an empty `Filename` still imports trackless, and a failed gunzip stays lenient on purpose (the helper cannot tell a corrupt member from a browser with no `DecompressionStream`). Both platforms' messages were re-worded so `classifyImportFailure` buckets them as `unparseable` rather than `unknown`.
- [x] **`LocalSessionStore` has no production consumer.** Closed 2026-08-24 by deleting it ([decisions § 707](../architecture/decisions.md)). It was never wired **and never had been** — `session_detail_screen.dart` carried no import to it in the very commit (`19e377e6`) that added the store and the mobile follow-along runner together, and `sessions_screen.dart` has always read `fetchSessionPlans` from the server; four docs said the runner was "backed by" it anyway, inheriting that commit's own message. Wiring it up was checked against the spec rather than assumed: `session_planner.md` does require an offline runner, but P2 and P3 shipped without it and P4's remainder does not need it, so the offline slice is design of record with nothing pulling on it — and the store as written already lagged the schema it would cache (`createLocal` mints no `club_id`, added by the P3 club-template slice). Deleted whole: the store, `StoredSessionPlan` / `StoredSessionBlock` / `StoredSessionItem`, its suite, its `offline_store_wipe.dart` registration, its row in the crash-atomic-write guard's store list, and the wipe registry test's expectation; the sign-out wipe still covers the remaining four screen-owned stores and the two guards that enumerate stores were moved in lockstep, not relaxed. `ApiClient.createSessionPlan` / `deleteSessionPlan` stay — a typed client's surface is its API, ~10 of its methods already have no mobile caller, and both are covered by the package's integration test. `session_planner.md § Persistence` now says the offline store is unbuilt.
- [x] **The seven `replaceFromServer` overrides repeat their gate line by line.** (Eight until `LocalSessionStore` was deleted, [§ 707](../architecture/decisions.md).) A `replaceResident(build)` base-class template would remove the repetition, but the overrides differ in signature and in how they read `rowsById` while building, so a behaviour-preserving rewrite of seven cache-fill paths is a much larger blast radius than the bug warranted ([§ 663](../architecture/decisions.md)). The architecture guard enforces the duplication rather than leaving it to convention (its floor moved 8 → 7 with the deletion, so it still fails on a re-ordering or a new store), which buys the durability without the risk — **the trigger is the count GROWING**. A store leaving takes the condition further away, not closer: reach for the template when the next one lands, not because one went. **CLOSED 2026-09-05 ([decisions § 1198](../architecture/decisions.md)).** Measured: the seven gates ARE identical -- every body opens with the same 40 characters, `requireInitialised('replaceFromServer');` -- so there is no divergence hiding in the duplication and the template stays refused on § 663's terms (its stated trigger is the count GROWING, and it is still seven). What the measurement DID find is that the guard § 663 leaned on could be evaded three ways, each confirmed by planting the shape in `lib/`: a non-async override sent the `async` anchor forward into an unrelated later method whose first statement was approved in its place (while still counting toward the floor); an override returning anything but `Future<void>` was not found at all; and the gate's argument was never read, so one naming another method passed. The scan now finds a declaration by name and walks the parameter list's own parentheses to the body -- necessary because `local_gym_store.dart`'s record-type signature contains braces, which is what the `async` anchor was standing in for.

### Surfaced by round 8 (2026-08-18)

- [x] **The web backup writer has the same undisclosed per-track swallow mobile just fixed.** — DONE 2026-08-18 ([decisions § 675](../architecture/decisions.md)). `buildBackupZip` returns a `BackupArchive` and stamps `complete` + `incomplete` into `manifest.json` in the Go writer's vocabulary; `/settings/account` discloses the shortfall on download and carries an archive's own `complete: false` through restore. The same pass found web's `createBackup` sharing § 668's actual root cause — unranged `runs` + `routes` selects clamped to 1000 rows — and paged both, and stopped the restore nulling `track_url` / re-homing `hr_series_url` when it uploads no blob.
- [x] **`parity.md`'s whole iOS "Sync and backup" column reads `✗`.** Closed 2026-08-24 ([decisions § 707](../architecture/decisions.md)) — settled at the level the code can settle, without a device. The column could not have been "right": `parity.md` marked "Backup ZIP restore (lossless)" iOS `✓` under Import while marking the same `BackupService` `✗` under Sync and backup. Nothing obstructs iOS on that path — no `Platform` gate on either tile, its handler, the containing section (`runStore != null` is its only condition), the screen, or the screen that pushes it; no method channel; `archive` is pure Dart and `file_picker` / `share_plus` / `path_provider` each declare iOS; `Info.plist` already carries the photo-library keys `file_picker` transitively forces and no entitlement is owed for a document picker or a share sheet; even the background-sync row is wired both ways (`registerProcessingTask` against a declared `BGTaskSchedulerPermittedIdentifiers`). So both `✗` ("not started") and `✓` ("working end-to-end") were false, and all eight rows now read `Partial` with the matrix's own wired-but-unverified notes idiom. `apps/mobile_ios/CLAUDE.md`'s omitted-tiles list is gone — it cited a `settings_screen.dart` comment that no longer exists, and all five tiles it named render on both apps out of the one shared file; `docs/ops/backup_restore.md` no longer calls the contradiction unresolved. The device run itself stays owed, recorded once on the iOS-verification item above rather than per-row.
- [x] **The challenges feature has no rows in `docs/product/parity.md` at all.** Closed 2026-08-18 — seven rows added to the Social section (create, join/leave, goal progress, on-pace hint, individual board, club-vs-club board, completion badge), every cell verified against the surface that renders it. Create is the one gap the exercise found: `SocialService.createChallenge` exists on mobile with **no caller anywhere**, so authoring is web-only and the row says so ([decisions § 686](../architecture/decisions.md)). Create, join, leave, goal progress, the on-pace hint, individual and club-vs-club boards, and the completion badge all ship on web and both mobile twins, and none of them has ever had a matrix row — which is exactly the silent drift the matrix exists to make visible. Surfaced while adding the standing-card row ([decisions § 615 amendment](../architecture/decisions.md)). Add them as a group rather than one at a time; `apps/web/src/routes/challenges/` and `challenge_detail_screen.dart` / `challenges_screen.dart` are the two ends to walk.
- [x] **§ 653's stated `in`-filter failure mode does not reproduce locally.** Closed 2026-08-18 by amending § 653 in place with a measured correction ([decisions § 685](../architecture/decisions.md)): the boundary is the gateway's **8 KiB request-line buffer**, not an id count — 207 uuids (8128 B) answered 200, 209 (8206 B) answered 414 — which also retires the "~15 KB" figure below. `chunk.dart` and `api_client.dart`'s badge-feed comment were corrected with it. **Still owed: nothing has been measured against the prod gateway.** The web call-site comments were corrected 2026-08-24 ([decisions § 707](../architecture/decisions.md)) — three in `core/data.ts`, plus `social/feed_merge.ts`'s header and its `mergeProfilePages` doc, and the `data.test.ts` guard whose own test name asserted the retired form; each now says the read is lost past the request-line budget and that which way the gateway refuses is a property of the deployment. It records that an over-long PostgREST `in` filter "returns an empty result rather than an error"; measured against the local stack while closing [§ 667](../architecture/decisions.md), the DELETE is refused with a **414 URI Too Long** past roughly 200 ids (~15 KB of URL) and answers 204 below it. Both failures lose the whole operation, so no chunking decision changes — but the mechanism is a property of the gateway in front of PostgREST rather than of the code, and § 653's wording is now the only place still asserting the empty-200 form as general. Worth confirming which form prod's gateway produces before any future work leans on either.

## deno.lock workspace-section drift (2026-08-24)

- [x] **Ask whether `deno.lock` needs a `workspace` section at all.** **Answered 2026-08-25 — no, it records one wherever the lock sits inside the npm workspace, so `sync:deno-lock` stays** ([decisions § 730](../architecture/decisions.md)). Closed as a question answered rather than as work done: nothing was built, and the measurement is what justifies keeping the guard instead of an unexamined assumption that it must be kept. `parity-types` now fails on a stale one ([decisions § 706](../architecture/decisions.md)), Dependabot cannot regenerate it, and the auto-merge job only queues on the required checks — so each weekly minor/patch `chore(deps)` PR now waits for someone to run `pnpm sync:deno-lock` and push. That recurring cost is **accepted, not pending**: having CI do the sync and commit onto the bot branch was refused outright in [§ 711](../architecture/decisions.md), and the reasons are stronger than a token scope — a `GITHUB_TOKEN`-authored push starts no workflow run, so `--auto` would wait forever for a `CI gate` context that can never appear, and the durable credential that would fix that carries `contents: write`, which is also what publishes a GitHub Release and so reaches `release-web.yml` / `release-backend.yml` / `release-ios.yml` in production. (The claim that `dependabot-auto-merge.yml` "runs with a read-only token today" was wrong in both directions: it already declares `contents: write`, and that is not what blocks the automation.) § 711's closing question — whether deno records the section at all when the lock does not sit at the workspace root — was the filed experiment, and it was run (deno 2.9.3). **A copy staged at `apps/backend/deno.lock`, checked with the same `deno check --no-remote --node-modules-dir=none` the sync script runs, comes back with all four sections**: `redirects`, `remote`, `version`, `workspace`, the `workspace` half carrying the same 32 specs, the same `members: ["apps/web"]` and the root `overrides` block — byte-for-byte the baseline's shape. Adding a sibling `apps/backend/deno.json` naming `{"lock": "./deno.lock"}` changes nothing beyond a `"lock" field can only be specified in the workspace root deno.json file` warning. The mechanism was established rather than inferred: a third variant removed the root `package.json` — the one declaring `workspaces` — from above the nested lock and left everything else identical, and deno then wrote `["redirects", "remote", "version"]` and no `workspace` section at all. So the section is a function of an npm workspace existing anywhere *above* the lock, not of the lock sitting at its root; relocating within the repo cannot remove it, and only taking the lock out of the npm workspace entirely would — which is not on the table, since the lock's load-bearing half is the Edge Functions' integrity pins the Supabase CLI writes at the repo root. The guard, the weekly chore and the bot problem therefore all stand, on the terms above.

## Surfaced by the #789 mobile runners-nearby build (2026-08-21)

- [ ] **Deploy gate: turn the runners-nearby flag on.** The whole surface now exists on every client — backend reader, web list + area setter, and (as of 2026-08-21) the mobile nearby list + `discoverable_nearby` opt-in + coarse-area setter — and every one of them is inert because the flag defaults off ([decisions § 270](../architecture/decisions.md) + [§ 701](../architecture/decisions.md)). What remains is a **deploy/config step, not code**: obtain owner + CISO/counsel sign-off on exposing person-location (Art 9-adjacent), then set `PUBLIC_ENABLE_NEARBY_RUNNERS` for the web env and pass `--dart-define=ENABLE_NEARBY_RUNNERS=true` to the mobile release builds. Flip web and mobile **together** — a runner who opts in on the phone becomes discoverable to opted-in web viewers immediately, since the gate is client-side render/read only and the eligibility rules live in the SECURITY DEFINER reader. Reviewers: the code path to read is `discoverable_runners_near` in migration `20270424000005` (every filter, coarse bucket output, reciprocity), `apps/web/src/lib/social/nearby_flag.ts`, and `apps/mobile_android/lib/nearby_flag.dart`.
- [x] **`ENABLE_NEARBY_RUNNERS` is the fourth inline copy of the same fail-closed flag parse on mobile.** Closed 2026-08-24 as the matched cross-platform change it had to be ([decisions § 709](../architecture/decisions.md)). `apps/mobile_android/lib/env_flag.dart#isTruthyFlagValue` is the new Dart twin of web's `core/env_flag.ts`, registered as a parity pair in both registries; all four mobile gates and the three web gates that were *also* carrying private copies (`off_route_alert.ts`, `cycle_plan_flag.ts`, `weigh_in_flag.ts` — this item's premise that every web `PUBLIC_*` gate already routed through the canonical parser was itself wrong) now delegate to their platform's canonical parser. `offRouteEscalationEnabled` and `adaptiveFitnessGateEnabled` keep their names and delegate on both sides symmetrically, so neither registered pair diverges or changes test count. `_weighInGate` was the real defect and is fixed: a widget test pins that `WEIGH_IN_GATE=yes` now opens the weigh-in sheet, and it fails against the old parse. Both suites carry a source guard against a ninth copy, and the house rule is in `conventions.md § Feature gates`.
- [x] **Three mobile deploy gates were unreachable in a release build.** Closed 2026-08-24 in the same round ([decisions § 709](../architecture/decisions.md)). § 701 recorded that `OFF_ROUTE_ESCALATION_ENABLED` and `ADAPTIVE_FITNESS_GATE` were absent from `main.dart`'s `String.fromEnvironment` bridge — and since release builds never load `.env.development` ([§ 13](../architecture/decisions.md)), neither could ever have been flipped on however a deploy was configured. Verified, and `WEIGH_IN_GATE` was in the same state; the audit found `USDA_FDC_API_KEY` (so the second food-search source was dead in every shipped build) and `TILE_URL_TEMPLATE` ([§ 68](../architecture/decisions.md)) too. All five are bridged, and `env_flag_test.dart` now scans every `dotenv.env[...]` read under `lib/` rather than asserting one flag at a time — `nearby_flag_test.dart`'s per-flag version passed the entire time the other three were broken. Passing the defines in `release-android.yml` / iOS `dart_defines.json` remains the deploy-time step each flag's own sign-off unlocks; `apps/mobile_android/deployment.md` lists them.
- [x] **The mobile nearby list has no "open Settings" affordance in its empty state.** ~~Web's empty card links to `/settings/preferences`; the mobile empty state names the path in prose instead, because `PeopleScreen` is embedded in `SocialScreen` and holds neither the `Preferences` nor the `SettingsSyncService` that `SettingsPreferencesScreen` requires.~~ Closed 2026-08-24 by building the seam rather than the thread-through: `settings_destination.dart` names the pushable Settings sub-screens and `HomeScreen` drains the parked intent with the dependencies it already holds ([decisions § 710](../architecture/decisions.md)). The nearby empty state now carries an "Open Preferences" button and its body no longer recites the path. A second caller went with it — `IntensityCard`'s age-estimated-max-HR caveat, which showed run-detail's exact copy with nothing to tap because it holds only a `SettingsSyncService`. Two candidates were refused on purpose and the reasons are in the ADR: `run_detail_screen.dart` already holds its own dependencies, and the nutrition screens await the pushed route to refresh on return, which a fire-and-forget intent cannot report.

## Surfaced while settling the iOS backup/restore contradiction (2026-08-24)

- [x] **All nine `Share.shareXFiles` call sites omit `sharePositionOrigin`, and the iOS target includes iPad.** Closed 2026-08-24 ([decisions § 714](../architecture/decisions.md)) — see the section below, which carries the measured facts. Two premises here were wrong: the reach is **21** call sites, not nine (ten `Share.share` text/link shares and two already-modernised `SharePlus.instance.share` calls go through the same `atSource:originRect` check in `FPPSharePlusPlugin.m`), and the pinned `share_plus 12.0.2` does not crash — it returns a `PlatformException` and presents no sheet at all, so the symptom on iPad was a silently dead share, not a crash.
- [x] **`parity.md` contradicts itself about what an iOS cell means.** Closed 2026-08-25 ([decisions § 739](../architecture/decisions.md)). The rule adopted is the one this entry proposed — `Partial` = wired but unverified, `✓` reserved for an observation, `✗` for genuinely absent code — stated once as a **derivation** from the Android cell inside a `<!-- parity-ios-rule -->` block, with the banner and the intentional-asymmetries copies deleted and the column key reduced to a pointer. The whole column was then swept in one pass: 268 of 337 cells moved, leaving 313 `Partial` / 11 `✗` / 13 `N/A` and no `✓`. Five rows depart from the derivation and each names its obstruction behind an `**iOS <symbol>:**` marker. **This entry's counts were right on their date** — 133 / 47 / 140 reproduces exactly on the 2026-08-24 commit — but two `✗` rows landed after it, so the base for this round measured 133 `✓` / 47 `Partial` / 142 `✗` / 15 `N/A` over 337 rows; the entry omitted the `N/A` bucket and the row total. `scripts/check_parity_ios_column.mjs` (21 unit tests) now reads the derivation table out of the document and enforces the column against it, refuses a second statement of the vocabulary anywhere in the prose, and refuses a `✓` outright; it runs in the ungated `parity-matrix` job, because `parity-types` is gated on `needs.changes.outputs.code` and a parity.md edit is a docs-only diff.

## Surfaced by the iPad share-anchor fix (2026-08-24)

- [x] **Every mobile share reached `share_plus` without a `sharePositionOrigin`, and the app ships to iPad.** Closed 2026-08-24 ([decisions § 714](../architecture/decisions.md)). All 21 call sites — nine `Share.shareXFiles`, ten `Share.share`, two `SharePlus.instance.share` — omitted the anchor while `apps/mobile_ios/ios/Runner.xcodeproj` sets `TARGETED_DEVICE_FAMILY = "1,2"` in all three configurations. On the pinned `share_plus` 12.0.2 the iOS plugin does not crash: it detects the empty rect and returns a `FlutterError` whose message is `sharePositionOrigin: argument must be set`, so the share throws a `PlatformException` into the call site's `catch` and **no sheet opens**. All three native rails (`shareUri`, `shareFiles`, `shareText`) pass through the same `atSource:` check, so the text shares were as broken as the file shares. Replaced by one `lib/share_sheet.dart` whose `shareFilesFrom` / `shareTextFrom` take the anchoring `BuildContext` as a required positional parameter — there is no `Rect`-taking overload and no un-anchored path — plus the migration off the deprecated `Share` statics onto `SharePlus.instance.share(ShareParams(...))`, four source guards in `architecture_guards_test.dart`, 6 derivation tests, and the byte-identical iOS mirror.
- [ ] **Device gate: verify the iPad share sheet on real hardware.** Mac- and iPad-only, and nothing above was observed on a device — the fix was derived from the plugin's own `FPPSharePlusPlugin.m`, the Xcode target's device family and the workspace lockfile pin, then host-tested. Owed: on an iPad (and under **Split View / Stage Manager** specifically), confirm each of the four share shapes actually presents — a file share from a modal card sheet (run / route / period / certificate PNG), a file share from a full screen (CSV export, account backup, GPX/KML route export), a text share after a dialog has popped (route detail "share link"), and a text share from a plain button (live link, coach invite, recap). The Stage Manager case is the one with a known residual: `share_plus` compares the anchor against `controller.view.frame` rather than `bounds`, so a correct view-local rect can still fail containment when the Flutter view is not anchored at the screen origin. If it does, the fix is upstream (a plugin issue/PR), not another parameter — do not paper over it with a guessed offset. Nothing ships disabled on this; the current behaviour on iPad is strictly worse than the fixed one.

## Surfaced by running round 12's export-hub e2e lane (2026-08-25)

- [x] **The queued export rail's CORS preflight was refused, so no browser could enqueue** — closed 2026-08-25 ([decisions § 725](../architecture/decisions.md)). `OPTIONS /v1/export/jobs` answered 405 with no allow headers; the web client posts from the site's origin and never the worker's, so the POST never left the browser and the worker logged nothing. Exact-origin allowlist (a wildcard is invalid on a credentialed request), sharing `LIVEHUB_ALLOWED_ORIGINS`.
- [x] **A signed Storage URL was fronted with the project base URL instead of the storage API path**, so every queued-export download answered 404 — closed 2026-08-25 ([decisions § 725](../architecture/decisions.md)). Storage returns `/object/sign/...` with no `/storage/v1` prefix; both shapes are now accepted.
- [ ] **`LIVEHUB_ALLOWED_ORIGINS` must be set in the worker's production environment before the queued export rail is usable.** It already gates the live hub's WebSocket origin check, and now also gates the export endpoints' CORS. Empty means no CORS headers, which is correct for a server-to-server deployment and fail-closed for the browser one — so a deploy that forgets it leaves the export button silently inert. Deploy-time checklist item for `apps/job_worker/deployment.md`, not code.
- [x] **Both boot scripts inherit the workstation's `GOTOOLCHAIN`.** `start-exporthub.sh` and `start-livehub.sh` both run a bare `go build`; on a machine with `go env GOTOOLCHAIN=local` and a Go older than `go.mod` requires, the lane fails at boot with `go.mod requires go >= 1.26.6`. CI installs the right Go so it does not bite there, and the failure is loud rather than silent, but a one-line `GOTOOLCHAIN=auto` in both scripts would remove a local-only stumble. **Done 2026-08-25** — set on the `go build` in both scripts, with the reason as a comment above it.
- [x] **Seven nutrition Playwright specs fail on any machine whose local calendar date differs from UTC's.** Found 2026-08-25 while running round 12's named specs, and confirmed to fail identically on an untouched `main` worktree against a freshly reset database, so it is neither this round's doing nor accumulated test residue. The mechanism: the specs build day-relative timestamps with `new Date(y, m, d, h, 0)` — the **Node process's** local zone — while `playwright.config.ts` pins the browser to `timezoneId: 'UTC'`. Run at 23:35 EDT the seeded entries carry a `started_at` on the local day, which the page (reading the UTC day) renders as *yesterday*, so nothing appears and the visibility assertions fail. The affected specs are exactly the ones that seed a day-relative `food_log` row: `daily-view` (grouping), `diary-date-navigation` (both), `dynamic-budget-journey`, `extended-nutrients`, `nutrition-day-journey`, `nutrition` (water goal). CI never sees it because hosted runners are UTC, so local and UTC dates always agree there. The durable fix is for the specs to build their timestamps in the same zone the browser is pinned to rather than in the runner's — not to widen a timeout or pin the suite to a timezone, which would only move the window. Worth doing because a suite that is green for sixteen hours a day and red for the other eight trains people to re-run. **Done 2026-08-25** ([decisions.md § 728](../architecture/decisions.md)) — `tests-e2e/fixtures/dates.ts` now builds every day-relative timestamp in the zone the config pins the browser to, the three separately-re-derived `noonUtcToday` helpers collapsed onto it, and `fixtures/dates.test.ts` scans the whole `tests-e2e` tree for local-zone day derivation so a new spec cannot re-derive it. The hand-enumerated list above missed one: `meal-detail` was affected too, and `dynamic-budget-journey` + `extended-nutrients` were already correct. Verified by running the pre-fix specs under `TZ=Pacific/Kiritimati` (6 of 32 red) and `TZ=Pacific/Midway` (the evening-seeded grouping test red) and the converted ones under both (32/32 green).

- [x] **Four non-nutrition Playwright specs still derive a day-relative date in the runner's zone** — **CLOSED 2026-08-25** ([decisions.md § 738](../architecture/decisions.md)), and the entry was wrong in three ways. Round 14 (#816) had already converted the three `dashboard/*` specs without updating this entry, so only `cross-cutting/dashboard-journey.spec.ts` was still listed from its four; and the count was never right — `settings/gear-backfill.spec.ts` carried a `not yet converted` reason from the first commit and appears in neither the enumeration nor the total (§ 728's prose names "the dashboard, gear and dashboard-journey surfaces", its numeral says four). **The `datetime-local` exemption did not survive**: `RunEditor` submits `new Date(startedAt).toISOString()` and the control holds no zone, so the browser resolves it in UTC — swept hour by hour, the submitted run lands outside the goal week the browser computes for 4/168 UTC hours in `America/New_York`, 11 in `Pacific/Midway` and 14 in `Pacific/Kiritimati`, where both goal assertions fail. **And the "deliberate" half was not**: the three coaching specs shifted LOCAL days (168 mismatches per zone-year in every DST zone tested, 0 in the three without DST), and `routes/segments.spec.ts` built local Jan 1 and read it back in UTC — `1993-12-31` instead of `1994-01-01` for every zone east of Greenwich, every day of the year, not only across New Year. Nine of the ten entries are converted; `plans/calendar.spec.ts` is the one genuine exemption (its formatters hash identically across eight zones over all 366 days of 2026), joined by `fixtures/plan-today.ts` for its explicit `timeZone: 'UTC'`. Four holes closed in the scan itself, each proved to fire: only one of the four lane configs was checked for the UTC pin, `getTimezoneOffset` was unbanned, a zoneless date-time literal handed to `new Date` / `Date.parse` was unbanned, and `.mjs` under `tests-e2e` was unscanned. Every converted spec re-run green under the ambient zone plus UTC−11 and UTC+14.

- [x] **`gear-lifecycle-journey.spec.ts` fails locally on the `service_role` read, never in CI — same workstation CLI divergence as the two known pgtap failures.** **CLOSED 2026-08-28** ([decisions § 777](../architecture/decisions.md)). **The filed instance was already fixed** — [§ 735](../architecture/decisions.md)'s round landed `tests-e2e/fixtures/db-read.ts` and converted both `gear_with_distance` rollup reads, so neither line this entry quotes exists on `main`. What survived is the filing's real point, and the class is much larger and differently shaped than filed. The guard shipped beside that fixture banned **one spelling**: a `??` with a literal default on the same line as an `expect`. An AST sweep of `apps/web/tests-e2e` for the actual defect — an awaited Supabase read whose `error` is neither destructured nor consulted, whose data is then judged by an `expect` — found **521 assertions across 397 reads in 147 files**, in three waves the old scan could not see: 368 absorbed the absence into a value (`?.`, `?? 0`, `?? []`), 51 needed no absorber because the read's own `null` was already what the matcher wanted, and 102 forced it with a `!`, a cast, or a null `count` from a `head: true` probe. **62 of the 521 PASS on a read that never reached the table**, which is the headline: 30 are `expect(row).toBeNull()` in the specs that exist to prove a boundary holds (`private-rls-negative`, `cross-user-mutations`, `public-runs-view`, `follow-engage-block`, `people-follow`), and the rest include the Art 17 cascade assertions in `account-deletion.spec.ts` / `account-data-rights-journey.spec.ts`, where a failed service-role read supplies the `[]` that `expect(cm.data ?? [], 'coach_messages must cascade').toEqual([])` wants. The workstation CLI divergence this entry describes fails **every** such read at once, so it costs an hour on the four loud specs and nothing visible on the sixty-two quiet ones. All 397 reads now go through `readRow` / `readRows` / `readMaybeRow` / `readCount`; the two `admin.auth.admin.getUserById` reads keep a raw shape because GoTrue reports a deleted user with a 404 rather than a null row, and now assert that status before reading the absence. `db-read.test.ts` bans the shape rather than the costume — a dropped-error binding may not appear inside an `expect` at all — and is proved to fail on the pre-fix tree (138 files, including this spec) and on the pre-§ 735 tree (naming lines 175 and 265, the two quoted here), and to pass now with no exemptions. `tsc -p tsconfig.tests-e2e.json` exits 0; the guard runs in the existing `test:unit` glob, so no new CI job. **The underlying CLI divergence is NOT fixed** — the four specs still fail locally — but the failure now names the read instead of the feature. The 147 converted specs are owed a Playwright run at integration. Original filing below. Found 2026-08-25 while verifying round 13's integrated branch, and **confirmed to fail identically on an untouched `origin/main` worktree**, so it is neither round 13's doing nor test residue. The failure is precise and worth recording because it reads like a broken derived cache and is not one: the UI assertions immediately above it PASS (`.gear-meta` renders `6.0 / 500` and `1 run`, so the `gear_with_distance` rollup is correct and the trigger fired), and only the backend cross-check fails — `admin.from('gear_with_distance').select(...).single()` returns no row, so `Number(rollup?.total_distance_m ?? 0)` collapses to `0` and asserts against `6000`. That is a `service_role` grant/visibility artifact of this workstation's Supabase CLI 2.109.1 against CI's pinned 2.84.2, the same family as `coach_roster_summary` test 10 and `donations_status_lock`'s `fundraiser_totals`, which fail locally for the same reason. **The trap is the shape of the failure**: an `.single()` that returns nothing is indistinguishable from a zero once `?? 0` absorbs it, so the assertion accuses the rollup instead of the read. Whoever fixes the CLI divergence should re-run all four together; whoever does not should not spend an hour on the gear triggers. A `.single()` whose error is asserted rather than discarded would make the real cause visible at the point of failure.

## Surfaced by the iOS parity-column sweep (2026-08-25)

- [x] **Spoken cues stopped on iOS the moment the run screen backgrounded, because `UIBackgroundModes` omitted `audio`** — closed 2026-08-26 ([decisions § 742](../architecture/decisions.md)). The premise held exactly as filed: `audio_cues.dart` calls `setIosAudioCategory(playback, [mixWithOthers, duckOthers], voicePrompt)` and the plist declared only `location` and `processing`. `<string>audio</string>` added, with the reason as a plist comment. **What the sweep could not have known is why nothing caught it:** `group('iOS Info.plist')` in `architecture_guards_test.dart` had twelve tests reading these very files behind `if (!file.existsSync()) return;`, and `mobile_ios` — the only package where they exist — is absent from the `test-packages` job's `melos exec --scope=` list, so not one of those assertions had ever executed. `apps/mobile_ios/deployment.md` had meanwhile said "Background Modes → Location updates + **Audio (for TTS)**" all along; the requirement was written down and the declaration file never followed. Replaced by `scripts/check_ios_native_declarations.mjs` + the `ios-native-declarations` CI job, which derives the requirement from the `playback` call rather than listing it, and verifies 43 declarations the same way. Still owed: the device run — background audio is not host-testable, and no Mac has been in the loop.
- [x] **`Runner.entitlements` carried no `com.apple.developer.aps-environment`, so an iOS build could not register for APNs** — closed 2026-08-26 ([decisions § 742](../architecture/decisions.md)). Declared, as `$(APS_ENVIRONMENT)` rather than a literal: one entitlements file serves all three Runner build configurations, and the sandbox and production APNs hosts reject each other's tokens, so `APS_ENVIRONMENT` is defined per configuration in the pbxproj (`development` on Debug + Profile, `production` on Release — what `flutter build ipa --release` archives). Pinning `development` and trusting the distribution export to rewrite it is the shape that ships a TestFlight build whose every push answers `BadDeviceToken`; an undefined setting expands to the literal string instead, which no profile carries, so the failure is loud at signing. The entry was right that this is a declaration and not a credential — the pubspec comment and `native_push.md` both said otherwise and are corrected. **Not a Mac-side action after all:** Xcode writes this key, but so can a text editor, and the guard now requires it. Still gated on the operator's Firebase / APNs artifacts for delivery, and on a signed build for verification (`codesign -d --entitlements` on the exported IPA).

## Surfaced by round 15's pgtap refusal-assertion audit (2026-08-25)

- [x] **A third vacuous refusal existed and only CI could see it.** Closed 2026-08-26 ([decisions § 741](../architecture/decisions.md)). `rls_fitness_snapshots_test`'s "anon cannot read fitness_snapshots" reads a table that **test 5 of the same file has just emptied**, so it passed whether or not the policy existed. The workstation audit scored it *killed* because the shared dev database held 41 committed `fitness_snapshots` rows left by Playwright and api_client runs — pgtap never rolled them back because they were never in its transaction — and with RLS removed those rows failed the assertion, which looks exactly like a policy working. On CI the table is genuinely empty (`seed.sql` files one row as a source-gate self-test and deletes it again at line 2597), so the mutation survived and the guard reported it on its first run. **The lesson is not about this test: a mutation operator is only as honest as the database it runs against, and ambient test debris can invert its verdict.**
- [x] **Two private-route refusal assertions had no row to refuse.** Closed 2026-08-25 ([decisions § 741](../architecture/decisions.md)). `rls_route_conditions_test`'s tests 10 and 13 asserted a non-owner and anon read no `route_conditions` rows on the private route while no fixture ever filed one there. **The policy itself holds** — a real report filed on that route by its owner is invisible to both, verified directly — so this was a vacuous test, not a leak. The owner now files that report and reads it back, and both assertions die under the mutation that previously left them green.
- [x] **One pgtap negative pinned neither a SQLSTATE nor a message.** Closed 2026-08-25. `activities_view_windowed`'s view-write refusal passed `null` for both, so any error satisfied it; its comment guessed 42501 or 0A000 and the real code is 55000 (a UNION view is not auto-updatable, and the rewriter answers before the privilege check). Pinned, with the missing INSERT grant asserted separately against the catalogue since no write attempt can ever observe it.
- [x] **The definer-relation refusals hold too — and the premise's boundary was wrong.** Closed 2026-08-26 ([decisions § 745](../architecture/decisions.md)). A second mutation operator now replaces the relation itself with a permissive definition inside a savepoint, one per relation in `apps/backend/scripts/pgtap_definer_neutralisers.mjs`, each keeping every predicate that says *which* rows the assertion asked about and dropping every predicate that says *whether the caller may see them*. **Nothing was vacuous: 50 of the 51 assertions the new operator measures die under it, and the one survivor is a real zero** (an age band cannot contain a runner who has no age, with its positive control in the assertion two above it). So §741's finding holds a second time — no RLS hole was hiding behind an unmeasured refusal. **The finding is about the boundary, not the tests**: the split is not definer-vs-invoker, which is what `prosecdef` records, but whether the relation carries an access predicate of its own — and **eight of the 24 relations that needed a replacement are `SECURITY INVOKER`**, filtering with a `= auth.uid()` or an `is_public = true` written into the body. `search_public_events` is the sharpest: with RLS fully bypassed the base-table read returns the members-only event while the RPC still returns nothing. The old `baseTables` filter was itself doing damage beyond the definer class: it excluded two `activities`-view reads the *first* operator could have measured all along, and seven more that turned out to need the second. Guard population 104 → 168. A second CI step validates each replacement against a subject its real relation provably hides, plus an end-to-end control that must kill a known-good refusal and leave a known-bad one standing.
- [x] **The vocabulary trade measured, then half-closed.** Closed 2026-08-26 ([decisions § 745](../architecture/decisions.md)). Both operators were run over **all 294** zero-or-empty assertions in the suite, not just the ones worded as refusals, which is the only way to see what the selector misses. The regex selected 126 and killed 121 of them, so its **precision is 96 %**. Of the 168 it did not select, **28 died too** — the measured recall gap, and it splits cleanly. Seven unambiguous words (`never sees`, `unfindable`, `enumerate`, `exposes no`, `reads nothing`, `gets nothing`, `absent from`) close 11 of those for free: every assertion they select dies, so not one new `EXPECTED_SURVIVORS` entry was needed. The largest remaining word is **`excluded` / `excludes`: 21 assertions, roughly half access control (a declared minor, a search opt-out, a block, a members-only event) and half ordinary filters (`byday=MO excludes it`, `free filter excludes the priced class`)**. One word, two claims — so the trade does stand, quantitatively: no regex separates them, and the alternative is not a 207-entry allowlist but a **117-entry** one, since 117 of the 157 assertions outside the vocabulary legitimately survive (cascade deletes, triggers that correctly did not fire, positive DELETE controls). The durable fix is neither: an opt-in `-- refusal: <why>` comment on the line above the assertion, said once by the author where the claim is. 31 assertions carry one today, all killed. Two of the 28 stay outside deliberately — `distance is returned as coarse bucket 0` and `a long-broken streak reports current = 0` expect a zero *value*, not an empty result, and a widening mutation moves a value, so a "kill" there would mean nothing.
- [x] **A kill over an owner-scoped RPC proves a subject exists in the transaction, not that the test built one.** Closed 2026-08-26 ([decisions § 751](../architecture/decisions.md)). **The premise was right and its boundary was wrong in both directions.** It is not confined to identity-only selectors: `gym_exercise_set_history('Bench Press')` carries an exercise name as its selector, the RPC normalises it, and `seed.sql` commits **six `Bench press` sets** — so that assertion was killed by committed data whether or not its fixture existed. And "seven assertions" was seven *relations*; the assertions over those seven number **eleven**, five of them `coach_roster_summary`'s alone. The `xmin` objection is an argument for scoping the right relation, not against scoping: every replacement now names a `subject` alias and reveals only that relation's rows carrying the current transaction's `xmin`, and the alias is **the relation the replacement's rows come from**, not the one the dropped predicate guarded — `run_gear rg` for `public_run_gear`, `coach_athletes ca` for the roster, `gym_sets s` for the sets view. **All 51 definer-measured assertions still die**, so every one had already filed its own subject; no pgtap fixture changed and the population is unchanged at 168 / 5 expected survivors. That the guard *would* have caught something is shown by deleting a fixture in a rolled-back transaction: the old operator still kills, the new one lets the assertion survive (demonstrated on `gym_sets` twice, `coach_athletes` and `runs`). Six of the 24 witnesses were `select 1;` and proved only that the replacement revealed the *seed* — they now file their own subject — and a third end-to-end control (a private route the seed already committed, which must survive) fails by name if any replacement loses its scoping.
- [x] **Four definer relations still have no permissive replacement, and none of their assertions is a refusal.** Closed 2026-08-26 ([decisions § 751](../architecture/decisions.md)). **It was two relations, not four**: `get_event_meet_point` and `public_run_counts` *are* registered and their refusal assertions die under mutation — what they contribute is one non-refusal shape claim each (a coord-less event, an empty input array), which is a different fact from the relation lacking a replacement. The genuinely unreplaced pair is `fetch_pending_reports` (1 assertion) and `challenge_leaderboard` (1). Both stay out, with the reason recorded rather than rediscovered: the first's zero is the RPC's own `status = 'pending'` filter, so a replacement may only drop the admin gate and the assertion would survive — an `EXPECTED_SURVIVORS` entry bought with a second copy of the queue query; the second expects a zero *value* rather than an emptiness, the class § 745 already excluded. They are declared in `UNREGISTERED_DEFINER_RELATIONS`, which the `--validate-operators` phase re-derives from the suite and fails in both directions, so a third such relation is now a decision someone makes on purpose rather than a chore the guard hands them at merge.
- [x] **The same debris exposure on the FIRST mutation operator is measured but not closed.** Closed 2026-08-26 ([decisions § 753](../architecture/decisions.md)). **The two counts are exactly right on a seed-only database and everything concluded from them is wrong.** 63 and 54 re-measure precisely after a `db reset`, `runs` at 9 assertions over 251 rows — but that is a census of TABLES, and the assertions carry the selectors: run all 117 candidates' own SQL against a seed-only database with RLS off, which is what the old operator's kill was worth, and **not one returns a row**. The residue was zero assertions wide, not 63. The 54 is wrong the other way — **three of them read `activities`**, a `security_invoker` view over `runs` / `gym_workouts` / `food_log`, so it is absent from the table census while being answerable by debris. And "a role change with no query to add a predicate to" is false: RLS is a set of policy EXPRESSIONS, and a policy is a query to add a predicate to. Neither proposed mechanism was needed. Operator 1 is now one extra permissive `SELECT` policy per RLS-enabled table (90 of them, 14.8 ms, created once per file and GUC-gated so it is inert outside a mutated span), revealing only the rows this transaction wrote, with the assertion running as the role the test gave it. Underneath sat a defect in § 751's own predicate: `xmin = pg_current_xact_id()::xid` matches only a top-level write, and pgtap's `lives_ok` / `throws_ok` run in a subtransaction — **11 false survivors across 9 files** on the first scoped run. A `SECURITY DEFINER` wrapper over `pg_xact_status` makes the test exact (visible + in progress = written here), and both operators now share it. Population unchanged at 168 / 5 expected survivors, no fixture changed, pgtap still 258 files / 2188 tests green.

## Surfaced by #789 round 19 (2026-08-27)

- [x] **`npm run test:unit` cannot run the web unit suite from a clean checkout, and CI fetches `tsx` over the network on every run instead.** Closed 2026-08-27 ([decisions § 764](../architecture/decisions.md)). **The premise reproduced exactly and the boundary was wrong twice.** `npm run test:unit` in a clean `npm ci` tree exits `127: tsx: command not found`, confirmed in an isolated checkout — but this is not "the one step in this job that is not hermetic": `parity-types` also pulls a Deno runtime, the Supabase CLI and a Docker stack over the network, and what is unique to `npx tsx` is that it fetches an **unpinned package with no lockfile entry and no integrity hash**. And it is not one call site: `cross-client-roundtrip` runs `npx tsx` four more times, so five in all, every one fixed by the same declaration. `tsx` is now an `apps/web` devDependency and the CI step runs the package script, so the command CI runs and the command its diagnosis says to reproduce with are one string. The addition moves no other resolution — the only new `name@version` across both lockfiles is `tsx@4.23.12`. A guard, `scripts/check_workflow_binaries.mjs`, now refuses any binary a package runner is handed unless a declared package provides it. Surfaced fixing § 757's CI failure (2026-08-27). `apps/web/package.json`'s `test:unit` invokes a bare `tsx`, which no lockfile installs — `tsx` is a declared dependency of nothing in this repo, so on a clean `npm ci` the script exits `127: tsx: command not found`. CI does not use the script: the `Web TS unit tests` step runs `npx tsx --test ...` directly, and `npx` resolves it by downloading it from the registry, which is why the lane is green while the documented command is broken. Two costs: the reproduce line the step itself prints (`npm ci && npx tsx --test ...`) is the only invocation that works, and every CI run takes an unpinned third-party binary off the network at test time — the one step in this job that is not hermetic. The durable fix is to declare `tsx` as an `apps/web` devDependency and point both the script and the CI step at the installed copy; it is a lockfile change on both `package-lock.json` and `pnpm-lock.yaml` plus a workflow edit, so it wants its own change rather than riding a CI hotfix.

## Surfaced by #789 round 21 integration (2026-08-28)

- [x] **A parity.md table row written without its LEADING pipe is invisible to all four guards that read the file.** Closed 2026-08-28 ([decisions § 779](../architecture/decisions.md)). **The count of four is right and one of the names is not.** The four are `check_parity_ios_column.mjs`'s three pipe-gated readers plus `check_parity_matrix.dart`, as [§ 774](../architecture/decisions.md) had it; `check_doc_checkboxes.mjs` gates on nothing of the kind and `parity.md` carries zero checkboxes, so it was never one of them. The real fourth copy of the rule is `check_parity_pair_registry.mjs`'s `parseSyncerRows`, over a different table in the same job, still carrying the pre-§ 774 `slice(1, -1)` besides. **Worse than filed on the iOS guard**: `readRows` reset its header on any non-pipe line, so one such row dropped the rest of its table too — 337 rows to 329, exit 0. The detector did not need widening (the risk the filing named), it needed GFM's own state: a table opens on a header plus a delimiter of the same width and every line after it is a row. That walk lives in `scripts/markdown_lines.mjs` beside the soft-wrap folder, which had the same blind spot from the other side, with a Dart half in the matrix guard. Both guards now refuse a row they cannot grade and a pipe-leading line in no table; the matrix guard got its first test suite, seven of whose ten cases fail against the old walk.

- [x] **The test that forces `GRANDFATHER_CUTOFF` to be bumped is also the way past the guard it protects.** Closed 2026-08-28 ([decisions § 775](../architecture/decisions.md)). **The premise held exactly and the proposed fix does not work as stated.** With the cutoff at `20270619` and 436 committed migrations, `isAfterCutoff` selected **0** of them — the scanned set was empty, measured. But scanning the whole tree with the cutoff ignored finds **32 violations across 30 files** (twelve `notifications_kind_check` widens `20260903_001`-`20270218_001`, thirteen `jobs_kind_chk`, four `runs`, one `rate_limits` FK), the newest at `20270410_001` — so "a frozen legacy boundary" would have to sit at `20270410`, still a date exempting 200-odd files nobody has read, and would still have to move the first time a violation shipped. So `GRANDFATHER_CUTOFF` is **gone**, not frozen: the 32 are grandfathered one at a time as `{filename, table, constraint}` in `GRANDFATHERED_VIOLATIONS`, every migration is scanned on every run (23 ms over 436), an entry exempts only what it spells, an entry matching nothing **fails** the guard, and **adding a migration needs no edit to the guard at all** — the bookkeeping edit was the bypass. An anonymous `add check (...)` gets a null key no entry can match, so it fails closed. 20 tests -> **23**, including the one the retired design made impossible (the scanned set is the whole tree and is never empty) and one that drops each of the 32 entries in turn and asserts the tree goes red. Original filing below. **The test that forces `GRANDFATHER_CUTOFF` to be bumped is also the way past the guard it protects.** Found integrating round 21, and it is the round's own theme one level up. `check_migration_online_safety.mjs` inspects only migrations NEWER than the cutoff, and `check_migration_online_safety.test.mjs`'s "GRANDFATHER_CUTOFF is the max version in the committed tree" asserts the cutoff is at least the newest migration — so every migration-bearing PR must bump it, and the bump removes that migration from the scan. The order is what decides whether the guard ran: bump after the check passes (what round 21 did, and what the cutoff's eight-round history shows every previous round did) and the migration was inspected; bump first and it never was, with both orders leaving an identical tree and an identical green test. At rest the scanned set is therefore empty by construction. The durable fix is to make the cutoff mean what its name says — a frozen legacy boundary that moves only to grandfather a *known* violation — and to scan everything after it on every run, which is what the guard already does and what nothing then needs to bump.

## Surfaced by #789 round 20 (2026-08-27)

- [x] **Ten more guards misread their own input, all latent, each with a reproduction.** **All ten CLOSED 2026-08-28** across [decisions § 773](../architecture/decisions.md) and [§ 774](../architecture/decisions.md), split five and five. "Latent" held for all ten on the committed tree, and it was the right thing to distrust: **three of the ten are false passes, not noise** — the GATT guard certifies § 410's own bug, the lambda guard reads a decoy inside the alias call's own arguments, and the pgtap guard scored a vacuous refusal *killed*, which is § 741's inversion reproduced at the instrument. The two guards § 770 never probed were probed here and **both misread too**, so the population is 17/17 measured rather than 15/17 measured and 2 assumed; `check_production_env.mjs` was the worst of the twelve, passing a Postgres connection string that Vite would have inlined into every client bundle. ORIGINAL FILING: Derived membership from the § 770 sweep: all **17** `check_*.mjs` guards were probed by running their own exported functions against a crafted input. Three were the filing, **ten more misread**, one is verified clean (`check_migration_versions.mjs`), two were not probed (`check_doc_checkboxes.mjs`, `check_production_env.mjs`). Every one of the ten exits 0 on the committed tree today, and each was checked for why — but three of the four filed items were understated in the same direction, so treat "latent" as unproven rather than safe. Each wants its own reproduction, pinning test, and — for the four with no test sibling — a CI step; that is a round, not a rider on § 770.
  - [x] **`check_workflow_binaries.mjs` — a runner whose binary is on the next physical line is dropped silently.** Closed 2026-08-28 ([decisions § 773](../architecture/decisions.md)). **Held exactly as filed, and latent by measurement rather than by assumption.** `run: |` / `npx \` / `totally-unpinned-cli` parsed to zero invocations and the folded scalar was the same hole; both reproduce. `parseInvocations` now scans logical lines — a `>`-headed block folded the way YAML folds it (paragraph breaks respected, `|` left alone), then a trailing backslash joined the way the shell joins it. The committed workflows carry **9 folded scalars and 124 continuation lines**, and the invocation set is bit-for-bit identical before and after (**16**, same files, same lines), which is now a test rather than a claim. `binaryOf` still returns null silently for genuine prose, which is correct once the two continuation shapes resolve before it is asked.
  - [x] **`check_lambda_alias_sync.mjs` — reads only the first `FUNCTIONS=(…)` in the file.** Closed 2026-08-28 ([decisions § 773](../architecture/decisions.md)). **The headline held; the secondary was wrong about its direction.** The per-environment array reproduces exactly (`[coach, osrm-proxy]` parsed from a script whose `preview` branch names only `coach`), and both directions now ask the right set — a Terraform alias must be in EVERY assignment to count as synced, any assignment's suffix must be declared in Terraform, and two disagreeing `NAME=` templates are reported. The 400-character window was filed as reading a stray `--name` off an unrelated command, i.e. loud noise; measured, the lazy first match means a decoy INSIDE the alias call's own arguments answers for it (`--function-name "$(aws ssm get-parameter --name live …)" --name stable` reads `live`) — a **false pass**, the sources "agreeing" on an alias the script never repoints. Second undocumented direction: `--name "live"` was captured with its quotes and reported as a rename. Both gone; the name comes off a command lexed by the new `scripts/shell_lex.mjs`. Committed script measured at 1 assignment, 1 template, 1 alias name.
  - [x] **`check_ios_native_declarations.mjs` — no blindness check on `swiftSources`.** Closed 2026-08-28 ([decisions § 773](../architecture/decisions.md)). **Both halves held.** Passing `swiftSources: []` against the committed tree yields **errors 0 and 41 `[OK]` lines instead of 43**, exit 0, with nothing saying two rules stopped enforcing. The blindness check is now symmetric and its subject is DERIVED from the rule tables (`swiftRuleKeys()`), so it exists only while a rule reads Swift and covers one added tomorrow. The missing reverse direction is closed too: a `*UsageDescription` key no rule claims is an error, exempted only by the `FIXED_PLIST_KEYS` admission that already carried `NSPhotoLibraryAddUsageDescription`. All 11 declared keys are accounted for — 10 by a rule, 1 by that list.
  - [x] **`check_watch_ble_uuids.mjs` — both parsers read UUIDs out of comments.** Closed 2026-08-28 ([decisions § 773](../architecture/decisions.md)). **Worse than filed: the guard does not merely fail to catch § 410, it certifies it.** "A stale UUID quoted in each file's comments agrees with itself" understates the measured result — give the firmware a live table shifted one position with the old table kept in a *trailing* comment (the characteristic scan is last-writer-wins) and a phone that never moved, and `compareTables` returns **zero errors and three `[OK]` lines**. `gatt_service` was also matched non-globally, so the `//!` module doc above the declaration wins. Both sources are now read through `scripts/comment_strip.mjs`, which tracks each language's strings honestly (Rust lifetimes are not char literals; Dart triple quotes span lines; block comments nest) and throws on what it cannot read; a second `gatt_service` throws rather than picking one. Measured: **0 UUIDs in comments** across both committed files (10 live attributes, 10 live constants), so the misread was latent.
  - [x] **`check_pgtap_refusal_assertions.mjs` — `parseTap` keys verdicts by description string.** `Map<description, ok>` over the whole mutant's TAP output, so two assertions in one file sharing a description collapse last-writer-wins and a genuinely vacuous refusal is scored *killed* on its twin's verdict — § 741's inversion pointed at the instrument. `EXPECTED_SURVIVORS` has the same collision. Latent: 0 duplicate descriptions across the committed pgtap tree (scanned with the guard's own `findCalls`/`literalOf`, no database needed). **Closed 2026-08-28** ([decisions § 774](../architecture/decisions.md)). Held, and wrong in BOTH directions rather than one: with the description shared, `ok` then `not ok` scores the SURVIVING (vacuous) assertion as killed — the § 741 inversion pointed at the instrument — and `not ok` then `ok` scores a genuinely killed one as a survivor. Both reproduced verbatim against `parseTap`. Every verdict is kept now and a repeated description is reported as unattributable rather than resolved by guessing, which also closes the `EXPECTED_SURVIVORS` collision by construction: an ambiguous assertion never reaches the survivor list, so no entry can excuse a second one. Latent confirmed and measured wider than filed — 0 duplicates across **2001** assertion descriptions in the **262** committed pgtap files, counting every pgtap assertion kind rather than only the refusal candidates, now pinned by a test.
  - [x] **`check_root_scripts.mjs` — naive shell tokenising plus a literal grep a refactor moves.** Closed 2026-08-28 ([decisions § 773](../architecture/decisions.md)). **All five misreads reproduce, and the filing missed the biggest one: the guard runs in NO workflow.** `npm run test:scripts` was its only caller and no job invoked it — the § 439 shape, and the reason none of the rest was going to be noticed. The summary line was overclaiming in the same register: **"passed (95 scripts)" over 28 claims, where 14 of the 95 are `//`-prefixed documentation headers whose values are prose**; it now reports 29 claims across 81 scripts with what it could not verify printed and counted. Rewritten over `scripts/shell_lex.mjs` with the exports and the test file it never had (20 cases), plus a diagnosed step in `workflow-lint`. `cd` is now resolved against where the previous `cd` left the script, so `dev:db:reset`'s `cd ../..` means the repo root rather than being vacuously true against it, and a `cd` out of the repository fails. `RESERVED_PNPM_VERBS` was deliberately NOT narrowed — `build`/`test`/`start` are pnpm script shortcuts and listing them costs a real check on `pnpm -C apps/web build`, but that is a judgement about pnpm's command surface rather than a misread; what changed is that the skip is reported instead of dropped.
  - [x] **`check_parity_pair_registry.mjs` — reads only the first physical line of the CLAUDE.md bullet.** `text.indexOf('\n', bulletAt)` truncates deliberately, to separate the lockstep bullet from the watch-port paragraph below it, so a soft wrap makes every pair named after the break invisible to check 1 — a pair registered in the prose and missing from the syncer table, which is § 604's exact defect. None of the anti-vacuity checks fire; the first line satisfies them. Latent: the committed bullet is one 70,133-character line. **Closed 2026-08-28** ([decisions § 774](../architecture/decisions.md)). Held, and the silent path is not the one filed. A WHOLESALE reflow is loud (the first line then ends before `The pairs are:` and the anchor check fires), and a single wrap mid-list is loud but misdirecting — measured at the `gym_prs` entry it hid **86 of 99** pairs and reported them as pairs the SYNCER table had added, which nobody had. The silent case is a new pair appended on a continuation line: invisible, **zero errors reported**, which is § 604's exact defect. The committed bullet measures **71,208** characters, not 70,133. Markdown's lazy continuation is applied now (`scripts/markdown_lines.mjs`, shared with the iOS-column guard), and the blank line separating the bullet from the watch-port paragraph is asserted rather than assumed.
  - [x] **`check_parity_ios_column.mjs` — rule 2 is per-LINE.** `checkSingleStatement` needs `\biOS\b` and a legend symbol on the same physical line, and markdown soft-wraps paragraphs freely, so a wrapped fourth statement defeats the check described as catching "the failure that happened". Latent: `parity.md` soft-wraps nothing today. Weaker sibling: `splitRow`'s `.split('|').slice(1, -1)` loses the last cell of a GFM row with no trailing pipe, then demands the author add a note that is already there — caught first by `check_parity_matrix.dart`'s 6-cell rejection in the same job. **Closed 2026-08-28** ([decisions § 774](../architecture/decisions.md)). Both halves held, and the sibling is worse than filed. Rule 2 is graded per folded line now; `parity.md` already carries **5 multi-line prose blocks** outside the rule block, so the wrapping habit is in the document rather than hypothetical. And `splitRow` is NOT caught first by `check_parity_matrix.dart`: that guard drops its own end-empties conditionally, so a row with no trailing pipe parses to seven cells and PASSES its 6-cell check, and it runs after this guard in the job besides. Reproduced: the lost cell is the Notes column, so rule 4 read the Apple Watch cell as the notes and demanded an `**iOS ✗:**` marker that was already there.
  - [x] **`check_pnpm_overrides.mjs` — a parent-scoped pin is enforced against every copy in the tree.** `pinTarget` extracts `{target, parent}` correctly and `checkResolutions` then uses `parent` only for the existence check; the version scan is `resolutions.filter(r => r.name === target)`. A scoped `@sveltejs/kit>cookie` therefore accuses a `cookie` copy under a different parent that the override never reached. Cannot fire today only because `package.json` declares a bare `cookie` pin beside the scoped one — narrowing to the scoped pin alone, a reasonable change, turns it on. **Closed 2026-08-28** ([decisions § 774](../architecture/decisions.md)). Held exactly as filed: reproduced against a tree where `express` carries its own nested `cookie@0.6.0`, which `@sveltejs/kit>cookie` accused. A scoped pin's subject now comes from the dependency graph, read per lockfile because the two record it differently — npm's key IS the install path (nested copy names its parent; a parent with no nested copy resolves the hoisted one), while pnpm's `packages:` is flat, so the edge comes from `snapshots:` (**146** parents in the committed lockfile, carrying both `kit>cookie` and `kit>devalue`). A scope over a parent that resolves nothing, and an empty snapshots graph under a parent that does resolve, are now errors rather than a quiet pass.
  - [x] **`apps/web/scripts/check_constraint_unions.mjs` — strips `--` before knowing whether it is inside a literal.** `.replace(/--[^\n]*/g, '')` runs over a migration with no string awareness, so a `--` inside a literal can delete the `check (col in (…))` clause it exists to read. Re-parsed through `apps/backend/scripts/sql_lex.mjs`: **the same 68 `table.column` → value sets**, and its realistic failure is a loud `no CHECK constraint found` rather than a false pass. The fix is three lines — import `splitSqlStatements` and parse `splitSqlStatements(sql).join(';')` — but it sits in another package root with no test sibling and no CI lane that would run one. **Closed 2026-08-28** ([decisions § 774](../architecture/decisions.md)). Held, and there is a SECOND direction the filing missed: the eaten run can also be the `alter table <t>` header that says which table the next line's clause belongs to, which files that clause under the PREVIOUS table — so a registered pair gets certified against another table's constraint, silently, rather than merely failing loud. Both reproduced. Parsed through `sql_lex.mjs` now; the re-measurement confirms **the same 68 `table.column` value sets** across **436** migrations, none of which the lexer refuses. The missing test sibling exists and runs in `parity-types` after the drift check, with its own `::error::` per § 764.

## Surfaced by the #789 partial-box reconciliation (2026-08-26)

- [x] **`parity-matrix` bundles four unrelated guards under one job name and is not held to per-step diagnosis.** Closed 2026-08-27 ([decisions § 764](../architecture/decisions.md)). **The count of four was right; "the other three steps already have one" was wrong, and the two-line fix would have closed one instance of a class with three members.** Two steps carried no `::error::`, not one — the pair registry, and the Dart matrix check, which was unnamed and therefore outside the rule entirely, so deleting a step's name had always been the shortest way to satisfy the guard. Rather than register the job, membership is now DERIVED: a job running two or more of this repo's guards is bundled by that fact. That is five jobs, and the registry named two — the three it never named are `parity-matrix`, **`workflow-lint` (five guards, zero diagnosed)** and **env-isolation's `unit` (two, zero)**, with `pgtap-rls` diagnosing two of three. Seven steps fixed. Kept bundled rather than split into four jobs: each split needs its own line in the `CI gate`'s `needs:` list, and an omitted line turns an unlabelled failure into an invisible one — so that list is now enforced too. [Decisions § 756](../architecture/decisions.md) put the doc-checkbox guard there because it is the one job a docs-only diff does not skip — but the job now runs the TS↔Dart pair registry, the parity.md iOS column, the doc checkboxes and the Dart matrix check, which is exactly the "the job name cannot say which one broke" condition `scripts/check_ci_diagnostics.mjs` exists for. It lists only `parity-types` in `DIAGNOSING_JOBS`. Adding `parity-matrix` there is a two-line change to that guard, but it fails until the job's first step (**CLAUDE.md vs shared-library-syncer pair registry**) gains an `::error::` of its own — the other three steps already have one. Do both together, or the guard's own suite goes red.

## Surfaced by the #789 e2e base-URL sweep (2026-08-26)

- [x] **~30 `tests-e2e` sites hard-code `http://localhost:7777` while `playwright.config.ts` advertises `PLAYWRIGHT_BASE_URL`.** Closed 2026-08-26 ([decisions § 743](../architecture/decisions.md)). The count was right — **34 occurrences across 20 files** — but "any run on a second port fails" was true of **20 of them**, and the other 14 were a different defect. Genuinely broken: 14 `request.get` / `request.post` calls that had no reason to name an origin at all (Playwright resolves a relative path against the lane `baseURL`), five `context.grantPermissions({ origin })` grants, which take an *exact* origin and on any other port land on a foreign one — `navigator.clipboard.writeText` then rejects and the spec takes the catch branch it exists to prove is not taken — and one bare Node `fetch`. Inert: four `?? 'http://localhost:7777'` fallbacks after a value the config always sets (unreachable), two Edge Function `redirect_uri` payloads whose asserted branch returns before the gate that reads them, and the config's own two. Five more re-read `process.env.PLAYWRIGHT_BASE_URL`, which is right in the sharded lane and wrong in the other three, since livehub / exporthub / sso set no override. **The override did not work end to end either**: the config pinned `webServer` to `pnpm run dev`, which is `vite dev --port 7777`, so `PLAYWRIGHT_BASE_URL` moved the tests without moving the server they ran against. All of it now resolves through `tests-e2e/fixtures/base-url.ts`, guarded by `fixtures/base-url.test.ts` — 4 of its 6 tests fail against `main` and name all 32 offending sites.
- [x] **`playwright.sso.config.ts` and `playwright.exporthub.config.ts` both bound dev port `:7779`.** Found by the same sweep, closed 2026-08-26 ([decisions § 743](../architecture/decisions.md)). CI runs them as separate jobs so they never collided there; locally `reuseExistingServer` makes whichever lane starts second silently adopt the first lane's dev server, and an export-hub run that inherits the sso server has no `PUBLIC_EXPORT_HUB_URL` — so the one spec the lane exists for takes the Edge Function fallback it was written to bypass. SSO moved to `:7780`, with `config.toml`'s `additional_redirect_urls` following it. **The local SSO lane needs a `supabase stop && supabase start` to pick that up**; CI starts fresh.
- [x] **The allowlist was unexercised, and the CI value was unread for a stronger reason than filed.** Closed 2026-08-26 ([decisions § 750](../architecture/decisions.md)). The count was right — **three** spec bodies carry a `redirect_uri` — but "the Edge Function answers before it compares against the allowlist" is true of only **two** of them. `invalid_code` and `invalid_scope` are answered in the top-level shape validation, upstream of `handleConnect` entirely; the third **does** reach the allowlist block and short-circuits *inside* it, on the empty-parse 503, one line above the comparison. And the CI literal was never in the spec's path at all: `STRAVA_ALLOWED_REDIRECTS` is written into the **`edge-functions`** job's `.env.local`, consumed by a separately-booted `supabase functions serve`, while `strava-import-guards.spec.ts` runs in **`e2e-web`** against the auto-started edge runtime, which sets no such var. So the foreign-origin guard passes because the allowlist is EMPTY, not because a foreign origin was rejected — and no `strava-import` test posts to the host that has the value. Measured across all four suites: no Deno test imported `strava-import/index.ts` (its `index.test.ts` is pure-helper, importing only `_shared/strava.ts`), no pgtap or web unit test touches it, and `handler_envelope.test.ts` names `strava-import` only in a comment about the gateway. **Neither the accept nor the not-in-the-list branch had any coverage anywhere.** Both now do, pure, in `_shared/redirect_allowlist.test.ts` (19 tests) — the wire layer cannot host the accept path, because accepting means proceeding to Strava's `/oauth/token`. Four source guards pin that the handler still calls the gate, still before the exchange, still fails closed when unset, and that none of the **four** functions carrying a `*_ALLOWED_REDIRECTS` var re-spells the parse. The workflow no longer names a port: it reads the line out of `.env.development`, which two further guards pin to `base-url.ts`'s `DEFAULT_E2E_PORT` and to the path the web client actually sends. A found-and-fixed bystander: `api_database.md` said an empty allowlist **disabled** the check "(single-tenant / dev)" — the exact fail-open the code refuses.
- [x] **`tests-e2e` is outside `svelte-check`'s `include`, so nothing typechecks the Playwright tree.** Closed 2026-08-26 ([decisions § 749](../architecture/decisions.md)). The premise held, and survives its obvious rebuttal: `.svelte-kit/tsconfig.json` names `src/`, `test/` and `tests/` only, Playwright's esbuild transpile never checks, **and the one lane that already reads the tree is not a typecheck either** — `parity-types` runs `tsx --test 'tests-e2e/fixtures/**/*.test.ts'`, and `tsx` strips types the same way. So the tree was outside every type check in the repo, not just svelte-check's. **Six was measured before round 16 landed; the real figure is ten.** All six filed are present and real; four more were not in the entry — a stale `@ts-expect-error` in `fixtures/auth.ts`, and three in `scripts/start-mock-oidc.mjs`, which a hand-run over `*.ts` could not have seen. **Two were more than cosmetic.** `extended-nutrients.spec.ts` passed an assertion message as `toHaveCount`'s *options* argument, off which Playwright reads only `timeout` / `signal` — so that explanation has never once printed on a failure; and `account-deletion.spec.ts` kept `plantedRunId` nullable only to serve a trailing `= null` whose comment claims the `afterAll` reads it, which it does not (the assignment was dead, and the nullability it forced is what broke the two call sites). The remaining eight are type-level: the two coaching specs assign a `BrowserContext` into an outer `let` from inside a `test.step` closure, so at the `finally` it is still `null` and `if (ctx)` narrows to `never` — both contexts are now created up front and closed unconditionally, deleting two `!` with them. Closed with `apps/web/tsconfig.tests-e2e.json` (a **second `tsc` root**, not a widened `include`: TypeScript does not merge `include` across `extends`, so widening the app's own would mean committing a copy of SvelteKit's generated list that goes stale silently) run as `check:e2e-types` in `parity-types`, whose `code` gate cannot skip a diff that could break it, and guarded by `tests-e2e/fixtures/tsconfig-coverage.test.ts`. Pre-fix 10 errors, post-fix 0.

## Surfaced by the #789 `apps/web` typecheck-coverage sweep (2026-08-26)

- [x] **`apps/web/scripts/*.mjs` is typechecked only by accident.** Closed 2026-08-26 ([decisions § 752](../architecture/decisions.md)). The mechanism held exactly as filed — `.svelte-kit/tsconfig.json` names `src/`, `test/`, `tests/` and `vite.config.*`, TypeScript does not merge `include` across `extends`, and `tsc -p tsconfig.json --listFilesOnly` names `scripts/env_isolation.mjs` and no other file under `scripts/`, confirming it rides in on `vite.config.ts`'s import. **The tree was one of three, and not the one that mattered.** Also inside no `tsc` program: `apps/web/lambda/` — sixteen files, of which eight are the production AWS handlers behind `/api/coach`, the four `/share/*` surfaces, the OG images, `generate-route` and `osrm-proxy` (only `share-entity/src/index.ts` was covered, and only because something under `src/` imports it) — plus `svelte.config.js` and `static/sw.js`, the web-push service worker shipped verbatim to every browser that registers it. Closed with **two** new roots (`tsconfig.node.json`, `tsconfig.service-worker.json` — they cannot be one file, because a service worker's globals come from the `WebWorker` lib and that cannot share a program with `DOM`), each declaring its `types` explicitly since the automatic type-root scan does not fire under this config chain. **47 errors in the Node root, 23 in the service worker**, and four were more than type-level: a `prerender` option `adapter-static` has never had (dead since the SvelteKit 1 template), a `paths.base` built from `BASE_PATH` with no check of the leading slash SvelteKit requires (a bad value 404s the whole site with nothing naming the cause), a nullable `Response.body` in the two run round-trip scripts' gzip decode, and an unchecked nullable `public_runs.started_at` fed to `new Date(...)`. The single biggest cause was one annotation: `fail()` is a function *declaration* ending in `process.exit`, which TypeScript infers as `void`, so no env guard in any of the four round-trip scripts narrowed anything — `@returns {never}` closed 24 at once. Guarded by `apps/web/scripts/tsconfig_coverage.test.mjs`, which names no tree: it reads every `tsconfig*.json` at the top of `apps/web`, follows each `extends` chain to whichever config declares `include` (so the generated SvelteKit one is read, not transcribed), and requires every compilable file git reports under `apps/web` to match one.
- [x] **`check_production_env.mjs` carried a full inline copy of § 709's affirmative set, and § 709's own guard could not see it.** Closed 2026-08-26 ([decisions § 752](../architecture/decisions.md)). Surfaced by the typecheck above (the copy's `flagOn` param was an implicit `any`). Not a divergence today — the four literals matched — but the guard that exists to stop one walked `src/` and `lambda/` for `.ts` and `.svelte`, and this copy was a `.mjs` under `scripts/`, outside both. The release guard now imports the canonical `isTruthyFlagValue` (Node 24 strips types off the `.ts` import, so the plain-`node` release and CI invocations are unchanged) and the guard's walk covers `scripts/` and `.mjs`, verified by restoring the copy and watching it fail by name.
- [x] **The same hole outside `apps/web`.** **CLOSED 2026-08-27** ([decisions § 757](../architecture/decisions.md)). Re-measured before anything was written: **35 tracked `.mjs` plus one `.js`, 386 errors**, not 33 and 369. The filing was right on its own date and the arithmetic reconciles exactly — `check_doc_checkboxes.mjs` and its test (13 errors) landed in the *same commit* § 752 did, from another slice of the same round, and a same-commit edit to `check_pgtap_refusal_assertions.mjs` moved it 35 -> 38; 369 + 13 + 3 + 1 = 386. So it was understated, and only because the tree moved under it. **Two claims were wrong on any date.** "Three package roots" is wrong in both directions: the sentence names *four* trees and only *two* are package roots (the repo root and `apps/backend`; `.github/` and `infra/` carry no `package.json`, which is why nothing had ever pointed a compiler at them). And the set is not `.mjs`: `infra/modules/web-stack/functions/www_redirect.js` is a **CloudFront Function** running at the edge on every viewer request to the site, in no program either — § 752's own shape repeating, where the production artifact is the one the filing did not count. Closed with two roots over a shared strictness base (`tsconfig.scripts.json`, and `tsconfig.cloudfront.json` because `cloudfront-js-2.0` is neither Node nor a browser), plus `scripts/tsconfig_coverage.test.mjs`, which names no tree and derives its one exemption from where `supabase/config.toml` sits. 251 of the 386 were unannotated parameters; the residue was **ten guards that were not guarding**, worst among them `check_migration_online_safety.mjs` passing a bare `ADD CONSTRAINT` riding beside a `NOT VALID` one on `runs` (the blocking-lock downtime it exists to stop), `check_lambda_alias_sync.mjs` letting any one of its three sources fall silently out of the alias union, `check_toolchain_pins.mjs` unable to see a `defmt-print` cache key that carried no version, and three guards that reported a clean tree over an empty input set. 271 unit tests across the 15 suites, every guard exits 0. Original filing below.
  - **The same hole outside `apps/web`: 33 tracked `.mjs` files under the root `scripts/`, `apps/backend/scripts/`, `.github/actions/` and `infra/` are in no `tsc` program, in a repo with no root `tsconfig.json` at all.** Measured 2026-08-26 during § 752: a `tsc` pass at the same strictness reports **369 errors**, concentrated in `check_ios_native_declarations.mjs` (46), `check_pnpm_overrides.mjs` (36), `check_toolchain_pins.mjs` (35) and `apps/backend/scripts/check_pgtap_refusal_assertions.mjs` (35). These are the repo's own CI guards — the files whose verdicts every merge depends on — spread across three package roots, so this needs its own root (or roots) and its own coverage guard rather than a line in `apps/web`'s. Not attempted here deliberately: § 752's guard can state `apps/web`'s coverage completely and would have to lie about the rest.

## Surfaced by the #789 repo-root typecheck-coverage sweep (2026-08-27)

- [x] **Nothing typechecks the Edge Functions.** **CLOSED 2026-08-27** ([decisions § 762](../architecture/decisions.md)). The premise held exactly and the counts reconcile, but **"47 of them production handlers" is the wrong label for that number**: 85 tracked `.ts` splits 47 non-test / 38 test, and the 47 is the count of non-test MODULES — eleven of them shared library files under `_shared/` with no HTTP surface at all, the rest the `lib.ts` / `handler.ts` / `paging.ts` halves the tests exercise. The number of deployed functions is **16**, one `index.ts` per directory (`config.toml` names seven of them, those needing `verify_jwt = false`). Sixteen internet-facing entrypoints is the alarming figure and it is smaller than the one filed; 85 is the figure the lane has to cover. **First-run count: 81** — 39 `TS2339`, 32 `TS2345`, 4 `TS2353`, 2 `TS2365`, 2 `TS2322`, 2 `TS18047` — and the § 757 shape the filing predicted did hold, with one omission producing 71 of them: no `createClient` call carried the `Database` generic, so `.from()` resolved to `never`, and the `_shared` helpers typed their client parameters as `ReturnType<typeof createClient>`, which is not what a real `createClient(url, key)` returns. **81 was a floor, not a total**: a `never` collapse conceals everything downstream, so binding the generic revealed 20 more, six of them in five files that had reported none. **The one file with zero errors was the one typed against `any`** — `delete-account/index.ts`, thirteen client parameters annotated `SupabaseClient` (i.e. `SupabaseClient<any>`), the best-typed-looking and least-checked file in the tier; typing it properly found its audit record was an `interface`, which gets no implicit index signature and so was not assignable to the jsonb column it is written into. Seven defects were more than type-level (the strava-import lookback validating one value and running on another; a nullable view owner used to build a Storage path and then passed to a NOT NULL RPC after the track was already decompressed; a jsonb column spread into an object literal; `gzipBytes` promising a `SharedArrayBuffer`-backed width its own first line would throw on; a stale declared return type; a cast erasing a nullable column; an untyped column-name table). No second generated artifact: `_shared/database.ts` re-exports `apps/web/src/lib/database.types.ts` type-only, so nothing crosses the boundary at runtime. Guarded by `scripts/edge_functions_typecheck_coverage.test.mjs`, which lifts the CI step's own `files=$(…)` expression out of `ci.yml` and runs it rather than restating the file set. **81 before, 0 after; 520 Deno tests pass, 0 fail, 23 ignored.** Original filing below. **Nothing typechecks the Edge Functions: 85 tracked `.ts` files under `apps/backend/supabase/functions/`, 47 of them production handlers, are in no type-checking program of any kind.** Found while scoping [decisions § 757](../architecture/decisions.md)'s coverage guard, which had to decide what to do about that tree and chose to exempt it *and say so* rather than pretend a `tsc` root could read it. CI runs `deno test --no-check` twice and `deno cache --no-check`, and there is no `deno check` anywhere in `.github/workflows/`. This is the same hole § 749 / § 752 / § 757 closed three times over, in the one tier that answers requests from the public internet — Stripe and RevenueCat webhooks, the account-delete and data-export handlers, the auth-email hook. It cannot be a line in a `tsc` root: the modules import over `npm:` / `jsr:` / `https:` specifiers tsc's resolver cannot follow. The fix is a `deno check` step over `supabase/functions/` in the `edge-functions` job, plus whatever it turns up — expect the § 757 shape, most of the count being one diagnostic and the residue being real. Its own slice: a different toolchain, a different failure surface, and a first-run error count nobody has measured.
- [x] **The Stripe SDK is `any` in every Edge Function that uses it, so the new typecheck stops at the payment boundary.** **CLOSED 2026-08-27** ([decisions § 765](../architecture/decisions.md)). The probe reproduced exactly and the diagnosis under it did not: **the declarations were never missing.** esm.sh serves them, Deno follows the `x-typescript-types` header, and `deno info --json` shows all ~450 of stripe's `.d.ts` files resolved with no error on any. What breaks is ambient-declaration MATCHING — esm.sh rewrites `declare module 'stripe'` to name the declaration file's own URL, and TypeScript matches an ambient module declaration against the LITERAL specifier text, so the runtime specifier matches nothing and the file it resolves to has no top-level exports. Importing that URL literally binds the whole SDK. So the fix is a `@ts-types` directive plus a three-line declaration file (`_shared/stripe.ts` + `_shared/stripe_types.d.ts`), **not** a dependency change: none of the three candidates the entry named was needed, and two would have been worse (an `npm:` specifier re-arms [§ 699](../architecture/decisions.md)'s boot-time registry dependency; a hand-written declaration is a maintained approximation of a 450-file SDK). No runtime byte changes and **no live test-mode key was involved** — the change is entirely type-level. Three further corrections. **Stripe was the only one of the tier's seven remote dependencies with this shape** (supabase-js at both targets, zipjs, sentry, cheerio and std/assert all resolve to real types, measured). **"All four payment functions" undercounts the boundary by one** — `stripe-events-webhook`, the inbound half that writes order status and seats attendees, hand-parses Stripe payloads as `Record<string, unknown>` and appears nowhere in the entry. And **binding the declarations does not finish the boundary**: what reaches a Stripe call is a function return, not a fresh literal, so no excess-property check runs and a `defualt_currency` in the params helpers compiled silently — closed with a per-call-site key assertion. **0 errors before, 2 with the declarations bound, 0 after** — both genuine, both the interface-has-no-implicit-index-signature shape § 762 found on `ThirdPartyOutcomes`. 524 Deno tests pass, 0 fail, 23 ignored. Original filing below.
  - **The Stripe SDK is `any` in every Edge Function that uses it, so the new typecheck stops at the payment boundary.** Surfaced by [decisions § 762](../architecture/decisions.md). All four payment functions (`donations-checkout`, `events-cancel`, `events-checkout`, `events-connect-onboard`) import `https://esm.sh/stripe@17.5.0?target=deno`, and the declarations that specifier serves resolve to `any`: measured with `declare const a: Stripe.Account; const bad: number = a.id;`, which type-checks. So `stripe.accounts.create(...)`, `stripe.checkout.sessions.create(...)` and every field read off their results are unchecked in a tier that now checks everything else — and worse than unchecked, since an `any` flowing into a narrowed local silently *widens* it back (`accountId` in `events-connect-onboard` had to be annotated round it). Not fixed here because the fix is to change how the dependency is resolved — a different esm.sh variant, an `npm:` specifier, or a hand-written declaration for the handful of shapes we read — not to annotate a call site, and the payment functions are the ones gated on the Stripe sign-off ([decisions § 139](../architecture/decisions.md)) so the change wants measuring against a live test-mode key.
- [x] **`stripe-events-webhook` parses Stripe's event payloads as `Record<string, unknown>`, and typing it costs a 10x eszip.** **CLOSED 2026-08-28** ([decisions § 785](../architecture/decisions.md)). **The cost was measured for the wrong import, by four orders of magnitude.** [§ 699](../architecture/decisions.md)'s 228 KB -> 2.4 MB is a VALUE import of a runtime module; a reader needs only the declarations, and a type-only import is erased before anything is bundled. `edge-runtime bundle` on this entrypoint, three ways: **761,148 bytes with no Stripe import, 761,378 with `import type` (+230 — the source text of the lines themselves), 3,373,077 with a value import (+2.61 MB, 4.43x)**. The shipped eszip carries zero `esm.sh/stripe` modules and zero of the `deno.land/std@0.177.1` polyfill tree, against the value variant's 6 and 131. § 699's second argument — that a type-only import re-arms the offline worker boot — is true in general (`deno info` finds a bare `node:http` in the SDK's graph) and **void for this function**, which already imports supabase-js at the default target and so already carries `node:buffer` + `node:process`: it is one of the seventeen § 699 left un-converted and could not boot offline before this change either. The shape is § 765's, not a third pattern: a narrow source interface per object, each pinned to `Stripe.Checkout.Session` / `Stripe.Charge` / `Stripe.Account` by an `AssertAssignable` instantiation, plus the six dispatched event types `as const satisfies Record<string, Stripe.Event.Type>` — all four proved live by injection (`TS2344` twice, `TS2820` once). **Four defects fell out.** The headline: **`checkout.session.completed` is not a payment.** For a delayed-notification method the Session completes with `payment_status: 'unpaid'` and the outcome lands days later, and the webhook marked the order paid + seated the attendee anyway while handling neither async event — a payment that never arrived left a paid order holding a seat, permanently, with no corrective event. Reachable by configuration alone: neither checkout params helper declares `payment_method_types`, so the live set is a dashboard checkbox. Both confirm arms now gate on `isPaymentSettled` before any write and both async outcomes are dispatched (**the Stripe endpoint must subscribe to them** — a deploy-checklist item, now in the docs). Then: an EXPANDED `payment_intent` read as null, which answers a refund `200 missing_payment_intent` and leaves the order holding its seat with no retry; an empty-string metadata value reached `.eq('id', '')` and 22P02'd forever; and `handleExpired` dropped its read error where both donation twins had been hardened against exactly that. **596 Deno tests pass, 0 fail, 23 ignored** (581 baseline), `deno check` 0 both sides, and every new assertion was proved to fail against the code it fixes. **Filed rather than closed:** an async refund that FAILS arrives as `charge.refund.updated` with `status: 'failed'`, which this webhook does not handle — the order stays `refunded` with its seat released and possibly re-promoted. That needs a seat-reinstatement decision, not a handler. Original filing below.
  - Original filing: Surfaced by [decisions § 765](../architecture/decisions.md), which bound the SDK's declarations for the four OUTBOUND payment functions. The fifth Stripe-touching function is the inbound one — it verifies the HMAC by hand and reads `checkout.session.completed` / `charge.refunded` bodies field by field with no SDK types, and it is the sole writer of `event_orders.status`, the thing that seats an attendee and the thing that releases capacity. Typing it against `Stripe.Event` means putting `?target=deno` in its graph, which [§ 699](../architecture/decisions.md) measured at 228 KB -> 2.4 MB per eszip; this is the function Stripe itself calls with retries on timeout, so that is a real cold-boot cost for a type-only benefit. Worth revisiting if the polyfill tree ever stops being the price of `?target=deno`, or with a small hand-written declaration for the four event shapes it actually reads — which is a defensible shape HERE, where the surface is four objects, and was not for the whole SDK.
- [x] **A destination-charge refund claws back the application fee without reversing the transfer.** **CLOSED 2026-08-28** ([decisions § 769](../architecture/decisions.md)). The fault held and the charge type held — measured from the code that CREATES the charge, not the refund: `buildCheckoutSessionParams` and `buildDonationSessionParams` both set `transfer_data.destination` + `application_fee_amount`, so a destination charge it is and `reverse_transfer` is the right instrument. **The mechanism was backwards, and the arithmetic forty times bigger than filed.** `refund_application_fee: true` does not claw our fee back: Stripe's words are *"To push the application fee funds back to the connected account, set the `refund_application_fee` parameter to `true`."* Sent alone it paid the fee TO the host on top of the ticket they already kept, so a cancelled $50 class at 250 bps cost the platform **$50.00** and paid the host **$50.00** — not "the class fee", the whole ticket plus our margin. Stripe does not treat the half-and-half state as a configuration: *"If you refund the application fee for a destination charge, you must also reverse the transfer."* Both flags now go, via a `buildRefundParams` helper so the call site can carry the `AssertNoUnknownParamKeys` guard every other Stripe call already had — this was the only money-MOVING call without it, and a typo'd `reverse_transfers` would restore the bug invisibly. **Not a reason to defer the code:** the sign-off gates prod, not code ([decisions § 139](../architecture/decisions.md)), the function still 503s fail-closed with `STRIPE_SECRET_KEY` unset, and what the sign-off now gets is a five-item pre-deploy checklist in `club_events.md § Refunds` rather than an unwritten path. Three more money flows fell out of the same read and are filed below.
- [x] **A partially-refunded registration could be neither attended nor cancelled — the whole chain was wired and dead at the last link.** **CLOSED 2026-08-28** ([decisions § 769](../architecture/decisions.md)). Found while auditing `events-cancel` for the entry above. `20270522_001` widened the buyer UPDATE policy to admit `partially_refunded`, `events-cancel` widened its order query to match, and both carry comments saying such a registration must stay cancelable — while `cancelAction` returned `'noop'` for it and a test *pinned* that, listing it beside `refunded`/`canceled`/`failed`. The buyer got `{ok:true, action:'noop'}` and the web toast said **"Reservation released"**: a success message over no refund and no freed seat. It now decides exactly as `paid` does, and the `refund_initiated_at` stamp CASes on the status read instead of a hardcoded `'paid'` (which matched zero rows and reported no error).
- [x] **A partial refund on a donation erased the whole donation from the charity's public thermometer.** **CLOSED 2026-08-28** ([decisions § 769](../architecture/decisions.md)). `fundraiser_totals` sums `amount_cents` filtered on `status = 'paid'` (`20270213_001`) and `donationStatusTransition` flipped `paid -> refunded` on any `charge.refunded` — the event ledger grew a partial branch in `20270521_001`'s companion change and the donation ledger, five lines below in the same file, did not. $5 back on a $500 donation removed **$500**. Partially closed: the status no longer moves on a partial refund (overstating by the $5 that came back beats understating by the $495 that did not, and is the only one of the two also TRUE about the status), the gap is logged for reconciliation, and the completing refund still moves it on. The exact fix needs a schema change — see below.
- [x] **`donations` cannot represent a partial refund at all, so `fundraiser_totals` overstates by whatever came back.** **CLOSED 2026-08-28** ([decisions § 776](../architecture/decisions.md), migration `20270620_001`). The filing held exactly: measured, a `charge.refunded` carrying `refunded: false, amount_refunded: 500` on a $500 donation classifies as `partial`, `donationStatusTransition` returns null, nothing is written, and the thermometer keeps counting $500 with **no later event correcting it** unless the refund completes to full. `donations.refunded_cents` now holds Stripe's **cumulative** `charge.amount_refunded`, so the write is idempotent and order-insensitive, and the CAS is compound (`.eq('status', <read>)` **and** `.lte('refunded_cents', <reported>)`) so two instalments delivered out of order cannot walk the figure back. `fundraiser_totals.raised_cents = sum(amount_cents - refunded_cents)` over `('paid','partially_refunded')` and `fundraiser_feed` shows each donor's net, because a gross figure beside a net total is two public numbers that do not add up. Two CHECKs bound the column and the lock trigger refuses a non-service-role write to it. **Deliberately larger than the filing in one place and smaller in another:** `partially_refunded + partial` is a self-transition for a donation where it stays null for an order (an order has no amount to rewrite), and a fully `refunded` row is EXCLUDED from the sum rather than netted to zero, because a pre-migration row carries `refunded_cents = 0` and netting would add its whole amount back. **Not backfilled** — see the entry below. 12 pgtap assertions added (20/20 pass).
- [x] **The host is documented as merchant of record in four places and is not one in any of them.** Surfaced by the § 769 sweep. Naming the settlement merchant is `on_behalf_of`, which no call site sends; Stripe: *"If `on_behalf_of` is omitted, the platform is the business of record for the payment."* The `club_events.md` parenthetical "they own refunds, chargebacks, and the tax relationship" is wrong on each — the platform issues the refunds with its own key, and *"For destination charges, with or without `on_behalf_of`, Stripe debits dispute amounts and fees from your platform account."* The two code comments and the engineering doc now state what the code does. **The decision itself is deliberately left open:** sending `on_behalf_of` moves settlement currency, fee structure and the identity on the buyer's card statement, and either vindicates or contradicts `/terms` §6, which tells the buyer the host is merchant of record — an owner + counsel call, now a ticked-or-not item on the pre-deploy checklist in `club_events.md § Refunds`. Note it is **required, not optional**, for a host outside the platform's region, and `buildAccountCreateParams` already accepts a `country`, so such a host can be onboarded today and every checkout for them would 502. **Documentation half CLOSED 2026-09-07** ([decisions § 1506](../architecture/decisions.md)) — and § 769's sweep undercounted: nine sites, not four. The five it missed include `club_events.md`'s own anti-pattern list and its Appendix A proposed ADR, which was queued for verbatim promotion into `decisions.md` and would have re-enshrined the disproved claim. Two non-docs sites are filed below.
- [x] **`donations-checkout`'s idempotency key is unique per invocation, so it dedupes nothing across retries.** **CLOSED 2026-08-28** ([decisions § 776](../architecture/decisions.md), migration `20270620000002`). The mechanism was exactly as filed — the mint site is unconditional and no read of any existing row precedes it — but the stated exposure was **wrong in the direction that matters**: a double-click is *not* reachable from the web caller, whose `submitDonation` sets an in-flight flag before the await and returns early while it is set. What is reachable is the retry after a failed or lost response, because the `catch` clears that flag deliberately. So a **client-side boolean was the entire defence against a double charge**, per page instance and reset on exactly the path where a retry is most likely; two tabs and any non-web caller had nothing. The filing also called dedupe a product call; measured, it is a *fact about the feature* that the key cannot come from the server — `events-checkout` reconstructs its own from `(buyer, event, instance)` **because the buyer is authenticated**, and a donor may be anonymous, so there is no identity to key on and repeat giving rules out the amount. `idempotency_key` is now a required UUID body field persisted as `donations.client_request_id` under a partial unique index; `resolveDonationIntent` resolves it before the Stripe call so the same donation id rebuilds byte-identical params and Stripe replays the session already open. A key against a different request is a 409 `params_changed`, a spent one a 409 `already_used` (reopening it charges a donor who already paid), a losing concurrent insert a 409 `idempotency_in_progress`. The row is written **before** the Stripe call, mirroring `events-checkout`: writing it afterwards left the only record of the attempt at Stripe, where the next call could not find it.
- [ ] **A refund issued from the Stripe *dashboard* reverses nothing unless the operator ticks it.** Surfaced by the § 769 sweep. No code can defend against this: the webhook processes such a `charge.refunded` identically, and host-cancel refunds are still manual pending `refund_orders_for_instance` (P2). It is a **runbook item for whoever holds the Stripe dashboard**, recorded on the pre-deploy checklist in `club_events.md § Refunds`. When `refund_orders_for_instance` is built it must construct its refunds through the same `buildRefundParams` shape.
- [ ] **A transfer reversal against a host who has already been paid out is unmeasured, and decides whether the § 769 fix can strand a buyer.** Surfaced by the § 769 sweep. Adding `reverse_transfer: true` means the refund now depends on recovering funds from the connected account; Stripe documents connected accounts going negative and being covered per `controller.losses.payments` / `debit_negative_balances`, but whether the reversal is permitted to go negative or the refund is refused outright was **not** verified — it needs operator `sk_test_` keys, not more reading. If it refuses, a buyer owed a refund gets none where previously the platform absorbed it. First item on the `club_events.md § Refunds` pre-deploy checklist.
- [x] **A column-level `revoke select (…)` is a no-op while the role holds table-level SELECT, and two migrations ship one anyway.** **CLOSED 2026-08-28** ([decisions § 781](../architecture/decisions.md)) — every claim in the filing reproduced against the live catalog, including the own-row self-read (a host reads `acct_…` today; after `20270621_001` the same query is `42501`) and the nil exposure on `donations` (0 rows to the donor of their own donation, both before and after). The sweep the filing suggested is **complete at two**, but its `grep` finds three sites and one of them is `20260707_001`'s prose warning; a lexer-based pass over all 439 migrations finds exactly the two real ones and no `revoke update (…)` / `revoke insert (…)` sibling. Two corrections to the filing's mechanism: the withheld set had to be **wider** than the five it names — `refunded_cents` and `client_request_id` arrived after the lockdown was meant to exist and a re-grant is cumulative, and `display_name` is what `fundraiser_feed` nulls on an anonymous row — and `20270408_001` did not *introduce* the table grant, it version-controlled one Supabase's default privileges had already made (measured: a fresh `public` table lands with `anon=arwdDxtm`). Fixed by `20270621_001` (two `revoke` + two `grant … (cols)`, no relation lock), registered column-by-column with its reason in `column_grant_lockdown_registry_test.sql` (34 rows, both new tables pinned by `role_grant_matrix_test.sql` too), and closed against a repeat by a static guard with **no allowlist**: `check_migration_column_revoke_noop.mjs` replays the set and reports a column revoke only while the table-level grant survives to the end, so a repair is a migration rather than an edit to the guard. Original filing below. Surfaced and **measured** by the § 776 sweep, against the live local catalog: on `donations` (`20270213_001`, five columns) and `instructor_payout_accounts` (`20261229_001`, `stripe_connect_account_id`), `has_column_privilege` is **true** for both `anon` and `authenticated` on every supposedly-revoked column, table-level SELECT is granted, and `pg_attribute.attacl` is null for every column of both tables — no column ACL exists at all. Postgres: *"if a role has been granted privileges on a table, then revoking the same privileges from individual columns will have no effect"*, and the grant matrix (`20270408_001`) grants it. `20260707_001` **documents this exact trap in its own header** and prescribes the fix — revoke table-level, then re-grant SELECT per public-safe column — and both later migrations reached for the broken shape anyway. Exposure differs sharply between the two: `donations` has no permissive client SELECT policy, so a client read returns zero rows and the loss is only the stated defence in depth; `instructor_payout_accounts` has one (`user_id = auth.uid()`), so a host's raw `acct_…` id is readable by its owner **today**, against that migration's explicit *"even the own-row SELECT policy should not hand the raw id to the client"*. Not fixed in the § 776 branch because the correct shape lands in two shared registry test files — `role_grant_matrix_test.sql` requires every table stay readable by `authenticated` (satisfied by the re-grant) and `column_grant_lockdown_registry_test.sql` requires a per-column carve-out to declare itself with a reason — both held by other agents that round. Sized at one migration (two `revoke` + two `grant … (cols)`) plus a registry entry per withheld column. **Sweep for the shape when fixing**: `grep -rn "revoke select (" apps/backend/supabase/migrations/` finds all three sites.
- [x] **The `edge-functions` warm-cache step does not walk the declaration graph, so the typecheck fetches ~450 `.d.ts` outside the retry wrapper.** Closed 2026-08-28 ([decisions § 775](../architecture/decisions.md)). **The filing is wrong: the fetches are already inside the retry.** `--no-check` skips TYPECHECKING, not type RESOLUTION. Measured on a cold `DENO_DIR`: the warm step downloads **776 URLs, 458 of them `.d.ts`** — `stripe@17.5.0/types/index.d.ts` and 285 further stripe URLs among them — and `deno check` immediately afterwards downloads **nothing**; run with no warm step at all, `deno check` downloads **403 URLs, 362 `.d.ts`**, every one a subset of the warm step's 776. So the ~450 figure is about right as a declaration count and wrong about its side of the wrapper, and a retry around the check would guard nothing. (`deno check --deny-import`, the instrument the filing used, is not one: on deno 2.9.3 it exits 0 while downloading. The cache file counts can tell warm from cold; it cannot.) What was asserted nowhere is the property that makes this true — that the warm step and the typecheck derive the **same** file set. Narrow the warm step to `*.test.ts` (which the sibling test step once was) and ~450 declaration fetches move outside the retry, making the filed hazard real. `edge_functions_typecheck_coverage.test.mjs` now lifts and runs **both** `files=$(...)` expressions rather than restating either, and fails when the warm set stops covering the checked one — verified failing against exactly that mutation. `deno install` is the one command left in that job with no retry; covered by the restored cache in the steady state, noted rather than pre-emptively wrapped. Original filing below. **The `edge-functions` warm-cache step does not walk the declaration graph, so the typecheck fetches ~450 `.d.ts` outside the retry wrapper.** Pre-existing since [decisions § 762](../architecture/decisions.md), restated in [§ 765](../architecture/decisions.md) rather than introduced by it. `deno cache --no-check` exists in that job specifically to absorb an esm.sh 5xx behind three retries, but `--no-check` skips type resolution, so stripe's declarations are pulled by `deno check` itself. On a warm cache (the steady state — the cache key is `hashFiles('deno.lock')` and the previous run's `deno check` populated it) nothing is fetched, verified with `deno check --deny-import`. On a cold one a CDN blip fails the lane with a bare fetch error. Dropping `--no-check` from the warm step would run the whole typecheck twice; the honest fix is probably a retry around the check itself.
- [x] **`strava-import` reports `rate_limited` and no client declares it.** **CLOSED 2026-08-27** ([decisions § 766](../architecture/decisions.md)). The premise held; the scope did not. `rate_limited` names **one of four** ways the page walk stops short of the end of the lookback window — the other three (a non-2xx upstream, a body that is not an array, the 20-page / 1000-activity cap) had **no field at all**, so declaring the flag and adding a line of copy would have left three of the four still reporting a truncated import as a finished one. `backfill` now returns `complete`, raised only on the two end-of-window exits. **The activities are not lost by the truncation — they are lost by the report**: there is no advanced cursor anywhere (`after=` is derived from `Date.now()` on every call, `sync_cursor` is written by nothing, `last_sync_at` is read by nothing but the two tiles), so a re-sync re-walks the same window — but neither client offers a way to raise `lookbackDays` above 90, so a runner told "sync complete" does not sync again and ninety days later the rest is unreachable. A found-and-fixed second sentence: `last_sync_at` was stamped after **every** walk, so a throttled sync also left the tile reading "Last synced just now"; it is now inside the `if (complete)`. Both clients grade the response through the new `strava_sync_result` parity pair instead of casting it — only an explicit `complete: true` earns the success copy, the opposite fail-closed direction to `cloudExportShortfall`'s and for a stated reason. A third defect nobody had filed: the phone dropped `failed` entirely, where web has reported it since that toast was written. The result does **not** join `import_failures`' vocabulary — that records per-activity refusals, and a truncated walk never saw the activities it skipped. `strava-import` is the only importer with this shape (parkrun fetches one page and fails loudly; the two ZIP importers walk a local archive and report their caps). Original filing below. Surfaced by [decisions § 762](../architecture/decisions.md), where widening `backfill`'s stale return type showed the flag has always been in the response body. Its own comment says it is there "so the client can show 'Strava is rate-limiting us, try again in 15 minutes' rather than treating partial as success" — but `StravaSyncResult` in `apps/web/src/lib/integrations/strava.ts` declares `imported` / `skipped` / `failed` / `athlete_id` only, `ApiClient.syncStrava` returns an untyped map, and no surface on either platform reads the key. So a rate-limited partial import still presents as a completed sync on both clients. Web-first per [§ 24](../architecture/decisions.md), one field on the result type plus a line of copy in six catalogues; left out of the typecheck slice because it is a client change, not a backend one.
- [x] **Neither client can ask `strava-import` to look back further than 90 days, so a truncation left too long has no in-app recovery.** Closed 2026-08-28 ([decisions § 768](../architecture/decisions.md)). Measured exactly as filed: two call sites, `syncStrava()` on `/settings/integrations` and `api.syncStrava()` on the Strava tile, **neither passing an argument**. Both now offer the window — a select beside "Sync now" on web, a "Sync older history…" picker in the tile menu on mobile — with the plain sync left at 90 for the reason the filing gives, and the three windows plus the maximum living in the `strava_sync_result` pair beside the fields that grade the answer, since what a client may ask for and what it may believe about the reply are one contract. A web guard pins the maximum to the function's own `400 invalid_lookback_days` bound. The pointer to the ZIP importer was already permanent under the card; what was wrong was its wording, which said the sync pulls 90 days. Original filing below. Surfaced while closing the entry above ([decisions § 766](../architecture/decisions.md)). The Edge Function accepts `lookbackDays` up to 365 and validates it properly; web's `syncStrava(lookbackDays = 90)` has no caller that passes anything, and `ApiClient.syncStrava` the same. That is what puts the 90-day fuse under a false "complete": a runner who now sees the partial banner can recover by syncing again, but one who dismissed the old "sync complete" and comes back in four months cannot reach the missed activities from the app at all. The Strava ZIP importer is the real answer for that case and is not offered anywhere near the sync affordance. The durable fix is a lookback control on the partial banner's retry path (surface the field the function already accepts) plus a pointer to the ZIP importer when the gap is older than the maximum — not raising the default, which would make every routine sync twenty times heavier against Strava's per-user budget.
- [x] **The 20-page cap tells the runner to sync again, and a re-sync cannot get past it.** Closed 2026-08-28 ([decisions § 768](../architecture/decisions.md)). Every claim held — `sync_cursor` appears in the DDL, two export column lists, the two generated type files and four doc lines, and is written by nothing. A truncated walk now records the window still to walk (`{v, from, after, before}`, epoch seconds) and the next sync resumes it; measured against a simulated 1,250-activity window, the first sync spends 20 fetches and records a cursor, the second starts past the 1,000th activity, finishes in 6 and clears it. **The rule the obvious design gets wrong is the containment test**: `after=` is recomputed from the clock, so a truncated walk's frontier is always newer than the floor the next same-lookback walk asks for, and comparing against it refuses every re-sync there is — hence `from`, the oldest instant the *job* covers, stored separately. The walk direction is measured off the page rather than assumed, because Strava orders the list oldest-first with `after` set and newest-first without, and that decides which end of the walked span is the frontier. No migration: the column has existed since `20260405_001` and is already in both generators and the Art 20 export. **Found while measuring: the walk has seven exits, not five** — `fetch` and `resp.json()` each reject, escaping `backfill` into a 500 `internal_error` that discarded every count the walk had earned; both now truncate like the rest. Original filing below. Also from § 766. The walk always restarts at page 1 with `after=` recomputed, so a runner whose window holds more than 1000 activities re-skips the same 1000 and hits the cap again — the new "sync again to finish" copy is, for exactly that runner, an instruction that can never succeed. It is unreachable in practice (1000 activities inside 90 days is 11 a day) and was left alone rather than papered over: the honest fix is for the cap exit to page from where it stopped, which means a real cursor on `integrations.sync_cursor` — a column that exists, is exported, and is written by nothing. That is a backend design change with its own resume semantics, not a client one, and it should not be smuggled in behind a copy string.
- [x] **`handleConnect`'s `if (!code)` branch is unreachable, and the mobile callback is absent from the committed dev allowlist.** From issue #789 section S; closed 2026-08-28 ([decisions § 768](../architecture/decisions.md)). Both held. The branch is gone, and so is a **second** dead one the filing did not name — the trailing `unknown_action` after the three action `if`s, dead for the same reason one level up; the dispatch is now a `switch` the compiler proves exhaustive. The one adjacent branch that **is** reachable, `handleConnect`'s `'<missing>'` log arm on an empty `redirect_uri` (the top level rejects an empty `code` but accepts an empty `redirect_uri`), is correct and left as found — `parseRedirectAllowlist` drops blanks, so an empty claim can never match. The allowlist entry was **not** only a dev-file matter: `threkir://strava-callback` was missing from `.env.example` too, which is the file an operator copies to configure production, where a whole-string comparison against a web-only list answers every in-app connect with 400 `invalid_redirect_uri`. Both files now carry it, pinned by a guard reading `kStravaCallbackUri` out of the Dart client.
- [x] **`check_migration_online_safety.mjs` splits SQL on `;` without tracking `$$`-quoted bodies.** Closed 2026-08-28 ([decisions § 770](../architecture/decisions.md)). **The fragmentation held and was far larger than "a function body"; "it fails in the safe direction" did not hold at all.** Over the 435 committed migrations the split turns **3441 statements into 7366 fragments** — 504 dollar-quoted bodies carry 3540 semicolons between them, so more than half of what the guard graded was never a statement. The unsafe direction is not the split but the comment strip that ran before it: `--` was eaten to end of line before anything knew whether it opened a comment, so a statement whose *literal* contains one loses its own terminator, is glued to the next line, and is vouched for by that line's `not valid` — a bare `ADD CONSTRAINT … CHECK` on `runs` reading as clean, which is the blocking-lock downtime the guard exists to stop and the § 757 defect one layer up. The shape is committed: `20270518_001` line 33 carries `-- the ` inside a `comment on index … is '…'` literal. A third variant needs no comment — `check (state <> 'not valid')` hands over the escape hatch. Closed with `apps/backend/scripts/sql_lex.mjs` (line + **nested** block comments, `''` and E-string escapes, quoted identifiers, `$tag$` bodies, throws on anything unterminated rather than consuming to EOF) rather than a wider regex; literals are blanked for the scan so their contents cannot vote. Measured with the cutoff ignored: **no committed migration changes verdict**, 27 change only the quoted text. The `check_compliance_drift.mjs` sibling closed with it — see the next entry.
- [x] **`check_compliance_drift.mjs`'s new-Edge-Function detector reads a hunk header as a claim about a file.** Closed 2026-08-28 ([decisions § 770](../architecture/decisions.md)), filed as a sibling of the entry above and understated by being called merely "the same character". Measured over the last 2000 commits: **109 touches of a `functions/*/index.ts`, 6 genuinely new, and the detector fired 28 times — 22 false positives, 0 false negatives**, the most recent being `d67ab6fdf`, the commit before the branch's base. git answers this exactly (`--diff-filter=A`) and is now asked; no heuristic was ever needed. Two § 757 shapes went with it: `git diff … -- ${path}` interpolated into a shell string splits a filename with a space into two pathspecs and reads the diff of neither (now `execFileSync` with an argv array), and two per-file `catch { return '' }` swallows turned a git failure into an empty diff that disabled that file's rules while reporting a pass. **Nothing could see any of it because the file had no exports and no tests** — the decisions are now pure functions over `{changed, added, diffFor}`, `main()` is the only part that talks to git, and the new suite pins the old detector's premise beside the new verdict.

- [x] **`check_toolchain_pins.mjs` matches a `defmt-print` install to a cache key per FILE, not per job.** Closed 2026-08-28 ([decisions § 770](../architecture/decisions.md)). **Both halves of the filing were wrong.** "The guard still fails, just with an extra misleading line" describes one of the two arrangements; mirror it — job B's key naming job A's version, so it restores A's binary and B's `command -v defmt-print ||` wins — and the guard prints **`[OK] cache key matches defmt-print 1.1.0`** over run 31623789083 repeating, the incident the file's own header records. And it is not "a shape the repo does not have today": `ci.yml` has held two sim jobs (`sim-firmware`, `sim-scenarios`) since the day it was written, agreeing only by hand. "Per-job matching needs step-boundary tracking" is true and already built: `parseSteps` in the sibling `scripts/check_ci_diagnostics.mjs`, tested. The fix is an import plus a line offset. A job that caches the binary and never installs it is now named too; an install present but not a valid pin stays silent so the range error is not buried.

- [x] **An unnamed step running a non-guard check in a bundled job is still exempt from per-step diagnosis.** Closed 2026-08-28 ([decisions § 770](../architecture/decisions.md)) **by measuring it and declining to widen.** Across the **6** bundled jobs there are **33** `run:` steps, **31** already in the rule's subject, and the exempt set is **exactly 2**: `npm ci` in `parity-types` and the `go install` that fetches actionlint in `workflow-lint`. Both are setup, and § 764's motivating case — actionlint's *invocation* — is already a named, diagnosed step, so the filing's premise has no instance left in the repo. **The cheap partial close would newly catch 0 of them.** The other implementation of the same sentence is not equivalent and is the one a reader reaches for, because `isGuardStep` also decides bundling: widening the predicate takes the bundled set from **6 jobs to 15** and the subject from **31 steps to 127**, of which **89** print no `::error::` today (`cp .env.example`, `npm run check --workspace=apps/web`, every "Parse version from tag" in every release workflow). One filing, two readings, 0 against 89. What was actually missing is that the blind spot was invisible — the guard reported 31 steps diagnosing themselves and nothing about the ones it never asked, which is why this entry had to ask someone to re-derive it. It now lists them as `[SKIP]` with job and command and counts them in the summary, and the real-tree test asserts no exempt step invokes anything from this repo — the property the widening was reaching for — without pinning a count.

## Surfaced by the #789 mobile challenge-create build (2026-08-26)

- [x] **`SocialService.createChallenge` is dead code in both twins — defined, never called.** Closed 2026-08-26 ([decisions § 754](../architecture/decisions.md)). The filing was accurate, which made it the rarity of the round: of `SocialService`'s 58 public methods it is the only one with zero references anywhere under `apps/mobile_*/lib`, `apps/mobile_*/test` or `packages/` — `myActiveChallenges`, `recomputeChallengeCompletion` and `fetchCancelledInstancesForEvents` all turn up in a naive sweep and are all called from inside `social_service.dart` itself. Closed by **building** the surface rather than deleting the method: § 24 makes mobile additive, `SocialScreen`'s FAB seam is a general contract that Challenges simply had no create surface to feed it, and § 747 had already shipped `rateLimitCreateChallenge` into all seven ARBs — a sentence no mobile code path could reach, which deleting the method would have stranded. `widgets/challenge_form_sheet.dart` + the hoisted FAB + 20 widget tests; `parity.md`'s row is now ✓ / Partial / ✓.
- [x] **Web's challenge goal field asks for the stored number with no unit named, so a typed `100` on a distance challenge is a 100 metre goal.** Closed 2026-08-27 ([decisions § 758](../architecture/decisions.md)). Fixed the durable way rather than by copying the mobile suffix onto web: mobile's inline `challengeGoalToStored` became the registered `challenge_goal` parity pair (web `social/challenge_goal.ts` ↔ `challenge_goal.dart`, 16 web / 14 Dart tests), and both editors now convert through it, echo the converted figure back in the leaderboard's own formatter where a conversion happened, and clear a typed number when the metric changes. One correction to the entry below: the catalogue count is **seven**, not six — `pt-PT.ts` landed in § 755 the day before. Original entry: Surfaced building the mobile form (2026-08-26, [decisions § 754](../architecture/decisions.md)). `challenges.goal_value` is metres for `distance` and `vert`, seconds for `duration`, a bare count for `activity_count` / `streak_days`; `ChallengeEditor.svelte` renders one `<input type="number">` labelled `challenges.goalOptional` ("Goal (optional)") with no suffix and no hint. The value is valid, the insert succeeds, and the progress bar fills on the entrant's first run — there is no error state to notice. The durable fix is web's half of what mobile now does: a unit suffix derived from the metric and the reader's `preferred_unit`, converted at the boundary, reusing `units.svelte.ts`. Costs three new keys (hours / activities / days suffixes) across the six catalogues plus a Playwright update, which is why it was not bundled into a mobile slice with four other agents in the same round. Mobile is deliberately stricter until then — the two clients agree on what is *stored*, not on what a bare `100` means.
- [x] **Nothing checks that a challenge's stored goal is achievable inside its own window.** Closed 2026-08-27 ([decisions § 758](../architecture/decisions.md)) by `challenges_goal_ck` (migration `20270615_001`, `NOT VALID` + `VALIDATE`), mirrored client-side by `checkChallengeGoal` so the refusal is named inline rather than arriving as a 23514. **Two corrections to the entry below.** `duration` is NOT window-bounded: the aggregate sums `duration_s` over runs whose START falls inside the window, so a run begun a minute before it closes carries its whole duration and a 112-hour Moab finish satisfies a goal longer than the window itself — a constraint refusing it would refuse a legitimate ultra challenge, and both suites plus the pgtap file pin that it is allowed. And the *bigger* hole was the one the entry did not name: `goal_value` accepted **zero**, which `recompute_challenge_completion` (returning early only on NULL, then comparing `value >= goal`) turns into a completion badge, a `completed_at` stamp and a `challenge_complete` notification for every participant on the nightly sweep, while both clients render the board as goal-less — and web's field carried `min="0"`, so it was reachable. Original entry: Same origin. Even with a unit-labelled field on both clients, `goal_value` accepts any positive number: a `streak_days` goal can exceed the days between `starts_at` and `ends_at`, and a `duration` goal the seconds between them. Both are row-local arithmetic over columns the row already carries, so this is a genuine `challenges_goal_ck` rather than client validation — and it belongs in the same change as the web field above, since both clients then owe the matching inline message. `20270302_001` is the precedent for widening a `challenges` CHECK.

## Surfaced by the #789 column-grant sweep (2026-08-27)

- [x] **`public.events` grants SELECT per-column rather than table-wide.** Closed 2026-08-27 ([decisions § 759](../architecture/decisions.md)). **Both observations reproduce and both conclusions drawn from them are wrong.** `has_table_privilege('anon','public.events','SELECT')` really is `false` while `set role anon; select count(*) from public.events` returns 12 — a `count(*)` names no column, and Postgres checks column privileges for the columns a query names — and a direct `select xmin`/`ctid`/`tableoid` as `anon` or `authenticated` really does raise `42501`. **Four tables share the shape and every one of them is a lockdown, not an accident**: `user_profiles` withholds 16 columns (`20260707_001` + `20260810_001`), `events` 3 (`meet_lat`/`meet_lng` + the Stripe Connect `host_user_id`), `clubs` 1 (`invite_token`), `checkpoint_crossings` 5 (the Art 9 weigh-in fields + `recorded_by`) — 25 in all, each with a named `SECURITY DEFINER` replacement read path, and `20270408_001`'s header states the invariant outright. **So there is no migration in this change**: the filed table-wide `grant select` would hand every signed-in caller an organiser's home coordinates, every public club's join token, every runner's date of birth and every racer's medical hold, and would fail `role_grant_matrix_test` and `rls_user_profiles_column_lockdown_test` on the way. **The consequence the filing was built on does not occur**: [§ 753](../architecture/decisions.md)'s operator 1 is a permissive RLS policy whose `USING` references `xmin`, and a policy expression is not column-privilege-checked — measured directly, `anon` reads `events` through it normally. § 753's aside is true only of operator 2's `SECURITY INVOKER` replacement relation, which is precisely why operator 2 keeps its owner bypass. Guard population unchanged at **168 assertions / 5 expected survivors** across 259 files. **The real defect is the drift nobody looked for**, and it has fired in prod: a re-grant is cumulative, so a column added after a lockdown is deny-by-default until an explicit grant lands — `clubs.is_verified` (`20260909_001`) shipped that way and took down every non-service-role read of `clubs` with `42501`, caught a day later by eleven `api_client` failures and repaired by `20260913_001`. The `select('*')` source scans police the call-site half of that class; nothing policed the grant half. `column_grant_lockdown_registry_test.sql` now names all 25 withheld columns with a reason each and pins the withheld set — the direction that detects drift, since pinning the *granted* set does not move when a column arrives ungranted. Four probes, each rolled back, trip the intended assertion by name.
- [x] **The write side carries the same shape with the mirror-image drift, and no registry.** Measured 2026-08-27 alongside § 759, deliberately not bundled. Four tables hold column-level INSERT/UPDATE grants for `authenticated`: `achievements` (UPDATE `is_public` only; the other 8 columns are server-awarded), `challenge_participants` (`team_club_id` only — `completed_at` is server-recomputed), `coach_messages` (`archived_at` + `reaction`, [decisions § 29](../architecture/decisions.md)), `event_attendees` (`attendance` + `order_id` blocked on **both** verbs since `20270520_001`). Its drift runs the other way — a column added to one of these is silently **unwritable**, `42501` on a PostgREST PATCH, with no integration suite to catch it the way `clubs.is_verified` was caught — and **three of the four still hold a table-level INSERT beside a column-scoped UPDATE**, which is the exact asymmetry [§ 584](../architecture/decisions.md) records for `event_attendees` before `20270520_001` closed it (a table grant implies every column, so the UPDATE lockdown was reachable through INSERT). Each of the four has an individual pgtap pin; none has the class-level one. Extending `column_grant_lockdown_registry_test.sql` to writes is the durable fix, but it needs the same per-column provenance pass the SELECT side got — per role and per verb, where the write side has more legitimate variety — so it is a slice, not a line. **CLOSED 2026-08-27** ([decisions § 763](../architecture/decisions.md)) — both counts hold and the headline defect has never fired, while the aside at the end of the sentence was live on `main`. Only **one** of the four column-scopes INSERT (`event_attendees`, `20270520_001`), so "silently unwritable" is unconditionally true only there; on the other three a new column would have been insertable-and-not-updatable, write-once by accident — and no column has been added to any of the four since its lockdown landed, measured across all 434 migrations. `anon` holds **no** column-level write grant at all, so the write side was never the read side's both-roles-identical shape. The § 584 asymmetry is real on three tables and **bites on one**: `achievements` has no permissive INSERT or DELETE policy so both grants are dead (measured — the INSERT raises `42501 new row violates row-level security policy`, the DELETE affects zero rows), `coach_messages`' `WITH CHECK` already confined a client to `role = 'user'` so only a client-chosen `id` and a backdated `created_at` were reachable, but `challenge_participants` was a live forgery path: join and leave are both own-row verbs, so `delete` + re-`insert` with `completed_at = now()` wrote the column the lockdown reserves for `recompute_challenge_completion`, verified under the participant's own JWT. `joined_at` was backdatable the same way. **A fifth defect nobody filed:** swept across every column, table and DML verb, `coach_messages` was the *only* place in the schema where `anon` held a privilege `authenticated` lacks — a table-level UPDATE against `authenticated`'s column-scoped `(archived_at, reaction)`, left by `20260518_001` revoking from `authenticated` only and then version-controlled by `20270408_001` from the drifted prod schema. Inert (the owner policy reads `auth.uid() = user_id`) but that is RLS covering for a grant, which is what § 759 refused on the read side. `20270616_001` column-scopes the two INSERTs to exactly what the clients send, revokes `anon`'s UPDATE, and revokes `achievements`' dead INSERT + DELETE so the class invariant needs no exemption. GRANT/REVOKE take **no lock on the target relation** (measured), so no online-DDL machinery applies. `column_grant_write_lockdown_registry_test.sql` is the class guard — six assertions, seven probes tripping all six by name; it caught two unregistered `coach_messages` INSERT withholdings on its first run, and the first draft of the new `challenge_participants` pin was § 741-vacuous (a non-existent challenge id raised the same `42501` from the `WITH CHECK`) until it was rewritten to perform the exploit's own DELETE first.

## Surfaced by the #789 round 24 Stripe webhook typing (2026-08-28)

- [x] **An asynchronous refund that FAILS is not handled, and the seat is already gone.** **CLOSED 2026-08-29** ([decisions § 789](../architecture/decisions.md)). The mechanism was real and slightly wider than filed — `charge.refunded` fires when a refund is *created*, including one Stripe holds `pending` because our balance does not cover it, so the seat-released-before-the-money-settles window is not confined to delayed-notification methods. `promote_event_waitlist` was read and confirms the seat is genuinely unrecoverable: nothing else re-seats. Two claims did not hold. **The event was wrong**: `charge.refund.updated` is *deprecated* in Stripe's own refund-events table ("Listen to `refund.updated` for updates on all refunds instead"), and the failed-refund section names `refund.failed` outright — the compiler suggested the deprecated one because it is the nearest string to a typo, not because it is the event to handle. All three refund-lifecycle events are now handled (`refund.failed` / `refund.updated` / the deprecated `charge.refund.updated`, for an endpoint still pinned below API version `2024-10-28.acacia`), gated on the *refund's own status* rather than the event type — `refund.updated` also fires when Stripe attaches an acquirer reference number, and acting on the type alone would walk a good refund back. **And `charge.amount_refunded` walking down is a red herring**: a Refund object carries no cumulative total, so nothing on this path reads it. **The proposed repair was also wrong**, measurably: CAS-ing back to `paid` would render "Refund in progress" forever (`refundPending = status === 'paid' && refund_initiated_at != null`, and the stamp is never cleared), present a seatless cancelled registration as a live one to `enforce_paid_order_for_priced_event`, and offer the buyer a button firing another automated refund at the rail Stripe just said cannot deliver. Built instead: a terminal `refund_failed` on **both** ledgers (migration `20270624000001`) — excluded from every seat predicate and from `fundraiser_totals` by construction, reachable only from `refunded`, seats deliberately untouched, and reversible only by the refund actually landing. The donation half of the filing was right; § 785's note that the twin "had already been hardened" was about the *read* hardening of § 769/§ 776, not about refund failure, which neither ledger handled.

- [x] **A `refund_failed` order is invisible to the person it happened to.** **CLOSED 2026-08-31** ([decisions § 825](../architecture/decisions.md)). Three of the filing's four clauses did not survive measurement. **The event page's problem was worse than a missing badge**: every order state renders inside `{:else if regState === 'already_registered'}`, and `regState` is derived from the *attendee* row, which § 789 deliberately does not restore — so the block could not draw at all and the page was offering the buyer a fresh **Register · $X** button while holding money it owed them. The new banner sits outside that block and outside the `!isPast && pricing` gate, because money we still owe does not stop being owed once the class has happened. **The donation ledger is not equally silent — it has no donor-facing surface to be silent on**: `donations` carries no client SELECT policy, so a donor cannot read their own donation row anywhere, in any state, and there is no `my_donations` RPC, route or screen. **And there is no mobile twin to write**: mobile has no paid-registration surface at all (`event_detail_screen.dart` contains neither `price` nor `stripe`; P3 in [club_events.md](../features/club_events.md)), so `parity.md`'s claimed "read-only price handoff" is not in the source. **The email decision is yes**, and its shape answers the mobile question too: a `refund_failed` **notification kind** (migration `20270701000001`) rather than a `jobs.kind`, because the notifications fan-out already turns one row into the inbox entry, the email and both pushes in all seven locales — and the inbox is the only one of those four mobile renders, and the only surface a donor can be reached on at all. No `kindMutePrefKey` entry: a per-kind opt-out of being told we still hold your money is not a control anyone benefits from having. An anonymous donation (`donor_user_id` null) is a recorded limit — the trigger guards on it so a 23502 cannot abort the webhook's own ledger UPDATE, and that donor stays a § 789 worklist item. Pinned by `refund_failed_notification_test.sql` (11/11 against the live local database), two Flutter widget tests that render the row, a Playwright case, and `order_status_copy_guard.test.ts`, which reads both ledgers' CHECK out of the newest migration to restate it and fails if the copy names a status the ledger cannot hold.

- [x] **A failed PARTIAL refund has no ledger representation, only a log line.** **CLOSED 2026-08-31** ([decisions § 823](../architecture/decisions.md)). The headline reproduces exactly: both reversal handlers end a failed partial with a `console.error` and `skipped: 'no_transition'`, no write — and the only other trace is `webhook_events`, whose three columns are `(provider, event_id, received_at)`, so nothing in the database relates the delivery to a ledger, an amount or an outcome. `event_orders` really does carry no refunded amount (`20261229_001`: `amount_cents`, `platform_fee_cents`, `refunded_at`, nothing else). **But `refunded_cents` was never accumulated**, and that sentence is the load-bearing error: `refundedCentsOfCharge` returns Stripe's own CUMULATIVE `charge.amount_refunded`, and the CAS carries `.lte('refunded_cents', …)`, so the column is a monotone LATCH on an externally-computed total with no `+=` anywhere. The choice was never accumulate-vs-derive; it was mirror-an-external-total vs sum-a-local-set. **And summing the local set is the one thing that had to be refused.** `charge.refunded` carries a Charge and no refund id, so no child row can be keyed off it; only the refund-lifecycle trio carries a Refund. A refund that succeeds instantly and emits no lifecycle event yields no child row at all, and which events an endpoint receives is dashboard configuration this repo cannot read (§ 789 measured that). Deriving the total from an incomplete set would count a succeeded refund with no row as money the charity kept — a bounded understatement traded for an unbounded OVERSTATEMENT on a public thermometer. So `refunded_cents` stays the mirror, and the child rows are the authority for REVERSALS only: `fundraiser_totals` / `fundraiser_feed` report `amount_cents - refunded_cents + least(reversed, refunded_cents)`, a correction that is strictly additive and clamped, and nothing new joins `derived_state.md` because nothing new is cached. **The direction of the error is also the opposite of what the entry implies**: the thermometer UNDERSTATES what the charity raised, which is § 789's "smaller, safe lie" — the money is ours while the ledger says it went out. Built: `payment_refunds` (migration `20270630000001`), one row per Stripe Refund, `stripe_refund_id` unique, exactly one parent ledger by CHECK, Stripe's own five-status vocabulary, `revoke all` from both client roles and no policy, plus a terminal-status latch in the write-lock trigger so an out-of-order `pending` cannot un-say a `refund.failed`. Idempotency is proved on the TOTAL rather than the row count, because the total is what a double-apply corrupts. **Two places the entry understated itself**: the worklist it asks to match does not exist for the full case either — `where status = 'refund_failed'` names the order but no amount, because the table has none — and "a schema change, not a webhook arm" is both, since nothing was writing a Refund id anywhere. Original entry: § 789 refuses to move `partially_refunded` on either ledger, for two good reasons (it is seat-bearing on orders; on donations the only alternative is non-idempotent arithmetic on `refunded_cents`). The cost is that the money discrepancy exists **only** in a `console.error`: `event_orders` carries no refunded amount at all, and `donations.refunded_cents` keeps stating an instalment that came back to us. Nothing queryable says so. The durable shape is probably a per-refund child row keyed on the Stripe Refund id — which is idempotent by construction, would let `refunded_cents` be *derived* rather than accumulated, and would give the partial case the same worklist the full case now has. Sized as a schema change, not a webhook arm.

- [x] **Which refund events reach us is dashboard configuration nothing in this repo records.** **CLOSED 2026-08-31** ([decisions § 826](../architecture/decisions.md)). The three events are now pre-deploy checklist items in [club_events.md](../features/club_events.md) beside the `sk_test_` / `ca_` / `whsec_` key items. The recording half had a better answer than a doc line: § 789's "this repo cannot read the delivery version" is true at *build* time and false at *run* time — every Stripe event payload carries `api_version`, and `parseStripeEventEnvelope` was throwing it away on arrival. It is now carried on the envelope, graded by `refundEventsApiEra` against `2024-10-28.acacia`, and logged on the refund branch beside the raw value, so a `legacy` line names an endpoint that is receiving only the deprecated `charge.refund.updated`. Compared on the ISO date prefix, because the `.name` suffix is a label and not an ordinal; an unparseable version claims `unknown`, never `modern`. **What it still cannot do** is see the endpoint's `enabled_events` list — an event that is not enabled is never delivered, so there is nothing to log and the failure mode stays the silent one. That half is unreachable from here in any form and is why the checklist entry exists.

## Surfaced by the #789 Edge Function vacuity sweep (2026-08-28)

- [x] **`donationStatusTransition — the scope default is full` is the one vacuous Edge Function test left standing.** **Closed 2026-08-28 on the round-24 integration branch**, where the two slices that could not both reach it met: § 788 measured it and left it in `EXPECTED_SURVIVORS` because `stripe-events-webhook` belonged to § 785, and § 785 rewrote the file without knowing it was vacuous. Both assertions now name `refunded` outright, and the `EXPECTED_SURVIVORS` entry is deleted in the same commit — its own staleness check fails the build otherwise, which is what made the two halves inseparable. The guard now runs with an empty exemption list, so the next survivor is a build failure rather than an entry. Original entry: Measured, not suspected: it is the single survivor of the mutation guard added in [decisions § 788](../architecture/decisions.md)arried. **Fix:** assert the pair equals `'refunded'` (the value the very next test in the file already pins independently, so nothing new is being claimed). It is filed rather than fixed only because `stripe-events-webhook` was owned by a different change in the same round. It is listed by name in `EXPECTED_SURVIVORS` in `apps/backend/scripts/check_edge_function_test_vacuity.mjs`; that entry's staleness check **fails the build** once the test is fixed, so closing this item means deleting the entry in the same commit.
- [x] **CLOSED 2026-08-31** ([decisions § 815](../architecture/decisions.md)) — **The 23 `handler_envelope.test.ts` integration tests are outside every mutation operator this repo has.** The gap was exactly as described and is now closed by `apps/backend/scripts/check_served_envelope_mutations.mjs`, which mutates the tree the HOST is serving one gate at a time and requires the case naming that gate to fail: **23 cases, 20 mutations, 24 declared kills, 0 survived, 0 unmeasured**, wired into the `edge-functions` job the aggregator already waits on. **The filing was wrong three ways.** The count is **22**, not 23 — the 23rd `Deno.test` is a placeholder with `ignore: true` and an empty body, added so `deno test` prints an ignored case instead of "no tests"; "23" is deno's *ignored* count, which includes it. The price was wrong: `functions serve` re-reads a changed module on the next request (measured under 1.4 s), so there is **no second mutant tree and no second host boot** — 58–69 s in the job that already booted everything. And "nothing suggests they are vacuous" was false for **three of them**: the bare-GET secret case was being answered 403 by the verify-token gate whatever the secret gate did; the verify-token gate had **no case at all**, so an attacker with the URL secret could complete a subscription handshake unobserved; and the non-create case asserted a 200 `OK` that the not-connected-athlete branch returns verbatim, so deleting the early return it exists to prove left it green. All three are fixed and each was a measured survivor first. A bystander found the same way: the job's `docker stop supabase_edge_runtime_backend` had never stopped anything — containers are named for `project_id`, which is `project-running`. **One defect in the operator itself surfaced only in real CI** and is fixed in the same closure: the round's reload wait accepted any probe answer that merely *differed* from the pre-mutation one, and `functions serve` restarts the runtime around a file change, so two consecutive gateway 503s let a round run against a host serving nothing — scoring its kills as killed and its **spares as moved**. Each mutation now declares the answer its mutant settles on and the wait requires that answer byte-identically twice; a gateway status may only be declared with a body no restart can produce.

## Surfaced by the #789 shared-constant sweep (2026-08-28)

Found by the class sweep behind [decisions § 787](../architecture/decisions.md). Four divergences in that round were fixed and registered in `scripts/check_shared_constants.mjs`; these are the ones that need their own change.

- [x] **CLOSED 2026-08-29** ([decisions § 790](../architecture/decisions.md)) **Exercise-name normalisation disagreed between SQL and both clients, on a PERSISTED grouping key.** `public.normalise_exercise_name` (migration `20270623000001`) is now the single SQL definition, written by explicit code point, and the four live RPCs call it; both clients respell the same class so one parser reads all three, and the class is registered in `check_shared_constants.mjs` (mutation-verified). Two `NOT VALID` + `VALIDATE` CHECK constraints make the stored key's canonicity the database's invariant, behind an idempotent batched backfill. **What the filing got wrong:** the count was **four** live functions, not seven — `20261225_001`, `20261231_001` and `20270323_001` hold definitions a later `create function` replaced, which is exactly the read-one-migration error § 787 records. Gap (a) held exactly. Gap (b) was wrong on three of the four characters it named, in the direction that understated the problem: measured on the live stack (PG 17.6, ICU provider) `\s` **matches** U+00A0, U+2007 and U+202F — only U+FEFF is excluded — and it also matches U+001C-U+001F, which neither client folds and the filing never mentions. The real defect was one layer deeper than either gap: `\s` is `[[:space:]]`, whose membership past ASCII is the collation's opinion (`chr(160) ~ '\s'` is true under the database default and false under `collate "en_US.utf8"`), so the four copies disagreed with **themselves across deployments**. And no backfill was needed: 0 of 47 persisted key rows diverged, because nothing in SQL ever wrote either column — both are stamped client-side, so the clients were always the persisting rail and the anchor. The shared fixture was extended as the filing suggested.
- [x] **CLOSED 2026-08-29** ([decisions § 791](../architecture/decisions.md)) — **The Dart rail of the CHECK-constraint class is guarded by nothing.** `check_constraint_unions.mjs` now reads Dart: **29 distinct declarations across 22 files** are registered as rails (enums read as snake_case, const string lists, `<String, …>` key maps, record lists), and a widened CHECK fails the Dart rail as loudly as the web one — proved by widening `routes.surface` in its migration and watching all three of its rails fail. **The filing undercounted: it said nineteen and its own enumeration lists twenty; measured, 29 do.** It missed `nutrition_totals.dart` `mealSlots`, `run_stats.dart` `_pbBracketLabels`, `export_job.dart` `_statusTokens`, `recurrence.dart` `RecurrenceFreq`, `recap_screen.dart` `RecapPeriod`, `preferences.dart` `DistanceUnit`, and all three of `routine_builder_sheet.dart`'s option lists. It also offered a choice of fix ("either extend `check_constraint_unions.mjs` … or register the ones that are real vocabularies in `check_shared_constants.mjs`") — the second would have split one class across two registries, which is the defect § 787 recorded, so the first was taken. Rails name `mobile_android` only and a test pins that its `mobile_ios` twin is byte-identical.
- [x] **CLOSED 2026-08-29** ([decisions § 791](../architecture/decisions.md)) — **`check_constraint_unions.mjs` covers 41 of 68 set-shaped CHECK columns.** The registry is now keyed on the COLUMN rather than on a union name and holds **all 67 live columns and 121 client rails (87 web, 34 Dart) across 46 files**; coverage is enforced in the guard, so a new set-shaped CHECK column fails until it is registered — with rails, or with an empty `clients` list and a `note` saying no client enumerates it (15 of the 67). **41 was exact; 68 was the raw replay count and 67 is the live one** — the phantom the filing named in its own last clause (`runs.kind`) is now excluded by reading `database.types.ts` for the live column set, and *registering* one fails. **"Eleven" was a large undercount: 37 declarations across 23 files live outside `types.ts`.** The two deliberate deltas the filing asked for a mode for are declared per rail as `allowExtra` / `allowMissing` rather than as a blanket wider/narrower mode — a blanket mode would tolerate the NEXT client-only value too — and each exemption is itself checked for staleness. `reports.status` turned out to need no exemption: its two resolved statuses are spelled inline with no named vocabulary, so it is one of the 15. **Not addressed here** (out of this guard's scope, still open): the two `not valid` caps from `20261124_001` that were never validated, and `text_limits.test.ts`'s VALIDATE case reading only `20270502_001`.
- [x] **CLOSED 2026-09-01** ([decisions § 867](../architecture/decisions.md)) — **Documented firmware counts are transcriptions, and five of them had rotted at once.** `scripts/check_watch_doc_counts.mjs` reads 24 firmware symbols out of the Rust that declares them and holds 111 statements across 17 present-tense docs to them, with `docs/custom_watch/` reconciled file for file so a new doc cannot arrive unread. **The design problem the filing named is solved by constraining the prose, not by widening the matcher**: a count about a symbol is written in one of a closed set of phrase templates and a sentence that will not take one loses its number instead, which is the count-free alternative the filing asked to be weighed first — five sentences took it, including one in `roadmap.md` that claimed the count was "parsed out of `ble.rs` ... rather than transcribed here" **in the same sentence that transcribed it**. Two directions, because either alone reads as complete: a template whose tail carries the wrong number fails, a template matching NOTHING fails (§ 850), and a sweep over a narrow noun list fails on any count-shaped phrase no template claimed, so a NEW stale count cannot arrive unread. **What the filing got wrong:** the population is not four numbers but about thirty, and its four examples are three — after the count-free pass, "nine characteristics" survives in exactly one doc, where the number is the point of the sentence rather than decoration. Dated logs are out of scope on § 793's own rule (`tier1_log.md`, the `apps/custom_watch/README.md` chronology, `decisions.md`), and the sweep's noun list is the honest statement of the boundary. Ten of the guard's 27 tests are mutations, in both directions. Original filing below.
  - Original: **Documented firmware counts are transcriptions, and five of them had rotted at once.** [§ 793](../architecture/decisions.md) fixed a stale GATT characteristic count in five files, a settings-version range in two, a `face::Metric` count, three `Page`-ring counts and a settings-menu row count — every one of them a number a doc restates about a Rust `enum` or `const` that nothing compares. The wire versions and frame caps are now guarded by `scripts/check_watch_wire_vectors.mjs`, but that guard reads *code against code*; nothing reads a doc's number against the enum it describes. The shape that would close it: a small registry of `(file, phrase, symbol)` — "nine characteristics" ↔ the `#[characteristic(` count in `ble.rs`, "41 built-ins" ↔ `Page` variants less the four `Screen*`, "eight rows" ↔ `MENU_ITEMS`, "40 variants" ↔ `face::Metric` — failing when the prose and the source part. Not done here because the phrasings are prose and a matcher over them is its own design problem; the alternative worth weighing first is writing every such count **count-free** (as `settings_menu.rs`'s "one checkbox per page" now is), which needs no guard at all.
- [x] **CLOSED 2026-08-31** ([decisions § 816](../architecture/decisions.md)) — **`scripts/comment_strip.mjs` cannot lex Dart `${…}` interpolation, so three test files are unreadable to every guard built on it.** `${` now opens a nested CODE region the lexer scans as code — its own strings (any quote style, raw or not), its own nested braces, its own further interpolations, its own comments (blanked, because a comment in a code region is a comment), and newlines regardless of the enclosing quote style. `r`-prefixed strings stay one verbatim span, since `$` is literal in them. An interpolation that never closes throws `unterminated string interpolation`, keeping § 793's rule that source a guard cannot read is source it must not report a verdict about. § 793's workaround is gone: `check_watch_wire_vectors.mjs` reads **every** Dart file under `apps/mobile_android/{lib,test}` — 1056 of them, up from the **7** the magic filter let through, with 514 test files skipped outright — and an unlexable file is now a hard error unconditionally rather than only when it carries a magic. **What the filing got wrong, in three places.** It said *three test files*; measured, **8 committed Dart files** could not be lexed — four distinct paths doubled by the byte-identical iOS twin — and one of the four is **library code, not a test**: `apps/mobile_android/lib/import_failures.dart`, whose CSV quoter is `'"${value.replaceAll('"', '""')}"'`. It said the newline case sits *inside a triple-quoted Dart string*; measured, `coach_screen_helpers_test.dart`'s is inside a **single-quoted** one — Dart permits a newline inside `${…}` whatever the enclosing quotes, which is strictly harder than what was filed, and that file also nests a brace-carrying map literal in the region. And it framed the workaround as merely narrow rather than structurally blind: the filter admitted a file only if one of the firmware's 16 wire magics appeared as hex *in its bytes*, so a golden spelled payload-only — or with its magic written `[0x43, 0x52, …]` — would have sat outside the sweep with nothing to say so. What it got right: the lexer tracked no code region at all, nothing was silently unchecked under the workaround, the fix belonged in the lexer, and `comment_strip.test.mjs` was the place for the cases. **Removing the workaround did find something**: with the magic filter gone, `status_color_literal_guard_test.dart`'s `_bannedHexes` — 21 six-digit colour literals in a list — joined into one 63-byte "vector" the registry had never heard of, so `dartHexConsts` now refuses a collection-shaped declaration (on the bracket, not on the comma: the registered `_goldenBlob` is `_hex('…' '…',)`, and the guard's own suite caught a comma rule that dropped it). Verified: **1969 of 1969** committed Dart files lex, up from 1961; across all **2129** Dart + Rust files the new lexer's output is byte-identical to the old one wherever the old one ran, **0** differences, and idempotent everywhere. **Left open:** the 14 `otherRail` reads over 6 Swift / Kotlin / TypeScript / Monkey C files are still taken from **raw, unlexed** source, so a commented-out declaration there can still answer for a live one — § 773's class, three languages over. Measured as latent today, not live: the nearest misses are `WatchIngestBridge.swift`'s doc comment naming `ArmedRoute.maxPoints` while that file's rail reads `maxRoutePoints`, and `grade_adjusted_pace.ts`'s `[MIN_SEGMENT_M]` in prose, which `tsConst` does not match. Closing it means teaching the lexer Swift's `\(…)`, Kotlin's `${…}` + `"""`, and TS template literals — a round of its own.
- [x] **CLOSED 2026-08-31** ([decisions § 817](../architecture/decisions.md)) — **Four CHECK vocabularies are spelled as INLINE unions, so no registry can name them.** The premise was exactly right — all four ARE enumerated by a client, anonymously, and each is now one exported type: `FinisherStatus` and `RaceSessionStatus` in `types.ts`, `ReportResolution` beside `ReportReason` in `data.ts`, `CoachMessageRole` in `coach/types.ts`, all four registered and green. **Every count in the filing was low.** `finisher_status` is written out **five** times in full (`core/data.ts` twice, `runs/event_results_csv.ts` three times), not four — the filing's own enumeration contradicted its headline — plus the `'dnf' | 'dns'` subset in `recordNonFinish`, now derived as `Exclude<FinisherStatus, 'finished'>` rather than re-typed. `reports.status` is **three** call sites, not two (`data.ts` plus two in `admin/reports/+page.svelte`). `coach_messages.role` is **six** spellings across three files, of which "every provider signature" is a third — the rest are `CoachRequestBody` and three inside `CoachChat.svelte`. Only `race_sessions.status` was counted correctly, at one. **Sixteen spellings over seven files, not the four/one/two/two the filing implied.** "About four lines apiece" holds for three of them: `reports.status` needs a staleness-checked `allowMissing: ['pending']` because the column admits three values and the moderation queue may only ever write two — a plain rail would have failed the guard on arrival. Each of the four is pinned by widening its real CHECK in memory and asserting the rail is what fails. Original filing below.
  - **Four CHECK vocabularies are spelled as INLINE unions, so no registry can name them.** `event_results.finisher_status` (`'finished' | 'dnf' | 'dns'`, written out four times — `core/data.ts` twice, `runs/event_results_csv.ts` three times, and a `recordNonFinish(status: 'dnf' | 'dns')` parameter), `race_sessions.status` (`'armed' | 'running' | 'finished' | 'cancelled'` on the race-session row type), `reports.status` (`'reviewed' | 'dismissed'` at two call sites) and `coach_messages.role` (`'user' | 'assistant'` in every provider signature) are all registered in `check_constraint_unions.mjs` as `clients: []` because there is no declaration to point at — they are the only four of the 15 no-client entries where a client genuinely DOES enumerate the column, just anonymously. Naming each as one exported type and registering it brings them under the guard for about four lines apiece. [decisions § 791](../architecture/decisions.md).
- [x] **CLOSED 2026-08-31** ([decisions § 818](../architecture/decisions.md)) — **The Dart side has no `Record<Union, X>`, so its exhaustive lookups are unguarded by construction.** The premise and both fix shapes were right; **the size was out by a factor of fourteen.** A sweep of the whole `apps/mobile_android/lib` tree found **50** distinct switch sites over a CHECK-constrained string column, not 4 — **24** naming their column's whole current vocabulary and **26** already stopping short of it. Four are coincidence rather than enumeration and are deliberately unregistered (`onboarding.dart`'s `planPresetForGoal` switches over primary-goal answers that happen to be spelled like PR brackets; `activity_timeline_list.dart`'s three read the `activities` view's own `kind` literal, which shares `run`/`lift`/`meal` with `notifications.activity_kind` without being it). The other **46 are registered across 30 files and 22 columns**, taking the Dart half from 29 declarations over 22 files to **75 over 45** and the registry from 121 rails to **171**. Of the four sites the filing named, only `_verbFor` is a full-vocabulary lookup — the three progression switches name **five of six** and let `none` fall through, which is correct and is now declared as a staleness-checked `allowMissing` (22 of the 46 carry one). **The const-list option was refused**: a list beside the switch is a second declaration nothing checks against it — the § 641 shape, a copy whose divergence is undetectable — and would have meant editing 46 production call sites. The new `switch` shape reads the labels out of the switch itself, addressed by the innermost ENCLOSING declaration (optionally class-qualified), refusing ambiguity; **no production Dart changed.** Two implementation traps worth the reader's time are in the ADR: length-preserving comment blanking, so a doc line cannot become an anchor, and a balanced-paren declaration scan, because Dart `{named}` parameter groups made `_verbFor` — the widest switch in the tree — invisible to the first draft. Left open: coverage is enforced per COLUMN, so a *new* Dart switch over an already-registered column is still not forced to register. Original filing below.
  - **The Dart side has no `Record<Union, X>`, so its exhaustive lookups are unguarded by construction.** § 791 deliberately does not register web's icon / label maps: `Record<ChallengeMetric, string>` and its ~12 siblings are made exhaustive by `tsc`, so registering them would guard the compiler's own work. Dart has no analogue — `profile_screen.dart`'s `_verbFor` switches over all 16 `notifications.kind` values with a `default:` fallback, and `gym_session_screen.dart` / `routine_detail_screen.dart` / `gym_detail_screen.dart` do the same over `gym_routine_exercises.progression`. Each is correct today (verified) and each would degrade SILENTLY to the generic branch the day its CHECK gains a value, because a Dart `switch` over `String` cannot be exhaustive. The registry's `strings` shape cannot read a switch; either the switches key off a registered const list, or a shape that reads `case '<value>':` labels is added.
- [x] **CLOSED 2026-08-31** ([decisions § 830](../architecture/decisions.md)) **The exercise key's case-folding half was collation-dependent.** `public.normalise_exercise_name` pins `collate "und-x-icu"` (migration `20270630000001`), the ICU root locale, which outranks the argument's own collation. Verified over all 1,112,063 assignable code points and a 10,031-string context corpus under six collations: **zero** disagreement, where the old derivation disagreed on 14 / 24 / 10 / 9,849 of those strings under `tr-TR` / `lt-LT` / libc / `C`. Cost 0.47 µs a call against 0.34 µs, unchanged for non-ASCII input. **What the filing got wrong, and it is the half about the clients:** "Postgres `lower(U+0130)` returns `i` + U+0307 … matching JS and Dart" is right about JS and **wrong about Dart**, which returns a bare `i` — Dart implements Unicode SIMPLE case mapping from an older table where JS implements full mapping from a newer one, and measured over every code point the two **disagree at 466**, a divergence between the halves of a registered parity pair that no rail could see. It was reachable: for `İtme`, mobile's key failed § 790's CHECK with a **23514 on a legitimate save**, one of 412 such code points. The blast radius the filing asked for is 1 code point between the two providers, 2 between ICU `en-US` and `tr-TR` (one of them **ASCII `I`** — `lower('I')` is U+0131 on a Turkish database), 3 under `lt-LT`, 1,406 under `C`, plus the contextual Final_Sigma rule a per-code-point sweep cannot see. U+0130 and U+03C2 are now folded **by name** on all three rails and registered in `check_shared_constants.mjs` (mutation-verified). **The option the filing named as the durable fix was measured and deferred, not skipped** — see the entry below.
- [x] **`revoke execute ... from public` has never withheld a function from `anon`, and 83 migrations say it does.** **CLOSED 2026-08-31** ([decisions § 827](../architecture/decisions.md)). **The class was real and is already repaired** — `20270625000001` + `20270626000001`, the `check_migration_function_revoke_noop.mjs` statement guard and the `anon_execute_contract_test.sql` / `anon_execute_registry_test.sql` state guards landed on the round-26 integration branch ([§ 799](../architecture/decisions.md), [§ 800](../architecture/decisions.md)) before this round opened. This round **re-measured it independently instead of re-doing it**, and every figure reproduces: 91 violating statements over **69 routines** in **59 migrations**, split 90 `from public`-only / 1 `from anon`-only, and post-fix 242 `public` functions with exactly **34** non-trigger routines anon may execute — all 34 on the deliberate allowlist. **Three of the filing's four numbers are wrong, and its one reassurance is inverted.** 242 is exact. **83** counts neither routines (69) nor migrations (59) nor statements (91). **31** is 33 on the `revoke execute on function` reading (41 counting `revoke all`, 48 today). **186** was never true of the image it was measured on — 117 there — and is the CI/Cloud figure, where the model says 183. And *"most are SECURITY INVOKER with RLS beneath, so the stated intent failing is not automatically a reachable hole"* is backwards: **61 of the 69 are SECURITY DEFINER**, which bypasses RLS entirely, leaving **48 SECURITY DEFINER directly-callable functions** anon held EXECUTE on — the job queue, the mail batches, the shadow-hide evaluator, `cleanup_stale_rate_limits` (an anonymous call cleared every elapsed rate-limit window, defeating every throttle in the app). The remaining gap this round closed is documentary: the house rule now lives in `conventions.md` § *A revoke names every channel the grantee can reach the object through*, and `api_database.md`'s uncomputed "31" is now a computed 48. Guard re-proved by mutation in both directions. Original filing below. Surfaced on the round-25 integration branch by `normalise_exercise_name_test.sql`'s grant assertion ([decisions § 790](../architecture/decisions.md)). Supabase ships an `alter default privileges` for the `postgres` role in schema `public` that grants EXECUTE on every new function to `anon`, `authenticated` and `service_role`, so a function arrives with an **explicit** anon grant that revoking PUBLIC does not reach — [§ 781](../architecture/decisions.md)'s column-revoke no-op, one object class over. Measured against the live catalog: 242 functions in `public`, 186 carrying `anon=X`, **83 of them named in a migration that revoked them `from public`** (`claim_next_job`, `block_user`, `coach_roster_summary`, `enforce_create_rate_limit`, `admin_unhide_target` among them). The 31 migrations writing `from public, anon` are the house form and are unaffected. Each of the 83 needs its own judgement — most are SECURITY INVOKER with RLS beneath, so the stated intent failing is not automatically a reachable hole — plus the guard that would keep the answer, which is the `check_migration_column_revoke_noop.mjs` shape for functions. A round of its own.
- [x] **CLOSED 2026-08-31** ([decisions § 831](../architecture/decisions.md)) **`gym_exercise_names()` grouped on the spelling, not just preserved it.** It groups on `normalise_exercise_name` now (migration `20270630000004`) and `uses` counts the whole group, so a tab-prefixed paste and a case variant are one suggestion rather than three; the display stays case- and spelling-preserving. **What the filing got wrong:** it said to pick the most-used spelling "as `gym_exercise_records` already does with its `array_agg(display order by started_at desc, display)`" — read, that sibling picks the most **recent** spelling. The idiom is reused, the ordering is not, and the pgtap fixture is built so the two rules disagree (the most-used spelling is the older one), verified by mutating the `order by`. Two RPCs now answer the same display question by different rules; unifying them changes a shipped records surface and is filed below. A group whose only spelling carries edge whitespace still shows it — trimming the display would mean a fourth hand-written copy of the whitespace class, which is the defect § 790 exists to have removed.
- [x] **The prerequisite is done: `gym_sets.exercise_key` is persisted, and nothing re-derives the key at read time.** Closed 2026-09-03 ([decisions § 1076](../architecture/decisions.md) + [§ 1077](../architecture/decisions.md)) — migrations `20270706000001` (column + `gym_sets_stamp_exercise_key` BEFORE INSERT OR UPDATE trigger) and `20270706000002` (keyset backfill, validated canonical / NOT NULL / length constraints, and all five readers moved onto the column). **The frozen 1,488-pair fold table is still open and is restated at the end of this file**; what is closed is the cost that blocked it — the fold is now paid once per WRITE. Measured: the same aggregate over a 15,000-set history went from 66-74 ms to 4.9-5.4 ms, and 2,241 ms to 196 ms at 500,000 sets. **Three corrections to the filing.** It says *four* RPCs re-derive the key; there are **five** — `gym_exercise_names` joined them in `20270630000004` and the entry was written against § 790's count. It offers *"a generated or trigger-maintained `exercise_key` + index"* as if the two were interchangeable: measured on a 500,000-row copy, the STORED generated column rewrites the table (2,756 ms under ACCESS EXCLUSIVE + ShareLock, filenode moved) where the plain column add took 5 ms, and it would make the very next edit to `normalise_exercise_name` a second rewrite, because Postgres never recomputes a generated value when the function under it is replaced. And the **index buys nothing** — every one of the five readers is user-scoped through `gym_workouts`, so on a 2,000-user / 500,000-set fixture the plan is identical with and without it (`Index Scan using ..._workout`, `Filter: (exercise_key = ...)`) and the key index's `idx_scan` stayed at 0. A fourth thing the filing could not have known: the persisted key is stamped by the SERVER, so a client no longer computes anything `gym_sets` stores — the case-table divergence can still split `gym_routine_exercises.exercise_key` and `exercises.name_key`, but the PR-bearing table is out of its blast radius. Original entry: [§ 830](../architecture/decisions.md) removed the *collation* dependence; the *version* one is untouched and was measured: after the two named folds the three rails still disagree at **465** code points between web and mobile, **410** between mobile and the server, **55** between web and the server (Cherokee, Vithkuqi, Garay, Medefaidrin, Latin Extended-D/E, supplementary planes). Each is a name mobile cannot persist without a 23514 from `gym_routine_exercises_exercise_key_canonical`. The fix is the one § 790 used for whitespace — a frozen table of the 1,488 code points carrying a non-identity simple lowercase mapping, named on all three rails — and it is blocked on cost, not on doubt: `translate()` over that table costs **60 µs** a call against **0.34 µs** for `lower()` (200,000 calls: 12.0 s against 0.068 s), and the four RPCs re-derive the key once per `gym_sets` row, so a 15,000-set history would pay **~1.9 s of pure folding** per `gym_workout_summaries` call. An ASCII fast path leaves non-ASCII names at 62 µs, taxing exactly the population the table protects. Sequence it after persisting the key on `gym_sets` (a generated or trigger-maintained `exercise_key` + index) so nothing re-derives it at read time — a change to the shape of the highest-volume gym table, so `migration_locks.md` governs it. An ASCII-only fold is **not** the cheap alternative: 397 code points between U+00C0 and U+052F fold identically on all three rails today and it would stop folding every one, splitting "Überzüge" from "überzüge".
- [x] **`gym_exercise_names` and `gym_exercise_records` pick a display spelling for the same key by different rules.** Closed 2026-09-03 ([decisions § 1050](../architecture/decisions.md)): migration `20270705000005` puts both on **most-recent**. **The entry's own motivating example cannot happen** — `normalise_exercise_name` folds case and whitespace only, so `'DB Bench'` (`db bench`) and `'Dumbbell Bench Press'` (`dumbbell bench press`) are different keys and both surfaces already list both under either rule; within one key the competing spellings differ only in capitalisation and whitespace. That makes most-used wrong for a reason stronger than consistency: **it is self-reinforcing on an autocomplete** — it offers the old capitalisation, the lifter accepts it (that is what a datalist is for), another set is logged under it, the counts never cross, and the re-capitalisation can never take effect. The tiebreak moved on both RPCs too: `length(display)` first, so a paste carrying a leading tab loses to its clean sibling, then the spelling under `collate "und-x-icu"`, because a bare comparison is resolved by the argument's own collation — the dependence [§ 830](../architecture/decisions.md) closed for the key itself — and each RPC's final `order by` on the returned list is pinned the same way. **The user-visible consequence is the opposite of the one filed**: the surface that changes is the AUTOCOMPLETE, not the records page, whose rule does not move (only its tiebreak became deterministic). Both mutations verified. Original entry: [§ 831](../architecture/decisions.md): the datalist picks the caller's most-**used** spelling, the records page the most-**recent** one, so the two surfaces can name one exercise differently. Both are defensible in isolation (a lifter who renamed "DB Bench" to "Dumbbell Bench Press" wants the new name on a records page and, for a while, the old one in an autocomplete they have used a hundred times) and neither is a key, so nothing is wrong downstream — but one question should have one rule. Unifying changes a shipped records surface, so decide the rule first.
- [x] **Three numeric caps hold different values on three rails, all conservative.** — **CLOSED 2026-08-29** ([decisions § 792](../architecture/decisions.md)). Three of the four are as filed and stay where they are: `club_posts.body` 1200 on exactly four surfaces vs 4096, `events.description` 1000 on two vs 2000, `training_plans.name` 80 on three vs 120. Being stricter than the column is a product decision, not a defect — a club post is a notice board and a plan name a header — so what changed is that each is written once instead of at nine call sites, with the containment now checked. **The parkrun one the entry called merely truncating is a reachable `23514`**: web carries no cap at all, not 30. The `maxlength="30"` on that page belongs to the runner handle, a different column. Both rails are now the column's own 32 — a parkrun id is `A` plus digits and approaches neither bound, and being stricter is exactly what silently truncated a 25-character id on the phone.
- [x] **Rate-limit *numbers* are unguarded; only the bucket vocabulary is registered.** — **CLOSED 2026-08-29** ([decisions § 792](../architecture/decisions.md)). Every figure verified by replay: eleven live call sites, eleven buckets, one-to-one, all ceilings exactly as filed. The two client doc comments no longer state the numbers — a transcription nothing reads was the whole complaint — and `api_database.md` gains a **bucket table** the guard parses, anchored on the backticked bucket name so it reads the row it means rather than the third number on a line. The guard also refuses something the prose could not express: **one bucket debited with two different ceilings**, which `check_rate_limit` cannot represent because it keys the counter by bucket alone. Not hypothetical — `create_report` has had four call sites over time and `clone_gym_routine_template` two; each resolves to one live site today, and the guard is what keeps it so.
- [x] **Watch-rail duplicates that are snapshot-pinned rather than compared.** **CLOSED 2026-08-29** ([decisions § 793](../architecture/decisions.md)). `scripts/check_watch_wire_vectors.mjs` reads every golden vector out of BOTH rails and compares them, plus a second registry for the values restated on more than one rail — the wire versions and frame caps, the Apple-Watch 512-point route budget across the three files that state it, the 40 m / 20 m off-route hysteresis across Rust / Dart / Swift / Kotlin, and the Minetti fit across Rust / Dart / TS / Monkey C. `LINK_SERVICE_UUID` went into `check_watch_ble_uuids.mjs`, where the service UUID's other two copies already live. **The filing was wrong twice.** It named five wire formats; there are six with paired goldens — it left out `TRK1`, the run blob, which is the only one that carries a finished run and the only one whose divergence loses data. And it said "every golden vector exists twice": `golden_vector_auto_lap_only` existed on the firmware rail alone, so both `flags3` fields were pinned on the phone only inside the 196-byte all-fields golden. It has a counterpart now. Measured first: all seventeen pairs agreed already, so nothing had drifted — the guard is a lock on a door that had not yet been tried. Proved by eight mutations, one per rule.
- [x] **Two watch docs state a firmware fact that is no longer true.** **CLOSED 2026-08-29** ([decisions § 793](../architecture/decisions.md)). Both claims were real, and both were understated. The stale "seven characteristics" was in **five** files, not one — `firmware.md`, `roadmap.md`, `privacy.md` (where it is a security claim), `quality_standards.md` and `local_testing.md`; `ble.rs` declares nine. `watch_settings.dart`'s comment was wrong at both ends, not just the top: `decode` accepts **v3 through v8**, so v1 and v2 no longer decode AND v8 was missing from the range the comment named. The sweep for the class found two more in `firmware.md` alone: it said the watch "records runs locally to LittleFS" (tier 1 has no filesystem — four 4 KiB internal-flash slots, 253 records), and it named `off_route_alert.dart` as the phone-side twin of the 40 m / 20 m thresholds when that module is the escalation-to-a-contact path and deliberately fires at 75 m / 90 s. `tier1_log.md` and § 377's own ADR text keep their counts: those are dated records, and this log rewrites no history. Four more of the class turned up in the same sweep and are fixed: `local_testing.md` said BTN3 walks "seven rows" of the settings menu when `MENU_ITEMS` is 8; `roadmap.md` said `SET1` v7 leaves "v1–v6 still decode" (v3–v6) and carried three stale page/metric counts against a `Page` enum that is now 41 built-ins plus four composed seats and a `face::Metric` catalogue that is now 40; and `settings_menu.rs`'s own doc comment priced the per-page mask at "33 checkboxes" against 45 pages, so it is now count-free. `tier1_log.md`'s §374 entry said `decode` accepts "v1–v7", which was wrong the day it was written rather than merely dated — it carries a bracketed correction rather than a rewrite.
- [x] **`body_metrics.weight_kg` has a reachable `23514`.** — **CLOSED 2026-08-29** ([decisions § 792](../architecture/decisions.md)). All bounds re-measured by replaying every migration; `weight_kg`, `target_percent_1rm` and `servings` are exactly as filed. **Three things the entry had wrong.** There is no `checkpoint_health` table — the weigh-in column is `checkpoint_crossings.body_weight_kg` (`between 20 and 400`, `20270201_001:71`); `checkpoint_health` names a *retention function*. `gym_routine_sets.target_percent_1rm` is listed as unbounded on the client and has **no client input surface at all** — no editor on either platform writes it, so there is nothing to bound. And the `min="0"` half is not reachable on the preferences card: the save path already coerces a non-positive value to null. **Four reachable raw errors were found, not one**: Settings › Preferences on BOTH fields (its `min`/`max` are decorative — the card saves from a button's `onclick`, not a form submit, so the height field's existing `max="300"` was worth nothing, the issue #677 trap again); the race-board weigh-in on both platforms, unbounded against 20..400, whose web label also read the unit off the DISTANCE preference while the parser read it off the WEIGHT one; recipe servings on mobile only (`parseTypedDecimal('0')` is `0.0`, not null — web was already clamped); and the parkrun number on web. `recipes.servings` also carries a `22003`, not a `23514`: its CHECK has no ceiling but `numeric(5,1)` does. All closed with the bound, the inline message and the i18n keys in seven web locales and seven ARBs, plus a Playwright case and two Flutter ones; the bounds themselves live once per client in `core/column_limits.ts` / `column_limits.dart`, keyed by `<table>.<column>`, and `check_shared_constants.mjs` proves each sits inside its column's own CHECK.
- [x] **`nutrition_targets`' sanity caps are a third home of two DB caps, inside an enforced parity pair.** **CLOSED 2026-08-31** ([decisions § 819](../architecture/decisions.md)). The premise is exactly right, and the mutation proves it: `body_metrics.weight_kg` is `> 0 and <= 500` and `user_profiles.height_cm` is `> 0 and <= 300`, both in `20261216_001`, so the pair's two literals were transcriptions — and widening the weight CHECK to 600 in a scratch migration set makes the existing containment guard raise **zero** errors about that column, because 250 is still inside 600. `COLUMN_CHECK_MAX` / `kColumnCheckMax` now holds what the COLUMN accepts, keyed the same `<table>.<column>`, and its guard demands **equality** with the CHECK it resolves by replay — refusing a cap below it (a stored value discarded as non-physical) as loudly as one above it, refusing an exclusive `< 500` rather than reading it as inclusive, and refusing a column whose CHECK has been dropped. Eight guard cases, six of them mutations. **Three things the filing had wrong.** The mirror-test count is **36**, not 34 — 18 each, now 19 each. **`ageYears > 120` is not a DB cap at all**: there is no age column, age is derived from `date_of_birth` and no CHECK bounds it, so it stays a stated ceiling — but it was written twice in the same file on both platforms and is now named once. And `column_limits`' own comment, written by § 792, said `height_cm` "allows anything above 0"; it caps at 300 and the registered client bound is EQUAL to it, not narrower. **The Health Connect clamp is worse than "unregistered"**: its 30–300 kg disagreed with the registered bound in BOTH directions — 300 is above the 250 the body-metrics field offers, so an import could seed a weight the runner then could not re-save, and 30 is above the 20 floor, so a light runner's real sample was dropped as noise. Knowingly left: the watch's `nutrition_targets.rs` keeps its literals, a one-way research-tier port with no database in reach. Original filing below.
  - Original: **`nutrition_targets`' sanity caps are a third home of two DB caps, inside an enforced parity pair.** `nutrition_targets.ts` / `nutrition_targets.dart` both refuse a stored profile with `weightKg > 500 || heightCm > 300 || ageYears > 120` — a defensive filter over data read BACK, so its numbers are the *column's* caps rather than the client input bounds `column_limits` now holds (250 / 300). The 500 and the 300 are transcriptions of `body_metrics.weight_kg` and `user_profiles.height_cm`; a CHECK widened to 600 would leave a legitimately-stored weight silently producing no calorie target on both platforms, with 34 mirror tests passing. Not closed in § 792 because the pair is under the `shared-library-syncer` lockstep and the fix wants a DB-cap accessor (`columnCheckMax`) that `column_limits` deliberately does not expose today — it holds what the CLIENT may send, not what the column accepts. Same shape, unregistered: `health_connect_importer.dart`'s "clamps to a sane 30–300 kg" on the imported body weight.
- [x] **`nutrition/recipes.spec.ts` fails whenever the seed's dinner falls inside the browser's UTC "today".** **CLOSED 2026-08-31** (`docs/testing/testing.md § Owning the day`; no new ADR — [§ 728](../architecture/decisions.md) already owns the class and this is its testing-guide rule). The shape and the cause are exactly as filed. **The window is not.** The seed's LATEST meal is `now() - 1 hour`, so the count is 2 only when the seed ran inside the **00:00–01:00 UTC hour** — 3 from 01:00, 4 from 04:00, 5 from 05:00 and **6 from 08:00 on**, which is most of the day rather than a six-hour window. Measured live against the local stack at 17:14 UTC: `Expected: 2, Received: 6`. The count is also not the only casualty — `ings.length`, the summed `calories` 250 and `protein_g` 25 all move with it. **And the mechanism is not § 728's.** `seed.sql` writes `now() - interval`, a `timestamptz` the DATABASE computes, so nothing is written in the runner's zone and no conversion happens: the seeded rows are on the browser's day legitimately and simply are not the spec's. Same family, different half. Fixed by taking BOTH offered shapes, because the calorie assertions need exclusivity and the ingredient assertion needs to say what it means: the spec clears the day through `browserDayStart()` the way the sibling `meal-templates.spec.ts` already does, seeds at fixed hours on the browser's calendar day, and names its two `item_name`s instead of counting. `fixtures/dates.test.ts` gains the rule — a spec driving `save-as-meal` or `save-as-recipe` must clear the day first — proved to name `recipes.spec.ts` against the pre-fix source. **A sweep of all 466 specs (≈90 read line by line) found no second genuine collision**, one fragile near-miss (`challenges/pace.spec.ts`, whose window admits the seed's own morning run but whose assertions are qualitative enough not to flip), and three disciplined scopings already in wide use. Original filing below.
  - Original: **`nutrition/recipes.spec.ts` fails whenever the seed's dinner falls inside the browser's UTC "today".** `save ingredients as a recipe` seeds two `food_log` rows and asserts the saved recipe has `ingredient_count === 2`, but the recipe is built from **every** entry on the diary's current day — and `seed.sql` places its meals relative to `now()`, so between roughly 00:00 and 06:00 UTC the seeded "Salmon, sweet potato & broccoli" is also today and the count is 3. Measured on this branch and on `main` alike (decisions § 792); it is [§ 728](../architecture/decisions.md)'s browser-vs-runner calendar day one layer on, since `playwright.config.ts` pins the context to UTC while the seed is written in the runner's zone. Fix shape: seed the two rows at a fixed hour inside the browser's day through `fixtures/dates.ts` and assert on the two `item_name`s rather than on a count of the whole day, or delete the day's other rows in the fixture.
- [x] **The race-board weigh-in and the body-metrics card disagree about which weight unit label to show.** **CLOSED 2026-08-31** ([decisions § 820](../architecture/decisions.md)). The key, the seven ARB strings and the rename are all as filed. **"Cosmetic today" is not.** The label and the suffix sit in one `InputDecoration`, so a runner on lbs reads **"Body weight (kg)"** with an **"lbs"** suffix — two units in one field, one of them wrong, not a stale string beside a correct one. Renamed to `checkpointWeighInBodyWeight` and de-unitted in all seven catalogues, pinned by a widget test that reads the rendered decoration under a lbs preference and a source guard that parses all seven ARBs, so a translator restoring "(kg)" in one language alone fails the PR; both proved to fail against the old string. **The sweep the entry asked for: two contradictions repo-wide**, no key-only cases, ~35 benign fixed-unit labels, and both contradictions mobile-only (web's own weigh-in label already interpolates the unit). The second is `prefsSplitIntervalDefaultSubtitle` — "Default (1 km for running, 5 km for cycling)" — where `ActivityType.splitIntervalMetresFor` returns **1 mi / 5 mi** in imperial by deliberate design, so the row named the wrong unit for exactly the runners that default exists for. Fixed in the same round, placeholders in all seven catalogues, pinned by a widget test under a mi preference. Original filing below.
  - Original: **The race-board weigh-in and the body-metrics card disagree about which weight unit label to show.** § 792 fixed the web board, which read `getUnit() === 'mi' ? 'lbs' : 'kg'` for the LABEL while `parseWeight` read the separate `weight_unit` signal — so a runner on km + lbs was shown "kg" and parsed as lbs. The mobile weigh-in sheet's label key is `checkpointWeighInWeightKg`, which names kg in the key and now carries a `suffixText` from the real unit; the key should be renamed and its seven ARB strings de-kg'd so the label cannot contradict the suffix. Cosmetic today (the suffix is authoritative), but the string still says "kg" in every locale.

## Surfaced by the #789 test-coverage round (2026-08-31)

Found while writing the coverage in [decisions §§ 807-814](../architecture/decisions.md). These are the ones that need their own change rather than being closed in that round.

- [x] **`check_production_env` and `env_isolation` are off the required gate.** Closed 2026-09-01 ([decisions § 862](../architecture/decisions.md) – [§ 866](../architecture/decisions.md)). Both suites now run in an `env-isolation` job inside `ci.yml`, named in the `CI gate`'s `needs:`; `env-isolation.yml` is deleted, `env-example-scan` came with them as a third step of the same job, and the path filter is deliberately not reproduced (every step is stdlib-only and install-free, so two jobs became one and the fold gives a runner slot back). **The filing's "both" was one item and a half.** `env_isolation.mjs` is a vite plugin, recorded under `NOT_A_GUARD`, not `OFF_THE_GATE`; `check_production_env.mjs` was the single entry, and it does not go — its CLI asserts about a RELEASE env, and `build-web` compiles against `placeholder.supabase.co`, the value the guard exists to refuse, so the only ci.yml form available was an inverted `if node …; then fail; fi` that would also pass when the guard CRASHED (§ 741's shape). Its reason is rewritten instead, which is the other half of the point: the old one had become false. **What was actually missing was a rule, not an entry.** The census asked only that a guard's suite be run by SOME workflow, so this state satisfied it for its whole life; it now has to be run by `ci.yml` or be named in `SUITE_OFF_THE_GATE` with a reason ([§ 863](../architecture/decisions.md)), which is one entry. The fold also closed a hole nobody had filed: the scan reads `apps/watch_ios/WatchApp/SupabaseService.swift`, which `.gitleaks.toml` allowlists whole and no filter path matched, so on a PR a live key pasted there was read by nothing ([§ 866](../architecture/decisions.md)).
- [x] **Three mobile dotenv gates read `dotenv.env[...]` with no try/catch, where their sibling fails closed.** **CLOSED 2026-08-31** ([§ 822](../architecture/decisions.md)). The filing got the framing right and every specific wrong. Right: `main.dart` loads dotenv before `runApp` unconditionally, so an uninitialised read is unreachable in the app; and the asymmetry was real and worth closing. Wrong: none of the three named gates was missing a guard. `offRouteEscalationEnabled` and `adaptiveFitnessGateEnabled` are pure `String? → bool` parsers that never touch dotenv and cannot throw — the reads are at their call sites, and all five of those guarded already, three with `try/catch` and two with `dotenv.isInitialized &&` (equivalent: `NotInitializedError` is the only thing `dotenv.env` throws). `_weighInGate` has had its `try/catch` since `71a255845`. What was actually wrong is the *binding*: key plus guard spelled at five call sites in two idioms, two of them duplicates, and two of them private top-level getters inside screen files — so the test the filing asked for could not be written. Each flag now owns one binding module (`off_route_flag.dart` / `adaptive_fitness_flag.dart` / `weigh_in_flag.dart`, beside the existing `nearby_flag.dart`, mirroring web's `*_flag.ts` layer), the parse stays in its parity-pair file, and `env_flag_test.dart` pins all four answering `false` against an uninitialised dotenv plus a guard that a gate key read literally outside its own module fails the suite.
- [x] **Two waits in `nutrition_screen_test.dart` are measured vacuous.** **CLOSED 2026-09-02** ([decisions § 990](../architecture/decisions.md)). Re-measured with an instrumented `pumpUntil`: both spent **0** loop turns, exactly as filed. The reason it was left standing — that `pumpUntil` cannot drive a route transition under the fake clock, so a predicate about the picker risks a timeout — is right about the picker and wrong about the wait: what is being waited on is a real file write, and the `OfflineSyncStore` entry file under the fake `path_provider` root is observable without the dialog's pop completing. Both predicates now read the disk (`meal_templates/` and `recipes/`); measured 5 turns each afterwards, and pointing the helper at a directory nothing writes makes both time out naming what they wait for. The `pumpAndSettle` that follows still does the route work. Every other `pumpUntil` in the file was measured in the same pass — all nine spend at least one turn. "The saved template/recipe to appear in the picker" spends zero loop turns: `find.text` matches the dialog's own `EditableText`, so the predicate is already true before the save. It is [§ 723](../architecture/decisions.md)'s class, and it survived that round's sweep. Left in place deliberately for now — `pumpUntil` cannot wait on a route transition under the fake clock, so every correct predicate risks a timeout instead, and the `store.rows.length == 2` wait that follows does spin and does cover the behaviour. Closing it means a predicate that can distinguish the dialog's text from the picker's, or a different wait primitive for route transitions.
- [x] **`LocalRunStore` and `LocalRouteStore` carry the same unserialised delete-vs-write shape [§ 821](../architecture/decisions.md) closed on `OfflineSyncStore`.** **CLOSED** 2026-08-31 — [§ 828](../architecture/decisions.md) + [§ 829](../architecture/decisions.md). The filing was right about the shape, the outcome and the reason it was worth its own slice, and it under-counted its own evidence twice. Its headline said twelve atomic-write call sites outside `OfflineSyncStore` while its own breakdown adds to **fourteen** (7 + 5 + 2); twelve is the count for the two stores it names in the title. The route-store outcome it measured reproduces exactly — a `delete` against an in-flight `save` is silently dropped, memory *and* disk keep the row, and a cold load keeps it too — and the run store does the same plus re-writes the index the delete had just pruned. What it missed is the sidecars, which are the worse half: `route_owner_tags.json` is read-modify-write with a `_ownerTagsTouched` ledger cleared by whichever write lands first, so eight overlapping `save` calls left as few as **two of eight** routes tagged, and an untagged route is drainable into another account on a shared device ([§ 67](../architecture/decisions.md)) — reproducible about one run in three. `watch_ingest_queue.dart` was taken too (the entry's own closing sentence names three stores): `setLastKnownOwner` is fired unawaited from both `main.dart` call sites, and its clear-vs-stamp race reproduces every time. And the `FileLock` the filing flagged "worth checking on the way past" turned out to exclude nothing at all — POSIX record locks are owned by the process, so two handles in one process, including the WorkManager isolate the lock named, both acquire (§ 829); it is gone, its `.lock` residue is swept, and an arch guard fails the suite if either store spells it again. § 821's chain fit and was lifted into `serialiseStoreWrite` in `core_models`, made re-entrant so a shared persist helper cannot deadlock behind its own caller. The recording path is deliberately **outside** it — pinned by a test that holds the chain open through a never-completed `Completer` and asserts a tick still lands.
- [x] **`gym_detail_visibility_test.dart` prints a `PathNotFoundException` on the double-tap case.** **CLOSED 2026-08-31** ([§ 821](../architecture/decisions.md)). The filing got the exception shape and the L4 catch right, and the diagnosis backwards on both halves. It is **in the test**, not in shared store logic: `persist` installs the row in `rowsById` synchronously, so the visibility chip flips a frame before the atomic write lands, `pumpUntil` returned on it, and the temp-directory teardown then deleted that write's `.tmp` out from under its rename — nothing to do with the double tap (the `_actionBusy` guard holds, `updateCalls` is 1) and equally reachable from the single-tap case. And the temp path has been unique per write since `writeStringAtomic` shipped, so the collision the filing implies could not happen. The test now waits on the file, which also pins the offline-first durability the chip was wrongly credited with proving. **But the family the filing was reaching for is real, and two members were measured in production code**: `clear()` (the sign-out wipe) deletes every file in the directory including the `.tmp` of an in-flight write, producing exactly that `PathNotFoundException`; and a delete that overtakes an in-flight write to the same id leaves the row's file back on disk with `rowsById` saying it is gone, resurrecting it at the next cold-load with no exception at all. Both are fixed by serialising every directory-mutating entry point on one chain keyed on the directory, and both are pinned by tests that fail on the old path.
- [x] **CLOSED 2026-08-31** ([decisions § 815](../architecture/decisions.md)) — **The 23 `handler_envelope.test.ts` integration tests are still outside every mutation operator.** Closed with the entry it restates, in the § 815 round. The 785 / 0 / 0 figure it cites was exact and still is; the 23 was deno's ignored count and the runnable set was 22 (23 now, with the verify-token case § 815 added). The fix shape it proposed — "a neutered SERVED tree between the boot step and the test step" — was the right half and the wrong operator: a blanket neuter proves only that each case notices a dead function, which is the weaker claim § 788 already makes about the other 785, and it would have found none of the three non-discriminating cases that a per-gate mutation with declared spares did.

## Surfaced by the #789 test-coverage round 28 (2026-09-01)

Found while writing the coverage in [decisions §§ 832-851](../architecture/decisions.md). Reported rather than half-fixed: each needs its own change, and two of them need a decision before any code.

- [x] **`catalogue_browse`'s accent fold diverges between web and mobile, and the parity registry cannot see it.** Closed 2026-09-01 ([decisions § 852](../architecture/decisions.md) through § 856). The entry understated it by counting only letters and only what the web `\p{Diacritic}` strip reaches: measured by dumping BOTH folds over every Unicode scalar value and diffing them, **14,719 code points folded differently, 13,746 of them letters** — the 1,443 the entry names, plus all 11,172 Hangul syllables (web decomposes them to jamo), the 1,002 CJK compatibility ideographs, and 466 code points Dart's own `toLowerCase` leaves uppercase because its case tables are older than the web's (the Georgian Mtavruli capitals, Cherokee, Osage, Adlam). The table is now GENERATED — `scripts/gen_catalogue_fold_table.mjs` derives it from the Unicode data Node carries and emits `apps/mobile_android/lib/catalogue_fold_table.dart`, 4,420 entries, the shape `age_grade_tables.dart` already models — and it carries the case mapping too, so the Dart fold no longer depends on the runtime's Unicode version. Hangul is computed rather than tabled (its decomposition is arithmetic; the generator asserts the arithmetic reproduces NFD for all 11,172). One thing had to move on web first: `toLowerCase` implements the Final_Sigma rule, which made U+03A3 the ONLY code point in Unicode whose fold depended on its neighbours — measured across every scalar value in three contexts — so web now collapses `ς` onto `σ`, a WIDENING that also fixes a real defect there (`οδοσ` did not reach `ΟΔΟΣ`). Two guards, because the two halves are different claims: `check_catalogue_fold_table.mjs` compares the committed table with a fresh render (and names an ICU bump apart from a hand-edit), and `catalogue_fold_table.test.ts` runs the SHIPPED web `fold` against that table. 14 mirror vectors each side, 12 of which the old table answered differently. The deliberate residual is canonical REORDERING — two adjacent surviving marks in non-canonical order, which no normalised text can be — stated in both doc comments and in § 856. Original entry: **`catalogue_browse`'s accent fold diverges between web and mobile on 1,443 letters, and the parity registry cannot see it.** Web folds with `normalize('NFD')` + `\p{Diacritic}` strip, which reaches every canonically-decomposable letter in Unicode; the Dart twin spells the class out as a 19-entry table because Dart's core library has no normalisation, and that table stops at Latin Extended-A. Measured over the BMP, **1,443 letters are de-accented by web and left intact by mobile**: all 245 of Latin Extended Additional (the Vietnamese set — `Đèo Hải Vân` folds to `deo hai van` on web and `đeo hải van` on mobile), 340 Greek across both blocks, 57 Cyrillic, 91 Latin Extended-B including the pinyin tone letters (`ǎ`, `ǐ`), Vietnamese `Ơ`/`Ư`, and 7 spacing diacritics inside Latin-1 itself (`´`, `¨`, `¯`, `¸`, `·`, `` ` ``, `^`) that web deletes outright. Two consequences, both real: a reader typing `hai` reaches the segment on web and not on the phone, and `byName` compares folded names, so **the catalogue sorts differently on the two platforms**. The doc comment on the Dart side is accurate about *why* it is a table and silent about *how far* the table reaches. `check_parity_pair_registry.mjs` compares the two registries, not the two behaviours, so nothing fails. The durable fix is the one `age_grade_tables.dart` already models — **generate** the table from NFD rather than hand-maintaining it, commit it, and guard it against the generator — not to narrow web to match, which would delete working behaviour. The letters with no canonical decomposition (`ø`, `đ`, `ł`, `ß`, `æ`, …) must stay out of it either way: web keeps them too, and folding them would invent an equivalence NFD does not have.
- [x] **If Supabase Cloud ships storage-api's `protect_objects_delete` trigger, the export sweep silently stops deleting anything.** Closed 2026-09-01 ([decisions § 857](../architecture/decisions.md)) — **measured, and it is not a forward risk.** CLI 2.84.2 (CI's pin) starts storage-api **v1.44.11** and CLI 2.109.1 (the workstation) starts **v1.62.5**; `0055-prevent-direct-deletes.sql` is in **both**, so the trigger is on CI too and the escape GUC the § 839 test set was load-bearing there, not a no-op. It is also **statement-level**, so it fires on a DELETE matching no rows: the nightly job has been raising every night whether or not anything was stale, and because the DELETE precedes `expire_stale_export_jobs()` no `data_export_jobs` row was expired either. `20270703000002` sets `storage.allow_delete_query` transaction-locally — the precedent `20260927_001`'s orphan cleanup already set — and counts the stale objects surviving its own DELETE, raising if any do: a delete FILTERED rather than refused reports zero through `get diagnostics` and used to go on to mark the row `expired` while the archive was still there. The fixture no longer hands the function the GUC, so the suite drives the escape rather than supplying it. The one thing that remains is an owner call, filed below.
- [x] **`check_shared_constants.mjs` reads the migration text for the photo buckets' mime lists, which is a different claim from what the applied row says.** Closed 2026-09-01 ([decisions § 858](../architecture/decisions.md)). **Both rails kept — they make different claims and neither subsumes the other:** the script compares the migration text against `STRIPPABLE_IMAGE_MIME_TYPES` and `kStrippableImageMimeTypes`, which is four source files agreeing with each other and which no SQL assertion can reach; pgtap reads the applied `storage.buckets` rows, which no source read can. The applied-row assertions are held over a DERIVED population — every bucket accepting any `image/` type — so a fifth image bucket is held to § 557 the day it appears rather than the day someone extends a list of four names, they carry the null arm the old `@>`/`<@` filter silently excluded (`null @> array[…]` is null, so a bucket that lost its allowlist dropped out of its own check), and `image/heic` / `image/heif` are now named outright across every bucket. The source rail was hardened in the same change: it compares under `match: 'all'`, which has no key coverage check, so a bucket it failed to resolve simply stopped being compared while the other three still agreed — both that and a statement assigning the column in a shape it cannot read (which it used to skip, reporting the value from BEFORE the change) now throw.

## Surfaced by the #789 test-coverage round 29 (2026-09-01)

- [x] **Decide whether the export retention sweep should delete `storage.objects` rows at all, and measure what happens to the bytes when it does.** Closed 2026-09-03 ([decisions § 1049](../architecture/decisions.md)) — **measured, and the belief is WRONG.** A 4 KiB `application/zip` probe uploaded through the real Storage API, aged past the seven-day window and swept by the shipped `cleanup_stale_export_blobs()`, loses its `storage.objects` row (the function reports 1, its post-condition passes, the object stops being listable) and its bytes stay on the storage backend with a **matching `sha256`**. Corroborating: the same volume held 74 files across every bucket against 0 rows, including 20 export archives nine days old that nothing has ever reaped; there is no `pg_cron` job touching storage bytes and the storage container runs one `node dist/start/server.js` and nothing else. The trigger says so itself — `Use the Storage API instead`, hint `This prevents accidental data loss from orphaned objects` — and the escape GUC removes the refusal without making a row delete into an object delete. **The decision: keep deleting the rows, and stop calling it retention.** Removing reachability is the part SQL can do and is strictly better than [§ 857](../architecture/decisions.md)'s predecessor state, which deleted nothing at all; but it is not an Art 17 erasure, and `docs/compliance/data-subject-rights.md` now says so instead of saying the bytes are reaped. Two limits on the claim: this is the local `file` backend, so what is proved is the MECHANISM (no database-side reaper exists; storage-api's own delete path is the only thing that removes a backend object), and confirming the residue on Cloud still means listing the bucket over the S3 endpoint. The structural fix is filed below rather than built: it needs a new value in `jobs_kind_chk`, a set-shaped CHECK whose coverage rule lives in `check_constraint_unions.mjs`, outside the backend lane. Original entry: [§ 857](../architecture/decisions.md) restored the sweep by setting storage-api's documented escape GUC, which is what `20260927_001`'s orphan-blob cleanup already does — but that trigger exists precisely because deleting the row is not deleting the object, and its own hint says so ("This prevents accidental data loss from orphaned objects"). `20260927_001` recorded the belief that "the actual blob bytes are reaped by the storage backend's background sweeper once the row is gone"; **that belief is stated nowhere else in this repo and was not measured.** If it is wrong, every Art 20 archive ever swept is still sitting in the object store, unreachable through the Storage API but not erased — a retention claim `docs/compliance/data-subject-rights.md` makes on our behalf. Measuring it needs a real project (list the bucket through the S3-compatible endpoint after a sweep), not more reading. The alternative shape is to move the sweep onto the Storage API — a job kind in the Go worker, which already writes the archive through that API and already holds the service key — leaving `expire_stale_export_jobs()` as the only SQL half. That is a structural change to a shipped retention path and to the cron schedule, which is why it was not taken with the fix: the fix restores a sweep that was deleting nothing at all, and it is strictly better than the state it replaced under either answer.

## Surfaced by the #789 test-coverage round 30, Wear OS tier (2026-09-02)

- [x] **`BODY_SENSORS_BACKGROUND` is declared in the Wear manifest and requested nowhere, so it can never be granted.** **CLOSED round 34 ([decisions § 1015](../architecture/decisions.md)) — by REMOVING the declaration, not by building the two-step flow this entry proposed.** The unrequestable half was confirmed exactly as filed. The premise underneath it was not: Google's Health Services permission table scopes `BODY_SENSORS_BACKGROUND` to `PassiveMonitoringClient`, gives `ExerciseClient` background access with no background permission at all, and lists `MeasureClient`'s background access as **no** — "not intended for background capture or workout tracking". `HeartRateMonitor` uses `MeasureClient`, so the permission was not the gate on this app's heart rate and granting it would have changed nothing about the ambient transition. The two-step request flow would have been correct in mechanism and inert in effect. `ManifestGuardsTest` now asserts the permission is ABSENT, carrying the measurement, so re-adding it fails until the client changes too. Two things the sweep found underneath it are separate entries: the recording service never started as a `health` foreground service at all ([§ 1016](../architecture/decisions.md)), and the ambient-HR capability itself needs `ExerciseClient` — filed below. Original text: Measured the same way [§ 881](../architecture/decisions.md) measured `POST_NOTIFICATIONS`: `AndroidManifest.xml` declares it (with a comment saying HR stops streaming once the display goes ambient without it, and that the Play Data Safety form lists it), and `grep -rn 'BODY_SENSORS_BACKGROUND' apps/watch_wear/**/*.kt` returns nothing — the only runtime request the app makes is `permissionLauncher.launch(...)` in `RunWatchApp.kt`, which asks for `ACCESS_FINE_LOCATION`, `BODY_SENSORS`, `ACTIVITY_RECOGNITION` and now `POST_NOTIFICATIONS`. It is registered as an exemption in `ManifestPermissionCoverageTest` rather than fixed, for a reason that is not scheduling: a **background** permission cannot ride the same request as its foreground half — Android requires a second request made only after `BODY_SENSORS` is granted, and bundling them gets the background one denied outright. So the fix is a two-step permission flow with its own UI state, and the thing it claims to fix — that Health Services keeps delivering `HEART_RATE_BPM` past the ambient transition on a Wear OS 3.5+ watch — is a platform behaviour no host-JVM test can observe and no emulator reproduces honestly (the emulator synthesises HR). It needs a real watch and a run long enough to dim. The visible symptom if the claim is right: `avg_bpm` on a long run reflects only the minutes the display was awake, which nothing on the wrist would report as an error.
- [x] **Denying location on the Wear pre-run screen is a silent no-op.** **CLOSED round 34 ([decisions § 1018](../architecture/decisions.md)).** Confirmed exactly as filed — the callback was two lines with no else — and the fix covers three more denials the same callback was absorbing: `BODY_SENSORS` (no heart rate), `ACTIVITY_RECOGNITION` (no step count) and `POST_NOTIFICATIONS` (no ongoing-run chip, the loss § 881 exists about). `permissionOutcome` grades the result map into `canStart` plus the ordered list of what was actually lost; `PermissionNotice` renders a sentence per entry plus ask-again, the app permission screen where the watch has one, and the written path. Ten new keys in all seven catalogues. The entry's reason for not fixing it — "a UI change, not a defect fix, and outside a coverage round's remit" — was the wrong call: a control that changes nothing is a defect. Original text: `RunWatchApp.kt`'s permission callback is `if (granted[ACCESS_FINE_LOCATION] == true) { showCountdown = true }` with no else branch, so a runner who taps GO and denies the prompt (or who denied it once before, after which the system may not prompt again) is returned to the pre-run screen unchanged — no message, no state change, no indication the tap did anything. The gate itself is correct and fail-closed; what is missing is the report. It is filed rather than fixed because the honest fix is a new user-visible string in all seven catalogues plus a decision about where it renders on a 46 mm screen that already carries a battery-saver chip, a sync chip, and a sign-in chip — a UI change, not a defect fix, and outside a coverage round's remit. The measurement is the two-line callback and the absent `else`.

## Surfaced by the #789 round 34 Wear OS sweep (2026-09-02)

Found sweeping `apps/watch_wear/` for one class — a platform capability the app
declares or requests whose failure path is silent. The population, the five
defects fixed and the negative results are in
[decisions §§ 1015-1020](../architecture/decisions.md). These four are what is
left: two need hardware, one needs a rewrite, one is a UI decision.

- [x] **Continuous heart rate on the wrist needs `ExerciseClient`, not a permission.** [§ 1015](../architecture/decisions.md) removed `BODY_SENSORS_BACKGROUND` after measuring that it gates `PassiveMonitoringClient` and that `MeasureClient` — what `HeartRateMonitor` uses — is documented as foreground-only and "not intended for background capture or workout tracking". So the symptom the removed permission claimed to fix is real and unaddressed: HR sampled through `MeasureClient` is not promised past the point the app stops being visible. The Wear OS answer for a workout is `ExerciseClient`, which the same table gives background access with **no** background permission, and which would also replace the app's own GPS + distance plumbing if taken whole. Scope it first: `ExerciseClient` owns the exercise state machine (`prepareExercise` / `startExercise` / auto-pause / laps) and would sit under, or replace, `RunRecordingService`'s own — so this is a recorder rewrite with a bench day at the end, not a client swap. What can be decided from source is that `MeasureClient` is the wrong client for a 12-hour ultra; what needs a watch is how much of the recorder should move. Scoped 2026-09-03 — [decisions § 1054](../architecture/decisions.md); the code is deliberately unwritten and the scoping is the deliverable. `ExerciseClient` is confirmed the right client (1.1.0-rc02 is already a dependency, `minSdk` is already 30) and confirmed a recorder rewrite, not a client swap: it subsumes 1,455 lines across eight files pinned by 212 host-JVM tests in 16 files, none of which can drive Health Services. Three breaking edges decide the sequencing, and all three are about something OTHER than the runner deciding the run is over — `ExerciseTrackedStatus.OTHER_APP_IN_PROGRESS` means one owner per device (today the app records through Samsung Health / Fitbit / Fit; under `ExerciseClient` it could not, or would need the recorder kept as a fallback), `AUTO_ENDED` / `AUTO_ENDED_PERMISSION_LOST` let the platform end a run, and recovery inverts from `CheckpointStore` to a platform-owned exercise that outlives the process. None of the three is reproducible on the emulator, so it needs the bench day the sibling entry wants; the order is probe `getCapabilitiesAsync` on hardware, then move heart rate ALONE while GPS and distance stay ours, then decide whether the rest moves at all. Two candidate "small safe pieces" were considered and rejected on the record, one of which is filed below.
- [x] **Nothing on the wrist reports that heart rate is unavailable mid-run.** [§ 1017](../architecture/decisions.md) made every acquisition failure fail closed rather than crash — but `RunningScreen` renders the bpm line only when `bpm != null`, so "declined the permission", "watch has no HR sensor", "Health Services refused registration" and "sample not acquired yet" are one blank space. The pre-run notice ([§ 1018](../architecture/decisions.md)) covers the permission case at the moment the runner can act on it, which is the dominant one and the reason this is filed rather than fixed; the device cases it cannot cover. Doing it properly means `RecordingRepository.Metrics` carrying an HR-availability tri-state, the service publishing it from the `.catch` and from `onRegistrationFailed`, and one more caption on a 46 mm screen that already carries distance, pace, bpm, steps and lap count. Worth doing when someone has a watch to see the layout on. Closed 2026-09-03 — [decisions § 1052](../architecture/decisions.md). Built as filed, with one correction to the shape: the caption reuses the slot the bpm reading occupies rather than adding a line, so the 46 mm layout is unchanged and the longest new string costs five characters against `146 bpm`. The root was `HeartRateMonitor.stream()` being a `Flow<Int>` — the monitor knew which of the four situations it was and discarded it one line later; it now emits `HeartRateUpdate(availability, bpm?)` and the service publishes it from both exits of its collector. Off-wrist is kept distinct from unavailable because it is the only one a runner can act on mid-run; the rest fold into one caption because none of them is. Found and fixed a second defect underneath: nothing ever cleared `bpm` once set, so a watch that slipped off the wrist at minute three rendered that figure for the rest of the run. Three strings in all seven catalogues; `HeartRateAvailabilityTest` pins the mapping against the SDK's own `DataTypeAvailability.VALUES` and fails if that list grows.
- [x] **`ActiveRunTileService.requestUpdate` is called unguarded from the recording path.** `RunRecordingService.startRecording` calls it inside `onStartCommand`, so a throw from `TileService.getUpdater(context)` on a build with no Tiles host would end the run the same way [§ 1017](../architecture/decisions.md)'s sensor streams did. Examined and deliberately left alone in that round: the updater is documented as best-effort, no failure of it has been observed, and adding a `try` on a hypothesis is the speculative-defence the house rules refuse. Recorded so the next reader knows it was looked at rather than missed. It becomes real the moment anyone sees a crash trace through `getUpdater`. Closed 2026-09-03 — [decisions § 1053](../architecture/decisions.md), and the filed premise was **wrong in both directions**. Wrong first: `getUpdater` was decompiled out of `tiles-1.6.2.aar` and only allocates, and the no-Tiles-host case named here is the one case AndroidX already handles — `buildUpdateBindIntent` logs and returns null, `requestUpdateInternal` logs and returns, no throw. So a guard justified on THAT scenario would indeed have been speculative defence. Wrong second: the call site is a layering violation anyway, because `CompositeTileUpdateRequester.requestUpdate` iterates its two delegates with no catch between them and they make one unwrapped IPC call each (`Context.bindService`, which documents `SecurityException`, and `Context.sendBroadcast`). What settles it is position, not probability: the start-path call sits between publishing `Recording` and subscribing GPS, HR, steps, the ticker and the checkpoint job, and the service already wraps every other auxiliary effect on that path (vibrator, TTS, `startForeground` § 1016, the three sensor streams § 1017) — the tile was missed, not decided. Guarded inside `requestUpdate` so it is one door no future caller can walk around.
- [ ] **The `health` foreground-service type is unverified on hardware.** [§ 1016](../architecture/decisions.md) makes `startForegroundCompat` pass `location|health` on API 34+ when the platform's documented prerequisite holds, gated on `checkSelfPermission` and wrapped so a refusal retries with location alone. That answers the archived followup's caution that an unvalidated FGS-type edit can crash `startForeground`. What it does not answer is the thing the change is for: whether heart rate now genuinely survives the runner swiping back to the watch face. That needs a real watch and a run long enough to dim — the emulator synthesises HR and reproduces none of it. Pair it with the bench day the entry above wants.

## Surfaced by the #789 coverage round 30 — infra (2026-09-02)

Found reading `infra/` against its own comments and against the workflows that
consume it ([decisions §§ 889-893](../architecture/decisions.md)). Everything
fixable from source was fixed in that round; these three are what is left. The
first is an operator step on merged code, the second an owner call, the third a
sized-but-deliberately-unstarted asymmetry.

- [x] **Drop `kms:Decrypt` from the two GitHub deploy roles — the `terraform apply` it exists for is not something any workflow does.** **CLOSED 2026-09-02** ([decisions § 1021](../architecture/decisions.md)). Confirmed by measurement rather than by reading: `parseSteps` over all 21 workflows finds 59 jobs, exactly one credentialed (`release-web.yml:release`) and exactly one running `terraform` (`terraform.yml`, uncredentialed by design), and they are not the same job — the four `terraform apply` strings in `ci.yml` / `terraform.yml` are prose in their own comments. Both env roots now leave `kms_decrypt_principal_arn` at its `""` default and the now-unused `github_oidc` remote-state read went with it. **The premise the removal really rests on is one the filing did not state**: no `aws_lambda_function` sets `kms_key_arn`, so the deploy role's `lambda:GetFunction` / `UpdateFunctionCode` — which it does hold and the release does call eight times — never transits this key. All eight were read; none sets it. `scripts/check_infra_iam.mjs` claim 9 holds both premises in BOTH directions, so a wire restored while nothing exercises it fails as standing privilege and an empty wire once either premise breaks fails as a release that would `AccessDenied` mid-deploy. **Two module comments were false and are corrected**: the execution role does not "decrypt the sops env at cold-start" (`data.sops_file` decrypts at apply, the env vars are plaintext, and no `kms` reference exists anywhere under `apps/web/lambda/`), and the CMK does not encrypt the Lambda env vars. That leaves the EXECUTION role's own grant unexercised by the same argument — filed below rather than removed, because emptying the statement's identifiers leaves a principal-less statement KMS rejects. Original filing below. Both env stacks pass their OIDC deploy role in as `kms_decrypt_principal_arn`, putting it in the `AllowLambdaAndDeployRolesToDecrypt` statement of the env's secrets CMK — the key protecting `ANTHROPIC_API_KEY` and `SUPABASE_SECRET_KEY`. `envs/prod/main.tf` justifies it as apply-time decryption "from the GitHub Actions runner"; **no workflow in this repo runs `terraform apply`.** `terraform.yml` runs `fmt` / `init -backend=false` / `validate` with no credentials by design, `release-web.yml` syncs S3 and updates eight Lambdas, `budgets.tf` states outright that these resources "are intended to be applied locally, not from CI", and `bin/deploy-prod.sh` applies under the operator's own SSO profile. The deploy policies carry no tfstate-bucket access and no `lambda:UpdateFunctionConfiguration`, so the role could not finish an apply even with the decrypt. Not a path to plaintext on its own — the ciphertext lives in the private estate repo, which CI never checks out — but it is a grant the pipeline does not use on the one key in the account whose loss is unrecoverable. **Operator:** set `kms_decrypt_principal_arn = ""` in `infra/envs/prod/main.tf` and `infra/envs/preview/main.tf` (the module's `compact()` drops it from the statement's `identifiers`), then `terraform apply` in each env stack; the Lambda execution role is a separate identifier in the same statement and is untouched. Restore it the day a workflow genuinely applies. Not fixed in-round because it needs an apply and this lane holds no credentials. [decisions § 892](../architecture/decisions.md).
- [ ] **Owner call: should an Identity Center admin be able to decrypt production secrets?** Today one can, and it is deliberate — `AllowOperatorSopsUseViaIamPolicies` delegates Encrypt/Decrypt/GenerateDataKey to the account root because `sops <file>`, `sops --set` (`bin/secret-set.sh`) and `sops updatekeys` (`bin/key-rotate.sh`) all decrypt before they re-encrypt, so an operator without Decrypt cannot rotate a secret at all. The key policy's own header claimed the opposite for as long as that statement had existed and is corrected in [decisions § 891](../architecture/decisions.md); what is left is the posture itself. Narrowing it means naming a dedicated operator principal in the key policy instead of root, which changes who holds the rotation flows and needs a decision about how that principal is assumed on a one-person estate — not a Terraform edit. Filed rather than done because the code change is the easy half and the wrong half to start with.
- [x] **`envs/preview` can set two of the three engine URLs; `envs/prod` can set all three.** **CLOSED 2026-09-02** ([decisions § 1024](../architecture/decisions.md)). Confirmed, and the "adding them before then is scaffolding" reading is wrong about what the change costs: the module input already exists and already defaults to `""`, so preview receives the same `""` before and after and the plan is byte-identical — the only thing that changes is that an operator CAN set it. Measured while confirming it, the asymmetry is **five** inputs wide, not one: `domain_name` (composed from `preview_subdomain`), the three reserved-concurrency caps (literals in preview WITH a stated reason), and `graph_cycle_url`, which was the only one with no reason recorded anywhere and the only one closed. `scripts/check_infra_coverage.mjs` claim 6 DERIVES the engine set from the module — a `_url` string input whose default is `""`, which is the value that means no engine and a graceful degrade — so a fourth engine is covered the day it lands with no registry to remember. Original filing below. `graph_cycle_url` is wired from a variable in `envs/prod/main.tf` and neither declared nor passed in `envs/preview/main.tf`, so preview takes the module default `""`, skips the graph-cycle generator and serves `round_trip`. Correct today and deliberate — preview has no sidecar to point at — but it is the only one of the three engine URLs preview cannot set, `graphhopper_url` and `osrm_url` are both declared there with exactly that "point it at an engine only if a preview must exercise the server-side path" rationale, and the asymmetry is recorded nowhere. Closing it is three lines (a variable, a wire, a description) the day a preview needs to exercise v3 loop generation; adding them before then is scaffolding for an engine that does not exist. [decisions § 893](../architecture/decisions.md).

## Surfaced by the #789 round 30 Lambda + edge-function coverage (2026-09-02)

Found while writing the coverage in [decisions §§ 894-898](../architecture/decisions.md). Each needs its own change rather than riding that round.

- [x] **The twenty in-app `PUBLIC_SITE_URL` folds under `src/routes/` still resolve the origin by hand.** Closed round 33 ([decisions § 970](../architecture/decisions.md)) — and the count was **twenty-two**, not twenty (the filing named six `/share/*` loaders where there are nine, and missed `sitemap.xml/+server.ts` and `u/[id]/+page.svelte`). The premise was also wrong: `||` catches the empty string but NOT a whitespace-only value, which is truthy and yielded a canonical of `   /share/run/<id>`. Original text: [§ 895](../architecture/decisions.md) routed the five production Lambdas through `siteOrigin` after finding they folded with `??`, which keeps an empty env var as the origin. The `src/` half spells it `env.PUBLIC_SITE_URL || DEFAULT_SITE_URL` in **twenty** places (`+page.ts` loaders for `/learn`, `/learn/[slug]`, `/learn/category/[category]`, `/recap/share/[id]`, six `/share/*` routes, plus the canonical `<svelte:head>` blocks on `/clubs/[slug]`, `/clubs/[slug]/events/[id]`, `/gym/[id]`, `/routes/[id]`, `/runs/[id]`, `/sessions/[id]`, and `/+page.ts`). `||` gets the blank case right, so none of them is wrong today — which is why this is filed rather than fixed in that round. What they do not do is trim a trailing slash, and none goes through the helper, so `siteOrigin` has five callers where it should have twenty-five and the rule lives in one place while twenty-five call sites re-decide it. The measurement is in the ADR: before § 895 the helper had **zero** callers, and its own doc comment states the rule the Lambdas were breaking. Closing this is a mechanical sweep plus widening `lambda_site_origin.test.ts`'s register to `src/routes/`.
- [x] **The coach Lambda dispatches its two sub-paths by substring, so the prod path table and the dev route table disagree about which paths exist.** Closed round 33 ([decisions § 967](../architecture/decisions.md)) — anchored dispatch, plus the half the filing missed: an unknown path under the prefix fell THROUGH to the coach turn under its 256 KB cap and now 404s. Original text: `rawPath.includes('/route-describe')` and `rawPath.includes('/route-request')`, against SvelteKit's exact `src/routes/api/coach/route-describe/+server.ts`. Measured: `POST /api/coach/x/route-describeZZ` reaches the route-describe handler and answers its `400 invalid route input`, where dev answers 404. Not exploitable — both sub-paths are Pro-gated and carry a **smaller** body cap (32 KB / 16 KB) than the coach path's 256 KB, so a mismatch routes a caller to a stricter handler, not a looser one. It becomes a defect the moment a third sub-path's name contains an existing one, at which point the first `includes` shadows it silently in production while dev routes both correctly. The fix is an anchored match on the path segment, and the case above belongs in `coach_lambda_handler.test.ts` beside the dispatch tests § 896 added.
- [x] **Five of the eight production Lambda handlers still have no behavioural test.** Already closed by PR #850 before round 33 opened; verified rather than fixed. All eight are imported and invoked: `coach` → `coach_lambda_handler.test.ts`, `generate-route` → `generate_lambda_handler.test.ts`, `osrm-proxy` → `osrm_proxy_lambda_handler.test.ts`, and all four share renderers plus `share-entity` → `share_lambda_handlers.test.ts`, which stubs `__SPA_SHELL_HTML__` and leaves the Supabase env unset so every lookup misses without a network call. The dispatch half the filing wanted reached IS reached — the two path regexes, the 404-vs-200 split and the 5-minute TTL are all asserted against a driven handler, not against source text. Original text: [§ 896](../architecture/decisions.md) covered `coach` and `generate-route`; `share-entity` already had one. `osrm-proxy` is importable with no native dependency and is the highest-value of the remainder — its request path IS the runner's waypoint coordinates, it is the only handler with a GET-only method gate, and its `allowDemoFallback: false` is what stops production leaking waypoints to the uncontracted `router.project-osrm.org` demo (issue #198), none of which is executed by anything. The four share/OG renderers (`share-run`, `share-route`, `share-badge`, `share-recap`) are harder and are why they were skipped: each imports an `@resvg` native `.node` addon and reads a build-time-substituted `__SPA_SHELL_HTML__`, so driving one needs either a stub for both or a split between the dispatch half and the render half. The dispatch half is the part worth reaching — the two path regexes, the 404-vs-200 split, and the 5-minute TTL on both, which today is asserted only as source text by `share_run_cache_control.test.ts`.
- [x] **The share Lambdas' catch-all 404 is the only response on those behaviours that sets no `cache-control`.** Closed round 33 ([decisions § 969](../architecture/decisions.md)) — and it was not the only one: the JSON **503** sets none either, and must NOT get the five-minute window (caching a transient failure at the edge turns a blip into a five-minute outage). Two headers, not one. Original text: Every other path in `share-run` / `share-route` / `share-badge` / `share-recap` / `share-entity` returns the deliberate `public, max-age=300, s-maxage=300, stale-while-revalidate=60` ([decisions § 797](../architecture/decisions.md)'s privacy window); `jsonResponse(404, { error: 'not found' })` — the misconfiguration branch, reached when CloudFront sends a path the handler's regexes do not claim — sets only `content-type`, so the TTL is whatever the behaviour's cache policy decides rather than the five minutes the surface has chosen everywhere else. Low consequence (it is reachable only from a behaviour/dispatcher mismatch, which `share_entity_dispatch_guard.test.ts` already fails the PR for), but it is an inconsistency inside one file and the fix is one header.

## Surfaced by the #789 firmware round 30 (2026-09-02)

Found by the date comparison in [decisions § 899](../architecture/decisions.md):
for every `apps/custom_watch/core/src/<m>.rs` with a same-named
`apps/web/src/lib/**/<m>.ts`, compare `git log -1 --format=%cs` on each side.
**49 modules pair up; on 16 the web source moved after the port was last
touched.** Five of those were fixed this round ([§ 900](../architecture/decisions.md)
through [§ 903](../architecture/decisions.md)); the rest are below with the web
commit that moved them, so the next reader starts from a measurement rather than
from the sweep. Reported rather than half-fixed: each needs its own change, and
three of them need a decision about scope before any code.

- [x] **`fitness::RunSource` cannot represent `parkrun` or `race`, and has no cycling exclusion.** Closed 2026-09-02 — [decisions § 996](../architecture/decisions.md). The source half was a real trap and is fixed: both variants added, and `is_qualifying` is now an exhaustive match rather than `!matches!(self, Other)`, so a variant added to the enum is a compile error until classified — web's stated reason for its total map, and the negated form was the same failure in the other direction. The **cycling exclusion is deliberately not ported** and `is_qualifying_run`'s doc says why: it needs an activity type `RunForFitness` has no producer for on a device with no sport selector, so the field would be unfillable and the case unreachable. The trigger that changes it is a phone push of run history, which would bring the field with its own wiring. Web `qualifyingRuns` gained `activity_type !== 'cycle'` in #747 and a total `Record<RunSource, boolean>` in #749, the latter to stop `parkrun` and `race` — a certified weekly 5K and a chip-timed official result, the two most authoritative sources there are — being silently omitted by an `||` chain. The watch's enum has seven variants ending in `Other`, and `is_qualifying` is `!matches!(self, Other)`, so both sources map to `Other` and are excluded, and there is no activity type on the wrist at all. Whether the watch should carry either is a scope question first: `qualifying_runs` has no consumer here (only `RecoveryAdvice` and `days_until_next_hard_session` are wired), and nothing pushes a per-run source over `SET1` today.
- [x] **`plan_progress` is missing `planWorkoutProgress`, added to web on 2026-08-11 (#757).** Closed 2026-09-02 as a recorded exclusion — [decisions § 997](../architecture/decisions.md). **The filing named one function; there are two** — `planDistanceBanked` is also absent and is part of the registered parity pair. Both need the plan's whole workout list and nothing pushes one: the two plan glance pages (`PlanReplan`, `PlanAdaptive`) render pre-computed views, and a per-week workout table has no wire and no room on a 168x96 panel. The two that ARE ported are ported because the phone sends their inputs. The exclusion and its trigger are now in the module doc rather than left as an unexplained count. Web's version excludes rest days from BOTH the numerator and the denominator, because the editor renders "Mark as done" on a rest day and a plan could therefore report over 100 % complete. Porting it is a new function rather than a repair, and there is no plan surface on the wrist, so this is a scope call.
- [x] **`streaks` has no `bestSince` bound, so a period card would report the all-time best.** Closed 2026-09-02 — [decisions § 995](../architecture/decisions.md). Confirmed and worse than filed: **`recap.rs` carried its own private copy of the whole streak algorithm** and its own 512-day cap beside `streaks.rs`'s, so one crate held two implementations of one rule with only one registered as a parity port — a § 641 divergence that was undetectable by construction, and a bound added to one would have reached neither. The copy is gone, the bound is a REQUIRED parameter (a caller that forgets it is what produced this), and both the year and month builders pass one. Measured: twelve consecutive days in March 2024 gave a 2026 card a 12-day best and a `streak-7` badge, since `compute_recap_badges` grades off the same number. Web added the optional lower bound in #747 for exactly this: a 40-day streak from two years ago was the headline number on a card titled "My 2026 in running". `recap::build_year_in_running_recap` on the wrist calls the unbounded form, and `recap` is itself unwired — but the two move together, so port them together when either gets a surface.
- [x] **`roadbook` has no `RoadbookTarget` / `TARGET_BAND_FRACTION`.** Closed 2026-09-02 as a recorded exclusion plus a filed wire change — [decisions § 997](../architecture/decisions.md). **Both of the filing's premises are false.** `route_markers` carries `parse_cutoff` and no `parse_target`, so the input does not exist on the wrist; and `build_roadbook` has **no non-test caller** — the wired Roadbook glance renders the checkpoints the phone pushes over `RBK1`, so a field added to that function's output is one nothing on the device can read. The new box below carries the two-rail change. Web #768 (2026-08-15) added the per-checkpoint target-time verdict beside the cutoff one, graded on a PROPORTIONAL band because two minutes down at hour twenty of a 200-miler is noise where the same two minutes at a 30-minute checkpoint is not. The watch already carries `route_markers::parse_target`, so the input exists; the Roadbook glance page is wired, which makes this the one entry here with a real wrist surface waiting for it.
- [x] **`live_freshness` has neither `liveElapsedS` nor `raceClockS`.** Closed 2026-09-02 as a recorded exclusion — [decisions § 997](../architecture/decisions.md). Both reconstruct a clock a SPECTATOR cannot see; this device is the runner's own and `record::Recorder` owns the elapsed clock, so the distinction does not arise. The surface the filing worried about already makes it: `cutoff_eta::next_cutoff_eta` takes `elapsed_s`, the race clock, and `record.rs`'s `the_cutoff_eta_projects_from_race_pace_not_moving_pace` pins that it is not the moving one. Porting a reconstruction of a measured fact would give a second answer to a settled question. `freshness_for` stays ported because a ping's AGE does have a wrist analogue. Web added both after the port (#763 on 2026-08-13, #808 on 2026-08-24). The module doc correctly says only `freshnessFor` is ported, so the 5-against-16 test count is scope and not drift — but the race-clock distinction the two functions exist to draw (a ping's `elapsed_s` is the runner's recording timer, frozen at the last fix; a cut-off is measured against wall-clock time since the start) is one the wrist's own cut-off surfaces make, and it is worth a deliberate answer rather than an inherited silence.
- [x] **`recap`, `plan_replan`, `segments`, `auto_segment_effort`, `age_grade` each moved on web after the port and were read this round without a defect being found.** **CLOSED 2026-09-04** ([decisions § 1161](../architecture/decisions.md) + [§ 1162](../architecture/decisions.md) + [§ 1163](../architecture/decisions.md)). Measured this time rather than read: each web commit to the counterpart since the port's own commit was diffed, the Rust function read against it, and a discriminating input constructed where the two could answer differently. **Two of the five were divergent, and neither was the change the filing pointed at.** `recap.rs` filled a `Vec<i32, MAX_STREAK_DAYS>` with raw run days where the cap is named for distinct days, so 730 chronological runs across 2026 reported `{best: 256, current: 0}` against web's `{best: 365, current: 365}` — pre-existing, not #747, whose `bestSince` bound the firmware already had. `segments.rs` still carried `SegmentGenderFilter::Nonbinary` seven weeks after migration `20270422_001` deleted the value end-to-end — that IS #322, filed as comment-only because that is what it was in `age_grade.ts`; in `segments.ts` the same commit deleted a union member. Both fixed with pinning tests. The other three are identical for what the firmware ports, with the evidence stated in § 1163. Original filing: `recap` #747 bounds the streak (the entry above), `plan_replan` gained the generator-v2 P2 surface, `segments` #733 added a batched `computeEffortsFromTrack` plus an `unwrapLonDeg` bbox prefilter the watch has no analogue of, `auto_segment_effort` #733 is the call-site half of that batching, and `age_grade` #322 is a comment-only change. Recorded so a later sweep does not re-read them from zero — none is a known divergence, and none has been shown to be identical either.
- [x] **The wire format's altitude field cannot hold a Hardrock-class course.** Partly closed 2026-09-02 — [decisions § 994](../architecture/decisions.md) — and the widening itself refiled below as the two-rail change it is. Measured exactly, as asked: the ceiling is 3276.7 m (3276.75 m already stores `None`), `plausible_alt` admits to 9000 m, and **69 % of a Silverton-Handies-Silverton profile stores no elevation** — and because the gain rule carries the last reading across a dropout the summit push nets to **zero vert** rather than to a gap. **A second defect underneath it was fixed:** `ele_dm_from_m` accepted -3276.8 m and returned `Some(i16::MIN)`, which IS `ELE_NONE`, so the reading encoded to the sentinel and decoded back as `None` while the value one decimetre above stored fine — the function's doc asserted the opposite and its test pinned the wrong claim. Also recorded where a reader meets it: the bench checklist now says an empty ALT column over the high half of a mountain run is the format, not a dead barometer, and `the_altitude_ceiling_is_far_below_what_the_device_believes` pins the 69 % as characterisation. `run_store::TrackPoint::ele_dm` is an `i16` of decimetres, so `record_cadence::ele_dm_from_m` stores `None` above 3276.7 m — and `elevation::plausible_alt` admits up to 9000 m, so the whole band between them is a plausible reading the track cannot carry. Handies Peak (4,285 m) and Leadville's Hope Pass (3,840 m) are both inside it, which means a synced track from those races carries no elevation across exactly the section the vert is in. The code calls this out as a frozen limit that "tier-2 widens", so the decision is made; what was missing is a place where the consequence is written down in the runner's terms.

## Surfaced by the #789 round 31 guard-tier coverage (2026-09-02)

Found while mutation-testing the repo-level guard tier — see
[decisions § 910](../architecture/decisions.md). Each needs a change in paths
that round did not own.

- [x] **Nine helper pairs declare cross-platform lockstep in their own headers and are registered in neither registry.** **CLOSED — verified 2026-09-02.** Independently measured on this round's base (`bb87b5ab3`): all nine (`tile_pack`, `streaks`, `fundraiser_progress`, `readiness`, `calories`, `column_limits`, `cycle_plan`, `text_limits`, `route_loop`) appear in CLAUDE.md's parity list AND in the `shared-library-syncer` table, `node scripts/check_parity_pair_registry.mjs` reports `[OK] 109 pair(s) registered identically in both registries` plus `441 registered path(s) exist on disk`, and `scripts/check_twin_claims.mjs`'s `KNOWN_GAPS` is now an EMPTY array — which is the load-bearing half, since that register fails the moment an entry stops being a violation, so it can only shrink. `check_twin_claims.mjs` reports 105 twin declarations across 3 source roots with none uncovered. Original filing below. `tile_pack`, `streaks`, `fundraiser_progress`, `readiness`, `calories`, `column_limits`, `cycle_plan`, `text_limits` and `route_loop`. The shared-library-syncer agent is the only automated detector of parity divergence and works from its own table alone, so a pair missing from it is a pair whose divergence is never caught — the § 641 (`turn_cues`, diverged in three implementations at once) and § 760 (accent fold, eighteen days) failure. CLAUDE.md's own `streak_card` entry calls `streaks` "the compute pair" while nothing registers it. `route_loop` already looks asymmetric: 35 mirror tests on web against 20 on Dart, with `bisectScale` exported on one side only — worth reading before registering, since registering a pair that has already diverged just moves the finding. Closing this is a CLAUDE.md bullet plus a syncer-table row per pair; `scripts/check_twin_claims.mjs` holds all nine in `KNOWN_GAPS` and fails the moment one is registered, so the register is the checklist.
- [x] **Five source headers declare a twin at a path the counterpart moved out of.** **CLOSED 2026-09-02** ([decisions § 989](../architecture/decisions.md)). All five were **already correct on `main`** — fixed in the round that filed them. Asking the question generally is what mattered: a scan of every `apps/web/src/**` path cited from `apps/mobile_android/lib`, `apps/mobile_android/test` and `packages/**` found 211 references and **26 distinct paths that do not exist**, from 37 files — including the Dart halves of the registered pairs `privacy`, `segments`, `training`, `training_load`, `recurrence`, `fitness`, `search_ranking`, `settings` and `routing_quality`, plus five `.test.ts` counterparts the mirror suites name as the thing they mirror. All 26 rewritten to where the file actually is. The guard landed as a Dart test in `architecture_guards_test.dart` rather than a script under `scripts/`: the claim is about mobile source comments, the suite that reads them already runs in `Test Flutter packages`, and living in `test/` mirrors it into the iOS twin for free. It carries a floor on the reference count so a broken regex fails loudly instead of passing with zero findings. `calories.dart` -> `apps/web/src/lib/calories.ts` (now `runs/calories.ts`), `readiness.dart` -> `apps/web/src/lib/readiness.ts` (now `training/readiness.ts`), `route_loop.dart` -> `apps/web/src/lib/route_loop.ts` (now `routes/route_loop.ts`), `streaks.dart` -> `apps/web/src/lib/streaks.ts` (now `runs/streaks.ts`), `runner_handle.dart` -> `apps/web/src/lib/runner_handle.ts` (now `social/runner_handle.ts`). The last belongs to a pair that IS registered, so the registries are correct and only the header is stale — which is exactly why `check_parity_pair_registry` cannot see any of them: it holds paths of its own. Each fix is one line of comment, four of them in `apps/mobile_android/lib` and therefore owing an iOS-twin mirror.
- [x] **CI installs Deno from a floating `deno-version: v2.x`.** **CLOSED 2026-09-02** ([decisions § 965](../architecture/decisions.md)). Pinned to **2.9.6** at both steps, `.tool-versions`' `deno` line uncommented to match, and a Deno rail added to `check_toolchain_pins.mjs` that refuses a CHANNEL rather than only an absent version — a Deno minor carries new `deno check` diagnostics, so `v2` would be as floating as `v2.x`. The version is not a guess: the last green CI run on `main` (5a81d206d) downloaded `deno-x86_64-unknown-linux-gnu.zip` from the **v2.9.6** release tag in both steps, so the pin reproduces exactly what CI ran green rather than proposing a version nothing has executed. The line stays COMMENTED in `.tool-versions`, like its `rust` / `golang` / `flutter` / `terraform` peers and unlike `nodejs` — the file's active lines are what `asdf install` materialises, and Deno is installed by its own installer here; the guard reads commented lines for exactly this reason. Original filing below. Two workflow steps take the channel rather than a version, which is the `channel: stable` shape [§ 595](../architecture/decisions.md) was about and [§ 705](../architecture/decisions.md) restated for the firmware's Rust channel: a Deno release lands, `deno check` or `deno test` gains a diagnostic, and CI fails on code nobody touched with the blame landing on whichever PR ran next. It is not fixed here because there is no in-repo Deno pin to fix it against and choosing the version is the deno lane's call — `scripts/check_toolchain_pins.mjs` reports the `.tool-versions` deno line as unbacked for exactly that reason. Closing it is a `deno-version` literal in both steps plus uncommenting `deno <version>` in `.tool-versions`, at which point the existing rail checks it like Flutter, Rust, Go and Terraform.
## Surfaced by the #789 round 31 Edge Function coverage (2026-09-02)

Found reading the Deno tier against its own comments while writing
[decisions §§ 914-916](../architecture/decisions.md). The three defects those
entries record were fixed in-round; these four are what is left. Each needs a
change this lane could not make correctly — one crosses into a path the round
did not own, two are documented deliberate choices that deserve a second look
rather than a unilateral reversal, and one is a correctness question about a
third-party payload whose real shape is not knowable from here.

- [x] **`refresh-tokens` runs `verify_jwt = false` with no rate limit and no floor on `CRON_SECRET`, and its one sibling in that position has both.** The function's own header states the stake: without the gate "any attacker hitting it would loop the entire `integrations` table through Strava's OAuth refresh endpoint, burning Strava API quota and forcing token churn". Its only gate is `timingSafeEqual(token, cronSecret)` on a bearer the caller supplies. `strava-webhook` — same posture, same class of gate, reachable by URL alone — carries two defences this one does not: a 32-character minimum on its secret ("Defends against a misconfigured deploy (e.g. `STRAVA_WEBHOOK_SECRET=test`)") and a 60/hour IP-keyed bucket placed deliberately BEFORE the secret check, because "`timingSafeEqual` closes the per-byte channel but doesn't slow the offline guess rate". **The floor is the half that is unambiguously right and it could not land in this round**: `.github/workflows/ci.yml` sets `CRON_SECRET=ci-cron-secret` (15 characters) for the served-envelope lane, so adding the floor means editing a workflow this lane did not own, and a floor chosen to accommodate a CI fixture is not a floor. **The rate limit is the half that needs a decision, not a copy**: `ipBucketKey` collapses every caller the trusted header does not identify into one shared bucket, and it is not established that pg_cron's invocation carries `cf-connecting-ip`, so limiting before the compare could let an attacker starve the hourly token refresh out of the same bucket. Spending the bucket only on a FAILED compare bounds the guess rate without that risk, and is a deliberate divergence from the sibling rather than an oversight — which is the call to make. **The floor half was closed 2026-09-02 at round-31 integration** — [decisions § 937](../architecture/decisions.md): the floor is in, the CI fixture was lengthened to clear it rather than the floor lowered to fit the fixture, and a source guard now reads both plus the sibling's. **The rate-limit half closed 2026-09-02 in round 33** — [decisions § 973](../architecture/decisions.md): the call the filing framed correctly, made the way it framed it. The bucket is spent on a failed compare rather than in front of it, so pg_cron cannot be starved out of the shared `unknown` bucket and the authorised path acquires no database dependency; it is narrowed once more to a bearer that was actually SUPPLIED, because a bearerless POST is a probe rather than a guess — which also keeps the sibling's 60/hour ceiling honest against a CI lane that would otherwise have spent 62 of it. `gate_invariants.test.ts` pins both placements, this function's and the sibling's opposite one.
- [x] **`strava-import`'s `sync` action is the only rate limit in the file that fails open, and it is the expensive one.** `connect` and `disconnect` both pass `{ failClosed: true }`; `sync` does not, on the stated grounds that it is "idempotent and read-mostly". The resource it spends is not ours: a sync walks up to ~20 pages of Strava's API and uploads a track per activity, against Strava's **per-application** budget. So on an RPC blip the guard disappears from the one action whose cost lands on a shared third-party quota, while the two cheap single-write actions stay guarded — the posture is inverted relative to `_shared/rate_limit.ts`'s own rule ("expensive paths where letting traffic through on RPC failure is worse than the false-positive denial"). The same open posture is on `parkrun-import` (an outbound scrape of parkrun.org.uk on our IP, whose own comment names IP-reputation drain as the threat), `race-results-import` and `race-listings-sync` (outbound calls carrying **our** RunSignUp / ChronoTrack / UltraSignup credentials). Left as filed rather than flipped because a false 503 during a DB blip is a user-visible regression on four live paths and the rationale on the `sync` leg is written down, not accidental. **Closed 2026-09-02 in round 33** — [decisions § 974](../architecture/decisions.md). All four flipped, and the reason the filing's own hesitation dissolves is that the fail-open rescues nothing: the RPC that failed is `check_rate_limit` against the SAME database each of these four must then write its rows to, so the request was going to fail anyway and the only variable is whether it burns a third party's budget first. The count was as filed — 13 call sites, 9 already fail-closed, and the 4 open ones exactly the 4 spending a resource that is not ours. `_shared/rate_limit_failclosed_guard.test.ts` now reads every call site in the tree by paren-balanced argument extraction and requires the posture on each.
- [x] **The UltraSignup leg reads one athlete's WHOLE history and stamps every result with a single listing's name, date and distance.** `ultraSignUpResultsUrl` hits `/service/events.svc/results/athlete?uid=…` — an athlete feed, not a race feed — and there is no race parameter on it. Every row it returns is then mapped through `mapOpts`, which carries the ONE `public_race_listings` row the request named, so a runner with twenty past ultras gets twenty `runs` rows all dated on the target race's date, all at the target race's distance, all pointing at its `race_listing_id`; their `external_id`s differ only by bib. `ultraSignUpScopeGate` does not cover this — it only requires an athlete id, which is what makes the feed athlete-scoped in the first place. Today the hole is closed by the absence of data rather than by a check (nothing populates UltraSignup listings; the listing sync is a stub), and the leg is credential-gated, so it is inert. **Note that [decisions § 914](../architecture/decisions.md) slightly widens the reachable path**: before it, two of those historical races sharing a bib raised 23505 and lost the whole batch, which accidentally blocked some of this; now the batch is deduped and inserts. The fix needs a field to attribute a result to the race with, and `UltraSignUpResult` declares none — closing it means either reading UltraSignup's real payload for a race identifier and filtering on it, or refusing the leg outright until one exists. Guessing at a field name from here would be worse than filing it. **Closed 2026-09-02 in round 33 by refusing** — [decisions § 975](../architecture/decisions.md). The filing was right in every particular and the harm was measured: three results from three different races become three `runs` rows differing only in bib and finishing time, all at the target race's date and distance. `ultraSignUpAttributionGate()` refuses at both doors — the import branch (before the credential read and before the fetch) and the availability probe, which previously answered on key presence and so would have advertised the tile the moment the key landed. The code is `503 provider_not_configured` because that is the seam both clients already disable their tile on, with `reason: 'results_unattributable'` so an operator who HAS set the key is not sent hunting. The mapper, extractor and URL builder stay standing behind the gate; the gate's doc says lifting it costs an OBSERVED payload plus a filter on the race id, not just deleting the gate.
- [x] **`parkrun-import` truncates silently at `MAX_PARKRUN_ROWS` and the response cannot say so.** The scrape loop stops at 5000 rows and answers `{ imported, skipped }`, where `skipped` means "already had it" — there is no `truncated` or `complete` field, so a caller cannot distinguish a whole import from a capped one. This is the shape the repo already litigates on the Strava side, where `parseStravaSyncResult` treats an absent `complete` as PARTIAL on purpose. Filed rather than fixed because it is not reachable in practice: 5000 parkruns is roughly 96 years of weekly Saturdays, and the most prolific parkrunner alive is around a fifth of the way there. It becomes worth the field the day the cap is lowered or a second table on the page starts contributing rows. **Closed 2026-09-02 in round 33** — [decisions § 976](../architecture/decisions.md) — and the reachability estimate held, which is why the fix went wider than the box. parkrun now answers `{ imported, skipped, total, complete }`, the walk counting past the cap instead of stopping dead at it (the same statement bounded the result set and destroyed the evidence that it had been bounded). **The same class one function over is reachable today**: `race-results-import`'s three extractors cap at `MAX_RESULTS_ROWS` (2000) and `runSignUpResultsUrl` narrows upstream by USER ID only, so a bib-scoped request fetches the whole finisher field and the bib filter runs afterwards — driven at 30,000 finishers, the caller's bib 25000 is not in the extracted 2,000 and they are told no result was found for a race they finished. That leg now reports `complete` on every success shape and answers `502 upstream_results_truncated` when a truncated fetch matched nothing. Default is `parseStravaSyncResult`'s (absent = PARTIAL), not `cloudExportShortfall`'s, for the stated reason: one transport, shipped with its callers.
## Surfaced by the #789 coverage round 31 — web unit lane (2026-09-02)

Found reading the 156 `apps/web/src/lib/` files #789 touched against their own
stated contracts ([decisions §§ 919-923](../architecture/decisions.md)).
Everything the lane could close without editing a Dart file was closed in that
round; these are what is left. The first two are halves of a parity pair, which
this round could not touch on one side only.

- [x] **`OffRouteAlertDetector` treats an unusable distance reading as "off route", so a non-finite value arms a safety escalation rather than suppressing it.** `update(offRouteDistanceMetres, nowMs)` documents `null` as "on-route / no route selected / no GPS fix" and resets the clock on it, but the guard is `offRouteDistanceMetres == null || offRouteDistanceMetres <= this.thresholdM` — and `NaN <= 75` is `false`, as is `Infinity <= 75`. So a reading the geometry could not produce starts the 90-second sustain clock and, if it persists, fires the once-per-run escalation to the runner's trusted contacts off a measurement nobody has. It also latches, so the false fire consumes the real one. The reachable path is `run_recorder`'s `_offRouteDistance`, which seeds `minDist = double.infinity` and returns it unchanged if every `d < minDist` comparison is false — which is what a route carrying a non-finite waypoint produces. Low probability, high consequence, and the fix is one line on each side: treat a non-finite distance exactly as `null`. **Not fixed in-round because this is a TS↔Dart parity pair (`off_route_alert`) and the two sides are byte-for-byte equivalent today; fixing web alone would create the divergence the pair exists to prevent.** The same edit is owed to `apps/mobile_android/lib/off_route_alert.dart`, where the only surface actually lives.
- [x] **The same detector wedges permanently if the device clock steps backwards while the runner is off route.** `sinceMs` is stamped from `nowMs` on the first off-route snapshot and never re-examined, so an NTP correction that moves the clock back makes `nowMs - this.sinceMs` negative for the rest of that off-route stretch and the alert can never fire. It self-heals only if the runner returns on-route (which resets `sinceMs`) and leaves again — i.e. it fails exactly in the case where they do not come back. `recentRunVolume` and `comebackLoad` both already clamp a future-stamped sample for the mirror-image reason, so the codebase has the idiom; this detector does not use it. Same parity-pair constraint as the entry above, and the same one-line shape: clamp `sinceMs` down to `nowMs` when the clock has moved behind it.
- [x] **`normaliseE164` claims to fold "the hyphen family" and folds three of its seven members.** `SEPARATORS` carries `-` (U+002D), U+2011 and U+2013, and omits U+2010 HYPHEN, U+2012 FIGURE DASH, U+2014 EM DASH and U+2212 MINUS SIGN — all of which a contact card, a CRM export or a word processor's autocorrect will put inside a pasted phone number. The failure is in the safe direction (the number is refused, not silently altered into a different reachable one, which is the case the `(0)` repair exists for) so nothing misdelivers; what it costs is an owner being told a correct number is invalid with no hint as to which character did it. Parity pair `e164`, identical on both sides today, so the class has to widen on both at once.
- [x] **`classifyStravaRow` imports a row of unknown activity type as a run, and the header index has no way to say the column is missing.** An empty `Activity Type` cell falls through the `t && !t.includes(...)` guard to `import`, and `importOne` then defaults `actType` to `'run'`. That is correct for a blank cell in a normal export, but `indexHeader` returns `-1` for a header it cannot find and `row[-1]` is `undefined`, so a Strava export era that renames the column would import a migrant's entire history — rides, swims, yoga — as runs, silently, with the disposition counter reporting them as imported. The required-column check covers only `Activity ID` and `Filename`. Worth deciding whether a missing type column is a fatal header error (matching the other two) or whether the unknown-type row should be dropped as `unsupported`; either is a behaviour change, which is why it is filed rather than taken. Closed 2026-09-02 — [decisions § 979](../architecture/decisions.md): a header naming NO activity type is refused alongside the other two required columns (with the column absent, every row is untyped, so dropping them all reports zero imported and several thousand dropped — a worse message than a named header failure), and `type` now falls back to `Sport Type` so an era that renames only the coarse column still imports. The blank CELL keeps its lenient `run` default deliberately. Web-only: the mobile importer does not share the shape.
- [x] **`cloudExportJobFromResponse` casts `format` where it grades everything else.** The function's own doc block is about being fail-closed in two directions and it is, for `status` and for a `ready` job with no URL — but `format: str(body.format) as CloudExportFormat | undefined` takes whatever string the server sent. A server answering `format: "zip"` renders "zip" on the export card. Cosmetic only (nothing branches on it) which is why it is not in the round, but it is the one unvalidated field in a module whose contract is that there are none. Closed 2026-09-02 — [decisions § 980](../architecture/decisions.md). The "renders 'zip' on the export card" premise was wrong: neither client renders the field, web reads it only as `=== 'gpx'` / `=== 'backup'` and mobile only as `== 'csv'`. What was real is that the cast made the TYPE a claim the value had never been checked against. Graded on both rails of the `cloud_export_helpers` parity pair at once — the Dart half took the same raw string — and mirrored to the iOS twin.
## Surfaced by the #789 native round 31 (2026-09-02)

- [x] **Neither Go service runs `go test -race` in CI, so the concurrency in the live hub is never exercised by the detector.** `ci.yml`'s `Test job_worker (Go)` and `Test graph_cycle sidecar (Go)` jobs both run a plain `go test ./...`. The job_worker binary is the most concurrent thing in the repo — an in-process pub/sub hub with per-room goroutines, a GC sweeper, a bridge poller, an atomic heartbeat and six HTTP servers on one mux — and nothing has ever run the race detector over it in CI. Both suites are race-clean today, measured locally on `test/789-r31-native`: `job_worker` 20 s → 35 s and `graph_cycle` 5 s → 5.5 s with `-race`, so the cost is one job-minute. Not fixed in-round because `.github/workflows/ci.yml` belongs to no lane this round; the change is `-race` on those two `go test` lines and nothing else (both jobs are already in the `CI gate` aggregator's `needs:`). Closed 2026-09-02 at round-31 integration — [decisions § 935](../architecture/decisions.md).
- [x] **The app has two elevation-gain rules and reports different vert for the same track depending on which surface asks.** `runs/run_stats.ts` `elevationGainMetres` sums every upward delta with no noise gate; `routes/route_simplify.ts` `computeElevationGain` gates at `ELEVATION_GAIN_MIN_DELTA_M` (3 m) and resets the reference on a real descent. Both carry the dropout carry-forward, so the difference is purely the gate. A run shown as climbing N metres therefore becomes a route claiming materially less the moment the runner taps "save as route", with no explanation anywhere — and on a barometer-noisy or GPS-altitude track the gap is large, which is the case the gate exists for. Both watch ports now match their own source ([decisions § 902](../architecture/decisions.md) for the run-stats one, [§ 925](../architecture/decisions.md) for the route one), so the rails are faithful and the inconsistency is upstream of them, on web, where it is a product decision rather than a port question: either the run stat should gate too, or the two should be documented as answering different questions. Closed 2026-09-02 — [decisions § 981](../architecture/decisions.md): the run stat now delegates to `computeElevationGain`, so there is one rule. It was not a product decision after all — **mobile has never had the second rule** (`run_detail_screen.dart` reads a run's climb through the Dart `computeElevationGain`), so the same run was reading differently on web and on the phone and documenting the split would have been writing down a bug. Measured on an AR(1) altitude-error model before deciding: a flat 30-minute run with barometer-grade error reported 143 m of climb ungated against 3 m gated, a real 200 m climb 267 m against 197 m, and a clean noise-free track 225 m on both.
- [x] **`route_geometry`'s `markerPointAtDistance` has no watch port, and the omission is not written down anywhere.** **CLOSED — already done, verified 2026-09-02.** The module doc of `apps/custom_watch/core/src/route_geometry.rs` has named the exclusion since `69839feca` (#848): "Web additionally carries `markerPointAtDistance`, the marker-authoring inverse; it has no wrist surface and is not ported, which is why the suites are 36 here against web's 43 rather than equal." Counts re-measured: 43 `test(` blocks in the web suite, 36 `#[test]` in the Rust one, and exactly 7 of the web blocks are `markerPointAtDistance` cases — 43 − 7 = 36, so the gap is entirely and only this. (An eighth web block mentions the function incidentally inside a `distanceAlongRoute` case, which is why a naive grep says 8.) Original filing below. Seven of the web module's 43 cases are that function; the other 36 are mirrored one-for-one, so the count gap is entirely this. It is the route-editor "place this course marker at mile 5" path and authoring is web-canonical per [§ 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive), so leaving it out is almost certainly right — but the module doc claims the port without naming the exclusion, which is how a later reader reads a 36-vs-43 count as drift. Either state the exclusion in the module doc or port it; do not leave the count unexplained.
- [x] **`unfed::Unfed::ALL` and `UnfedClass::ALL` are hand-written registers that a new variant can silently miss, and the width assertions iterate them.** **CLOSED — verified 2026-09-02.** Closed by [decisions § 960](../architecture/decisions.md) in `9bb122d64` (#851); the box was never ticked. Re-verified independently on this round's base: `apps/custom_watch/core/src/unfed.rs` now carries a `#[cfg(test)]` module that reads its own source with `include_str!` and holds both `ALL` arrays against the enum bodies they claim to enumerate (`every_unfed_variant_is_registered_in_all`, `every_unfed_class_variant_is_registered_in_all`), plus the two distinctness checks the filing asked for (`no_two_reasons_read_the_same`, `no_two_classes_share_a_slot_token`) and the hint-class pairing. Both parsers carry a floor on the variant count and an is-identifier check, so a body this parse cannot speak for fails loudly rather than reporting agreement over zero variants. The module went from zero runtime tests to five. Original filing below. `class()` and `reason()` are exhaustive matches, so adding a variant forces both to be updated — but `ALL` is a plain array, and the `const` block that asserts every reason fits `face::COLS` walks it. A variant added without registering therefore compiles with a reason of any width and draws off the panel, on the one module whose entire purpose is that every empty page speaks one vocabulary. Nothing checks that two variants do not share a `reason()` string either, which would defeat the same purpose from the other direction. The module has zero runtime tests today (its coverage is the `const` block alone). A source-level guard counting enum variants against the array, plus a distinctness check over `reason()` and `slot_token()`, is the shape.
- [x] **`ELEVATION_GAIN_MIN_DELTA_M` now exists on three rails and is registered in none of them, which is exactly how it came to be missing from one.** Web `routes/route_simplify.ts` `ELEVATION_GAIN_MIN_DELTA_M = 3`, mobile `route_simplify.dart` `kElevationGainMinDeltaM`, and — since [decisions § 925](../architecture/decisions.md) — `watch_core::route_simplify::ELEVATION_GAIN_MIN_DELTA_M`. `scripts/check_watch_wire_vectors.mjs`'s `CONSTANT_ROWS` is the registry built for exactly this shape (it already holds the off-route hysteresis and five Minetti constants across four rails each, with `rustConst` / `dartConst` / `tsConst` readers), and one row would make a future one-sided change fail the `watch-wire-vectors` job instead of shipping. Not added in-round because `scripts/` belongs to no lane this round; the entry is `rustRail(RS_ROUTE_SIMPLIFY, rustConst('ELEVATION_GAIN_MIN_DELTA_M'))`, `dartRail('apps/mobile_android/lib/route_simplify.dart', dartConst('kElevationGainMinDeltaM'))`, `otherRail('apps/web/src/lib/routes/route_simplify.ts', tsConst('ELEVATION_GAIN_MIN_DELTA_M'))` — a `why` naming the noise gate, and nothing else. The same argument applies to the 5 m span in `isTrackRenderable`, which is a bare literal on all three rails. Row added 2026-09-02 at round-31 integration and mutation-checked on each of the three rails — [decisions § 936](../architecture/decisions.md); the `isTrackRenderable` span is still open, and needs naming on each rail first.
- [x] **`training_load::median` panics on an empty slice, and its only caller's emptiness guard is the whole proof.** **CLOSED — verified 2026-09-02.** Closed by [decisions § 961](../architecture/decisions.md) in `9bb122d64` (#851); the box was never ticked. Re-verified: the signature is `fn median(xs: &mut [f64]) -> Option<f64>` and it returns `None` on an empty slice before the `xs[mid - 1]` that used to underflow, `compute_calibration` folds the emptiness branch into the same expression, and `median_of_nothing_is_none_rather_than_a_panic` pins it. Original filing below. `xs.len() / 2` is 0 for an empty slice, `0.is_multiple_of(2)` is true, and `xs[mid - 1]` then underflows — in release, where `overflow-checks = false`, that wraps to `usize::MAX` and the index panics anyway. `compute_calibration` returns early on `trimps_per_km.is_empty()` so it is unreachable today, and the NaN half of the same function was closed this round ([decisions § 926](../architecture/decisions.md)). Filed rather than fixed because the honest options are a signature that cannot express the empty case or a sentinel that changes what a caller reads, and neither is worth choosing without a second caller to design against.
## Surfaced by the #789 round 31 Playwright e2e lane (2026-09-02)

The three defects this lane found were fixed in-round
([decisions § 904](../architecture/decisions.md) / [§ 905](../architecture/decisions.md)).
These are what is left: two more instances of the same `functions.invoke`
envelope shape that no spec of this lane proves wrong today, one honest
untestable, and one narrow undo bug.

- [x] **`startDonationCheckout` and `edgeFunctionExport` still rethrow the raw `FunctionsHttpError`.** Both were left alone because neither currently PUTS the message in front of a person — the fundraiser page catches and shows the fixed `fundraiser.donateFailed`, and the legacy export path's message reaches a toast that reads as a generic failure either way. So nothing leaks the internal sentence there, and there was no defect for a spec to prove. What both DO lose is the refusal itself: `donations-checkout` can answer `fundraiser_closed` / `host_cannot_take_payment`, and a donor told only "could not start the donation" cannot tell "this fundraiser has ended" from "try again in a minute". `core/edge_function_error.ts` now exists, so closing this is an unwrap plus a code→copy map on each, exactly the shape the event-register path took. Do it when either surface next gets worked on rather than as a sweep — each needs its own copy decisions and its own i18n keys. Both closed 2026-09-02 — [decisions § 982](../architecture/decisions.md) (donation) and [§ 983](../architecture/decisions.md) (export). Two corrections to the filing: the code is `owner_cannot_take_payment`, not `host_cannot_take_payment` (a map written from this text would have silently never matched, which is now guarded), and the export path did NOT merely reach a generic toast — `settingsAccount.exportFailed` interpolates `{error}`, so "Edge Function returned a non-2xx status code" was rendered to the subject verbatim.
- [x] **`settings/integrations-connected.spec.ts`'s "Disconnect Strava" test cannot pass against a local stack, and nothing says so.** Disconnect goes through the `strava-import` Edge Function on purpose (it revokes at Strava's end and wipes the vault rows rather than doing a bare DELETE), so it needs the Strava env CI's functions host carries and the local one does not. Reproduced against `origin/main` with this lane's source changes reverted, so it is environmental and not a regression — but a spec that is red on every developer's machine and green on CI trains people to ignore reds. Either skip it when the env is absent (the way `paywall-wire.spec.ts` auto-skips on `BYPASS_PAYWALL`) or say so in `test_inventory.md`; a silent permanent red is the worst of the three options. **Closed 2026-09-02 by taking the second option, because the first rests on a false premise.** Measured: the disconnect path needs NO Strava credentials (`plantIntegration` writes no tokens, so `handleDisconnect` resolves an empty access token and skips the deauthorize outright), and CI's `e2e-web` job carries none either — it runs `supabase start` and never `functions serve --env-file`, and no `apps/backend/supabase/.env` is committed — so an env-keyed skip would have skipped on CI too and deleted the coverage. The real local failure is the ONE edge-runtime container shared across every worktree: it is bind-mounted at the tree it was started from, and once that directory is replaced under it every invoke answers `{"code":"BOOT_ERROR"}` / 503 with `failed to determine entrypoint` in its log — reproduced against strava-import, race-listings-sync and race-results-import together. That is not per-spec and no per-spec skip can express it, so the cause is written into the spec header and `test_inventory.md` instead.
- [x] **A deferred wear-log delete whose commit later fails repaints a stale list into whichever gear modal is open.** `removeWearLog` in `/settings/gear` captures `before = wearLogs` and restores it on undo OR on a commit failure. Undo is immediate and safe; the commit runs when the window closes, by which time the runner may have closed that modal and opened another pair's, and `restore` then writes gear X's observations into gear Y's open modal. A reload fixes it and nothing is destroyed (the row survives, which is the fail-safe direction), so this is cosmetic — but it is the only adopter of the undo queue and the same shape will be wrong in a worse place when the second one arrives. The durable fix is for `restore` to be a no-op when the list it snapshotted is no longer the one on screen, which means the callback needs to know what it is restoring INTO. Closed 2026-09-02 — [decisions § 984](../architecture/decisions.md). The queue's contract is untouched, so nothing is owed on the Dart side. Reference identity turned out to be the wrong test (`wearLogs` is a `$state` array, so the value read back is a proxy, not the array assigned); an explicit epoch bumped by a single `setWearLogs` is, and a source guard fails the PR on any write that bypasses it.
## Surfaced by the #789 round 31 mobile lane (2026-09-02)

The three boxes this lane could close outright are ticked above — the two
`off_route_alert` halves and the `e164` hyphen family, each fixed on both sides
of its parity pair together
([decisions §§ 929-930](../architecture/decisions.md) and
[§ 933](../architecture/decisions.md)). Nine more pairs were registered after
being measured, four of them carrying live divergences that were fixed
([§ 931](../architecture/decisions.md)). What follows is what the lane found and
did not take.

- [x] **`public.runs` has no `distance_m >= 0` or `duration_s >= 0` CHECK, and at least one client was written believing it did.** Closed 2026-09-02 ([decisions § 939](../architecture/decisions.md)): migration `20270704000001` adds `runs_distance_m_check`, `runs_duration_s_check` and `runs_elevation_gain_m_check`, each `NOT VALID` then validated, behind a predicate-scoped repair that matches nothing on a healthy database. The bound is NOT the `>= 0` the box asked for: `'NaN'::numeric >= 0` is true, so each numeric term names NaN explicitly and `elevation_gain_m` names Infinity too. Two exploits measured over PostgREST with an ordinary password-grant JWT before the fix — a NaN run took rank 1 of every distance challenge its author had joined, and a negative `duration_s` became the account 5k best. Not registered in `check_constraint_unions.mjs`: its coverage rule reads set-shaped `check (col in (…))` only and an entry for a range column fails it. Pinned by `runs_physical_quantity_bounds_test.sql` (16 assertions, three mutations killed). Original entry: The CSV importer passed a negative distance straight through under a comment naming "the DB CHECK (distance_m >= 0, duration_s >= 0)" as the source of truth. Those CHECKs are on `event_results` and `gym_workouts`; `runs.distance_m` is `numeric(10, 2) not null` with no constraint, and `duration_s` is a bare `integer not null`. The client half is closed ([decisions § 934](../architecture/decisions.md)) but the column is still the wrong shape: `numeric` accepts `NaN` and `Infinity` as well as negatives, so any writer that gets it wrong — a future importer, a hand-run SQL fix, a third-party integration — silently poisons every aggregate that sums the column. Not taken in-round because `apps/backend/` belongs to no lane this round AND because these are populated prod tables: the change is a `NOT VALID` add plus a separate `VALIDATE`, per `docs/backend/migration_locks.md`, and it needs a backfill decision for any row already out of range. Add `runs_distance_m_check` and `runs_duration_s_check` together, and register the column in `check_constraint_unions.mjs`'s `PAIRS` with an empty `clients` list and a note (no client enumerates a range).
- [x] **`classifyStravaRow` and `indexHeader` are web-only, and the mobile analogue does not share the defect — so the filed entry above needs a web owner, not a mobile one.** Determined this round: `apps/web/src/lib/integrations/strava-zip-*.ts` has no Dart counterpart, because mobile's Strava path (`strava_importer.dart`) reads the API rather than a bulk-export ZIP, and mobile's own CSV importer resolves columns to a nullable index with an explicit missing-required-columns failure rather than to `-1`. So the `row[-1] → undefined → 'import'` chain exists on web alone. The mobile lane closed the equivalent class where it does exist (an unvalidated `activity_type` reaching a CHECK-constrained column, § 934), but the web entry stands untouched and unticked. Closed 2026-09-03 — [decisions § 1042](../architecture/decisions.md). **The filing was stale in its own last sentence**: the entry above is `- [x]`, closed by § 979 on 2026-09-02. Its ownership claim was right and the chain WAS still live — on three other columns. `Activity Date` unresolved stamped every row with the moment of the import (five years of history on today, corrupting streaks, PR brackets, the calendar and training load); `Moving Time` gave every run a zero duration; neither distance column resolved gave every run zero distance. All six required columns are now refused at the header, naming whichever are missing, with the distance requirement satisfied by either block; and a single row whose date cell cannot be parsed is refused and reported in the `ImportFailureReport` rather than filed under today. Pinned by `strava_zip_required_columns.test.ts` — executable on `indexHeader`'s -1 premise, source-level on the guard, both mutation-tested.
- [x] **Every mobile deploy gate that reads a value out of the settings bag inherits the `cycle_plan` shape, and nothing sweeps for it.** `cycle_plan` failed differently on each platform for a malformed date because `parseInt` yields NaN where `int.parse` throws; fixed there ([decisions § 934](../architecture/decisions.md) records the class, § 931 the pair). The settings bag is jsonb, so every `int.parse` / `double.parse` / `DateTime.parse` reached from a value another client wrote has the same exposure, and the non-throwing `tryParse` family has the mirror-image one — it accepts the literals `NaN` and `Infinity`, which is what put a non-finite coordinate into a `Waypoint` on all four import formats. A source-level guard in the mobile suite (no bare `int.parse` / `double.parse` on a settings-bag or file-parsed value; every `double.tryParse` result checked for `isFinite` before it reaches a model) would turn a class the lane found three instances of into one that cannot recur. Filed rather than written because the guard needs an allowlist for the many legitimate in-repo parses and that list is the real work. Closed 2026-09-02 — [decisions § 1011](../architecture/decisions.md). **The filed class is empty**: exactly two call sites in the whole tree read a jsonb bag key and both were already handled. The class BESIDE it is real — `double.tryParse('NaN')` is NaN and `('1e400')` is Infinity, both non-null — and its allowlist is empty, so the guard was writable after all: 26 call sites, 23 already conforming. Three were not, one live on a shipped path (`geocoding.dart` built a `PlaceResult` from a Nominatim string with no finiteness and no range test; the MapTiler branch was exposed too, because `jsonDecode('1e400')` is Infinity).
- [x] **`csv_run_importer` now imports `preferences.dart` to reach the `ActivityType` vocabulary, which drags Material into a pure parser.** The import is `show ActivityType` and the alternative was worse — retyping the five values would create a second unregistered rail against `runs_activity_type_check`, which is the drift the constraint-union guard exists to prevent. But the vocabulary does not belong in a file that also holds `IconData` getters and a `SharedPreferences` cache. Moving the enum to `packages/core_models` (where `MetadataKeys` already lives, and which the importer already imports) would fix it properly, at the cost of touching every one of the eight `ActivityType.values` call sites plus the rail's registration in `check_constraint_unions.mjs`. Worth doing next time `core_models` is open. Closed 2026-09-02 — [decisions § 1013](../architecture/decisions.md).
- [x] **31 numeric columns in `public` carry no CHECK constraint at all, and two of them are rendered on a public map.** Closed 2026-09-03 ([decisions §§ 1046-1048](../architecture/decisions.md) + [§ 1051](../architecture/decisions.md)): migrations `20270705000001` (the eleven an anonymous spectator reads), `20270705000002` (the remaining NaN-capable ones), `20270705000003` and `20270705000004` (the integer family), each `NOT VALID` then validated with the pre-flight query set in its header. **The count is right for the subset it was about and wrong for the question it asks**: 31 is exactly the `numeric` / `double precision` set, name for name, but **79** numeric columns carried no single-column CHECK — the other 48 are integer-family, where NaN is unreachable at the type and the exposure is a nonsense sign. Both halves are closed. The ping exploit was measured, not reasoned about: from an ordinary account's own session one INSERT lands `lat = 'NaN'`, `lng = 'Infinity'`, `ele = 'NaN'`, `distance_m = 'NaN'`, `elapsed_s = -5`, `bpm = -40` on its own public run, and `anon` reads all six back. PostGIS raised a coercion NOTICE and clamped only its own derived point. **One column was not merely unbounded but unstatable**: `duplicate_plan_week` parked the tail of a plan at negative `week_index` to renumber it under a per-row unique constraint, so `week_index >= 0` broke the plan editor's duplicate-week button — closed by making that constraint `deferrable initially immediate` rather than by moving the hop into a positive scratch space (§ 1048). Five exemptions with reasons: four sequence-backed surrogate keys and the generated `segments.length_m`. The guard the entry asked for is rule 3 of `numeric_bounds_reject_nan_test.sql`, which now *requires* a bound rather than only grading the ones that exist, mutation-verified in both directions; and the entry's expectation that `check_constraint_unions.mjs` would need an entry does not apply — these are range bounds and its coverage rule reads `check (col in (…))` only. Original entry: The 2026-09-02 sweep that produced [decisions § 940](../architecture/decisions.md) closed every *existing* numeric bound that admitted NaN. It says nothing about a column that was never bounded, and the catalogue lists 31: `live_run_pings.lat` / `lng` / `ele` / `distance_m` and `race_pings.lat` / `lng` / `distance_m` (client-written, drawn on the anonymous spectator map — a NaN coordinate is a broken render for every viewer, not just the writer), `events.meet_lat` / `meet_lng`, `routes.distance_m` / `elevation_m`, `segments.length_m`, `global_segments.elevation_m`, `gym_workouts.volume_kg` (a derived cache, so its writer is a trigger — but nothing stops a direct write if a grant ever widens), `events.distance_m`, `event_checkpoints.position_m`, `route_markers.position_m`, `route_conditions.position_m`, `checkpoint_crossings.body_weight_pct`, `achievements.value_num`, `challenge_badges.final_value`, `monthly_funding.amount_received`, `plan_weeks.target_volume_m`, `plan_workouts.target_distance_m`, `training_plans.goal_distance_m` / `vdot`, and the five `fitness_snapshots` metrics. Not taken in the same round because each one needs its own answer — a latitude's range is not a distance's, `training_stress_bal` is legitimately negative, and a blanket finiteness constraint would be 31 constraints nobody had thought about individually. The two ping pairs are the ones to do first: they are the only entries on the list whose bad value is visible to someone other than its author. `numeric_bounds_reject_nan_test.sql` already holds the machinery — extending its rule 1 to *require* a bound rather than only grade the ones that exist is the shape of the guard once the columns are bounded.

- [x] **`npm run check:script-types` is RED on `test/789-r31-integration` itself, in three files no round-32 lane touched.** 14 errors across `scripts/check_toolchain_pins.mjs` (6), `scripts/check_toolchain_pins.test.mjs` (2) and `scripts/check_twin_claims.test.mjs` (6) — mostly `TS7006` implicit-`any` parameters on destructured helper arguments, plus three `TS2345`s where a `string | null` is passed to a parameter tsc has inferred as `null | undefined` because every call site in view passes a literal `null`. Confirmed **not** introduced by the sql lane: `git diff --name-only f20c19abd..HEAD` names no file under `scripts/`, and all three failing files are byte-identical to their base-commit versions. The `workflow-lint` job runs this guard, so the branch fails CI on it regardless of what the four lanes land. The fix is a JSDoc `@param` annotation on each destructured helper signature (the tree is `checkJs` + `strict` with no root `tsconfig.json`, so an un-annotated parameter has nothing to infer from) and widening the two toolchain-pin helper signatures to the `string | null` they are actually called with. Not taken by the sql lane because `scripts/` is outside every lane's owned paths this round and a concurrent edit there would conflict; handed to whoever integrates. **Closed — verified 2026-09-03** ([decisions § 1056](../architecture/decisions.md)). Verified before acting rather than after: `npm run check:script-types` exits 0, and `git diff --stat main -- scripts/` from the round-35 wear branch is empty, so all three files are byte-identical to today's `main` and the guard is green on both. Round 33's `9bb122d64` (PR #851) fixed it in exactly the shape this entry prescribed — JSDoc `@param` annotations, 43 of them in `check_toolchain_pins.mjs` alone, with no tsconfig loosened. Nothing left to do.
## Surfaced by the #789 round 32 wrist lane (2026-09-02)

Four defects were fixed in-round across the three wrist tiers
([decisions §§ 944-948](../architecture/decisions.md)). What follows is what
this lane found and could not take, mostly because the artefact belongs in
`scripts/` or `.github/`, which no lane owned this round.

- [x] **`apps/watch_garmin/scripts/check_garmin_source.sh` is not wired into CI, so nothing runs the only guard the Garmin tier has.** **CLOSED 2026-09-02** ([decisions § 963](../architecture/decisions.md)). Its own `watch-garmin-source` job rather than a step in `watch-ios-locale-parity`: that job is already bundled and already annotates per step, so rule 2 would have been satisfied — but the job NAME is what a reader sees red in the checks list, and "watchOS source-level guards" going red for a Garmin defect is a name wrong about the TIER, which no per-step annotation corrects. Ungated, for the reason its sibling is. Two steps (the guard, then its own new 20-case mutation suite), each with an `::error::`, and the job named in the `CI gate` `needs:` list — both obligations were proved enforced by deleting them and watching `check_ci_diagnostics.mjs` refuse. Writing the suite found a live defect underneath the filing: `body_of` located declarations by SUBSTRING, so `function onTimerResetX` satisfied the claim that `onTimerReset` is overridden. Original filing below. It needs no Connect IQ SDK — bash + python3, the same shape as `apps/watch_ios/scripts/check_xcstrings_parity.sh`, which the `watch-ios-locale-parity` job already invokes the same way. Wiring is one step in an existing job (the `watch-ios-locale-parity` job is the natural host: it is already the "read the tier a compiler cannot reach" job) plus, if it becomes its own job, an entry in the `CI gate` aggregator's `needs:` list — a job the gate never waited for counts as no result, so an omitted line makes its red invisible rather than merely unlabelled. If it lands as a second guard inside an existing job, that job becomes a bundled one and every check step in it must print its own `::error::`. Until this is done the guard is a script: it passes today and nothing will notice the day it stops.
- [x] **The 99:00 live-pace ceiling and the 0.4 m/s walk gate are unregistered rails of `check_watch_wire_vectors.mjs`.** **CLOSED — verified 2026-09-02.** Round 33's guard lane ([decisions § 962](../architecture/decisions.md)) added both `CONSTANT_ROWS` entries and they run: `node scripts/check_watch_wire_vectors.mjs` reports `[OK] live-pace ceiling (s/km) = 5940 on 2 rails` and `[OK] live-pace walk gate (m/s) = 0.4 on 2 rails`. The filing's optional "if the chain is to be closed end to end" half is closed too, by three links rather than by one four-rail row: `apps/custom_watch/core/src/alerts.rs`'s `the_pace_band_ceiling_is_the_apps_own_live_pace_ceiling` asserts `PACE_BAND_MAX_S_PER_KM == grade_adjusted_pace::MAX_PACE_S_PER_KM`, and the `pace-band ceiling (s/km)` row pins that same firmware constant against the phone's `kWorkoutPaceMaxSecPerKm` (`= 5940 on 2 rails`). So Garmin `MAX_PACE_S` = firmware GAP = firmware alerts = phone, with every link read by something that fails. Original filing below. Both are now named constants on the Garmin side (`MAX_PACE_S`, `MIN_SPEED_MPS`), so `mcConst` can read them; the firmware holds `grade_adjusted_pace::MAX_PACE_S_PER_KM` and `MIN_SPEED_MPS`, whose doc comments both name the Garmin field as their source. Two new `CONSTANT_ROWS` entries — the ceiling across `RS_GAP` / `MC_GAP` (and, if the chain is to be closed end to end, `alerts::PACE_BAND_MAX_S_PER_KM` and the phone's `kWorkoutPaceMaxSecPerKm`, which are already a registered pair with each other), and the walk gate across `RS_GAP` / `MC_GAP`. Nothing is drifting today; the point is that a comment claiming a mirror is an instruction, not an enforcement.
- [x] **The phone-to-watch route-push envelope is read from one end only.** **CLOSED 2026-09-02** ([decisions § 966](../architecture/decisions.md)). Claim (7) of `check_watch_ios_source.mjs`, which already reads the opposite direction on the same session and is already CI-wired, so no new job. Measured before it was written: all three rails parse to the same five keys (`route_id`, `route_name`, `route_distance_m`, `route_lat`, `route_lng`), so the filing's "they agree today" is confirmed and this covers the future rather than a live defect. The comparison is pairwise over all three, in both directions, and each rail's rename fires it independently. The phone rail's parse deliberately reads its `args[…]` subscripts AND its repacked dictionary literal: `routeUserInfo` does both in one function, so a key it lifts and forwards under another name is a field dropped between two of its own lines. Original filing below. `WatchIngestBridge.routeUserInfo` (phone, Swift) writes `route_id` / `route_name` / `route_distance_m` / `route_lat` / `route_lng` into a `transferUserInfo` payload; `ArmedRoute.decode` (watch, Swift) reads exactly those five back; `apple_watch_route_bridge.dart` supplies them as method-channel args, a third rail. `check_watch_ios_source.mjs` reads the OPPOSITE direction on the same session (the `transferFile` run hand-off metadata) and does not touch this one, and `check_watch_wire_vectors.mjs` registers only the point-cap constant across those three files, not the key set. The three lists agree today — checked by hand this round — but a key renamed on one side is a route the runner arms on the phone that silently never reaches the wrist, which is precisely the failure the hand-off guard was written after. The claim is the same code: parse `payload["k"]` / `args["k"]` off each rail and compare the sets in both directions.
- [x] **No GAP rail has a minimum-RISE gate, only a minimum-run one, so sub-noise altitude reads as a wall on all four.** Closed 2026-09-02 — [decisions § 992](../architecture/decisions.md). Every arithmetic claim in the filing re-measured true (0.60 grade, factor 5.396; a 1 m step at 2.50). **The instrument is the window, and a rise threshold cannot work at any window**: the rise a real grade produces over the window scales with the window exactly as the noise does, so a 3 m gate zeroes every real grade under 60 % / 15 % / 10 % at a 5 / 20 / 30 m window while the GPS-altitude noise it aims at has a 1.95 / 3.82 / 4.61 m rise sigma and walks straight through — and on a CLEAN 3.3 % climb a 1 m gate read 19.5 % slow. `MIN_SEGMENT_M` is 20 m on all four rails: measured over an AR(1) altitude-error model, a flat 30-minute run reported GAP 47.9 % fast at 5 m against 26.8 % at 20 m under GPS altitude, and 9.0 % against 2.1 % at power-hike pace under a barometer, for a 2.5 % cost on the most oscillating realistic profile. **No `CONSTANT_ROWS` row is needed** — the `minimum segment before a grade sample is trusted (m)` row already binds all four, and `node scripts/check_watch_wire_vectors.mjs` reports it at 20 on 4 rails. `roadbook` shares the constant on three rails and moves with it. The Garmin rail is source-level only (no Connect IQ SDK on this machine). `MIN_SEGMENT_M = 5.0` gates the horizontal leg a grade sample is measured over; nothing gates the vertical. The repo's own barometric noise floor is `ELEVATION_GAIN_MIN_DELTA_M = 3` — a delta smaller than that is not treated as gain at all — and a 3 m rise over a 5 m run is a grade of 0.60, which clamps to `MAX_GRADE` and yields a factor of 5.396: the field would report a pace 5.4x faster than raw off a rise the gain path discards as noise. Even a single 1 m step is 0.20 grade and a factor of 2.50. This affects `grade_adjusted_pace` on all four rails (web, Dart, firmware, Garmin) identically, so it is not a Garmin defect and must not be fixed on one rail — it is a lockstep change to a registered constant set, plus a decision about whether the right instrument is a longer window or a rise threshold. Worst on a watch with no barometric altimeter, where `Activity.Info.altitude` is GPS altitude and the noise is metres, not tenths.
- [x] **The Garmin field picks its pace unit from `distanceUnits`, which may not be the setting the runner set for pace.** **CLOSED 2026-09-02** ([decisions § 964](../architecture/decisions.md)). Verified against the Connect IQ API docs rather than against an SDK: `System.DeviceSettings.paceUnits` exists, is `System.UnitsSystem` (`UNIT_METRIC` / `UNIT_STATUTE`) exactly as `distanceUnits` is, and is available since **API 1.0.0** against this app's `minApiLevel` of 3.1.0 — so every device in `manifest.xml` has it. One line, plus a sixth claim in `check_garmin_source.sh` and two cases in its suite, because the two members share a type: the wrong one compiles, runs, and is wrong only on a watch whose two preferences differ. Original filing below. `System.getDeviceSettings()` exposes distance and pace as separate unit preferences on Garmin devices, and a runner who logs distance in miles while reading pace in min/km is a real configuration. `GradeAdjustedPaceView.initialize` reads `distanceUnits`. Not changed in-round: the Connect IQ SDK is on no machine here, so which member exists and what it returns cannot be checked, and swapping to an API this lane cannot compile would trade a possible mislabel for a possible build break. Verify against the SDK docs the next time anyone has it installed; if `paceUnits` is the right member, the fix is one line and the value is read once at construction either way.
## Surfaced by the #789 coverage round 32 — infra + Lambda tier (2026-09-02)

Six defects this lane found were fixed in-round
([decisions §§ 949-953](../architecture/decisions.md)): the coach Lambda taking
the whole shared secrets file, `bin/key-rotate.sh` matching no rule in the
estate `.sops.yaml` on either env (so the compromise-response path was
unconditionally broken), five Lambda cores writing twenty bytes of the caller's
bearer token to CloudWatch, the osrm-proxy 405 with no `Allow`, a Function-URL
source guard that could not fail on a ninth Lambda left open, and a log-hygiene
reachability walk that stopped at the `$lib` import spelling. These are what the
lane found and did not take.

- [x] **The distribution-wide `404 -> /index.html at 200` rewrite destroys every Lambda origin's deliberate 404, and two Lambdas were written believing it does not.** **CLOSED 2026-09-02** ([decisions § 1022](../architecture/decisions.md)) — and the proposed fix was the wrong one. The S3-never-404s premise **holds and was proved from this repo**: the site bucket policy is one statement granting `s3:GetObject` on `${bucket}/*` to `cloudfront.amazonaws.com` and nothing else (`s3:ListBucket` appears nowhere under `infra/` outside the two deploy-role policies), and the origin is `bucket_regional_domain_name` + OAC — the REST endpoint, not the `s3-website-` one that does 404. **But that is exactly why dropping the mapping is worse, not smaller**: the only responses the block ever sees are then the Lambdas' own, whose 404 bodies are `<p>This link isn't available.</p>` — unstyled, English-only, no navigation — and serving those would replace the SPA's designed localized not-found card (`share/run/[id]`'s `.notfound-card`, reached today ONLY because of this rewrite) on ten paths. `share-run/src/index.ts`'s own "the browser will fall back to the SPA's 404 surface" comment is true *because of* the mapping it describes as absent. The mapping now answers **404 with the shell body**: a crawler gets the status that de-indexes a URL (far more decisively than a `noindex` on a 200), a reader still lands on the app, and the bytes a viewer receives do not change at all. 403 stays at 200 and must. `scripts/check_infra_coverage.mjs` claim 4 fails any 4xx/5xx -> 2xx mapping, with the one SPA deep-link exemption declared and its reason recorded; a declared exemption nothing uses fails too. Original filing below. CloudFront models `custom_error_response` per DISTRIBUTION, not per behaviour, so the 404 mapping in `infra/modules/web-stack/main.tf` rewrites a share Lambda's not-found answer into the SPA shell at 200. Both `apps/web/lambda/share-run/src/index.ts` ("crawlers get a clean signal") and `apps/web/lambda/share-entity/src/index.ts` ("a clean crawler signal", "a 404 rather than guess") state the opposite in their own comments; main.tf's comment states the true behaviour and records it as MEASURED for the 403 case (issue #590, 2026-07-21). Consequence: every `/share/{run,route,recap,badge,event,profile,club,race,session,workout}/<id>` for a private, deleted or never-existing entity serves a crawler the generic SPA shell at 200 with **no `noindex`** — the tag exists only in a body that never ships — so those URLs are soft-404s Google is invited to index. The durable fix is to **drop the 404 mapping and keep the 403 one**: the site bucket policy grants `s3:GetObject` and nothing else (no `s3:ListBucket`), so S3 answers a missing key with 403 AccessDenied and never 404 — which is precisely why the 403 mapping exists and why the 404 one buys the SPA nothing. Not taken in-round because it is a production behaviour change this lane could neither `plan` nor verify (no AWS credentials, by design), and if the S3-never-404s premise is wrong it breaks every deep link at once. Verify it against the real distribution first (`curl -I https://threkir.com/share/run/<a-private-id>` should become a 404 carrying the Lambda's own body), then delete the `error_code = 404` block. The four share wrappers' new runtime suite already pins the `noindex` body, so the moment the mapping goes the assertion becomes a claim about what a crawler sees rather than about what a function returns.
- [x] **The three WAF rate-limit scope-downs match `uri_path` with `text_transformation = NONE`, while CloudFront's own behaviour matching normalises the path independently.** **CLOSED 2026-09-02** ([decisions § 1023](../architecture/decisions.md)). `URL_DECODE` added at priority 1 on all three, **without a live measurement and deliberately so**: whether CloudFront decodes before behaviour matching is a fact about the edge and is NOT claimed here, but being wrong about it cannot cost anything, because on a search string containing no `%` decoding can only ADD matches — `%XX` becomes one byte and the string never lengthens, so a raw path whose first ten characters are already `/api/coach` still has them afterwards. It is a strict widening of a RATE LIMIT, never of an allow/deny; if CloudFront does not decode either, the cost is counting a request the edge was going to 404. `LOWERCASE` stays out and the guard now fails if anyone adds it. `scripts/check_infra_coverage.mjs` claim 5 also fails a rate-based rule with no readable scope-down — which would apply the counter to every static asset and turn a backstop into an outage. Original filing below. `infra/modules/web-stack/waf.tf` filters each rate-based rule to `STARTS_WITH "/api/coach"` / `"/api/routes/generate"` / `"/api/routes/osrm"` with a single `NONE` transformation. Two matchers, two normalisations: any encoded spelling CloudFront resolves to the behaviour but WAF does not resolve to the prefix reaches the Lambda with the per-IP cap not applied — and those three paths are the only ones on the distribution that cost money or engine CPU to serve. Standard hardening is a second `text_transformation` at priority 1 with `type = "URL_DECODE"` (adding `LOWERCASE` would be wrong here, because CloudFront path patterns are case-sensitive and `/API/coach` genuinely does not route to the Lambda). Not taken because it cannot be verified offline: whether CloudFront decodes before behaviour matching, and in what order WAF and a viewer-request function run, are both facts about the live edge rather than about this repo. Confirm with one `curl` against preview before changing prod.
- [ ] **The preview env's KMS key is not wired into the estate `.sops.yaml`, so nothing can encrypt a preview secrets file today.** Observed while verifying the `key-rotate.sh` fix against the real `../infra-secrets/.sops.yaml` (creation rules only — nothing decrypted, no ARN recorded): the `^threkir/prod\.sops\.yaml$` rule carries a resolved KMS ARN, and the `^threkir/preview\.sops\.yaml$` rule still carries the literal `KMS_RUNNING_PREVIEW_ARN_PLACEHOLDER` that `bin/sops-init.sh` is supposed to replace. (The slot was `running/` when this was filed. The placeholder kept its old name through the rename, which no longer matters: since [decisions § 1615](../architecture/decisions.md) `bin/lib/estate.sh` finds the rule by the file's path, not by the placeholder's name.) `sops` refuses to encrypt against a placeholder, so `bin/secret-set.sh preview <KEY>` cannot work and the preview Lambda env has whatever `terraform apply` last gave it. Not a repo defect — the fix is one operator action against the private estate repo (`aws sso login --profile threkir` then `bin/sops-init.sh preview`, which reads `terraform output kms_key_arn` from `infra/envs/preview` and writes it into that rule, then seeds the encrypted file — a step that could not have succeeded before § 1615, because sops matched the seed's `/dev/stdin` input against no rule) — but it was invisible until now because `key-rotate.sh`, the one script that reads that rule back, could not find any rule at all and reported the same sentence for a wired key and an unwired one. Filed so the state is written down somewhere in the open repo; the key material itself stays where it is.
- [x] **`generate-route` re-implements the body cap instead of importing the helper that exists to stop exactly that drift.** Closed round 33 ([decisions § 968](../architecture/decisions.md)) — and it was three of the four body-capping endpoints, not one: the coach's own two sub-paths also spelled their caps twice. The `400 invalid body encoding` branch is **not** dead code and was not deleted: unreachable through the declared string type, but a non-string body reaches it, and removing the catch turns a client-side malformation into a generic 503. Original text: `apps/web/src/lib/coach/body.ts` owns `decodeLambdaBody`, and its module comment records why: the coach's two wrappers once diverged on `bodyStr.length` (UTF-16 code units) versus decoded bytes, letting a multi-byte payload roughly three times the cap through. `apps/web/lambda/generate-route/src/index.ts` inlines its own `Buffer.from(...)` plus a `byteLength` check with a private `BODY_LIMIT_BYTES`. It is byte-correct today, so this is not a live defect — it is the same one-copy-per-wrapper shape, one Lambda over, with a source guard (`security_guards.test.ts`, "Coach endpoint enforces a 256 KB body cap on both wrappers") that names only the coach. Two smaller things ride along in the same file: `decodeLambdaBody`'s `400 invalid body encoding` branch is unreachable, because `Buffer.from(s, 'base64')` ignores invalid characters rather than throwing; and the coach wrapper's sub-path dispatch is `rawPath.includes('/route-describe')`, a substring test where a suffix test is meant. Neither is exploitable — every sub-path behind `/api/coach*` runs the same auth and paywall gates — but both read as stricter than they are.
## Surfaced by the #789 round 32 shared-packages lane (2026-09-02)

Five defects this lane found were fixed in-round with their pinning tests
([decisions §§ 954-958](../architecture/decisions.md)). What follows is what it
found and did not take.

- [x] **Every generated `double` field in `db_rows.dart` casts with `as num`, while the web twin's type for the same two columns is `number | string`.** **CLOSED 2026-09-02** ([decisions § 985](../architecture/decisions.md)). The premise is right and the string arrival is no longer hypothetical: measured over the local stack, `json_agg` — the shape PostgREST builds its body with — renders `'NaN'::numeric` as `"NaN"` and `'Infinity'::numeric` as `"Infinity"`, and `'NaN'::numeric >= 100` is **true**, so `global_segments_distance_m_check` admits precisely the value the cast cannot decode. The count was low: **69** generated `double` reads, not 50. `gen_dart_models.dart` now emits `_toDoubleOrNull` / `_toDouble` at the top of the file and every `double` column reads through them — a `num` or a parseable `String`, screened for finiteness, resolving to `null` for a nullable column and `double.nan` for a non-nullable one (never `0`, which is a plausible-looking lie where NaN is unmistakably no answer, and which is also exactly what web's `Number(...)` yields). Ints stay on `as num`: no integer Postgres type has a non-finite value and `to_json` never quotes one. Pinned by `packages/core_models/test/db_rows_numeric_test.dart`; reverting the generator and regenerating fails 6 of its 7 cases. `GlobalSegmentRow.fromJson` reads `distance_m` as `(json['distance_m'] as num).toDouble()`, and `catalogue_browse.dart`'s own doc block says web widens those fields "because PostgREST may hand a `numeric` column over as a JSON string" and asserts this side "has already coerced through `as num` at its own boundary". `as num` is a cast, not a coercion: if that string ever arrives, the cast throws a `TypeError` inside `fromJson` and the catalogue screen dies where web renders. The package already believes the wire can do this — `effort_rank.dart`'s `_usableRank` explicitly accepts `raw is String ? num.tryParse(raw)` — so the two boundaries in one package disagree about the same risk. 50 generated reads share the shape. Not taken in-round because `db_rows.dart` is generated and the fix belongs in `scripts/gen_dart_models.dart`, which no lane owns this round; the change is to emit a shared `_toDouble(Object?)` helper that accepts a `num` or a parseable `String` and to have every `double` column read through it. Verifying which columns PostgREST actually stringifies needs the local stack, which this lane did not own either.
- [x] **`ApiClient._uploadTrack` throws `JsonUnsupportedObjectError` on a non-finite waypoint, and only one of its five producers now screens for one.** **CLOSED 2026-09-02** ([decisions § 986](../architecture/decisions.md)). Confirmed in every particular, including that the throw is an `Error` and not an `Exception` (measured). The decision the filing declined to make: **drop the point, at the serializers rather than at the producers.** A non-finite coordinate is the absence of a location (§ 954), so dropping it removes nothing real, while refusing the upload leaves the runner with exactly what they have today — a run that never leaves the phone. `finiteWaypoints` in `core_models` is applied in `Run.toJson` (covering the local store's five encode sites and the backup archive at once), in the in-progress slice writer, and in `ApiClient._trackBlobJson`, so every one of the five producers and every future one is covered without a remember-to rule. The drop is counted and `debugPrint`ed at the upload site. A second instance was found in the same pass and taken: `Run.toJson` also failed to encode a non-finite `distanceMetres`, which would have taken the shared local index down with the run, and it now resolves to zero. **No terminal-error taxonomy was added**: the retry-forever loop is a symptom of a permanent failure existing, and removing the failure removes the loop — a category with no reachable member is a preemptive abstraction. Pinned by `packages/core_models/test/finite_track_test.dart` (7), two cases in `api_client_codecs_test.dart` and two in `local_run_store_test.dart`; making `finiteWaypoints` return its input unchanged fails all three suites. `jsonEncode(track.map(_waypointToJson).toList())` refuses a non-finite double, so a single such point makes the run unuploadable — and the local store's own `_encodeJson` fails the same way, so it is unsaveable too. The recorder's entry point is now guarded ([decisions § 956](../architecture/decisions.md)) and the four route importers are ([§ 954](../architecture/decisions.md)), but the tracks that reach `saveRun` from the Wear OS and watchOS bridges, from `strava_importer.dart`'s API path, from the backup-restore path, and from `resumeSession`'s caller-supplied seeded track are not. The failure is also the wrong SHAPE for the caller: it is an `Error`, it is indistinguishable from a transient upload failure, and the sync loop retries it forever. Filed rather than fixed because the honest answer is a decision this lane should not make alone — dropping the point silently alters the runner's track, and refusing the upload needs a terminal, classifiable error the sync drain can park rather than retry.
- [x] **`readAllPages` terminates on the first short page, so a deployment whose `db.max-rows` is below `kPostgrestPageSize` silently truncates.** **CLOSED 2026-09-02** ([decisions § 987](../architecture/decisions.md)). Confirmed by reading, and the fix took the first of the two shapes offered plus a half the filing did not name. The startup assertion was rejected because PostgREST does not tell a client its cap, so the assertion would have to infer it — which is what the loop now does, without a second mechanism to keep in step; **no backend change is needed and none was filed.** The half the filing missed: detecting the cap is not enough, the walk must also advance by the rows it RECEIVED rather than by the size it asked for, or a loop that noticed the truncation would skip the very rows the server withheld and turn it into a corruption. Stops on an empty page, or on a page both short of the ask and shorter than its predecessor. Cost is one extra round trip when the FIRST page is short (that page has no predecessor, and the two causes are indistinguishable without asking again); every other shape costs what it did. Measured over the local stack that a past-the-end range answers `200 []` — but `416` if the caller asks for an exact count, so that requirement is now written into the function's contract; none of the six callers asks. Pinned by four new cases in `paged_read_test.dart`, including a 1,400-row read against a server capping at 500; restoring the old rule fails three of them. The module's own doc says the server "caps at `db.max-rows` and answers with the truncated page and a 200 — no error, no flag", and then uses `page.length < pageSize` as the exhausted signal with `pageSize` fixed at 1000. If the cap is ever set below 1000, the first request comes back short and the loop reports a partial history as complete — the exact bug the function exists to remove, from the other side. `apps/backend/supabase/config.toml` sets no `max_rows`, so today this rests on the platform default matching the constant, with nothing anywhere asserting the two agree. The durable shape is for the paging loop to stop only on a page it asked for and did not fill AND that was shorter than the previous one, or for a startup assertion against the deployment's cap; either is a decision about the backend contract, which no lane owned this round.
- [x] **The mobile Routes screen tells the user Import "covers GPX / KML / KMZ / GeoJSON / TCX" and the picker offers two of them.** **CLOSED 2026-09-02** ([decisions § 988](../architecture/decisions.md)). Confirmed, and there were **four** disagreeing statements, not two: the screen comment said five formats, `routesEmptyBody` said **three** (GPX/KML/TCX — the filing read the comment as the copy), the picker took two, and `parity.md` marked GeoJSON, TCX and KML/KMZ all Android-`✓`. There were also **three** dispatches, not one — `routes_screen.dart`'s `_parseRouteFile` carried its own `ext == 'kml' ? 'kml' : 'gpx'` and never consulted `detectRouteFormat`, so the file-picker path could not have reached a new format even after the set widened. Widened for GeoJSON and TCX (both parsers already shipped and tested, just unreachable), picker allowlist now DERIVED from the format set plus a `json` -> `geojson` alias, the third dispatch deleted, sniff taught `<TrainingCenterDatabase` and a JSON opening, copy updated in all seven ARBs and pinned by a suite that reads them. **KMZ deliberately not added**: the blocker is not the parser but that the mobile pipeline is `String`-typed from `readAsString` through the `compute` request to the dispatch, and `readAsString` throws on a zip before any parser is reached — filed below. `parity.md`'s KML/KMZ row is split so KMZ reads `✗` on both mobile platforms. `routes_screen.dart`'s empty-state comment and `l10n.routesEmptyBody` say five formats; `FilePicker.pickFiles(allowedExtensions: ['gpx', 'kml'])` and `kSupportedRouteImportExtensions` allow two, and `routeFromImportedFile` dispatches to `fromKml`/`fromGpx` only, so a `.geojson` or `.tcx` handed in through the OS share sheet is parsed as GPX and fails. KMZ is worse: nothing in the Dart tree opens a zip, so it cannot work at all — this lane corrected the `gpx_parser` library docstring and pubspec description, which both claimed KMZ the package has never had an `archive` dependency to implement. Web accepts all five. Either widen the mobile picker and dispatch (GeoJSON and TCX are already parseable; KMZ needs an unzip step) or correct the copy; leaving it is a promise the app does not keep. `apps/mobile_android/` belongs to no lane this round, and the change is a twin edit plus an i18n key across seven ARBs.

## Surfaced by the #789 round 33 guard lane (2026-09-02)

- [x] **The `isTrackRenderable` 5 m span gate is still a bare literal on the web and Dart rails.** **CLOSED 2026-09-03** ([decisions § 1028](../architecture/decisions.md)). Filed correctly in every particular. Named `MIN_RENDERABLE_SPAN_M` on web and `kMinRenderableSpanM` on the Dart twin (plus the iOS mirror), and registered as the three-rail `renderable-track span gate (m)` row. Each rail's test now makes two claims that fail to different mutations — the value flat, and two tracks bracketing the threshold by 1 cm THROUGH the constant, so a rail declaring it beside a literal that has drifted goes red; the flat assertion is what says which number, because the registry compares the rails against each other and moving all three together would satisfy it. The firmware's own test comment, which said the other two rails could not be registered, was corrected in the same change. Original filing follows. The wrist half is closed: `apps/custom_watch/core/src/track_projection.rs` now declares `MIN_RENDERABLE_SPAN_M` and compares against it, with a test that fails both when the value moves and when the comparison stops reading the constant. Web's `apps/web/src/lib/routes/track_projection.ts` still ends `isTrackRenderable` on `> 5`, and the Dart twin inside `apps/mobile_android/lib/widgets/track_preview.dart` on `> 5` at line 488. `scripts/check_watch_wire_vectors.mjs` reads a rail by the NAME of its constant (`tsConst` / `dartConst`), so until both are named the three cannot be registered as a `CONSTANT_ROWS` row and the number is held together by nothing — the same shape the live-pace ceiling was in before this round. Closing it is one named constant on each of the two rails (`MIN_RENDERABLE_SPAN_M` / `kMinRenderableSpanM`, to match the existing `ELEVATION_GAIN_MIN_DELTA_M` / `kElevationGainMinDeltaM` pair) plus a three-rail row. Not taken here because `apps/web/` and `apps/mobile_android/` belong to other lanes this round.
- [x] **`main` was red at this round's base commit, on `Test Flutter packages`.** **CLOSED 2026-09-02** ([decisions § 991](../architecture/decisions.md)) — the guard lane recorded it and could not own it; the mobile lane root-caused it in the same round. The failure was `sync_service_test.dart` racing its own `tearDown` into `errno 39`, and the cause sat one layer below the test: `didChangeAppLifecycleState` and `start()` are `void` and discarded `_trySync`'s future, so nothing could observe a cycle finishing and the test's `saveBatchCallCount` assertion was satisfied before `markManySynced` had written. `SyncService` now holds the in-flight cycle. Original filing follows. Run 33690185523 against `bb87b5ab39d20331840781cea1f206b60d8abce5` (PR #850, 2026-09-02 22:24Z) failed that job and therefore the `CI gate`; the two runs before it were green. Recorded rather than investigated: this lane owns `scripts/`, `.github/workflows/`, `apps/watch_garmin/` and `apps/custom_watch/`, none of which that job reads, and chasing a Flutter failure from a guard lane would have meant editing another lane's files mid-round. Whoever integrates round 33 should confirm it is not inherited into the merge.

## Surfaced by the #789 round 33 Lambda lane (2026-09-02)

Five filings closed in-round with their pinning tests ([decisions §§ 967-971](../architecture/decisions.md)); four of the five were wrong before they were anything else, and the corrections are recorded above and in the ADRs. What follows is what this lane found and did not take.

- [x] **Eight guard tests carry their own copy of a comment stripper, and the correct order is now enforced but not single-sourced.** **CLOSED 2026-09-02** ([decisions § 1000](../architecture/decisions.md)). Thirteen spellings across twelve files, not eight: the filing's walker was the ordering guard's own, which reached `src/lib/**/*.test.ts` only, and four more lived under `tests-e2e/` — three of them in the wrong order, none of them hiding anything today (measured: byte-identical output either way over every file those three scan). Single-sourced into `src/lib/core/strip_comments.ts`, a one-pass scanner rather than another chain of `.replace` calls, because the string-literal case the filing names cannot be closed by a substitution — nor can the regex-literal case underneath it, which no copy handled either. Strings and regex literals are emitted verbatim so a misread can only leave a comment standing, never delete code. The ordering guard is replaced by a register of every file still allowed to spell a block strip, with its count and reason, staleness-checked in both directions. One copy remains by design: `src/lib/integrations/strava_zip_strictness.test.ts` was another lane's this round, and is a register entry naming that reason. Original filing below. [§ 971](../architecture/decisions.md) fixed the ordering in all six that strip both comment forms, and `security_guards.test.ts` now fails a PR that puts the block strip first — so the class cannot regress. What it does not do is remove the duplication: `site_url.test.ts`, `lambda_site_origin.test.ts`, `coach_lambda_handler.test.ts`, `security_guards.test.ts`, `lambda_log_hygiene.test.ts` and `paged_read_guards.test.ts` each spell the same four-line `code()` helper, and `cloud_export_transport.test.ts` + `strava_zip_strictness.test.ts` spell a two-`replace` variant of it. Eight copies of one rule is the same shape [§ 968](../architecture/decisions.md) and [§ 970](../architecture/decisions.md) are about, one layer up. The durable fix is a single exported `stripComments` the guards import; not taken in-round because it means editing test files across four lanes' territory in a round where five agents share one checkout, and because the ordering guard already makes a wrong copy fail loudly. Whoever takes it should also handle the one case none of the eight handles: a `/*` inside a STRING literal, which opens a phantom block exactly as the `//` case did.
- [x] **The two CSS comment scanners cannot see a `//` and do not need to, but nothing records that.** **CLOSED 2026-09-02** ([decisions § 1001](../architecture/decisions.md)). A sentence in each header, and the skip is now DECLARED rather than detected: § 1000's register lists each CSS scanner with the number of block-comment strips it may spell and why, and fails when that count moves — so a reader who "fixes" one to match the JS guards fails the guard instead of quietly blanking a protocol-relative `url()`. `security_guards.test.ts` is in the register too, for the same reason: both its `<style>`-block scans are CSS. Original filing below. `contrast_guard.test.ts` and `rtl_css_guards.test.ts` strip `/* … */` alone and are correct — `//` is not a comment in CSS — but they sit beside six guards where stripping block comments alone was the defect, and the new ordering guard deliberately skips them by detecting that they strip only one form. A reader who "fixes" them to match the others would start blanking real CSS containing `//` (a protocol-relative URL, a `content:` string). One sentence in each file's header would close it; not taken because both files are outside this lane's scope.
- [x] **Consider a method gate on the five share Lambdas, or accept the OPTIONS render.** **CLOSED 2026-09-02** ([decisions § 1005](../architecture/decisions.md)). The `curl` was not needed: a CORS preflight only succeeds against a response carrying `Access-Control-Allow-Origin`, and NEITHER the five handlers NOR `aws_cloudfront_response_headers_policy.security` emits any `access-control-*` header — checked both — so a browser preflight against these paths already fails and no crawler sends one. Two things the filing did not have: `cached_methods` is `["GET", "HEAD"]`, so OPTIONS is the one allowed method the edge cache cannot absorb (a GET storm costs one render per five-minute window, an OPTIONS storm one render per request), and the in-repo clients were swept for a preflight-triggering fetch to `/share/*` or `/og/*` — there are none. One shared `shareMethodRefusal` rather than five copies, fail-closed on an absent method, `Allow: GET, HEAD`, `no-store`. Driven for real against all five handlers on OPTIONS / POST / DELETE / no-method, with HEAD pinned as still rendering. Original filing below. [§ 972](../architecture/decisions.md) added the guard that keeps `allowed_methods` narrow, which is what makes the missing gate safe. It leaves one thing measured and unaddressed: `OPTIONS` IS in `allowed_methods` and IS forwarded to the origin, and an `OPTIONS /og/run/<id>.png` runs a full ~50 ms `resvg` render and answers `200 image/png` — on paths the WAF rate-limit rules do not scope. Adding the gate turns that into a 405, which is almost certainly right (nothing should preflight a crawler-facing image URL) but could not be verified from this lane against the live distribution. Decide it with one `curl -X OPTIONS` against preview before changing five handlers.
- [x] **`lambda_site_origin.test.ts` is now named for a scope it no longer has.** **CLOSED 2026-09-02** ([decisions § 1002](../architecture/decisions.md)). Verified: the file drives `buildShareRunMeta` / `renderShareRunHeadTags` / `buildShareEventHead` / `renderShareEventHeadTags` and reads no Lambda source at all. `git mv` to `share_head_origin.test.ts`, header rewritten to say why the `lambda_` prefix was once right, inventory line added. Original filing below. Its `PUBLIC_SITE_URL` caller register moved to `core/site_url.test.ts` in [§ 970](../architecture/decisions.md), because that file already walked `src` and `lambda` together for the sibling scan. What remains under the `lambda_` name is two behavioural tests about what the share head builders emit under a misconfigured env — true of the Lambdas but stated through the shared `$lib/share` builders, so the name over-narrows what it covers exactly as the body-cap guard's name did in [§ 968](../architecture/decisions.md). Renaming it to `share_head_origin.test.ts` is a `git mv` plus a line in `docs/testing/test_inventory.md`; not taken in-round to keep the diff on that inventory file small while four other lanes are appending to it.

## Surfaced by the #789 coverage round 33 — web unit lane (2026-09-02)

Two consequences of [decisions § 981](../architecture/decisions.md) that this
lane's paths could not reach, plus one guard shape worth copying.

- [x] **`elevationGainMetres` is now a two-line adapter with one caller, and the caller should read the rule directly.** **CLOSED 2026-09-02** ([decisions § 1004](../architecture/decisions.md)). Adapter deleted; the page calls `Math.round(computeElevationGain(run.track))`. Measured before deciding: BOTH of the adapter's guards were dead — the caller already tests `run?.track`, and `computeElevationGain` returns 0 for an empty track and for a single point — so the whole adapter was `Math.round`. All five of its cases were already pinned in `route_simplify.test.ts`, except an ABSENT `ele` (as opposed to an explicit `null`), which is what the page's raw waypoints carry; that assertion was added there rather than lost. The § 981 "one rule" case cannot survive the deletion, so it is replaced by a source guard over the page — derive through `computeElevationGain(`, import it from the module that owns the rule, and never name the adapter again. Falsified twice: a reintroduced local `reduce` over positive deltas, and importing the rule from another path. Original filing below. § 981 made `runs/run_stats.ts`'s `elevationGainMetres` delegate to `routes/route_simplify.ts`'s `computeElevationGain` so the app has one elevation rule rather than two. It was left as an adapter only because its single call site — `apps/web/src/routes/runs/[id]/+page.svelte:801`, `run?.track ? elevationGainMetres(run.track) : 0` — is outside the paths this lane owned. The tidy end state is the one mobile already has: delete the adapter, and have the page call `Math.round(computeElevationGain(run.track))` the way `run_detail_screen.dart` calls the Dart twin. Two files (`run_stats.ts` + the page) plus moving the four `elevationGainMetres` cases in `run_stats.test.ts` onto whatever survives; the "one rule" case added by § 981 is the one that must not be lost, since it is the only thing pinning that the run stat and the route summary answer identically.
- [x] **`watch_core::run_stats::elevation_gain_metres` is now a faithful port of an adapter, so the watch has the two rules web just stopped having.** Closed 2026-09-02 — [decisions § 993](../architecture/decisions.md). Measured on the wrist before deciding: a +/-1 m sawtooth over 30 samples of dead-flat road banked **15 m** of climb ungated and 0 gated, while a 300 m staircase read 300 m either way. The rule now has one home — Rust has no structural typing and the watch no allocator, so the seam is `route_simplify::elevation_gain_from_samples`, an iterator over the altitude samples both point shapes yield. The filing's prediction held: all five existing cases passed unchanged, so the pinning case is new. The live vert path (`elevation::VertAccumulator`) is a third mechanism, not a third rule, and is untouched. The watch carries BOTH — `run_stats.rs`'s ungated positive-delta sum ([§ 902](../architecture/decisions.md)) and `route_simplify.rs`'s 3 m-gated one ([§ 925](../architecture/decisions.md)) — and until § 981 that faithfully mirrored web. It no longer does: web has one rule now, and it is the gated one. The watch is a one-way port outside the enforced lockstep so nothing fails today, but a device that renders a run's vert from `run_stats` and a loaded course's from `route_simplify` will disagree with itself on the same terrain, which is exactly what § 981 measured (143 m of climb on a flat half-hour). The edit is the same shape web took — `elevation_gain_metres` calls `route_simplify::compute_elevation_gain` and rounds — plus reconciling the four `elevation_gain_*` cases in `run_stats.rs`'s own test module, three of which already use deltas above the gate and pass unchanged. `apps/custom_watch/` belonged to no lane this round.
- [x] **`edge_function_contract.test.ts` now cross-checks a REFUSAL vocabulary, and only for one of the two checkout pairs.** **CLOSED 2026-09-02** ([decisions § 1003](../architecture/decisions.md)). Measured first: all seven codes the event page maps ARE in `events-checkout`'s source, so this closed a hole rather than a live defect. Both pairs now run off one register rather than a second hand-written copy; two data are per-pair because the halves genuinely differ — `events-checkout` templates none of its codes, so the donations guard's `prefixes.size >= 2` floor would have failed it, and the event page groups four codes into one ternary branch so its code-to-copy window is wider. Falsified three ways: spelling the event page's `host_cannot_take_payment` as `owner_cannot_take_payment` (the exact confusion that produced the original filing) fails both event cases; collapsing `event_full` onto the generic line fails the second; and the reverse swap on the fundraiser page still fails the donations half, so the generalisation did not weaken it. Original filing below. [§ 982](../architecture/decisions.md) added a guard that every code the fundraiser page compares is one `donations-checkout` can actually send — written because the followup requesting the work named `host_cannot_take_payment`, a code that function has never returned. `startEventCheckout`'s caller (`clubs/[slug]/events/[id]/+page.svelte`) has had the same shape of map since § 904 and is NOT under that guard: its literals (`event_full`, `sales_closed`, `instance_cancelled`, `host_cannot_take_payment`, `event_has_no_host`, `event_not_priced`, `stripe_not_configured`) are unverified against `events-checkout`'s source. The generalisation is small — the guard already parses both sides — but the event page's catch block is delimited differently, so it wants its own slicer rather than a shared one written blind.

## Surfaced by the #789 round 33 mobile lane (2026-09-02)

Seven items were closed in-round with their pinning tests
([decisions §§ 985-991](../architecture/decisions.md)), including the
`sync_service_test.dart` flake that had `main` red. What follows is what this
lane found and could not take.

- [x] **Moving `ActivityType` out of `preferences.dart` needs an edit to `apps/web/scripts/check_constraint_unions.mjs`, which no mobile lane may make.** The filed item is exactly right — `csv_run_importer.dart`, a pure parser, imports `preferences.dart show ActivityType`, and `preferences.dart` imports `package:flutter/material.dart` because the enum carries an `IconData get icon`. Verified this round: the enum is referenced from **31** files under `apps/mobile_android/lib`, and `packages/core_models` has no Flutter dependency at all (only `flutter_lints`, a dev dep), so the `icon` getter cannot travel with it and must become an extension in a Flutter-side file — `activity_type_labels.dart` is the natural host, since it already resolves the label the enum deliberately does not carry. **The blocker is one registry line.** `check_constraint_unions.mjs` registers the rail as `{ file: 'apps/mobile_android/lib/preferences.dart', decl: 'ActivityType', shape: 'enum' }` for `runs.activity_type`; the guard parses the enum out of that exact file, so moving the declaration fails the `parity-types` job until that path changes to the new home. A re-export does not satisfy a `shape: 'enum'` rail. Web already has the shape this should copy — a leaf `apps/web/src/lib/runs/activity_type.ts` holding `ACTIVITY_TYPES` beside the `types.ts` union — so the durable mobile answer is a leaf `packages/core_models/lib/src/activity_type.dart`. Do it in a change that owns both trees, or hand the one-line registry edit to whoever owns `apps/web/scripts/`. Closed 2026-09-02 — [decisions § 1013](../architecture/decisions.md). The round-34 mobile lane owned both trees, so the declaration and both registry lines moved together. `IconData get icon` became an extension in `activity_type_labels.dart`; `splitIntervalMetresFor` could not travel either, for the opposite reason (it reads `DistanceUnit`, still in `preferences.dart`), and is an extension there.
- [x] **28 mobile test files pair a temp directory with widget taps and no `pumpUntil` at all.** Measured while closing [§ 991](../architecture/decisions.md): 86 files under `apps/mobile_android/test/` create a temp directory, 51 delete it in a `tearDown` and 29 in an `addTearDown`, and 28 of them drive the UI (`tester.tap` / `pumpAndSettle`) without ever waiting on a store's own completion signal — `dashboard_screen_test`, `home_screen_test`, `run_screen_test`, `runs_screen_test`, `settings_screen_test`, `you_screen_test` and 22 more. That is the population of the § 723 residue class, not its membership: `pumpAndSettle` is the right wait for a purely-visual assertion, and only a test whose tap starts real store I/O it then fails to wait for is an actual member. Separating the two needs the whole suite run with `pumpUntil` (and the store's `notifyListeners`) instrumented to log ordering, which is a full-sweep run this lane's environment caps forbid. The technique is written down in § 723 and § 990 and worked both times it was used; the run is the missing part. Partly closed 2026-09-02 — [decisions § 1012](../architecture/decisions.md). Rather than auditing the 28, the § 991 SHAPE was searched for in the production code and found in exactly two places, both in `LocalRunStore`'s in-progress recording path — the one path deliberately kept off the serialised write chain, so `debugWritesSettled()` never answered for it, and both of whose drivers discard the future. It now has its own `debugInProgressSettled()`. The two named members wait on it. The remaining population stands as § 991 left it, but the two ROOT CAUSES are gone.
- [x] **A permanent per-run upload failure other than a non-finite waypoint would still retry forever.** [§ 986](../architecture/decisions.md) removed the one such failure that was known, and deliberately did not build a terminal-error taxonomy for a category that would then have no member. The candidate that remains unmeasured is a track blob past the `runs` bucket's object-size limit: `saveRunsBatch` catches it per-run, leaves the run unsynced, and the next cycle re-gzips the same bytes and fails identically. Measuring it needs the bucket's configured limit and a synthetic ultra-length track; if such a class exists, the answer is a classifiable terminal failure the drain parks plus a surface that tells the runner, not a retry cap. Measured 2026-09-02 — [decisions § 1009](../architecture/decisions.md). **It is reachable**, and the category now has a member. A track blob gzips to 27.3 bytes per waypoint (flat from 3.6k to 500k points), so the bucket's 25 MiB limit is 959,883 waypoints: 266.6 hours of 1 Hz recording, which the recorder cannot reach, but a 94.3 MiB GPX that deflates to 8.3 MiB — sixty times inside the 500 MiB cap `importFromZip` puts on a Strava archive. Closed at the source (the uploader refuses an over-size blob before spending the network, with a typed `TrackTooLargeException` that `classifyImportFailure` buckets as `tooLarge`). The PARK half is filed below rather than half-built.
- [x] **`RunRow.toJson` can still hand `jsonEncode` a value it refuses.** [§ 986](../architecture/decisions.md) made the DOMAIN `Run.toJson` total, which covers the local store and the backup archive. The generated `RunRow` is a separate serializer on the PostgREST path: `runRowFromRun` screens every double it lifts out of the metadata bag through `runPromotedDouble`'s finiteness check but passes `run.distanceMetres` straight through, and the `upsert` body is JSON-encoded by the supabase client. That specific hole is closed at the source (the domain object cannot carry a non-finite distance past `toJson` any more, and `runs_distance_m_check` refuses one server-side since § 939), so this is a latent shape rather than a live defect — a future writer that builds a `RunRow` directly rather than through a `Run` would reopen it. The durable close is for the generated `toJson` to emit only encodable values, which is a `gen_dart_models.dart` change of the same shape as § 985's. Closed 2026-09-02 — [decisions § 1010](../architecture/decisions.md). The filing called it "a latent shape rather than a live defect"; measured, it is **live, and four holes rather than one**. § 986 screened the domain serializer's OUTPUT while `runRowFromRun` reads the FIELDS, and the object a sync drain uploads is the resident one, not the screened copy written to disk. Fixed in `runRowFromRun` rather than in `gen_dart_models.dart`: what a non-finite value MEANS is a per-column question, so `distance_m` coerces to 0 through a shared `storableDistanceMetres` and a bag key is dropped.
- [x] **KMZ route import needs a bytes pipeline, not just a parser.** [§ 988](../architecture/decisions.md) widened mobile route import to GPX / KML / GeoJSON / TCX and removed every promise of KMZ rather than adding it. The parser side is nearly free: `archive` is **already a transitive dependency** of this workspace (`pubspec.lock`), it is pure Dart, and `RouteParser.fromKmz` would be a `ZipDecoder` call handing the inner document to the existing `fromKml`. The cost is the pipeline: `routes_screen._pickAndImport` and `SharedFileImportService.importPath` both `File.readAsString()`, and both hand a `String` through a `compute` isolate request to `routeFromImportedFile` — and `readAsString` throws on a zip before any parser is reached. Widening means those two reads become `readAsBytes`, the two isolate request types carry bytes, and the dispatch decodes to text per format. Web has done this since it was written (JSZip), so this is a real parity gap, not a design difference.

## Surfaced by the #789 round 33 Edge Function lane (2026-09-02)

Six items were taken in-round ([decisions §§ 973-977](../architecture/decisions.md)).
What follows is what this lane found and could not take, either because the
artefact lives in `apps/web/` or `apps/mobile_*` (which no lane owned this
round) or because the change is a product decision rather than a defect fix. Closed 2026-09-02 — [decisions § 1025](../architecture/decisions.md). § 988's honesty was verified first and holds. Built: both entry points read bytes, the two isolate requests carry them, and `routeTextFromImportedBytes` unwraps a KMZ before the unchanged string-typed dispatch. `archive` needed no new dependency and `gpx_parser` stays dependency-free.
- [x] **`isProviderConfigured` fails open on the phone, and the filing's "same question on web" is false — web is already fail-closed and is the reference implementation.** Counted, as the box asks: there is **ONE** logical copy of the fail-open shape, in `apps/mobile_android/lib/race_service.dart` plus its iOS twin. Web has **none**: all three probes (`isRunSignUpConfigured` / `isUltraSignUpConfigured` / `isChronoTrackConfigured`) route through `isProviderNotConfigured`, which returns "not configured" for a 429, any 5xx, an unreadable cross-origin body and a status-0 `FunctionsFetchError`, each with a comment naming the missing-credential rule. Only a readable non-429 4xx reads as configured there. The Dart twin answers `true` for every one of those cases (`on FunctionException catch (e) { … return true; }` plus a bare `catch (_) { return true; }`), so **the two platforms disagree about a configured provider whenever the probe does not cleanly reach the gate** — and the filing's other observation is confirmed: `races_screen.dart`'s `_probe()` wraps the call in a try/catch that can never fire, because `isProviderConfigured` swallows everything. Fix is to port web's grading to Dart (429 and 5xx and a transport failure all read as unavailable), mirror to the iOS twin, and then the screen-level catch becomes reachable and can go. Not taken here: `apps/mobile_*` is outside this lane. Closed 2026-09-02 — [decisions § 1007](../architecture/decisions.md). Web's grading is ported to `raceProbeUnavailable`, a top-level pure function so the decision is testable without a client. One correction: the dead `_probe()` catch was NOT removed. It is unreachable, but web's own callers of these probes carry the identical `try { … } catch { available = false }`, the sibling `_run()` in the same file wraps the equally-total `searchRaceListings`, and a probe is the layering contract's named example of an auxiliary network effect — deleting it on the phone alone would create a divergence in the file the change exists to close.
- [x] **Both clients probe the wrong function for the UltraSignup RESULTS tile, and after [§ 975](../architecture/decisions.md) that is now materially wrong.** Web's `isUltraSignUpConfigured` and mobile's `raceImportProviders['ultrasignup'].probeFunction` both invoke `race-listings-sync` with `{ provider: 'ultrasignup' }`. That answers for the **listings sync** leg, which is real and legitimately gated on `ULTRASIGNUP_API_KEY`. The **results-import** leg now refuses unconditionally (the athlete feed carries no race identifier), so provisioning the key would light up an import tile whose very next call 503s. The two happened to agree while both gated on the same env vars; they no longer do. Fix is one line per platform: probe `race-results-import` with `{ provider: 'ultrasignup', probe: true }`, exactly as the ChronoTrack spec already does. `race-listings-sync` must NOT be made to lie about its own leg to paper over this. Closed 2026-09-02 — [decisions § 1008](../architecture/decisions.md). Verified against both clients and both functions before applying; the one-line fix per platform was right. The guard on each platform derives its premise (it reads the probe branch and asserts UltraSignup is still refused independently of its credential) rather than restating the fix, so lifting § 975 tells the next reader to re-decide instead of deleting an assertion. **RunSignUp was deliberately left probing the sync** — see the round-34 filing below.
- [x] **Nothing reads the `complete` flag either importer now returns.** `parkrun-import` answers `{ imported, skipped, total, complete }` and `race-results-import` answers `complete` on every success shape ([§ 976](../architecture/decisions.md)), and the contract is `parseStravaSyncResult`'s — **an absent `complete` is PARTIAL**. Today the field is honest and unread on both platforms, so a capped parkrun import and a 2,000-finisher-truncated race import still present as whole. The web Strava side already has the parser and the copy to mirror (`strava_sync_result.ts` ↔ `strava_sync_result.dart` + the shortfall banner); this wants the same shape for the two scrapers, plus a sentence for the new `502 upstream_results_truncated`, which today falls through to a generic import-failed message on both clients. Closed 2026-09-02 — [decisions § 1014](../architecture/decisions.md). `parseImportCompleteness` grades both, fail-closed in `parseStravaSyncResult`'s direction and for that rule's own reason (one transport per importer, shipped alongside its callers). Three shared `integrations.*` keys across seven web catalogues and seven ARBs, all phrased to avoid grammatical-number agreement because the noun in the "n of total" sentence agrees with the TOTAL, not the count. The 502 `upstream_results_truncated` has its own sentence on both platforms.
- [ ] **Lifting the UltraSignup refusal needs a payload, not a code change.** [§ 975](../architecture/decisions.md)'s gate is deliberately not liftable on its own: `ultraSignUpAttributionGate()` refuses because `UltraSignUpResult` declares no field naming which race a row came from, and deleting the gate restores the mis-attribution exactly. Closing it is (1) observe UltraSignup's real `/service/events.svc/results/athlete?uid=` response and record the race identifier's field name, (2) add it to the row shape and filter mapped rows against the listing's `provider_race_id`, (3) then remove the gate — in that order, in one change. `ultrasignup_attribution.test.ts`'s first case is a characterisation test that goes red the day the mapper CAN attribute, which is the signal to do this.
- [ ] **`race-listings-sync`'s field names and endpoints are unverified against a live payload.** [§ 977](../architecture/decisions.md) writes the whole path behind the unchanged credential gate and handles the unverifiable half explicitly — every reading is optional-with-drop and counted, so a differently-shaped feed answers `synced: 0` with `unusable` equal to the row count on the first call, and a wrong endpoint answers a non-2xx which is refused `502`. What is still owed the day a key arrives: one real call, then reconcile `readRunSignUpRace` / `readUltraSignUpRace` / `providerRacesUrl` / `extractProviderRaces` against what came back. Two specific things to check first — whether `distance_units` really spells miles `M` (the unit table refuses both `M` and `m` on purpose, since folding them files a 100-mile race as a 100-metre one), and whether the date is month-first (a day-first feed announces itself on the first race past the 12th of a month, and everything before that is silently transposed).
- [ ] **Nothing ever calls `race-listings-sync` with `sync: true`.** The sync is opt-in ([§ 977](../architecture/decisions.md)) because every existing caller is a credential probe and a page load must not walk a provider feed. That means the calendar still fills from nothing until something asks: a `pg_cron` schedule (the `refresh-tokens` shape, with its own `CRON_SECRET` gate — note this function is `verify_jwt = true` today, so a cron caller needs a different door), or an operator-triggered admin surface, or a "refresh races near me" affordance on `/races` sized to the 2/hour bucket. That is a product decision, not a defect, and it is the last step of #10.

## Surfaced by the #789 round 34 — web lane (2026-09-02)

- [x] **One JS comment stripper is still hand-rolled, and it is a register entry away from being the tenth.** [§ 1000](../architecture/decisions.md) single-sourced eleven copies into `core/strip_comments.ts` and left exactly one: `src/lib/integrations/strava_zip_strictness.test.ts` spells the two-`replace` variant, because `src/lib/integrations` belonged to another lane in that round. It is not merely duplication — that variant is one of the two that never handled a `/*` inside a string literal, and the module it scans (`strava-zip.ts`) is a parser full of quoted path fragments. The change is four lines: delete the local `stripComments`, import the shared one, delete its entry from the `REGISTER` in `security_guards.test.ts`. The guard fails the moment the entry goes stale, so the two halves cannot be done separately. Closed 2026-09-03 — [decisions § 1033](../architecture/decisions.md). The box's own question was answered first and the answer was no: `strava-zip.ts` spells no block-comment delimiter anywhere, in code or in a string, so the two strippers were measured byte-identical on it. A latent hole, closed with its register entry in the same commit.
- [x] **The single-source guard keys on the BLOCK strip, so a JS scanner that blanks only `//` would still slip through.** [§ 1000](../architecture/decisions.md)'s register detects a hand-rolled stripper by the `/\*[\s\S]*?\*\/` needle, which every one of the eleven copies happened to spell. A new guard that blanks line comments alone would be wrong — the prose above a rule frequently sits in a `/** … */` block — and nothing would say so. It was not closed in-round because the line-strip spellings do not enumerate cleanly (`/^\s*\/\//`, `/\s\/\/.*$/`, `/(^|[^:])\/\/[^\n]*/`, `/\/\/.*$/gm`, a `(?<!:)` lookbehind — and `/^\s*\/\//` also appears in code that is not stripping anything). The durable close is probably the other direction: assert that a file which READS a source tree and scans it for a pattern imports the shared stripper, rather than trying to recognise every way of not doing so. Closed 2026-09-03 — [decisions § 1034](../architecture/decisions.md). The spellings do not enumerate; the DELIMITERS do — three of them, each of which any comment-recognising regex must escape a slash in — so the guard keys on those, over source already blanked through the shared stripper, and falls from 118 source readers to 8 files with a registered reason. Building it found the gap had already cost something: `track_preview_mount_guard`'s own block spelling differed from the register's needle, and a `/*` inside an HTML comment blanked **906 lines** across five `.svelte` files while the guard reported a pass — § 971 recurring where its guard could not look. Same helper also blanked code points against UTF-16 offsets, misaligned by one emoji. Three further scanners converted; measured unchanged.
- [x] **The three API Lambdas still spell their own 405 where the five share ones now share one.** [§ 1005](../architecture/decisions.md) gave the share handlers a single `shareMethodRefusal`; coach, generate-route and osrm-proxy each still write the comparison, the status, the `Allow` header and the body inline, with the coach one going through a stream writer rather than a returned object. [§ 1006](../architecture/decisions.md)'s population guard accepts either shape, so nothing is unguarded — this is duplication, not a hole. Folding the three in wants a helper that can express BOTH the allowed-method set (POST for two, GET for one) and the two response shapes, which is a bigger change than the three-line saving justifies on its own; worth doing the next time one of those wrappers is opened for another reason. Closed 2026-09-03 — [decisions § 1035](../architecture/decisions.md). Done properly rather than declined, because reading the three showed it was not only duplication: the share refusal sends `cache-control: no-store` and **none of the three API copies sent any cache directive at all**, so a 405 from `/api/coach`, `/api/routes/generate` or `/api/routes/osrm` was cacheable and would have outlived its own fix. `core/method_gate` takes the allowed set and returns the PARTS, which is what serves both response shapes — the coach's stream caller costs eight lines, nothing else changed shape.

## Surfaced by the #789 coverage round 34 — infra lane (2026-09-02)

Four filings closed in-round with their guards ([decisions §§ 1021-1024](../architecture/decisions.md));
two of the four were wrong about the fix before they were anything else, and
the corrections are in the ADRs. What follows is what this lane found and did
not take. This lane holds no AWS credentials by design, so every item below
says which half is verified from the repo and which is a claim about the live
edge, plus the one command that settles it.

- [x] **The Lambda execution role's `kms:Decrypt` on the secrets CMK is unexercised too, and emptying the statement is a structural change.** **CLOSED 2026-09-05 as DO NOT TAKE** ([decisions § 1267](../architecture/decisions.md)). The filing is factually right and its action is wrong: this grant and the entry below it are one architectural fact seen from two sides, and deleting the grant is the single thing that would make the plaintext-secret finding permanently unfixable. Every repair for that finding — a CMK on the function's environment, or a ciphertext env var the handler decrypts at cold start — needs the EXECUTION role to hold exactly this `kms:Decrypt`, which is what turns it from unexercised into load-bearing. Leave the statement, the variable and claim 9's restore knob alone. The live-configuration read the filing named is no longer what settles it. [§ 1021](../architecture/decisions.md) removed the deploy role from `AllowLambdaAndDeployRolesToDecrypt` and left the execution role, which by the same argument never uses it: `data.sops_file` decrypts at APPLY time and `local.lambda_env` puts the result into `environment { variables }` as plaintext, so the handler reads `process.env` and calls no KMS API — measured, no `kms` reference exists anywhere under `apps/web/lambda/`. The log-group encryption is done by the `logs.<region>.amazonaws.com` service principal under its own statement, not by the execution role. Not taken because `compact()` would then leave a principal-less `identifiers` list, which KMS rejects, so closing it means deleting the statement AND the module's `kms_decrypt_principal_arn` variable — removing the restore knob `scripts/check_infra_iam.mjs` claim 9 uses in the other direction. **Verified from the repo:** no `aws_lambda_function` sets `kms_key_arn`, so the CMK is not the env-var key. **Not verified:** the live function's own configuration. Settles it: `aws lambda get-function-configuration --function-name threkir-web-prod-coach --query KMSKeyArn` returning `None`.
- [ ] **The deploy roles can read the production secrets in plaintext, and § 1021 does not change that.** **Analysed 2026-09-05, fix specified, not taken** ([decisions § 1267](../architecture/decisions.md)). It is **fixable, not inherent** — but the filing's own cost estimate is wrong by a factor of four and that is why it read as deferrable. Measured by walking every `*_lambda_env` local in `infra/modules/web-stack/main.tf` for a `data.sops_file` merge: **TWO functions carry a sops secret, not eight** — `lambda_env` (coach: `ANTHROPIC_API_KEY`, `SUPABASE_SECRET_KEY`, `OPENAI_API_KEY`) and `generate_route_lambda_env` (`GRAPHHOPPER_API_KEY`, `GRAPH_CYCLE_API_KEY`). The five share Lambdas and the OSRM proxy take public values only. The filing's premise is confirmed from `aws lambda update-function-code help` (aws-cli 2.36.4): the response carries `Environment -> Variables`. **Do NOT reach for `kms_key_arn` first** — `check_infra_iam.mjs` claim 9 holds "no `aws_lambda_function` sets `kms_key_arn`" as a live premise and, the moment one does, DEMANDS `kms_decrypt_principal_arn` be wired back to the deploy role, handing back the grant the fix is about; whether that demand is right (does `UpdateFunctionCode` need `kms:Decrypt` on the function's CMK, or return `Environment.Error`?) is a live API behaviour no lane can settle. **The route immune to that is a ciphertext env var the handler decrypts at cold start under the execution role**: `kms_key_arn` stays unset so claim 9 is untouched, `GetFunctionConfiguration` hands out a blob useless without `kms:Decrypt`, and the grant in the entry above becomes load-bearing. Cost: one `kms:Decrypt` per cold start on two functions plus a small shared module. Not taken here — unplannable without AWS credentials, six Terraform changes already unapplied, and it needs an edit to `scripts/check_infra_iam.mjs`. Owner trees: `infra/modules/web-stack/` + `apps/web/lambda/{coach,generate-route}/` + `scripts/check_infra_iam.mjs`. The sops values become plaintext Lambda env vars at apply time, and every API that returns a `FunctionConfiguration` returns them — including `lambda:UpdateFunctionCode`, which the release genuinely needs and calls eight times. So a `production`-environment GitHub Actions job can read `ANTHROPIC_API_KEY` and `SUPABASE_SECRET_KEY` by uploading the same zip and reading the response. The module's own claim-8 comment already says this half out loud ("visible to anyone who can call GetFunctionConfiguration") without following it to the deploy role. Closing it is not an IAM edit — it is not putting plaintext secrets in a Lambda environment at all: a fetch at cold start (Secrets Manager / SSM Parameter Store), or a ciphertext env var the handler decrypts, at which point the execution role's `kms:Decrypt` becomes load-bearing for the first time and the item above inverts. That is a design change with a cold-start cost, an availability dependency and a per-Lambda code change across eight functions, and it wants a decision before any code.
- [x] **`lambda:GetFunction` and `lambda:GetAlias` on both deploy roles are exercised by nothing.** Measured: the only `aws lambda` verbs any workflow runs are `update-function-code` and `update-alias`, and no workflow runs any `bin/` script — `bin/lambda-alias-sync.sh` is the one caller of `get-alias` and runs under the operator's own SSO profile. Same shape as the KMS grant [§ 1021](../architecture/decisions.md) closed. Filed rather than taken because it removes an unexercised verb without removing any access: the role reads the same `FunctionConfiguration` out of the `UpdateFunctionCode` it must be able to perform (the item above). Worth doing as least-privilege hygiene the next time `infra/github-oidc` is applied, together with a re-read of whether `lambda:PublishVersion` is required for `update-function-code --publish` or is a third unused verb — that one could not be settled from the repo. Closed 2026-09-03 — [decisions § 1085](../architecture/decisions.md). All three verbs removed from both roles. **`lambda:PublishVersion` was a third unused verb**, settled from the request shape rather than from the repo: `--publish` is a boolean member of the single `UpdateFunctionCode` request (rendered by `aws lambda update-function-code --generate-cli-skeleton`, which prints that operation's own input model), `PublishVersion` is a separate operation the release never issues, and IAM models one action per operation. The durable half is `check_infra_iam.mjs` claim 10, which derives the verb set from the workflows' `run:` bodies and fails in BOTH directions — an extra verb is standing privilege, a missing one is an `AccessDenied` mid-release.
- [x] **The share Lambdas' 404 body is a single unstyled English sentence, and [§ 1022](../architecture/decisions.md) is what stops a reader ever seeing it.** `notFoundHtml()` in `share-entity` (and the equivalent in `share-run` / `share-route` / `share-recap` / `share-badge`) is `<p>This link isn't available.</p>` with a `noindex` — no styling, no navigation, no localization. It is unreachable today because the distribution replaces every 4xx body with the SPA shell, which is why the SPA's designed `.notfound-card` renders instead. The durable end state is for those handlers to return the SPA shell AT 404 themselves, at which point the distribution's 404 mapping becomes redundant and can go; the interim is correct but leaves five handlers carrying a body that is one config change away from being what a reader sees. `apps/web/lambda/` belonged to no lane this round. Handler half closed 2026-09-03 — [decisions § 1036](../architecture/decisions.md). All five return `notFoundShell()` — the SPA shell through the existing `injectEntityHead`, so the same stripping that keeps a stale `og:*`/canonical/JSON-LD off a 200 keeps it off a 404 — and the `noindex` now survives whether or not the edge substitutes. It falls back to a minimal noindex document on an unusable shell rather than returning it unspliced (no `noindex`) or throwing into the outer envelope (a 503, the § 1004 defect). **The distribution half is still owed and is `infra/`:** with the handlers honest, `custom_error_response`'s `error_code = 404` block is redundant, and dropping it is what puts the `noindex` on the wire. Filed below.
- [ ] **Whether CloudFront decodes a path before behaviour matching is still unmeasured.** **The prescribed probe was run and CANNOT settle it — 2026-09-05** ([decisions § 1266](../architecture/decisions.md)). Two reasons, and the second is the durable one. (1) `preview.threkir.com` has no DNS record and never had one: `infra/envs/preview/main.tf:38` sets `aliases = []`. (2) Against prod the experiment is **degenerate**: `/api/%63oach/turn` and the plain control `/api/coach/turn` return byte-identical responses, but so does `/api/zzz-no-such-route` under both `GET` and `POST` — every path lands on the one substituted error body, so treatment and control are indistinguishable from a path matching nothing. No variant of the probe helps while that is true. **So this item is ORDERED behind § 1084's apply**, which is the change that lets an unmatched path answer differently from a matched one; until then the only thing that settles it is `aws cloudfront get-distribution-config`, i.e. a credential. Re-file the probe after the apply. Original filing below. [§ 1023](../architecture/decisions.md) added `URL_DECODE` to the three WAF scope-downs on a one-way-failure argument rather than on a measurement, which is sound for THAT change (it can only widen a rate limit) but leaves the underlying question open — and the answer decides whether an encoded spelling was ever reaching the Lambda uncapped. Settles it against preview, no prod exposure: `curl -sS -o /dev/null -w '%{http_code}\n' 'https://preview.threkir.com/api/%63oach/turn'` — a 4xx from the Lambda's own auth gate means CloudFront decoded and routed it, a 403/404 from the SPA fallback means it did not. The same run answers the sibling question [§ 972](../architecture/decisions.md) left open about `OPTIONS` on the share behaviours.
- [x] **`terraform validate` passes on both env roots and on no module.** Run this round after every Terraform edit: `terraform init -backend=false` then `terraform validate` in `infra/envs/prod` and `infra/envs/preview`, both `Success!`, plus `terraform fmt -check -recursive` clean over the whole tree. What that does NOT cover is a `plan`: no lane has AWS credentials by design, so none of §§ 1021-1024 has been planned against real state, and the four changes are a KMS key-policy edit, a CloudFront `custom_error_response`, three WAF `text_transformation` blocks and one new module input at its default. The first is on a `prevent_destroy` key and is the one to read the plan for; the last is a no-op by construction (the module already received `""`). Apply preview first. Re-run 2026-09-03 by the round-36 infra lane after §§ 1084-1085: `terraform init -backend=false` + `validate` in `infra/envs/prod` and `infra/envs/preview` both `Success!`, `terraform fmt -check -recursive` clean over the whole tree. Still open for the same reason — no lane holds AWS credentials, so §§ 1021-1024 and now §§ 1084-1085 have never been planned. The two added this round are a `custom_error_response` deletion on the distribution and an IAM action-list narrowing on both deploy roles; neither touches a `prevent_destroy` resource, and the second is confirmed by the next release rather than by a plan. **Closed 2026-09-03** ([§ 1111](../architecture/decisions.md)) — re-run, and the re-run found a real gap that was not `validate`. `terraform init -backend=false` + `validate` in `infra/envs/prod` and `infra/envs/preview` both `Success!`, `terraform fmt -check -recursive infra` clean. The module still cannot be validated standalone (re-measured: `Provider configuration not present`, even though it declares `configuration_aliases = [aws.us_east_1]`), and its transitive coverage is now MEASURED rather than asserted — an undeclared-input-variable reference added to `modules/web-stack/waf.tf` fails `validate` in `envs/prod`, naming the module file and line. What was genuinely uncovered was `fmt`: the step lived inside the matrix job with a per-stack `working-directory`, and `terraform fmt` without `-recursive` reads one directory, so drift in the module was invisible — measured, all five per-stack steps exit 0 where a recursive pass exits 3. `fmt` is now one recursive job over `infra/` and `check_infra_coverage.mjs` reads the workflow's fmt invocations as scopes and fails when any Terraform directory falls outside them. Still no `plan` behind §§ 1021-1024 or 1084-1085 — no lane holds AWS credentials — so the standing verification item is re-filed below rather than retired.

- [ ] **The 403 -> SPA-shell mapping is distribution-wide, so every API refusal reaches the caller as `200 text/html`.** Measured on prod 2026-09-05 ([decisions § 1266](../architecture/decisions.md)): `POST https://threkir.com/api/coach/turn` returns **HTTP 200, `content-type: text/html`, the 5,666-byte SPA shell**, and so does `POST /api/zzz-no-such-route`. CloudFront has no per-behaviour `custom_error_response`, so the one `error_code = 403 -> response_code = 200 -> /index.html` block in `infra/modules/web-stack/main.tf` rewrites a 403 from ANY origin on ANY path. A client doing `JSON.parse` on a refusal gets a syntax error instead of the refusal, and a monitor watching status codes sees a success. **Both halves of the conflict are already documented and neither can move**: [§ 1022](../architecture/decisions.md) is right that 403 must stay at 200 because every dynamic client route is a missing S3 key, and [§ 1084](../architecture/decisions.md) removed the sibling 404 mapping naming `/api/coach*` specifically — the 403 one it left is the one that fires for an unauthenticated call, which is the normal case. So this is not a mapping edit: the shapes are a CloudFront Function on the API behaviours that suppresses the substitution, or an API origin that never emits a bare 403. That is a design choice with an availability and a cost dimension, which is why it is filed rather than taken. One thing to settle first, cheaply, once §§ 1021-1024 / 1084-1085 are applied: whether `/api/coach*` has a live cache behaviour in the prod distribution at all — from outside, a Lambda 403 rewritten by the mapping and an unrouted path falling through to S3 are indistinguishable, and `aws cloudfront get-distribution-config` separates them in one call. Owner tree: `infra/modules/web-stack/`. **The measurement is real and the named cause is WRONG — re-diagnosed 2026-09-06 ([decisions § 1307](../architecture/decisions.md)). Read that before acting on this entry.** `apps/web/src/lib/coach/handler.ts` returns **401** for an unauthenticated call, at two separate exits. Nothing on this distribution maps 401, so it passes through untouched — the entry's "the 403 one it left is the one that fires for an unauthenticated call, which is the normal case" cannot be right. The only 403 the coach path emits is the AI-disclosure consent refusal, which needs an authenticated caller with a profile row and is unreachable by the request that was measured; and the behaviour is not the explanation either, since `/api/coach*` has its own `ordered_cache_behavior` targeting `lambda-coach` with `POST` in `allowed_methods`. So the measured 200 text/html is **not** a coach 403 being rewritten — it is evidence the request never reached the Lambda, which is the issue #590 shape `main.tf`'s own comment records as proven once (only one of the two OAC grants, Function URL 403s before invocation, mapping rewrites it to the shell at 200, "the surface reads healthy while the Lambda never runs"). A stale prod bundle or a prod `/api/coach*` behaviour that does not match the repo read identically. **Every one of those is a live coach outage, not a body-format nuisance, and neither coach alarm can see it** — both key on Lambda `Errors` and `Duration`, which a request that is never invoked does not emit. The diagnostic gets CHEAPER, not more expensive: half the proposed `get-distribution-config` call is already answered from the repo (the behaviour is declared), and the half that matters needs no credentials at all — an unauthenticated `POST /api/coach/turn` that reaches the Lambda answers **401 with a JSON body**, so anything else is proof it did not, independently of the mapping. That is one `curl` against a public endpoint. The distribution-wide mapping is still a real design problem worth solving; it is just no longer the explanation for the one measurement behind this filing. Owner: whoever holds prod credentials, starting with the `curl`.

## Surfaced by the #789 round 34 firmware lane (2026-09-02)

Eight of the nine boxes this lane took are ticked in place above; these are what
it could not take, plus what the sweep for the same class turned up. The first
two are the same shape and the same reason: a wire field the phone also decodes,
so the firmware cannot move it alone.

- [x] **Widen `run_store::TrackPoint::ele_dm` to `i16` metres, the unit `CRS1` already chose.** **CLOSED 2026-09-03** ([decisions § 1026](../architecture/decisions.md)). The filed design was right and was followed exactly: `FORMAT_VERSION` 5, whole metres in the same two bytes, `MIN_FORMAT_VERSION` still 3, `ELE_NONE` still `i16::MIN` and unit-independent. Two things the filing did not name and this change added. The unit resolution is now a NAMED function on each rail (`run_store::ele_metres` / `eleMetresFromStored`) rather than a division spelled inline, because reading a v3 blob as metres is a silent tenfold error — magic matches, CRC matches, every field decodes — and the boundary is registered as `ELE_METRES_VERSION` / `kEleMetresVersion`. And **all three version bounds are now `CONSTANT_ROWS`**: `FORMAT_VERSION` against `_maxSupportedVersion` was the pair nothing could see, since this format flows watch->phone so the two halves are writer-and-ceiling rather than encoder-and-decoder, and each suite pinned only its own end. The metre is rounded rather than truncated, with the range test applied to the rounded value or a reading half a metre below the floor would round onto the sentinel. `record_cadence`'s ceiling-cost test is replaced by its inverse, `every_plausible_altitude_now_reaches_the_track`, which pins the NESTING of `plausible_alt`'s window inside the field's over all 9,501 metres of it — the hole re-opens from either side. Original filing follows. [decisions § 994](../architecture/decisions.md) measured the cost of the decimetre field — the ceiling is 3276.7 m, `elevation::plausible_alt` admits to 9000 m, and **69 % of a Silverton-Handies-Silverton profile stores no elevation**, with the summit push netting to zero vert because the gain rule carries the last reading across the dropout. [§ 334](../architecture/decisions.md) already made this exact decision for the course frame with the reason written down ("a decimetre `i16` caps at 3276.7 m, below Mont Blanc, and this is a mountain-ultra device"); the run track never followed, so the same watch can be handed a course it cannot record itself running. **The design, so it is not re-derived:** `FORMAT_VERSION` 5 storing whole metres in the same two bytes (`i16` metres spans +/-32,767 m — past Everest with 2x margin, and 1 m resolution is below both noise floors and below the 3 m gain gate); `MIN_FORMAT_VERSION` stays 3 and the unit resolves per header version, so **existing v3/v4 blobs stay readable with their original decimetre meaning** rather than being rejected or silently misread 10x high. `ELE_NONE` stays `i16::MIN` and `ele_dm_from_m`'s exclusion of it stays. **It needs a lane owning both trees**: `apps/mobile_android/lib/sim_watch_sync.dart` (plus its iOS twin) is the phone's production decode side — it reads the field as `eleDm / 10.0` and its `_maxSupportedVersion` is 4, so a firmware-only bump ships a watch whose runs the phone refuses outright — and the golden vector in `apps/mobile_android/test/sim_watch_sync_test.dart` that `scripts/check_watch_wire_vectors.mjs` pins moves with it. Half-metres were considered and rejected: metres match `CRS1`, and nothing consumes sub-metre altitude (`computeElevationGain` gates at 3 m, GAP now measures over 20 m).
- [x] **`RBK1` v2 for the per-checkpoint target time, so the wrist can show the verdict web has.** **CLOSED 2026-09-03** ([decisions § 1027](../architecture/decisions.md)). The design was right, and one of its predictions was wrong in a way that made the change smaller again: **no `CONSTANT_ROWS` row is owed for the band fraction.** The filing expected a second rail to acquire it, but under this design none does — the verdict is graded in `roadbook.dart` beside the builder that holds the band, `watch_roadbook.dart` only maps the already-graded verdict onto the wire, and the firmware deliberately never carries a threshold it never applies. The fraction stays held by the existing web<->Dart `roadbook` parity pair. What did land: a 19-byte v2 checkpoint carrying `target_elapsed_s(4) | target(1)`, both-or-neither (a time with no verdict, a verdict with no time, and an unknown status byte each refuse the whole frame — in Rust the pairing is a `CheckpointTarget` struct, so the half-present case is unrepresentable rather than merely rejected); `MIN_ROADBOOK_FORMAT_VERSION` 1 so an un-updated phone's v1 push still loads with no targets, since v1's CRC covers the same bytes and the layout is a strict prefix; golden vectors moved on both rails; and a `CONSTANT_ROWS` row for the checkpoint STRIDE, which is the thing a one-sided change would misread every field after the first checkpoint through while the CRC still matched. **And the wrist renders both halves**: a signed glyph per row beside the cut-off flag, plus the next target's TIME on the row below the window — a glyph with no number behind it is a claim a runner at hour twenty cannot check, and a wire field no page reads would be the § 24 mistake the whole entry exists to avoid. Original filing follows. [decisions § 997](../architecture/decisions.md) records why `roadbook`'s `RoadbookTarget` / `TARGET_BAND_FRACTION` is not ported, and it is a wire gap rather than a port gap: v1's checkpoint is `cum_dist_m | leg_dist_m | projected_elapsed_s | cutoff | flags` with the flags byte holding only `CHECKPOINT_FLAG_REFILL`, and `build_roadbook` — the ported builder — has no non-test caller, so the Roadbook glance renders only what is pushed. Closing it is: a v2 checkpoint carrying `target_elapsed_s` (a `u32`, 0 meaning none) and its verdict, the same shape the cutoff byte already has; `apps/mobile_android/lib/watch_roadbook.dart` emitting it, with the phone computing the verdict as it already computes the cutoff one (the proportional band is web's `TARGET_BAND_FRACTION` = 1 % with a 60 s floor, and it must be the phone's because a target is a per-marker `target_clock` / `target_elapsed_s` meta the watch never sees); the golden vector on both rails; and a `CONSTANT_ROWS` row for the band fraction once a second rail holds it. `route_markers::parse_target` is NOT needed on the wrist under this design — the meta stays phone-side, which is why the filing's "the input exists" premise was false in a way that also makes the fix smaller.
- [x] **`plan_replan` is missing `easeOffNextWeek`, and `segments` is missing three functions.** **CLOSED 2026-09-03** ([decisions § 1029](../architecture/decisions.md) + [§ 1030](../architecture/decisions.md)). The filing invited a read against the web source and the read found the premise half wrong and a live divergence underneath it. **`plan_replan` was not missing the ease-off RULES** — they were all present, inlined in `replan_remaining`, producing the same changes. What was missing is the reason web exports the function: its second caller. `plan_adaptive_replan.rs` implements web's P2 arms 1 and 3 and **omits arm 2**, the deep-fatigue deload override, along with `ADAPTIVE_DEEP_FATIGUE_TSB`, `ADAPTIVE_HIGH_ACWR`, `AdaptiveReason::DeloadFatigue` and `isDeeplyFatigued` — while its own header called the P2 gate "a faithful port". The omitted arm is the only one where the runner is at genuine risk, so a fatigued runner whose load said "bleed it off whatever the plan says" got nothing. `ease_off_next_week` is now extracted and public, both callers use it, and the override is ported with host tests. **`segments` split two ways**: `computeEffortsFromTrack` is ported and `build_segment_effort_rows` now batches through it as web's own caller does (the cumulative array and the median step are properties of the track, so the per-slice loop was 64 x (512 haversines + a 512-element sort) for an answer that does not depend on the slice — identical results, which is why nothing could notice); `computeGlobalSegmentEffort` / `_Efforts` are recorded as excluded in the module header, because they match against a free-standing catalogue geometry the watch holds none of and no frame delivers. A third finding on the way: `trend_code`'s doc comment has named a pinning test `trend_code_matches_discriminant` since it landed and that test existed nowhere, so the doc's argument about the explicit discriminants rested on nothing; it is written now. Original filing follows. Found re-running the [§ 899](../architecture/decisions.md) date comparison this round (14 of the original 16 pairs still show web ahead). `plan_replan.rs` exports one `pub fn` against web's two — the generator-v2 P2 surface the round-30 sweep named but did not enumerate. `segments.rs` exports three against web's six: `computeEffortsFromTrack` (the #733 batched walk, named in the round-30 filing), plus `computeGlobalSegmentEffort` and `computeGlobalSegmentEfforts`, which it did not. Each needs the same § 24 decision the five in that sweep got — port with host tests, or record the exclusion in the module doc — and neither module was read against its web source this round, so nothing here is a known divergence. `settings` shows the same date gap and is NOT one: web's five extra exports are `detectPlatform` / `deviceLabel` / `getDeviceId` / `dropUserCache` / `peekCachedSettings`, browser-platform helpers with no firmware analogue, plus two test hooks.
- [x] **The GAP window is 20 m on four rails and `roadbook` shares it on three, with nothing pinning that the sharing is deliberate.** **CLOSED 2026-09-04** ([decisions § 1159](../architecture/decisions.md)). Taken as the mechanised form rather than the prose one: `CONSTANT_ROWS` rows in `check_watch_wire_vectors.mjs` gained a `consumers` block — files that TAKE a value from the rail that declares it rather than restating it, each with the import that must match its source exactly once, plus the reason the sharing is deliberate. The roadbook's four rails (web, both Dart, firmware) are registered, and a guard test proves the failure: a `roadbook.ts` that declares its own window fails the check while every rail in the row still agrees with every other, which is precisely what nothing could see before. The reverse direction cannot be mechanised the same way — a reader retuning `MIN_SEGMENT_M` moves every rail together and the guard stays green — so the four GAP modules also carry one sentence naming the roadbook as a second consumer. Original filing: [decisions § 992](../architecture/decisions.md) raised `MIN_SEGMENT_M` after measuring that the shipped 5 m was below `ELEVATION_GAIN_MIN_DELTA_M / MAX_GRADE`. `roadbook` imports the constant on web, both Dart rails and the firmware, so its effort allocation moved too — correctly, for the same reason, and its suites pass unchanged. What is not written down anywhere is that the two SHOULD share it: a later reader who decides the roadbook wants its own window has no note saying the sharing was a decision, and a later reader who tunes the GAP window has no note saying three roadbook rails move with it. One sentence in each `roadbook` module header, or a `CONSTANT_ROWS` `why` naming the second consumer. Not taken because the wording belongs in `apps/web/` and `apps/mobile_*` roadbook files this lane did not own.
- [x] **Nothing pinned the GAP window's value on any rail before this round, and the new pins are relationship-shaped rather than golden.** **CLOSED 2026-09-04** ([decisions § 1160](../architecture/decisions.md)). Three suites (four files with the iOS twin) now grade one frozen synthetic switchback — the clean 6 % climb oscillating +/- 8 m every 150 m over 3 km that § 992 measured on — against the truth the same walk gives with no window at all, and must both stay inside 3 % of it and report the frozen 311 s/km. Measured against a true 302.611 s/km: 5 m → 304 (-0.46 %), 20 m → 311 (-2.77 %), 25 m → 316 (-4.42 %), 30 m → 322 (-6.41 %), 200 m → 426 (-40.78 %); the noise-floor pin admits nothing under 19.396 m, so the two together admit only [19.40 m, 24.97 m]. **The AR(1) generator this filing asked for is not needed and was not committed**: every noisy row in § 992's table improves as the window grows, so they motivate the FLOOR, which the relationship pins analytically and exactly; the clean oscillation is what binds the ceiling and it is deterministic. The eight fixture numbers are one `CONSTANT_ROWS` row read out of each rail, because a golden pace says nothing unless the track under it is the same track. The Garmin rail is excluded and says why: its field is a streaming estimator with no batch entry point to grade a track through. Original filing: Every suite on every rail passed at 5, 20, 30 and 50 m — measured by running web's `grade_adjusted_pace` + `roadbook` + `pace_analysis` + `fuel_plan` suites at each. The new test per rail asserts the relationship to the noise floor (`ELEVATION_GAIN_MIN_DELTA_M / MIN_SEGMENT_M < MAX_GRADE`, and the factor under 2.1), which fails at 5 m but would pass at 200 m — a window long enough to erase real terrain. The upper bound is measured in § 992's table and pinned by nothing. A golden case would be one synthetic track with a known true GAP and a tolerance, mirrored across the four rails; it wants the AR(1) fixture generator to be committed somewhere both a `tsx --test` and a `cargo test` can read, which is a cross-lane artefact.

## Surfaced by the #789 round 34 mobile lane (2026-09-02)

- [x] **`race-results-import` charges a credential PROBE to the import bucket, so [§ 977](../architecture/decisions.md)'s split needs applying there too — and RunSignUp's probe is parked behind it.** `race-listings-sync` gives probes their own `race-listings-sync:probe` bucket at 60/hour free precisely because two probes on one page load spent a free runner's whole 2/hour sync allowance and the next load 429'd. `race-results-import` has no such split: `checkRateLimitTiered(…, 'race-results-import', 8, 32, 3600)` covers probes and real imports alike. [§ 1008](../architecture/decisions.md) moved UltraSignup's probe there, taking the settings screen from one to two of that bucket per load; moving RunSignUp — which the function's own probe-branch comment says should be probed there, and which is the last client asking `race-listings-sync` a question about a leg it does not own — would make it three, so roughly two settings visits an hour would exhaust a free runner's ability to import a result at all. With [§ 1007](../architecture/decisions.md) in place an exhausted bucket also reads as "provider unavailable", so the failure would be silent and total. Backend lane: add the `race-results-import:probe` bucket, then move RunSignUp's `probeFunction` in the same change. Closed 2026-09-03 — [decisions § 1041](../architecture/decisions.md). The filing is accurate, including its warning: verified that `checkRateLimitTiered` answers 429 on denial and `raceProbeUnavailable` grades a 429 as unavailable (§ 1007), so an exhausted bucket presented as "provider unavailable" for a provider that is configured and working — silent and total. `race-results-import:probe` now carries 60/240 per hour beside the import bucket's 8/32, and every entry in mobile's `raceImportProviders` names `race-results-import` in probe mode, so no mobile client asks `race-listings-sync` about a leg it does not own. Guarded on both sides by suites that PARSE the two limits and fail if the probe bucket is ever no more generous than the import one (mutation-tested). **Web's `isRunSignUpConfigured` still probes the sync** — it lives in `apps/web/src/lib/core/data.ts`, outside this change's owned paths; not a user-visible divergence (both legs gate on the same two env vars, so the answers agree today), and filed below.
- [x] **The drain still does not PARK a terminal upload failure, now that one exists.** [§ 1009](../architecture/decisions.md) measured the over-size track blob as reachable and closed it at the source — the uploader refuses before spending the network, with a typed `TrackTooLargeException`. What it does NOT do is stop the retry: `saveRunsBatch` returns a bare `Set<String>` of failed ids, five call sites mark everything else synced, and the residency invariant keeps the run and its million-point track resident forever. Closing it needs per-run blocked state in `LocalRunStore` (a sidecar beside `synced_ids.json`), the five call sites reading a classification rather than an id set, a surface, and its i18n — and, before any of that, a product answer to what a runner can actually DO about a run whose track is too big to store. Guessing at that answer is how a taxonomy becomes an abstraction nobody wanted, which is why § 986 refused to build one and why this is filed rather than half-built. The measurement in § 1009 is what the next lane should start from. **Closed 2026-09-03** — [decisions § 1070](../architecture/decisions.md). The product answer taken: every number on the run is a COLUMN and only the trace is a Storage object, so the run is not lost — one artefact of it is. `dropTrack` empties the track locally, which un-parks the run and lets the ordinary drain upsert it with `track_url = null`; the card offering it sits beside an **Export a copy** button opening the existing GPX/TCX/FIT sheet, and the confirm goes through `confirmDestructive` because the trace is the only record of where someone went. Two states only (`RunPushOutcome.retryable` / `.blocked`, `RunPushBlockReason` with one member whose `name` is an on-disk value), in `core_models` so the store reads it without depending on `api_client`. Parking is defined as **leaving the drainable set**, which is one rule with two consequences: `unsyncedRuns` excludes it (no retry) and the residency invariant excludes it (the track stops being hydrated for a drain that will never run), while `_summaries` still holds it so the list renders it and `runById` hydrates it on demand. Two failure paths moved with it — `saveRunsBatch`'s every-track-failed branch RETURNS rather than throws whenever anything is blocked (a throw reports "nothing landed" and leaves the caller no id to park), and `SyncService` sets `anyFailure` from `retryable` alone, because a backoff retry for a parked run is the same forever-loop one layer up. Thinning the track to fit (`simplifyToBudget`) was refused: it rewrites the recorded data and moves every track-derived number with no honest place to say so.
- [x] **`parseImportCompleteness` lives in the `strava_sync_result` pair because a mobile lane cannot register a new one.** [§ 1014](../architecture/decisions.md) put the scraper-importer parser beside `parseStravaSyncResult` in the pair that already owns that rule. The naming is now wrong in one direction — parkrun and race-results logic in a file called `strava_sync_result` — and the honest split is a registered `import_completeness` pair (web `integrations/import_completeness.ts` ↔ `packages/core_models/lib/src/import_completeness.dart`, Dart side in the shared package because `api_client` consumes it, like `strava_sync_result` and `profile_query`). It was not done here because a new pair needs rows in BOTH registries — the root `CLAUDE.md` table and `.claude/agents/shared-library-syncer.md` — and a pair named by neither is a pair whose divergence nothing detects ([§ 641](../architecture/decisions.md)). Do it in a change that owns the root `CLAUDE.md`. Closed 2026-09-03 — [decisions § 1039](../architecture/decisions.md). Split as filed: web `integrations/import_completeness.ts` ↔ `packages/core_models/lib/src/import_completeness.dart`, with rows in BOTH registries (`node scripts/check_parity_pair_registry.mjs` reports 110 pairs registered identically). The one thing the filing did not name is the direction of the dependency: both parsers grade counts by one rule and nothing in this repo compares two modules on the SAME platform, so the primitives (`importResponseCount` / `importResponseText`) live in the NEW module and `strava_sync_result` imports them — the reverse would leave parkrun and race-results grading depending on a module named after Strava, one level down. A source guard on each side pins the composition. Correction found while the registries were open: both claimed the `strava_sync_result` pair ran 17 web / 16 Dart when it was 23 / 22 — the counts predated § 1014's six added cases. After the split the claim is true, and the new pair is 7 each.
- [x] **`DistanceUnit` should follow `ActivityType` into a `core_models` leaf.** [§ 1013](../architecture/decisions.md) moved the activity vocabulary out of `preferences.dart` because a pure parser had to import `package:flutter/material.dart` to reach it. `DistanceUnit` is the same kind of thing — a CHECK-constrained vocabulary rail for `user_profiles.preferred_unit`, registered in `check_constraint_unions.mjs` against that exact file — and it is why `ActivityType.splitIntervalMetresFor` had to stay behind as an extension in `preferences.dart` rather than travel with the enum. Nothing drags a widget toolkit in to reach it today, so it is not urgent the way `ActivityType` was; do it the next time both trees are owned by one change, and the extension folds back into the leaf. Closed 2026-09-03 — [decisions § 1040](../architecture/decisions.md). `DistanceUnit` and `kMetresPerMile` are now `packages/core_models/lib/src/distance_unit.dart` and `splitIntervalMetresFor` is a member of `ActivityType` again; the `user_profiles.preferred_unit` rail in `check_constraint_unions.mjs` moved with the declaration and still resolves, with a local half added in `core_models`' own suite reading the enum against the migration's CHECK. The footprint was 34 files under `apps/mobile_android` (mirrored to the twin) because Dart imports are not transitive, and 12 of them then carried an unused import or a stale `show` — both warnings, which `dart analyze` exits 2 on. The § 1013 collision hazard is why every added import is a `show` rather than a plain barrel import: an unqualified one in a file that imports `package:flutter/material.dart` collides on `Route`.
- [x] **The `geocoding.dart` coordinate guard has no web twin, and web has no analogue to twin.** [§ 1011](../architecture/decisions.md) added `isUsableLatitude` / `isUsableLongitude` to the mobile geocoder, mirroring the contract `RouteParser._isUsableLat` already applies to a coordinate out of a file. `apps/web/src/lib/routes/geocoding.ts` has no matching validation — but it also has no Nominatim string-parse path, so there is nothing to fix by symmetry and nothing to register as a pair. Worth a look when the web geocoder is next open: `jsonDecode('1e400')` is `Infinity` in JS too, so its `as number` reads of a provider's `center` array carry the same exposure the mobile MapTiler branch did. **Closed 2026-09-03** — [decisions § 1066](../architecture/decisions.md). The filing was wrong in its premise and right in its instinct: web has TWO Nominatim string-parse paths, and four defects, each measured against the pre-change module rather than reasoned about — a MapTiler `1e400` centre reached the search dropdown as an infinite longitude, a latitude of `91` survived BOTH providers (the Nominatim path checked `isFinite` and nothing checked range), one unusable bbox corner made the geocoded radius NaN, and a 200 with an unparseable body threw a SyntaxError out of a function contracted to return null. `isUsableLatitude` / `isUsableLongitude` now gate every coordinate out of a provider response, 7 new tests. **Not registered as a parity pair, deliberately**: the two MODULES are not in lockstep and never were (web carries a third `aborted` outcome, takes an `AbortSignal` where mobile injects a fetcher, and falls back to Nominatim in the geocode half where mobile returns null), so a row would assert a lockstep that does not exist — worse than no row. The registries are not a web lane's to edit in any case; see the round-36 findings below for the recommendation and for the mobile half § 1011 did not reach.

## Surfaced by the #789 round 35 — web lane (2026-09-03)

Four boxes taken, all four ticked in place above. Two of the four filings were
right about the artefact and wrong about the cost — one because the hole it
predicted had already been exploited, one because the duplication it declined
to fold was hiding a real header drift. What follows is what this lane found
and could not take.

- [x] **The distribution's 404 mapping is now redundant, and dropping it is what puts the `noindex` on the wire.** [§ 1036](../architecture/decisions.md) made all five share handlers return the SPA shell at 404 with their `noindex` in it, which is the premise [§ 1022](../architecture/decisions.md) said would make `custom_error_response { error_code = 404 }` unnecessary. Until it goes, CloudFront still replaces the Lambda body with `/index.html` fetched from the S3 origin, so the reader sees the same card either way but the `noindex` is still discarded — the tag exists on the wire only once the mapping is gone. **The `error_code = 403` block must stay at 200 and is unrelated**: that is the SPA deep-link path (every dynamic client route is a missing S3 key) and answering it 403 breaks the whole app. `infra/modules/web-stack/main.tf`, which no lane owned this round. `share_lambda_handlers.test.ts` pins the handler half; the infra lane wants a Terraform-source assertion that the 404 block is absent, in the same change, or nothing states why the handlers now carry a body they did not before. Closed 2026-09-03 — [decisions § 1084](../architecture/decisions.md). The block is gone, the 403 one stays at 200, and the Terraform-source assertion the filing asked for is `scripts/check_infra_error_responses.mjs` — which reads BOTH halves, so a share handler that stops returning `notFoundShell` fails the PR rather than quietly putting its bare sentence on ten paths. It joins the already-bundled `infra-guards` job. Two extras fall out: `/api/coach*`'s JSON 404 stops being answered as an HTML shell, and `check_infra_coverage.test.mjs`'s two 404-anchored tests were re-anchored. Not verified against the live edge — no lane holds AWS credentials.
- [x] **`.claude/agents/shared-library-syncer.md` is unreachable from a web-only lane, and `core/method_gate` may want a Dart twin.** The gate is server-side today (eight Lambdas, no mobile analogue), so it owes nothing — but the mobile client's own refusal-shaped reads of an Edge Function 405 are not routed through anything shared, and if that ever becomes a pair it needs rows in BOTH registries ([§ 641](../architecture/decisions.md)). Noted rather than filed as work: nothing is divergent today. The reason it is written down is that this lane could not have added the rows even if it had wanted to — the root `CLAUDE.md` and the agent file are outside a web lane's ownership, which is the same wall [§ 1014](../architecture/decisions.md)'s filing hit. **CLOSED 2026-09-05 ([decisions § 1219](../architecture/decisions.md)).** Decided rather than left open: **no Dart twin is owed, and the reason is structural, not "not yet"**. `methodRefusal` BUILDS a 405 for the eight Lambdas that gate their own method; a parity pair exists where both platforms must make the same DECISION, and only one platform makes this one. A grep of `apps/mobile_android/lib/` and `packages/*/lib/` finds no read of a 405 status anywhere, so there is nothing to keep in step either. If a client-side reading of a 405 is ever wanted it is a CLASSIFIER, not a twin of a builder, and it belongs in `import_failures` — already a registered pair, already keyed on `errorStatus` for 429 / 401 / 403 / 413 — as one more branch. The registry-reachability half of the filing is real and unchanged: this lane owns both registries and confirmed them consistent (`check_parity_pair_registry.mjs`: 110 pairs identical in both, 445 paths on disk).
- [x] **`security_guards.test.ts` is now 3,800 lines and 91 tests, and two of this round's four items landed in it.** The delimiter register, the Lambda method-gate population guard, the two `console.error` scans and roughly eighty unrelated assertions share one file, so any lane touching a cross-cutting guard collides with any other lane touching a different one — this round it was the same file twice in two commits. Splitting it by concern (source-scanner hygiene / Lambda contracts / client column reads / CI workflow shape) would make the collisions structural rather than incidental, and the population assertions each guard carries make the split mechanically checkable. Not taken because a file move of that size across a round with five concurrent lanes is exactly the wrong time. **Closed 2026-09-03** — [decisions § 1065](../architecture/decisions.md). Split into twelve concern files rather than the four the filing sketched: those four were the concerns that *collided this round*, not a taxonomy — `privacy_guards` (21), `infra_guards` (12), `consent_guards` (11), `lambda_guards` (9), `a11y_guards` (8), `credential_guards` (6), `rate_limit_guards` (6), `edge_function_guards` (5), `paywall_guards` (5), `ci_workflow_guards` (4), `xss_guards` (3), `source_scanner_guards` (1). The one-test file is the point rather than a leftover: the comment-delimiter REGISTER is the single thing every lane adding a source scanner must edit. Verbatim is machine-checked in both directions — all 91 bodies appear byte-identical in exactly one of the twelve, the sorted name sets are identical, and `npm run test:unit` reports the same 4,742 before and after. The one non-verbatim segment is the register itself, whose `security_guards.test.ts` entry of 4 became two entries of 2 because the two CSS-reading scans landed in different files. The ~50 prose pointers naming the old path were repointed where they describe CURRENT behaviour; the 17 in `decisions.md` and the per-round entries in `test_inventory.md` were deliberately left, because an ADR records what was true when it was written.

## Surfaced by the #789 round 35 Wear OS / scripts lane (2026-09-03)

Three defects fixed in-round ([decisions §§ 1052-1053](../architecture/decisions.md),
[§ 1055](../architecture/decisions.md)), one item scoped and deliberately not
built ([§ 1054](../architecture/decisions.md)), one verified already closed and
six negative results recorded ([§ 1056](../architecture/decisions.md)). These
two are what the lane found and could not take.

- [x] **`avg_bpm` is saved as the run's average heart rate with no record of how much of the run it covered.** Falls out of [§ 1054](../architecture/decisions.md)'s scoping: `MeasureClient` is documented foreground-only, so on a twelve-hour ultra the samples that reach `bpmSum` / `bpmCount` may be only the minutes the runner spent looking at the watch — and `stopRecording` writes that mean into `QueuedRun.avgBpm`, which becomes the run's `avg_bpm`, with nothing anywhere saying it covered 3 % of the run. Not a `MeasureClient` bug and not fixed by moving to `ExerciseClient` alone: the same question would arise from a sensor that drops out. It became *computable* only with [§ 1052](../architecture/decisions.md) — the availability state means the recorder can now measure the seconds during which HR was `Available` and compare them to elapsed. Not taken in-round because the fix is a decision about saved data, not a wrap: either suppress the claim below a coverage threshold (changes what a completed run stores) or carry the coverage as a metadata key, which needs an entry in `docs/backend/metadata.md` and therefore an owner in `apps/backend/` as well as `apps/watch_wear/`. Decide the shape before writing it; the wrist half is small once the shape is settled. Closed 2026-09-03 — [decisions § 1083](../architecture/decisions.md). **Both shapes, and both fit inside `apps/watch_wear/` plus the registry row** — the watch posts straight to Supabase, so no phone-side parser is involved. `metadata.hr_coverage` records the share of ACTIVE elapsed time the sensor delivered, and `avg_bpm` is suppressed below 0.5 because a mean over less of the run than not is not the run's average. Measured on the recording ticker against the AGE of the last sample, not by closing an interval on an availability event — a foreground-only client the platform stops feeding need never report `UNAVAILABLE`, and an open interval credits the silence. Null coverage is *unmeasured*, not zero, so a pre-field checkpoint recovers its average unqualified.
- [x] **`store.queue.first()` inside the drain is the residual of the class [§ 1055](../architecture/decisions.md) closed at the boundary.** That entry put a `.catch` on `store.queue` where it is *collected*, because a DataStore-backed flow reports a corrupt file by failing rather than by reading empty. The drain reads the same flow with `.first()` in `drainQueueLocked`, and `drainQueue` is called from eight sites, four of them inside `viewModelScope.launch` blocks with no guard — so an unreadable queue still reaches the thread's uncaught handler and kills the process the recording service is in. Deliberately not wrapped in-round: unlike the stream case there is a real decision underneath, namely what the drain should *do* when it cannot read the queue (report to the sync chip? back off? treat it as empty and risk a silent no-op?), and a blanket `catch` would pick the worst of those three by default. The broader shape is that `RunViewModel` has 23 `viewModelScope.launch` blocks and no `CoroutineExceptionHandler`; installing one on the scope would turn every remaining instance into a logged failure instead of a process death, which is probably the right durable fix and is a decision about the whole view model rather than about the drain. Closed 2026-09-03 — [decisions § 1082](../architecture/decisions.md). Both rungs. Every one of the 23 `viewModelScope.launch` sites now goes through `launchGuarded`, which installs a `CoroutineExceptionHandler` that logs, and `ViewModelStreamResilienceTest` fails the build on a bare one. The drain's own decision is **report and back off**, not treat-as-empty: an empty read drains nothing, arms no backoff and CLEARS `syncError`, so the runner is told their runs are synced by the pass that could not open the file holding them. A `force` drain still gets through, which is what makes a retry after a reboot possible.

- [x] **`packages/run_recorder` needed `hide ActivityType` and only a compile caught it.** [§ 1013](../architecture/decisions.md) put `ActivityType` in `core_models`; `geolocator_apple` exports an `ActivityType` of its own (iOS's LOCATION activity type). `run_recorder` imports both, and `dart analyze` on either package reports nothing — only building a test that reaches `run_recorder` fails. That is a general hazard for any name added to the `core_models` barrel, and nothing sweeps for it. A guard that resolves the barrel's exported names against the exported names of every direct dependency of every package that imports it would catch the class; whether that is worth writing depends on how often the barrel grows. Closed 2026-09-03 — [decisions § 1043](../architecture/decisions.md), as a measured **no**. **The filed premise is wrong**: the collision is an `ambiguous_import` ERROR at the use site, so `dart analyze` in the COLLIDING package reports it twice and exits 3 (reproduced by removing the `hide`), and CI's `Test Flutter packages` job already runs `melos exec -- dart analyze` over `apps/mobile_{ios,android}` and `packages/**`. Analysing `core_models` or the app reports nothing because the analyzer diagnoses only the files it is pointed at — which is what made the hazard read as invisible. The guard as specified would also be wrong twice: over the barrel's 115 public type-level names and the 48 direct dependencies of the six importing packages there is exactly ONE collision (`Route`, against `package:flutter`, harmless and already `hide`-handled), and `ActivityType` is not in that set at all because `geolocator_apple` is TRANSITIVE — it would have missed the case that motivated it. What was written instead: a note on the barrel saying where a collision surfaces and to analyse the consumers, plus `barrel_test.dart` pinning the two properties nothing else fails on (every `src/` library is exported; the package still has no Flutter dependency).

## Surfaced by the #789 round 35 mobile / integrations lane (2026-09-03)

Five items closed in-round ([decisions §§ 1039-1043](../architecture/decisions.md)),
two of them on filings that were wrong about their own premise. What follows is
what this lane found and could not take, all of it because the artefact belongs
to a tree another lane owned.

- [x] **Web's `isRunSignUpConfigured` still probes `race-listings-sync`, which now makes it the only client left asking that function about a leg it does not own.** [§ 1041](../architecture/decisions.md) added the `race-results-import:probe` bucket and moved every mobile probe onto the results leg; the web half lives in `apps/web/src/lib/core/data.ts`, outside this change's owned paths. It is a two-line change — `supabase.functions.invoke('race-results-import', { body: { provider: 'runsignup', probe: true } })`, matching the `isUltraSignUpConfigured` shape three functions below it — plus, once it lands, `race-listings-sync`'s own probe branch has NO caller and the `race-listings-sync:probe` bucket becomes dead, which is worth deciding about in the same change rather than leaving a bucket nothing charges. Not urgent: both legs gate on `RUNSIGNUP_API_KEY` + `RUNSIGNUP_API_SECRET`, so the two probes answer identically today and the divergence is architectural rather than user-visible. Owner tree: `apps/web/src/lib/core/`. **Closed 2026-09-03** — [decisions § 1064](../architecture/decisions.md). Moved as filed. The second half decided the other way: the `race-listings-sync` probe branch is **not** dead and stays. Its credential gate sits above the `if (!isSync) return { configured: true }` early return, and that early return is what makes a sync opt-in — the fail-safe default § 977 installed so a page load cannot walk a provider feed. Removing it would make an unasked-for call perform a sync, which `wiring.test.ts` pins against by name; removing the `race-listings-sync:probe` bucket would charge that same call to the 2/hour sync bucket, which is the defect § 977 split the buckets to fix. What went caller-less is the probe as an advertised capability, so the correction owed was `api_database.md`'s claim that the web + mobile UIs probe it — made in the same change.
- [x] **Both stale comments in that file are fixed.** Closed 2026-09-03. The one filed counted web's two probes and mobile's two; today mobile has none and web has one. A **second** comment the entry did not mention — *"Both web probes hit this function"*, in `a probe does not spend the sync bucket` — was equally stale for the same reason. Both now describe the tree as it stands, and the first names the pending move of the last web probe so the comment survives it rather than needing a third correction. Original entry: Its "a call that did not ask for a sync performs none" case explains itself with "Every caller in the tree is a credential probe — web's two `isRunSignUpConfigured` / `isUltraSignUpConfigured` invocations and mobile's two `probeFunction` entries". After [§ 1008](../architecture/decisions.md) and [§ 1041](../architecture/decisions.md) mobile has **zero** entries pointing there and web has one. The assertions are all about `race-listings-sync/index.ts` and still hold, so nothing is red — but a comment that miscounts its own callers is how the § 977 reasoning gets misread next time. One-sentence fix. Owner tree: `apps/backend/supabase/functions/race-listings-sync/` (this round's brief scoped that lane out).
- [x] **The Strava ZIP importer's file-level refusals are hardcoded English rendered raw into the UI.** **CLOSED 2026-09-03** ([decisions § 1058](../architecture/decisions.md)). Done as filed, and the count grew from four refusals to **six**: `Not signed in` was thrown at the top of BOTH importers and rendered the same raw way, and Garmin's `Upload a .fit file or a .zip bundle.` is the same class from the same page — closing Strava alone would have left `/settings/integrations` half translated. `integrations/import_refusal.ts` carries a reason IDENTIFIER plus the specifics as data (the archive's own size and the cap it passed, the column labels); `i18n/import_refusal_message.ts` assembles the sentence, six keys in each of the seven catalogues. Anything thrown that is NOT one of the six is framed inside a `{error}` slot rather than shown bare — the issue #345 shape, and the two new framing keys are registered in `error_framing.test.ts`. The per-row `detail` in the `ImportFailureReport` deliberately stays raw English: it is a diagnostic pasted into a spreadsheet, beside a `reason` that IS keyed, which is why the bare-`Error` guard is anchored to the file-level region and not to the file. Original filing below. `strava-zip.ts` throws four English `Error`s — the 500 MB cap, "Not a Strava export zip (missing activities.csv)", "activities.csv contained no rows", and the missing-required-columns list [§ 1042](../architecture/decisions.md) extended — and `settings/integrations/+page.svelte` assigns `err.message` straight to `zipError`. Every other string on that page resolves through `m()`. It predates this round and was not made worse by it (the extended message is the same class as the one it replaced), but it is now the most informative message in the flow and the one a non-English operator most needs. Doing it properly means the thrown value carrying a reason IDENTIFIER plus the column list as data — the shape `import_failures.ts` already uses for its reasons and `RaceResultsTruncated` uses for identity — rather than a sentence, then keys in all seven catalogues. The Garmin importer's throws have the same shape. Owner trees: `apps/web/src/lib/integrations/` (the throw), `apps/web/src/routes/settings/integrations/` (the render) and `apps/web/src/lib/i18n/` (the keys) — no single lane owned all three this round.
- [x] **The Strava-ZIP strictness guard is source-level where it could be executed, which is what kept the required-column refusal inline.** **CLOSED 2026-09-03** ([decisions § 1059](../architecture/decisions.md)). Exactly as filed: `missingRequiredStravaColumns(idx)` now lives beside `indexHeader` in `strava-zip-header.ts` and BOTH guards execute it — the strictness suite's activity-type case is a real call with a real header, and `strava_zip_required_columns.test.ts` measures every column's absence, the either-block distance rule, the § 979 Sport-Type-only era, and the complete-header case that would otherwise let a guard pass while refusing every legitimate export. The one claim left as source is the one executing the function cannot make: that the importer still asks it, before it reads anything. Original filing below. Filed by the mobile lane as "the last hand-rolled comment-stripper"; that half was closed by the web lane in this same round ([§ 1033](../architecture/decisions.md)) — the file now imports `core/strip_comments.ts` and its `REGISTER` entry is gone. The half that survives: `strava_zip_strictness.test.ts` reads the slice between `const idx = indexHeader(header)` and `const seen = await` for the literals `idx.type < 0` and `Activity Type`, which is why [§ 1042](../architecture/decisions.md) left the six-column refusal INLINE in `strava-zip.ts` (a module that imports supabase-js and cannot be executed under raw tsx) instead of moving it to `strava-zip-header.ts`, which exists precisely so header logic can be unit-tested for real. Re-anchoring that assertion onto a pure `missingRequiredStravaColumns(idx)` would turn a source-level guard into an executed one on both sides. Owner tree: `apps/web/src/lib/integrations/`.

## Surfaced by the #789 round 35 backend/SQL lane (2026-09-03)

Three items closed in-round ([decisions §§ 1046-1051](../architecture/decisions.md)).
What follows is what the lane found and could not take, in each case because the
artefact lives outside the paths it owned.

- [x] **The export sweep's archives need a Storage-API reaper, and it is a Go-worker job kind.** [§ 1049](../architecture/decisions.md) measured what `cleanup_stale_export_blobs()` actually does: the `storage.objects` row goes, the archive stops being listable, and **the bytes stay on the storage backend with a matching `sha256`**. The sweep buys reachability, not erasure, and the SQL tier cannot buy more than that — a row delete is not an object delete, which is exactly why `protect_objects_delete` exists and why its hint reads "This prevents accidental data loss from orphaned objects." The durable shape is the one the closed entry named: a job kind drained by `apps/job_worker/`, which already writes the archive through the Storage API and already holds the service key, leaving `expire_stale_export_jobs()` as the only SQL half. It was not built in the backend lane because the enqueue needs a new value in `jobs_kind_chk` — a **set-shaped** CHECK, so `apps/web/scripts/check_constraint_unions.mjs`'s coverage rule demands a `PAIRS` edit naming every client rail, and that script was outside the lane. So this needs a change owning `apps/job_worker/` **and** `apps/web/scripts/`. Two design notes for whoever takes it, both from § 1049. First, the sweep currently destroys the only record of which blobs need erasing: once the row is gone nothing in the database knows the object exists, so an API-based reaper has to either run BEFORE the row delete or list the bucket. Enqueue-then-delete in one transaction is the cheap ordering. Second, a backlog table was considered and refused rather than half-built — an object name embeds a user id, so a table holding it after that user's row is deleted needs its own retention answer and its own interaction with `delete-account`, which is the same owner call as the payout-style gates. A queue nothing drains is worse than an honest gap. **Go half closed 2026-09-03; the SQL half is filed below and is one commit** ([§ 1112](../architecture/decisions.md)). `handler_export_blob_reap.go` + `SupabaseClient.ListStorageObjectsWithMeta` / `DeleteStorageObjects` + 15 tests are on `main`. The design differs from the filing's on one point and it removes the ordering problem rather than sequencing around it: the reaper LISTS the bucket instead of being handed paths, and since the list API reads the same `storage.objects` rows, the API delete removes bytes and rows together and the SQL row-delete becomes redundant rather than something the Go half must run before. An object the list API did not date is skipped, not erased; a non-positive `retention_days` falls back to the default; the default window is the SQL sweep's own 7 days and a test fails if they diverge. NOT in `Worker.dispatch` — `worker_dispatch_coverage_test.go` fails a `case` for a kind `jobs_kind_chk` forbids, and that CHECK is a migration this lane could not land.
- [x] **`docs/compliance/data-subject-rights.md` was corrected by a lane that does not own `docs/compliance/`.** **CLOSED 2026-09-04** ([decisions § 1136](../architecture/decisions.md)) — **the correction is right and stands**, re-read claim by claim against the code rather than against the filing: the sweep deletes `storage.objects` rows, `protect_objects_delete` exists because a row delete is not an object delete, the probe's bytes stayed with a matching `sha256`, `20260927_001`'s background-sweeper belief is unsupported, the Storage-API delete does remove the file (which is `delete-account`'s path), and the residual is that the measurement was on the local `file` backend. No retraction owed. **One sentence had gone stale and is fixed**: it called the durable fix "filed with an owner", and [§ 1112](../architecture/decisions.md) has since WRITTEN it — but `export_blob_reap` ships unroutable (`jobs_kind_chk` forbids the kind, nothing enqueues it, `Worker.dispatch` carries no case), so the state the paragraph describes is still the state in production. Both `data-subject-rights.md` and `retention.md` now say the fix is half built and inert and name the four statements that finish it. **One thing was missing rather than wrong** and is added: a Storage-API reaper cannot reach the bytes past sweeps already orphaned — it lists `storage.objects`, and those rows are gone — so the 74 files § 1049 counted stay on the backend whatever the reaper does. The `Status: scaffold` framing was not touched. Original filing below. [§ 1049](../architecture/decisions.md)'s measurement falsified the paragraph that said whether the bytes are reaped "is not measured here", and leaving a retention claim standing on a belief now known to be wrong was worse than editing outside the owned tree — so the backend lane rewrote that one paragraph. It is flagged here rather than hidden: if a compliance owner re-reads the file, that hunk is the one to check, and the surrounding `Status: scaffold` framing was not touched.
- [x] **The four filters are `> 0` now — and the branch the entry did not name could not save the run at all.** Closed 2026-09-03 ([decisions § 1078](../architecture/decisions.md)), migration `20270706000003`. The filed half is exactly as described and the four are tightened. The half it missed is reachable and worse: the **whole-run** branch has no positivity filter at all, and `runs_duration_s_check` is `duration_s >= 0` on purpose (a run imported with a distance and no time is a row we store, and `20270705000004` left it so). A zero-second run in a PR bracket is therefore storable, becomes the FASTEST candidate for that bracket, reaches `personal_records`, and is refused there by `best_time_s > 0` — from inside an AFTER trigger on `runs`, so the 23514 fails the INSERT of the run. Measured: an ordinary session inserting `(duration_s => 0, distance_m => 5000, source => 'app')` cannot record the activity, and the error names a table it has never heard of. So the entry's *"only a CHECK now stops it awarding a zero-second PR"* is right that the CHECK stops the award and wrong that stopping it there is harmless. The bound stays in the refresher rather than moving onto `runs.duration_s`: not having run 5 km in zero seconds is a PR-eligibility rule, not a storage rule. Pinned by `personal_records_positive_times_test.sql` (6 tests, each with its positive control). Original entry: `20270705000004` bounds `runs.fastest_5k_s` / `_10k_s` / `_half_marathon_s` / `_marathon_s` at `> 0`, which makes the RPC's own `and fastest_5k_s >= 0` filter equivalent rather than load-bearing — but the filter is still written in the shape that admits the bad value, four times, and a reader could reasonably conclude it is the guard. Tightening the four filters to `> 0` is a one-line-each change inside a SECURITY DEFINER body with a shipped pgtap suite over it; it was not taken in-round because the constraint is the root fix and re-emitting the whole function to change a comparison operator is a bigger diff than the defect justifies. Do it the next time that function is open for another reason. The same reading applies to `personal_records.best_time_s`, now `> 0` at the column.

## Surfaced by the #789 round 35 integration pass (2026-09-03)

Both entries below are closed; they are recorded because the class recurs every
round that fans out across lanes ([decisions § 1057](../architecture/decisions.md)).

- [x] **Two failures existed only in the merged tree.** The `RBK1` v2 stride
  moved fields in two suites the firmware lane had no reason to open, and a
  `$lib` import in a new web unit test resolved only in the worktree that
  happened to carry a generated `.svelte-kit/tsconfig.json`. Closed
  2026-09-03 — the roadbook layout constants are public and both suites derive
  from them, and an anchored guard now refuses an aliased import under
  `src/**/*.test.ts`.
- [x] **Two lane filings contradicted each other after the merge.** The mobile
  lane appended a ticked copy of the `run_recorder` entry instead of ticking it
  in place, leaving an open and a closed copy (`check_doc_checkboxes` caught
  it), and its filing that `strava_zip_strictness.test.ts` is "the last
  hand-rolled comment-stripper" went stale the moment the web lane converted
  that file in the same round. Closed 2026-09-03 — the duplicate is gone and
  the filing is rewritten to keep only the half that survives. Worth carrying
  forward: a lane cannot see what a sibling lane closed, so any filing that
  counts remaining instances of a class needs re-reading at integration.


### Surfaced by the #789 round 36 web / core + guards lane (2026-09-03)

- [x] **Four more script-end-tag regexes carry the `js/bad-tag-filter` defect, and three of them are production HTML sanitisers.** Found when CodeQL flagged the one copy [§ 1065](../architecture/decisions.md)'s split moved into `consent_guards.test.ts` — it reports an alert per changed FILE, so a byte-identical regex that had sat on `main` since it was written surfaced only because the code got a new path. That one is fixed. The siblings are not, and they are worse: `share/entity_spa_shell.ts:39`, `share/share_route_spa_shell.ts:50` and `share/share_run_spa_shell.ts:52` each strip an existing JSON-LD block with `<\/script>` and **no whitespace tolerance at all**, so a block spelled `</script >` survives the strip and the per-entity JSON-LD is then appended beside a stale default; `metadata_registry_guard.test.ts:113` and `:118` use the `<\/script\s*>` shape the fixed one had. The parser rule is that `</script` followed by whitespace, `/` or `>` closes the block and everything to the first `>` is junk, which `<\/script(?=[\s/>])[^>]*>` states. Not taken in the same change: three of the five are the served share-page sanitisers, and rewriting an XSS-adjacent production sanitiser at the tail of a five-lane integration, where it would get no review of its own, is the same argument [§ 1065](../architecture/decisions.md)'s filing made about moving a file mid-round. Owner tree: `apps/web/src/lib/share/`. Closed 2026-09-03 — [decisions § 1086](../architecture/decisions.md). **The stated symptom was the small half.** Measured on the shells these files' own tests already use: against `</script >` the lazy `[\s\S]*?` does not stop, it runs on to the SPA bundle's own `</script>` and consumes the stale block, `</head>`, `<body>`, the `#svelte` mount div and the bundle tag — after which `injectEntityHead` searches for a `</head>` that is no longer there, takes its malformed-shell branch and returns the wreckage with none of the per-entity meta. A share page with no meta, no mount point and no application, not a stale node beside a real one. The search also found fourteen more of the same rule in the same files: every `<\/title>` (7) and `<\/head>` (5) closes the same way, `</title >` leaves the generic title standing for a crawler to pick and `</head >` drops the whole injection. 19 of the tree's 20 raw-text end-tag regexes were intolerant; only § 1065's own was right. The JSON-LD OPEN tag was widened too — it matched one exact attribute spelling, so a CSP nonce would have left a second `WebPage` node standing. `apps/web/src/lib/raw_text_end_tag_guard.test.ts` fails the PR on the intolerant shape anywhere in `src/` or `lambda/`, tests included. **Measured after the fix, which corrects the severity:** a production build's `apps/web/build/index.html` — the shell all five share Lambdas embed — carries a `<title>` from `app.html` and nothing else the strips look for: no canonical, no `og:`/`twitter:`/`description` meta, no JSON-LD. `SeoHead.svelte` spells its own close without whitespace and no `.svelte` file in the tree spells one with it, so the class was **latent everywhere, not live**. It is build output, so its bytes are a property of `app.html`, the adapter and whatever minifier is in the chain — which is the argument for stating the parser rule rather than matching today's emission. That measurement also turned up why the shell is so bare; filed separately below.

- [x] **Six prose pointers still name `security_guards.test.ts`, and all six live in trees no web lane owns.** **CLOSED 2026-09-03 at integration**, which owns every tree at once. All six repointed, and each destination was found by locating the assertion rather than by taking the filing's word: five matched what this entry proposed, and **one did not**. `config.toml:116` pins "the per-IP rate limit on the anon branch" — the anon BRANCH OF THE FUNCTION, not a git branch — which is `rate_limit_guards.test.ts:16` (it reads `clip-public-track/index.ts` and asserts at least two `checkRateLimit` calls, per-user plus anon), not the `ci_workflow_guards.test.ts` proposed here. The other five landed as filed: `privacy_guards` for the accepted-subset-of-strippable migration comment, `consent_guards` for `seed.sql`, `lambda_guards` for the `TIER_LIMITS` lockstep comment in `variables.tf`, `ci_workflow_guards` for `template-sync-ignore`, and `source_scanner_guards` for the stripper register. No pointer naming a deleted file remains anywhere outside `decisions.md` and this file, where they are historical records.

- [x] **`docs/testing/test_inventory.md` has no round-36 entry for the guard split.** **CLOSED 2026-09-03 at integration** — the collision this entry names is real per-lane and gone once the five lanes are one tree, so the paragraph was written there. The twelve per-file counts in it were recounted on the merged tree rather than copied from the lane's report; they total 91, which is what the pre-split file held.

- [x] **Mobile's `geocodePlace` — the geocode half — still reads a provider centre through a bare `as num`.** [§ 1011](../architecture/decisions.md) added `isUsableLatitude` / `isUsableLongitude` to `geocoding.dart` and applied them to the two SEARCH paths only; `geocodePlace` (the MapTiler-only geocode with the bbox radius) checks `centerRaw is! List || centerRaw.length < 2` and then casts, so `1e400` reaches a `LatLng` and a bbox corner of the same shape makes `radiusM` a NaN — the two exposures [§ 1066](../architecture/decisions.md) just closed on web, one function below where § 1011 stopped. The bbox cast is worse than web's was: `(bboxRaw[0] as num)` throws outright on a non-numeric corner, inside a `try` that reports the whole geocode as null, so a malformed bbox loses a usable centroid rather than falling back to the 5 km default. Owner tree: `apps/mobile_android/lib/geocoding.dart` (+ the iOS twin). **Closed 2026-09-03** — [§ 1092](../architecture/decisions.md). Both premises re-measured before fixing: `jsonDecode('1e400')` is `Infinity` and passes `is num`, and a string bbox corner throws a `TypeError` out into the catch-all. The centre now goes through `isUsableLongitude` / `isUsableLatitude`, and a bbox is usable whole or not at all — an unusable one falls back to the 5 km default with the centroid intact, which is what the `try` was swallowing. Six tests, each verified failing against the pre-fix file.

- [x] **The web/Dart geocoders are NOT a registerable parity pair, and the recommendation is that they stay unregistered.** Recorded so the question is not reopened as an oversight. The two modules diverge by design — web's `PlaceSearchOutcome` carries a third `aborted` state, web takes an `AbortSignal` where mobile injects a `GeocodingFetcher`, and web's geocode half falls back to Nominatim where mobile's returns null on an empty key — so a registry row would assert a lockstep that has never held, which is a worse failure than no row ([§ 604](../architecture/decisions.md) + § 641: the registry's value is that a row means something). What genuinely IS one contract is the pair of coordinate predicates, and the registry is keyed on module paths with no precedent for registering two functions out of a module. If a future change disagrees, it needs rows in BOTH the root `CLAUDE.md` table and `.claude/agents/shared-library-syncer.md` — neither of which a web lane owns, which is the same wall [§ 1014](../architecture/decisions.md) and § 1039's filings hit. Closed 2026-09-03 — [decisions § 1089](../architecture/decisions.md). Verified against both files rather than against this filing: all three divergences hold today, and there is a fourth — Nominatim rejects a stock HTTP-library User-Agent, so the Dart default fetcher sets one, and a browser forbids scripts from setting that header at all, so the two can never converge on one fetch layer. The `GeocodingFetcher` seam is also `Future<String> Function(Uri)`, returning a **body**, so there is no `res.ok` on the Dart side to mirror. Recommendation unchanged: no row, and the coordinate predicates stay unprotected rather than buying their safety with a standing lie about the other four differences.

- [x] **`geocodePlaceWithKey`'s MapTiler branch had no body-parse guard where its search sibling did, and the class is not searched.** [§ 1066](../architecture/decisions.md) added one after measuring that a 200 with an unparseable body threw a `SyntaxError` out of a function documented to return null. The two sat forty lines apart in one file and only one had the `try`. Nothing in the tree looks for the shape — an `await res.json()` outside a try inside a function whose contract is a null or an `unavailable` outcome — and `geocoding_math.ts` is unlikely to be the only place it occurs. A source guard over the web tree would say; it was out of scope for a round already carrying a 3,728-line file move. Owner tree: `apps/web/src/lib/`. Closed 2026-09-03 — [decisions § 1087](../architecture/decisions.md). **The class had two more members and the first was the same file:** `geocodeViaNominatim` — the other branch of that same exported function — parsed bare a hundred lines below the branch § 1066 fixed, while its own *search* sibling forty lines further down had carried the guard all along, so a dev stack with no MapTiler key took the unguarded road exclusively. The second is sharper: `snapToRoad` returns the unsnapped input point on a non-2xx and has no throwing branch anywhere, so no caller has a reason to wrap it. Both guarded, both pinned. The guard could NOT be keyed on the declared return type: `lookupBarcode` returns `Promise<FoodSearchResult | null>` and throws on a parse failure deliberately, the null being reserved for a genuine no-match — the contract lives in the doc comment. `apps/web/src/lib/response_body_parse_guard.test.ts` therefore carries a register of counts and reasons over the tree's 36 body-parse sites; 10 are deliberately unguarded across four files, and drift in either direction fails. That count is also the only pin `snapToRoad` can have — `routing.ts` reaches `$env/static/public` through `core/supabase`, so the raw `tsx --test` suite cannot load it.
### Surfaced by the #789 round 36 mobile lane (2026-09-03)

Three boxes taken, all three ticked in place above. One of the three filings
described shipped behaviour and was itself the defect; a second's census of the
work remaining had drifted in three of its four numbers. What follows is what
this lane found and could not take.

- [x] **`debugWritesSettled()` deadlocks rather than failing when its zone precondition is unmet, and nothing says so at the call site.** [decisions § 1072](../architecture/decisions.md) measured the rule: the probe is only awaitable when the write it waits on was queued from inside `tester.runAsync`, because `serialiseStoreWrite` links its chain with `.then` and a fake-zone continuation is a microtask only `pump` drains. The failure mode is the worst shape available — a test that hangs to the runner's timeout with no message, which on CI reads as an infrastructure flake rather than as a misuse. The doc comment on `OfflineSyncStore.debugWritesSettled` names the race it closes but not the precondition. Two options, neither taken here because the fix belongs beside the helper rather than beside a caller: state the precondition in the doc comment (cheap, and enough for a reader who is already looking), or make the probe *detect* it — the chain could record the zone each link was queued from and the probe could throw a named error naming the offending call site. The second is the durable one; it wants a look at whether `Zone.current` is a usable discriminator inside `runAsync` before anyone commits to it. **Closed 2026-09-03** — [§ 1093](../architecture/decisions.md). `Zone.current` was measured first and is not a discriminator: across five configurations run in isolated processes, the queueing and awaiting zones are non-identical in all four cases where the comparison is defined, two of which settle and two of which hang — and because every `tester.runAsync` forks a fresh zone, an identity check would have fired on `nutrition_screen_test`, the file that proves the helper works. A drainability probe fails too. So the wait is bounded on `Zone.root` and completes with a `StateError` naming the precondition, the `pumpUntil` remedy and the call site, and all three wrappers state the precondition in their doc comments. The measurement also corrected the rule twice: an intervening `pump` does not rescue a fake-zone write, and a fake-zone await hangs as readily as a `runAsync` one.
- [x] **26 mobile test files pair a temp directory with widget taps and no `pumpUntil` at all, and the sweep is now actionable.** The earlier survey filed this as a population with no stated remedy, which is why nobody has worked it. § 1072 supplies the missing half: after a fake-zone tap, wait on an **observable outcome** with `pumpUntil`; `debugWritesSettled` is for a **real-zone** write, and reaching for it otherwise deadlocks. With that rule the 26 are triage rather than research. Two of them (`gym_exercise_screen_test.dart`, `route_builder_screen_test.dart`) delete the temp dir from `addTearDown`, which is the shape that races a write in flight; the rest use `try/finally` or `tearDown`, which race it just as readily. Not taken here because it is a sweep, not a fix, and this lane's three items already carried one. **Swept and closed 2026-09-03 — nothing needed converting** ([§ 1097](../architecture/decisions.md)). Re-counted first: naively 27 at `076b624c8` over 89 temp-dir files, and **25** once comment text is excluded — two "members" match only on a comment saying why the file does NOT use `pumpAndSettle`, the same defect § 1072 found in the `pumpEventQueue` count. Both files named as the worst shape are non-members (`gym_exercise_screen_test.dart` writes inside `runAsync` and its one tap navigates; `route_builder_screen_test.dart` saves in real-zone `test()` bodies and its widget taps drive a dialog that pops a result). The discriminator also misses the other way: the two real members were fixed by § 1012 and still count, because their correct lever is `debugInProgressSettled`, not `pumpUntil`. All 25 scanned for a tap followed by a store assertion — zero — plus the four highest-risk hand-read.
- [x] **Web's `provider_probe.ts` has no registered Dart twin, and the probe rule now lives on both platforms unregistered.** [decisions § 1073](../architecture/decisions.md) moved mobile onto web's `probeSaysConfigured` rule — configured iff the invoke did not throw — but stated it natively inside `RaceService.isProviderConfigured` rather than as a `provider_probe.dart`. Two reasons, and only the first is about taste: a one-call-site helper wrapping a `try`/`catch` is a preemptive abstraction, and — decisively — a Dart twin would be a new **parity pair**, which needs rows in BOTH the root `CLAUDE.md` table and `.claude/agents/shared-library-syncer.md`, neither of which the mobile lane owns. A pair named by one registry is a pair whose divergence nothing detects ([§ 641](../architecture/decisions.md)), so half-registering it would be worse than not registering it. The rule is now duplicated across platforms with nothing comparing the two, which is exactly the shape § 641 and [§ 852](../architecture/decisions.md) were both about. Do it in a change that owns the root `CLAUDE.md`: either register the pair, or record in both modules' headers that the rule is deliberately single-sourced per platform and why. Cheap either way; the cost of leaving it is that the next divergence is invisible. **Closed 2026-09-03 — not registered** ([§ 1095](../architecture/decisions.md)). The two halves have no shared signature: `@supabase/functions-js` RETURNS `{data, error}` while Dart's `functions_client` THROWS, so a Dart twin would take an error value no Dart caller holds and a pair's syncer would have no computation to compare. What the pair would have bought is bought directly instead — `race_providers_test.dart` now reads `provider_probe.ts` and fails if it stops exporting `probeSaysConfigured`, stops being the nullish check, or grades a status again. `race_service.dart`'s header records the decision; **the reciprocal note in `provider_probe.ts`'s header is still owed** — the mobile lane cannot edit `apps/web/`.
- [x] **The parked-push state is mobile-only, and deliberately owes web nothing — worth saying so before someone files it as a § 24 inversion.** [§ 1070](../architecture/decisions.md)'s `blocked_runs.json`, `RunPushOutcome` and the run-detail card exist because the phone has a local queue that retries; the web app is always online and has no unsynced-run concept at all (`parity.md`'s "Bulk sync button" row already records that). `RunPushOutcome` lives in `core_models` rather than in `api_client` so the store can read it without a client dependency, not because a second platform is expected to consume it. If web ever grows an offline queue this is the design to mirror; until then a missing web surface here is correct. **Confirmed and closed 2026-09-03** — [§ 1096](../architecture/decisions.md). Re-verified against the code, not the entry: `blocked_runs.json` + `RunPushOutcome` + the run-detail parked card are all live on mobile, `apps/web/src/lib` contains no reference to an unsynced queue or a blocked run at all, and `local_run_store.dart` imports `core_models` and `flutter/foundation` only — which is the layering reason the outcome type sits there. Recorded as a `parity.md` row of its own beside the bulk-sync row, marked N/A for web with the reason, so the next reader meets it in the matrix rather than in an ADR.
### Surfaced by the #789 round 36 backend / SQL lane (2026-09-03)

- [x] **`TestRedisHub_SubscribeWithHistoryReplaysThenStreams` flaked once on CI, and the obvious explanation is measurably wrong.** It failed on PR #854 (run 33798704651) with `timed out waiting for the live ping` after 1 s, in a tree that PR does not touch, having passed the previous four runs. The natural diagnosis is that `rdb.Subscribe` returns before Redis has registered the subscription, so the `Publish` immediately after `SubscribeWithHistory` is dropped by a server with no subscriber yet — which would also undermine the SUBSCRIBE-to-LRANGE window reasoning the surrounding comment rests on, and would make a spectator miss their first ping. **Measured, and it does not happen**: a probe that builds a fresh miniredis, subscribes, takes `Channel()` and publishes immediately, 300 times under `-race`, lost **0 of 300**. `Channel()` establishes the subscription before it returns. So that is ruled out and the hypothesis should not be re-derived. What remains unexplained is a plain 1-second deadline missed under `-race` on a 2-core shared runner while `go test -race ./...` runs every package at once — goroutine starvation rather than a lost message. It could not be reproduced here: 40 runs of the single test and 6 runs of the whole `livehub` package under `-race` all passed. **Raising the deadline is not the fix and must not be the fix** — if a message can be lost, a longer wait never finds it; if it is starvation, a longer wait hides how slow the path became. Closing it needs an instrumented reproduction that records whether the forwarder goroutine ever received the message, which distinguishes the two in one run. Owner tree: `apps/job_worker/internal/livehub/`. **Closed 2026-09-03 — and the filing's rule-out was itself wrong** ([§ 1113](../architecture/decisions.md)). The message CAN be lost. The 0-of-300 probe ran on an idle box and called `Channel()` on its own goroutine where `startForwarder` calls it inside one; re-probed under the condition the flake occurred in (two cores saturated, `-race`), the same round lost **8 of 400**, every loss with `PUBLISH` reporting **zero receivers** — the server saying it had no subscriber at that instant. `Client.Subscribe` calls `pubsub.Subscribe`, DISCARDS its error, and that writes the SUBSCRIBE and releases the connection without reading a reply; go-redis's own `ExamplePubSub` does a `Receive` for exactly this reason. Both subscribe paths now block for the `*Subscription` confirmation: **0 of 400** on the identical load. The deadline was not raised. The test now keeps the receiver count and, on a missed deadline, waits a grace window before failing so it says whether the ping was lost or merely late; the regression pin is 25 rounds down both paths asserting one receiver each, which fails within a few rounds on the unfixed code under load and passes 20 consecutive `-race` runs on the fixed one. `go test ./...` and `go test -race ./...` green across all 14 packages.

Four items closed in-round ([decisions §§ 1076-1080](../architecture/decisions.md)),
three of them on filings that were wrong about their own premise. What follows is
what the lane found and could not take, plus the one thing the closed items
sequenced rather than finished.

- [x] **The frozen 1,488-pair case-fold table is now affordable and is still not built.** This is the surviving half of the entry above. [§ 830](../architecture/decisions.md) closed the *collation* dependence; the *runtime-version* one is untouched and measured — after the two named folds the three rails still disagree at **465** code points between web and mobile, **410** between mobile and the server, **55** between web and the server (Cherokee, Vithkuqi, Garay, Medefaidrin, Latin Extended-D/E, supplementary planes). The blocker was cost: `translate()` over the table is 60 µs a call against 0.34 µs for `lower()`, and five RPCs re-derived the key once per `gym_sets` row. [§ 1076](../architecture/decisions.md) removed that — the fold is now paid once per WRITE, so the table's cost is bounded by how often a lifter logs a set. What remains is the table itself, on three rails: `EXERCISE_*` in `gym_prs.ts`, `gym_prs.dart`, and `normalise_exercise_name`. Two notes for whoever takes it. The SQL rail is cheap to WRITE — `create or replace function` plus the keyset backfill `20270706000002` establishes, no rewrite, because the key is trigger-maintained and not generated — **but it cannot ship on its own, measured 2026-09-03** ([decisions § 1102](../architecture/decisions.md)). Three VALIDATED CHECKs name the function (`gym_sets_exercise_key_canonical`, `gym_routine_exercises_exercise_key_canonical`, `exercises_name_key_canonical`), two of them over client-stamped columns, so the function is the acceptance predicate the other two rails write against. Demonstrated in a rolled-back transaction: replace the function so it folds one extra code point, and a row stored under the old fold survives (Postgres re-checks nothing) but `update t set name = name` — a no-op self-update — raises 23514, a fresh insert of the same pair is refused, and `validate constraint` fails. Blast radius measured: Postgres's ICU lowercase map is non-identity at 1,433 code points, a current single-char table (Unicode 16.0) at 1,459, and they disagree at 28 — 27 the newer table lowercases and ICU does not (U+1C89, U+A7CB/CC/DA/DC, the Garay block U+10D50-52 and 19 more), one the other way (U+0130, already folded by name since § 830), and **zero** mapping to different values. So the SQL-only edit moves the fold at exactly the newest-block code points the table exists to protect, and makes every stored key containing one of them un-updatable. The three rails move together or not at all. The CLIENT rails are the expensive ones and they are where the divergence lives: mobile's `exercise_key` writes to `gym_routine_exercises` and `exercises` are still client-stamped under a validated CHECK, which is the path § 830 measured a 23514 on. An ASCII-only fold is **not** the cheap alternative: 397 code points between U+00C0 and U+052F fold identically on all three rails today and it would stop folding every one, splitting "Überzüge" from "überzüge". Owner trees: `apps/web/src/lib/gym/`, `apps/mobile_android/lib/`, `scripts/check_shared_constants.mjs` — no single lane owned all three this round. **CLOSED 2026-09-05 ([decisions § 1175](../architecture/decisions.md) + [§ 1176](../architecture/decisions.md) + [§ 1177](../architecture/decisions.md) + [§ 1178](../architecture/decisions.md)).** All three rails moved in one change: the frozen Unicode 17.0 SIMPLE-lowercase table is generated by `scripts/gen_exercise_fold_table.mjs` into `apps/web/src/lib/gym/exercise_fold_table.ts` + `apps/mobile_android/lib/exercise_fold_table.dart` and into the new `public.exercise_fold_case` (migration `20270709000010`), and no rail reaches for its runtime's `toLowerCase()` / `lower()` any more. **Residual disagreement is zero**: every assignable code point through each rail's whole shipped pipeline, chunk-hashed, gives md5 `76a152d0e2a86b0b39a311597b42633b` on all three -- 465 / 410 / 55 becomes 0 / 0 / 0. **Two of this entry's numbers were wrong.** The ICU-vs-frozen-table delta is not 28 (27 one way, 1 the other): measured on this box it is **55 one way and 0 the other**, because the 28 was taken against Unicode **16.0** and the "1 the other way" was U+0130, a differing VALUE rather than a fold ICU has and the table lacks. Everything else re-measured true -- 465 / 410 / 55, the 1,488 pairs, 60.2 us against 0.35 us. A third thing the entry could not have known: **web does not move at all**, because the frozen table IS web's own answer -- over 8,896,512 strings (every code point in eight contexts) the old and new web pipelines differ on zero, so the whole move is bringing mobile and the server onto what web already said. Blast radius on real rows: 0 of 23 `gym_sets`, 0 of 4 `gym_routine_exercises`, 0 of 43 `exercises` re-key. Simple rather than FULL mapping because a per-code-point table can carry neither a context (Final_Sigma, still folded by name) nor a 1:many expansion (U+0130, now a table entry -- its hand-written pre-fold is deleted from all three rails); every entry being 1:1 is what lets the SQL rail be a `translate()`. The `collate "und-x-icu"` pin goes with the `lower()` it pinned, so the derivation is now collation-FREE rather than collation-pinned and the `immutable` marking is true in fact. `scripts/check_shared_constants.mjs` compares the three tables, the version stamps and the SQL ASCII fast path against the table's own ASCII half; there is deliberately no re-render guard ([§ 1176](../architecture/decisions.md)) because the table is frozen rather than tracking.
- [x] **`auth-email` has no positive-path test, and it is not blocked on a credential.** (Closed 2026-09-03 — [decisions § 1100](../architecture/decisions.md).) The filing was right on every point including the SMTP one. `ci.yml`'s boot step now writes `SMTP_HOST=host.docker.internal` / `SMTP_PORT=54325` / `SMTP_FROM` beside the hook secret — `127.0.0.1` is the functions CONTAINER's own loopback, and the CLI starts that container with `--add-host host.docker.internal:host-gateway`, which is why the committed `supabase/functions/.env` already uses that pair (the network alias `inbucket:1025` resolves from there too and would have worked equally). The case signs a signup payload, POSTs, and reads the delivered mail out of Mailpit. Two assertions, both checked for discriminating power against the live host: the subject must be the FRENCH catalogue entry (the same payload with no locale renders `Confirm your email address`, with `de-DE` renders `Bestätige deine E-Mail-Adresse`), and the body must carry this run's unique `token_hash`. The payload carries no `user.id`, so the handler skips its `user_settings` read and the case needs no service-role key. Mail is found by searching on the recipient, never by clearing the catcher. 25/25 handler-envelope cases pass against a host booted the way CI boots it; negative control with a wrong secret fails at the 200 assertion with `401 bad_signature`. [§ 1079](../architecture/decisions.md) found the "positive-path Edge Function tests" filing wrong and this is the honest residue. The Standard Webhooks secret is ours (`ci.yml` already writes `SEND_EMAIL_HOOK_SECRET` into `.env.local`) and Mailpit is already in the local stack, so a correctly-signed request would deliver and be assertable against the Mailpit API — the same shape the three `stripe-events-webhook` side-effect cases already use. What blocks it is that the function reads `SMTP_HOST` / `SMTP_PORT` / `SMTP_FROM`, none of which is in `.env.local`, and adding them means editing `.github/workflows/ci.yml`. That file was outside this lane. The one real subtlety: the function runs inside the edge-runtime container, so the host must be reachable from there (`host.docker.internal` or the Mailpit container name), not `127.0.0.1`. Owner tree: `.github/`.
- [x] **`refresh-tokens` has no positive-path test, and this one genuinely needs a mocked upstream.** (Closed 2026-09-03 — [decisions § 1101](../architecture/decisions.md).) **The filing is wrong on both halves.** The seam it names is a `globalThis.fetch` stub, not an injectable parameter, and `refresh-tokens/lib.test.ts` has been using it since PR #438: "refresh-tokens routes through the CAS helper with the read refresh token" drives the success branch through a stubbed Strava token response and asserts `refreshed === 1` plus the compare-and-swap arguments, and `sweep_invariants.test.ts` adds "each integration is refreshed with its OWN token, and the count is the successes" — 16 unit tests, all passing. Building an injection parameter would have been a second seam for a solved problem. What was genuinely missing sits one layer up: all three `refresh-tokens` cases in `handler_envelope.test.ts` are 403s, so a handler that refused everything passed all three and the gate OPENING had no coverage anywhere. Added, and it needs no upstream either — the fixture is an integration inside the sweep's one-hour window (so the select is not empty, which is what would make the 200 vacuous) holding no vault secret, so `get_integration_tokens` yields nothing and the loop `continue`s before `refreshStravaToken`; that the upstream was not contacted is then observed rather than assumed, because a real call with an unusable grant would 4xx and stamp `disconnected_at`, which the test reads back. Its success branch POSTs to strava.com's OAuth token endpoint with a real client secret and a real refresh token, so no test-chosen secret reaches it — this is the narrow case the closed filing described in general terms and is true only here. The durable shape is an injectable token-exchange fetch (the `_shared/strava_upstream.test.ts` seam already exists for the import path) rather than a live credential in CI. Owner tree: `apps/backend/supabase/functions/refresh-tokens/` — not taken this round because the lane's own items were larger, not because it is blocked.
- [x] **`donations_status_lock_test.sql` fails on this workstation and passes on CI, and the divergence is now costing a lane a diagnosis every round.** (Closed 2026-09-03 — [decisions § 1098](../architecture/decisions.md), migration `20270707000001`.) The filing's shape was right and its size was not: enumerating "the routines the default currently covers" is the work, and the answer is **one**. Measured against the local image, where `proacl` is exactly `{postgres}` plus what a migration stated: the 7 RPCs the Edge Functions call as service_role and the 5 the Go worker calls already carry an explicit `service_role` entry; all 95 RPC names the web / Lambda / mobile / `api_client` trees call already carry an explicit `authenticated` entry and none has a NULL `proacl`; every base table already grants service_role the four DML verbs by name (`20270702000002`); and the 74 functions still on a NULL `proacl` are trigger bodies whose every out-of-migration reference is a comment, not a call. So the migration grants `fundraiser_totals(uuid)` to `service_role` and nothing else — `fundraiser_feed` is deliberately refused, having no service_role caller. `anon_execute_registry_test.sql` gains the positive half of its registry (the role holds EXECUTE, and holds it through an entry naming that role rather than through PUBLIC or a NULL `proacl`), with its CI-vacuity limit stated. Before: `Files=286, Tests=2601`, `donations_status_lock_test` bad plan (19 planned, 0 ran). After: `Files=286, Tests=2622`, all pass. It dies at its first `fundraiser_totals` call with `permission denied for function`, as `service_role`. Measured on the local stack: `fundraiser_totals`' ACL is `{postgres=X,anon=X,authenticated=X}` — no `service_role` — and `20270630000001` issues no explicit grant for it, relying on Supabase's `alter default privileges`. The workstation CLI is 2.109.1 and CI pins 2.84.2, and the two images disagree about which roles that default covers. Nothing in the repo is wrong today, but the durable fix is the house form § 790 already established for `normalise_exercise_name`: grant EXECUTE **by name** to every role that must call the function, rather than inheriting whatever the image's default privileges happen to be. That is a migration re-emitting the grants for the routines the default currently covers, and deciding the list per routine is the work. Owner tree: `apps/backend/supabase/migrations/` — filed rather than taken because the list needs a deliberate per-routine judgement, not a sweep.
## Surfaced by the #789 round 36 Wear OS / infra lane (2026-09-03)

Four items closed ([decisions §§ 1082-1085](../architecture/decisions.md)), one
verification item re-run and left open for the reason it was already open (no
lane holds AWS credentials, so nothing is `plan`ned). No filing in this lane's
set turned out to be wrong. What follows is what the lane found and could not,
or deliberately did not, take.

- [x] **A corrupt run queue is invisible on the pre-run screen, and the count that would show it is frozen.** **CLOSED 2026-09-03** ([decisions § 1104](../architecture/decisions.md)). Both calls made deliberately. **(a) Retry, not `retry`-vs-`catch` by taste**: catching is not a judgement about corruption, it is a judgement that the first failure is the last one, and a read is one file open — so `store.queue` is `retryWhen`-ed, unbounded, with `queueReadRetryDelayMs` doubling 1 s → 60 s. Deliberately NOT `DrainBackoff`'s 60 s → 30 min curve: that throttles a network round trip, this throttles a local open whose failure takes the recovery affordance off screen. The shift is clamped because `1_000L shl 64` is `1_000L shl 0` on the JVM and an unclamped exponent silently resets the backoff to its base. **(b) The counted chip's own slot becomes a retry chip**, claiming no figure — "Sync ?" and a persistent error line both rejected for the reasons filed, and what was missing was the affordance rather than a sentence. Not gated on `online` (the read is local and the network is not a party to it), still gated on `authed` (`drainQueue` returns from `awaitAuth()` before reading anything), and the warning colour is not the only signal — the label differs and the content description carries the whole sentence. `queueUnreadable` is a second field rather than a nullable count so "never read yet" cannot flash the chip on every cold start, and BOTH readers of the file write it so the drain and the chip cannot disagree. 8 new Wear tests (74 classes / 725 at that commit, from 717). Original filing below. [§ 1082](../architecture/decisions.md) makes the drain report an unreadable queue as `syncError`, which renders on `PostRunScreen` only — the screen the runner is on immediately after the run that just entered the queue, which is where it matters most. It does NOT render on `PreRunScreen`, whose "Sync N runs" chip is gated on `queuedCount > 0`, and `queuedCount` comes from `observeQueue`'s collection of `store.queue` — a flow whose [§ 1055](../architecture/decisions.md) `.catch` TERMINATES it. So after one failed read the count is frozen at its last value for the life of the process (0 on a cold start), the chip never appears, and a runner who reboots into a corrupt queue is shown nothing at all. Two decisions sit underneath and neither is the drain's: whether the queue stream should be `retry`ed rather than caught (a corrupt DataStore file will fail again, but the failure could also be transient I/O), and what a pre-run surface should say on a 1.4-inch screen when the count itself is unknown — "Sync ?" is worse than silence, a persistent error line competes with the battery and recovery notices already anchored there. Entirely inside `apps/watch_wear/`. Not taken because it is a UI/state design decision, not the saved-data one the closed filing asked for, and picking it by default is the mistake that filing warned about.
- [x] **`RecordingRepository.Metrics.avgBpm` carries two different claims at two different times.** **CLOSED 2026-09-03** ([decisions § 1105](../architecture/decisions.md)). `Metrics` carries `finishedHr: HeartRateClaim?` — the pair § 1083 already returns — written once by whoever makes the `Finished` transition and null everywhere else; null claims no heart rate rather than falling back. **The filing's premise was wrong in the direction that made the fix better**: the rolling mean is not "the live figure the running screen renders". `RunningScreen` takes the instantaneous `bpm`, the active-run tile carries no heart rate at all, and the grader reads the raw `bpmSum`/`bpmCount` off the service rather than off this state — so the live write had NO reader between the sample that produced it and the stop that overwrote it, and its only effect was the hazard. It is therefore deleted, not renamed. That hazard was real: `update` is a CAS loop because five writers race on it and cancellation cannot interrupt a non-suspending call in flight, so the collect's transform could land after the stop's and restore an ungraded mean over an average the grader had suppressed. **On the source guard**: split by whether a type can express it. "Both save paths grade through `heartRateClaim`" stays a source guard — that two named call sites call one function is not a Kotlin type. The ordering assertion is DELETED rather than converted, because its premise goes with the second writer. Replacing it: one real test that applies the collect's transform after the stop's on the actual repository and asserts the suppressed claim survives, plus a scan widened from the two save bodies to the whole recorder — the collect was dividing the pair in a body the old guard deliberately excluded. Original filing below. During a run it is the live rolling mean the running screen renders; at `Finished` it is the graded claim [§ 1083](../architecture/decisions.md) produced, because `stopRecording` overwrites it in the same `update`. `RunViewModel.handleFinishedRun` reads the field and cannot tell which it got — it is correct only because the overwrite happens first, and `HeartRateCoverageTest` pins that ordering with a source guard rather than a type. The durable shape is a separate field (or a `FinishedHeartRate` on the `Finished` transition) so the ungraded live figure is not reachable from the save path at all. Small, `apps/watch_wear/` only, and deliberately not bundled into the change that introduced the grading.
- [x] **Nothing reads `hr_coverage`, so a suppressed `avg_bpm` is indistinguishable from a run with no heart rate at all.** [§ 1083](../architecture/decisions.md) writes the key and states that its most useful reading is when the average is ABSENT — but every reader of `avg_bpm` (`apps/web/src/routes/runs/[id]/+page.svelte`, `apps/mobile_android/lib/screens/run_detail_screen.dart`, the export CSV) renders nothing when it is missing, which is the same thing they render for a phone run recorded with no strap. A run-detail line reading "heart rate covered 12 % of this run" is what makes the suppression legible instead of looking like data loss. Both trees belong to other lanes; the registry row is written and the data will be there when a reader is. **Web half closed 2026-09-03** — [decisions § 1088](../architecture/decisions.md); the mobile half was built independently in the same round. Run detail now resolves three states rather than one: coverage present with no average states the share and that it was too little for an average; a coverage of exactly `0` (sensor enabled, nothing delivered) gets its own sentence rather than "covered 0 %", and `hrCoveragePercent` floors a nonzero fraction at 1 % so rounding can never reach that reading; and an average that IS reported carries the share it was taken over whenever that is under 100 %, because 142 bpm over 62 % of a twelve-hour run is not that run's average. Absent stays *unmeasured* — a non-number, a non-finite one, or a fraction outside `0..1` keeps the old copy rather than reporting a duty cycle nobody measured. Three i18n keys in all seven catalogues, unit tests on the grading, three `hr-zones.spec.ts` cases on the rendered states. The CSV export still hands over `avg_bpm` unqualified — filed below. **Mobile half closed 2026-09-03** — [§ 1094](../architecture/decisions.md). `run_detail_screen.dart` now reads the key into three states: an average with no coverage record renders unchanged, a partly-covered average is qualified by an `HR coverage` cell (full coverage is not drawn — it qualifies nothing), and a SUPPRESSED average takes the `Avg HR` slot itself reading `12% covered`, because a reader looking for a missing number looks at the label it is missing from. `0%` is a real answer; an out-of-range, non-finite or non-numeric value is read as no record rather than clamped. Eight widget tests, seven ARBs. The web half is a separate lane's.
- [x] **`apps/watch_ios` writes `avg_bpm` with no coverage notion, and the registry row says so in prose only.** **CLOSED 2026-09-03** ([decisions § 1106](../architecture/decisions.md)) — on the truth, because the filing's second option cannot be written as filed. **There is no `source = 'watch_ios'`**: `SupabaseClient.kt` and `ContentView.syncRun` both put the literal `"watch"`, so the absence of `hr_coverage` is not self-addressing — a reader cannot tell an Apple Watch's workout-scoped average from a Wear run predating the key or recovered from a checkpoint that never carried it, the third case being one the registry row itself documents. **The measurement is filed, not taken, and the binding reason is structural**: watchOS hands a run to the phone through `WCSession.transferFile(_:metadata:)` and claim (6) of `scripts/check_watch_ios_source.mjs` reads both ends, so `hr_coverage` on the envelope fails the PR until `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift` lifts it — a tree no lane owned this round. Secondarily, no Xcode here, and the two cheap approximations each measure something else (`HKStatistics`' start-to-end span credits a mid-run silence as covered; ticking on `didCollectDataOf` measures HealthKit's batching). What shipped: the scope statement on `HealthKitManager.summaryAverageBPM`, where a maintainer reads it before adding an assumed `1.0`, naming all three meanings of the absence; and a `MetadataRegistryTests` guard pinning that no watchOS source writes `hr_coverage` and that the statement stays. **The `docs/backend/metadata.md` replacement text is handed to integration** — that file belonged to another lane. Swift unbuilt (see the § 888 box above); both runnable watchOS guards pass. Original filing below. HealthKit's `HKLiveWorkoutBuilder` average is workout-scoped, so the watchOS figure has a genuinely better coverage story than the Wear one did — but it records no figure, so a reader holding two runs cannot tell which claim it is looking at. Either the same key from the watchOS side (its own measurement, not an assumed 1.0 — an assumed one would be a fabricated measurement, which is worse than none) or an explicit statement that absence on a `source = 'watch_ios'` run means workout-scoped. `apps/watch_ios/` belonged to no lane this round.
- [x] **The `noindex` claim in [§ 1084](../architecture/decisions.md) is a repo-level claim, not an observation of the edge.** **SETTLED 2026-09-05** ([decisions § 1266](../architecture/decisions.md)) — the claim is confirmed and stays repo-level, now as an observation. The filing's own command could never have run: `infra/envs/preview/main.tf:38` passes `aliases = []`, so the preview distribution is given no domain name, and `getent hosts preview.threkir.com` returns not-found while `threkir.com` resolves. Measured against prod instead — `GET https://threkir.com/share/run/00000000-0000-0000-0000-000000000000` answers **HTTP 200, `text/html`, 5,666 bytes, `server: AmazonS3`, `x-cache: Error from cloudfront`, and NO `robots` meta anywhere in the body**. The `custom_error_response` deletion is unapplied and the `noindex` is not on the wire. Content is not the stale half: the shell's `last-modified` is `2026-08-15T23:30:39Z` against `web@1.4.0` published 22 minutes earlier, so the bucket is current and the distribution config trails the Terraform. What remains is the apply, tracked by the standing no-credentials item below rather than here. Original filing below. The mapping is gone from the Terraform and the guard keeps it gone, but nothing here has seen a byte off CloudFront. Settles it against preview, no prod exposure, one command: `curl -sSi 'https://preview.threkir.com/share/run/00000000-0000-0000-0000-000000000000' | head -40` — a `404` status whose body carries `<meta name="robots" content="noindex">` closes it; a 404 whose body is the plain S3 `/index.html` means the distribution still substitutes and the apply has not run. The same fetch answers the sibling question about whether `/api/coach*` now returns its JSON 404 rather than an HTML shell.
- [x] **Claim 10's derivation is per-role but the invoked set is repo-wide.** `check_infra_iam.mjs` compares BOTH deploy roles against the union of `aws lambda` verbs any workflow runs, so a verb only the production release calls would still be required on the preview role. That is correct today (release-web.yml runs the same steps down both branches) and would be wrong the moment a workflow deploys only one environment — the guard would then demand a verb preview never uses, and the fix would look like widening the role rather than narrowing the derivation. Pairing each verb with the environment its job targets is the honest version; `parseReleaseWorkflow` already reads that branch for claim 2. Not taken because it is unfalsifiable against today's workflows: there is no verb the two branches disagree about, so the stronger guard and the weaker one pass on exactly the same input. **Closed 2026-09-03** ([§ 1110](../architecture/decisions.md)). A verb is now attributed to the environments its job can assume a role for — the `AWS_DEPLOY_ROLE_ARN_<ENV>` Secrets it names — narrowed by the step's own `if:` when that reads an output of the job's env branch; a verb run under an assumed role whose environment cannot be read FAILS rather than going unclaimed, and an environment a workflow deploys to that no role grants fails too. The filing's own objection is answered rather than waved past: because no committed workflow can produce a disagreeing input, the pin is a synthetic two-branch workflow carrying a verb only the production leg runs, and four tests separate the two derivations — the verb attributes to `prod` alone, the preview role passes without it, holding it on the preview role fails as standing privilege, and a prod role lacking it fails the other way. The union derivation passes the second and fails the third. `node scripts/check_infra_iam.mjs` reports 24 properties (16 invocations attributed across 2 environments); 77 unit tests; `npm run check:script-types` clean.

### Surfaced by the #789 round 37 web lane (2026-09-03)

- [x] **The four SPA-shell head injectors are four copies of one strip pipeline, and that is how the `js/bad-tag-filter` regex survived on `main` in triplicate.** **CLOSED 2026-09-04** ([decisions § 1114](../architecture/decisions.md)). Done in exactly the shape the filing named: `share/head_splice.ts` exports `stripStaleHeadSignals(html, signals)` and `spliceIntoHead(html, headTags)`, and each of the four injectors names the signals its own head supplies — all four for entity / route / run, `['title', 'social']` for recap, so the recap page keeps whatever canonical and JSON-LD the shell itself carries. The strips are applied in a fixed internal order whatever order a caller names them in, so two injectors selecting the same set cannot produce different bytes. **Behaviour is byte-identical**, measured rather than argued: a harness ran all four injectors plus `notFoundShell` over eleven shells — the three § 1086 end-tag spellings, an overlapping `<script>` open, an attribute-carrying block, a `</head\n>`, a headless document, the empty string, a minimal head, and the real `build/index.html` — and diffed 73,830 bytes of output before and after to zero. 14 new tests pin the two steps directly, including the one the filing exists to protect: a recap injection leaves the shell's canonical and `WebSite` node standing. `countHeadSignals` lives in the same module and reads the same pattern sources, so § 1115's shell guard cannot measure something other than what the strips act on. Original filing below. `share/entity_spa_shell.ts`, `share/share_route_spa_shell.ts` and `share/share_run_spa_shell.ts` carry byte-identical title / social-meta / canonical / JSON-LD strips down to the comment wording; `share/share_recap_spa_shell.ts` carries the first two. [§ 1086](../architecture/decisions.md) had to fix the same three regexes three times, which is the cost this shape charges every time. The durable fix is one `stripStaleHeadSignals` plus one `spliceIntoHead` in `share/`, with each injector composing the steps it needs — **not** a single all-in-one: the recap head emits neither a canonical nor a JSON-LD block, so giving it the canonical/JSON-LD strips would delete the shell's own `WebSite` node from every recap share page. Ruled out as the in-round fix because a refactor inside a bug fix is what [conventions.md § Commit cadence](../architecture/conventions.md) says to split, and these are the served share-page sanitisers. Owner tree: `apps/web/src/lib/share/`.
- [x] **`routing.ts` exports three functions nothing imports, kept alive by a guard that asserts they exist.** Repo-wide, the only references to `snapToRoad`, `fetchRoute` and `fetchFullRoute` are their own definitions and `consent_guards.test.ts:215`, which requires each to exist and to issue its OSRM call through `osrmProxyFetch`. The one real consumer, `RouteBuilder.svelte`, imports `osrmProxyFetch` alone and builds its OSRM paths inline — the guard's own comment says so. So the browser-direct-coordinate-leak invariant of issue #198 is being enforced over three functions no user path reaches, the dead code has to be maintained ([§ 1087](../architecture/decisions.md) just fixed a live defect in one of them), and a future caller would adopt an untested helper. The durable fix is to delete all three and narrow the guard to `osrmProxyFetch` plus the RouteBuilder call sites it already checks; deleting exported production code was outside the scope of a round scoped to four filings. Owner tree: `apps/web/src/lib/routes/routing.ts` + `apps/web/src/lib/consent_guards.test.ts`. **CLOSED 2026-09-04 ([decisions § 1119](../architecture/decisions.md)).** The filing is accurate — verified by `command grep -rnE "\b(snapToRoad|fetchRoute|fetchFullRoute)\b"` over the whole tree, whose only web hits are the three definitions, the guard's loop and the `response_body_parse_guard` register (the Dart `snapToRoad` in `routing.dart` is a separate live implementation, not a caller). All three deleted, along with the `TrackPoint` import and the `Osrm*` interfaces that only they used; `routing.ts` is now the proxy client and nothing else. **Adoption was rejected on the merits, not on scope**: `RouteBuilder` needs a `radiuses=` cap, a per-request `AbortSignal`, a retry ladder, batching in threes and a `routeVersion` check on both sides of every `await`, so growing the helpers to carry that for one consumer is an abstraction with one call site. The invariant moved onto the live path rather than being dropped: routing.ts must export exactly ONE function and reach the network exactly ONCE under `OSRM_PROXY_BASE`, and every OSRM service path literal in `RouteBuilder.svelte` must be handed to `osrmProxyFetch` — inline or through a `const` the call is given — with no bare `fetch()` in the file carrying one, over `stripComments`ed source and behind a population assertion so the per-path loop cannot pass vacuously. **The filing's one overstatement**: the live path was not guarded by *nothing* — it had a host-constant `doesNotMatch` and a `>= 2` count of `osrmProxyFetch(` call sites — but the count is the problem, since a bare `fetch()` added beside the two existing proxy calls does not move it. Proven against five planted violations, each failing the guard on an otherwise-green tree.
- [x] **The Art 20 export hands a runner `avg_bpm` with no `hr_coverage` beside it, which is the exact confusion § 1088 just closed on the page.** **CLOSED 2026-09-04** ([decisions § 1134](../architecture/decisions.md)) — on the Edge Function rail, with one correction to the filing and the load-bearing half re-filed. **The filing's "indistinguishable" is not literally true**: the CSV's `metadata` column is `JSON.stringify(md)` verbatim, so `hr_coverage` has always ridden along inside it and a reader who opens that cell can tell a suppressed average from a strap-less run. Art 20 completeness was never at issue. What is true and worth fixing is narrower — a PROMOTED column stating a qualified number with its qualifier buried in a JSON blob two columns over misleads at the level anyone actually reads a CSV. `hr_coverage` is now a column immediately after `avg_bpm` (adjacency is the mechanism; a consumer reading by index is broken by any header change and one reading by name is unaffected either way), and `CSV_COLS`' standing "new keys go in the metadata column" rule keeps one stated exception: a key earns a column when a column ALREADY there cannot be read without it. The cell is GRADED, not passed through — `hrCoverageCell` refuses a non-number, a non-finite one and anything outside `0..1`, the same refusal `hrCoveragePercent` makes for the page, so a writer storing a percentage cannot put `85` in a column documented as a fraction; `0` renders `0` because an enabled sensor that delivered nothing is a measurement. Four new tests pin the header position, the three heart-rate shapes against the strap-less contrast, the grading, and that a refused value still rides verbatim in the `metadata` column. **The Go worker's copy is filed below and is the half most runners reach** — mobile has no EF path at all and always enqueues (§ 724). No migration and no query change was needed: `metadata` is already in `RUNS_SELECT`. Original filing below. `apps/backend/supabase/functions/export-data/render.ts:60` lists `avg_bpm` as a CSV column and no column carries `hr_coverage`. Two consequences, both on data the runner is taking to another platform: an average that covered 51 % of the run exports as a bare number with nothing saying so, and a *suppressed* average — the Wear recorder drops it below 0.5 — exports as an empty cell indistinguishable from a run recorded with no strap. The registry row already calls the key public-safe, so there is no privacy question to settle; it is a header entry and a `csvEscape(stringy(md.hr_coverage))` beside the existing line. Owner tree: `apps/backend/`.
- [x] **`runDetail.hrAvgOnly` promises per-point heart-rate samples "soon" to every user with an imported Strava run, in all seven locales.** The copy reads "A full zone distribution needs per-point samples, which the recording apps will start writing alongside GPS soon." [metadata.md](../backend/metadata.md)'s `avg_bpm` row states the current position plainly — "No mobile recorder writes per-point `bpm` yet — added when the recorder gains streaming-HR support" — so the sentence is a roadmap promise embedded in shipped UI copy with no date and no owner, shown on the commonest HR shape there is. Either the recorder work gets scheduled or the copy should state what the page has rather than what it expects to have. Seven catalogue edits, no code. Owner tree: `apps/web/src/lib/i18n/locales/`. **CLOSED 2026-09-04 ([decisions § 1121](../architecture/decisions.md)).** Recorder work cannot be scheduled from a web lane, so the second resolution was taken: the copy states the present. English now reads "A zone distribution needs a heart-rate reading at each point of the track, which this run does not carry" — checkable by the reader against the page in front of them, where "soon" was not. All seven catalogues moved in the same commit (leaving one on the old sentence is the same promise, made only to the readers of that language), each keeping its existing register, and pt-BR / pt-PT keep the `registrou` / `registou` split that makes them two catalogues. `messages_parity` (key set + `{bpm}` placeholder fidelity across all seven) and `catalogues` (the >100-string pt divergence floor) both pass. The existing Playwright pin `tests-e2e/runs/hr-zones.spec.ts` asserts the opening clause and the interpolated average, both of which survive, so no spec change was needed. Six of the seven strings are model-authored and are added to the native-review item above.
- [x] **The prerendered landing page is overwritten by the SPA fallback, so `/` ships with none of the `<head>` `seo.md` says it has.** **CLOSED 2026-09-05 ([decisions § 1268](../architecture/decisions.md) - [§ 1272](../architecture/decisions.md)).** Both prior diagnoses were half right and each hid the other. Measured with `prerender = true` and the fallback still named `index.html`: `.svelte-kit/output/prerendered/pages/index.html` IS written, at 14,873 bytes with every head signal, and `build/index.html` comes out at 5,785 - the fallback, generated after the prerendered pages and to the same name. So the ORIGINAL mechanism is real but was dormant while nothing prerendered to that path, and § 1116 ruled out a mechanism that was merely not yet reachable. Both defects are closed together because either alone is worse than neither. Shipped: `prerender = true` on `+page.ts`, `fallback: "200.html"`, the five `lambda/*/build.mjs` `spaShellPath`s, the CloudFront 403 `response_page_path`, and `scripts/check_infra_error_responses.mjs`'s `REQUIRED_MAPPING.page` - seven files, one commit, because the filename is a three-rail contract ([§ 1272](../architecture/decisions.md)) and a rail left behind fails nothing in CI and only production. Measured result: `build/index.html` 14,875 bytes with title + canonical `https://threkir.com/` + description + 8 `og:` + 4 `twitter:` + `Organization` + `WebSite`; `build/200.html` 5,784 bytes with none. Bundle budget unmoved. **Two corrections worth carrying forward.** (a) The entry's ORDERING claim - "there is no order of merge, deploy and `terraform apply` that keeps deep links working" - is true over those three acts and false once a one-off `aws s3 cp index.html 200.html` pre-seed is allowed; the three-step sequence is in [§ 1269](../architecture/decisions.md) and `docs/features/seo.md`, stated at the confidence it has (never executed against AWS) with the one thing that would falsify it named. (b) § 1167's prediction that the shell would gain a title is wrong: the shell MOVED, so `spa_shell_head_signals.test.ts` keeps `{title: 0, social: 0, canonical: 0, jsonLd: 0}` and only its filename constant changed - all four strips are still acting on nothing. Pinned by `src/lib/seo/prerendered_head_contract.test.ts` (the built artifact, widened to every prerendered page and its canonical-vs-served-path, [§ 1271](../architecture/decisions.md)) and `src/lib/seo/spa_shell_filename.test.ts` (the three rails, [§ 1272](../architecture/decisions.md)). Prior state, kept because its measurement is still the record: **STILL OPEN. The measurement is right and the named cause is WRONG — re-diagnosed 2026-09-04 ([decisions § 1116](../architecture/decisions.md)).** Original filing below. Reproduced the artifact exactly as filed: a production build's `build/index.html` carries `<title>Threkir</title>` and nothing else of the four head signals, while `build/learn.html` carries a canonical and thirteen social meta tags. But **nothing overwrites anything** — `/` is never prerendered in the first place. `.svelte-kit/output/prerendered/pages/` holds `learn.html`, `learn/`, `sitemap.xml` and `app-capabilities.json` and **no `index.html`**, and `build/index.html` has the empty body and bare `kit.start(app, element)` of a generated fallback rather than a prerendered page's hydration payload. `src/routes/+page.ts` exports no `prerender` and there is no root `+layout.ts` to set one, so `/` is a client-rendered route like `/dashboard`. **Acting on the filed cause would take the site down**: `fallback: "200.html"` alone removes `build/index.html` from the output entirely, CloudFront's `default_root_object` then 403s on the site root, that 403 maps back to `/index.html` ([§ 1022](../architecture/decisions.md)), and all five share Lambdas' `build.mjs` fail at bundle time on a missing file. **The real fix is four changes across three trees and none is optional**: (1) `export const prerender = true` in `apps/web/src/routes/+page.ts`, so a landing page exists to write; (2) `fallback: "200.html"` in `apps/web/svelte.config.js`; (3) `spaShellPath` → `build/200.html` in each of the five `apps/web/lambda/{share-badge,share-entity,share-recap,share-route,share-run}/build.mjs`; (4) `response_page_path = "/200.html"` on the 403 `custom_error_response` in `infra/modules/web-stack/main.tf`. Fewer than all four either changes nothing (1 alone — the fallback still overwrites) or breaks the site (2 alone). Not taken in this lane because it owns only (2). The observability half IS landed: `share/spa_shell_head_signals.test.ts` ([§ 1115](../architecture/decisions.md)) reads the built artifact and fails the day its head signals change, so the flip is a stated event rather than a silent one. **The four changes are right but their ORDERING is the part that was not stated, and it is why this stayed open at round 38's integration rather than being landed with everything else.** There is no order of merge, deploy and `terraform apply` that keeps deep links working, because the 403 mapping is wrong for one of the two artifact states either way. Land the code first and the bucket holds `index.html` (now the landing page) beside `200.html`, while the live 403 still points at `/index.html` — so every deep link serves the LANDING page and hydrates a `/` payload on a different URL. Apply the infra first and the 403 points at a `200.html` that does not exist yet. The safe route is a three-step operator sequence: emit `200.html` WITHOUT prerendering `/` (changes 2 and 3 only, at which point `index.html` is still the shell and nothing moves), deploy, `terraform apply` change 4, then land change 1 in a second PR. Whoever takes this should confirm each step against the edge before the next, since no lane holds AWS credentials and none of it can be verified from the repo. Note also that since [§ 1167](../architecture/decisions.md) moved the default title into the root layout, the shell carries NO title either, so step 1 is now what restores a title to the site root as well. Original filing below. Measured on a production build of `apps/web`: `build/index.html` carries `<title>Threkir</title>` from `app.html`, the CSP meta, the icons and the module preloads — no `<link rel="canonical">`, no `og:`/`twitter:`/`description` meta and no `Organization` / `WebSite` JSON-LD. `src/routes/+page.svelte` builds all of it through `SeoHead` and `+page.ts` resolves the canonical host, and a genuinely prerendered sibling proves the mechanism works — `build/learn.html` carries both a canonical and an `og:title`. The cause is `svelte.config.js`'s `fallback: "index.html"`: adapter-static writes the prerendered pages and then generates the fallback **to the same path**, so the SPA shell replaces the landing page. [seo.md](../features/seo.md)'s render map states "`/` landing | prerendered | `SeoHead.svelte` + `+page.ts` | `Organization` + `WebSite`", which is what the source intends and not what the artifact contains. Three knock-ons: the site's most important indexable page has no canonical, no description and no structured data; the five share Lambdas embed that bare shell, which is why three of the four strips in each injector ([§ 1086](../architecture/decisions.md)) remove content the shell has never contained while their comments assert "adapter-static emits a default set that would render as duplicates"; and the same shell is what CloudFront serves for every 4xx ([§ 1022](../architecture/decisions.md)), so no deep link carries a canonical either. **Not fixable inside one tree**, which is why it is filed rather than taken: naming the fallback something else (`fallback: "200.html"`) frees `index.html` for the real landing page, but the share Lambdas' `build.mjs` all read `build/index.html` by name, the CloudFront error-response mapping and default root object are `infra/`, and the strips would become live again and want their tests. Owner trees: `apps/web/svelte.config.js` + `apps/web/lambda/*/build.mjs` + `infra/modules/web-stack/`.
- [x] **Three of the four strips in each SPA-shell injector are dead code today, and their comments assert the opposite.** **CLOSED 2026-09-04** ([decisions § 1115](../architecture/decisions.md)). The measurement is confirmed on a fresh production build: `build/index.html` carries `{title: 1, social: 0, canonical: 0, jsonLd: 0}`, so only the title strip has work to do and the three others are dead today. Resolved the way the filing asked — the strips stay, the false sentence goes, and the claim is now a test rather than prose. `share/spa_shell_head_signals.test.ts` asserts the four counts against `src/app.html` unconditionally (a fallback page's `%sveltekit.head%` contributes only the hash-CSP meta and the modulepreload links, so the template alone decides them) and against `build/index.html` whenever a build is present, requiring the two to agree; a third case pins the consequence directly, that stripping the three absent signals is a no-op on the shell while stripping the title is not. It fails in BOTH directions and was proved non-vacuous by adding a canonical to `app.html` and watching two of its three cases fail. Each injector's comment now says what was measured and points at the test instead of asserting a default set the artifact has never contained. **One gap, filed below**: the artifact half is skipped in the `test-web` CI job, which does not build — wiring it into `build-web` after the build needs the workflow tree. Original filing below. Consequence of the finding above and separable from it. `injectEntityHead`, `injectShareRouteMeta` and `injectShareRunMeta` each strip a stale `<title>`, the `og:`/`twitter:`/`description` meta, a `<link rel="canonical">` and a JSON-LD block; measured against a production `build/index.html`, only the `<title>` strip has anything to act on. Each carries the sentence "adapter-static emits a default set that would render as duplicates of our per-run tags and confuse some crawlers", which is false of the current artifact. Do NOT resolve this by deleting the strips: if the landing-page finding is fixed by giving the fallback its own filename, the shell becomes the prerendered `/` and all four strips become load-bearing at once. The durable resolution is to fix the shell first and then pin what it contains — a guard reading `build/index.html` after the `build-web` job and asserting which signals the shell carries would make either state a stated one, and is what would have caught this. Owner tree: `apps/web/src/lib/share/`.
### Surfaced by the #789 round 37 mobile lane (2026-09-03)

- [x] **Run detail's whole secondary-stat grid is gated on the run having GEOMETRY, so six geometry-independent stats vanish on a track-less run.** **CLOSED 2026-09-04** ([decisions § 1124](../architecture/decisions.md)). The filing is accurate. Every cell was read and classified against what it computes: the grid holds ten cell definitions (two of which — the partial-coverage and coverage-only heart-rate cells — are mutually exclusive, so at most nine ever render together), and exactly three need geometry: elevation gain and loss, gated on `_hasElevation`, which walks `run.track`, and the grade-adjusted pace, which is `gradeAdjustedPaceSecPerKm(run.track)`. The remaining seven come off the row or the metadata bag and are no less true of a run with no track; the filing's count of six is the same set with the two coverage variants counted once. Taken in the shape the filing named rather than by widening the condition: `_secondaryStatCells` builds the list first, each cell gates on its own datum, and the section renders only when the list is non-empty — so the padding collapses instead of drawing an empty frame, and the next cell added cannot inherit the bug. **One cell had no datum test at all and needed one added**: `_showCalories` is the universal `show_calories` pref, not a value check, and `estimateRunCalories` answers 0 for a distance it cannot use and for a product past the range both platforms share — so a run stopped before it moved would have reported "0 kcal" as the estimate the moment the grid stopped being hidden. It now gates on `_showCalories && calories > 0`, which is what web has always done. **Web was read for the same shape and does not have this bug** — its `/runs/[id]` key-stats grid has no geometry gate and every optional cell carries its own `{#if}` — but reading it surfaced the inverse defect on the canonical surface, filed at the end of this document: web renders elevation unconditionally, so a track-less run is told it climbed `0 m`. Five widget tests pin it (`run_detail_secondary_stats_test.dart`, mirrored to the iOS twin): a run with a track and full data renders all eight cells it carries data for; a track-less Health Connect-shaped import renders heart rate, steps, cadence, calories and age grade while the three geometry cells stay absent; a suppressed average still reports its § 1083 coverage with no track; a run with nothing renders exactly one `StatGrid` (the primary); and calories are absent rather than `0 kcal` when the estimate is unusable. Original filing below. `run_detail_screen.dart` wraps the grid in `if (run.track.length >= 2 || _hasElevation)`, and the cells inside it are elevation gain / loss and grade-adjusted pace (which do need geometry) plus calories, steps, cadence, average heart rate, HR coverage and age grade (which do not). A Health Connect import writes `avg_bpm` with no route (`health_connect_importer.dart` averages `HEART_RATE` samples in the workout window), and a manually-added run has no track at all, so those runs render no heart rate for a reason that has nothing to do with heart rate — which is the same class of illegible absence [§ 1094](../architecture/decisions.md) just closed one layer in. Widening the condition with six more disjuncts is the wrong shape; the durable fix is to build the cell list first and gate the block on `cells.isNotEmpty`, which is also what stops the next cell inheriting the bug. Not taken there because it predates that change, affects five other cells, and restructuring a 40-line widget block belongs in its own commit. Web's `/runs/[id]` should be read for the same shape. Owner tree: `apps/mobile_android/lib/screens/run_detail_screen.dart` (+ the iOS twin), `apps/web/src/routes/runs/[id]/`.
- [x] **`provider_probe.ts`'s header still does not say why it has no Dart twin, so the decision is recorded on one side only.** [§ 1095](../architecture/decisions.md) settled that the probe rule stays single-sourced per platform and put the reasoning in `race_service.dart`'s doc comment plus a mobile source guard that reads `provider_probe.ts` directly. The reciprocal note — that Dart states the same rule natively, that the two client libraries differ in whether a failure is returned or thrown, and that `race_providers_test.dart` is what compares them — belongs in `provider_probe.ts`'s own header, which the mobile lane cannot edit. Until it is there, a reader of the web file sees a helper with no twin and no explanation, which is how [§ 641](../architecture/decisions.md)'s pairs got declared in headers and registered nowhere. Owner tree: `apps/web/src/lib/core/provider_probe.ts`. **CLOSED 2026-09-04 ([decisions § 1120](../architecture/decisions.md)).** The header now names `RaceService.isProviderConfigured` as the other statement of the rule, gives the reason the two halves cannot share a signature (`@supabase/functions-js` RETURNS `{data, error}`, so web's half is one expression over a nullable error; `functions_client` THROWS, so Dart's is control flow around the invoke, and a Dart twin would take an error value no Dart caller holds), and names `race_providers_test.dart` as what compares them. Written against the two mobile files rather than paraphrased from the ADR. One constraint is recorded with it because it is invisible from the web side: that guard drops every line beginning with `///` before asserting the file does not grade a status, so the prose has to stay in `///` lines — a `/* */` block or an in-code note would fail a Dart test a web editor has no reason to be reading. The guard's three claims were re-evaluated against the edited file (export present, nullish check intact, no `status` token surviving the `///` strip) rather than run under Flutter: this lane may not touch mobile and the worktree has no resolved pub config.
- [x] **`metadata.md`'s `hr_coverage` row still reads "— (no reader yet)", which is now wrong on mobile.** [§ 1094](../architecture/decisions.md) added `apps/mobile_android/lib/screens/run_detail_screen.dart` as a reader (and the iOS twin), and the web lane is adding its own independently. The registry's Readers column is what a future writer consults before changing the key's shape, so a row claiming no reader invites exactly the change that breaks one. The mobile lane could not edit `docs/backend/`. Owner tree: `docs/backend/metadata.md`. **CLOSED 2026-09-03 at integration**, which owns `docs/backend/` and both readers at once. The Readers cell now names the web page (§ 1088) and the mobile screen (§ 1094) and says what each renders.
- [x] **Nothing detects a widget test that starts store I/O from a fake-zone tap; three successive censuses have tried to and all three counted the wrong thing.** [§ 1097](../architecture/decisions.md) closed the third, whose discriminator ("mentions `pumpUntil`") both counted comment text as evidence and counted the two correctly-fixed files as unconverted because their right lever is `debugInProgressSettled`. The property that matters is dynamic — did this tap start a write nobody waited for — and no grep over helper names can see it. Two instruments could: run the suite with the store's `notifyListeners` and `serialiseStoreWrite` instrumented to log the zone each write was queued from and whether the temp directory outlived it (§ 991's technique, never actually run because it needs a full-suite pass), or make `writeJsonAtomic` REPORT rather than swallow a write into a deleted directory, which turns the race into a named failure at the moment it happens instead of a silent one. The second is cheaper and does not need a special run. Owner tree: `apps/mobile_android/lib/`, `packages/core_models/lib/src/store_write_chain.dart`. **CLOSED 2026-09-04 ([decisions § 1129](../architecture/decisions.md) + [§ 1131](../architecture/decisions.md)).** Built as the dynamic instrument rather than a fourth grep. `serialiseStoreWrite` now accepts a test-only `StoreWriteObserver` — id, key and the `StackTrace.current` of the queueing call on the way in, settlement reported from the same continuation that completes the queueing caller's own future, and in release one static null test per operation with no stack captured and no id allocated. `apps/mobile_android/test/flutter_test_config.dart` installs it for the whole suite and fails any test that ends with an operation still open, naming the store directory and the call site. Run over the 107 test files that can reach the chain it found **17 tests across 7 files**, including one (`gear_rotations_screen_test.dart`) this filing's own § 1097 hand-read and cleared. All 17 are fixed (§ 1131) rather than exempted, because the property is a race — three identical runs reported 17/17/16 — which rules out the staleness-checked exemption register this repo would otherwise want. The second instrument the filing named (make `writeJsonAtomic` report) was not needed separately: `writeStringAtomic` already rethrows a create into a deleted directory, and the reason that error is unattributable is the zone it is delivered on, which is what § 1130 fixes.
- [x] **The bounded settle cannot report when the awaiting zone is the fake one, which is row 5 of § 1093's own table.** `storeWritesSettled` arms its watchdog on `Zone.root` so the timer fires regardless of the caller's zone, but the error is DELIVERED through the awaiting zone's continuation — so a fake-zone `await` with no pump behind it still hangs to the runner's timeout, exactly as before. The measured case is a write queued from the fake zone and settled from the fake zone; a test in that state is wedged for a second reason anyway, which is why it was left. Closing it needs the report to reach the runner rather than the awaiter — `Zone.root.run` plus a `FlutterError.reportError`, or the test-package's own `registerException` — neither of which `core_models` can reach without a dependency it does not have and should not take. The honest version may be a thin `@visibleForTesting` reporter injected from the Flutter side. Owner tree: `packages/core_models/lib/src/store_write_chain.dart`, `apps/mobile_android/lib/`. **CLOSED 2026-09-04 ([decisions § 1130](../architecture/decisions.md)).** Premise verified first: at `c4b6137ec` a fake-zone write awaited from the fake zone with no pump prints `TimeoutException after 0:00:25.000000` and nothing else. The fix direction the filing named does NOT work and the measurement says so — in a wedged body a root-zone timer's `stderr.writeln` / `print` / `debugPrint` all surface immediately while `FlutterError.reportError` produces no output at all, because the test binding parks it in `_pendingExceptionDetails` until the body ends. So the diagnosis goes to `debugStoreWritesSettledSink`, the thin reporter injected from the Flutter side that the filing offered as the honest version, and it prints. Both deliveries are kept: completing the future serves an awaiter that can be resumed, the sink serves the runner when it cannot, and which happened is decided one root-zone second after the bound rather than in advance — § 1093 having measured that no property of the zone says. Both directions are pinned in `store_writes_settled_zone_test.dart`.
### Surfaced by the #789 round 37 backend / SQL lane (2026-09-03)

- [x] **`api_client`'s `clipTrackForUser` has been calling a function `authenticated` may not execute since 2026-05-21, and an architecture guard pins its shape.** **CLOSED 2026-09-04** ([decisions § 1142](../architecture/decisions.md)). Every premise reproduced: the ACL on the reset local stack is `{postgres=X/postgres,service_role=X/postgres}`, the api_client call site was the only `.rpc('clip_track_for_user')` in the repo, and nothing in either mobile `lib/` reached it. Of the filing's two options the re-grant is the one that was never available — `20270521_001`'s own header is the argument against it (the function trims LEADING in-zone points, so a three-point probe answers one bit and ~40 calls binary-search a victim's home to metre precision, on a PostgREST RPC no Edge Function rate limit covers), and a mobile surface wanting the method would be asking for the oracle back. So the method is deleted. The privacy-zone invariant is untouched: mobile's non-owner run surfaces already clip through `fetchClippedTrackForRun` (the `clip-public-track` Edge Function, service-role inside), and runs now have ONE client clipping path instead of one live and one dead. The three fail-closed assertions go with it; the fourth was rewritten rather than deleted — `public_route_screen must not call clipTrackForUser` would have become an assertion about a method nobody has, so it now forbids the run-bound `fetchClippedTrackForRun`, which is the claim it was making. Mirrored byte-for-byte to the iOS twin; `flutter test test/architecture_guards_test.dart` 234/234. **A second dead RPC caller in the same file was found by the § 1139 guard and deleted with it**: `ApiClient.publishPlanAsTemplate` calls `publish_plan_as_template`, which no migration has ever created — `TrainingService`'s copy was rewritten as the multi-table INSERT and says so, the api_client one was left behind. Both had zero callers. Two things the filing could not have known are filed below: `apps/mobile_android/CLAUDE.md`'s public_run_screen line and two `lib/` comments still name the deleted method. Original filing below. `20270521_001_heatmap_shadow_hidden_and_completion_race.sql` ends with `revoke execute on function clip_track_for_user(uuid, jsonb) from authenticated;` and no later migration re-grants it — measured on the local image, the ACL is `{postgres=X, service_role=X}`. `packages/api_client/lib/src/api_client.dart:4564` is the ONLY `.rpc('clip_track_for_user')` call anywhere in the repo (web reaches the clipping through `clip-public-track` and `clip_route_for_viewer` instead), it runs under the caller's own JWT, and it would now get a 42501 that its documented fail-closed `catch` swallows into `[]` — a silently empty track rather than an error. Nothing in `apps/mobile_android/lib/` calls it: every remaining mention is a comment saying *not* to (`global_segments_screen.dart`, `public_route_screen.dart`), and `architecture_guards_test.dart` carries three assertions pinning the method's fail-closed shape and one forbidding `public_route_screen` from using it. So this is guarded dead code whose grant was withdrawn four months ago. Not a privacy hole — the failure direction is closed — but the guard is protecting a method that can no longer work, and the next reader will believe it does. The durable fix is to delete the method and its guards (the twin invariant makes it two trees), or, if a mobile surface is meant to have it, to re-grant `authenticated` by name in a migration that says why. Owner trees: `packages/api_client/`, `apps/mobile_android/test/` + the iOS twin — not this lane's.
- [x] **The stated-grant registry is vacuous on the image CI runs, and only a guard that reads migration TEXT would bite on both.** **CLOSED 2026-09-04** ([decisions § 1139](../architecture/decisions.md) + [§ 1141](../architecture/decisions.md)). Built as filed — `apps/backend/scripts/check_stated_function_grants.mjs`, its `--test` file (28 cases) and a step in the `pgtap-rls` job, each check printing its own `::error::` per the bundled-job rule; `node scripts/check_ci_diagnostics.mjs` passes. **But the filing understates the problem by one image.** Assertions (7)-(8) joined `pg_get_function_identity_arguments(p.oid) = t.args`, which renders a named parameter as `p_fundraiser_id uuid` against a registry that says `uuid`, so the join matched ZERO routines and both assertions aggregated over an empty set — vacuous on the workstation too, not only on CI. Proved with a known-bad row (`fundraiser_feed`/`service_role`, which holds no such grant): under the old join the assertion reports nothing, under `oidvectortypes(p.proargtypes)` it reports the violation. Fixed, plus a new assertion (9) as the control the pair never had — every registered routine must resolve to exactly one function in `public`. **The guard is bigger than the filing sized it, in the one direction that matters:** the obligation is DERIVED from the call sites (client trees owe `authenticated`, Edge Functions + Go worker owe `service_role`) rather than registered, so it needs no inventory to be kept by hand. It found three real violations on its first run — §§ 1140, 1142. It refuses rather than reporting clean on: no migrations, no registry rows, a caller tree with no source files, an `alter default privileges … on functions`, or a non-literal callee. Original filing below. `anon_execute_registry_test.sql` gained assertions (7)-(8) with [§ 1098](../architecture/decisions.md): each routine a named role must call holds EXECUTE, through an ACL entry naming that role rather than through PUBLIC or a NULL `proacl`. On the workstation image that is a real check, because `proacl` there is exactly `{postgres}` plus what a migration stated. On Supabase Cloud and CI's pinned 2.84.2 the `alter default privileges` puts an anon / authenticated / service_role entry on every new function BY NAME, so both assertions hold whether or not any migration said anything — the registry cannot tell a stated grant from an inherited one on the only image the PR gate runs. The durable fix is the shape `check_migration_function_revoke_noop.mjs` already has for the negative direction: replay the migration text per function signature (create / drop / grant / revoke in order), derive the stated ACL, and fail when a registered (routine, role) pair is not in it. That reads no catalogue, so it bites on every image and in CI. Sized: one `.mjs` under `apps/backend/scripts/`, its own `--test` file, and a step in the `pgtap-rls` job — which makes that job run a SECOND guard, so every one of its check steps then owes its own `::error::` per the rule `check_ci_diagnostics.mjs` enforces. Owner tree: `apps/backend/scripts/` + `.github/workflows/ci.yml`.
- [x] **`fundraiser_feed` is one image change away from the failure `fundraiser_totals` just had, and nothing would say so.** **CLOSED 2026-09-04** ([decisions § 1139](../architecture/decisions.md)), by the guard filed above rather than by a registry row. The feed is still not granted `service_role` — that judgement stands — but the inventory it rests on is no longer taken once: `check_stated_function_grants.mjs` derives the caller set on every run from `apps/backend/supabase/functions` and `apps/job_worker`, so the PR that adds a server-side reader of `fundraiser_feed` fails on that PR, naming the routine, the role and the calling file. A registry row would have been the weaker fix: it states today's answer without noticing tomorrow's caller. **The filing's worry was not hypothetical and was already realised elsewhere** — the same derivation found `host_can_take_payment`, called as service_role by both checkout Edge Functions with no stated grant, live on the money path ([§ 1140](../architecture/decisions.md), migration `20270708000001`); § 1098's hand inventory missed it because both call sites put the routine name on the line after `.rpc(`. Original filing below. [§ 1098](../architecture/decisions.md) deliberately granted only `fundraiser_totals` to `service_role`, because the feed has no service_role caller and granting one for symmetry is the sweep that entry refused. That judgement stands, but it rests on a caller inventory taken once: the two functions are the same page's pair, `20270630000001` replaced them in one statement block, and `20270213_001` gave both the identical `revoke all … from public` + `grant … to anon, authenticated`. The moment any server-side code reads the feed — an operator worklist, a receipt mailer, a Go-worker job — it works on Cloud and 42501s on the workstation, which is exactly the diagnosis this round spent a lane on. There is no guard for that direction: assertion (7) of `anon_execute_registry_test.sql` only checks routines someone REGISTERED. The durable fix is the migration-text guard filed above plus a registry entry per routine, so an unstated grant a caller depends on fails the PR that adds the caller rather than the round after. Owner tree: `apps/backend/scripts/` + `apps/backend/supabase/tests/`.

### Surfaced by the #789 round 37 watch lane (2026-09-03)

- [x] **`apps/watch_ios` can measure heart-rate coverage honestly, and the thing blocking it is the WCSession envelope guard, not HealthKit.** CLOSED 2026-09-04 ([decisions § 1156](../architecture/decisions.md)). Both premises re-verified and both hold. The measurement is built: coverage advances on `WorkoutManager`'s existing 1 s `.recording` tick against the AGE of the newest sample's own `mostRecentQuantityDateInterval().end`, with Wear's figures verbatim (0.5 threshold, 30 s freshness window) so the two clients mean one thing by one column. **The blocker still blocks and was not worked around:** claim (6) reads both ends of the envelope, `syncRunDirect` is `#if DEBUG`, and relaxing the guard was never an option — it exists because Apple-Watch runs once reached the phone with no `activity_type` exactly that way. So the measurement is spent on the OTHER half of § 1083's grading, which needs no envelope at all: `avg_bpm` is now SUPPRESSED below 0.5 coverage, and suppression reaches the row by omitting a key that already exists. That half was the one worth having first — a mean over three minutes of a twelve-hour run saved as the run's average is a wrong number reaching run detail, the coach context and the export, where a missing coverage figure is only an unanswered question. Every unusable input keeps today's behaviour (`coveredSeconds` nil until the `HKWorkoutSession` actually starts — unmeasured, not zero; non-finite and non-positive clocks pass the mean through), because on an uncompilable sensor path a measurement that failed toward zero would delete a good `avg_bpm` on every run rather than on the runs it is about. The pure arithmetic (`HeartRateCoverage.creditedStep` / `.claim`) is separated from the accumulator precisely so the suite can exercise it — a real `HKWorkoutSession` cannot be constructed in the test host — and 11 cases cover it. Nothing is added to the Xcode project. **UNBUILT: no Xcode on this workstation**; both runnable guards pass and neither compiles anything. The remaining envelope work is re-filed below. Original filing follows. Filed closing the coverage box ([decisions § 1106](../architecture/decisions.md)). HealthKit does expose what a real measurement needs: `HKStatistics.mostRecentQuantityDateInterval()` gives the SAMPLE's own timestamp inside `didCollectDataOf`, so `WorkoutManager`'s existing 1 s `.recording` tick can accumulate active milliseconds against sample age exactly as Wear's `advanceHrCoverage` does — and better, because it grades the sensor rather than HealthKit's delivery batching. The exact answer is a post-`finishWorkout` sample query over the workout interval, which needs no freshness heuristic at all. **Neither can land from a watch-only lane**: the finished run reaches the phone through `WCSession.transferFile(_:metadata:)`, and claim (6) of `scripts/check_watch_ios_source.mjs` reads both ends of that envelope, so an `hr_coverage` key the watch sends fails the PR until `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift` lifts it out — that lift is one line beside the existing `avg_bpm` one, in a tree this lane does not own. Secondary blocker: no Xcode on this workstation, and an accumulator that silently reads nil would write `hr_coverage: 0.0` on every watchOS run, which is the fabricated measurement the original filing rules out — so it wants a simulator or device run, not just a compile. Wants one change spanning `apps/watch_ios/` + `apps/mobile_ios/ios/Runner/` + the `hr_coverage` registry row's Writers column. Owner tree: `apps/watch_ios/WatchApp/HealthKitManager.swift`, `apps/watch_ios/WatchApp/ContentView.swift`, `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift`.
- [x] **The crash-recovery prompt's "Save it" is silent while the run queue is unreadable, and the prompt is not re-offered when the read recovers.** CLOSED 2026-09-04 ([decisions § 1154](../architecture/decisions.md)). Both halves, together, because (b) is what makes (a) reachable on a cold start. (a) The takeover now states `sync_queue_unreadable` in the warning colour and DISABLES "Save it" while it stands — no new string, so no locale work, as the filing said. Disabled rather than tappable-and-inert: an affordance that does nothing reads as a broken app rather than as a fault that will pass, and it does pass, because `observeQueue` retries the read on its own backoff and the chip re-enables when it succeeds. **Discard stays live**, which the filing did not settle: it is the only exit from a takeover with no Start button, and disabling it would strand a runner who wants to record now behind a corrupt file. (b) `onQueueBecameReadable` re-takes the grade, hooked into BOTH sites that clear the flag — the retrying stream and the drain a runner's own retry goes through — because the file has two readers and hooking one leaves the other clearing the flag while the prompt stays withheld. Gated on the transition so an ordinary per-save emission does not re-read the checkpoint store, and skipped while a prompt is already up. **A second defect fell out of measuring the screen:** the takeover was a non-scrolling Column whose title + distance + two 52 dp chips already exceed the ~152 dp a 192 dp round watch leaves inside its padding, so **Discard was falling off the bottom edge with no way to reach it** before this change added a line — it now scrolls, rotary-wired, centred while it fits. `RecoveryPromptDisclosureTest` (5 source-grep assertions, including a both-readers count so a third clear site cannot be added without a third re-raise); each assertion confirmed to discriminate by deleting the line it guards. Verified: `./gradlew assembleDebug testDebugUnitTest --no-daemon`, 733 tests, BUILD SUCCESSFUL. Original filing follows. Residual from [§ 1107](../architecture/decisions.md), which made the grade fail closed but left the display decision. Two halves of one gap. (a) `recoverCheckpoint` now grades `Ignore` on an unreadable queue and returns without clearing anything — correct for the data, but the runner taps "Save it" and nothing visibly happens, because the recovery prompt is a full-screen takeover in `PreRunScreen` with no failure line and `syncError` renders on `PostRunScreen`. (b) `checkRecovery` runs once from `init`, so a queue that becomes readable later in the same process never re-raises the prompt it withheld — the checkpoint survives to the next launch, which is correct but slower than it needs to be. Both want the same small thing: the recovery prompt disclosing the unreadable queue and disabling "Save it" while it stands, using the `sync_queue_unreadable` string that already exists in all seven Wear catalogues, so no new locale work. Not taken because it is a display decision on the one screen § 1104 was deliberately careful with, and because the prompt is currently never raised in the cold-start case at all — closing (b) is what makes (a)'s disclosure reachable, so they must land together. Owner tree: `apps/watch_wear/android/app/src/main/kotlin/com/runapp/watchwear/ui/RunWatchApp.kt` + `RunViewModel.kt`.
- [x] **`docs/backend/metadata.md`'s `hr_coverage` row tells readers the absence means Apple Watch, and `source` cannot support that.** Filed rather than fixed because `docs/backend/` belonged to another lane this round; the replacement text is written and ready to apply ([decisions § 1106](../architecture/decisions.md)). In that row's Notes, **replace** the sentence `Wear OS only today — \`apps/watch_ios\` reads HealthKit's workout-scoped average and has a different coverage question.` with: `**Wear OS only, and the absence is three-way ambiguous — read this before inferring anything from a missing key.** \`apps/watch_ios\` writes no \`hr_coverage\`: its \`avg_bpm\` is \`HKLiveWorkoutBuilder\`'s average over the \`HKWorkoutSession\`, which holds the sensor for the life of the session rather than the life of the foreground app, so the failure this key measures against does not arise the same way — and an assumed \`1.0\` would be a fabricated measurement rather than a missing one (decisions § 1106). But **both watch clients write \`source = 'watch'\`** — there is no \`watch_ios\` value — so a \`source = 'watch'\` run with no \`hr_coverage\` is one of three things and nothing on the row distinguishes them: (1) an Apple Watch run, workout-scoped; (2) a Wear run recorded by a build predating this key; (3) a Wear run recovered from a checkpoint that never carried it. A reader must not read the absence as evidence about coverage in either direction. Measuring it honestly on watchOS is filed — it needs \`mostRecentQuantityDateInterval()\` on the recorder's own tick, or a post-\`finishWorkout\` sample query over the workout interval, plus a matching lift in \`apps/mobile_ios/ios/Runner/WatchIngestBridge.swift\`, because claim (6) of \`scripts/check_watch_ios_source.mjs\` reads both ends of the WCSession envelope.` The Writers column stays Wear-only. Owner tree: `docs/backend/metadata.md`. **CLOSED 2026-09-03 at integration.** The watch lane's replacement prose was applied verbatim: the absence is stated as three-way ambiguous, `source = 'watch'` is named as the reason the row cannot distinguish the cases, and the honest-measurement path is pointed at claim (6) of `check_watch_ios_source.mjs`.
- [x] **`apps/watch_ios`'s `hr_coverage` guard is written but has never been compiled.** CLOSED 2026-09-04 ([decisions § 1156](../architecture/decisions.md)) — closed as ADDRESSED, not as compiled: there is still no Swift compiler on this box and CI's `Test watchOS app (Swift)` job remains what settles it, so this joins the § 888 population rather than leaving its own standing box. The guard did need changing, in the direction the round-38 brief predicted. Its `hr_coverage`-writers assertion is still TRUE and is kept — the key is still not sent — but the method asserted the watch makes no coverage CLAIM, which is a different sentence from writing no coverage KEY, and the watch now measures one; a guard whose name contradicts the shipped behaviour is a guard nobody can read, so it is renamed to `testWatchOSWritesNoCoverageKeyAndGradesTheAverageItDoesWrite`. It also gained two real assertions rather than only a rename: the suppression threshold must exist (delete it and the measurement silently stops having any effect at all), and `WorkoutManager.swift` must contain NO reference to `summaryAverageBPM`, so the ungraded mean cannot reach the finished run or the 15 s checkpoint a crash recovers from. It still adds no file to the Xcode project and still reuses only that class's own private helpers (`repoRoot`, `stripComments`) and Foundation. Both runnable guards re-run here and pass: `node scripts/check_watch_ios_source.mjs` (15 claims), `bash apps/watch_ios/scripts/check_xcstrings_parity.sh` (56 strings x 7 locales); neither compiles anything. Original filing follows. Filed with the change it guards ([decisions § 1106](../architecture/decisions.md)). `MetadataRegistryTests.testWatchOSMakesNoCoverageClaimAndSaysSoWhereTheAverageIsProduced` reuses only that class's existing private helpers and Foundation, and adds no file to the Xcode project — so the compile risk is low but it is not zero, and no Swift compiler has seen it. Both runnable watchOS guards pass here (`node scripts/check_watch_ios_source.mjs`, `bash apps/watch_ios/scripts/check_xcstrings_parity.sh`), and neither compiles anything. This joins the population in the § 888 box above rather than replacing it: the first CI `Test watchOS app (Swift)` run on this branch settles it, and if it is red it is this method. Owner tree: `apps/watch_ios/WatchAppTests/MetadataRegistryTests.swift`.
### Surfaced by the #789 round 37 infra / Go lane (2026-09-03)

- [x] **`export_blob_reap` has a handler and no way to be enqueued, and the SQL that finishes it is four statements.** [§ 1112](../architecture/decisions.md) landed `handler_export_blob_reap.go` + `ListStorageObjectsWithMeta` / `DeleteStorageObjects` + 15 tests, and deliberately did NOT add the `Worker.dispatch` case: `internal/worker_dispatch_coverage_test.go` fails a `case` for a kind `jobs_kind_chk` forbids, and that CHECK is a migration the infra lane could not land. All four pieces must land in ONE commit — the dispatch case without the CHECK fails that test in the orphaned direction, the CHECK without the case fails it in the unrouted direction. **(1)** The migration, following `20270603_001`'s own shape (drop, re-add `not valid`, `validate` — the two-step is what keeps the scan off the queue table's lock class):
  ```sql
  alter table public.jobs drop constraint jobs_kind_chk;
  alter table public.jobs
    add constraint jobs_kind_chk
    check (
      kind in (
        'map_match', 'token_refresh', 'strava_event', 'photo_process',
        'notification_email', 'lifecycle_email', 'safety_email', 'web_push',
        'weekly_digest', 'native_push', 'lifecycle_drip', 'route_photo_process',
        'club_photo_process', 'safety_sms', 'data_export', 'export_blob_reap'
      )
    )
    not valid;
  alter table public.jobs validate constraint jobs_kind_chk;
  ```
  **(2)** The enqueue, a singleton so a night the worker was down cannot stack a backlog of identical sweeps:
  ```sql
  create or replace function enqueue_export_blob_reap()
  returns void
  language sql
  security definer
  set search_path = public
  as $$
    insert into public.jobs (kind, payload, max_attempts)
    select 'export_blob_reap', '{}'::jsonb, 3
    where not exists (
      select 1 from public.jobs j
      where j.kind = 'export_blob_reap' and j.status in ('queued', 'running')
    );
  $$;
  revoke execute on function public.enqueue_export_blob_reap() from public, anon, authenticated;
  grant execute on function public.enqueue_export_blob_reap() to service_role;
  ```
  **(3)** The schedule, ten minutes AHEAD of `cleanup-stale-export-blobs`'s `'23 4 * * *'` so the erasure runs before the row delete that would hide the worklist from it:
  ```sql
  select cron.schedule(
    'enqueue-export-blob-reap',
    '13 4 * * *',
    $$select public.enqueue_export_blob_reap()$$
  );
  ```
  **(4)** The Go line, in `dispatch`'s switch in `apps/job_worker/internal/worker.go`: `case "export_blob_reap": return w.handleExportBlobReap(ctx, job)` — plus a `'export_blob_reap'` case in `apps/backend/supabase/tests/jobs_kind_allowlist_test.sql`. **One open design question for whoever takes it**: `cleanup_stale_export_blobs()` also sweeps `runs` objects matching `%/exports/%`, and the reaper reaches those only under a per-user prefix (`{"bucket":"runs","prefix":"<uid>/exports/"}`) because the Storage list API is non-recursive by default and the handler's walk descends folder by folder — a whole-bucket `runs` walk is O(users) list calls. Either enqueue one job per user with a live export archive, or accept that the legacy leg keeps the row-delete-only behaviour and say so. Owner tree: `apps/backend/supabase/migrations/` + one line in `apps/job_worker/internal/worker.go`. **CLOSED 2026-09-04 ([decisions § 1144](../architecture/decisions.md), [§ 1145](../architecture/decisions.md), [§ 1146](../architecture/decisions.md)).** Migration `20270708000010_route_export_blob_reap.sql` widens `jobs_kind_chk` through the `NOT VALID` + `VALIDATE` two-step, adds the singleton `enqueue_export_blob_reap()` and schedules it at `'13 4 * * *'`; `Worker.dispatch` gained the one line, in the same commit. The coverage test was measured in both directions on the branch — the dispatch line alone reports `export_blob_reap` orphaned, the migration alone reports it unrouted. Two departures from the filing's SQL, both recorded in § 1144: the enqueue calls `expire_stale_export_jobs()` first, so no subject is handed a path to an archive the reap is about to erase during the ten minutes before the sweep would have expired it; and the open design question is answered as "the legacy `runs` leg keeps row-delete-only behaviour", because nothing has written that prefix since `20270602_001` moved both producers to the `exports` bucket and the nightly sweep has been deleting those rows since `20260720_001` — so a per-user walk would be O(users) list calls over a set the Storage list API cannot see. Closing it also surfaced two defects in the shipped handler, both fixed here in their own commits: the storage walk returned paths no object could match for exactly the two prefix shapes the reaper uses (§ 1145 — the nightly whole-bucket sweep listed nothing and would have logged success), and the reap selected on AGE alone in whatever bucket the payload named, so a `runs` job erased every GPS track past the window (§ 1146). The pgtap `lives_ok` case the filing also asks for is filed below: `apps/backend/supabase/tests/` was another lane's tree this round.
- [ ] **The export bytes already orphaned by past sweeps cannot be reached by any Storage-API reaper, including the one just built.** [§ 1049](../architecture/decisions.md) counted 74 files across every bucket against 0 rows in `storage.objects`, including 20 export archives stamped nine days earlier, and measured that `sha256` still matches. [§ 1112](../architecture/decisions.md)'s reaper lists the bucket to derive its worklist, and the list API reads `storage.objects` — the rows that are gone. So every byte a previous sweep orphaned is invisible to it by construction, and will stay on the backend forever. Erasing them needs something outside the Storage API: an S3 lifecycle rule on the storage bucket keyed on the object prefix, or a one-off operator pass with direct backend access. Neither is a code change in this repo, and both are Art 17 relevant — this is the residue of a retention path that has never erased anything. Not filed as blocked on the reaper: the reaper stops the pile GROWING, and this is the pile. Owner tree: an operator + `docs/ops/`. **The `docs/` half is discharged as of 2026-09-07** — `retention.md`'s Art 20 row already carries it per § 1173, so `docs/ops/` comes off the owner line and what remains is purely the operator action.
- [x] **`gofmt` is not gated anywhere, and seven files under `apps/job_worker/` are unformatted on `main`.** The `test-worker` and `test-graph-cycle` jobs in `ci.yml` run `go vet ./...` + `go test ./...` and nothing else; `command grep -rn "gofmt" .github/` finds no invocation. Measured on `main`: `gofmt -l .` from `apps/job_worker` lists `internal/handler_photo_process_test.go`, `internal/livehub/privacy_test.go`, `internal/livehub/server_test.go`, `internal/mailer_test.go`, `internal/stravahook/server.go`, `internal/stravahook/server_test.go` and `internal/worker.go` — seven files, all struct-field alignment, all invisible to `vet`. The cost is not the whitespace: it is that a diff touching one of them re-aligns a block the author did not edit, so the review reads as a bigger change than it is, and the same mechanism as the Terraform `fmt` gap [§ 1111](../architecture/decisions.md) closed one directory over. The durable fix is a `gofmt -l` step in each Go job that fails on a non-empty list (`if [ -n "$(gofmt -l .)" ]; then …; exit 1; fi`, with its own `::error::` per § 764 since both jobs would then run a second check), landed in the same commit as the seven-file reformat. Not taken here: `ci.yml` is another lane's file this round, and reformatting files four other lanes are editing concurrently is the one change guaranteed to conflict. Owner tree: `.github/workflows/ci.yml` + `apps/job_worker/`. **Reformat half done 2026-09-04 ([decisions § 1147](../architecture/decisions.md)); the box closes when the CI gate lands.** `gofmt -l .` was re-measured on `main` rather than trusted and listed those seven files exactly; they are formatted and `gofmt -l .` is now empty in both Go trees (`apps/graph_cycle` was already clean), in a commit of its own. The `gofmt -l` step in `test-worker` + `test-graph-cycle` is the round-38 infra/CI lane's — `.github/` was not this lane's tree. The split is safe only in this direction: a reformat with no gate leaves the tree correct and unenforced, while a gate with no reformat fails every Go job on `main` at once. **CLOSED 2026-09-05 ([decisions § 1150](../architecture/decisions.md)).** Both halves have landed and the box was simply never ticked: the reformat went in round 38 under § 1147, and the `gofmt -l` gate in `test-worker` + `test-graph-cycle` went in the SAME round under § 1150 — the filing's "the box closes when the CI gate lands" was written before that lane's own commit. Re-measured on `8a414ff79` rather than trusted: `GOTOOLCHAIN=auto gofmt -l .` is EMPTY in `apps/job_worker` and in `apps/graph_cycle`, so the gate that § 1150 says "ships red" is green as of this commit. Both jobs' three check steps each print their own `::error::`, which is what § 764 asks of a job that runs a second check.

- [x] **Nothing in `terraform.yml` can block a merge, because the required check is an aggregator in another workflow.** **CLOSED 2026-09-04** ([decisions § 1149](../architecture/decisions.md)). **The filing was right in every particular** — re-verified: the gate `needs:` 32 jobs, all 32 are keys of `ci.yml`, and `ci.yml` runs no `terraform` command. Neither candidate fix in the filing was taken. Naming the three contexts in branch protection puts a path-filtered required check on every PR (it never starts on a web-only diff, so it never reports) and lives in a setting no lane can read, so its removal would leave no trace in the repo. Moving the three job bodies into `ci.yml` would have carried the `stack:` matrix with them, and `scripts/check_infra_coverage.mjs` reads that matrix and the `terraform fmt` invocations out of `terraform.yml` by path — the fix would have broken the § 890 mechanism it depends on. Instead `terraform.yml` is now `on: workflow_call` only and `ci.yml` CALLS it from a `terraform` job that is in the gate's `needs:` list. A called workflow's job results fan into the calling job, so that one entry covers every job the file gains later; `check_ci_diagnostics.mjs` rule 3 now counts 33 jobs and fails if the entry is dropped (demonstrated); and actionlint, already in the gated `workflow-lint` job, fails `ci.yml` if `terraform.yml` loses its `workflow_call` trigger or is deleted (both demonstrated) — so the wiring cannot be severed from either end without a red. The path filter moved onto a new `infra` output of the `changes` job, and `pull-requests: write` went with the triggers (no step posts anything). What is still NOT verified from here is the branch-protection setting itself; the claim this closes is the narrower one, that the Terraform checks are inside the context branch protection is documented to require. Original filing below. The `CI gate` job in `ci.yml` `needs:` 32 jobs and none of them is from `terraform.yml` — nor could one be, since `needs:` is scoped to its own workflow. `ci.yml` runs no `terraform` command at all (its three matches are prose in comments, which is what [§ 1021](../architecture/decisions.md) already measured). So `fmt (whole tree)`, `validate (<stack>)` and `Trivy IaC scan` are advisory rows unless branch protection separately requires them, which no lane can read and which this repo's own CLAUDE.md contradicts: "the single required **CI gate** status check". This is [§ 764](../architecture/decisions.md)'s hazard exactly — "a job outside the list has a red that blocks nothing and reads as one more green row among thirty" — one workflow over, and it is worse here because `terraform.yml` is also path-filtered to `infra/**`, so on most PRs the rows are not merely non-blocking but absent. `infra-guards` (the three `.mjs` guards) IS in the gate and does block, so what is unprotected is precisely `fmt`, `validate` and Trivy. Two candidate fixes, and the choice is the owner's: add the terraform jobs' equivalents to `ci.yml` behind the existing `changes` filter so the gate can wait for them, or add them to branch protection by name and accept that a path-filtered required check blocks every PR that does not touch `infra/`. Owner tree: `.github/workflows/` + branch protection.
- [x] **`terraform validate` passes on both env roots and on no module — re-run each round.** Standing item, re-filed rather than retired because its reason has not changed. **Re-run 2026-09-05 (round 40)** ([decisions § 1267](../architecture/decisions.md)): `terraform init -backend=false` + `validate` in all FIVE root stacks — `infra/bootstrap`, `infra/dns`, `infra/github-oidc`, `infra/envs/prod`, `infra/envs/preview` — every one `Success!`; `terraform fmt -check -recursive infra` exit 0; Terraform v1.13.0. The module still refuses a standalone `validate` (`Provider configuration not present` for `aws_wafv2_web_acl.coach`) and this round settles that it is **not fixable and should stop being re-asked**: `configuration_aliases = [aws.us_east_1]` DECLARES a provider the caller supplies, so a module carrying one cannot be a root module by construction. The two ways out are both worse than the state — a `provider` block inside the module is the anti-pattern that makes it uncallable with `count`/`for_each` and breaks both env callers, and a throwaway wrapper root is a second copy of `infra/envs/prod`. **Its transitive coverage was widened from asserted to measured**: [§ 1111](../architecture/decisions.md) planted one undeclared-variable reference in `waf.tf` and read `envs/prod`. This round planted one in `waf.tf`, `alarms.tf` AND `outputs.tf` in turn and read BOTH env roots — six runs, all six exit 1 with `Reference to undeclared input variable`, all six restored clean. Also measured and deliberately NOT changed: a standalone `init` on the module drops an untracked `.terraform.lock.hcl` that `infra/.gitignore` does not cover, because the five ROOT stacks' lock files are committed (`git ls-files infra/`) and ignoring the name would stop tracking theirs. Delete the stray one by hand. Still no `plan` behind §§ 1021-1024, 1084-1085, 1111 — no lane holds AWS credentials — and the unapplied state is now observed rather than inferred, see the § 1084 entry above. Owner tree: `infra/`. 2026-09-03 (round 37): `terraform init -backend=false` + `validate` in `infra/envs/prod` and `infra/envs/preview` both `Success!`; `terraform fmt -check -recursive infra` clean; the module still refuses a standalone `validate` and is now MEASURED as transitively validated ([§ 1111](../architecture/decisions.md)). What remains uncovered is unchanged and is not a `validate` question: no lane holds AWS credentials, so §§ 1021-1024, 1084-1085 and now 1111 have never been `plan`ned against real state. The unapplied state is now observable rather than assumed — prod still answers `/share/run/<missing>` with the S3 SPA shell at 200 (see the § 1084 entry above), which is what § 1084's `custom_error_response` deletion would stop. Apply preview first; the round-37 changes touch a workflow and a guard, not a resource, so only §§ 1021-1024 + 1084-1085 carry plan risk. Owner tree: `infra/`. **Re-run 2026-09-06 (round 41)** ([decisions § 1308](../architecture/decisions.md)): Terraform v1.13.0, `terraform init -backend=false` + `validate` in all five root stacks (`infra/bootstrap`, `infra/dns`, `infra/github-oidc`, `infra/envs/prod`, `infra/envs/preview`) every one `Success!`; `terraform fmt -check -recursive infra` exit 0 both before and after this round's edits. Transitive module coverage re-measured, not asserted: an undeclared-variable reference planted in `modules/web-stack/main.tf` fails `validate` from BOTH env roots naming file and line, and the file restores byte-clean. § 1267's standalone-module finding was not re-litigated (`configuration_aliases` makes a module a non-root by construction) and no stray `.terraform.lock.hcl` was created, because no standalone `init` was run. Still no `plan` behind §§ 1021-1024, 1084-1085, 1111 or § 1268's `custom_error_response` — no lane holds AWS credentials — so it is **re-filed in the round-41 section below**. What the round found instead is what `validate` structurally cannot see: three false comments on the 403 block, closed in the same round.
- [x] **Every prerendered page ships TWO `<title>` tags, and the first one is `Threkir`.** **CLOSED 2026-09-04** ([decisions § 1167](../architecture/decisions.md)). The measurement was reproduced exactly as filed on a fresh production build — 15 of 16 built HTML files carried two `<title>` elements, `build/index.html` the only exception — and the fix is the shape the filing named, after settling the premise it flagged as unmeasured. **That premise was wrong**: Svelte 5 does NOT concatenate, it dedupes. `Renderer.set_title` in `svelte/src/internal/server/renderer.js` keeps one title per document by depth-first path comparison, letting the deepest component's displace every ancestor's, so a default in the root layout's `<svelte:head>` is REPLACED by a page's own rather than reproduced beside it — measured by planting a sentinel default and finding it in none of the fifteen prerendered pages. What the dedupe cannot see is a literal in `app.html`, because that is not a component. So the literal is gone and the default now lives in `src/routes/+layout.svelte`. Measured after: every prerendered page carries exactly one title, its own; `build/index.html` carries none. **The titleless shell is deliberate, not an oversight** — adapter-static's fallback renders no components, so no layout default can reach it, and the alternative that keeps it titled (moving the literal AFTER `%sveltekit.head%` so the page's own comes first) ships a spec-forbidden duplicate on fifteen pages and plants a trap for § 1116's landing page. Both consequences are filed below: the `spa_shell_head_signals.test.ts` re-measurement it forces, and the site root's raw HTML having no title until `/` is prerendered. Five assertions in `apps/web/src/lib/seo/document_title.test.ts` pin the source and artifact halves, each proved non-vacuous — the fifth added after the replacement COMMENT in `app.html` named `%sveltekit.head%` in prose and SvelteKit substituted the whole head into the comment, leaving the CSP meta inert and the literal placeholder standing as raw text in `<head>` while every head-shape assertion still passed ([§ 1169](../architecture/decisions.md)). Original filing below. Measured 2026-09-04 on a production build of `apps/web`: 15 of the 16 built HTML files carry two `<title>` elements — the only one that does not is `build/index.html`, the SPA fallback. `src/app.html` writes a literal `<title>Threkir</title>` before `%sveltekit.head%`, and every prerendered page then contributes its own through `<svelte:head>`, so `build/learn/couch-to-5k.html` reads `<title>Threkir</title><title>Couch to 5K: your first month — Threkir</title>`. Per the HTML spec the document title is the **first** `title` element, so a raw fetch — every non-JS crawler, every social unfurler, and the browser tab until hydration replaces it — gets the generic site name on all twelve `/learn` pages, which are the only surfaces this app deliberately prerenders **for** indexing ([learn.md](../features/learn.md), [decisions § 161](../architecture/decisions.md)). Not fixed in the round that measured it because the obvious repair is wrong in both directions: deleting `app.html`'s title leaves the SPA fallback (the site root and every deep-link 403 body, [§ 1022](../architecture/decisions.md)) with no title at all and shows the raw URL in the tab, and it would also kill the one strip in each share-Lambda injector that currently has work to do ([§ 1115](../architecture/decisions.md)). The durable shape is a default title emitted from the root layout's `<svelte:head>` rather than from the template, so a page's own title replaces it instead of following it — which needs a measurement first, because Svelte's SSR `<svelte:head>` concatenates rather than dedupes and a layout title may simply reproduce the pair one level down. Owner trees: `apps/web/src/app.html` + `apps/web/src/routes/+layout.svelte`.

- [x] **The `/learn` hub and every `/learn/category/[category]` page emit no structured data, and `seo.md`'s render map claimed `Article` + `BreadcrumbList` for all three learn routes.** **CLOSED 2026-09-04** ([decisions § 1168](../architecture/decisions.md)). The measurement was reproduced on a fresh production build: `build/learn.html` and all six `build/learn/category/*.html` carried zero `application/ld+json` blocks against one `Article` on each of the seven guides. `buildLearnCollectionJsonLd` in `apps/web/src/lib/learn/learn_meta.ts` now emits a `CollectionPage` for both index kinds — **not** the `Article` the render map named, because an index of guides is not an article and a node asserting the wrong type describes a page that does not exist — carrying the same `BreadcrumbList` rungs `buildGuideJsonLd` already builds minus the article's own (hub: Home → Learn; category: Home → Learn → category; the last rung names the page and carries no `item`, as the guide builder already did) plus an `ItemList` of the guides the page lists, in the order it renders them, with `numberOfItems` read off the list itself. A page listing nothing omits the `ItemList` rather than claiming an empty collection. Measured after: the hub and all six category pages carry one `CollectionPage` block each, the guides are unchanged at one `Article`. Seven unit tests on the builder plus three artifact assertions in `apps/web/src/lib/seo/learn_structured_data.test.ts` (type per route kind, breadcrumb depth and 1..n positions with a last rung that links nowhere, non-empty self-consistent `ItemList`), each proved non-vacuous against a mutated build. `docs/features/seo.md`'s row is now wrong in the other direction and its correction is filed below — that file belonged to another lane this round. Original filing below. Measured 2026-09-04 on a production build: `build/learn.html` and `build/learn/category/racing.html` contain zero `application/ld+json` blocks, while `build/learn/couch-to-5k.html` carries one `Article` with an embedded `breadcrumb`. The doc has been corrected to say what is there (the row is now split in two), so this box is about the **gap**, not the drift. Seven guide pages are richly marked up and the two page types a reader actually lands on from a search for the category — the hub and the six category indexes — carry nothing, so neither is eligible for a breadcrumb trail in results and neither declares itself a collection of the guides it lists. The shape is `CollectionPage` (or `ItemList`) plus a `BreadcrumbList` of the same three-or-four rungs `learn_meta.ts` already builds for the guide pages, which is a reuse of an existing builder rather than a new vocabulary. Owner tree: `apps/web/src/lib/learn/` + `apps/web/src/routes/learn/`.

- [x] **The SPA-shell head guard's artifact half never runs in CI, because the job that builds and the job that tests are different jobs.** `apps/web/src/lib/share/spa_shell_head_signals.test.ts` ([decisions § 1115](../architecture/decisions.md)) reads `src/app.html` unconditionally and `build/index.html` only when a build is present. `test:unit` runs in `test-web`, which never builds, so the artifact assertion self-skips there; `build-web` builds and never runs it. The template half is the binding one today — a fallback page's `%sveltekit.head%` contributes only the hash-CSP meta and the modulepreload links — but the artifact half is what would catch the SvelteKit or adapter-static upgrade that changes that, and it is the half that flips the day the landing page reaches `index.html`. Wanted: one step in `build-web` in `.github/workflows/ci.yml`, after `Build web (production)`, running `npx tsx --test src/lib/share/spa_shell_head_signals.test.ts` from `apps/web` (`tsx` is declared in `apps/web`'s devDependencies, so it satisfies § 764's no-unpinned-`npx` rule) and printing its own `::error::` on failure, per the bundled-job rule that job is already subject to. `build-web` is already in the `CI gate` aggregator's `needs:`, so nothing changes there. Owner tree: `.github/workflows/`. **CLOSED 2026-09-05 ([decisions § 1210](../architecture/decisions.md)).** Taken together with the five-more-assertions filing below, as that entry asks: ONE step in `build-web`, after the bundle budget, running all three suites from `apps/web` through `npx tsx --test`, with its own `::error::`. Both premises re-measured and both hold — `tsx` is declared in `apps/web`'s devDependencies (and already mapped in `check_workflow_binaries.mjs`'s `PROVIDERS`), and `build-web` was already in the `CI gate`'s `needs:`, so the gate list and the job count are unchanged at 33. Measured against a real production build on this branch: all 11 cases pass with `apps/web/build` present; with it parked, `# no apps/web/build` prints 7 times and the run still exits 0 at `tests 11 / pass 4`.

- [x] **`test_inventory.md` still says the body-parse register is "the only pin `snapToRoad` can have", and `snapToRoad` no longer exists on web.** [§ 1119](../architecture/decisions.md) deleted `routing.ts`'s three unimported exports and dropped the file from `response_body_parse_guard.test.ts`'s register, so `docs/testing/test_inventory.md`'s line about that register ("It is also the only pin `snapToRoad` can have, since `routing.ts` reaches `$env/static/public` through `core/supabase` and the raw `tsx --test` suite cannot load it") describes a function the web tree no longer holds. The register itself is correct and its count claim — 36 sites, 10 registered across four files — moved to 33 and 8 across three (measured, not estimated); that sentence and those two numbers want re-reading against the file. One-paragraph doc edit, no code. Not done in the round that caused it because `docs/testing/` was another lane's tree and a shared doc is the worst place to take a merge conflict. Owner tree: `docs/testing/test_inventory.md`. **CLOSED 2026-09-05 ([decisions § 1217](../architecture/decisions.md)).** **The filing was already stale** — the round-38 merge (`8a414ff79`) had dropped the `snapToRoad` sentence and moved the numbers, without ticking the box. Both numbers RE-MEASURED here rather than trusted, by replaying the guard's own scan (`stripComments` + its `BODY_PARSE` regex + its `tryBodies` span walk) over `apps/web/src` and `apps/web/lambda`: **33 sites total, 8 unguarded across three files** (`integrations/import.ts` 4, `nutrition/food_search.ts` 3, `routes/route_describe_client.ts` 1), exactly what `test_inventory.md` now states and exactly what `REGISTER` declares. No edit was needed.
- [x] **`routing.dart`'s header calls itself a "Dart port of `apps/web/src/lib/routes/routing.ts`", which now holds neither function it names.** [§ 1119](../architecture/decisions.md) deleted web's `snapToRoad` / `fetchRoute` / `fetchFullRoute`; the live Dart `snapToRoad` + `fetchRouteThrough` in `apps/mobile_android/lib/routing.dart` are untouched and still correct, but their header (and `apps/mobile_android/lib/CLAUDE.md`'s `routing.dart` row, and `docs/product/parity.md`'s "Snap-to-road / trail (OSRM)" row) describe a port relationship whose other end is gone. **This is not a parity-pair break** — the two were never registered in the root `CLAUDE.md` list or `.claude/agents/shared-library-syncer.md`, which is why nothing failed — so the fix is documentary: say that the Dart file is the sole implementation of the OSRM client, that web reaches OSRM through `RouteBuilder.svelte`'s inline paths and the `osrmProxyFetch` proxy client, and that the two are deliberately not a lockstep pair. Do it in a change that owns mobile plus `parity.md`. Owner tree: `apps/mobile_android/lib/routing.dart` + the iOS twin + `apps/mobile_android/CLAUDE.md` + `docs/product/parity.md`. **CLOSED 2026-09-05 ([decisions § 1218](../architecture/decisions.md)).** The filing is accurate and its recommended framing was taken: `routing.dart`'s header and `apps/mobile_android/CLAUDE.md`'s row now say the Dart file is the SOLE client-side OSRM implementation and deliberately NOT a lockstep pair, naming what web has instead (`OSRM_PROXY_BASE` + `osrmProxyFetch`, with `RouteBuilder.svelte` building its service paths inline). The filing missed a SECOND dead reference in the same header: it claimed to mirror a web `assertOsrmConfiguredForProd`, which no longer exists — the posture moved into the proxy handler's fail-closed 501. Both fixed, mirrored into `apps/mobile_ios/`. `docs/product/parity.md`'s row was outside this lane's tree and is filed below.

### Surfaced by the #789 round 38 mobile run-detail lane (2026-09-04)

- [x] **Web's run-detail renders elevation unconditionally, so every track-less run is told it climbed `0 m`.** **CLOSED 2026-09-04** ([decisions § 1164](../architecture/decisions.md)). **All four claims verified against the file before acting; all four held.** The elevation cell now gates on the samples rather than on the answer — `elevationSourceTrack` returns the track when any point carries an `ele` and null otherwise, `realElevationGain` is null when it does, and the cell is absent. The opposite error is pinned separately: a genuinely flat run over a track that DID record altitude still renders its real `0 m`, because suppressing that reports a measurement as an absence. `hasElevation` at line 1076 was NOT reused — it reads `baseTrack`, which prefers the map-matched track, and the gain is computed from `run.track`; gating one track's sum on another track's samples is a second bug waiting. `totalSteps` and `avgCadence` now require a datum (matching mobile's `> 0` gates), a fractional step count truncates the way mobile's `_steps` does, and a reported `cadence_spm` that rounds away to zero claims nothing rather than falling through to the pedometer count — mobile returns the rounded reading and hides the tile, so falling through would make the two platforms disagree about one run. The count was not repaired but deleted: `keyStatsCount`'s hand-maintained `6 +` is replaced by a derived `keyStats` list of `{label, value}` cells that the template renders with an `{#each}`, and `showActivityFiller` reads `keyStats.length` — so item 4 cannot recur, and a cell added later cannot desynchronise a count that is the list's own length. Gating predicates extracted to `$lib/runs/key_stats` with 13 unit tests; the elevation rule itself stays called on the page per § 1004's guard, and two further guard cases pin the `{#each}` and the filler's read of `.length`. No new i18n keys. Original filing below. `apps/web/src/routes/runs/[id]/+page.svelte:804` derives `realElevationGain = run?.track ? Math.round(computeElevationGain(run.track)) : 0` and line 1676 renders it as a key stat with no `{#if}` at all. A manual entry and a summary import have no track, and a track carrying no `ele` sums to 0 the same way, so three different states — no track, altitude never recorded, and a genuinely flat run — all render the identical `0 m`. That is the fabricated-zero half of the class [§ 1124](../architecture/decisions.md) just closed on mobile, where the cell already gated on `_hasElevation` and is absent rather than zero. The change is to gate the cell on the same test mobile uses (`run?.track?.some((p) => p.ele != null)`, which the file already computes as `hasElevation` at line 1076 for the elevation chart) and to decrement `keyStatsCount`'s always-rendered `6` accordingly. Two smaller instances of the same shape sit beside it and are worth taking in the same pass: `totalSteps` accepts any `number`, so a stored `steps: 0` renders as `0` where mobile requires `> 0` (no writer in this repo emits it, but the metadata bag has no schema and a third-party import could); and `avgCadence`'s `Math.round((totalSteps / (movingSeconds / 60)) || 0)` returns `0` rather than `null` for that same zero, so the run then also claims `0 spm`. `keyStatsCount`'s own comment is separately wrong today — it counts Calories among the six that "never hide" while the template gates it on `showCalories && estimatedCalories > 0`, so the odd/even activity-filler flips the wrong way whenever the estimate is unusable or the pref is off. Not taken here: the round-38 mobile run-detail lane owns no path under `apps/web/`. Owner tree: `apps/web/src/routes/runs/[id]/+page.svelte`.
- [x] **The Go worker's export CSV has no `hr_coverage` column, so the rail most runners reach still exports `avg_bpm` unqualified.** [§ 1134](../architecture/decisions.md) added the column to the Edge Function's `apps/backend/supabase/functions/export-data/render.ts`; `apps/job_worker/internal/dataexport/server.go` mirrors that column list BY HAND (`csvColumns`, whose own comment says "mirrors the EF's column order") with no guard comparing the two, and it is the queued rail — mobile has no EF path at all and always enqueues ([§ 724](../architecture/decisions.md)), web takes the EF only when `PUBLIC_EXPORT_HUB_URL` is unset. So the two live transports now emit different headers and the more-used one is the unfixed one. The change is three edits in that one file plus a constant: **(1)** `MetaHRCoverage = "hr_coverage"` in `apps/job_worker/internal/schema/schema.go` beside `MetaAvgBPM`; **(2)** `schema.MetaHRCoverage` inserted into `csvColumns` immediately after `schema.MetaAvgBPM`; **(3)** the matching cell in `csvRow`, which must NOT be `stringy(md[schema.MetaHRCoverage])` — the EF grades it, so the Go side needs the same refusal or the two rails disagree on a malformed value: a `float64` (JSON's only number type), finite, within `0..1`, formatted the way `stringy` formats a float64, and `""` otherwise. Port `hrCoverageCell` from `render.ts` and mirror its four refusal cases in `server_test.go`. **While there, consider a guard**: nothing compares `csvColumns` with `CSV_COLS` today, which is why this drift is possible at all — a Go test that reads `render.ts`, extracts the `CSV_COLS` array and requires equality would make the mirror enforced rather than hoped for, and would have failed this change instead of allowing it. Owner tree: `apps/job_worker/`. **CLOSED 2026-09-05 ([decisions § 1171](../architecture/decisions.md)).** The filing was already stale when this lane opened it: `8a414ff79` (#856) had landed all three edits — `schema.MetaHRCoverage`, the column in `csvColumns` immediately after `schema.MetaAvgBPM`, and a `hrCoverageCell` in `csvRow` that grades rather than passes through — plus the guard the filing only "considered", `TestCSVColumnsMatchTheEdgeFunctionRail` in `apps/job_worker/internal/dataexport/hr_coverage_test.go`, which parses `CSV_COLS` out of `render.ts` and requires ordered equality with `csvColumns`. Verified by running it, and again by the seven columns § 1171 added: that guard is what forced both rails to move together.
- [x] **The Art 20 export's `runs` projection is short by seven real columns on BOTH transports, and a promotion migration silently emptied four of them.** [§ 1135](../architecture/decisions.md). Measured against the generated row type: `public.runs` has 24 columns, the exporters select 17, and `concluded_at`, `elevation_gain_m`, `race_listing_id`, `fastest_5k_s`, `fastest_10k_s`, `fastest_half_marathon_s` and `fastest_marathon_s` reach neither `runs.csv` nor the backup archive's `runs.json`. The four PR times are the sharp case: migration `20270325_001` promoted them out of `runs.metadata` into real columns **and stripped the keys from the bag in the same UPDATE**, so they used to ride inside the `metadata` column of both formats and stopped the day that ran. `manifest.json` cannot surface any of this — its completeness contract is row counts against each section's total, and every row is present. Both projection sites are hand-written lists: `RUNS_SELECT` at `apps/backend/supabase/functions/export-data/index.ts:100` (plus the `shape:` projection in `writeBackupZip`, which enumerates the same fields again for `runs.json`) and `ExportRun` + its select in `apps/job_worker/internal/dataexport/server.go`. The change wanted: add all seven to both selects and to both `runs.json` projections; decide separately whether any earn a CSV column (`elevation_gain_m` has the strongest claim, since the CSV's `elevation_m` reads the metadata key and a writer that only sets the column exports an empty cell) and, if so, add them to `CSV_COLS` and `csvColumns` together. Not done in the round that found it because fixing only the EF widens the ROLLBACK rail while mobile and configured web keep enqueuing to the worker — near-zero benefit to a real data subject, and a second live-transport divergence on top of § 1134's. **Also worth a guard in the same change**: a test that reads the `runs` Row out of `apps/web/src/lib/database.types.ts` and fails when a column appears in neither projection would have caught `20270325_001` on the PR that landed it. Owner tree: `apps/backend/supabase/functions/export-data/` **and** `apps/job_worker/`. **CLOSED 2026-09-05 ([decisions § 1171](../architecture/decisions.md)).** Both transports, as one change. The two selects are now the WHOLE table (`RUNS_SELECT`, `exportRunsSelect`) and each format declares what it OMITS instead of what it carries, so a column added to `runs` is exported by default: the CSV declines `user_id` + the two Storage paths (26 columns now, with `elevation_gain_m` beside the `elevation_m` metadata key and the four `fastest_*_s` restored), the backup `runs.json` declines `user_id` alone, the GPX manifest declines the paths and the two timestamps. Executed as omissions on both rails — `omitKeys` in TS, an embedded `ExportRun` with the dropped field shadowed in Go (`json:"-"` does NOT shadow; see § 1171). The guard the filing asked for is four guards, each shown to fail by perturbing what it guards: the generated `runs` Row vs `ExportRun` and vs the EF select, the worker struct vs its own select, the `main.go` bridge field by field, and the two renderers. NOT done here and filed below: `docs/compliance/data-subject-rights.md` still describes the gap as open, and that file is outside this lane's tree.
- [x] **`docs/testing/test_inventory.md` still says `export-data/render.test.ts` has 13 tests; it has 17.** [§ 1134](../architecture/decisions.md) added four — the `hr_coverage` header position, the three heart-rate shapes against the strap-less contrast, `hrCoverageCell`'s refusals, and that a refused value still rides verbatim in the `metadata` column. The entry at `docs/testing/test_inventory.md:1041` needs its count corrected and one clause describing the new cases; `docs/testing/` belonged to no lane in the round that added them. Owner tree: `docs/testing/`. **CLOSED 2026-09-05 ([decisions § 1217](../architecture/decisions.md)).** **The filing was already stale** — the round-38 merge (`8a414ff79`) had already written 17, without ticking the box. Counted here rather than trusted: `apps/backend/supabase/functions/export-data/render.test.ts` holds **17** `Deno.test(` declarations. Lane 1 is editing that file concurrently this round, so the figure may move again before integration.

### Surfaced by the #789 round 38 worker/reaper lane (2026-09-04)

- [x] **`export_blob_reap` is routed but two pgtap files still describe a queue without it.** Both are one line and both were out of this lane's tree (`apps/backend/supabase/tests/` belonged to the backend lane; this lane held exactly one migration file). Neither omission fails anything today, which is the point — they are the rails that would have caught the drift, sitting one kind behind. **(1)** `jobs_kind_allowlist_test.sql` needs a `lives_ok` case for `('export_blob_reap', '{}'::jsonb)` and its `plan(18)` bumped to `plan(19)`. That file's own header names this exact failure: "`club_photo_process` (20270301_001) and `safety_sms` (20270410_001) shipped their migration and their handler but were never added below, so for two rounds the suite passed while asserting nothing about them." `export_blob_reap` is the third. **(2)** `anon_execute_contract_test.sql`'s `server_only` fixture needs `('enqueue_export_blob_reap', true)`. Assertions (1) and (2) of that file already cover the new function — it is revoked from `public, anon, authenticated`, so it is withheld and passes — but (5) and (6) are the positive controls that a signed-in account cannot reach the cron/job-queue family and that `service_role` keeps the operator hook, and those read a literal list. Without the row the new enqueue is outside both controls. Owner tree: `apps/backend/supabase/tests/`. **CLOSED 2026-09-05 ([decisions § 1180](../architecture/decisions.md)).** Both claims verified before acting and both held. `jobs_kind_allowlist_test.sql` gained the `export_blob_reap` `lives_ok` (payload `{}`, matching the enqueue) and `plan(18)` became `plan(19)` — 16 `lives_ok` + 3 `throws_ok`, counted. `anon_execute_contract_test.sql`'s `server_only` fixture gained `('enqueue_export_blob_reap', true)`; the migration revokes it from `public, anon, authenticated` and grants `service_role`, so it satisfies both (5) and (6), and the fixture is a table rather than an assertion so `plan(6)` is unchanged.
- [x] **A night the worker is down still orphans export bytes, and the reaper cannot reach them afterwards.** [§ 1144](../architecture/decisions.md) schedules `enqueue-export-blob-reap` at 04:13, ten minutes ahead of `cleanup-stale-export-blobs` at 04:23, so in the normal case the Storage-API erasure happens before the row delete and the sweep finds nothing left to orphan. The lead is best-effort: if the worker is down (or the job fails its three attempts) the sweep still deletes the `storage.objects` rows at 04:23, and the bytes then join the pile [§ 1049](../architecture/decisions.md) measured — unreachable by any Storage-API reaper, because list reads the rows that are gone. This is a NEW orphan created after the reaper shipped, distinct from the residue box above, which is about the pile that already exists. Three candidate fixes and the choice is the owner's. (a) Stop the sweep deleting `exports` rows and rely wholly on the worker: closes the leak, but trades away the unconditional removal of reachability that is the one thing SQL can guarantee, so a long worker outage leaves week-old archives listable. (b) Have the sweep skip a row whose object the reaper has not yet had a chance at — needs the sweep to know the reap's outcome, which is a status read across tiers. (c) An S3 lifecycle rule on the `exports` bucket prefix as a backstop, which also erases the existing pile and is the same operator action the § 1049 residue box already needs. (c) subsumes both piles and is the recommendation. Owner tree: `apps/backend/supabase/migrations/` or an operator + `docs/ops/`. **CLOSED 2026-09-05 ([decisions § 1172](../architecture/decisions.md)).** Option (a), and the objection this entry raised against it is measurably false: the trade named was "a long worker outage leaves week-old archives listable", but the `exports` bucket carries NO `storage.objects` policies (`20270602_001`, deliberately) and `20260816_001` had already carved `exports/` out of the owner-folder SELECT on `runs` — an export is reachable through the server-minted 10-minute signed URL and nothing else, so the row delete was removing a reachability nobody had. Migration `20270709000001` unschedules `cleanup-stale-export-blobs` and leaves its BODY untouched, so every `prosrc` assertion in `export_surface_contract_test.sql` still holds and the function stays as a `service_role` break-glass. `expire_stale_export_jobs()` is unaffected — § 1144 already made it unconditional by putting it in the pg_cron enqueue. The same migration answers § 1144's open question about the legacy `runs/{uid}/exports/*` leg: the enqueue now derives one prefix-scoped job per user who still has a legacy archive, so the set empties itself, and the singleton guard is per-PAYLOAD. Two cross-tier Go guards, both negative-controlled: the `jsonb_build_object` keys against the handler's payload struct, and a replay of every migration's `cron.schedule`/`unschedule` proving the sweep is not scheduled again. Not done and filed below: an ALERT for a worker down long enough to overrun retention, which needs the post-condition check to move, and `docs/backend/api_database.md`'s cron table still lists the sweep as scheduled — both outside this lane's tree.
- [x] **`migration_locks.md`'s "both halves in one migration" advice and `check_migration_online_safety.mjs` both treat a same-file `NOT VALID` + `VALIDATE` as online, and it is not.** Measured this round ([decisions § 1148](../architecture/decisions.md)): `apps/backend/scripts/apply-pending-migrations.sh` applies each migration as `begin;` … `commit;` (it has to — the ledger row commits atomically with the SQL), and `migration_locks.md` itself asserts the CLI wraps a file the same way, which is its stated reason `CREATE INDEX CONCURRENTLY` errors as apply-time DDL. A lock taken by DDL is held until the transaction ends, so in a file holding `drop constraint` + `add … not valid` + `validate constraint` the `ACCESS EXCLUSIVE` from the `drop` is still held during the `validate` scan; `VALIDATE`'s `SHARE UPDATE EXCLUSIVE` is subsumed by it, never a downgrade. Same-file, the two-step and the single-step take the identical lock for the identical duration. Two owners. **(1)** `docs/backend/migration_locks.md` § CHECK constraints says "putting both in one migration is still far better than the single-step form (the `NOT VALID` add doesn't scan, and the `VALIDATE` uses the weaker lock)" — the parenthesis is true statement-by-statement and false as applied, and the sentence should say the split must be across MIGRATIONS to buy anything, which the same section's "ideally a later migration" already hints at. `20270603_001`'s own comment carries the same wrong claim about `jobs`, and § 1148 corrects it for the record rather than editing an applied migration. **(2)** `apps/backend/scripts/check_migration_online_safety.mjs` grades the statement shape, so a same-file two-step is exactly the shape it looks for: it passes a migration taking the lock it exists to prevent, on a table its own `GUARDED_TABLES` names. The durable fix is to grade the pair by FILE — a `VALIDATE` in the same file as the `ADD` (or after a `DROP CONSTRAINT`) on a guarded table is a violation, not a compliance — with the existing grandfather-by-name list absorbing the shipped history. That is [§ 764](../architecture/decisions.md)'s hazard one tier over: a guard whose green means less than its name says. Not taken here: `docs/backend/` and `apps/backend/scripts/` were both outside this lane, which held exactly one migration file. Owner tree: `docs/backend/migration_locks.md` + `apps/backend/scripts/check_migration_online_safety.mjs` (+ its `.test.mjs`). **CLOSED 2026-09-05 ([decisions § 1180](../architecture/decisions.md)).** Both owners done. The doc now says the split must be across migration FILES and its lock-reference row for `VALIDATE CONSTRAINT` carries the qualifier; the guard walks each file in order and reports a second violation kind, `same_txn_validate`. The rule is BROADER than the filing proposed, and the breadth is load-bearing: keying it on constraint DDL alone would have missed `20261127_001`, which validates after an `ADD COLUMN` — the lock is the table's, not the constraint's. Any earlier `ALTER TABLE` action other than another `VALIDATE`, plus `CREATE INDEX` / `CREATE TRIGGER` on the same table, escalates; DML does not, because `ROW EXCLUSIVE` and `SHARE UPDATE EXCLUSIVE` do not conflict. Within one `ALTER TABLE` the action order is irrelevant, since the statement takes the strongest lock any action needs. **The filing's parenthetical about `GRANDFATHER_CUTOFF` is stale** — [§ 775](../architecture/decisions.md) retired the cutoff for a name list before this filing was written, and the guard's own tests already pinned that; no cutoff exists to regress. 28 `same_txn_validate` entries across eleven files join the 32 `blocking_add` ones, and `kind` is now part of the allowlist key so one exemption cannot cover the other class. The guard's own suite had pinned the wrong verdict in as many words ("passes a NOT VALID jobs_kind_chk widening"); that test is inverted and nine more cover the new kind.
- [x] **The root `CLAUDE.md` says `ci.yml` holds "the same 33 CI jobs", and [§ 1149](../architecture/decisions.md) made it 34.** One-line correction the infra-ci lane could not take because it does not own the root `CLAUDE.md`. The block is at `CLAUDE.md` under `.github/workflows/`, and reads today: `ci.yml             → the same 33 CI jobs run on every PR and push to main` / `                       (no PR-only/push-only split); the single required status` / `                       check is the "CI gate" aggregator that needs: all of them`. Replace the count with **34** and, on the third line, say what the 34th is, because it is the one job that is not a job: `check is the "CI gate" aggregator that needs: all of them — one` / `                       of which calls terraform.yml, whose own jobs could never` / `                       be named in a needs: list (decisions § 1149)`. Verified rather than guessed: `parseJobKeys` in `scripts/check_ci_diagnostics.mjs` reports 33 keys besides `ci-gate` after the change, and the guard's own summary line now reads "`ci-gate` waits for all 33 of them" (it excludes the gate itself, the prose counts it, hence 34). No guard reads the number in `CLAUDE.md`, which is why it drifted unnoticed through every round that added a job. **CLOSED 2026-09-05 ([decisions § 1217](../architecture/decisions.md)).** **The filing was already stale**: the round-38 merge this branch was cut from (`8a414ff79`) had already replaced 33 with 34 in `CLAUDE.md`, without ticking the box. Re-measured rather than trusted — `parseJobKeys` over `.github/workflows/ci.yml` returns **34** job keys and `parseNeeds(text, 'ci-gate')` returns **33**, so 34 is right. The residual falsehood the round-38 fix left is the next clause: with 34 jobs stated, an aggregator that "needs: all of them" is off by one, because it cannot wait on itself. Now reads "which needs: every one of the other 33 — among them `terraform`, a caller job whose real work is terraform.yml, whose own jobs could never be named in a needs: list (decisions § 1149)". The guard the filing asked about is possible and is FILED below rather than built, because `scripts/` was not this lane's tree.
- [x] **Terraform added outside `infra/` would be format-checked, validated, Trivy-scanned and CI-triggered by nothing at all.** Pre-existing, and surfaced rather than created by [§ 1149](../architecture/decisions.md): the new `infra` output of `ci.yml`'s `changes` job inherits the old `terraform.yml` trigger's path list (`infra/`, `.trivyignore`, the workflow file), so a `.tf` file under some other tree does not even start the job — exactly as the trigger did not. `scripts/check_infra_coverage.mjs` cannot report it either: `terraformDirs(INFRA_DIR)` walks `infra/` and nothing else, so its claim "every directory holding `.tf` is in the matrix" is scoped to a tree the file is not in. Today no such directory exists (measured: `find . -name '*.tf' -not -path './infra/*'` outside `node_modules` is empty), which is why this is filed rather than built for. The durable fix is one guard-side change, not a filter widening: have `check_infra_coverage.mjs` enumerate Terraform directories from the REPO ROOT and fail on any outside `infra/` with a message saying that tree is covered by no CI job — that turns "a stack somewhere else" from a silent gap into a red, and leaves the filter honest at `infra/`. Owner tree: `scripts/` (+ a one-line filter widening in `.github/workflows/ci.yml` if the answer is instead to support such a tree). **CLOSED 2026-09-05 ([decisions § 1211](../architecture/decisions.md)).** Taken exactly as filed — the guard changes, the path filter stays honest at `infra/`. `scanStrayTerraform` sweeps from the repo root and fails on any Terraform directory outside the top-level `infra/`, naming the tree and what does not read it; the premise was re-measured first and holds (no `.tf` outside `infra/` today). Because the verdict is an absence, the sweep also reports whether it saw `infra/` at the top level and fails when it did not, so a walk that never started cannot read as clean. Mutation-tested end to end: a `.tf` planted under `docs/ops/` fails the guard by name at exit 1, which also proves the walk really descends. Eight new cases in `check_infra_coverage.test.mjs` (81 total, all green). One residual stated in § 1211 rather than closed: `infra-guards` is gated on the `code` output, so a `.tf` committed under `docs/` lands before the sweep sees it and then fails every PR after — late, but loud.

### Surfaced by the #789 round 38 watch lane (2026-09-04)

- [x] **`apps/watch_ios` now measures heart-rate coverage and cannot send it: one line in `apps/mobile_ios/`, one in `ContentView.syncRun`, one registry row.** [§ 1156](../architecture/decisions.md) built the measurement and spent it locally on the `avg_bpm` suppression, because claim (6) of `scripts/check_watch_ios_source.mjs` reads BOTH ends of the `WCSession.transferFile(_:metadata:)` envelope and fails the PR on a key the phone never lifts. The remainder is one change across three trees, and it has to land as one change or CI is red between them. **(1)** `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift`: lift `hr_coverage` out of the metadata envelope beside the existing `avg_bpm` lift, same shape, a `Double`. **(2)** `apps/watch_ios/WatchApp/ContentView.swift`, in `syncRun`'s dictionary literal: `let hr = workoutManager.finishedRun.map { ... }` — the run already carries the graded average; `FinishedRun` needs a `hrCoverage: Double?` field alongside `averageBPM`, both taken from the one `healthKit.heartRateClaim(activeElapsedSeconds:)` call `WorkoutManager.stop()` already makes, and the checkpoint's `RunCheckpoint` needs the same optional field (added with `decodeIfPresent`, so an older checkpoint decodes as nil — unmeasured, which is exactly what it is). Write the key only when the claim's `coverage` is non-nil: an assumed figure is a fabricated one. **(3)** `docs/backend/metadata.md`'s `hr_coverage` row: add `apps/watch_ios` to the Writers column and REPLACE the "**Wear OS only, and the absence is three-way ambiguous**" passage — after this lands, a `source = 'watch'` run with no `hr_coverage` is still ambiguous, but the population changes (an Apple-Watch run whose `HKWorkoutSession` never started joins the Wear cases, and a normal Apple-Watch run leaves them). That row belonged to no lane this round and belongs to whichever lane owns `docs/backend/` next. **Secondary, and it is a real gate rather than a formality:** the accumulator has never run — no Xcode here — and its one silent failure mode is reading blank on a device, which would write `hr_coverage: 0.0` on every run. The local suppression already fails closed against that (nil is unmeasured, not zero), but the KEY is a durable record and wants a simulator or device run before it is written, not just a green compile. Owner tree: `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift`, `apps/watch_ios/WatchApp/{ContentView,WorkoutManager,CheckpointStore}.swift`, `docs/backend/metadata.md`. **CLOSED 2026-09-05 ([decisions § 1207](../architecture/decisions.md)).** All three trees landed in one commit, so the guard was never red between them. `FinishedRun` and `RunCheckpoint` carry `hrCoverage` (the checkpoint with `decodeIfPresent`, so a pre-field checkpoint recovers as nil), `stop()` and `writeCheckpoint` take BOTH halves from ONE `heartRateClaim` call rather than grading twice, `syncRun` writes the key only from a non-nil claim, `WatchIngestBridge.swift` lifts it, and claim (6) now reports nine keys instead of eight — verified to BIND by deleting the lift and watching it fail. `MetadataRegistryTests`' assertion was inverted rather than deleted: it now requires the exact `if let <x> = run.hrCoverage { metadata["hr_coverage"] = <x> }` shape and fails on any `??` near `hrCoverage`. **Two things the filing did not have.** (a) It said part (1) was one line in `apps/mobile_ios/`; it is one line there and TWO more past it, because `WatchIngest._runFromArgs` (`main.dart`) and `runFromWatchPayload` (`watch_ingest_queue.dart`) each build `metadata` from an explicit key allowlist that does not name `hr_coverage` — so the key reaches the phone's Swift and is dropped before the row. Filed below; the `metadata.md` row and `apps/watch_ios/CLAUDE.md` both state that limit rather than claiming the writer works end to end. (b) The secondary gate stands unchanged: still no Xcode here, the accumulator has never executed, and a blank timestamp source on a device would send `hr_coverage: 0.0` on every run — the non-nil gate does not catch that, because zero is a measurement. Recorded on the registry row and in § 1207.
- [x] **"Discard" on the Wear crash-recovery prompt destroys the run's only durable record on one tap, with no confirmation.** Found while closing [§ 1154](../architecture/decisions.md) and deliberately not widened into it. `discardCheckpoint` calls `checkpoints.clear()` immediately; the checkpoint is, by [§ 1107](../architecture/decisions.md)'s own words, "the run's only durable record" when the queue does not hold the run. That is the house `destructive-action-without-confirm` anti-pattern the `/ux-hunt` loop exists for, on the most consequential button in the app, read by a runner who has just crashed out of a recording. It is **worse since § 1154** rather than merely unchanged: while the queue is unreadable, Discard is now the only enabled control on the screen, so a runner who wants their run and finds "Save it" greyed out has exactly one thing left to press. The fix is the Wear idiom used elsewhere in this tree — a two-press confirm, or the `undo_queue` deferred-commit shape web and mobile already share (decisions § 523) — not a dialog, which does not fit the takeover. Wants a string in all seven catalogues. Owner tree: `apps/watch_wear/android/app/src/main/kotlin/com/runapp/watchwear/ui/RunWatchApp.kt` + `RunViewModel.kt` + `res/values*/strings.xml`. **CLOSED 2026-09-05 ([decisions § 1206](../architecture/decisions.md)).** Two presses inside `CONFIRM_WINDOW_MS`, not a dialog, exactly as the filing directed — but the "Wear idiom used elsewhere in this tree" turned out not to exist: `apps/watch_wear` had no confirmation of any kind before this (grep for confirm/Dialog/onLongClick over `src/main/kotlin` returns nothing). The shape taken is the estate's own, from `apps/custom_watch`'s FACTORY ERASE (§ 378) and PAIR PHONE (§ 432): arm, announce the arm, commit on a second press, lapse on a timer. New pure `ui/ConfirmGuard.kt` (`confirmPress` + `CONFIRM_WINDOW_MS`) with 7 host-JVM tests, the chip relabelling to `discard_confirm` and showing `discard_stake` while armed, both strings in all seven catalogues, and `RecoveryPromptDisclosureTest` extended to pin that the press is graded through `confirmPress`, that `onDiscardRecovery()` appears exactly ONCE in the whole takeover, and that the arm lapses. Verified by breaking it: reverting the chip to `onClick = onDiscardRecovery` fails the new test. Full `:app:testDebugUnitTest` green afterwards — 741 tests, 0 failures.
- [x] **`RotaryScrollWiringTest` pairs rotary scrolls with a `requestFocus` by IDENTIFIER, so a correctly-wired list under any other variable name fails the build.** Found by hitting it: [§ 1154](../architecture/decisions.md)'s new scrolling takeover named its `FocusRequester` `recoveryRotary` and the guard failed, counting 4 `rotaryScrollable(` call sites against 3 matches of the literal regex `rotaryFocus\.requestFocus\(\)`. Renaming to the file's own idiom was the right call for that change and is what shipped, but the guard's claim — "each rotary list needs its FocusRequester focused" — is about the PAIRING, not about a variable name, and as written it also passes vacuously in the opposite direction: two `rotaryScrollable` sites both focusing the same requester count 2 against 2. Widen the second regex to `\w+\.requestFocus\(\)` and, better, match each `rotaryScrollable(...)`'s own `focusRequester = <name>` argument against a `<name>.requestFocus()` in the same composable, so the count is a pairing rather than two tallies. Low priority, no live defect — filed because the next person to add a rotary list will lose the same twenty minutes. Owner tree: `apps/watch_wear/android/app/src/test/kotlin/com/runapp/watchwear/RotaryScrollWiringTest.kt`. **CLOSED 2026-09-05 ([decisions § 1205](../architecture/decisions.md)).** Taken the better of the two options the filing offered — the pairing, not the widened regex. `RunWatchApp.kt` is split into top-level functions (the scope a `remember { FocusRequester() }` lives in), each `.rotaryScrollable(` argument list is brace-matched, its own `focusRequester = <name>` read, and that name required to be created and focused in the same function with no two call sites sharing one. A shared `RotaryWiring` helper replaces the duplicate count-vs-count check `RecoveryPromptDisclosureTest` carried, so the takeover cannot drift back to the identifier form on its own. Not weakened: the total call-site floor stays, and every call site must fall inside a parsed function or the guard fails rather than silently exempting it. Measured on all three shapes — the renamed requester passes the new guard and fails the old one (4 against 3); deleting `RoutePickerScreen`'s `requestFocus()` fails the new guard NAMING that function; and two lists sharing one requester fail the new guard while passing the old one at 5 against 5.
- [x] **Three rails break a tie between two future long runs on the same date three different ways, and one of the three is an invalid comparator.** Found while measuring the `plan_replan` firmware port for [decisions § 1163](../architecture/decisions.md); out of scope for that lane, which owned neither web `training/` nor `apps/mobile_android/lib/plan_replan.dart`. `apps/web/src/lib/training/plan_replan.ts:142` sorts `futureLongRuns` with `(a, b) => (a.scheduledDate < b.scheduledDate ? -1 : 1)` — a comparator that **never returns 0**, so it is not a valid ordering and `Array.prototype.sort`'s stability does not apply to the equal pair; V8's TimSort may put either first. The Dart twin `plan_replan.dart:174` uses `compareTo` (correct, returns 0) but `List.sort` is UNSTABLE, so its tie order is also arbitrary. The firmware `plan_replan.rs:169` scans with a strict `<` and keeps the FIRST encountered in week-then-workout order, which is deterministic and is the only one of the three that is. The input is a plan carrying two non-past, non-taper `kind == 'long'` workouts on the same `scheduled_date` — a double long-run day, or a plan edited to move one — and the consequence is that the make-up distance is added to a different session on each platform. Fix: make web's comparator return 0 on equality and give both web and Dart an explicit deterministic tiebreak (workout id, or the input index the way `routine_history` and `readiness` already do), then pin it in the mirror suites. Pre-existing at port time; not caused by any of the commits § 1163 examined. **CLOSED 2026-09-05 ([decisions § 1200](../architecture/decisions.md)).** Fixed on all three rails, but two thirds of the diagnosis measured out differently. V8's TimSort with that comparator was measured over 260,000 random arrays — 2 to 570 elements, 1 to 5 distinct dates, including shapes built to force galloping — and never once disagreed with a valid stable sort, so the web rail was accidentally deterministic rather than arbitrary; every comparison path in TimSort reads "not less than" as "go right", and the descending-run detector that would break stability refuses to open on an equal pair. Dart is where the divergence was real, and for a reason the filing did not name: `List.sort` is an insertion sort up to 33 elements and a dual-pivot quicksort past it, so it was stable BY LIST SIZE — measured over 20,000 random cases, zero divergences at 33 or fewer and 12,024 past it. `plan_weeks.week_index` has no ceiling and `plan_workouts` no per-week cap, so 34 future long runs is reachable. Both scripting rails now SCAN for the earliest rather than sorting, which is the shape the firmware already had and needs no appeal to any sort's stability — deliberately not the `routine_history` / `readiness` house shape, which those two need because they consume a whole ordered list where this reads only `[0]`. Pinned twice on each of the three suites: the two-workout case, and a 40-element case past Dart's insertion-sort threshold.
- [x] **The Garmin GAP rail has no reference-track golden, so the four-rail window bracket is a three-rail bracket.** [decisions § 1160](../architecture/decisions.md) froze one synthetic switchback on web, both Dart rails and the firmware, and each must report 311 s/km at the shipped 20 m window; `apps/watch_garmin/source/GradeAdjustedPaceView.mc` carries the same `MIN_SEGMENT_M` (held equal by `check_watch_wire_vectors.mjs`) and no equivalent test. Two reasons it was not taken, both real: the Monkey C field is a STREAMING estimator with no batch entry point that grades a whole track, so the fixture would have to be fed sample by sample through `updateGrade` and the result read off `gapPace` — a different test, not a mirror — and there is no Connect IQ SDK on this machine, so anything written there is source-level only and unexecuted (the same caveat § 992 recorded). The firmware's own `GapEstimator` has the same shape and could carry the streaming form first, which would give the Monkey C rail something to mirror rather than invent. Not owned by the lane that filed this. **CLOSED 2026-09-05 ([decisions § 1201](../architecture/decisions.md)).** The streaming objection turned out not to bind: `GradeTracker.onSample` re-anchors on exactly the condition the three batch graders close a segment on, so the fixture is fed to a real tracker one sample at a time and each closed window credited its horizontal length times the factor the tracker's own grade earns it. That reduction returns 311 s/km against a true 302.611 s/km, a 2.77 % cost — the same two figures § 1160 recorded for the other three rails, reached through this rail's own window, clamp and Minetti fit. The eight fixture constants are declared with the same names the TS and Rust rails use and were read through `check_watch_wire_vectors.mjs`'s own `mcConst` regex: all four rails produce byte-identical joined specs. **The Monkey C is still unexecuted** — no Connect IQ SDK here — so it sits BELOW *host-tested* on `quality_standards.md`'s ladder; what was executed is a faithful transliteration on the host, re-run under emulated 32-bit floats (Monkey C's default `Float` width) and reporting the same 311 with 0.448 of rounding headroom. The remaining enforcement is one line in `check_watch_wire_vectors.mjs`, filed below — until it lands the four goldens agree without anything checking that they still do.
- [x] **Three places still name `ApiClient.clipTrackForUser`, which no longer exists.** [§ 1142](../architecture/decisions.md) deleted the method (and `publishPlanAsTemplate`) from `packages/api_client/`, and the two architecture-guard trees were updated in the same change, but three references outside those trees were left because the backend-grants lane does not own `apps/mobile_android/`. (1) `apps/mobile_android/CLAUDE.md` line 91 says `public_run_screen.dart` "routes the fetched track through `clipTrackForUser` for non-owner viewers" — that was **already wrong** before the deletion (the screen calls `fetchClippedTrackForRun`, the `clip-public-track` Edge Function, and has since `20260619_001`), so it is now wrong twice and points a reader at a privacy path that does not exist. (2) `apps/mobile_android/lib/screens/global_segments_screen.dart:479` and (3) `apps/mobile_android/lib/screens/public_route_screen.dart:103` carry explanatory comments naming the method to say why it is *not* used there; both should name `fetchClippedTrackForRun` / `clipRouteForViewer` instead. All three are comment/doc edits, and (2) and (3) need the byte-identical iOS mirror. Owner trees: `apps/mobile_android/` + `apps/mobile_ios/`. **CLOSED 2026-09-05 ([decisions § 1218](../architecture/decisions.md)).** The filing is accurate on all three sites and UNDERCOUNTS the class: a repo-wide `command grep -rn clipTrackForUser` returns **twenty** sites, not three. Two are history and are correct as written (migrations, pgtap comments, ADRs, `api_database.md`). Four were live falsehoods and are fixed here — the filing's three plus `apps/mobile_android/CLAUDE.md:271`, which named it again describing the segment catalogue; both Dart edits are mirrored byte-identically into `apps/mobile_ios/`. The remaining **fourteen live in `.claude/agents/` and `.claude/commands/audit/`**, outside this lane's tree, and are the sharp ones: they are the acceptance criterion an auditor is told to check. Filed separately below.
- [x] **`data.ts` still casts `event_next_instance_going_counts` to `never` for a reason that expired.** `apps/web/src/lib/core/data.ts:3079` calls the RPC as `'event_next_instance_going_counts' as never` with a second `as never` on the params, under a comment reading "Not yet in database.types.ts (orchestrator regenerates on landing the 20270122_001 migration); the RPC name is cast until then." That migration landed — `apps/backend/supabase/migrations/20270122_001_event_next_instance_going_counts.sql` is committed and `database.types.ts` carries the routine — so the casts now do nothing but turn off type-checking on a live call, which is exactly what the generated types exist to provide. Drop both casts and the comment, and let `tsc` check the argument shape. Found while writing `check_stated_function_grants.mjs`, whose call-site reader had to learn to skip a comment sitting between `.rpc(` and its name because of this site. Owner tree: `apps/web/`. **CLOSED 2026-09-05 ([decisions § 1185](../architecture/decisions.md)).** Both casts and the comment are gone, together with the sibling `return data as never` in `fetchRunsOnRoute`, and a guard in `data.test.ts` fails the PR when any `.rpc()` in the file names its function as anything but a bare string literal. The filing was right that the casts were stale and **wrong about what removing them buys**: `core/supabase.ts` calls `createBrowserClient` with no `Database` generic, which the declaration defaults to `any`, so `tsc` checks no `.rpc()`, `.from()` or `.select()` anywhere in the web app and never did. Measured: passing the generic yields 79 svelte-check errors across 10 files. Filed below.
- [x] **`api_database.md` states four measured counts that nothing re-measures, and three of them had gone stale.** The paragraph under "So the only portable withholding names both grantees" in `docs/backend/api_database.md` said the function-revoke guard replays 448 migrations (it replays 469) and that 48 migrations write a function-level `from public, anon` revoke, 36 as `revoke execute` and 12 as `revoke all` (measured 2026-09-04 by replaying the tree through `sql_lex.mjs`: 54 files, 42 and 12). All four were corrected in the § 1139 round, which is the second time this paragraph has been hand-corrected — the numbers go stale on any PR that lands a migration, and no guard reads them. The durable fix is to derive them: have `check_migration_function_revoke_noop.mjs` print the three revoke counts and the migration total, and have a doc test assert the paragraph agrees with its output (the shape `check_shared_constants.mjs` already uses for a constant with two rails). Until then a reader is being given a figure nobody checks. Owner trees: `docs/backend/` + `apps/backend/scripts/`. **CLOSED 2026-09-05 ([decisions § 1181](../architecture/decisions.md)).** Derived, not corrected a third time. `check_migration_function_revoke_noop.mjs` exports `countPortableRevokes` (reusing its own `GRANT_OR_REVOKE` parse, not a second reader) and prints all four beside its OK line; its test suite parses the four out of the paragraph and asserts them, and the assertion was mutation-checked so it cannot report green over prose it no longer matches. **A fourth staleness the filing did not name:** the paragraph as corrected in § 1139 read "48 migrations … (42 as `revoke execute`, 12 as `revoke all`)", and 42 + 12 is 54 — the total disagreed with its own parenthesis without any measurement being needed. Re-measured today: **470 migrations, 55 files, 43 `revoke execute`, 12 `revoke all`** (this round's `20270708000010` adds one to each of the middle two against § 1139's figures). The same shape in `migration_locks.md` § Forward guard — "436 today" and "32 entries", both stale — was deleted rather than corrected, since neither carried anything its own sentence did not.
- [x] **Switching the map matcher to OSRM silently strips `ele`, `ts` and `bpm` from every matched track, and three web surfaces read the matched track by preference.** Measured while closing [§ 1164](../architecture/decisions.md). `OSRMMatcher.Match` in `apps/job_worker/internal/matcher_osrm.go:204` builds its output as `TrackPoint{Lng: c[0], Lat: c[1]}` off the response geometry — the three optional fields on `internal.TrackPoint` (`ele`, `ts`, `bpm`) are dropped, because OSRM's `/match` returns coordinates and nothing else. That is invisible today only because the shipped default is `PassthroughMatcher`, which copies the input points verbatim; the swap is one env var (`OSRM_URL` in `apps/job_worker/main.go:347`), and the trigger in migration `20260609_001` enqueues a `map_match` job for EVERY run with a `track_url`, so the day that variable is set every run's matched track loses its altitude, its timestamps and its heart rate. `/runs/[id]` then degrades in three places at once, because `baseTrack` prefers `matchInfo.track` whenever it has two points: `hasElevation` goes false and the whole Elevation Profile section DISAPPEARS from runs that recorded altitude (`+page.svelte:1117`, `:1749`); `elevations` collapses to all-zero behind it; and `RunMap`'s pace heatmap falls back to the flat single-line render, since it gates on `hasTrackTimestamps(track)` (`RunMap.svelte:541`) over the same preferred track. Two candidate fixes and they are not equivalent. The durable one is in the worker: OSRM's `/match` returns a `tracepoints` array that indexes each input point onto the matched geometry, so the matcher can carry `ele`/`ts`/`bpm` across rather than discard them, and every consumer keeps working with no client change. The narrower one is on the page: source the elevation chart from whichever track actually recorded altitude (`elevationSourceTrack` from `$lib/runs/key_stats` already answers that) and pass `onhover` only when that track IS `baseTrack` — the linked cursor is an index into the track the map draws, and the two index spaces are different lengths once the geometry is re-sampled, so wiring it across them would point the map marker at the wrong place rather than at nothing. Not taken in § 1164's lane: the worker is another tree, and the page-side half needs the index-space decision above plus a matched-track e2e fixture that does not exist yet (nothing under `tests-e2e/` plants a `matched_track_url` with a Storage object). Owner tree: `apps/job_worker/internal/matcher_osrm.go`, or `apps/web/src/routes/runs/[id]/+page.svelte`. **CLOSED 2026-09-05 ([decisions § 1170](../architecture/decisions.md)).** The durable half, in the worker. `matchChunk` now pairs `tracepoints[i]` with `chunk[i]` — the array holds one entry per INPUT coordinate in input order — moves the position onto the snapped `location` and leaves `ele` / `ts` / `bpm` alone; the route geometry is no longer read, so `overview=false`, and `tidy` is switched OFF because every point it drops is now a lost heart-rate sample rather than a tidier line. A mis-indexed `tracepoints` array, an absent one, and a `null` entry each carry the affected points through RAW rather than dropping them, which makes `Match` length-preserving: one output point per input point, in order. The now-unreachable `len(out) < 2` skip is gone. The page-side half is not re-filed — length preservation is exactly the index-space property it was blocked on, and `hasElevation` / `hasTrackTimestamps` are both true again over `baseTrack` with no client change. The matched-track e2e fixture the filing named as missing is still missing and is filed below.
- [x] **The store-write watch has been run over 119 of the mobile suite's 535 test files, not all of them.** [§ 1129](../architecture/decisions.md) armed `flutter_test_config.dart` for every test in `apps/mobile_android/test/`, but the population it was measured against is the 119 files that can reach `serialiseStoreWrite` at all — 64 pairing a temp directory with widget taps, 39 more naming a store class, 4 mocking `path_provider`, and 12 covering the eight screens that construct a store internally. The derivation for the rest is that a store needs a directory and `OfflineSyncStore.init` without an override calls `getApplicationDocumentsDirectory`, which throws in a test that has not mocked it — but that is a derivation, not a run, and this lane is barred from a whole-suite pass on a shared 14 GB box. If a file outside the 119 flags on the first full run it is a real find with its own call site attached, not a false positive; fix it with `pumpUntilStoreWritesSettle` the way § 1131's seventeen were fixed, and do not reach for `allowStoreWritesToOutliveTest`, which is only for a test whose subject is an unsettleable write. Owner tree: whoever runs the next full `flutter test`. **CLOSED 2026-09-05 ([decisions § 1197](../architecture/decisions.md)).** The population was COMPUTED rather than re-derived, and it is **193 files, not 119** -- the watch can only fire in a test whose import closure reaches one of the four call sites of `serialiseStoreWrite` (`local_route_store.dart`, `local_run_store.dart`, `offline_sync_store.dart`, `watch_ingest_queue.dart`; the last is absent from the derivation's own account of itself), and 343 of the 536 files provably cannot. So "run it over all of them" was the wrong question by 343 files, and the 74-file gap between 119 and 193 was where the coverage claim was thin. All 536 were run anyway because the measured cost is small: **42 cgroup-capped batches, 768 s of wall clock, ~7,050 tests, peak RSS 1.42 GB**. It caught **nothing** -- not one file outside the original 119 left a write in flight, so § 1131's fix set was complete. Two timeout clusters (3 cases in `routes_screen_test.dart`, 4 in `routing_test.dart`) were contention from nine agents sharing the box, not finds: both pass alone and on a re-run of their own batch.
- [x] **`showTopBanner` arms a three-second fake timer that only becomes visible once a test waits for the path that arms it, and five tests now carry a four-second pump because of it.** [§ 1131](../architecture/decisions.md) found this by fixing something else: five of the seventeen tests it drained then failed `A Timer is still pending even after the widget tree was disposed`, because `_saveAsMeal` / `_saveAsRecipe` / `_stamp` end by showing a banner and the tests had never reached that far. The pump is honest (the banner's own display duration, a delay that models elapsed time per [§ 715](../architecture/decisions.md)), but it is a line every future test that waits for one of those completion paths will also need, and nothing tells the author until the assertion fires. The durable fix is on the widget: `showTopBanner` could return a handle a test can dismiss, or register its timer somewhere `tearDown` can drain, so the pump stops being folklore. Do not solve it by widening a timeout or by not waiting for the completion path — not waiting is the defect § 1129 exists to catch. Owner tree: `apps/mobile_android/lib/widgets/top_banner.dart` + its iOS twin. **CLOSED 2026-09-05 ([decisions § 1195](../architecture/decisions.md)).** Both proposed fixes were measured impossible: the framework raises the pending-timer assertion from inside `_runTestBody`, before any `tearDown` runs (a scratch test with `tearDown(hideTopBanner)` fails identically), and a returned handle is unreachable from a test whose caller is a screen. The population was also five times larger than filed -- **70 drain sites across 34 files**, each having rediscovered the remedy independently. Fixed on the widget: `_TopBannerWidget` is stateful, arms the dismissal timer in `initState` and cancels it in `dispose`, so a pill whose tree is torn down cancels its own clock and a pill that is never built arms none. Cancel-on-dispose ALONE was not enough and the run said so -- `showTopBanner` only inserts an `OverlayEntry`, so a banner shown on a path the test never pumps again has no `State` to dispose; arming from `initState` is what closes that half. 60 terminal drains deleted; the ten that also do other work stay. Three pinning cases in `top_banner_test.dart`, each verified failing against the old widget.
- [x] **`OfflineSyncStore._loadAll` skips its index write for a fresh empty store partly to dodge a widget-test hazard that is now detectable.** The carve-out's own comment says it "keeps widget tests that init an empty store outside `tester.runAsync` from deadlocking the fake-async zone on the index write" — production behaviour shaped by a test-harness failure mode, of exactly the kind [§ 1129](../architecture/decisions.md) now names at the moment it happens rather than leaving to deadlock. The other two reasons given (nothing to cache, no pointless disk write on a cold load of an unused store) stand on their own, so this is not a bug and the carve-out may well be right; what is filed is that its *stated* justification now cites a hazard the suite reports. Re-read it, drop the test-harness clause if the first two reasons carry it, and pin whichever behaviour is chosen — there is currently no test asserting that a fresh empty store writes no index. Owner tree: `apps/mobile_android/lib/offline_sync_store.dart` + its iOS twin. **CLOSED 2026-09-05 ([decisions § 1196](../architecture/decisions.md)).** Measured, and the filing's premise is wrong: the hazard is NOT detectable. `_readIndex` short-circuits on a missing file, so the index write is the only real-async await a fresh empty store's `init()` would carry -- a probe initing one outside `tester.runAsync` never sees it complete, and a five-file batch of the screens that do this sat at zero CPU for seven minutes printing nothing. That is a HANG, and § 1129's watch fires in `tearDown`, which a test that never ends never reaches. The carve-out therefore stays; its comment now states the mechanism instead of implying the clause is a courtesy. The two production reasons are asserted rather than argued for the first time: three cases in `offline_sync_store_drain_race_test.dart` pin `init()` completing under fake-async pumping alone, no index for an empty store, and a DRIFTED index still corrected -- the skip is conditioned on there being no index, not on the store being empty. The first two were verified failing against the removal.
- [x] **`docs/testing/testing.md` § Patterns documents `pumpUntil` as the way to wait for real-event-loop work and does not mention the write watch or `pumpUntilStoreWritesSettle`.** [§ 1129](../architecture/decisions.md) armed a suite-wide assertion that fails any test ending with a store operation still on the chain, and [§ 1131](../architecture/decisions.md) added the helper that satisfies it when the store belongs to the screen and there is no handle to call `debugWritesSettled` on. A reader of the testing guide hitting that failure for the first time has nothing in the docs to route them; the message itself names the remedy, but the guide is where the rule belongs beside `pumpUntil` and the fixed-delay ban. Add a paragraph under § Patterns naming the watch, the helper, and `allowStoreWritesToOutliveTest`'s single honest use. `apps/mobile_android/CLAUDE.md` has already been updated; `docs/testing/` belonged to another lane this round. Owner tree: `docs/testing/testing.md`. **CLOSED 2026-09-05 ([decisions § 1195](../architecture/decisions.md)).** `testing.md` § Patterns gained a numbered section 5 naming the watch, the three honest waits in preference order (`pumpUntil` on an observable outcome, `debugWritesSettled` from inside `runAsync` when the test holds the store, `pumpUntilStoreWritesSettle` when the store is the screen's), and `allowStoreWritesToOutliveTest`'s single honest use. It also routes the SECOND failure the drain surfaces, correctly: the pending-timer assertion is not a thing to pump past, it is a widget that does not own its own clock. The section intro's "Three patterns" was stale at five before this and now says six.

### Surfaced by the #789 round 38 web-head lane (2026-09-04)

- [x] **`spa_shell_head_signals.test.ts` pins a shell state that no longer exists, and all three of its cases now fail.** Forced by [decisions § 1167](../architecture/decisions.md), which removed `app.html`'s literal `<title>`. Measured with that file's own `countHeadSignals` patterns against this branch: `src/app.html` and `build/index.html` are both `{title: 0, social: 0, canonical: 0, jsonLd: 0}`, and `stripStaleHeadSignals(shell, ['title'])` is now a no-op on the shell. So (1) `the SPA-shell template carries exactly one title…` fails, (2) `a built shell agrees with the template it was generated from` fails, and (3) `only the title strip has work to do against the shell today` fails — its premise is inverted, all FOUR strips are dead against the shell now, not three. **This is the guard working, not breaking**: its own header says a signal disappearing "means a strip lost its subject … worth a deliberate decision", and § 1167 is that decision. Not applied here because `apps/web/src/lib/share/` was another lane's tree this round. The edit: set `SHELL_SIGNALS` to `{ title: 0, social: 0, canonical: 0, jsonLd: 0 }`; rewrite case (3) to assert that stripping ALL FOUR signals is a no-op on the shell (it is — measured), which keeps the case's shape as a direct statement of the injector comments' claim rather than deleting it; and update the four injector comments in `entity_spa_shell.ts`, `share_route_spa_shell.ts`, `share_run_spa_shell.ts` and `share_recap_spa_shell.ts`, which now say the shell carries one title, to say it carries none and that the strips stay because § 1116's landing-page fix makes all four live in one edit. Nothing about the Lambdas' OUTPUT changes: each splices its own title in regardless, so a share page still goes out with exactly one. Owner tree: `apps/web/src/lib/share/`. **CLOSED 2026-09-05 ([decisions § 1190](../architecture/decisions.md)).** Both halves were already on `main` when this round opened, and neither was applied by an integrator: commit `8a414ff79` — the round-38 PR this entry was filed FROM — created `spa_shell_head_signals.test.ts` new, already carrying `{title: 0, social: 0, canonical: 0, jsonLd: 0}` and already asserting that stripping all four is a no-op, and rewrote the four injector comments in the same commit when it extracted `head_splice.ts`. So the filing described a mid-round tree that never landed. Re-measured here rather than taken on trust: all three cases pass against a real build (`npx vite build` then the three named suites, 12 pass / 0 skip / 0 fail), and none of the four injector comments claims the shell carries a title — each defers to the measured guard. **The audit of those four comments is what was actually owed, and it found two real defects the filing did not know about:** the recap injector emitted a canonical it never stripped, and the run injector stripped a JSON-LD the badge path never replaced — see § 1190 and the round-39 section at the end of this file.

- [x] **`seo.md`'s render map is wrong again on the two Learn index routes, in the opposite direction.** [decisions § 1168](../architecture/decisions.md) gave the hub and the six category pages the structured data the row used to over-claim and the round-37 correction then under-claimed. The row corrected on 2026-09-04 reads `| `/learn`, `/learn/category/[category]` | prerendered (`entries()`) | inline `<svelte:head>` | — (none emitted; measured 2026-09-04) |`; it should now read `| `/learn`, `/learn/category/[category]` | prerendered (`entries()`) | inline `<svelte:head>` + `buildLearnCollectionJsonLd` | `CollectionPage` (with an embedded `BreadcrumbList` + an `ItemList` of the guides listed) |`. Not applied here because `docs/features/seo.md` was another lane's file this round and had already been corrected once the same day. Worth stating alongside it that the artifact, not the source, is what `apps/web/src/lib/seo/learn_structured_data.test.ts` now reads — the original drift survived because a correct `Article` builder existed while two of the three routes never called anything. Owner tree: `docs/features/seo.md`. **CLOSED 2026-09-05 ([decisions § 1191](../architecture/decisions.md)).** The replacement row was verified against `buildLearnCollectionJsonLd` and both route files before being applied, not pasted: the hub and all six category pages emit a `CollectionPage` carrying a `BreadcrumbList` and an `ItemList`, measured on a production build (one JSON-LD block per page, `@type` `CollectionPage`). Because this row has now been hand-corrected in both wrong directions in consecutive rounds, the durable half was taken too: `learn_structured_data.test.ts` now READS the row against the same `DECLARED_TYPE` map its artifact cases assert, and that case needs no build, so it binds in `test-web` today rather than inheriting the artifact cases' skip. Proven by planting both historical drifts — the "none emitted" cell and the `Article` cell — and watching each fail.

- [x] **The site root's raw HTML now carries no `<title>`, and the only fix is the four-part landing-page change already filed.** **CLOSED 2026-09-05 ([decisions § 1268](../architecture/decisions.md)).** The stated dependency is real and it did close this box with nothing further to do: `build/index.html` is the prerendered landing page now and carries `<title>Threkir - track anywhere, review everywhere</title>` in its raw HTML, measured on a production build. The untitled document did not disappear - it moved to `build/200.html`, which CloudFront serves for deep links only and no longer for the site root, so the consequence § 1167 weighed and took is paid back on the surface it was taken from. Original filing below. Stated consequence of [decisions § 1167](../architecture/decisions.md), weighed and taken rather than discovered. `build/index.html` is adapter-static's SPA fallback; it renders no components, so `%sveltekit.head%` contributes only the hash-CSP meta and the modulepreload links and a root-layout default cannot reach it. CloudFront serves that file as the site root and as the body of every deep-link 403 ([§ 1022](../architecture/decisions.md)), so a consumer that does not execute JS — a social unfurler above all — now gets no title for `https://threkir.com/` where it used to get `Threkir`. The cost is bounded: the shell's body is empty and it carries no canonical, description or JSON-LD either ([§ 1116](../architecture/decisions.md)), so it was never indexable in any useful way; Google and Bing render JS and reach the real `SeoHead` on `/`; and the five share Lambdas splice their own title in, so no share page is affected. **The fix is not to put a literal back** — that reinstates the duplicate on fifteen pages — it is § 1116's four-part change, whose part (1), `export const prerender = true` in `apps/web/src/routes/+page.ts`, makes `index.html` the real landing page with a full head, at which point this entry closes with nothing further to do. Owner trees: `apps/web/src/routes/+page.ts` + `apps/web/svelte.config.js` + `apps/web/lambda/*/build.mjs` + `infra/modules/web-stack/`.

- [x] **Five more artifact assertions self-skip in CI for the reason already filed against the shell guard.** `apps/web/src/lib/seo/document_title.test.ts` and `apps/web/src/lib/seo/learn_structured_data.test.ts` ([decisions § 1167](../architecture/decisions.md) + [§ 1168](../architecture/decisions.md)) each read `apps/web/build` only when a build is present — verified here by parking `build/` and watching five of the seven cases report `# no apps/web/build`. `test-web` never builds and `build-web` never runs them, exactly as the round-37 filing for `spa_shell_head_signals.test.ts` describes. The two source-level cases in `document_title.test.ts` do bind in `test-web` (they read `src/app.html` and `src/routes/+layout.svelte`), so the duplicate-title regression cannot silently return; what is uncovered is the artifact half — the JSON-LD claims in particular, which have NO source-level twin and are therefore ungated in CI today. Fold into the step that filing already asks for: one step in `build-web` after `Build web (production)` running `npx tsx --test src/lib/share/spa_shell_head_signals.test.ts src/lib/seo/document_title.test.ts src/lib/seo/learn_structured_data.test.ts` from `apps/web`, with its own `::error::` per the bundled-job rule that job is already subject. Owner tree: `.github/workflows/`. **CLOSED 2026-09-05 ([decisions § 1210](../architecture/decisions.md)).** Folded into the one `build-web` step the shell-guard filing above asks for, exactly as this entry directs. The count was understated, and measuring it is what showed by how much: parking `apps/web/build` and running all three suites prints `# no apps/web/build` **7** times, not five — the shell guard's own artifact case and `spa_shell_head_signals`'s template-vs-shell agreement case are in the same state. All 11 pass against a real build, so nothing in the three suites is currently red against an artifact.

### Surfaced by the #789 round 39 backend-sql lane (2026-09-05)

- [x] **`apps/backend/CLAUDE.md` describes the online-safety guard as catching only the missing `NOT VALID`, which is now half of what it does.** [§ 1180](../architecture/decisions.md) gave `check_migration_online_safety.mjs` a second violation kind, `same_txn_validate` — a `VALIDATE CONSTRAINT` scanning a guarded table while an earlier statement in the same file still holds a write-blocking lock on it. Two passages in that file now understate the guard: it "fails a migration that adds a CHECK/FK to a guarded table without `NOT VALID`" (§ Migration locks, ~line 139) and, of the `notifications` / `jobs` widen shape, "the add-half trips its check" (~line 182). The second is the more misleading of the two, because it is written as advice to someone about to write the widen: the correct instruction is `ADD … NOT VALID` in this migration and `VALIDATE CONSTRAINT` in a **later** one, and a same-file two-step now fails. The `GRANDFATHERED_VIOLATIONS` sentence is also one key short — entries are `{filename, kind, table, constraint}` now, with `kind` in the key so an exemption for a blocking add cannot silently cover a same-transaction validate in the same file. `docs/backend/migration_locks.md` (this lane's tree) is already correct and is where the detail lives; these are the pointers into it. Not taken here: `apps/backend/CLAUDE.md` was outside this lane's tree. Owner tree: `apps/backend/CLAUDE.md`. **CLOSED 2026-09-05 ([decisions § 1183](../architecture/decisions.md)).** Granted this tree in a second wave and closed. **The filing understated it.** Both passages were corrected and the `GRANDFATHERED_VIOLATIONS` sentence now names the `{filename, kind, table, constraint}` tuple and says the list is for migrations already applied to prod — but the section's closing `sql` block, introduced by "Use the two-step", was itself a same-file `drop` + `add … not valid` + `validate`: the exact shape the guard now fails, with an inline comment ("separate; SHARE UPDATE EXCLUSIVE, writes pass") asserting the thing § 1180 disproved. A reader copying this file's own example would have written a migration that fails CI. It is now two fenced files. **And the two cited exemplars were half wrong**: `20261124_001_content_length_caps.sql` → `20270503_001_free_text_length_caps.sql` is a genuine cross-file split and is now cited with both halves named, but `20260621_001_runs_track_url_path_check.sql` validates in the SAME transaction and is one of the 28 entries § 1180 grandfathered as `same_txn_validate` — the file was naming a violation as the model to copy, and it is now called out by name rather than quietly dropped. Neither the block nor the exemplar list mentions the guard, so a search for references to it would not have found either.

- [x] **`anon_execute_contract_test.sql`'s `server_only` fixture is hand-kept, and 16 non-trigger `public` functions revoked from `authenticated` sit outside it — two of them in the very cron/job-queue family it is the positive control for.** Measured this round by replaying every migration's function-level revokes: 42 non-trigger `public` functions carry a revoke naming `authenticated`, and the fixture lists 26. Absent: `award_achievements_for_user`, `clip_track_for_user`, `delete_user_integration_secrets`, `delete_user_provider_secrets`, `enqueue_safety_overdue_emails`, `notify_data_export_ready`, `privacy_aware_route_geom`, `privacy_aware_start_point`, `privacy_distance_m`, `privacy_in_any_zone`, `recompute_event_ranks`, `refresh_club_member_count`, `refresh_gym_workout_totals`, `refresh_route_run_count`, `sweep_challenge_completions`, `try_consume_strava_quota`. The file's header does justify a subset — "264 pgtap files exercise those functions as `authenticated`, so an over-broad revoke fails in the suite that covers the feature" — but that answers only the **over**-revoke hazard, which is assertion (6)'s direction. It does not answer (5)'s: nothing asserts that a signed-in account cannot reach `enqueue_safety_overdue_emails` or `sweep_challenge_completions`, because a suite exercising a function as `authenticated` proves the opposite claim. Assertions (1) and (2) close anon for the whole schema; there is no whole-schema rail for `authenticated` at all, so a future cron function whose migration forgets `authenticated` in its revoke list is caught by nothing. This is [§ 1182](../architecture/decisions.md)'s class a third time — a list that vouches for what it names and is silent about what it omits. **Not fixed here, deliberately:** `keeps_service_role` has to be true only where `service_role` really holds EXECUTE at the END state, and this lane held no Supabase stack, so adding 16 rows would have been authored blind against assertions (5) and (6) at once. The durable fix is not a longer fixture either — it is a replay of the migration text in the shape `check_migration_function_revoke_noop.mjs` already uses, deriving the family from what the repo STATES rather than from the catalogue the assertion reads (deriving it from the catalogue makes (5) vacuous by construction). Owner trees: `apps/backend/supabase/tests/` + `apps/backend/scripts/`, for a lane that can run pgtap. **CLOSED 2026-09-05 ([decisions § 1233](../architecture/decisions.md#1233-the-server_only-fixture-is-derived-from-the-migration-text-now-and-the-cron-family-has-a-rail-that-no-list-can-go-stale-against)).** The filing's measurement was exact — 42 non-trigger `public` routines carry a revoke naming `authenticated`, the fixture listed 26, and the sixteen absent are the ones named. The durable fix is the one the filing proposed: `apps/backend/scripts/check_server_only_registry.mjs` derives the family by replaying the migration TEXT (reusing `statedGrants`) and fails the PR on a routine the repo withholds but the fixture omits, a fixture row no migration revokes, or a `keeps_service_role` the migrations contradict — so the pgtap assertions still grade the CATALOGUE against a list derived from a different source, and (5) does not become vacuous. `keeps_service_role` comes from the replay rather than a catalogue reading for the reason the previous lane could not resolve without a stack: on CI's image every fresh routine arrives with a `service_role` entry by name. Text and catalogue agreed on all 42 rows and all 42 flags, measured. **The filing's own gap is closed separately**: a guard grading statements cannot demand a revoke nobody wrote, so pgtap assertions (7)/(8) derive the cron family from `cron.job` — 13 routines — which cannot be satisfied except by unscheduling the job. Mutation-tested on both sides, 4 + 5 planted defects.

- [x] **`jobs_kind_allowlist_test.sql` still holds two lists where one would do.** [§ 1182](../architecture/decisions.md) added an assertion that the CHECK's admitted set equals the set the file exercises, which turns a silent omission into a loud failure — but the exercised set is still sixteen hand-written `lives_ok` calls beside a hand-written expected string. The single-list form drives each `lives_ok` off a fixture table carrying `(kind, payload)`, so the rows ARE the assertions and the same fixture is what the CHECK is compared against; `plan(N)` then self-enforces, because a fixture row added without bumping it is a pgtap plan mismatch. Not taken here: this lane held no Supabase stack and CI is what runs pgtap, so rewriting nineteen assertions blind onto a ten-lane integration branch was the wrong trade against one reasoned-about `is()`. Owner tree: `apps/backend/supabase/tests/`, for a lane that can execute it. **CLOSED 2026-09-05 ([decisions § 1237](../architecture/decisions.md#1237-four-smaller-corrections-and-the-bookkeeping-obligation-a-new-migration-actually-carries)).** A `job_kind (kind, payload)` fixture table now drives every `lives_ok` and is the same set the CHECK is compared against, so the two lists became one and `plan()` self-enforces. Four planted violations — a dropped fixture row, a fixture kind the CHECK forbids, a misspelled kind, and a CHECK widened with no fixture row — each caught, on the comparison assertion and, where the count moved, on the plan too.

- [x] **`apps/backend/CLAUDE.md`'s seed-fixture row counts for `runs` and `routes` are a judgement, not a measurement, and nothing re-derives either.** [§ 1181](../architecture/decisions.md)'s rule was applied to that file this round: four tooling counts nothing re-derives were deleted (`migrations/ # ~50 files` against a tree of 470, with the same line already telling the reader `ls supabase/migrations/`; "rebuilt this way ~12 times (latest `20270218_001_auto_hide_reports.sql`)" — measured 14 files rebuild `notifications_kind_check` and the latest is `20270701000001_refund_failed_notification.sql`, both stale, and the sentence's claim is recurrence rather than a tally; "All 30 existing postgis/pg_trgm migrations carry this header", where 35 files carry it and the population the "All" quantifies over cannot be defined by grep without over-counting comments and function bodies; and "~34s over 168 assertions" for the pgtap refusal guard, which prints its own assertion and file counts). One count was **corrected** rather than deleted — the seed food-log entries, stated as 17 against the 16 rows the single `INSERT INTO food_log` inserts, confirmed by three independent counts; `3 gym workouts`, `23 sets`, `4-point body-metrics` and `2 connected integrations` were each re-measured and are right. **`12 runs` and `5 routes` were deliberately left alone**: `seed.sql` carries ten-plus `INSERT INTO runs` statements (several `INSERT … SELECT`) staged for individual e2e scenarios, so the total row count is not what those figures mean — they describe the primary set a reader sees after a reset, which is an editorial claim no static count settles. Stating a number measured against the wrong population is how the `api_database.md` paragraph came to read "48" beside "42 as `revoke execute`, 12 as `revoke all`", so the two were not touched on a measurement that would not have supported them. Someone with the stack up can settle both against a reset database in one query. Owner tree: `apps/backend/CLAUDE.md`. **CLOSED 2026-09-05 ([decisions § 1237](../architecture/decisions.md#1237-four-smaller-corrections-and-the-bookkeeping-obligation-a-new-migration-actually-carries)).** Settled against a reset database, as the filing asked. Both figures were wrong and so was the sentence above them: `seed.sql` provisions **three** users, not "exactly one" (`alex@test.com` and `morgan@test.com` back the social surfaces), and `runner@test.com` holds **225 runs across five sources** (`app`, `strava`, `healthkit`, `parkrun`, `race` — the file listed four) **and 43 routes**; 251 runs / 45 routes across all three users. Per § 1181 the numbers come out rather than being corrected, because they describe a fixture that grows with every e2e scenario; the query that re-derives them is stated in their place, with the measured totals and their date as a footnote.

### Surfaced by the #789 round 39 docs-registry lane (2026-09-05)

- [x] **Fourteen agent and command definitions tell an auditor to verify a symbol the tree deleted.** [§ 1218](../architecture/decisions.md) fixed the four live references to `ApiClient.clipTrackForUser` inside `apps/mobile_*`; the remaining fourteen are outside that tree and are the ones that matter, because they are an **acceptance criterion**, not a description. `.claude/commands/audit/privacy-zones.md` (its `description:` frontmatter, its opening rule, and its fail-closed step 4), `.claude/commands/audit/README.md`, `.claude/commands/audit/auth.md`, `.claude/agents/code-reviewer.md`, `.claude/agents/auditors/repo-security-auditor.md`, `.claude/agents/ui-polisher.md`, `.claude/commands/polish-ui.md` and six persona hunters under `.claude/agents/personas/races/` each instruct a reader to check that every non-owner track render "routes through `clipTrackForUser`". No client holds that method: the only client clipping path for a run is `fetchClippedTrackForRun` (the `clip-public-track` Edge Function) and for a route `clipRouteForViewer`. An audit whose criterion names a symbol the tree does not have either reports clean having checked nothing, or reports the absence as a privacy defect. Every one is a text substitution; `/audit/privacy-zones` should also gain the owner-vs-non-owner split the real path has. Owner tree: `.claude/`. **CLOSED 2026-09-05 ([decisions § 1261](../architecture/decisions.md)).** **Sixteen, not fourteen** — ten race-persona hunters, not six. Every one now names the path the tree has, and the filing's own replacement was half wrong: the route helper is `fetchClippedRouteForViewer` on web and `ApiClient.clipRouteForViewer` on mobile. `/audit/privacy-zones` gained the two-path split and a fail-closed step that says which way each of the four helpers fails, because they are not uniform.
- [x] **No guard re-derives the `ci.yml` job count stated in the root `CLAUDE.md`, and it has been wrong after most rounds that added a job.** [§ 1217](../architecture/decisions.md) measured the current value (34 job keys, `ci-gate` needing the other 33) and corrected the prose, but the number will drift again on the next added job exactly as it has before. `scripts/check_ci_diagnostics.mjs` already computes it — `parseJobKeys(readFileSync('.github/workflows/ci.yml'))` — so the guard is a handful of lines in a file that already runs in `workflow-lint`: read the root `CLAUDE.md`, match `/ci\.yml\s+→ the same (\d+) CI jobs/`, and fail when the captured number is not `parseJobKeys(...).length`, with a second assertion that the block still matches at all (so a reworded line fails loudly rather than passing vacuously). Adding it makes `check_ci_diagnostics.test.mjs`'s job a bundled one under the § 764 rule, so the new step needs its own `::error::`. Owner tree: `scripts/`. **CLOSED 2026-09-05 ([decisions § 1260](../architecture/decisions.md)).** Rule 6 in `scripts/check_ci_diagnostics.mjs`. Both stated figures are correct today and are two different claims, not one: `parseJobKeys` reports **34** job keys and `parseNeeds(ci.yml, 'ci-gate')` **33** entries, so the guard checks each against its own sentence. A phrase that matches nothing is a hard error. No workflow step was added, so no job becomes bundled under § 764.
- [x] **Nothing re-derives the per-file test counts in `test_inventory.md`, and 49 of 111 were wrong.** [§ 1217](../architecture/decisions.md) re-measured the census section, but that is a snapshot and the next round that adds a test makes it stale again. The file already prescribes the recompute command, which is what makes a guard cheap: walk every `### \`<path>\` — N tests` heading ABOVE the `Suite totals` divider, resolve the path, count `test(` / `testWidgets(` (Dart), `Deno.test(` (Deno), `test(` / `it(` (web/scripts), `func Test` (Go) or `plan(N)` (pgtap), and fail on a mismatch. Two carve-outs are required and both are already stated in the file's own text: a heading whose text declares a parameterised count (`l10n_generated_parity_test.dart`'s "1 + 3 per catalogue", `catalogues.test.ts`'s one per locale) is a RUNTIME count and must be skipped rather than replaced — a naive sweep would have overwritten two correct numbers with wrong ones — and every heading BELOW the divider is a per-round historical record and is out of scope. A heading naming a path that no longer exists should fail too; twenty do today, all of them below the divider. Owner tree: `scripts/` + `docs/testing/`. **CLOSED 2026-09-05 ([decisions § 1259](../architecture/decisions.md)).** `scripts/check_test_inventory_counts.mjs` — **225 figures across 149 headings** now re-derived, including the 78 per-file bullets the filing did not reach. The population is anchored on the heading NAMING A FILE, so deleting the number fails and so does deleting the path. Both runtime counts are in `PARAMETERISED` rather than corrected. Correcting the rest found more than filed: 12 headings named a web helper that had moved into a subdirectory (`segments.test.ts` stated 8 against 44), one named a renamed file, four aggregates put their figures where nothing could attribute them, and 48 counts were stale.
- [x] **`docs/product/parity.md`'s snap-to-road row still calls `routing.dart` a "Dart port of `apps/web/src/lib/routes/routing.ts`".** [§ 1218](../architecture/decisions.md) corrected the same claim in the Dart header and in `apps/mobile_android/CLAUDE.md` and recorded why the two are deliberately not a lockstep pair; `docs/product/parity.md:114` was outside that lane's tree and still carries the sentence, plus a "9 unit tests" figure for a suite that holds 36. One row, one edit. Owner tree: `docs/product/parity.md`. **CLOSED 2026-09-05 ([decisions § 1262](../architecture/decisions.md)).** The row now records why the two are deliberately not a lockstep pair (§ 1119 deleted web `routing.ts`'s three helpers), and the test figure is re-measured: `test/routing_test.dart` declares **36**, not 9.
- [x] **`apps/job_worker/deployment.md:229` opens five table columns against a three-column header, from two unescaped pipes inside a code span.** The cell writes `` `{format: 'csv'|'gpx'|'backup'}` ``; GitHub's table parser splits a row on every pipe that is not `\|`-escaped, and a code span is no shelter — so the row renders with the `Notes` prose torn into three extra cells and the reader is shown a mangled table beside two correctly-rendered rows. Fix is `'csv' \| 'gpx' \| 'backup'`, the form `docs/backend/metadata.md` and `docs/backend/settings.md` already use correctly in 16 of their own rows. Found by counting unescaped-pipe-delimited cells against the header across all 349 markdown tables in the repo (2,638 body rows); this is the only one, so it is a one-line fix, not a class. Owner tree: `apps/job_worker/`. **CLOSED 2026-09-05 ([decisions § 1262](../architecture/decisions.md)) — the filing was already stale.** The pipes are escaped at `b7f3467a3`, the base commit this round was cut from, so the fix landed in the same PR that filed it and the box was never ticked. Re-measured by the filing's own method: across **369 markdown tables and 2,845 body rows**, counting `|`-delimited cells with backslash escapes honoured, **zero** rows disagree with their header.
- [x] **261 of the repo's 4,085 relative markdown links do not resolve, and 249 of them are in `decisions.md`.** Nothing in CI checks a prose link. Three classes, measured: **240 bare `#NNN` anchors** — the in-file citation idiom `[§ 739](#739)`, which never resolved, because GitHub's slug for `## 739. Title…` is `739-title…` and not `739`; **14 full-slug anchors that resolved once and stopped** when the heading was reworded, which is the silent half — `[§ 359](#359-climb-detection-is-hysteretic-on-both-edges-…)` against a heading now reading "Climb detection answers two questions — …", plus `[§ 33](#33-privacy-zones-live-in-usersettings-…)` where the slug drops an underscore GitHub keeps, and `privacy.md`'s `#the-erase-378` against a `## The erase (§ 378)` whose real slug carries two hyphens; and **2 genuinely missing files** — `docs/features/integrations.md:584` links `features.md#gpx--kml-import` and there is no `docs/features/features.md`, and `SUPPORT.md`'s `../../issues` is a GitHub-web-relative link that only resolves on github.com. (The other five "missing" hits are false positives of any such checker: `[N](16)` inside a wire-format description, and an illustrative `[Apr 25 long run](../../../../../../runs/<id>)` in prose.) The durable fix is a `scripts/check_doc_links.mjs` in `workflow-lint`: resolve every relative target on disk, and for a `.md` target with a fragment, slug its headings the way GitHub does — lowercase, drop everything outside `[A-Za-z0-9_ -]`, then map **each** space to a hyphen (collapsing runs of spaces is the mistake that makes such a checker report 30 false dead links on a clean tree, which is what happened before this measurement was trusted). Bare `#NNN` is 92 % of the finding and is one mechanical rewrite; do it in the same change or the guard cannot be turned on. `decisions.md` is append-only by convention, so the rewrite needs an explicit owner decision. Owner tree: `scripts/` + `docs/architecture/decisions.md`. **CLOSED 2026-09-05 ([decisions § 1258](../architecture/decisions.md)).** `scripts/check_doc_links.mjs` plus the rewrite, in the same change. Re-measured over a wider population than the filing's — every file `git ls-files '*.md'` returns, `.claude/` included: **4,402 relative links across 324 files, 303 dead**, not 261. 263 bare anchors (all in `decisions.md`), 10 reworded, **30 missing files** rather than 2 — 29 of them race-persona hunters walking up three levels to `reviews/README.md` from five below the root. Rewritten to full slugs deliberately, because a rot CI catches in the PR that causes it is a different thing from a rot nobody can see; the guard derives the correct slug and names it in the failure. The five "false positives" the filing predicted did not arise — they sit inside code spans, which the checker blanks.

- [x] The `node-version: 24` inputs track a major line rather than an exact
  patch. Left as-is: a Node minor/patch break is loud and local to the job
  that runs it, not a silent change to what gets built or shipped. **CLOSED 2026-09-05 ([decisions § 1214](../architecture/decisions.md)) — pinned, and the stated reason for leaving it was half wrong.** "Not a silent change to what gets built or shipped" does not hold: `release-web.yml` builds the shipped web bundle under the same floating input, so the production artifact was compiled by whichever 24.x the runner resolved that morning. The other half is the argument § 595 rejects for Flutter and `check_toolchain_pins.mjs`'s own header rejects for Deno, and Node is the runtime under ~30 `node --test` guard suites, `tsc` and every `vite build` here. All 22 steps across four workflows now name **24.20.0**, the current Krypton LTS, verified against `nodejs.org/dist/index.json`. No file outside `.github/` needed to change: the guard compares `.tool-versions`' `nodejs` line on the major alone by design, so `nodejs 24` still matches, and it now reports `22 setup-node step(s) on Node 24.20.0`.

### Surfaced by the #789 round 39 infra-ci lane (2026-09-05)

- [ ] **`gitleaks` and CodeQL run on every PR and block no merge, because the required check can only wait for jobs in its own workflow.** **gitleaks half CLOSED 2026-09-05** ([decisions § 1264](../architecture/decisions.md)); the CodeQL half stays open and is the OWNER's, sharpened below. **The filing's stated obstacle to folding gitleaks does not exist.** "Both upload SARIF to the Security tab" is true of `security.yml` and false of `gitleaks.yml`: read at the pinned SHA `e0c47f4`, none of `gitleaks-action`'s four source files contains `code-scanning`, `sarifs` or `security-events` — it writes a job summary, uploads a SARIF ARTIFACT this repo already disables, and `process.exit(1)`s on a detection. So its exit status was the entire signal and nothing waited for it. It is now CALLED from an ungated `gitleaks` job in `ci.yml` named in the `CI gate`'s `needs:` (the § 1149 shape), measured at 18 s against a gate critical path of 18 m 34 s; the unexercised `security-events: write` scope is gone and `GITLEAKS_ENABLE_COMMENTS` is off rather than failing a POST `pull-requests: read` cannot authorise. **The CodeQL half was wrong about what a fold would buy, and is not taken.** `github/codeql-action/analyze` at the pinned SHA declares 18 inputs and none is a severity threshold, so an alert never turns the job red — folding would gate on the analysis COMPLETING, not on what it found. And `getAnalysisKey()` is `${workflowPath}:${jobName}` resolved from the RUN's workflow, so a fold re-keys all four analyses at once, the exact churn `security.yml`'s header records being burned by. **The ask for the owner is therefore narrower than the filing's:** in branch-protection / rulesets, ADD the code-scanning results requirement (the `CodeQL` check run, not the four `analyze` job names) at whatever severity threshold is wanted, and leave the required-status-check set at exactly `CI gate` — which now covers gitleaks. Nothing in this tree can read or make that change. Original filing below. Branch protection requires exactly one context, the `CI gate` aggregator in `ci.yml`, and a `needs:` entry is scoped to its own workflow — so `gitleaks.yml`'s scan and `security.yml`'s four CodeQL legs are advisory in practice, however red. That is [§ 775](../architecture/decisions.md)'s shape, and the repo has already applied the fix twice by FOLDING the workflow in ([§ 771](../architecture/decisions.md) for the bundle budget, [§ 862](../architecture/decisions.md) for env-isolation). Neither of these two folds the same way: both upload SARIF to the Security tab, `security.yml` deliberately runs Trivy at `exit-code: '0'` and reports rather than blocks ([§ 8703 region](../architecture/decisions.md)), and CodeQL's own triggers were deliberately widened to every commit for Scorecard's SAST check. So the durable answer here is probably **branch protection**, not a fold: add the `gitleaks` job and the CodeQL `analyze` matrix as additional required contexts. That is a repo-settings change no file in this tree can make, which is why it is filed rather than taken. Measure before deciding: a secret committed to a PR today produces a red `gitleaks` row and a mergeable PR. Owner: repo settings (+ a line in [decisions § 81](../architecture/decisions.md) recording that the required set is no longer exactly one). **CodeQL half re-measured and re-homed 2026-09-06 ([decisions § 1305](../architecture/decisions.md)); still open, because it is still a repo setting.** Round 41 was pointed at this with "the § 1149 caller-job precedent is very likely the shape of the answer here", so § 1264's conclusion was re-derived rather than inherited: read against `analyze/action.yml` at the pinned SHA, the action declares 18 inputs — `check_name`, `output`, `upload`, `cleanup-level`, `ram`, `add-snippets`, `skip-queries`, `threads`, `checkout_path`, `ref`, `sha`, `category`, `upload-database`, `post-processed-sarif-path`, `wait-for-processing`, `token`, `matrix`, `expect-error` — and not one is a severity threshold. § 1264 stands; a fold gates on the analysis completing and re-keys every analysis to buy it. **What was missing was not another measurement.** The ask has lived in this bullet since round 40, which is a place engineers read and operators do not, and the repo carried no document at all describing what protects `main`. `docs/ops/deployment.md` now has a `Merge gates` section: the required set, why `terraform.yml` and `gitleaks.yml` are called rather than triggered, the setting to enable (the code-scanning **results** requirement on the `CodeQL` tool), the cheaper halfway option stated as a different guarantee rather than a weaker one (adding the four `analyze` job names gates on the scan RUNNING — worth having, since a broken extractor fails those jobs and nothing else in CI would notice), and the two things that must change with the setting because nothing in the repo can observe it. One correction to the original filing: it asked for "a line in decisions § 81"; § 81 is the custom watch's five-button input layout, and no ADR describes the required-context set, which is why the record went to `docs/ops/`. **Re-derived a third time 2026-09-06 — [decisions § 1356](../architecture/decisions.md) — still open, still a repo setting.** The load-bearing claim was re-read first-hand against `analyze/action.yml` at the pinned SHA rather than inherited: 18 inputs, none a severity or alert threshold. What HAD been weighed wrongly is that the two guarantees were treated as one. The cheaper halfway option ("gates on the scan RUNNING — worth having, since a broken extractor fails those jobs and nothing else in CI would notice") was framed as another branch-protection change and therefore equally out of reach; but the part of it that has actually bitten this repo is narrower and IS reachable, and requiring the `analyze` jobs would not have caught it. Twice a compiled leg reported clean over source it never read (§ 1304, § 1352) with the `analyze` job GREEN. `scripts/check_codeql_coverage.mjs` now fails that state from inside the gate. What remains outside it is a leg that fails outright or a bad `queries:` value — loud states, unlike the silent one. The fold's cost is written into `docs/ops/deployment.md § Merge gates` so it is not re-derived a fourth time: it re-keys all four analyses, and `security.yml`'s own `push`/`pull_request` triggers would have to come off in the same change or two uploads to one SARIF category make the alert set flip-flop.
- [x] **`.tool-versions`' `nodejs` comment says "CI states a major", which stopped being true this round.** [§ 1214](../architecture/decisions.md) pinned all 22 `setup-node` steps to `24.20.0`; the file's header block still reads `nodejs — every workflow's actions/setup-node step (major only; asdf wants a resolvable version, CI states a major)`. The parenthetical's first two clauses are still right — the COMPARISON is major-only, by `check_toolchain_pins.mjs`'s design, and that is what lets `nodejs 24` keep matching — but the last clause now describes the state before this round. One clause, e.g. `(compared on the major only; asdf wants a resolvable version, and CI states an exact patch)`. Not taken here because `.tool-versions` was outside this lane's tree. Owner tree: `.tool-versions` (root). **CLOSED 2026-09-05 ([decisions § 1216](../architecture/decisions.md)) — and the comment was the smaller half.** The clause is corrected, but re-reading `toolVersionAgrees`'s own carve-out showed all three of ITS stated reasons had gone false too: `24` is the spelling asdf cannot resolve and `24.20.0` is not, § 1214 made CI name the patch, and there is no runner-image bump left to absorb because CI no longer follows the image. So `nodejs` is now compared EXACTLY like every other plugin and `.tool-versions` reads `nodejs 24.20.0`. Measured rather than argued: under the old major-only comparison `nodejs 24.0.0` against a CI pin of `24.20.0` PASSES — twenty minor releases apart, called equal — and under the exact one the guard fails at exit 1 naming both versions. A pin nothing compares is the class this lane spent both waves closing. Two test changes: the carve-out's own test is replaced by one pinning the exact rule (including the `24.0.0` hole), plus a committed-source test that `.tool-versions`' nodejs line equals the one version every `setup-node` step names.

- [x] **`check_ci_diagnostics.mjs` reads `.github/workflows/` and not `.github/actions/`, so the two composite actions' `run:` blocks are outside all five of its rules.** `readWorkflows(WORKFLOW_DIR)` is the subject of rules 1-5; `check_workflow_binaries.mjs` reads BOTH directories (it carries its own `ACTION_DIR`), and `check_toolchain_pins.mjs` reads the action files too. Measured today the gap is empty — neither `start-supabase/action.yml` nor `install-playwright/action.yml` carries a `failure()`-conditioned diagnosis or a backtick inside a double-quoted echo — so this is a scope hole, not a live defect. Widening `readWorkflows` would change rules 1-4's subject as well as rule 5's, which is a bigger claim than a hunt round should make unreviewed: rule 3 (every job named in the gate's `needs:`) is meaningless for a composite action, so the widening needs a per-rule subject rather than one file list. Owner tree: `scripts/check_ci_diagnostics.mjs`. **CLOSED 2026-09-05 ([decisions § 1215](../architecture/decisions.md)).** Taken as the filing framed it — a per-rule subject, not one wider file list. Rules 1 and 5 read both directories; rules 2, 3 and 4 stay workflow-only because bundling, a `needs:` entry and the gate's own verdict are all properties of a JOB, which a composite action does not have. `RULE_SUBJECTS` records the split with a reason and the guard prints it, and an empty action list is now a failure rather than a quiet skip. `parseSteps` and `parseActionSteps` share one collector, and the refactor was proved neutral: 437 steps across the 21 committed workflows parse byte-identical to the previous implementation. **The filing's "measured today the gap is empty" held** — rule 1 finds no `failure()` diagnosis in either action (the repo-wide count stays 9) and rule 5's 126 further shell lines (2,046 → 2,172) are all clean, so this closed a scope hole and not a live bug. Mutation-tested end to end instead: a backtick-in-double-quotes echo and an unscoped `if: failure()` diagnosis planted in `install-playwright/action.yml` each fail the guard at exit 1, at the right line, naming the composite action rather than a job. 10 new cases (53 total).

### Surfaced by the #789 round 39 web-seo lane (2026-09-05)

- [x] **The `Mode` cell of the Learn render-map row names `entries()` for a route that has none.** `docs/features/seo.md` reads `| `/learn`, `/learn/category/[category]` | prerendered (`entries()`) | …` for the two index routes, but `apps/web/src/routes/learn/+page.ts` is a STATIC route carrying `export const prerender = true` and no `entries` export at all — only `learn/category/[category]/+page.ts` and `learn/[slug]/+page.ts` have one, because only they have a dynamic segment to enumerate. The cell is not wrong about the mode (both routes do prerender), only about the mechanism for one of the two, so it was left alone rather than churned a third time in two days by the lane that had just corrected the row's other two cells. The [§ 1191](../architecture/decisions.md) guard reads the STRUCTURED-DATA cell only; widening it to the mode cell means teaching it what each route file exports, which is a different and larger claim than the one that kept drifting. Owner tree: `docs/features/seo.md`. **CLOSED 2026-09-05 ([decisions § 1193](../architecture/decisions.md)).** Taken further than filed, because the reason the cell could not be right is that ONE row described TWO routes whose modes differ. `/learn` and `/learn/category/[category]` now have a row each, so every cell states something about a single named file, and the § 1191 guard is widened from the structured-data cell to the whole row: the mode cell must name `prerendered` exactly when the loader exports `prerender = true` and `entries()` exactly when it exports `entries`; the head-owner cell must name `<svelte:head>` exactly when the page uses one and must name the JSON-LD builder the page actually calls and only that one (which also earned `/learn/[slug]` its `buildGuideJsonLd`, previously unnamed). All three cells are now derived from route source, so still no build is needed and the case binds in `test-web`. Proved on five planted drifts: both directions of the `entries()` claim, a head-owner cell that stops naming its builder, and the two historical structured-data drifts.

- [x] **`notFoundShell` strips the shell's `description` and social meta and puts none back, which is deliberate and pinned nowhere.** `entity_spa_shell.ts` routes its 404 body through `injectEntityHead`, which strips all four signals, while the head it splices is a `<title>` plus `<meta name="robots" content="noindex">`. That is the documented intent — its own comment says a page saying the entity is GONE must carry nothing about it, whatever the shell arrived with — and it is the one deliberate exception to the rule [§ 1190](../architecture/decisions.md) states, that an injector strips exactly what its own head replaces. No test states it: `entity_spa_shell.test.ts` covers the noindex and the malformed-shell fallback, but nothing asserts that a shell arriving WITH a description leaves without one. It costs nothing today because the shell carries none, which is exactly the condition under which § 1190's two defects hid. A case feeding `notFoundShell` a shell carrying all four signals and asserting the output carries only the title and the robots meta would make the exception a statement rather than a silence. Owner tree: `apps/web/src/lib/share/`. **CLOSED 2026-09-05 ([decisions § 1190](../architecture/decisions.md)).** Pinned, and the gap was real in a sharper way than filed: the existing cases assert the SHELL's og:title, og:image, stale canonical and `WebSite` node are gone, which all stay true if something NEW is put in their place — so a tidy-up bringing this path into line with the § 1190 per-head rule would have passed every one of them. Measured: adding a `<link rel="canonical">` to the 404 head leaves 14 of the 15 cases green. The new case counts what the document carries with `countHeadSignals` — the same patterns the strips act on — and requires `{title: 1, social: 0, canonical: 0, jsonLd: 0}` plus exactly one robots directive, and its text states why the asymmetry is intended: a canonical here would point a crawler at the missing entity as though it were the page's home, and a JSON-LD node would be structured data about something that no longer exists.

- [x] **`stripStaleHeadSignals` removes a JSON-LD block by running its replace to a fixpoint, which can synthesise a match the input never contained.** The loop in `head_splice.ts` re-runs `replace` until the string stops changing, added to satisfy `js/incomplete-multi-character-sanitization`. Deleting a span can splice the two sides together, so a second pass can match a `<script type="application/ld+json">…</script>` formed out of the wreckage of the first — the same hazard `document_title.test.ts` and `learn_structured_data.test.ts` both spell out in their own comments as the reason they consume HTML comments in ONE alternation rather than stripping them in a prior pass, and then chose the alternation instead. The two are inconsistent about the same risk. Nothing is broken: the input is our own build artifact, not attacker-controlled, and `head_splice.test.ts` pins the overlapping-open case the loop was written for. But the durable form is the one the two seo guards already use — a single alternation whose first branch consumes what must be skipped — and if that is adopted the CodeQL suppression the loop exists for has to be re-checked rather than assumed. Not taken here because it is a sanitiser rewrite with no observed defect behind it, and the round's budget went to the two strip/inject disagreements that DID have one. Owner tree: `apps/web/src/lib/share/head_splice.ts`. **CLOSED 2026-09-05 ([decisions § 1192](../architecture/decisions.md)).** The loop was exploitable, and it was also unnecessary — both halves measured rather than argued. Unnecessary: a global replace consumes every leftmost non-overlapping match the input holds, and an open with any close after it always matches at that open, so after one pass the only opens left are ones with no close anywhere, which no number of passes removes either; over 500,000 generated documents a single pass never once left a matchable block, and the suite's own overlapping-opens case — the one the loop was written for — passes unchanged without it. Exploitable: put `<scr` before a real block and `ipt type="application/ld+json">` after it, and deleting the block joins them into an open whose nearest close is the SPA bundle's, so the second pass swallowed `</head>`, the mount div and the bundle tag and the splice then returned the shell unmeta'd — the exact § 1086 damage, from the mechanism added to prevent it, in all 48 documents of that family. Loop removed, case pinned, and the tree no longer disagrees with itself: the two seo guards' one-alternation rule and this module now say the same thing. **Amended the same day ([decisions § 1194](../architecture/decisions.md)): removing the loop was half the fix.** Running the whole share suite rather than the module's own file turned up a test in `share_route_spa_shell.test.ts` asserting the loop's behaviour was REQUIRED, and it was guarding something real — deleting a match to the EMPTY STRING joins the characters either side, so the output can be re-parsed into a JSON-LD element the input never held. Both forms were wrong: one synthesises a block, the other chases it and cannot stop. A match is now replaced by a newline, which a tag name cannot contain, so the join is prevented rather than arbitrated; applied to all four signals, since the hazard is general to delete-based rewriting. The two cases now bracket it from both sides.

### Surfaced by the #789 round 39 go-export lane (2026-09-05)

- [x] **`docs/compliance/data-subject-rights.md` still describes the seven-column export gap as open, and it was closed the same day.** Lines ~53-62 state that "both transports read the run row through a hand-written column list, and neither list has been widened", naming `concluded_at`, `elevation_gain_m`, `race_listing_id` and the four `fastest_*_s` as reaching "neither `runs.csv` nor the backup's `runs.json`". [§ 1171](../architecture/decisions.md) widened both selects to the whole table and inverted the direction — each format now declares what it OMITS — so the paragraph is describing a state that no longer exists, in the document a regulator or a data subject would read first. It needs replacing with the omission contract (CSV declines `user_id` + the two Storage paths; the backup `runs.json` declines `user_id`; the GPX manifest declines the paths and the two timestamps) and with the four coverage guards that now hold it. Not done in the lane that closed it because `docs/compliance/` is outside its tree. Owner tree: `docs/compliance/`. **CLOSED 2026-09-05 ([decisions § 1262](../architecture/decisions.md)).** Replaced with the omission contract per format and the four guards that hold it, named.
- [x] **Two compliance documents carry the S3-lifecycle remedy that [§ 1173](../architecture/decisions.md) has just shown does not exist for this project.** `docs/compliance/data-subject-rights.md:140` ("something outside the Storage API (an S3 lifecycle rule on the object …") and `:148` ("still means listing the bucket over the S3-compatible endpoint"), plus the long **Art 20 export artifacts** row in `docs/compliance/retention.md:24`, which repeats "they need an S3 lifecycle rule or an operator pass". Measured out of the shipped `storage-api:v1.62.5`: the S3-compatible endpoint's `listObjectsV2` is `SELECT ... FROM storage.objects`, so it sees exactly what `list` sees and can neither confirm nor erase an orphan, and on Supabase Cloud the backend bucket is Supabase's, so there is no bucket to attach a rule to. The same `retention.md` row is stale in two further ways: it says the durable fix is "half built and inert", which [§ 1144](../architecture/decisions.md) routed, and it names `cleanup-stale-export-blobs` as the mechanism, which [§ 1172](../architecture/decisions.md) has unscheduled. Owner tree: `docs/compliance/`. **CLOSED 2026-09-05 ([decisions § 1262](../architecture/decisions.md)).** Both documents corrected, and the `retention.md` row's two further staleness claims with them — the reaper is routed (§ 1144) and the nightly row delete is unscheduled (§ 1172).
- [x] **`docs/backend/api_database.md`'s cron table still lists `cleanup-stale-export-blobs` at `'23 4 * * *'` as the export-retention job.** [§ 1172](../architecture/decisions.md) unscheduled it in migration `20270709000001`; the scheduled path is now `enqueue-export-blob-reap` at `'13 4 * * *'` alone, which also carries the `expire_stale_export_jobs()` call. The table has no row for that job at all (it landed in `20270708000010` while this file belonged to another lane), so the doc currently names one job that no longer runs and omits the one that does. The `expire_stale_export_jobs()` bullet a few lines above it also says it is "called by `cleanup_stale_export_blobs`", which is now the break-glass caller rather than the scheduled one. Owner tree: `docs/backend/`. **CLOSED 2026-09-05 ([decisions § 1237](../architecture/decisions.md#1237-four-smaller-corrections-and-the-bookkeeping-obligation-a-new-migration-actually-carries)).** The filing was right on both counts. The `cleanup-stale-export-blobs` row is replaced by `enqueue-export-blob-reap` at `'13 4 * * *'`, and the `expire_stale_export_jobs()` bullet now names the enqueue as the scheduled caller with the sweep as break-glass. **Six further live jobs were missing besides** (`cleanup-account-deletion-receipts`, `sweep-challenge-completions`, `enqueue-weekly-digest`, `enqueue-lifecycle-drip`, `enqueue-safety-overdue-emails`, plus the export one), so the table named 16 of 22 live schedules; it is now diffed against `cron.job` on a reset stack — names AND schedules — and the re-derivation query sits beside it. Two prose references to the sweep as the retention path are corrected in the same change.
- [x] **`enqueue_export_blob_reap()` has no pgtap coverage at all, and it now has logic worth pinning.** It landed unpinned in `20270708000010` (`grep` over `apps/backend/supabase/tests/` matches neither the function nor the `export_blob_reap` kind), and `20270709000001` has since given it a derived worklist: one job per user who still holds a legacy `runs/{uid}/exports/*` archive, deduplicated across that user's several archives, none for a plain track object in the same bucket, and a singleton guard that is per-PAYLOAD rather than per-kind. All of that was verified against a throwaway `supabase/postgres` container with the surrounding objects stubbed, which is weaker than driving the real schema: the stub cannot catch a `jobs` CHECK or an RLS interaction. Worth an `export_surface_contract_test.sql` block that calls the enqueue twice over a staged `storage.objects` fixture and names the resulting payloads. Owner tree: `apps/backend/supabase/tests/`. **CLOSED 2026-09-05 ([decisions § 1237](../architecture/decisions.md#1237-four-smaller-corrections-and-the-bookkeeping-obligation-a-new-migration-actually-carries)).** Eight assertions in `export_surface_contract_test.sql` drive the enqueue against the real schema: the emitted payload set (default `exports` job plus one per user holding a legacy `runs/{uid}/exports/*` archive, deduplicated, none for a plain track in the same bucket), `max_attempts` 3, the `expire_stale_export_jobs()` composition, and the singleton guard proved per-PAYLOAD by draining one payload and re-calling. Mutation-tested with five replaced function bodies (dropped expire, kind-wide guard, no `distinct`, table-default `max_attempts`, prefix filter widened to the whole bucket) — each caught. Note the singleton guard is indistinguishable per-kind vs per-payload on a FIRST call, because the `not exists` subquery of an `INSERT … SELECT` reads the pre-statement snapshot; only the drain-and-recall case separates them.
- [x] **A worker down long enough to overrun export retention now says nothing louder than a queued `jobs` row.** [§ 1172](../architecture/decisions.md) took the sweep off the clock deliberately — its row delete was the only thing that could permanently orphan a byte — and the failure mode is now a bounded, recoverable retention overrun instead of permanent residue. What went with it is the nightly `raise` that `20270703000002` added: `cleanup_stale_export_blobs()`'s post-condition check only fires when something calls it. The alert wants a check that can raise WITHOUT also deleting — a `count(*)` over `storage.objects` past retention plus one grace day (so the ten-minute window in which an object crosses the line between the reap and the check cannot false-positive), scheduled on its own. That means editing the function or adding a sibling, and `export_surface_contract_test.sql` drives the current body assertion-by-assertion, so it belongs with that suite's owner. Owner tree: `apps/backend/supabase/migrations/` + `apps/backend/supabase/tests/`. **CLOSED 2026-09-05 ([decisions § 1234](../architecture/decisions.md#1234-a-worker-that-is-down-is-invisible-to-both-job-alerts-and-export-retention-is-where-that-silence-has-a-deadline)), and the diagnosis was too narrow.** Measured: `find_stuck_jobs` selects `status = 'running' and locked_at is not null`, `find_failed_jobs` selects `status = 'failed'` — so a job no worker ever CLAIMED is `queued` with a null `locked_at` and is in **neither** set. A down worker is invisible to both alerts for every kind in the allowlist, including `safety_sms` (the overdue-runner escalation) and `data_export` (Art 20 fulfilment); export retention is one symptom, not the condition. So `20270710000004` ships both halves: `find_backlogged_jobs` / `jobs_backlog_summary` (the cause, every 10 min, clocked on `scheduled_at` so a `defer_job` backoff is not a stalled queue) and `export_retention_overrun` (the condition — a reap that ran and erased nothing is `done` while the bytes stay — counting over the sweep's own predicate past 7 days plus the grace day the filing correctly asked for, daily at 04:43). It RETURNS rather than raising, unlike the sweep: that raise is a post-condition on a DELETE it just performed, and a standing check that raised would fail a cron run every day for as long as a recoverable overrun lasted. 13 assertions, 7 planted defects, all caught.
- [x] **Nothing under `tests-e2e/` plants a `matched_track_url` with a Storage object, so no test exercises a run whose MATCHED track is the one being rendered.** `/runs/[id]`'s `baseTrack` prefers `matchInfo.track` whenever it has two points, and the elevation profile, the `elevations` series and `RunMap`'s pace heatmap all read it — [§ 1170](../architecture/decisions.md) fixed the worker so those three keep working under OSRM, and the fix is pinned on the Go side only. A fixture that seeds a `run_matched_tracks` row plus its gzipped Storage object would cover the seam the two halves meet at; it was named as missing when the OSRM half was filed and it is still missing. Owner tree: `apps/web/tests-e2e/`. **CLOSED 2026-09-06 — already fixed when this was filed.** `insertMatchedTrack` and `runs/matched-track-render.spec.ts` landed in the same PR (#859, commit `856150274`) and the box was left un-ticked. Verified by running the whole `tests-e2e/runs/` directory: 167/167 green. The spec's assertion was strengthened this round — see the § 1357 entry above.
- [x] **`docs/testing/test_inventory.md` does not know about the round-39 export and matcher guards.** New files: `apps/job_worker/internal/dataexport/runs_projection_test.go` (three guards over the generated `runs` Row), `apps/job_worker/internal/export_run_row_test.go`, `apps/job_worker/main_test.go` (the first test in the `main` package), plus new cases in `handler_export_blob_reap_test.go`, `matcher_osrm_test.go` and the Edge Function's `render.test.ts`. No CI guard enforces the inventory, so this is hygiene rather than a break. Owner tree: `docs/testing/`. **CLOSED 2026-09-05 ([decisions § 1259](../architecture/decisions.md)).** All three new files documented (`runs_projection_test.go` 3, `export_run_row_test.go` 1, `main_test.go` 1 — the last needed a `###` of its own, being in package `main` and so outside the `internal/` aggregate), and the changed suites' counts are re-derived by the new guard rather than transcribed.

### Surfaced by the #789 round 39 watch lane (2026-09-05)

- [x] **`hr_coverage` reaches the phone's Swift and is dropped by both Dart decoders before it reaches the row.** The remainder of [§ 1207](../architecture/decisions.md), and the half its own filing did not have. `apps/watch_ios` now measures the figure and packs it into the `WCSession.transferFile(_:metadata:)` envelope, and `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift` lifts it into the method-channel payload — but past that bridge the payload meets two hand-written key allowlists, and neither names it: `WatchIngest._runFromArgs` in `apps/mobile_android/lib/main.dart` (the live channel) and `runFromWatchPayload` in `apps/mobile_android/lib/watch_ingest_queue.dart` (the replay path for a payload queued while signed out) each build `metadata` from `avg_bpm` / `activity_type` / `last_modified_at` only. Both are two lines of the shape already sitting beside them — `final c = raw['hr_coverage']; if (c is num) metadata[cm.MetadataKeys.hrCoverage] = c.toDouble();` — and `MetadataKeys.hrCoverage` already exists in `packages/core_models/lib/src/metadata_keys.dart`. Each needs the byte-identical iOS twin mirror ([§ 39](../architecture/decisions.md)). Not taken here: `apps/mobile_android/`, `apps/mobile_ios/lib` and `apps/mobile_ios/test` were all outside this lane, which owned only the iOS HOST project. The `hr_coverage` registry row in `docs/backend/metadata.md` and `apps/watch_ios/CLAUDE.md` both state this limit explicitly rather than claiming the writer works end to end, so nothing currently reads as more finished than it is. Owner trees: `apps/mobile_android/lib/` + `apps/mobile_ios/lib/`. **CLOSED 2026-09-05 ([decisions § 1254](../architecture/decisions.md)).** Not by the two-lines-per-decoder shape this entry proposed: two hand-written decoders for ONE payload is the defect, and measuring them against each other found a second divergence going the other way. `WatchIngestBridge.swift` sends `track` as JSON TEXT, `_runFromArgs` had a string branch and `runFromWatchPayload` did not — so an Apple Watch run that arrived while the runner was signed OUT was queued, drained on the next sign-in, and landed with an **empty track**, looking complete. `_runFromArgs` also never read the per-point `bpm` `metadata.md` already claimed the watch-ingest decoder reads. `WatchIngest` now normalises the channel map once and delegates to `runFromWatchPayload` on both branches (the byte-identical `_parseSource` went with it), which gained the string-track branch; `hr_coverage` is forwarded verbatim like `avg_bpm` with a finiteness check, because a non-finite double fails the whole run's JSON encode. `watch_ingest_single_decoder_guard_test.dart` pins it behaviourally (four planted mutations, each refused); 131 targeted Dart tests green, twin mirrored and diffed.

- [x] **The watchOS coverage accumulator has still never executed, and its one silent failure mode writes `hr_coverage: 0.0` on every run.** Closed 2026-09-06 — [decisions § 1299](../architecture/decisions.md). **Half of this filing was already false when it was written and is corrected rather than carried further.** The quoted expression `stats.mostRecentQuantityDateInterval()?.end` has not existed since [§ 1156](../architecture/decisions.md): the assignment ends `?? Date().timeIntervalSinceReferenceDate`, so a blank interval falls back to the delivery instant and credits normally. `git log -S` puts that fallback in the same commit as the accumulator (#856) and this text one round later (#857). The residual routes to a run-long zero are not silent in the same way — a `mostRecentQuantity()` that never answers also leaves `currentBPM` at a dash on the wrist, and a declined Health read grant genuinely had a sensor delivering none of the time, which HealthKit gives an app no way to distinguish anyway. **The half that was true — it had never executed — is fixed at the root.** `HeartRateCoverageAccumulator` is a value type owning the credited total, the tick mark and the sample stamp, with `begin` / `reset` / `noteSample(atEpoch:)` / `advance(activeElapsedSeconds:nowEpoch:)`; `advance` takes the instant it is measured at instead of reading a clock, so a whole run walks through it in a millisecond. `HealthKitManager` delegates. Nine new cases in `HealthKitFailureTests` cover a sensor delivering throughout, one quitting at twenty minutes of an hour (1230 credited seconds of 3600, coverage 0.34, `avg_bpm` suppressed — the path the figure exists for), a late first sample, a silence charged once rather than re-credited, a repeated tick, a non-finite tick, and both unmeasured branches. **Rung: *host-tested*, and NOT above it.** No Swift compiler exists on this workstation or in Linux CI; the tests are written to run in the macOS `test-watch-ios` job and have not run anywhere yet. Every expected constant was independently re-derived by simulating the shipped semantics outside Swift, which is evidence about the arithmetic and none about the Swift. The original ask stands unchanged and is re-filed below: one recorded run on a simulator or paired watch, `hr_coverage` present and plausibly non-zero, per the *bench-verified* rung in `docs/custom_watch/quality_standards.md`. Owner: whoever next has a Mac and a paired watch.

- [x] **The Wear PostRunScreen "Discard" still destroys an unsynced run on one tap — the twin of the two watchOS Discards closed by [§ 1208](../architecture/decisions.md).** Found while hunting for § 1206's class on the other watch. `apps/watch_wear/.../ui/RunWatchApp.kt`'s bottom-end `×` button renders only under `!synced && summary != null` and calls `vm::discard`, which is `store.remove(id)` — the run leaves the local durable queue and its track goes with it. The button's own comment argues single-tap is fine because "the run isn't running, just unsaved, and a Discard tap can be re-triggered if dismissed by mistake", which is a claim about re-opening a *confirm*, not about recovering a *run*: nothing re-triggers `store.remove`. Not taken in this lane despite owning the tree, and the reason is a layout decision rather than effort: the control is a 52 dp icon `Button` whose whole visual is `discard_short` (`×`), so § 1206's arm-and-relabel has nowhere to render — and this module's own `ScreenWiringTest` states that "colour alone is not a signal", which rules out tinting it and calling that the arm. The fix needs either an armed confirm strip on the PostRun layout or a promotion of the control to a labelled Chip, and neither can be judged without seeing it render, which this box cannot do (no Wear emulator was run here). The machinery is in place: `ui/ConfirmGuard.kt`'s `confirmPress` / `CONFIRM_WINDOW_MS` and the `discard_confirm` / `discard_stake` strings already exist in all seven catalogues. Owner tree: `apps/watch_wear/`. **CLOSED 2026-09-05 ([decisions § 1253](../architecture/decisions.md)).** The arm had nowhere to render ON the 52 dp glyph, so it replaces the cluster the glyph sits in: Sync / Next / `×` give way to `discard_stake` above a labelled `discard_confirm` chip, and only a tap on that chip calls `onDiscard`. The confirm target is bottom-CENTRE rather than the bottom-END corner the arming tap landed in, so a double tap in place reaches nothing. `cd_discard_confirm` in all seven catalogues announces the arm to TalkBack, which the `×` and the confirm would otherwise have shared. Pinned by `PostRunDiscardConfirmTest` (five planted mutations, each refused). The filing's "cannot be judged without seeing it render" was half right: the Kotlin IS verifiable here — `./gradlew assembleDebug testDebugUnitTest` is green at 744 tests on this workstation — and only the 192 dp round-bezel layout still wants an emulator.

- [ ] **`apps/watch_ios/WatchApp/Info.plist` declares `WKWatchOnly` while the app's entire sync path depends on a paired iPhone companion.** Noticed while auditing the plist against the code for the manifest-over-declaration class; filed rather than changed because it cannot be settled from Linux. `WKWatchOnly` is the "this app has no iOS companion" declaration, and this app's Release sync path is `WCSession.transferFile(_:metadata:)` into `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift`, with routes pushed the other way over the same session — a counterpart relationship, not a standalone one. It coexists today with the checkbox `apps/watch_ios/CLAUDE.md` keeps unticked ("phone-side receiver + sign-in UI live; checkbox remains unticked pending end-to-end verification on paired physical devices"), which is the one thing nobody has done, so this is a live candidate cause for that verification failing rather than a certainty. It may equally be correct-as-is: `WatchApp.xcodeproj` is a standalone project rather than an embedded target of the Flutter `Runner`, which is plausibly why it was set. What is needed is an Apple-doc read on `WKWatchOnly` versus `WCSession` counterpart pairing plus one paired-device test, both of which want a Mac. Not something `scripts/check_watch_ios_source.mjs` can decide — it can read the key, but not what Apple does with it. Owner: whoever does the paired-device verification. **SETTLED, NOT FLIPPED, 2026-09-05 ([decisions § 1256](../architecture/decisions.md)).** The "it may equally be correct-as-is" horn is resolved: `WKWatchOnly` is a TRUE description of the Xcode project and a FALSE description of the app. Apple documents it as "no iOS companion" and as mutually exclusive with `WKCompanionAppBundleIdentifier`; `apps/mobile_ios/deployment.md` already states the two ship as one `.ipa` and lists that companion key as required, and the bundle id `com.threkir.app.watchapp` is the companion naming rule exactly. **But `Runner.xcodeproj/project.pbxproj` contains ZERO occurrences of `WatchApp` / `watchos` / `Watch App` (measured), so the embedding that doc describes does not exist** — flipping the plist alone would declare a companion the project does not bundle, i.e. move the lie, and it would be asserted against `test-watch-ios`, which installs on an UNPAIRED watch simulator. What is owed is the build integration, which is the same missing piece as the untickable paired-device checkbox; § 1256 lists the five steps a Mac must run in order. The audit found a live defect beside it and it IS fixed: `GENERATE_INFOPLIST_FILE = NO` makes every `INFOPLIST_KEY_*` inert, so `INFOPLIST_KEY_CFBundleDisplayName = "Threkir"` did nothing and the watch app was named **`WatchApp`** on the wrist. Name moved into the plist, both inert settings deleted, and claim (10) of `check_watch_ios_source.mjs` now refuses an inert `INFOPLIST_KEY_*`, a missing `CFBundleDisplayName`, and `WKWatchOnly` beside a companion id (five mutations pinned). **Re-measured 2026-09-06 by the round-41 watch lane and unchanged, so the entry stays open honestly rather than by inertia:** `WatchApp/Info.plist` still carries `WKWatchOnly` and no `WKCompanionAppBundleIdentifier`, and `apps/mobile_ios/ios/Runner.xcodeproj/project.pbxproj` still contains zero occurrences of `WatchApp` / `watchos` / `Watch App` (measured, not recalled). Nothing on Linux can advance this: the remedy is § 1256's five-step build integration, which is the same missing piece as the untickable paired-device checkbox. Not taken in-round for that reason, not for lack of a decision. **Re-measured again 2026-09-06 by the round-42 watch lane, unchanged, and the surrounding gap IS closed — [decisions § 1349](../architecture/decisions.md).** The measurements: `WatchApp/Info.plist` still carries `WKWatchOnly` and no `WKCompanionAppBundleIdentifier`; `apps/mobile_ios/ios/Runner.xcodeproj/project.pbxproj` still contains **zero** occurrences of `WatchApp` / `watchos` / `Watch App` (`grep -c`, not recalled); the watch bundle id is still `com.threkir.app.watchapp` against the phone's `com.threkir.app`; and eight files under `WatchApp/` reference `WCSession`. So the key still describes the PROJECT truly and the APP falsely, and § 1256's five Mac steps are still the remedy. What was missing was any way for that to stop being true unnoticed, and that is now claim (10)'s second half: the plist must follow the build in BOTH directions — `WKWatchOnly` beside an embed fails (the integration landed, flip the key), a companion id with no embed fails (the "move the lie" outcome). The embed is recognised by Apple's Embed Watch Content destination, the built product name, or the watch's own bundle id read off its project rather than written down. Four mutations pinned. **The item stays open: nothing here flipped a plist, and no rung above *read* was reached on the Swift.**

- [x] **The DEBUG direct-upload path drops `avg_bpm` and `hr_coverage`, and its metadata is typed `[String: String]` so it cannot carry either.** `apps/watch_ios/WatchApp/SupabaseService.swift`'s `RunPayload.metadata` is `[String: String]` and is built with `activity_type` + `last_modified_at` only, where the WCSession envelope now carries seven keys including two numeric ones. Low stakes and deliberately filed rather than fixed: the whole file is wrapped in `#if DEBUG` and Release builds compile it out, so no shipped run takes this path. But it is the path a watch-sim-alone developer uses to see a row land, and a row that silently lacks the heart-rate fields is exactly the shape that makes someone debug the wrong tier. Fixing it means widening the payload type to `[String: Any]` (or a small `Encodable` wrapper), which is more than a one-line change and touches the only Supabase-writing code in the app. Owner tree: `apps/watch_ios/`. **CLOSED 2026-09-05 ([decisions § 1255](../architecture/decisions.md)).** Typed, not deleted: the path is the only way a watch-sim-alone developer sees a row land with no phone, so deleting it removes the capability rather than the defect. `RunPayload.metadata` is a `RunMetadata: Encodable` carrying `activity_type` / `last_modified_at` / `avg_bpm?` / `hr_coverage?`, and Swift's synthesized `encodeIfPresent` omits a nil rather than writing `null`, which is § 1207's rule exactly. The durable half is claim (9) of `check_watch_ios_source.mjs`, which requires the WCSession envelope's key set to be contained in the union of `RunPayload`'s and `RunMetadata`'s stored properties, with `DIRECT_ONLY_FIELDS` naming the two the phone supplies and failing when an exemption goes stale — six mutations pinned. Still read-not-compiled on the Swift side.

### Surfaced by the #789 round 39 shared-rails lane (2026-09-05)

- [x] **`check_twin_claims.mjs` reads about three quarters of the twin claims in the tree, and its floor is set too low to notice.** Measured, and the root cause of [decisions § 1204](../architecture/decisions.md) — and, one level up, of why the § 641 class keeps recurring. Two mechanisms. **(1)** `headerComment` folds only the comment block at the very top of a file and breaks at the first non-comment line, so a file whose doc block sits after an `import` returns an EMPTY header: 27 files carry a twin-shaped claim naming an `apps/` path that the guard never sees at all, including `apps/web/src/lib/runs/run_stats.ts`, `runs/grade_adjusted_pace.ts`, `runs/age_grade.ts`, `runs/pace_analysis.ts`, `apps/mobile_android/lib/elevation.dart` and `lib/fitness.dart`. Fix: fold the first doc-comment BLOCK in the file rather than the block at offset zero — skip leading `import` / `library;` / blank lines before starting, or scan the first N lines and take the longest comment run. **(2)** Inside the headers it does read, `DECLARATION` wants a verb from a fixed list then a backticked path ending in `.ts`/`.dart`, and misses four forms found in the tree: `the Dart twin \`path\`` with no *of* (`training/hr_zones.ts`), `Mirrors apps/…/x.dart` with no backticks (`training/workout_kind_color.ts`), a backticked path with a `:symbol` suffix (`runs/run_stats.ts` names `…/run_stats.dart:movingTimeOf`), and `ported from \`path\`` (`training/goals.ts`). Widening for those four over the same headers finds 27 more declarations — 24 for already-registered pairs, two unregistered, one dangling. Suggested regex: add `(?:Dart|TS) twin` and `[Pp]orted from` and `[Dd]art port of` to the verb alternation, make the backticks optional, and allow a `(?::[A-Za-z0-9_]+)?` suffix after the extension. **(3)** `MIN_DECLARATIONS = 60` exists to catch exactly a scanner that stopped matching, but the census is 106, so it has 46 of slack and a 25 % shortfall passes it. Raise it with the widening, and keep it within ~10 of the real count. Owner tree: `scripts/check_twin_claims.mjs` + its `.test.mjs`. **CLOSED 2026-09-05 ([decisions § 1243](../architecture/decisions.md)).** Both named mechanisms fixed and the floor raised to 155 with a test that fails when its slack under the real census exceeds 15. Census **106 -> 167**, none lost; the 37 empty-header files and the four unreadable declaration forms are all read now. Each of the three changes was mutation-tested by reverting it (2, 7 and 1 failing cases respectively). Two further forms are measured and re-filed below rather than fixed: a RELATIVELY-named counterpart (11 cross-platform instances) and a declaration sitting below the file's first statement.
- [x] **`hr_zones` is a THREE-rail helper registered in neither registry, and the Wear OS rail diverges from the other two in both directions.** `apps/web/src/lib/training/hr_zones.ts` says it is "Kept algorithmically in lockstep with the Dart twin `apps/mobile_android/lib/hr_zones.dart` … and the Wear OS `resolveZoneCutoffs` in `SupabaseClient.kt`" — three rails, and the pair appears in neither the root `CLAUDE.md` list nor `.claude/agents/shared-library-syncer.md`, so nothing has ever compared them (it escapes `check_twin_claims` by the missing-*of* form above). **The TS↔Dart half is measured CLEAN**: every age 5..120 through `tanakaMaxHr` and every max HR 80..240 through `zoneCutoffsFromMaxHr`, byte-identical output on both, because `Math.round` and Dart's `.round()` differ only on negative halves and neither range reaches one. **The Kotlin rail diverges twice.** (a) It applies `s.maxHrBpm` with NO range validation where both others require `80..240`; `max_hr_bpm` is a jsonb prefs key with no CHECK anywhere, and `apps/web/src/routes/settings/account/+page.svelte:590` writes it as a bare `parseInt(maxHr, 10) || null` with no bound at all (the preferences page's `min="100" max="230"` is an advisory HTML attribute on a field that autosaves `onblur`), so a stored 300 gives the watch `[180, 210, 240, 270, 300]` while web and mobile ignore it and fall back to Tanaka or the 190 default. (b) It returns `null` when neither a max HR nor a usable DOB is available, where both others return the legacy `zoneCutoffsFromMaxHr(190)` — plausibly deliberate, but it is a behavioural difference under a header that claims lockstep, so it should be stated on all three rails or removed. Two sub-fixes worth separating: clamp the write path (an out-of-range max HR should be refused at the input with a message, not silently ignored by two clients out of three), and bring the Kotlin rail's validation in line. Owner trees: `apps/watch_wear/android/app/src/main/kotlin/com/runapp/watchwear/SupabaseClient.kt`, `apps/mobile_android/lib/hr_zones.dart`, `apps/web/src/routes/settings/`, plus both registries. **CLOSED 2026-09-05 ([decisions § 1245](../architecture/decisions.md)).** The filing is right in both directions and both were verified before acting. (a) is a defect and is fixed: the Kotlin rail now applies the same 80..240 bound, and the bound is named once per rail rather than spelled inline three times. A stored 0 -- what an emptied number input posts -- was worse than the 300 the filing named: it gave an all-zero ladder putting every sample above Z5. (b) is deliberate and was KEPT, not levelled: the watch shows no zones rather than a stranger's, and that is now stated on all three rails and pinned by the Kotlin suite's own no-signal case. The pair is registered in both registries as a three-rail helper. The Kotlin fix was mutation-tested (reverting the `takeIf` fails exactly the two rows written for it) and the suite was run: `./gradlew --offline :app:testDebugUnitTest --tests '*UniversalSettingsTest*'`, 36 tests. **The write-path clamp is NOT closed and is re-filed below.**
- [x] **Five more existing TS↔Dart pairs are in neither registry, so nothing detects their divergence.** Both halves exist on disk for every one; each web half's header claims a mirror, in one of the forms `check_twin_claims` cannot read. `goals` (`training/goals.ts` ↔ `lib/goals.dart`), `workout_kind_color` (`training/workout_kind_color.ts` ↔ `lib/workout_kind_color.dart`), `run_stats` (`runs/run_stats.ts` ↔ `lib/run_stats.dart` — and this one already has a third rail in the firmware, `core/src/run_stats.rs`), `elevation` (`routes/elevation.ts` ↔ `lib/elevation.dart`), `adaptive_fitness_flag` (`training/adaptive_fitness_flag.ts` ↔ `lib/adaptive_fitness_flag.dart` — the env-binding half deliberately kept out of the `plan_adaptive_replan` pair, which is a reason to register it separately, not a reason to leave it unregistered). None was measured for actual drift by this lane — registering them is what makes the question answerable. Register in BOTH the root `CLAUDE.md` paragraph and `.claude/agents/shared-library-syncer.md` in the same change ([decisions § 641](../architecture/decisions.md)); half-registering is worse than not registering. Owner tree: both registries. **CLOSED 2026-09-05 ([decisions § 1244](../architecture/decisions.md)).** All five registered in both registries, plus six more the widened `check_twin_claims` exposed (`hr_zones`, `auth_errors`, `food_search`, `nutrition_totals`, `payload_hash`, `race_match`, `workout_labels`/`training_labels`) -- eleven in total. Measured while registering rather than deferred: `elevation` and `run_stats` are clean on their shared subsets and the differences in `goals`, `workout_kind_color`, `auth_errors` and `workout_labels` are idiomatic, not divergences. `workout_kind_color` had no web suite at all and now has one. Four more declarations were REWORDED rather than registered because they are not pairs (`revenuecat`, `strava`, `route_describe_client`, `fitness_card`); one remains open and is re-filed below.
- [x] **`current_week.ts`'s twin declaration names a file that does not exist.** Its header says the pure helper "mirrors a byte-identical Dart twin (`apps/mobile_android/lib/widgets/current_week.dart`)"; there is no such file — the twin is `apps/mobile_android/lib/current_week.dart`, and `apps/mobile_android/lib/widgets/` holds `current_week_strip.dart`, the widget. The pair IS registered correctly in both registries, so nothing is unguarded; what is wrong is the sentence a reader follows. `check_twin_claims`'s file-exists claim was written for precisely this and cannot see it, because the declaration is inside a parenthesis rather than after one of its verbs — it comes free with the widening filed above. Owner tree: `apps/web/src/lib/training/`. **CLOSED 2026-09-05 ([decisions § 1244](../architecture/decisions.md)).** The header now names `apps/mobile_android/lib/current_week.dart` and says the widget that renders it is `widgets/current_week_strip.dart`. It also called a TS-Dart pair *byte-identical*, which only the iOS twin ever is; that is gone too. The widened reader does see this one, exactly as the filing predicted.
- [x] **The Garmin GAP golden is a fourth rail that nothing joins to the other three yet — one line in `check_watch_wire_vectors.mjs`.** [decisions § 1201](../architecture/decisions.md) landed the fixture, the reduction and the frozen 311 s/km in `apps/watch_garmin/source-test/GradeAdjustedPaceTest.mc`, declaring the eight constants under the same `GAP_REFERENCE_*` names the TS and Rust rails use. Verified by running the guard's own `mcConst` regex against the file: all four rails produce byte-identical joined specs today. The enforcement needs two lines added to `scripts/check_watch_wire_vectors.mjs`: `const MC_GAP_TEST = 'apps/watch_garmin/source-test/GradeAdjustedPaceTest.mc';` beside the existing `MC_GAP`, and `otherRail(MC_GAP_TEST, gapReferenceSpec(mcConst, GAP_REFERENCE_UPPER)),` in the `GAP reference track (fixture and the pace it must grade to)` row's `rails` array. No new machinery — `mcConst` and `GAP_REFERENCE_UPPER` both already exist and are already used. Until it lands the four goldens agree with nothing checking that they still do. Owner tree: `scripts/`. **CLOSED 2026-09-05 ([decisions § 1246](../architecture/decisions.md)).** Two lines as filed; the row reads five rails and they agree. Mutation-tested: moving the Monkey C 311 to 312 fails as a disagreement, deleting one `GAP_REFERENCE_*` constant fails as an unmatched read rather than as a silently shorter spec.
- [x] **`check_garmin_source.sh` grew a seventh claim and tightened its vacuity guard; the mutation suite that makes its green mean anything has rows for neither.** The guard's own header says a source-reading guard that has stopped matching passes vacuously and that `scripts/check_garmin_source.test.mjs` is what makes it meaningful — and that file is in another tree. Two rows are owed. **(1) Claim 7** ([decisions § 1201](../architecture/decisions.md)): three mutations of `source-test/GradeAdjustedPaceTest.mc` must be refused — deleting one `const GAP_REFERENCE_*` (message: `declares \`const GAP_REFERENCE_PERIOD_M\` 0 times`), replacing `var t = new GradeTracker();` with anything else (`does not drive a \`GradeTracker\``), and rewriting `if (segHoriz >= $.MIN_SEGMENT_M)` as `>= 20.0` (`does not read \`$.MIN_SEGMENT_M\``). **(2) The suffixed rename** ([decisions § 1202](../architecture/decisions.md)): the existing vacuity test renames the class to `GapView`, which the old substring check caught; the case it missed is `GradeAdjustedPaceViewV2`, which CONTAINS the name — add that rename as its own test asserting `would pass on an empty file`. All five mutations were run by hand at authoring and all five are refused; none is measured in CI. Owner tree: `scripts/check_garmin_source.test.mjs`. **CLOSED 2026-09-05 ([decisions § 1246](../architecture/decisions.md)).** Six rows, not two: the three claim-7 mutations as filed, plus a constant declared TWICE (the other way a name stops being readable exactly once), the `GradeAdjustedPaceViewV2` suffix rename, and the same shape on the test module. Every row was mutation-tested against the GUARD rather than only against the source -- relaxing the exactly-once count, dropping the tracker read, dropping the `$.MIN_SEGMENT_M` read and restoring the substring vacuity check each fail exactly the rows written for them.
- [x] **`filterRelinkCandidates` inherits its tie order from whatever Postgres returned, and the two clients may not fetch in the same order.** The same class as [decisions § 1200](../architecture/decisions.md) at much lower reachability, filed rather than fixed because the Dart half is another lane's tree and a one-sided fix would CREATE a divergence. `apps/web/src/lib/training/relink_candidates.ts:94` sorts on `started_at` descending with a valid comparator, and `apps/mobile_android/lib/relink_candidates.dart:77` uses `compareTo` — both correct, both stable at the list sizes involved (Dart's `List.sort` is an insertion sort up to 33 elements), so two runs sharing an exact `started_at` come out in the order the FETCH returned them. Neither fetcher's `.order()` was checked by this lane for a secondary key, and that is the question to answer first: if they differ, the picker's row order differs by platform for a same-instant pair. The durable fix if so is a total order on both rails — `started_at` desc then `id` asc, matched in both halves and in both mirror suites, and ideally in the two `.order()` clauses too so the tie never reaches the helper. Reachability is genuinely low (two runs at the identical instant for one owner, e.g. an import racing a manual entry), which is why this is filed as a note rather than as a defect. Owner trees: `apps/web/src/lib/training/` + `apps/mobile_android/lib/relink_candidates.dart` + both mirror suites. **Closed** by [§ 1241](../architecture/decisions.md). Measured: V8 0 divergences from a stable sort in 800,000 trials; Dart 0 in 400,000 at <=33 elements and 320,000/320,000 past it. The question the entry said to answer first is answered: NEITHER fetcher has a secondary `.order()` key and the two queries differ, so stability alone was never enough — both halves now carry a total order (`started_at` desc, then `id` asc), which makes the fetch order unobservable and the `.order()` change unnecessary.
- [x] **`apps/custom_watch/README.md`'s per-core test counts go stale on every later change and nothing reads them.** The 2026-07-11 batch note states a count per ported core — `plan_replan` "13", and a `watch_core` total of "924 host tests". `plan_replan` was 15 before this round and is 17 now; the crate is at 2,428. `scripts/check_watch_doc_counts.mjs` reads firmware SYMBOLS against present-tense docs and deliberately does not read these dated batch notes, so it passes either way — correctly, since rewriting a dated record would misdescribe the batch it describes. That leaves a reader with figures nobody checks. Two candidate fixes and they are not equivalent: mark the batch notes as as-of (a single "counts as of 2026-07-11" line above the list, which costs nothing and makes the staleness honest), or move the per-core counts out of the dated notes into a present-tense table the doc-count guard can read the way it reads the symbol claims. The second is the durable one. Not taken here: `check_watch_doc_counts.mjs` is another lane's tree, and the choice between the two is a docs-shape decision rather than a firmware one. Owner trees: `apps/custom_watch/README.md` + `scripts/check_watch_doc_counts.mjs`. **CLOSED 2026-09-05 ([decisions § 1257](../architecture/decisions.md)).** Neither of the two candidate fixes as stated: the doc-count guard's own header already excludes dated logs on § 793's rule, so hand-correcting a batch note falsifies the record AND rots again. What was missing was a PRESENT-TENSE figure — there was none anywhere in the doc set. `apps/custom_watch/CLAUDE.md` (in `DOC_FILES`) now states it, the README chronology carries one banner saying its counts are as-of and pointing there, and no historical number was touched. The figure is a new `core.host_tests` registry row resolving `#[test]` across the modules `core/src/lib.rs` DECLARES; **validated against the runtime before being trusted** — the static count is 2428 and `cargo test --target x86_64-unknown-linux-gnu -p watch_core` reports `running 2428 tests`. Workspace sweep measured separately: `bin/watch-test.sh` = **2,868 passed / 0 ignored** (roadmap.md states 2,407 as of 2026-07-31; exempted rather than corrected, it is out of this lane's tree and carries its own command + date). Adding `host tests?` to `SWEEP_NOUNS` found a live stale count: `privacy.md` said `privacy.rs` has "nine host tests" and it has **eight** — corrected and registered. Six new tests, three mutation runs.

### Surfaced by the #789 round 39 web-core lane (2026-09-05)

- [x] **The web Supabase client is untyped, so `database.types.ts` never reaches a single query — and typing it costs 79 errors across four trees.** [§ 1185](../architecture/decisions.md). `apps/web/src/lib/core/supabase.ts:4` calls `createBrowserClient(url, key)` with no `Database` generic; `@supabase/ssr` declares it `<Database = any, …>`, and `app.d.ts` carries only a bare `SupabaseClient`, so every `.from()`, `.select()` and `.rpc()` in the app is unchecked. A renamed RPC, a dropped `p_*` parameter, or a select list missing a column the caller's declared type requires all ship green. Two comments in `data.ts` (the `.rpc()` guard's own rationale, and `CLUB_SELECT_COLS`'s `as const` note about `GenericStringError`) describe protections that only follow from a typed client. **Measured, not estimated**: adding `createBrowserClient<Database>` and running `svelte-check` gives **79 errors in 10 files** — 62 in `core/data.ts`, 5 in `lib/components/PlanEditor.svelte`, 5 in `lib/backup/backup.ts`, 3 in `lib/stores/auth.svelte.ts`, 2 each in `lib/settings/settings.ts`, `routes/settings/devices` and `routes/settings/account`, 1 each in `routes/settings/preferences`, `routes/runs/[id]` and `routes/onboarding`. By kind: 15 `string | null` not assignable to `string | undefined` (write shapes built from a read row), 13 `Record<string, unknown>` into a `Json` parameter, 7 `Record<string, unknown>` not assignable to `Json | undefined`, 4 `number | null` vs `number | undefined`, and a tail of union narrowings (`'km' | 'mi'`, `'free' | 'pro' | 'lifetime'`, `SegmentGenderFilter`, the age-group union). `createServerClient` in `supabase-server.ts` has the same gap. None of it is hard, but it spans four lanes' trees and wants its own change. Owner tree: `apps/web/` (cross-tree). **CLOSED by the #789 round-43 web-typed-client lane** ([decisions § 1362](../architecture/decisions.md)–§ 1366): both clients and `App.Locals.supabase` now take an `AppDatabase` derived from the generated `Database`, and all 79 errors are closed. The measurement held exactly, per file — the tenth file in the count was `PlanEditor.svelte`, whose five entries are pre-existing *warnings*, not errors. Four of the 79 were live defects, not type debt: see § 1364.
- [x] **`/runs`'s source filter offers 4 of the 8 values `runs_source_check` allows, and the enumeration is unregistered.** Closed, and NOT by registering a fifth rail as the filing prescribed -- the enumeration is gone. The dropdown is now spread from `RUN_SOURCES` (already registered for `runs.source` in `check_constraint_unions.mjs`) and named through a `Record<RunSource, string>`, so tsc refuses an incomplete map and there is no second list to drift. `runs.sourceWatch` / `runs.sourceRace` added to all seven catalogues; the other four are brand marks. `lib/runs/runs_list_guards.test.ts` fails on a re-hand-enumerated list or a missing label (mutation-tested on both), because a subset of a union typechecks fine. `apps/web/src/routes/runs/+page.svelte:311-317` lists `all / app / strava / parkrun / healthkit`. The column's CHECK (`20260505_001_narrow_union_check_constraints.sql:15`) allows `app, watch, healthkit, healthconnect, strava, garmin, parkrun, race`, and both `parseRunSource` and `sourceLabel` handle all eight — so a Wear OS or Apple Watch runner sees a "Watch" badge on every card and no Watch option in the filter, and the same holds for a Garmin ZIP import (`garmin`), a Health Connect sync (`healthconnect`) and imported race results (`race`). `apps/web/scripts/check_constraint_unions.mjs:904` registers `RunSource` and `RUN_SOURCES` for `runs.source` but not this fifth, narrower rail, which is why it drifted silently — the same class § 787 closed for the PR RPC. Not fixed in the round that found it because two of the four missing labels need copy: `garmin` and `healthconnect` are brand strings, but `watch` and `race` need new keys in all seven web catalogues, and `apps/web/src/lib/i18n/` was another lane's tree. The change wanted: add the four options, add `runs.sourceWatch` / `runs.sourceRace` to every catalogue, and register the dropdown as a `strings` rail in `PAIRS` so the next value added to the CHECK fails the PR here. Owner tree: `apps/web/`.
- [x] **Four `core/data.ts` reads render a transport failure as an empty or zeroed truth, and the fix for each already exists next door.** All four discard `{ error }` (or bind it and return a neutral value), so an RLS blip or a 5xx is indistinguishable from "there is nothing here". (a) `fetchPublicRun` (`data.ts:803`) uses `.single()` without binding `error`, so a transient failure on a shared `/share/run/<id>` link renders **"Run not found"** to every recipient — verbatim the bug `fetchRunById` was fixed for 300 lines above, whose comment says so; `fetchPublicRunAttribution` (`:838`) drops it the same way. (b) `fetchRoutesWithError` (`:1552`) checks only `ownedRes.error`; the three saved-route reads use `?? []`, so a failed `saved_routes` read renders the runner's own routes with `error: null` and their 30 bookmarks silently gone — in the one function shaped to prevent exactly that. (c) `enrichClubs` (`:2565`) discards the `club_members` read, so every club reports `viewer_role: null` and an owner loses their admin controls while an active member is offered "Join". (d) `fetchFundraiserForRun` / `fetchFundraiserForEvent` (`:11595`) return null on error where their own sibling `fetchFundraiserById` throws with a comment explaining why a donor must get a retry rather than "the campaign does not exist". Each fix wants a caller change (`fetchUpcomingEvents` and `fetchPastEvents` have the same shape but their caller sits in one `Promise.all` that would blank the club page on a throw), so they were filed rather than half-applied. Owner tree: `apps/web/`. **CLOSED 2026-09-05 ([decisions § 1220](../architecture/decisions.md)).** All five reads now report the failure separately, and the two callers that had to change did: `fetchPublicRun` mirrors `fetchRunById` exactly (`PGRST116` is the only genuine miss) and `RunShareView` grew a retry branch beside `notFound`, reusing `runDetail.loadErrorTitle` / `runDetail.retry` rather than adding a key to seven catalogues; `fetchPublicRunAttribution` returns `{attribution, error}` and `/runs/[id]` routes its failure to the retry card; `fetchRoutesWithError` checks all three saved-route reads; `enrichClubs` returns `{clubs, error}`, the shape all four of its callers already return; and the two fundraiser reads throw like their documented sibling. Five source guards, each negative-tested. **The one caveat**: `FundraiserSection` already catches deliberately (a transient failure must not hide the owner's Create CTA), so the fundraiser fix makes the failure diagnosable but does not yet give a donor a retry — that surface needs new copy and is filed below.
- [x] **Two "exact" counts are unbounded reads that PostgREST truncates at 1000, one of them on the paid-registration path.** `fetchEventRsvpSummary` (`apps/web/src/lib/core/data.ts:3607`) documents that its going/maybe/declined/waitlisted counts "must stay exact even when the displayed roster is only its first page", then selects every `event_attendees` row for the instance with no `limit` and no server-side aggregate. Past 1000 rows the capacity/waitlist math believes a full event has room, and the viewer's own row can fall outside the page — so `viewerStatus` reads null and the page re-shows "Register for £X" to someone who has already paid, burning a Stripe session and a capacity-holding pending order. The error half of exactly that scenario is already hardened three lines below (`if (error) throw error`, with a comment naming it); only the truncation half is open. `fetchChallenges` (`:11413`) has the same shape plus a dropped error: its `challenge_participants` read spans every listed challenge id at once, so a couple of popular public challenges exhaust the budget and a genuine participant's own row falls past the cap — their "My challenges" tab empties and every board under-reports. `fetchChallengeById`'s read (`:11498`) is unbounded too, which is how the "offered Join to someone already in" case its own comment describes is still reachable. The fix shape is next door in both files: `event_next_instance_going_counts` pushes the tally into SQL. Note `joinChallenge` (`:11653`) is the only insert in the file that does not absorb a `23505`, so the duplicate-join that follows surfaces as a generic failure toast. Owner tree: `apps/web/` + a migration. **CLOSED 2026-09-05 ([decisions § 1222](../architecture/decisions.md)).** The "1000" in this entry was copied, not measured; it is now measured, and it is right for this stack: `PGRST_DB_MAX_ROWS=1000` on the running PostgREST v14.14, and a plain read returns `Content-Range: 0-11/*` — the total is `*`, so a client reading only `data` gets no truncation signal at all (an exact count fills it in: `0-11/12`). `fetchEventRsvpSummary` is now four `count: 'exact', head: true` requests plus one scoped single-row read for `viewerStatus`, issued together — no rows transferred, and the viewer's status no longer depends on their row appearing in a page. `fetchChallenges` / `fetchChallengeById` read `challenges.participant_count`, the trigger-maintained cache `20270308_001` added and `derived_state.md` documents, which `select('*')` was already carrying; the membership read is scoped to the caller, so it is bounded by the number of challenges listed rather than by how many people are in them. **No migration was needed** — this entry's assumption that it wanted one was wrong in both halves. The `joinChallenge` 23505 note is NOT closed and is re-filed below.
- [x] **`publishPlanToLibrary` silently drops four fields `publishPlanAsTemplate` copies.** `apps/web/src/lib/core/data.ts:4527` copies `target_pace_sec_per_km`, `target_pace_tolerance_sec`, `structure` and `notes` per workout; the club-template sibling at `:4661` copies those plus `target_pace_end_sec_per_km` and `pace_zone`, and on the plan head copies `source` and `rules` where the library path copies neither. All the columns exist (`20260420_001_plan_editor.sql:19-24`) and are rendered by `routes/plans/[id]/workouts/[wid]/+page.svelte:294`. So an author who publishes a plan whose tempo workouts carry a `pace_zone` label and a progression end-pace keeps both when publishing to a club and loses both when publishing to the public library — and `clone_public_plan` (`20270126_001:162`) does not carry them either, so every cloner gets a plan with the zone chips and progression paces gone, plus no `rules` for the adaptive re-plan. Owner tree: `apps/web/` + a migration for the clone RPC. Worth a guard that derives the two field lists from each other rather than listing them. **CLOSED 2026-09-05 ([decisions § 1228](../architecture/decisions.md)).** Confirmed by reading both bodies: the library half dropped `target_pace_end_sec_per_km` + `pace_zone` per workout and `source` + `rules` on the head. Both publishers now build their rows through the one shared list in `apps/web/src/lib/core/plan_copy.ts`, so there is nothing left for the two to disagree about, and a census guard derived from `database.types.ts` fails the PR when a new `plan_workouts` / `plan_weeks` / `training_plans` column is neither copied nor excused with a reason. Mutation-tested three ways. **The `clone_public_plan` half is NOT closed** — the RPC still drops the same four columns on read-back, which is a migration; re-filed below.
- [x] **`PUBLIC_ROUTE_LIST_COLS` is cast to the full `Route` but selects no `waypoints`, so routes bookmarked from Explore render a grey placeholder instead of a mini-map.** `apps/web/src/lib/core/data.ts:1519` selects nine columns; `:1595-1596` casts the rows `as unknown as Route[]`, and `Route` requires a non-nullable `waypoints: TrackPoint[]` plus `is_starred`, `is_public`, `description`, `tags`, `updated_at`. `data.test.ts:226` already asserts `waypoints` must be in the OWNED list because "a routes-list/picker consumer reads it", and `routes/+page.svelte:476` is that consumer — so on `/routes` your own routes get a thumbnail and every route you saved from Explore (the path `data.ts:1576`'s own comment calls "the dominant case") gets the `{#if route.waypoints}` fallback, with no crash and no error. Withholding waypoints from the public read is legitimate (`clip_route_for_viewer` owns that decision); the defect is modelling the narrow row as the wide type. The fix is either to route the thumbnail through the clip RPC or to give the public half its own narrower type — not to widen the select. Only reachable because the client is untyped (first item above). Owner tree: `apps/web/`. **CLOSED (data-layer half) 2026-09-05 ([decisions § 1229](../architecture/decisions.md)).** The diagnosis holds exactly as written. Fixed the way the entry recommends and NOT by widening the select: the public rows are filled with `waypoints: []` + `is_starred: false` through `publicRouteListFill`, where `[]` is the signal `RouteTrackPreview` already reads as "fetch this viewer's clipped line" (the shape RouteExplorer's cards pass), so the thumbnail routes through `clip_route_for_viewer` as the entry's first option asks. A guard derives the withheld set from the two column-list constants themselves, so widening `ROUTE_LIST_COLS` fails until the public half accounts for the new column. **Two halves remain and are filed below**: `/routes`'s `{#if route.waypoints && ...}` still gates the preview out (one block in a page this lane does not own), and the list rows are still typed `Route` while the projection omits `description` / `tags` / `is_public` / `updated_at` on both halves.
- [x] **The run-detail elevation chart sums ungated deltas (the array that plotted every missing altitude at sea level is fixed).** Consequence (ii) closed by [decisions § 1224](../architecture/decisions.md): `elevationSeries` in `apps/web/src/lib/runs/key_stats.ts` fills each dropout with the line between its neighbours instead of `?? 0`, keeps the series 1:1 with the track for the linked cursor, and needs two samples rather than one to mount the chart -- measured on a 30-minute alpine climb with 55 dropouts, the chart's total fell from 116,484 m to 4,460 m over a 600 m climb and its y-axis from 0-2,405 m to 1,793-2,405 m. Consequence (i) is still open and is now a single four-line change in a file the lane did not own: in `apps/web/src/lib/components/ElevationProfile.svelte`, replace the inline ungated `totalGain` sum with `computeElevationGain` from `$lib/routes/route_simplify` (`elevations.map((ele) => ({ lat: 0, lng: 0, ele }))`), so the panel grades a run by the same `ELEVATION_GAIN_MIN_DELTA_M` rule the key stat does -- 4,460 m against 3,958 m on that same track today. Fixes all five consumers of the component at once. Owner tree: `apps/web/`. `apps/web/src/routes/runs/[id]/+page.svelte:1131` derives `elevations = baseTrack.map((p) => p.ele ?? 0)` and hands it to `ElevationProfile.svelte`, whose own `totalGain` (`:42-49`) is an unfiltered sum of positive deltas. Two consequences. (i) The panel's "Total gain" and the key-stat "Elevation" grade the same run by different rules — the key stat applies `ELEVATION_GAIN_MIN_DELTA_M`, which exists because an ungated sum "reported 143 m of climb on a flat half-hour" (§ 981); the chart is that ungated sum. (ii) `?? 0` means a track with intermittent `ele` (tree cover, a tunnel, a GPX import where only some points carry `<ele>`) plots the gaps at 0 m, and `hasElevation` (`:1136`) only requires ONE point to carry an altitude — so an alpine run at 1800-2400 m gets a y-axis stretched from 0 with vertical cliffs at every dropout. Not fixed here: `lib/components/ElevationProfile.svelte` was another lane's tree, and the honest fix (drop null samples rather than zero them, and grade the chart's own total through `computeElevationGain`) spans both files. Owner tree: `apps/web/`. **CLOSED at round-40 integration** — `ElevationProfile.svelte`'s `totalGain` now reduces through `computeElevationGain` from `route_simplify`, so the tooltip and the route-detail tile share one gated reduction; all five consumers are fixed by it.
- [x] **The share-as-image card advertises a different pace than the page it was generated from.** Closed by [decisions § 1223](../architecture/decisions.md) — one `paceSeconds` derived feeds the grid, the GAP threshold and the card, and the card's time cell moved to the same clock and says so. `apps/web/src/routes/runs/[id]/+page.svelte:2236` renders `formatPace(run.duration_s, run.distance_m)` — pace over ELAPSED time — while the key-stat grid's "Avg pace" (`:941`) uses `paceSeconds = movingSeconds > 0 ? movingSeconds : run.duration_s`. On a 10 km with 10 minutes of traffic-light stops the page says 5:00 /km and the PNG the runner posts says 6:00 /km, with nothing on the card saying it is elapsed pace. Filed rather than fixed because both readings are internally coherent — the card pairs elapsed time with elapsed pace — and which one a share card should carry is a product call, not a bug fix. Owner tree: `apps/web/`.
- [x] **Back-navigating to `/runs` collapses an expanded list to 50 cards and throws away the restored scroll position.** Closed: `renderLimit` is in the snapshot payload, and the reset effect now compares the filter SET (`renderWindowKey`) rather than firing on any write to it -- a restore assigns the filters, so a one-shot suppression flag would have gone stale whenever the restored filters equalled the mount-time defaults and the effect never re-ran. `runs_list_guards.test.ts` derives its requirement from `capture`'s own field list, so any future field that is captured and never restored fails too; mutation-tested against a dropped restore line and a removed comparison. `apps/web/src/routes/runs/+page.svelte:289` holds `renderLimit` as component state; it is not in the `snapshot.capture` payload (`:451-462`), and the `$effect` at `:294-306` resets it to `PAGE_SIZE` whenever any filter signal changes — which the restore itself does (`:472-477`). So: filter to "All time · Longest" (which forces `fetchMode = 'full'`), press "Show 50 more" three times to reach 200 cards, scroll to card ~180, open one, press Back — the runs, filters and `hasMore` all restore, the `rAF` calls `window.scrollTo(0, s.scrollY)`, but only 50 cards are mounted so the document is a quarter of its captured height and the browser clamps the scroll to near the top. Exactly the failure the snapshot block's own comment (`:427-438`) exists to prevent. The fix is to carry `renderLimit` in the snapshot and suppress the reset on a restore-driven filter write. Owner tree: `apps/web/`.
- [x] **`/runs/heatmap`'s "Back to runs" button goes to `/history`.** Closed: the link is `/runs`, and it goes BACK rather than forward when the runner arrived from `/runs`, so the snapshot (filters + scroll) survives -- the same `afterNavigate` + `handleBack` shape `/clubs/[slug]` and `/runs/[id]` already use, kept as a real `<a>` so a deep hit, a middle-click and the keyboard still work. Pinned by `apps/web/tests-e2e/runs/list-navigation.spec.ts` (Playwright, NOT run in the lane). `apps/web/src/routes/runs/heatmap/+page.svelte:25` is `href="/history"` under the label `runsHeatmap.backToRuns`. The only entry point to the page is the `/runs` toolbar link (`runs/+page.svelte:654`), so the button reliably lands the runner on the cross-modal timeline instead of the run list they came from — and because it is a push rather than a back, it discards the `/runs` snapshot (filters + scroll) too. One-line, but it belongs with whoever settles the `/runs` vs `/history` relationship. Owner tree: `apps/web/`.
- [x] **`buildBoard` + `projectRunner` fail OPEN to `safe` on an unusable crossing time, on a race-director cutoff board.** Closed by [decisions § 1225](../architecture/decisions.md) on both halves of the pair plus the web-only glue -- `gradeCutoff` returns null on a non-finite margin, `projectRunner` drops a non-finite `elapsedS`, and `buildBoard` drops a crossing that is unparseable or predates the race start instead of clamping it. Also caught: an unusable stamp on the FINAL checkpoint used to promote the runner to `finished`. `apps/web/src/lib/runs/checkpoint_board.ts:99` computes `elapsedS: Math.max(0, (new Date(c.inTime).getTime() - raceStartMs) / 1000)`, and `Math.max(0, NaN)` is **NaN**, not 0. `gradeCutoff` (`checkpoint_projection.ts:76`) is `marginS < 0 ? 'miss' : marginS < CUTOFF_TIGHT_S ? 'tight' : 'safe'`, so a NaN margin fails both comparisons and lands on the optimistic terminal branch: the board tells a race director a runner made a cutoff nothing is known about, with `marginS: NaN` beside it. `projectRunner` already fails CLOSED one line up — it refuses `paceSPerM` unless `lastElapsedS > 0`, with a comment saying an unusable sample must leave cutoffs ungraded — but the reached-checkpoint verdict never got the same treatment. Same clamp also turns a crossing stamped before the race start (a volunteer's tablet running fast, the precise case that comment names) into `elapsedS: 0`, which then grades `safe` with the full cutoff as its margin. Neither suite has a NaN or unparseable-`inTime` case. Not fixed in the round that found it because `checkpoint_projection` is an enforced TS↔Dart parity pair and the Dart half's `gradeCutoff` has the identical ladder — hardening one side alone would diverge the pair. The change wanted, on both halves together: `gradeCutoff` returns null on a non-finite margin, and `buildBoard` drops a crossing whose elapsed time is not finite rather than clamping it. Owner trees: `apps/web/` + `apps/mobile_android/` + `apps/mobile_ios/`.
- [x] **`recap.ts`'s `currentStreakDays` is structurally always 0 for the year or month you are living in.** `apps/web/src/lib/runs/recap.ts:313` anchors `computeRunStreaks` at `new Date(year, 11, 31, 23, 59)` and `:448` at the end of the month. For the CURRENT period that instant is in the future, so the streak walk looks for a run on 31 Dec, then on 30 Dec (the Strava grace day), finds neither, and reports `current: 0` — a runner on a live 40-day streak opening `/recap/2026` today gets zero. `recap.test.ts:451` asserts `currentStreakDays === 0` and rationalises it ("Anchored at 31 Dec 2026, nine months after the last run"), which is *also* the right answer for that fixture, so the suite is green over a case it never exercises. Web renders only `bestStreakDays` today, so this is a wrong value in the exported `YearInRunningRecap` contract and in every published snapshot rather than on screen — but mobile's `recapSnapshotJson` serialises the same field. Not fixed here: `recap` is an enforced TS↔Dart parity pair, the anchor should be `min(now, endOfPeriod)` on both halves, and the existing test needs a second fixture with a run dated today before the assertion means anything. Owner trees: `apps/web/` + `apps/mobile_android/` + `apps/mobile_ios/`. **CLOSED (web half) 2026-09-05 ([decisions § 1221](../architecture/decisions.md)).** Both builders now anchor `current` at the earlier of the period's end and `now`, threaded in as an optional parameter so it is stateable in a test; `best` keeps the period's own end so a past year still clamps at its own 31 Dec. The test that agreed for the wrong reason was rewritten to state its `now`, and three new cases cover what it never exercised — a streak running up to and including today, the grace day one morning before today's run, and a past year still clamping. Reverting the fix now fails with `a streak running up to and including today is the current streak — actual: 0, expected: 4`. **The Dart half is NOT done and the pair is temporarily diverged** — see the filing below.
- [x] **`recap.dart` reads only `metadata.elevation_m` where `recap.ts` reads the promoted column first — a live parity-pair divergence.** `apps/mobile_android/lib/recap.dart:237` calls `metadata.elevation_m` "the canonical key (jsonb)". It has not been canonical since migration `20270302_001` promoted total ascent to `runs.elevation_gain_m`; the web half reads the column and falls back to the key (now via `key_stats.storedElevationGainM`, [§ 1187](../architecture/decisions.md)), and the vert challenge aggregate sums the column. Latent rather than live today, because the migration backfilled column-from-key and both current writers populate both — but the moment any writer sets only the column, the phone's Year-in-Running total will silently omit that run's climb while the web's counts it. Owner trees: `apps/mobile_android/` + `apps/mobile_ios/` (the Dart half of a pair the web half of this round changed). **Closed** by [§ 1240](../architecture/decisions.md), at a different place than the entry named. The fix cannot live in `recap.dart` — the Dart domain `Run` has no `elevationGainM`, and `ApiClient._runFromRow` dropped `elevation_gain_m` while surfacing `activity_type` / `is_dnf` / the four `fastest_*_s` / `track_url` / `hr_series_url` back onto the bag. Measured with `ApiClient.debugRunFromRow`: a row with `elevation_gain_m: 312.5` and no bag key gave `totalElevationM: 0.0` before and 312.5 after. The seam now stashes it, and the read-side guard the omission survived for lack of — derived from `kRunPromotedMetadataColumns` + `kRunMirroredMetadataColumns`, five planted mutations each caught — is in `packages/api_client/test/api_client_codecs_test.dart`. The write direction was checked and is not implicated: `_runUpsertBody` strips nulls, so a round trip never cleared the column.
- [x] **A walk-run session's rest steps render the raw slug `walk` on the web run detail, in every locale.** Closed: `stepLabel` gains a `walk` case with `runDetail.stepWalk` / `runDetail.stepWalkNumbered` in all seven catalogues (wording taken from the mobile `ttsStepWalk` ARBs so the page says what the phone said), and the `default:` branch now resolves `runDetail.stepOther` rather than the raw slug -- `kind` is a `string` off a schemaless bag written by a phone that ships independently of the web build. `workout_review_guard.test.ts` reads the recorder's own `WorkoutStepKind` enum and fails when a kind has no case, so the seventh kind cannot land unlabelled; mutation-tested against a removed case, a slug-returning default and an added enum member. `apps/web/src/routes/runs/[id]/+page.svelte`'s `stepLabel` handles `warmup / cooldown / steady / rep / recovery` and then `default: return s.kind`. `WorkoutStepKind.walk` is a real serialized kind (`packages/run_recorder/lib/src/workout_runner.dart:501`), emitted whenever `recovery_pace == 'walk'` — which is exactly what the beginner walk-run generator produces (`training.dart:630`), the preset the onboarding wizard seeds for a beginner goal. So a couch-to-5k runner whose phone showed "Run 1/8" / "Walk 1/7" opens the same run on the web and sees a bare lowercase **`walk`**, untranslated, for a French or Japanese reader too. Needs `runDetail.stepWalk` + `runDetail.stepWalkNumbered` in all seven catalogues, which is why it was filed rather than fixed in a lane that did not own `lib/i18n/`. Note the `default:` branch should render *something* named rather than the slug for any future kind. Owner tree: `apps/web/`.
- [x] **`gradeAdjustedPaceSecPerKm` returns NaN out of a `number | null` signature on a non-finite coordinate, and the Dart twin throws.** Closed by [decisions § 1226](../architecture/decisions.md) on both halves plus the iOS twin. The filed guard is applied, and the root cause with it: an unusable segment is now SKIPPED the way a segment with no timestamps already is, so one bad fix no longer erases a three-hour ultra's GAP. Also covers a non-finite altitude, the second way a NaN reached the accumulator. With the shared clamped haversine now imported ([§ 1187](../architecture/decisions.md)) a valid coordinate pair can no longer produce NaN, but a track point carrying a non-finite lat/lng still can: `segHoriz` goes NaN, `NaN < MIN_SEGMENT_M` is false so the anchor walk never resets, and the final `adjDistM <= 0` guard is false for NaN so `Math.round(NaN)` is returned. Dart's `(timeS / (adjDistM / 1000)).round()` **throws** `UnsupportedError` on the same input, from inside a widget build. The change wanted, on both halves together: `!(adjDistM > 0) || !(timeS > 0)`, the NaN-rejecting form `race_day.ts:244`'s `goalFeasibility` already uses in the same directory. Left out of the fixing round deliberately — applying it to one half of a registered pair is the divergence the round was closing. Owner trees: `apps/web/` + `apps/mobile_android/` + `apps/mobile_ios/`.
- [x] **`race_day.ts`'s negative-split header states a delta the code does not apply, and its pacing guards let NaN through.** Closed by [decisions § 1227](../architecture/decisions.md) -- the HEADER was wrong and moved, because `race_phases.ts` independently ships the same ±`deltaPercent`-per-half reading for the same named strategy and is a pair with a firmware port. One correction to the filing: `race_day.test.ts:179` is a COMMENT, not an assertion, so nothing in the suite held the magnitude; two assertions now do, one of them derived from `buildPhasePlan`'s own factors so the two implementations fail the PR the day they diverge. NaN guards applied to both helpers plus `unitMetres` and `deltaPercent`. The doc comment at `apps/web/src/lib/runs/race_day.ts:81-87` says "A 2% negative split for a 4:30/km marathon means first half ~4:33, second half ~4:27". The code (`:100`) applies `delta = avgPerUnit * deltaPercent / 100` to each half, so 2% gives 270 ± 5.4 s = **4:35 / 4:25** — the documented example describes a total-spread-of-2% implementation, i.e. 1.11%. The code is right (`race_day.test.ts:179` pins ±2% of 300, and `race_phases.ts:387` independently uses 1.02/0.98 for the same strategy); only the header is wrong, and a maintainer tuning `deltaPercent` against it is off by ~2×. Separately, `evenSplitPacing` (`:60`) and `negativeSplitPacing` (`:95`) guard with `distanceM <= 0`, which NaN passes — the panel then renders a NaN average pace and one bogus split holding the whole predicted time — while `goalFeasibility` (`:244`) in the same file already uses the NaN-rejecting `!(x > 0)`. Owner tree: `apps/web/`.
- [x] **Three smaller `lib/runs` items found by the same sweep.** All three resolved, one of them by disagreeing with the filing. (a) The `> 50` floor is now the named `SPLIT_TAIL_MIN_M` and the comment states the consequence outright -- the split table's total may be up to one floor short of the headline distance, because a wrong pace on a named row is worse than a total under one percent light on a 5 km -- pinned by a test that bounds the shortfall by the constant rather than by 50. Note the filing's framing is off in one respect: `run_stats` is NOT a registered parity pair (mobile derives splits from the recorder's ticks, not from the track), so there is no Dart half to move. (b) `extraCount` replaces the four `Math.max(0, Math.trunc(x ?? 0))` sites; the Dart half needs no mirror because `RecapExtras.photoCount` is an `int` there and cannot be non-finite -- verified, and the web case is pinned. (c) NOT changed, and the reasoning is now in both halves' doc comments: `mondayOf` is deliberately ISO-Monday and not `week_start`, because a recap is a snapshot PUBLISHED to `public_recaps`, re-rendered by `/recap/share/[id]` and the OG image for readers who are not the runner, and written by the Dart producer from another device -- a week boundary varying with a preference would make one artifact mean two things with nothing in the payload saying which, and honouring it would be a signature change through a registered pair plus a re-interpretation of every snapshot already published. `current_week.ts` is the runner's own frame and rightly differs. Web gains 2 tests the Dart side does not: the `extraCount` NaN case it cannot reach, and a direct `mondayOf` case blocked there by `_mondayOf` being private (the behaviour is covered on both sides by the existing `topWeek` tests). (a) `run_stats.ts:126` says "Final partial split if there are remaining metres" over a `> 50` floor, so a 5.04 km run renders 5 splits summing to 5000 m and the table's total silently disagrees with the run's headline distance — the floor is a reasonable choice, the sentence above it just does not say so. (b) `recap.ts:320` / `:455` clamp with `Math.max(0, Math.trunc(extras.photoCount ?? 0))`, which propagates NaN rather than flooring it, so a non-numeric count from `fetchRecapExtras` lands as `NaN` in the recap object and in every published snapshot. (c) `recap.ts:166`'s `mondayOf` hardcodes a Monday-anchored week for the top-week aggregation while `training/current_week.ts` (a registered pair) honours the runner's `week_start` pref over the same runs — so a US Sunday-first runner's big Sunday long run is attributed to different weeks by the dashboard and by the recap. All three are inside the `recap` / `run_stats` parity pairs, so each wants its Dart half in the same change. Owner trees: `apps/web/` + `apps/mobile_android/` + `apps/mobile_ios/`.
- [x] **`insertFitnessSnapshot` writes a row on every `/dashboard` mount and the trend chart holds 60 points.** `apps/web/src/lib/core/data.ts:979` gates on data sufficiency (`vdot == null && chronicLoad == null && qualifyingRunCount < 3`) under a comment claiming it stops write-spam; there is no unique index, no per-day dedupe and no retention on `fitness_snapshots` (`20260507_001:26-58` has only a non-unique index), and `dashboard/+page.svelte:565` calls it on mount. `fetchFitnessSnapshots(60)` returns the newest 60, so a runner who opens the dashboard three or four times a day fills every slot with duplicate same-day points inside ~2½ weeks and the real multi-month trend scrolls off. The `{ error }` is unbound and the caller's `.catch(() => {})` cannot fire either, because supabase-js resolves rather than throws on a PostgREST error — a fact `data.ts:1349` states explicitly elsewhere. Wants a per-(user, day) unique index plus an upsert. Owner tree: `apps/web/` + a migration. **CLOSED (caller half) 2026-09-05 ([decisions § 1232](../architecture/decisions.md)).** Every claim in this entry was checked and holds: no unique index, no per-day dedupe, the `{ error }` unbound, and `fetchFitnessSnapshots(60)` newest-first. The entry's proposed fix — "a per-(user, day) unique index plus an upsert" — is half wrong, and the measurement is worth keeping: `fitness_snapshots` has self SELECT / INSERT / DELETE policies and NO UPDATE one, so `ON CONFLICT DO UPDATE` is refused by RLS; and `on_conflict=` names columns, not expressions, so it cannot address a derived-day target at all. The caller therefore gates on a read of the newest `computed_at` plus a pure `fitnessSnapshotDue` (UTC day, fail-closed) and absorbs a 23505 on the insert — correct with no constraint, race-proof once one exists. The index half (`fitness_snapshots_user_day_uniq`) is the backend lane's. DDL half CLOSED 2026-09-05 ([decisions § 1235](../architecture/decisions.md#1235-one-fitness-snapshot-per-runner-per-day-and-the-append-only-shape-that-made-the-duplicates))**, migrations `20270710000001` + `20270710000002`. `fitness_snapshots_user_day_uniq` over `(user_id, snapshot_day)` — a stored `date` derived from `computed_at` by a BEFORE INSERT trigger and pinned on UPDATE. A stored column rather than an expression index because `timezone()` is STABLE (Postgres refuses the expression in an index and in a STORED generated column alike) and because PostgREST's `on_conflict` takes columns, never an expression or an index name. Existing duplicates are reduced first, batched per user, keeping the greatest `(computed_at, id)` per group — with the migration stating plainly that batching inside a wrapped file bounds the work per statement and not the lock (§ 1148). **An UPDATE policy ships with it**: the table was append-only, which only worked because every mount appended — enforce one row per day without an update path and the day's numbers freeze at the first mount. 15 pgtap assertions, 7 planted defects. The client half (read-then-insert with a 23505 absorb, or an upsert now that the UPDATE arm exists) is the `web-data` lane's. **Both halves are in one PR, so the sequencing note in the caller half is historical: the UPDATE policy the DDL half added means an upsert is now possible, and the read-then-insert path was written to be correct either way.**
- [x] **`data.ts` has one Storage call routed through the table registry and one nested-embed read that does not normalise.** `apps/web/src/lib/core/data.ts:1319` is `supabase.storage.from(TABLES.runs)` where every sibling uses `BUCKETS.runs` — the same string today, which is exactly why a later rename of either registry would break it silently. And `fetchNextRsvpedEvent` (`:2952`) reads `ev.clubs.slug` directly while `fetchEventClubSlug` (`:3011`) and `fetchNotifications` (`:8384`) both normalise the same PostgREST embed with `Array.isArray(clubs) ? clubs[0] : clubs`; if the array shape ever comes back here the dashboard card either throws or deep-links to `/clubs/undefined/...`. Owner tree: `apps/web/`. **CLOSED 2026-09-05 ([decisions § 1230](../architecture/decisions.md)).** One correction: there are TWO Storage calls on the table registry, not one — `saveRun`'s track upload and its HR-sidecar upload, both spelled `supabase.storage\n.from(TABLES.runs)` across a line break, which is why a single-line grep found neither. Both now name `BUCKETS.runs`, and a guard asserts every `supabase.storage.from(...)` in the file does. The embed half was right as filed: `fetchNextRsvpedEvent` read `ev.clubs.slug` through where three siblings normalised, so the collapse is now the shared `singleEmbed` in `data_normalise.ts` at all four sites, the card skips a candidate whose slug will not resolve, and a guard fails on any restated `Array.isArray(x) ? x[0] : x`.

### Surfaced by the #789 round 39 web-core lane, second wave (2026-09-05)

- [x] **`recap.dart` still anchors the current streak at 31 December, so the pair is diverged and a phone-published snapshot of an in-progress period carries a zero.** [§ 1221](../architecture/decisions.md). The web half is fixed; `apps/mobile_android/lib/recap.dart:345` is `final endOfYear = DateTime(year, 12, 31, 23, 59);` and `:483` `final endOfMonth = DateTime(year, month + 1, 0, 23, 59);`, each passed straight to `computeRunStreaks` as its `today` anchor. The change is the same shape as the web one and needs no new concepts: add a trailing optional `DateTime now = ...` to `buildYearInRunningRecap` (`:248`, currently `[RecapExtras extras = const RecapExtras()]`) and `buildMonthInRunningRecap` (`:408`), thread it through the month builder's own call to the year builder (`:414`), add the two-line `currentAnchor(periodEnd, now)` helper, and pass `currentAnchor(endOfYear, now)` / `currentAnchor(endOfMonth, now)`. Dart cannot default a parameter to `DateTime.now()` (it is not a compile-time constant), so make it `DateTime? now` and resolve `now ?? DateTime.now()` inside — that is the only shape difference from the TS side. Mirror the three new web tests into `apps/mobile_android/test/recap_test.dart` (the web suite is the reference), then run the iOS twin mirror. Mobile's `recapSnapshotJson` serialises `currentStreakDays` into `public_recaps`, so until this lands a snapshot published from a phone for the current year or month renders a zero on the web share page while the web-built one renders the real streak. Owner trees: `apps/mobile_android/` + `apps/mobile_ios/`. **Closed** by [§ 1239](../architecture/decisions.md): ported with the `DateTime? now` shape the entry describes, six mirrored cases (two of which the filing's own reasoning would have written wrong — see the ADR), all verified failing against the old anchor.
- [x] **A donor still cannot tell a failed fundraiser read from "no campaign" — the data layer now throws, the surface still degrades silently.** [§ 1220](../architecture/decisions.md). `fetchFundraiserForRun` / `fetchFundraiserForEvent` throw now, which is the root fix, but `apps/web/src/lib/components/FundraiserSection.svelte:41-53` catches and sets `fundraiser = null` on purpose — a deliberate policy that a transient failure must not hide the owner's "Create fundraiser" CTA, and correct as far as it goes. The consequence for the other reader is unchanged: a non-owner following a shared link to a run whose fundraiser read failed sees the section render nothing at all, exactly as if the runner had never created one. The component already models a *second* failure precisely (`totalsFailed`, so a totals blip is not shown as "0 raised"), which is the pattern to copy: add a `loadFailed` state, keep the owner-CTA fallback for the owner, and render a one-line retry for everyone else. Needs a new key in all seven catalogues under `apps/web/src/lib/i18n/locales/` — the reason it was not taken in a lane that does not own that tree. Owner tree: `apps/web/`. **CLOSED 2026-09-05 ([decisions § 1231](../architecture/decisions.md)).** Taken exactly as prescribed — a `loadFailed` state, the owner-CTA fallback kept, a one-line `role="alert"` retry for everyone — plus the Playwright spec `tests-e2e/fundraising/section-load-failure.spec.ts` (written, NOT run in-lane), whose second case is the control the first needs: a run with genuinely no campaign must still render nothing. **This entry's stated blocker was wrong**: `fundraiser.loadFailed` and `fundraiser.retry` already exist in all seven catalogues (`/fundraisers/[id]` added them), so no new key was needed and the i18n-tree ownership was never the obstacle.
- [x] **`joinChallenge` is the only insert in `data.ts` that does not absorb a `23505`, so a double-tap on Join surfaces a raw failure toast.** Carried forward from the entry above it, which is otherwise closed. `apps/web/src/lib/core/data.ts`'s `joinChallenge` inserts into `challenge_participants` and rethrows whatever comes back; the table's `(challenge_id, user_id)` primary key makes a second tap — or a tap on a stale list where `joined` was computed before the fix landed — a `23505` unique violation, which is the success case restated. Every other join-shaped insert in the file swallows it. One-line, but it wants the same `23505`-is-idempotent treatment applied consistently rather than case by case. Owner tree: `apps/web/`. **CLOSED 2026-09-05 ([decisions § 1230](../architecture/decisions.md)).** Confirmed: `joinChallenge` rethrew unconditionally while five siblings compared `error.code !== '23505'` themselves. Taken as the filing asked — consistently, not case by case: `isDuplicateKeyError` now lives in `core/supabase_error.ts` and all six sites route through it, with a guard that fails when any site restates the literal and one that names the four join-shaped writers. The predicate is exact, not a `235*` prefix, so a dangling FK / CHECK / RLS refusal still reaches the caller.
- [x] **The `enrichClubs` fail-closed choice returns an empty club list on a membership-read blip, which is honest but is not what a member wants to see.** [§ 1220](../architecture/decisions.md). The fix made a failed `club_members` read report `{ clubs: [], error }` rather than asserting non-membership, which is right — but it discards clubs the caller *did* successfully read. Every caller renders the error banner instead of the list, so nothing is lost today; the better end state is for the club rows to render with an explicit "couldn't check your membership" affordance rather than the whole page becoming an error, since browsing a club list does not require knowing your own role. Needs new copy and a decision about which surfaces degrade that way. Owner tree: `apps/web/`. **CLOSED 2026-09-05 ([decisions § 1231](../architecture/decisions.md)).** `enrichClubs` now returns the rows it read with every `viewer_role` / `viewer_status` null and the error beside them, and `SocialClubs` renders the list under a `socialClubs.membershipUnknown` notice (new, in all seven catalogues) when it has rows, keeping the full error card for when it has none. One surface is deliberately NOT degraded that way and the guard pins it: `fetchClubBySlug` still fails the whole read, because an unknown role on a club detail page silently downgrades an owner to the member view — a worse lie than an error. Four mutations tested.

### Surfaced by the #789 round 39 casefold lane (2026-09-05)

The flagship item closed in full — all three rails moved together and now answer
identically at every assignable code point. Two of the filing's numbers were
wrong and are corrected in [decisions § 1175](../architecture/decisions.md).
What follows is what the lane found beside it and could not take, because every
one of them lives outside the lane's tree.

- [x] **Six surfaces group exercises by `trim().toLowerCase()` instead of `normaliseExerciseName`, and two of them lose a PR badge because of it.** CLOSED [§ 1248](../architecture/decisions.md) + [§ 1249](../architecture/decisions.md) + [§ 1250](../architecture/decisions.md) — with three corrections to the filing (there are SIX web sites, not five: `+page.svelte:270` was unlisted; the "vs last time" hint at `:537` reads a map keyed by the same naive expression that writes it, so it was self-consistently wrong rather than blank; and the first block always kept its chip, only later spellings lost theirs). Two further mobile sites the audit missed are closed too. `WorkoutPrResult` now carries `key` so no caller re-derives it, `distinctExerciseCount` gives the three counts one home, and a source guard on each platform stands over the class. Found while auditing what else folds an exercise name. Five on web — `apps/web/src/routes/gym/[id]/+page.svelte` lines 257, 326, 537 and 541, and `apps/web/src/routes/dashboard/+page.svelte` line 252 (`liftExerciseCount`) — and one on mobile, `gymExerciseCount` in `apps/mobile_android/lib/screens/gym_screen.dart` line 43 (and its iOS twin). None of these is a persisted key, so none can raise a 23514; they are all worse in a quieter way. **The counts are wrong**: `trim().toLowerCase()` does not collapse an internal whitespace RUN, so "Bench  Press" and "Bench Press" in one session are counted as two exercises by the header summary, the dashboard card and the mobile list while every other surface — the PR tracker, `gym_workout_summaries`, `gym_exercise_set_history` — buckets them as one. **The two lookups are worse than wrong, they silently return nothing**: `prByExercise` is keyed at line 257 by `workoutPrs`' returned `exerciseName`, which is the DISPLAY spelling of the first set that mapped to the bucket, and is read back at 537/541 by `block.name.trim().toLowerCase()`, which is the block's own spelling. Two spellings of one lift in one workout — the exact case the canonical key exists to merge — miss each other, and the "vs last time" hint and the PR badges just do not render, with no failure anywhere. The durable fix is one line each: import `normaliseExerciseName` from `$lib/gym/gym_prs` / `gym_prs.dart` and key on it, in the same change on both platforms. Worth a guard too — nothing stops a seventh site being written, and a grep-shaped source test over `apps/web/src/routes` + `apps/mobile_android/lib` for a `toLowerCase()` applied to an `exercise_name` would have caught all six. Owner trees: `apps/web/src/routes/`, `apps/mobile_android/lib/screens/` — this lane owned neither.

- [x] **A phone that has not updated can no longer write 465 exercise names rather than 410, until it does.** DECIDED: an honest doc note, not a client-visible mitigation — [§ 1252](../architecture/decisions.md), note landed in `docs/features/gym_programming.md`. Measured: of the 465, ZERO lie in Latin-1 Supplement, Latin Extended-A, Latin Extended-B, Cyrillic, Latin Extended Additional or Greek Extended, so reaching the refusal means naming a lift in Cherokee, Georgian Mtavruli, Deseret, Adlam, Garay, Medefaidrin, Vithkuqi or Sidetic on a pre-§ 1175 build. A special-cased "update the app" message would also be a lie in the general case: the current client raises the same `23514` for version-independent reasons and cannot tell the two apart without re-deriving the server's fold. The durable fix is re-filed below. Stated as a measured consequence of [§ 1175](../architecture/decisions.md), not as a regression: the fold now WIDENS on the server at 55 code points, so a client build from before this change disagrees with the server's CHECK at 465 code points where it previously disagreed at 410. Every one of the 55 is in a script no shipped exercise name plausibly carries — U+1C89, the Latin Extended-D additions U+A7CB/CC/CE/D2/D4/DA/DC, the Garay block U+10D50-65, the Sidetic block from U+16EA0 — and the surface closes to ZERO the moment the client updates, where before this change it never closed at all. There is no code owed; what is owed is a decision about whether a `23514` from `gym_routine_exercises_exercise_key_canonical` or `exercises_name_key_canonical` should reach the user as "update the app" rather than as the generic save failure it is today. That message lives in the mobile save paths, which this lane did not own.

- [x] **The Dart `toLowerCase()` gap is 465 code points wide for every OTHER Dart consumer too, and only this key is now protected.** SURVEYED, and the filing measured it with the wrong instrument — see [§ 1251](../architecture/decisions.md). 465 is Dart against the FROZEN TABLE; a site that must merely agree with a web rail is governed by Dart against JS, which is 150 code points over U+0020-U+2FFF, of which ONE is in Latin-1 Supplement + Latin Extended-A (U+0130), one in Greek and Coptic (U+037F), and ZERO in Latin Extended-B, Cyrillic, Latin Extended Additional or Greek Extended. The reachable survivors are ranked and re-filed individually below; everything else is unreachable by measurement (handle match bounded by `^[a-z0-9_]{3,30}$`, certificate filename strips to `[A-Za-z0-9]` before folding, uuid hex, and ~20 machine-token folds). The frozen table fixes the exercise key. It does not fix `catalogue_browse.dart` (which § 854 already closed by generating its own case mapping into `catalogue_fold_table.dart`), and it does not fix any other Dart site that lower-cases user text for comparison — `normaliseE164`, club/segment name matching, the search paths. None of those persists a value under a CHECK, so none can produce a refusal; the failure mode is the quiet one, a phone matching fewer strings than the web does for the same query. The measurement is in hand and cheap to re-run (`scripts/gen_exercise_fold_table.mjs` renders the authority; Dart's own table is 1,023 non-identity code points against the frozen 1,488). What is not in hand is a survey of which Dart comparisons are user-visible enough to be worth a table lookup, which is its own slice.

- [x] **`scripts/gen_exercise_fold_table.mjs` has no `package.json` alias and no line in the parity registries.** ALREADY CLOSED, and the filing was stale on all three parts — the integrator applied them inside PR #857 itself, the same commit that filed this box. Verified at `b7f3467a3`: `package.json:90` carries `"gen:exercise-fold-table"`, the repo-root `CLAUDE.md` `gym_prs` paragraph carries the FOURTH-rail sentence naming the generated tables and [§ 1176](../architecture/decisions.md), and `.claude/agents/shared-library-syncer.md:29` names both generated files on the `gym_prs` row. No code owed. Three one-line edits this lane could not make because all three files belong to other trees. (1) Root `package.json` should carry `"gen:exercise-fold-table": "node scripts/gen_exercise_fold_table.mjs"` beside `gen:icon-font`, so the generator is discoverable the way every other committed-artifact generator is. (2) The repo-root `CLAUDE.md` parity paragraph's `gym_prs` entry should say the pair now carries a FOURTH rail — the generated `exercise_fold_table.ts` / `.dart` frozen at a named Unicode version, compared by `check_shared_constants.mjs`, never hand-edited — and that regenerating it is a migration, not a codegen refresh ([§ 1176](../architecture/decisions.md)). (3) `.claude/agents/shared-library-syncer.md`'s `gym_prs` row should name the two generated table files so an agent editing one side is told about the other. Exact wording was handed to the integrator with this lane's report.

### Surfaced by the #789 round 39 mobile-testinfra lane (2026-09-05)

- [x] **`apps/mobile_android/CLAUDE.md` § Tests still ends by telling the reader to pump past `showTopBanner`'s timer.** Its last sentence reads "Waiting for a screen's completion path often also arms `showTopBanner`'s 3 s dismissal timer, which then trips `A Timer is still pending`: pump past it (decisions § 1131)." That is false as of [§ 1195](../architecture/decisions.md) -- the pill owns its own clock now, so a screen test that reaches a completion path needs no drain at all, and 60 of the 70 that carried one had it deleted. The replacement sentence is in `docs/testing/testing.md` § Patterns 5 and can be copied down: a pending timer under a widget you control is that widget's bug, not the test's, and the assertion is raised before any `tearDown` so no harness cleanup can reach it. `apps/mobile_android/CLAUDE.md` belonged to another lane this round. Owner tree: `apps/mobile_android/CLAUDE.md`. **Closed** by [§ 1238](../architecture/decisions.md): the sentence now states § 1195's rule, and the stale `route_conditions_test.dart` parenthetical (whose drain is the undo window, not the banner) was corrected in the same pass.
- [x] **`_outsideWindow` is a byte-identical private static in two stores and hand-rolled inline in a third.** Found while measuring the seven `replaceFromServer` gates for [§ 1198](../architecture/decisions.md). `local_food_store.dart:339` and `local_gym_store.dart:341` carry the same five-line predicate character for character (`at == null` is inside the window; `start` is inclusive, `end` exclusive), and `local_routine_store.dart:293` open-codes `windowStart != null && entry.value.lastModifiedAt.isBefore(windowStart)`, which is that predicate with a null `end` and the null-`at` branch unreachable behind a non-nullable field. Nothing compares the three, so the half-open convention can drift on one and change which synced rows a fill is allowed to clobber -- the same class of silent divergence the parity registry exists for, one platform down. The home is obvious (`OfflineSyncStore` itself, `@protected static`), which is why this is a filing and not a defect: it is a mechanical move, but it touches four store files plus their iOS twins and belongs to whoever is already in them. Not the duplication § 663 filed -- that one was the gate, and the gate turned out to be identical. Owner tree: `apps/mobile_android/lib/local_*_store.dart` + `offline_sync_store.dart` + the iOS twins. **Closed** by [§ 1242](../architecture/decisions.md): now the top-level `outsideFetchWindow` in `offline_sync_store.dart` (top-level, not `@protected static` — `@protected` is an instance-member annotation), with the first direct tests the predicate has ever had, four planted mutations each caught.
- [x] **`_parseTs` is a byte-identical private static in six stores.** Measured while extracting `outsideFetchWindow` for [decisions § 1242](../architecture/decisions.md): `local_food_store.dart:329`, `local_gear_store.dart:256`, `local_gym_store.dart:333`, `local_meal_template_store.dart:271`, `local_recipe_store.dart:273` and `local_routine_store.dart:405` each carry `static DateTime? _parseTs(dynamic v) { if (v is String && v.isNotEmpty) return DateTime.tryParse(v)?.toUtc(); return null; }` character for character. Re-derive with `command grep -rn "_parseTs" apps/mobile_android/lib/*.dart | command grep "static DateTime"`. It is a larger instance of the shape § 1242 just closed and the same argument applies: every one of them feeds the newer-wins comparison (`local.lastModifiedAt.isAfter(serverTs)`), so a copy that stopped normalising to UTC, or that started accepting a non-String, would let a stale server fetch clobber a locally-newer synced row on that store alone. The home is the same one — beside `outsideFetchWindow` in `offline_sync_store.dart`. Mechanical, but twelve files with the iOS twins, and three of the six belonged to no lane in round 40. Owner tree: `apps/mobile_android/lib/local_*_store.dart` + `offline_sync_store.dart` + the iOS twins. **CLOSED 2026-09-06 ([decisions § 1289](../architecture/decisions.md), [§ 1290](../architecture/decisions.md), [§ 1291](../architecture/decisions.md)).** The byte-identical claim was verified before anything moved and holds exactly (all six bodies hash the same). The finding is that the same read exists NINETEEN times in the family in five behaviourally distinct spellings: the six, plus `local_crossings_store.dart`'s `_parseTime` and the inline `StoredFood.startedAt` / `StoredGymWorkout.startedAt` getters (which drop the `.toUtc()`), `entriesForRange` + `_idsInWindow` (open-coded again), seven `fromJson` clock reads, and gear's `_parseDate` — whose missing `.toUtc()` is LOAD-BEARING, because `purchased_at` / `retired_at` are `date` columns and normalising local midnight moves the calendar day. All the timestamptz readers now go through `parseServerTimestamp` in `offline_sync_store.dart` (§ 1242's home); `_parseDate` stays, marked, with the reason stated. `store_timestamp_reader_guard_test.dart` derives the family from `extends OfflineSyncStore<` and refuses any other `DateTime.parse` / `tryParse` without a `zone-verbatim:` reason; five mutations planted, five caught.
- [x] **Nine banner drains and two `_drainBanner` helpers survive [§ 1195](../architecture/decisions.md), each doing something else as well.** The 60 that were the last statement of their test are gone. What is left is not folklore that was missed -- every one of them is followed by further pumping whose purpose is not the banner (`event_detail_calendar_test.dart:290`, `route_detail_screen_test.dart:245` + `:368`, `route_markers_panel_test.dart:388` + `:427`, `segments_panel_test.dart:241`, and two in `run_screen_recording_flow_test.dart` that advance the RECORDING clock mid-test, which is why they were deliberately put back after the sweep first removed them). Their comments now name a reason that no longer applies, so each needs reading on its own terms: keep the pump, rewrite or drop the comment. Same for the `_drainBanner` helper in `nearby_area_test.dart:137` and `route_detail_watch_course_test.dart:348` (9 call sites between them) and `drainBanner` in `run_screen_recording_flow_test.dart`, whose docstring explains that "its timer lives in the root Overlay, not the screen, so screen dispose does not cancel it" -- still true of screen dispose, no longer the reason the helper exists. Also worth removing while there: `settings_email_reoptin_test.dart:110`'s `tearDown(hideTopBanner)`, which was measured incapable of what its position implies (the pending-timer assertion fires before any `tearDown`), though it is harmless as global-state cleanup. Deliberately left rather than swept: nine judgement calls in seven files is a different change from the mechanical deletion, and getting one wrong silently shortens a recording clock. **Closed** by [§ 1238](../architecture/decisions.md), and the filing was wrong about every one of them: measured by removing each and running the file, all 10 are removable and none of the three stated reasons held — the "further pumping" is the settle after the drain, the two recording-clock pumps sit after their test's last `expect`, and the two helpers have 18 call sites between them, not 9. A census found three more sites the entry never listed (four trailing `hideTopBanner()` calls in `route_detail_review_delete_test.dart` + `watch_live_screen_test.dart`, one in `settings_email_reoptin_test.dart`, and a drain inside `top_banner_test.dart` itself). 103 lines gone across 11 files, 191 tests still passing. The `tearDown(hideTopBanner)` was probed before removal, not taken on trust.
- [x] **`docs/testing/test_inventory.md`'s row for `offline_sync_store_drain_race_test.dart` is three tests short.** [§ 1196](../architecture/decisions.md) added a `a fresh empty store loads without writing an index` group to it. Nothing reads the inventory -- there is no guard over it, checked -- so this is drift, not a break, and it is listed here only because `docs/testing/test_inventory.md` belonged to another lane this round. Owner tree: `docs/testing/test_inventory.md`. **REFUTED 2026-09-06 at round-42 integration.** Measured on the merged tree: the file holds 8 `test(` plus 1 `testWidgets(` = 9, and the row states 9. `check_test_inventory_counts.mjs` agrees and raises no complaint about it. The claim was written against a count that excluded `testWidgets`; nothing is owed.
- [x] **`clone_public_plan` drops the same four plan columns [§ 1228](../architecture/decisions.md) just closed on the publish side.** `apps/backend/supabase/migrations/20270126_001:162` inserts the clone's `plan_workouts` naming `target_pace_sec_per_km`, `target_pace_tolerance_sec`, `structure` and `notes`, and its `training_plans` insert names neither `source` nor `rules` — so a plan published to the library WITH its progression end-paces, zone chips and adaptive-re-plan rules (they are copied now) still hands a cloner a plan without them. Verified by reading the RPC body: the two column lists are `target_pace_end_sec_per_km`- and `pace_zone`-free, and `rules` / `source` appear nowhere in it. The web half is done and the guard that keeps it done is `apps/web/src/lib/core/plan_copy.test.ts`; the RPC is a `create or replace function` in a new migration, and a TS guard cannot reach it — the durable shape is the third-rail treatment `normalise_exercise_name` got, i.e. a guard that reads the SQL body's column list against the TS one. Owner tree: `apps/backend/` + `apps/web/` (the guard). **CLOSED at round-40 integration** — fixed by the backend lane under [§ 1236](../architecture/decisions.md); the box could not be ticked from that branch because the entry lives in another lane's file.
- [x] **`/routes` still gates the route-card preview out for every route saved from Explore — one block, in a page [§ 1229](../architecture/decisions.md)'s lane did not own.** `apps/web/src/routes/routes/+page.svelte:476` reads `{#if route.waypoints && route.waypoints.length > 1}` and falls through to a `route` glyph otherwise. The data layer now hands those rows `waypoints: []` (the signal `RouteTrackPreview` reads as "fetch this viewer's clipped line"), so the component would fetch and draw the clipped mini-map if it were mounted — it is simply never mounted. `RouteTrackPreview` already renders its own `map` placeholder when the clip RPC returns nothing, so the page's `{:else}` branch is redundant once the gate goes. Exact change: replace the whole `{#if …}<RouteTrackPreview …/>{:else}<span class="material-symbols" aria-hidden="true">route</span>{/if}` with an unconditional `<RouteTrackPreview routeId={route.id} waypoints={route.waypoints ?? []} ownerUserId={route.user_id} />`, and pin it with a Playwright assertion that a bookmarked public route's card carries `[data-testid="route-preview-map"]` or the component's own placeholder rather than the page's. Owner tree: `apps/web/src/routes/routes/`. **CLOSED at round-40 integration** — the gate is gone; `RouteTrackPreview` receives `waypoints={route.waypoints ?? []}` unconditionally and falls through to `clip_route_for_viewer`, and its own `map` placeholder covers a route with genuinely no polyline.
- [x] **The routes list is still typed `Route` while the projection omits four columns on BOTH halves.** [§ 1229](../architecture/decisions.md) filled the two columns `public_routes` withholds relative to `ROUTE_LIST_COLS`, which closes the `waypoints` lie; the residue is that `ROUTE_LIST_COLS` itself selects 11 of `routes`' columns and `Route` (`= Omit<RouteRow, 'waypoints'|'surface'> & {…}`) declares every one, so `description`, `tags`, `is_public` and `updated_at` are absent at runtime under a type that promises them — on the OWNED half too, which is why it is orthogonal to that entry rather than part of it. Verified against the constant and `database.types.ts`; no consumer reads any of the four today, which is what `data.test.ts:226`'s enumerated list pins. The durable fix is a `RouteListItem` (a `Pick<Route, …>` over exactly the projected columns) returned by `fetchRoutes` / `fetchRoutesWithError`. Blast radius, measured: four annotations in three trees — `components/EventEditor.svelte:64`, `components/RunEditor.svelte:38`, `routes/routes/+page.svelte:21` and `:226`, plus `routes/clubs/[slug]/+page.svelte:115` (`transferableRoutes` only; `clubRoutes` comes from `fetchClubRoutes`, which is a genuine `select('*')`). `Route` stays assignable to a narrower `RouteListItem`, so `EventEditor`'s `[...mine, ...clubs]` merge keeps typechecking. Wants one lane owning all four. Owner tree: `apps/web/`. **CLOSED (type + guard + the one consumer this lane owns) 2026-09-06 ([decisions § 1294](../architecture/decisions.md)).** The diagnosis holds; the COUNT does not — the omitted set is **eleven**, not four (`description`, `tags`, `is_public`, `updated_at` as filed, plus `featured_at`, `geom`, `geom_public`, `is_featured`, `shadow_hidden`, `slug`, `start_point`), measured against `database.types.ts`' 22-column `routes` row and the 11-name constant ([§ 1295](../architecture/decisions.md)). "No consumer reads any" is confirmed and extends to all eleven: the union of columns the four consumers read is exactly the projection. Built the durable shape rather than the `Pick<>` the entry proposed — `apps/web/src/lib/routes/route_list_columns.ts` declares the column tuple ONCE (`as const satisfies readonly (keyof Route)[]`, so a dropped column fails to compile), derives the `.select()` string from it and `Pick`s `RouteListItem` from the same tuple, so the query and the type cannot describe different sets. Pinned by 11 `@ts-expect-error`s and two `AssertNever` aliases (a runtime test cannot see an absent field, § 1296), mutation-tested both ways, plus a guard comparing the derived strings against the literals `core/data.ts` passes to `.select()`. `/routes/+page.svelte` consumes `RouteListItem` today. **The residue is one lane's file and is filed below**: `fetchRoutes` / `fetchRoutesWithError` still RETURN `Route[]`, and three consumer annotations follow it.

### Surfaced by the #789 round 40 web-landing lane (2026-09-05)

- [ ] **The SPA shell's filename moved in the repo, and the live distribution has not been taken through the sequence that makes that safe.** [§ 1268](../architecture/decisions.md) renamed the shell from `index.html` to `200.html` across all three rails; [§ 1269](../architecture/decisions.md) states the three-step operator sequence that moves a LIVE distribution without a window where the 403 body is wrong — (1) `aws s3 cp s3://<bucket>/index.html s3://<bucket>/200.html` while the two are byte-identical, (2) `terraform apply` the 403 to `/200.html`, (3) deploy the tag. **The repo change is merged and the sequence has never been executed**, so a deploy that skips step 1 or reorders it serves the prerendered landing page — broken, since its asset URLs resolve under the deep link's own directory ([§ 1270](../architecture/decisions.md)) — for every client route until the apply catches up. This is a deploy/credential step, not unwritten code: no lane in round 40 holds AWS credentials. The one claim in § 1269 that is derived rather than measured is that the pre-seeded object survives the release's `aws s3 sync --delete` (it excludes `*.html`, and an excluded destination key is not a deletion candidate); confirm it by running step 1, running a release, and checking `200.html` is still listed. Owner: whoever holds `preview`/`prod` credentials, before the next `web@` tag reaches either environment. **Still open — it is the deploy, and no lane holds credentials — but the repo half around it is fixed 2026-09-06 ([decisions § 1306](../architecture/decisions.md)).** Two problems with where the sequence lived. Its closing paragraph in `docs/ops/deployment.md` still warned that the five `build.mjs` embed `build/index.html` and listed the paths that "must move to `200.html`" — true when written and merged in the same PR, so an operator was being told the repo sits in a half-migrated state it left immediately, and sent to fix files that are already correct. Re-measured: `svelte.config.js` emits `fallback: "200.html"`, all five `build.mjs` resolve `build/200.html`, `spa_shell_head_signals.test.ts` reads that artifact, the `custom_error_response` names `/200.html`, and § 1272's guard makes a regression fail the PR. And nothing pointed the person who would trip the gate: `docs/ops/releasing.md`'s "Before cutting a release" list already carries this exact shape of one-off gate (Play Health-apps declaration, Android developer verification, Supabase Auth URL config) and this one was not on it. It is now. Two things the runbook had not said and now does: the steps are **per-distribution** (`prod` and `preview` cut over independently, and a runbook in the singular invites doing it once), and an operator can read which side of it an environment is on without guessing — the 403 entry's `ResponsePagePath` in `DistributionConfig.CustomErrorResponses`, `/index.html` meaning not yet. § 1269's one derived claim is restated as an instruction to falsify on the first run.
- [x] **`.github/workflows/release-web.yml` still describes `apps/web/build/index.html` as "the SPA shell the Lambda embeds", five times.** Lines ~193, 201, 210, 218 and 225 each name the file each share Lambda's bundler reads; [§ 1268](../architecture/decisions.md) moved that to `build/200.html`, so all five now point a reader at the prerendered landing page. Comment-only, no behaviour: the S3 sync at ~312-315 needs no change, because its `--exclude "*.html"` already covers `200.html` and gives it the same 60 s cache the other HTML gets, and its redundant `--exclude "index.html"` beside that is harmless. Not taken in this lane — `.github/` is the infra-ci lane's tree. **ALREADY CLOSED when filed — verified 2026-09-06 (round 41), no edit needed** ([decisions § 1265](../architecture/decisions.md)). The web-landing lane filed this against `.github/` as another lane's tree; the round-40 infra lane had already moved all five comments in the same PR, so `main` has read `apps/web/build/200.html` at lines 193, 205, 214, 222 and 229 since `7e56afb78`. The "comment-only, no behaviour" half is confirmed rather than assumed: every one of the five sits above a `run:` of the form `node lambda/<name>/build.mjs`, which names no filename — the path that matters is `spaShellPath` inside each `build.mjs`, and all five of those resolve `build/200.html` too. The S3-sync claim is confirmed as well: the filter list is unchanged and its comment at ~317 explains why in both directions.
- [x] **The stated reason web e2e runs on `vite dev` rather than `vite preview` is refuted by the artifact, in two independent ways.** `apps/web/tests-e2e/playwright.config.ts` (header, ~line 13) and `docs/testing/test_inventory.md:1060` both say the adapter-static fallback "computes its asset base via `new URL(\"..\", location).pathname` — for `/runs/<id>` that resolves to `/runs`, breaking `_app/` asset URLs", and that "production fixes this with a CloudFront viewer-request rewrite". Measured on a production build: the fallback carries `base: ""`, absolute asset URLs (`/_app/...`, `/favicon.png`) and a bare `kit.start(app, element)` with no hydration payload — it is a correct universal shell at any depth. The `new URL(".", location)` form is real but belongs to PRERENDERED pages, which is exactly why the landing page cannot double as the deep-link body ([§ 1270](../architecture/decisions.md)); the comment describes that mechanism and attributes it to the fallback. And there is no path-rewriting viewer-request function: the only CloudFront function in `infra/modules/web-stack/functions/` is `www_redirect.js`, which 301s www→apex and never touches `request.uri`. Whether `vite preview` is now a viable e2e server is a separate question worth measuring — it would test the artifact CI actually ships — but the two sentences are wrong either way. Not fixed here: `tests-e2e/` and `docs/testing/` are other trees, and no lane in round 40 runs Playwright, so a server change cannot be verified. **CLOSED by the round-41 e2e lane for the config half** ([§ 1311](../architecture/decisions.md#1311-the-reason-web-e2e-runs-on-vite-dev-is-that-eleven-serverts-endpoints-are-not-in-a-static-build--not-an-asset-base-break-which-the-artifact-refutes-twice)), with a third refutation from the framework source — kit's `render.js` computes the relative base only when `!state.prerendering?.fallback` — and an answer to the open question: `vite preview` would NOT work, because eleven `+server.ts` endpoints are absent from an adapter-static build and two `cross-cutting` specs post to `/api/coach` for real. The `docs/testing/test_inventory.md:1060` half is still open — that file is the parent's, and the replacement wording is in the round-41 e2e lane's report.

### Surfaced by the #789 round 40 shared-rails lane (2026-09-05)

- [x] **Two more twin-declaration forms are measured and still unread, and one of them cannot be widened into without rewording four other trees.** [§ 1243](../architecture/decisions.md) closed three of the five mechanisms; these are the other two, both measured over `readSources()`. **(1) A RELATIVELY-named counterpart.** `DECLARATION` anchors the path at `apps/` or `packages/`, so `the mobile twin of web's \`training/adaptive_fitness_flag.ts\``, `Twin of the job half of web's \`lib/backup/cloud_export_helpers.ts\``, `Dart twin of web \`core/undo_queue.ts\``, `twin of web's \`current_week.ts\`` and 7 more cross-platform instances are invisible. Resolving one means guessing a root; a suffix match against `SOURCE_ROOTS` resolves 11, but two of them are `settings_safety_screen.dart -> core/data.ts` and `password_change.ts -> settings_account_screen.dart` — claims about a screen and a 12,000-line data module, neither registrable — so widening buys two real pairs at the cost of four header rewords in other trees. The cheaper durable fix is the opposite direction: make the headers name repo-rooted paths (one was already fixed this round, `adaptive_fitness_flag.dart`) and leave the guard strict. **(2) A declaration below the file's first statement.** `headerComment` now steps over an import prologue but still stops at the first line of real code, which is right — a claim 300 lines down is not a header — but it means `adaptive_fitness_flag.dart`, whose module doc sits under a `const`, is read as having no header at all. Both are false NEGATIVES only: nothing the guard reports is wrong. Owner tree: `scripts/check_twin_claims.mjs` plus whichever trees hold the headers. **CLOSED in the round-41 guards lane** ([§ 1314](../architecture/decisions.md)) — both forms are read. The filing was wrong in both halves and in the same direction: the relative form buys ZERO new pairs from the header alone (all eleven resolve uniquely, six already registered, five unregistrable), and the below-the-statement form is not low-value, because **the two mechanisms intersect** — `off_route_flag.dart` and `weigh_in_flag.dart` are named relatively AND sit under a documented top-level `const`, so each was invisible twice over and both were live § 641-class pairs in neither registry ([§ 1315](../architecture/decisions.md)). The four header rewords the entry priced were avoided entirely: a claim the registries cannot carry now resolves to a `NOT_PAIRS` entry instead ([§ 1317](../architecture/decisions.md)). Census 167 → 190, floor 155 → 178.
- [x] **`max_hr_bpm` is a jsonb prefs key with no CHECK, and the two web write paths still accept any integer.** The READ side is closed on all three rails ([§ 1245](../architecture/decisions.md)) — web, Dart and the Wear OS `resolveZoneCutoffs` now all ignore a value outside 80..240 — so a typo can no longer give the same run different zones on the wrist and the phone. The write side is untouched and was another lane's tree this round: `apps/web/src/routes/settings/account/+page.svelte:590` writes `parseInt(maxHr, 10) || null` with no bound at all, and the preferences page's `min="100" max="230"` is an advisory HTML attribute on a field that autosaves `onblur`, so neither refuses a 300. The exact change wanted: import `MAX_HR_BPM_MIN` / `MAX_HR_BPM_MAX` from `$lib/training/hr_zones` (they exist now, named for this) and refuse an out-of-range value at the input with a message, rather than writing a number three readers then silently discard. Note the HTML attributes disagree with the constants (100/230 against 80/240) and should become the constants too. Mobile has no max-HR input. Owner tree: `apps/web/src/routes/settings/`. **CLOSED 2026-09-06 ([decisions § 1407](../architecture/decisions.md), [§ 1408](../architecture/decisions.md)).** Verified before acting and the filing is right about the web half: neither page has a `<form>` element in the path that saves, so the `min`/`max` attributes never reach constraint validation, and both wrote a raw `parseInt`. Both now gate on `isUsableMaxHrBpm`, the same predicate `defaultZoneCutoffs` calls, so the write refuses what the reads ignore by construction rather than by a second range check; the refusal names the range through a new `limits.maxHrOutOfRange` key in all seven catalogues and reuses the demographics card's `field-error` + `aria-invalid` + disabled-save idiom. The attributes are now the constants. Pinned by a composition guard (both pages reach the predicate, neither writes from a raw `parseInt`, neither re-hardcodes a bound), all four regressions mutation-verified. The bound was deliberately NOT moved into `column_limits.ts` — that module's guard resolves each key's CHECK from the migrations, and this key has no column. **The filing's closing claim that "mobile has no max-HR input" is REFUTED**: `settings_preferences_screen.dart`'s `_editMaxHr` is one, and it was already bounded (`_pickInt` pops `null` out of range, so nothing is stored) — it only spelled 80/240 as fresh literals, and now reads `kMaxHrBpmMin`/`Max`. § 1408 additionally added the `check_shared_constants.mjs` entry comparing all three rails' copies of the range, which did not exist.
- [x] **`core/auth_gates.ts` declares a mirror in a SCREEN, which cannot be registered, so it sits as the one remaining `KNOWN_GAPS` entry.** Its header says web's consent gates are mirrored "in `apps/mobile_android/lib/screens/sign_up_screen.dart`", which is true and is exactly what the syncer row for `auth_gates` already records in prose — but `check_twin_claims` reads it as a declared pair and no row can carry a screen file. The fix is a one-sentence reword in the header so it states the relationship without the declaration shape (the pattern used for `revenuecat`, `strava`, `route_describe_client` and `fitness_card` this round: name the counterpart without a declaration verb in front of a repo-rooted path). `apps/web/src/lib/core` was another lane's tree this round, which is the only reason it is still open. Owner tree: `apps/web/src/lib/core/`. **CLOSED at round-40 integration** — the header no longer asserts a pair, so the entry is retired and `KNOWN_GAPS` is now empty. Census 162 against the 155 floor.
- [x] **Three "Dart port of" modules are now declared NOT to be lockstep pairs, and nothing re-checks that judgement.** [§ 1244](../architecture/decisions.md) reworded `revenuecat.dart`, `strava.dart` and `route_describe_client.dart` because each wraps a different SDK or is network glue rather than shared logic, and registering them would have made both registries assert a lockstep that does not exist. That judgement is now prose in four headers and nothing tests it — if `strava.dart` ever grows the native OAuth callback its own header anticipates, or `revenuecat.dart` and its web counterpart converge on a shared entitlement shape, the honest answer flips and only a reader will notice. Low priority and possibly not worth machinery; recorded so the next reader knows the classification was deliberate rather than an oversight. Owner tree: `apps/mobile_android/lib/`. **CLOSED in the round-41 guards lane** ([§ 1316](../architecture/decisions.md)) — it was worth machinery, and the population is ten rather than three: § 1244's four, `auth_gates`, and five more the widened reader surfaced. `NOT_PAIRS` in `check_twin_claims.mjs` is a declared, reason-carrying register in the `UNREGISTERED_DEFINER_RELATIONS` shape, and each entry records the set of top-level public names BOTH files declare, re-measured every run and compared as an exact set — growth is the convergence the entry exists to catch, shrinkage is how a blind symbol reader would present itself, and a file with no public surface at all fails outright. `strava` carries the widest shared surface (three function names, three different implementations) and its entry names it as the one to re-judge first.

### Surfaced by the #789 round 40 casefold lane (2026-09-05)

The flagship item closed in full and two of its four filings were wrong in
ways worth reading — the counts in [§ 1248](../architecture/decisions.md) and
the instrument in [§ 1251](../architecture/decisions.md). What follows is what
the lane found beside them and could not take, because every one lives outside
the lane's tree. The three web ones are already named as `PENDING` /
`BROAD_MODULES` entries inside
`apps/web/src/lib/gym/exercise_key_source_guard.test.ts`, whose staleness test
fails the moment an entry stops excusing a real site — so closing one of these
means deleting its entry in the same change.

- [x] **`share_workout_meta.ts`'s `distinctExerciseCount` counted a lift logged under two spellings twice, and its own doc comment claimed otherwise.** **CLOSED** by the #789 round 41 web-share-coach lane ([§ 1274](../architecture/decisions.md)). The filing was right and understated the merge set: measured against `normaliseExerciseName`, `trim().toLowerCase()` failed on FOUR pairs, not one — an internal run (`Bench  Press`), a U+00A0, a tab, and `İncline Press` vs `incline press` — and merged only the trailing-space pair the old comment named. The module's export keeps its `SharedWorkoutSet[]` signature (`routes/share/workout/[id]/+page.svelte` imports it and is another lane's tree) and is now an adapter over `gym_prs`' canonical `distinctExerciseCount`, holding no fold of its own. Two assertions added and mutation-tested against the old expression; the `PENDING` entry is gone.
- [x] **`coach/context.ts`'s per-workout exercise tally split one lift into two in the prompt the model reads.** **CLOSED** by the #789 round 41 web-share-coach lane ([§ 1275](../architecture/decisions.md)). Confirmed at `summarizeRecentLifts`, and the fix went further than the filing's "only the `names.add` half moves": the hand-rolled `Set` was the whole reason the loop touched anything but tonnage, so the tally is one `distinctExerciseCount(mine.map(...))` call and the loop is the volume sum it also did. `LiftSummary.exercises` documented itself as "distinct exercise NAMES in the session", which described the wrong thing truthfully; it now states the claim the code makes. Two assertions added, the canonical-key one mutation-tested; the `PENDING` entry is gone.
- [x] **The web exercise-catalogue picker matched FEWER names than the mobile one — and reached a state where the exercise existed, could not be found, and could not be added.** **CLOSED** by the #789 round 41 web-share-coach lane ([§ 1276](../architecture/decisions.md)). The filing was right about the fold and understated the consequence. `hasExact` has always folded canonically while `filtered` did not, and the empty state renders on `filtered.length === 0 && !canCreate` — so an entry the naive filter cannot see but `hasExact` can find hides the list AND suppresses the create affordance. Simulated over the component's two derived expressions, three catalogue spellings reach that dead end (`Bench<U+00A0>Press`, `Bench  Press`, `İncline Press`, each queried with the ordinary spelling), and the entry that traps the picker is one a user can create in the same field. Folding both sides through `normaliseExerciseName` closes it structurally: an entry whose key equals the query's key necessarily contains it. The filing's note about the sort was followed — `localeCompare` stays, and the measured divergence is re-filed below against the mobile side. The `PENDING` entry is gone and the list is now empty on both platforms.
- [x] **The club slug is derived on two rails and they disagree on a Turkish club name — the only reachable, PERSISTED survivor of the case gap.** [§ 1251](../architecture/decisions.md). **CLOSED** — [§ 1279](../architecture/decisions.md), [§ 1281](../architecture/decisions.md), [§ 1282](../architecture/decisions.md). The derivation is now one registered parity pair, `apps/web/src/lib/social/club_slug.ts` ↔ `apps/mobile_android/lib/club_slug.dart`, folding through `catalogue_browse`'s generated table: `İzmir` answers `izmir` on both rails, and `Zürich Runners` reaches `zurich-runners` rather than `z-rich-runners` because a stripped diacritic leaves its base letter where the raw strip left a hyphen. Extraction was half the fix — `slugify` sat inside `core/data.ts`, whose test file is a file of SOURCE guards precisely because the module cannot be imported into a unit test, so the web half of a persisted two-platform contract had no behavioural test and could not be given one where it sat. Two divergences the filing had not named came out of putting the rails side by side: the phone applied NO length cap where the web capped at 48 (reachable window 49 to 80 characters, since `clubs_name_len_chk` bounds the name at 80), and the phone REFUSED to create a club whose slug came back empty — so every club named entirely in Cyrillic, Greek, Hebrew, Arabic, Thai or CJK was uncreatable there, under a message claiming the name had no letter or digit. Existing slugs are deliberately not re-minted (a slug is a URL), and the fold's slightly wider collision class is absorbed by the four-attempt retry both create paths already had — verified rather than assumed.
- [x] **The food-search dedupe key folds user text on two runtimes that disagree at `İ`.** [§ 1251](../architecture/decisions.md). **CLOSED** — [§ 1280](../architecture/decisions.md). `dedupeFoods`' name+brand key on both rails now folds through `catalogue_browse`'s generated table. The lesson is bigger than the fix: `food_search` was ALREADY a registered parity pair whose two halves name each other, and none of that could have caught this, because every parity instrument in the repo compares SOURCE and the two sources agreed — `trim().toLowerCase()` on both. The runtimes underneath them disagree at 466 code points. Both registry entries now say so. The cosmetic rail got the same instrument as the persisted one deliberately: folding accents is the CORRECT key here and not merely the parity-preserving one, because Open Food Facts and USDA are independent catalogues that spell one product two ways. **The route-name half of this item was refiled** — it is not in `data.ts` as the hand-down said, and it carries a second, larger divergence beside it; see the round-41 section below.
- [x] **Stamp `gym_routine_exercises.exercise_key` and `exercises.name_key` server-side, the way `gym_sets.exercise_key` already is.** [§ 1252](../architecture/decisions.md). The durable fix for the whole stale-client class, and the pattern is already in the tree one column over: `gym_sets_stamp_exercise_key_trigger` (migration `20270706000001`) is a BEFORE INSERT OR UPDATE trigger whose own comment says it "stamps unconditionally, so a client-supplied exercise_key is replaced rather than rejected". The other two columns instead carry a CHECK that REFUSES a disagreeing client value, which is what couples them to the client's Unicode version — 410 code points before [§ 1175](../architecture/decisions.md), 465 after, and 55 newly so. Extending the same trigger to both turns every version of that refusal into a silent correction and takes all three numbers to zero permanently; the CHECKs can stay as belt-and-braces since nothing would then be able to violate them. Needs a migration and a re-validate, so it belongs to whoever owns `apps/backend/`. Owner tree: `apps/backend/supabase/migrations/`. **CLOSED 2026-09-06** (migration `20270711000001`, [decisions § 1284](../architecture/decisions.md)). Both triggers landed with the same unqualified `before insert or update` shape and the same constant `''` default, so the two `Insert` types stop obliging a client to compute a key the server derives. **No re-validate was needed and that is a proof rather than a shortcut**: both CHECKs are already VALIDATED, which is a statement about every row, so the derivation held for all of them and a backfill would have touched nothing. The CHECKs stay — a correcting trigger makes them unviolatable rather than redundant, and they are what turns a lost trigger into a loud refusal; `exercise_key_server_stamped_test.sql` (11 tests) disables the trigger to prove that half is still live. One existing assertion had to move with it and two more were quietly emptied by the change — see [§ 1287](../architecture/decisions.md).

- [x] **`assign_plan_to_athlete` propagates the COACH's `vdot` and `current_5k_seconds` onto the athlete's plan row, and `20260721_001`'s rule is about templates only.** Noticed while fixing the clone paths ([decisions § 1236](../architecture/decisions.md#1236-clone_public_plan-was-the-third-clone-path-and-none-of-the-three-carried-the-plans-own-attribution)) and deliberately not changed, because it is a design question rather than an omission. The three head-inserting routines split three ways on this: `clone_plan_template` and `clone_public_plan` both write literal `null, null` with the comment "publisher-private fitness data — never propagated to the clone", `20260721_001` backs that with `private.strip_template_private_fields` (BEFORE INSERT OR UPDATE, nulls both columns on any `is_template` row) so a template cannot hold them at all — and `assign_plan_to_athlete` writes `src.vdot, src.current_5k_seconds` from a source plan that is NOT a template and therefore CAN hold them. So a coach assigning one of their own plans lands their own fitness numbers on a row the athlete owns and can read. Two defensible readings and the code states neither: either the paces are the coach's prescription and the VDOT they were derived from is part of the prescription (keep it, and say so in the migration), or it is the same publisher-private class the other two paths null and the strip trigger should not have been scoped to `is_template` (null it, and extend the trigger). The privacy-shaped reading is the one § 1236 could not take blind. `plan_clone_pace_columns_test.sql` now asserts the template side of this and has no assertion either way for the assignment side. Owner tree: `apps/backend/supabase/migrations/` + `apps/backend/supabase/tests/`. **CLOSED 2026-09-06** (migration `20270711000002`, [decisions § 1285](../architecture/decisions.md)) — the privacy reading, plus a **third column the filing did not name**: `notes`. `20270508_001` classifies all three as one set and `privacy_guards.test.ts` calls them "the publisher private fields" together; plan-level `notes` is the coach's own free text ("training constraints, injury history"), it is authored through `PlanMetaEditor.svelte`, and **no athlete-facing plan surface renders it at all**, so it was pure latent disclosure. The prescription reading was checked and does not hold: `plan_workouts` carries the prescription in full, `plan.vdot` renders as an unattributed "VDOT 52.3" header stat on five surfaces, and `current_5k_seconds` is a measurement of the coach under any reading. The strip trigger was deliberately **not** extended: RLS's `with check (auth.uid() = user_id …)` makes this RPC the only path that can create a row owned by somebody else, and a trigger keyed on `assigned_by_coach_id` would fire on every later update and delete the ATHLETE's own notes. `assign_plan_to_athlete_test.sql` goes 8 → 11, asserting at the assigned row (the source now holds all three, so the copy is observable) and pinning that `rules` still propagates.
- [x] **`apps/job_worker/deployment.md`'s alerting table does not mention `jobs-backlog-alert`, which is the alert its own entry for `jobs-failed-alert` implies exists.** That file says of the failed-jobs alert that it "is the safety net the async webhook needs" and notes `defer_job` flips an exhausted retry into `failed` "so it shows up here rather than stalling un-claimable in `queued`" — a sentence that is true and that reads as if the queued case were therefore covered. It is not: a worker that never runs never reaches `defer_job`, and [decisions § 1234](../architecture/decisions.md#1234-a-worker-that-is-down-is-invisible-to-both-job-alerts-and-export-retention-is-where-that-silence-has-a-deadline) measured that such a job is in neither `find_stuck_jobs`' set nor `find_failed_jobs`'. `20270710000004` adds `jobs_backlog_summary()` on a `*/10` schedule and `export_retention_overrun()` daily, both landing in `cron.job_run_details.return_message` like their siblings, and both need a scraper route (`backlogged_count > 0`, `overrun_count > 0`) and a row in that table plus its runbook checklist. `docs/backend/api_database.md` carries them already. Not taken in § 1234's change: `apps/job_worker/` was outside that lane. Owner tree: `apps/job_worker/deployment.md`. **CLOSED at round-40 integration** — both rows are in the alerting table with the reason the failed-jobs alert cannot cover them (a never-claimed job is `queued` with a null `locked_at`, so it is in neither `find_stuck_jobs` nor `find_failed_jobs`), plus the two deploy-checklist items.

### Surfaced running the round-40 lanes' Playwright specs at integration (2026-09-05)

- [x] **Two of the round's three new Playwright specs are `test.fixme`, and both were written by lanes that were forbidden from running them.** Every other assertion in both files passes; these two do not, and each carries its ruled-out list in a comment above it. **(1)** `tests-e2e/fundraising/section-load-failure.spec.ts` — "a non-owner gets a retry": the `page.route` interception that should turn the `fundraisers` read into a 500 never becomes the response `fetchFundraiserForRun` sees, so the section renders the seeded campaign instead of the error banner. Ruled out: the cookie banner (now pre-accepted), an anonymous viewer (`/runs/[id]` requires a session and redirected to `/login` — fixing that is what stopped the SIBLING test passing vacuously, so the round is net ahead), and glob-vs-regex route matching. Remaining candidate: the read reaches PostgREST by a path the matcher does not see. **(2)** `tests-e2e/gym/exercise-key-fold.spec.ts` — passes run ALONE, fails run after its siblings, always because only one of the two UI-created workouts exists by the time it queries. Ruled out: leftover `metadata.gym_session_draft` rows (a `beforeEach` now deletes them, failure unchanged) and stamp collision (the LIKE is stamped per run). Both behaviours have passing unit coverage; what is missing is the end-to-end proof. **The transferable lesson is the rule that produced them:** a lane that cannot run a spec ships one that does not work — 4 of the 3 files' assertions failed on first execution, and two were spec defects that would have been caught in seconds by the author. **CLOSED by the round-41 e2e lane** — both were spec defects and neither ruled-out list named the cause. (1) was not an interception problem at all: `/runs/[id]` does not mount `FundraiserSection` for a non-owner, so no `fundraisers` request was ever made and the SIBLING test was passing for that reason rather than its own ([§ 1309](../architecture/decisions.md#1309-runsid-never-mounts-the-fundraiser-section-for-a-non-owner-so-the-spec-written-to-prove-the-non-owner-branch-was-asserting-against-a-component-that-was-not-on-the-page)); the spec now anchors on the club-event page, the one non-owner mount. (2) fails run ALONE, and neither draft rows nor stamps were involved: `hasText` is a substring match and the second title (`... pr`) was a prefix of the first (`... prior`), so the post-save wait matched a row already on the page and the service-role read raced the insert ([§ 1312](../architecture/decisions.md#1312-hastext-is-a-substring-match-so-a-spec-whose-second-title-was-a-prefix-of-its-first-waited-on-a-row-that-was-already-there)).


### Surfaced by the #789 round 41 web-share-coach lane (2026-09-06)

Closing the last three `PENDING` entries in
`apps/web/src/lib/gym/exercise_key_source_guard.test.ts` emptied the list, and
emptying it made the guard's blind spot legible: it bans the WRONG fold and is
blind to NO fold. A scan of every web comparison of an `exercise_name` found
five sites in that second shape. Details in
[§ 1276](../architecture/decisions.md) - [§ 1278](../architecture/decisions.md).

- [x] **`routineFromWorkout` can save a routine holding two `gym_routine_exercises` rows with the same `exercise_key`, and adherence then matches sets against both.** `apps/web/src/lib/gym/gym_routine.ts:163` groups consecutive logged sets into a routine exercise on `last.exerciseName === name` — the raw trimmed DISPLAY spelling — and then stamps the block `exerciseKey: normaliseExerciseName(name)`. So a workout whose consecutive sets carry two spellings of one lift ("Bench Press" then "bench press", or either against a U+00A0 / U+0130 variant) saves TWO rows under one key. Nothing refuses it: `20270709000010`'s own comment records that `gym_routine_exercises` has no unique index on the key, and `computeRoutineAdherence` matches on `(exerciseKey, setIndex)`, so the two rows then compete for the same logged sets and the session roll-up double-counts. Adjacency itself is correct and must stay — a superset alternates A/B/A and has to render as three blocks — so the fix is to make the adjacency test the canonical key rather than the spelling, which merges the two spellings without merging across an interleaved lift. `gym_routine` is a registered parity pair and `apps/mobile_android/lib/gym_routine.dart:221` carries the identical test, so this is one latent defect on two rails, not a divergence, and the change owes a matched Dart edit plus the iOS mirror. Owner trees: `apps/web/src/lib/gym/` + `apps/mobile_android/lib/gym_routine.dart`. **CLOSED 2026-09-06 by the round-42 web-gym-key lane, and the adherence half is REFUTED** ([§ 1322](../architecture/decisions.md)). `computeRoutineAdherence` matches on `(exerciseKey, **stepIndex**)`, not `(exerciseKey, setIndex)` — `refKey(exerciseKey, stepIndex)` is the module's only key builder and `setIndex` is documented there as display-only and not unique across a routine — so two blocks occupy distinct step ordinals and grade independently. Nothing double-counts; the harm is the two blocks themselves. Both rails now measure adjacency on the canonical key, and the drop guard beside it moved onto the key too, which closed a real web/Dart divergence at U+0085.
- [x] **Four display surfaces group exercise blocks on the raw spelling, so two consecutive sets of one lift can render as two blocks beside a header stat that says one exercise.** The same shape as the item above without the persistence: `apps/web/src/lib/components/GymEditor.svelte:153` and `:173`, `apps/web/src/routes/gym/[id]/+page.svelte:245`, and `apps/web/src/routes/share/workout/[id]/+page.svelte:41` all test `last.name === s.exercise_name`. The share page is the sharpest illustration because both numbers are on screen at once: since [§ 1274](../architecture/decisions.md) its "1 exercise" line is canonical while the block list beside it is not. Cosmetic, but it is the same one-word change (compare the canonical key, keep the adjacency) and it should land with the routine fix so the two cannot drift apart. Owner trees: `apps/web/src/lib/components/` + `apps/web/src/routes/gym/` + `apps/web/src/routes/share/workout/`. **CLOSED 2026-09-06** — all four now compare the canonical key through the new shared `sameExerciseName` (`gym_prs.ts` ↔ `gym_prs.dart`), as does the mobile workout detail (`gym_detail_screen.dart:529`), which the filing did not list. Pinned by a second scan in `exercise_key_source_guard.test.ts`: the existing guard bans the WRONG fold and there is no fold here to see, so it was blind to this shape by construction. Mutation-tested against a planted regression in the real tree.
- [x] **The exercise-catalogue picker orders its list differently on the two platforms, and the divergence resolves toward web.** Left unowned since [§ 1249](../architecture/decisions.md) sent it to § 1251's survivor list and § 1251 did not carry it. Web sorts on `a.name.localeCompare(b.name)` over the display name; the mobile picker sorts on `normaliseExerciseName(a.name).compareTo(...)`, a UTF-16 code-unit compare over folded keys. Measured over `['Ab Wheel', 'Bench Press', 'Élévation latérale', 'Overhead Press', 'Row', 'Überzug', 'źcisk', 'Zercher Squat']`: web interleaves the accented names where a reader expects them, the phone files all three after `Zercher Squat`, and the two lists diverge from position 2. Ordering a human-facing list is not keying it, so `localeCompare` is the right instrument and the web side was deliberately left alone (round 40's own filing said the same). Dart's core library ships no collator, which is why the mobile picker sorts the way it does — the fix is a collation on the mobile side, not a fold on the web one. Owner tree: `apps/mobile_android/lib/widgets/exercise_catalogue_picker.dart`. **CLOSED by the round-42 web-catalogue-picker lane** ([§ 1334](../architecture/decisions.md)). The pessimism was in the conclusion, not the premise: Dart still ships no collator, but it does not need one to stop filing `É` after `Z`. The mobile picker now sorts DIACRITIC-FOLDED display names through `fold` — the generated Unicode table `catalogue_browse` already carries — and measured over the filing's own eight names it produces `[Ab Wheel, Bench Press, Élévation latérale, Overhead Press, Row, Überzug, źcisk, Zercher Squat]`, byte-for-byte the order web's `localeCompare` produces. Closed on its own evidence; the residual (a fold is not a collation — `ø`, `đ`, `ł`, `ß`, `æ` have no canonical decomposition and still file after `z`) is stated in the comparator and in the ADR.
- [x] **The catalogue picker's create affordance is suppressed by a name it is not showing, whenever a category filter is on.** `hasExact` scans every entry while `filtered` also applies the category filter, on both platforms (`ExerciseCataloguePicker.svelte`, `exercise_catalogue_picker.dart`). So selecting "legs" and typing the exact name of a chest exercise renders "No exercises match." with no create button and no explanation — the residual case of the dead end [§ 1276](../architecture/decisions.md) closed for the fold. Not fixed here because it is not the fold and the honest repair is a new sentence ("that exercise is in another category"), which needs a key in all seven web catalogues plus the seven ARBs and a matched Dart edit. Lower severity than § 1276's: the user can widen the filter, and nothing is unreachable. Owner trees: `apps/web/src/lib/components/` + `apps/mobile_android/lib/widgets/` + the i18n catalogues. **CLOSED by the round-42 web-catalogue-picker lane** ([§ 1332](../architecture/decisions.md)), and the repair the filing proposed is NOT the one that landed. Narrowing the exact-match test to the filtered set resolves two bad ways, not one: against a seeded GLOBAL the insert SUCCEEDS (`exercises_global_name_key` is partial on `author_id is null`, so the author's own partial index cannot see it) and the catalogue then holds two rows under one key; against the user's own custom it hits `exercises_author_name_key`, `createCustomExercise` returns null, and the picker says "Couldn't add that exercise." after having said the exercise does not exist. The test stays catalogue-wide and the picker gained a third state instead: `hiddenExact` names the entry the category filter is hiding, and both platforms now say "“Bench Press” is already in the catalogue, under Chest." rather than "No exercises match." Key `gym.catalogue.otherCategory` in all seven web catalogues, `gymCatalogueOtherCategory` in all seven ARBs.
- [x] **The catalogue picker fix ships with no behavioural pin, because `apps/web` has no harness that can hold one.** [§ 1278](../architecture/decisions.md). The unit suite runs under `tsx --test`, which cannot compile a Svelte component, and the tree has zero `.svelte` component tests against 107 components. The two count fixes beside it are pinned by mutation-tested unit tests; the picker's is pinned only by the source guard, which proves the naive fold is gone and not that the canonical one matches. What is owed is a Playwright case: seed a catalogue entry spelled `Bench<U+00A0>Press`, type `bench press` into `[data-testid=catalogue-search]`, and assert the entry is listed — the assertion that would have failed before this change. Note `tests-e2e/gym/exercise-key-fold.spec.ts` already exists and is `test.fixme` for an unrelated cross-spec interaction, so whoever takes this should read that file first. Owner tree: `apps/web/tests-e2e/gym/`. **CLOSED by the round-42 web-catalogue-picker lane** ([§ 1333](../architecture/decisions.md)). The premise held — `tsx --test` still cannot compile a Svelte component and the tree still has zero component tests — so the pin came from moving the decision OUT of the markup rather than from teaching the harness to render it: `apps/web/src/lib/components/exercise_catalogue_picker.ts` is an ordinary pure module with 13 mutation-tested cases beside it. A component-test harness was priced and refused (four devDependencies, a second unit runner, a `CI gate` `needs:` entry, and `lib_structure_guards`' unit-glob claim stops describing the suite) — a permanent tax on 107 components to pin one. The Playwright case the filing describes is still genuinely owed and is refiled below: it is the only thing that can prove the sentence reaches the DOM.
### Surfaced by the #789 round 41 web-routes lane (2026-09-06)

- [x] **`fetchRoutes` / `fetchRoutesWithError` still return `Route[]`, so the truthful list type is opt-in at the call site.** [§ 1298](../architecture/decisions.md) states the change as a diff rather than an intention, because "narrow the return type" is the sentence that gets re-derived from scratch. In `apps/web/src/lib/core/data.ts`: delete the two literal column constants and import `ROUTE_LIST_COLS`, `PUBLIC_ROUTE_LIST_COLS`, `RouteListItem`, `PublicRouteListRow` from `../routes/route_list_columns`; return `RouteListItem[]` from both fetchers; change the four local annotations inside `fetchRoutesWithError` (`owned` :1627, `saved` :1652, `byId` :1667, `merged` :1683) and the `r is Route` predicate (:1679) to `RouteListItem`; change `as unknown as Omit<Route, 'waypoints' | 'is_starred'>[]` (:1670) to `PublicRouteListRow[]`, after which the `as Route` on the filled object (:1671) is unnecessary — the spread structurally IS a `RouteListItem`, which the module's own type-level fill check already asserts. Then three consumer annotations: `RunEditor.svelte:38`, `EventEditor.svelte:64`, `clubs/[slug]/+page.svelte:115`. `Route` stays assignable to `RouteListItem`, so `EventEditor`'s `[...mine, ...clubs]` merge with `fetchClubRoutes` (a genuine `select('*')`) keeps typechecking. `route_list_columns.test.ts` accepts either shape of `data.ts` and fails when neither is present, so it survives its own remedy. Owner tree: `apps/web/src/lib/core/` (+ `lib/components/`, `routes/clubs/`). **REFUTED 2026-09-06 — already done, in the very commit that filed this.** `git log -S 'RouteListItem[]' -- apps/web/src/lib/core/data.ts` names one commit, `856150274` (#859), which is where this entry was written. On `main` at that sha `fetchRoutes` and `fetchRoutesWithError` both return `RouteListItem[]`, the two literal constants are gone and the four imports are in place, all five internal annotations are `RouteListItem` / `PublicRouteListRow`, the `as Route` on the filled object is gone, and all three consumers (`RunEditor.svelte:38`, `EventEditor.svelte:64`, `clubs/[slug]/+page.svelte:115`) already annotate `RouteListItem[]`. Nothing was owed. The filing describes the diff it shipped alongside.
- [x] **The two public-route RPC readers cast a fifteen-column `public_routes` row to the twenty-two-column `Route`.** `nearbyPublicRoutes` (`data.ts:1470`) and `searchPublicRoutes` (`:1518`) both call RPCs declared `setof public_routes`, and both end `as Route[]` — omitting `waypoints`, `is_starred`, `geom`, `geom_public`, `start_point`, `description` and `shadow_hidden`, of which `waypoints` is the non-nullable `TrackPoint[]` whose absence was the whole of [§ 1229](../architecture/decisions.md). Not live today: the only consumer, `RouteExplorer.svelte`, reads `is_featured` and `tags`, both of which the view serves. No new declaration is owed — the generated `Database['public']['Functions']['search_public_routes']['Returns'][number]` already IS the truthful type, so the fix is to stop casting past it. Owner tree: `apps/web/src/lib/core/`. **CLOSED 2026-09-06 ([decisions § 1328](../architecture/decisions.md)).** Both now return `PublicRouteSummary`, added to `route_list_columns.ts` as `Pick<Route, keyof PublicRouteViewRow & keyof Route>` — derived from the generated view row so a migration moves it, and `Pick`ed from `Route` rather than taken verbatim because postgres cannot prove a view column NOT NULL and the generated row types all fifteen as nullable. `RouteExplorer.svelte` is the one consumer and needed only its two annotations. Pinned three ways: an `AssertNever` that the view names nothing `Route` does not, one `@ts-expect-error` per withheld column (mutation-tested), and `PublicListIsAViewSubset`, which is a stronger claim than the existing runtime column check.
- [x] **The `Route` overlay declares `shadow_hidden`, which every read path deliberately strips.** `fetchRouteById` destructures it off the owner read with a `void` and a comment calling it server-owned moderation state the client has no business reading (`data.ts:1774`); `public_routes` projects it away; the list projection never selects it. So the one column the read boundary is unanimous about is the one the client type promises on every path. The fix is `Omit<RouteRow, 'waypoints' | 'surface' | 'shadow_hidden'>` in `apps/web/src/lib/types.ts` — a one-word edit that then fails `core/mock-data.ts`'s `makeRoute`, which sets `shadow_hidden: false` in an object literal typed `Route`, so it wants the two trees in one change. Owner tree: `apps/web/src/lib/` (`types.ts` + `core/mock-data.ts`). **CLOSED 2026-09-06 ([decisions § 1327](../architecture/decisions.md)).** `Route` is now `Omit<RouteRow, 'waypoints' | 'surface' | 'shadow_hidden'>`; `makeRoute` dropped the literal (excess-property checking caught it, as filed); `fetchRouteById` keeps the destructure because the wire still carries the column. Pinned by `AssertNever<Extract<keyof Route, 'shadow_hidden'>>` in `route_list_columns.test.ts`, stated against `Route` rather than `RouteListItem` so re-adding the column to the overlay fails; mutation-tested both directions.
- [x] **A narrowed `.select()` anywhere else in `data.ts` has the same untyped gap, and only `routes` has been swept.** The projection-vs-type mismatch is a class, not an instance: `CLUB_SELECT_COLS` (`data.ts:2057`) enumerates eighteen columns of `clubs` and its rows are read as the full `Club`, and the file carries further hand-enumerated select lists. None was measured this round. The durable sweep is the `conventions.md` rule this lane added — declare the column tuple once, derive the select string and the row type from it — applied to each remaining list, with the `@ts-expect-error` pin per withheld column so a runtime suite is not asked to see something it cannot. Owner tree: `apps/web/src/lib/core/`. **SWEPT 2026-09-06 ([decisions § 1329](../architecture/decisions.md) + [§ 1330](../architecture/decisions.md)); three residuals re-filed below.** All 170 narrowed selects in `data.ts` were paired with the type their rows are handed back as. Most are already right — 30-odd fetchers return purpose-built interfaces (`RecentRunOption`, `EventPhoto`, `PublicCrossing`, `EventResultRow`, `SafetyContact`, `EventCheckpoint`, `ActivityRow`, `FitnessSnapshotRow`, `EventException`) whose key sets match their projection exactly. Four real instances were found and fixed: `Event` and `ClubMember` (§ 1329, five columns, three of them column-grant REVOKED rather than merely unprojected), `fetchRuns`' free-form `columns?: string` and `fetchRunsForPeriodSummary` (§ 1330). `CLUB_SELECT_COLS` is REFUTED: it enumerates nineteen columns, not eighteen, and `Club` declares exactly those nineteen. Guards added: a projection-equals-overlay equality for `events` / `clubs` / `club_members`, a migration-replay check that neither the projection nor the overlay names an ungranted column, and `@ts-expect-error` pins on `PeriodSummaryRun`.

### Surfaced by the #789 round 41 mobile-fold lane (2026-09-06)

- [x] **The route-name search folds on two runtimes that disagree at `İ`, and the sort beside it is a larger divergence than the search.** [§ 1283](../architecture/decisions.md). Refiled with corrected paths: the round-40 filing sent this to `core/data.ts` and it is not there — `data.ts` carries exactly two `toLowerCase` sites, `slugify` (now [§ 1279](../architecture/decisions.md)'s shared helper) and the exact-`@handle` float in `searchPeople`, which [§ 1251](../architecture/decisions.md) already ruled unreachable behind `user_profiles_handle_format`'s `^[a-z0-9_]{3,30}$` and which has no Dart twin. The real rails are `apps/web/src/routes/routes/+page.svelte:153/159` against `apps/mobile_android/lib/screens/routes_screen.dart:402/413`. **Measured:** `'İstanbul Loop'.toLowerCase()` is `i̇stanbul loop` in Node, which does not contain `istanbul`, so a route named `İstanbul Loop` is found by typing `istanbul` on the phone and not on the web — the same reversal [§ 1249](../architecture/decisions.md) recorded for the catalogue picker. One line each side: `fold` from `catalogue_browse`. **The `az` SORT next to it is a separate and bigger question and must NOT be folded reflexively** — web uses `localeCompare` (a collation, whose answer depends on the host's ICU data) and the phone `toLowerCase().compareTo()` (a code-unit order), and those disagree far beyond one code point: a collation puts `Å` beside `A` where a code-unit comparison puts it after `Z`. Making them agree means choosing which order the product wants on both platforms, which is why `catalogue_browse` deliberately compares FOLDED names with `<` / `>` rather than collating at all. Owner trees: `apps/web/src/routes/routes/` + `apps/mobile_android/lib/screens/routes_screen.dart`. **CLOSED 2026-09-06** — both rails now fold through `catalogue_browse`'s generated table, and the sort goes through a new shared `compareFoldedNames` that `sortCatalogue`'s own `byName` delegates to, so the catalogue and the routes list are one function rather than three copies. The filing's warning was right and is now measured: the search fold differs at **466** code points, while `localeCompare` against a folded code-unit order disagrees on **31.75 % of all pairs** of the 145,672 Unicode letters (17.38 % over just the Latin/Greek/Cyrillic 830), with **every** letter code point mis-ordered against at least one other. Folding alone would have left web collating, so both sides compare folded keys with `<`/`>`; ties break on `id`, which also removes a latent instability since Dart's `List.sort` is not stable. [decisions § 1337](../architecture/decisions.md).
- [x] **The web club editor accepts a name with no letter or digit in any script, where the phone now refuses it.** [§ 1281](../architecture/decisions.md). The phone's pre-flight was fixed this round to test the NAME for `[\p{L}\p{N}]` rather than testing the derived SLUG for emptiness — which is what unblocked every non-Latin club name there. `ClubEditor.svelte:62` gates on `!name.trim()` and nothing else, so a club called `!!!` is creatable on the web today and lands under the fallback slug `club`. Two defensible readings and the code states neither: either a name is anything non-blank (and the phone should drop its predicate) or a name must name something (and the web should gain it, with `clubFormErrSlug`'s sentence as the copy — the key already exists in all seven ARBs but not in the web catalogues). Not taken here because `apps/web/src/lib/components/ClubEditor.svelte` and the web i18n catalogues are outside this lane. Owner trees: `apps/web/src/lib/components/` + `apps/web/src/lib/i18n/locales/`. **CLOSED 2026-09-06** — the second reading was taken (a name must name something) and the predicate moved INTO the `club_slug` pair as `clubNameNamesSomething`, so the two clients cannot disagree about it again; `clubFormErrSlug`'s sentence is now a key in all seven web catalogues too. It is deliberately NOT the phone's `[\p{L}\p{N}]`: a property class is answered by each runtime's own Unicode tables exactly as `toLowerCase` is, and the accept-list diverges between Node and Dart at **4,657** code points — every one a letter the newer table knows and the older refuses, which is § 1281's bug re-imported by the predicate that fixed it. The shipped refuse-list `[^\p{Z}\p{P}\p{S}\p{Cc}\p{Cf}]` diverges at **104**, in the harmless direction. [decisions § 1338](../architecture/decisions.md).
- [x] **`ApiClient.createClub` is a second club-insert path with no caller, no slug cap and no collision retry.** Found while verifying that the phone's create path really does retry on 23505 before deciding [§ 1279](../architecture/decisions.md)'s fold could widen the collision class safely — it does, but the retry lives in `SocialService.createClub` (`apps/mobile_android/lib/social_service.dart:701`), and `packages/api_client/lib/src/api_client.dart:4727` carries a parallel `createClub` that inserts directly, has no retry, and is referenced by nothing under `apps/mobile_*/lib` or any test. Dead code is bad enough; dead code that writes a unique column without handling the uniqueness violation is a trap for the next caller. Delete it, or route it through the same retry. Owner tree: `packages/api_client/`. **CLOSED 2026-09-06** — deleted, with `_clubSafeCols` (its only reader) going too; verified caller-less across every `.dart` in the repo first. It also never minted the `invite_token` an invite-only club needs, so a caller would have got an unshareable club as well as an unhandled 23505. The replacement is a guard rather than an absence: `architecture_guards_test.dart` now COUNTS club-insert sites across `apps/mobile_*/lib` + `packages/` and requires exactly one, in `social_service.dart` — a count and not a check for the deleted symbol, because a guard naming `ApiClient.createClub` would pass the moment the next duplicate is called something else. Mutation-verified. [decisions § 1339](../architecture/decisions.md).
### Surfaced by the #789 round 41 backend-sql lane (2026-09-06)

- [x] **`routineFromWorkout` groups routine blocks on the raw display spelling, so one lift logged under two capitalisations becomes two routine blocks.** `apps/web/src/lib/gym/gym_routine.ts:163` and `apps/mobile_android/lib/gym_routine.dart:221` both take `last.exerciseName == name` — the raw string — and only then stamp `normaliseExerciseName(name)` onto the new block, so "Bench Press" followed by "bench press" in one session saves two `gym_routine_exercises` rows carrying the same `exercise_key`. Cosmetic, not a correctness defect: consecutive sets under the SAME spelling already merge, so grouping on the canonical key instead would merge nothing that is deliberately separate, and adherence is unaffected (see below). One line on each rail of a registered parity pair. Owner trees: `apps/web/src/lib/gym/` + `apps/mobile_android/lib/` — neither is this lane's. **CLOSED 2026-09-06 — the same defect as the round-41 web-share-coach entry above, not a second one.** This filing's severity reading was the correct one (cosmetic); that one's `(exerciseKey, setIndex)` claim was reading a stale signature. [§ 1322](../architecture/decisions.md).
- [ ] **Do NOT add a unique index on `gym_routine_exercises (routine_id, exercise_key)`.** Filed here as a negative because it was proposed to this lane as the durable fix for the entry above and it would be a regression. Two rows of one routine sharing a key is the heavy-top-set-then-back-off pattern, which the tree names by that name and has already paid to support: `gym_adherence.ts` / `gym_adherence.dart` match on `(exerciseKey, **stepIndex**)` — the ordinal in the expanded step list — precisely because the per-block `setIndex` "restarts inside each exercise block, so a routine that programs the same exercise twice … minted identical keys and collapsed both blocks onto one logged set". Full reasoning and the measurement in [decisions § 1286](../architecture/decisions.md). No action wanted; the entry exists so a later round does not take it on a stale filing's face. **Re-verified 2026-09-07** ([decisions § 1489](../architecture/decisions.md)) against the live catalogue and all three client rails: round 44's `gym_sets_exercise_key_nonempty_chk` is a `length >= 1` floor on a different table, not a uniqueness claim, so the negative stands unchanged. **Deliberately left unticked** — a ticked negative reads as "done, stop reading", and this is a standing instruction to whoever proposes the index next.
- [x] **Three client sites still compute an exercise key the server now overwrites, and one of them still documents a failure mode that can no longer happen.** [decisions § 1284](../architecture/decisions.md). Since `20270711000001` both `Insert` types carry the column as optional, so `apps/web/src/lib/core/data.ts:9585` (`name_key: normaliseExerciseName(name)` in the custom-exercise create), the `exercise_key` the web routine save/publish paths send at `data.ts:9880` / `:9973`, and the mobile equivalents can all simply stop sending it. Harmless as they stand — the value they send is the value the trigger writes — but every one of them is a rail that can drift, which is the whole reason the column moved server-side. `apps/mobile_android/lib/gym_prs.dart:135` additionally still warns that a caller may write these two columns "without a 23514", a caution that describes a refusal the schema no longer performs. Owner trees: `apps/web/src/lib/core/` + `apps/mobile_android/lib/` — neither is this lane's. **CLOSED 2026-09-06, and the count was two on web** ([§ 1323](../architecture/decisions.md)). `data.ts:9880` in this filing is `fetchGymRoutine`'s READ mapping, not a write. Removed: `createCustomExercise`'s `name_key`, `createGymRoutine`'s `exercise_key`, and `ApiClient.createGymRoutine`'s `colExerciseKey`; `gym_prs.dart`'s comment now states that the 23514 is gone and that a stale table splits the bucket silently instead. `ApiClient.createCustomExercise`'s `required String nameKey` is deliberately left — see the new entry at the end of this file.
- [x] **A correcting BEFORE trigger empties every `lives_ok` pin around the constraint it fronts, and nothing fails.** [decisions § 1287](../architecture/decisions.md). Adding the § 1284 triggers broke one assertion loudly (`normalise_exercise_name_test.sql` 13, a `throws_ok` on the 23514) and silently emptied two more — assertions 21 and 26, the reachability pins for the U+0130 and frozen-table fold widenings, whose whole content was "this client-supplied key satisfies the CHECK". With the column stamped before the CHECK runs, a `lives_ok` there survives a server that had stopped folding those code points entirely. All three are repaired in this round by reading the stored key back through `returning`. **The general obligation this leaves is unaudited**: no guard knows which positive pgtap assertions sit downstream of a correcting trigger, so the same emptying can recur on the next column that gets one. A `check_pgtap_refusal_assertions.mjs` sibling for POSITIVE assertions — mutate the trigger rather than the policy — is the shape that would catch it. Owner tree: `apps/backend/scripts/` (not this lane's this round). **CLOSED 2026-09-06** ([§ 1324](../architecture/decisions.md)). The check lands in `check_pgtap_refusal_assertions.mjs`'s STATIC phase rather than as a new script, so it runs in the CI job that already exists. It replays the migrations to derive the 19 columns a live BEFORE trigger assigns UNCONDITIONALLY and reports any `lives_ok` supplying one: 4 of the suite's 224, two registered with their reason and two repaired by not supplying the column. The 13 assertions supplying a *conditionally* stamped column are measured and filed below, not ruled on.
- [x] **`docs/backend/api_database.md` documents `gym_routine_exercises` and `gym_sets` but has no entry for the `exercises` catalogue table at all.** Noticed while documenting the `exercises_stamp_name_key_trigger`: `exercises` (migration `20270222_001` — `author_id` null for the seeded globals, the two partial unique indexes over `name_key` that decide what counts as a duplicate custom exercise, the `on delete set null` from `gym_sets.exercise_id`) appears in that file only inside the `normalise_exercise_name` paragraph, so its RLS shape and its uniqueness semantics are documented nowhere. `exercises_rls_test.sql` was the only statement of the contract. **CLOSED 2026-09-06** — a row in the "Other shipped tables" summary now carries the seeded-global-vs-custom split, the two partial unique indexes and what they mean now the key is folded, the `on delete set null` from `gym_sets.exercise_id`, and the new stamping trigger.

### Surfaced by the #789 round 41 mobile-stores lane (2026-09-06)

- [x] **`instance_start` is minted from the reader's timezone on mobile, and from the true instant on web.** Full trace + evidence in [decisions § 1291](../architecture/decisions.md). `recurrence.dart` sets `useUtc = e.timezone != null`, so a recurring event with no declared timezone gets occurrences built with the unnamed `DateTime(...)` constructor — a LOCAL instant. That reaches `api_client` as `EventView.nextInstanceStart`, and every one of its `instance_start` sites serialises with a bare `.toIso8601String()` (`rsvpToEvent`, the attendee read, `fetchEventResults`, `fetchRaceSession`, the run-photo query, `upsertCheckpointCrossing` — lines 3462 / 4911 / 4928 / 4972 / 5008 of `packages/api_client/lib/src/api_client.dart`), which writes no zone designator for a local `DateTime`. Postgres resolves the zone-less literal in the session's own TimeZone and matches `instance_start` with `=`, so two readers in different zones write two different keys for one occurrence. Web's `recurrence.ts` keeps the same `event.timezone` branch but a JS `Date` is an instant and `toISOString()` always emits `Z`, so web writes the true instant and a phone RSVP does not appear in the web attendee list for that occurrence. **Read-derived, not executed** — no run was made against a live occurrence; the precondition is recurring + `timezone` null + reader not at UTC. The durable fix is one change in one place: either `api_client` normalises every `instance_start` (all consumers move together) or `recurrence.dart` stops minting local instants. Fixing it in one store would be worse than leaving it — it would file that store's rows under a key no other surface writes, which is why `local_crossings_store.dart`'s site carries a `zone-verbatim:` marker instead. Owner tree: `packages/api_client/lib/src/api_client.dart` + `apps/mobile_android/lib/recurrence.dart` + the iOS twin (+ `apps/web/src/lib/social/recurrence.ts` if the parity pair moves). **CLOSED 2026-09-06 ([decisions § 1343](../architecture/decisions.md)).** Fixed at `api_client`, and the parity pair does NOT move: `expandInstances` names the same absolute instant on both platforms, and the divergence is purely in serialisation, so changing `recurrence.dart` would move one half of a correct pair. The entry's framing that every mobile writer sends the unnormalised value is **wrong and that is what settled the design**: `social_service.dart` already normalises at all ELEVEN of its `instance_start` sites and `race_controller.dart` at its persisted one — `api_client` was the sole outlier, so the fix brings one file onto a contract the rest of the tree had already adopted. Two of the six sites are opposite ends of one feature (a photo is TAGGED through the normalised `social_service` path and READ BACK through the unnormalised `api_client` filter), so an event gallery could fail to list the photos that same phone had just uploaded. A SECOND defect of the same shape was found in the store while fixing it and is fixed with it: `local_crossings_store` compared `instance_start` as a STRING in `rowsForCheckpoint` and in `replaceFromServer`'s scope test, and one occurrence has at least three spellings there (`+00:00` from PostgREST, `.000Z` from `CheckpointCrossingRow.toJson`, whatever the store itself wrote) — so a server-fetched crossing never matched and "who has already been stamped here" listed only this phone's own stamps. Both now compare instants. The `zone-verbatim:` marker on `pushCreate` is gone: the site reads through `parseServerTimestamp` and refuses an unreadable instance rather than guessing.
- [x] **`storedClockOrNow` falls back to `now`, which lets a corrupt clock freeze a synced row against the server forever.** [decisions § 1290](../architecture/decisions.md) made a wrong-typed `last_modified_at` behave like an absent one instead of discarding the whole record, and deliberately did not change the fallback VALUE. `now` is not obviously right: a `synced` row handed `now` wins `local.lastModifiedAt.isAfter(serverTs)` in every subsequent `replaceFromServer`, and `rewriteAll` persists that clock, so the row keeps winning across launches and the server can never overwrite it. The epoch (`DateTime.fromMillisecondsSinceEpoch(0, isUtc: true)`) loses every newer-wins comparison while still keeping an unsynced row and still pushing it, which is right in both directions. The change is one line in `offline_sync_store.dart`, but it moves behaviour for the three inputs (absent / empty / unparseable) that already took `now` before this round, so it wants its own measurement of what actually reaches it. Owner tree: `apps/mobile_android/lib/offline_sync_store.dart` + the iOS twin. **CLOSED 2026-09-06 ([decisions § 1342](../architecture/decisions.md)).** The freeze is real and reproduces from the code: `rewriteAll` persists the winning clock, so the row goes on beating the server across launches rather than only for the session. The measurement the entry asked for found a SECOND symptom the filing did not name — `local_routine_store` / `local_recipe_store` / `local_meal_template_store` each sort their library `b.lastModifiedAt.compareTo(a.lastModifiedAt)`, so an unreadable record also claimed the head of the most-recently-used list. Moved to `DateTime.utc(1970)` and renamed to `storedClockOrEpoch` so the name states it and all seven `fromJson` call sites had to be re-read; the sentinel is exported as `kUnknownStoredClock`. Verified that the epoch costs nothing in the other direction: `replaceFromServer` sorts every non-`synced` row into `preserved` before the comparison runs, so unsent work is still kept and still pushed.
- [x] **`DateTime.tryParse` rolls out-of-range components over rather than refusing them, and three stores read a `date` column with it.** `2026-13-45T99:99:99Z` parses to 2027-02-18T04:40:39Z; `2026-06-32` parses to 2026-07-02. Pinned in `offline_sync_timestamps_test.dart` for `parseServerTimestamp`, whose callers only compare or store the result, so a confident wrong answer is harmless there. It is NOT obviously harmless for `local_gear_store.dart`'s `_parseDate`, which feeds `purchasedAt` / `retiredAt` into `api.createGear` / `api.updateGear` and therefore writes the rolled-over day back to a `date` column. Nothing produces such a value today (the mobile date picker cannot), so this is a robustness gap rather than a live defect. Owner tree: `apps/mobile_android/lib/local_gear_store.dart` + the iOS twin. **CLOSED 2026-09-06 ([decisions § 1344](../architecture/decisions.md)).** The rollover is real and reproduces exactly as filed. The headline's COUNT is wrong — **one** store reads a `date` column, not three: `gear.purchased_at` / `gear.retired_at` are the only `date` columns any store in the family touches (checked against every `create table` in `apps/backend/supabase/migrations`), and the entry's own body names only that one. Fixed above the two readers rather than inside gear's: `parseIsoStrict` is now the single `DateTime.tryParse` in the family and range-checks every component against the calendar the text claims, and both `parseServerTimestamp` and the new shared `parseCalendarDate` go through it — so the timestamp side is hardened too, where the filing had judged the rollover survivable. Gear's private `_parseDate` is gone rather than exempted, and the guard's allowance moved with it plus a new assertion that the shared file holds exactly ONE raw-parse site. **§ 1289's prose has the hemisphere backwards** and is corrected in § 1344: normalising a `date` to UTC moves the day for a device AHEAD of UTC, not west of Greenwich (measured across three zones), which is also why a west-of-Greenwich test runner cannot detect that mutation.

### Surfaced by the #789 round 41 infra-ci lane (2026-09-06)

- [x] **CodeQL analysed one of this repo's two Go modules, and the explicit build was what turned off the fallback that would have caught the other.** Closed 2026-09-06 ([decisions § 1304](../architecture/decisions.md)). `security.yml`'s `codeql-go` job had one build step — `go build ./...` in `apps/job_worker` — under a comment reading "Our only Go module is apps/job_worker". `apps/graph_cycle` has been a second module since the route-loop sidecar landed: its own `go.mod`, 27 source files, a `Test graph_cycle sidecar (Go)` job in `ci.yml`, a `release-graph-cycle.yml`, and a Trivy job **in the same file** that builds its image — so its dependencies were scanned and its source never was, on a service that terminates HTTP. Go has no build-less extraction ("CodeQL will analyze whatever source code is built by your specified build steps"), so the set of modules built is the set scanned, and a database holding one module's code looks no different from one holding two. **The single build was also what suppressed the safety net**, which is why this survived: `analyze-action.ts` at the pinned SHA carries `runAutobuildIfLegacyGoWorkflow`, which walks every module, and skips when "at least one file of Go code has already been extracted" — which building `job_worker` guarantees. Had the step never been added, both modules would have been scanned. Fixed by enumerating `go.mod` directories from the tree rather than listing them, so a third module is scanned the day it lands, with a population floor of two so a `find` that stopped matching fails instead of going green having scanned nothing (mutation-tested: pointing `find` at a name nothing matches refuses at `COUNT=0`). Both modules build clean; `actionlint` and all six workflow/infra guards pass.

- [x] **Three comments on the CloudFront 403 block were false, two of them contradicting the same block they sit in.** Closed 2026-09-06 ([decisions § 1308](../architecture/decisions.md)). The block's opening line still read "SPA fallback — SvelteKit static fallback is index.html" while the paragraph 30 lines below states the shell is `/200.html` and the resource sets exactly that. A second sentence described the distribution rewriting "a 404 to the shell at 404" — a mapping [§ 1022](../architecture/decisions.md) added and [§ 1084](../architecture/decisions.md) removed — contradicting the block six lines beneath it that explains why no 404 mapping exists. The irony is on the record: that same paragraph congratulates itself for resolving an earlier pair of contradictory comments in this very file ("Two comments in one file cannot both be true; this is the one that was wrong") and had since acquired two more. `terraform validate` has no opinion about comments, `fmt` does not read them, and `check_infra_error_responses.mjs` parses `error_code` and `response_page_path` rather than prose — so nothing read these, which is the general form of the problem. The replacements name the mechanism and cite the ADR that owns it, and the deploy-order note now also points at `docs/ops/deployment.md` so the runbook and the resource that needs it are one link apart.

- [ ] **CodeQL's Kotlin leg claimed `apps/mobile_android/android` and never reached it — 11 platform-channel bridges are unscanned, including a `BroadcastReceiver`.** Same defect class as [§ 1304](../architecture/decisions.md)'s Go one, found by looking for it once the Go leg was understood, and the reason it is filed rather than fixed in the same change is cost. `security.yml`'s `codeql-kotlin` job has exactly one build step, `./gradlew compileDebugKotlin` in `apps/watch_wear/android`; CodeQL analyses what the build steps compile; and the `runAutobuildIfLegacyGoWorkflow` fallback that had been quietly covering the Go side is Go-specific with no Java/Kotlin analogue. So the 128 Kotlin/Java files under `apps/watch_wear` are analysed and the 11 under `apps/mobile_android/android` are not — `MainActivity.kt`, `RunActionReceiver.kt` (a `BroadcastReceiver`, a classic Android attack surface), `HealthRoutePermissionBridge.kt`, `CalendarBridge.kt`, `BatterySettingsBridge.kt` among them. These are the platform-channel bridges that mediate health permissions, calendar access and foreground-service actions, which is exactly the code Java/Kotlin queries are for. **The fix is not another build line**: the Flutter host's Gradle build needs the pinned Flutter SDK set up in this job (`local.properties`, the generated plugin registrant), so it means adding a Flutter setup step to a CodeQL job and paying its minutes on every PR and every weekly cron. That is a real trade to weigh, not an oversight to sweep up at the tail of a round. The header comment now states the gap instead of claiming the coverage, so whoever takes this starts from the truth. Owner tree: `.github/workflows/security.yml`. **PARTIALLY ADDRESSED 2026-09-06 — [decisions § 1352](../architecture/decisions.md) — and the scan itself is still what is owed.** The 11 files are still unscanned; what changed is that the gap is now a declared VALUE rather than a sentence. The build step enumerates Gradle projects from the tree (§ 1304's shape) and skips only what a `CODEQL_KOTLIN_UNBUILT` declaration names with a reason, which the same step's loop reads — so a third Gradle project is built the day it lands rather than silently unscanned, and if it cannot be built here CI goes red and somebody decides. `scripts/check_codeql_coverage.mjs` (in `workflow-lint`, which is in the `CI gate`) lifts each compiled leg's own `find` out of the workflow and RUNS it against an independent walk, so reverting either leg to a hardcoded path, a walk that stops matching, a stale exclusion and an exclusion list that covers everything all fail the PR. Two things were checked rather than assumed and neither offers a cheaper route: `build-mode: none` cannot stand in (GitHub's own docs — "if `build-mode` is set to `none` and Kotlin code is found, the Kotlin code will not be analyzed"), and there is no Java/Kotlin analogue of the Go autobuild net. So the remaining ask is the trade this entry already names: a Flutter SDK setup + `melos bootstrap` inside the CodeQL job, paid on every PR and every weekly cron. It was NOT taken here because it could not be verified from a lane forbidden to run Flutter, and shipping an unverifiable multi-minute Gradle step into the security workflow is worse than the gap. **Still open 2026-09-07, and the cheap fix is refused with a reason** ([decisions § 1501](../architecture/decisions.md)): the only cheap host is `build-mobile-android`, whose Kotlin task is UP-TO-DATE after the APK build, and an extractor that extracts nothing reports as clean as a full scan — the exact failure §§ 1304/1352 exist to prevent. The count is corrected too: 8 main-source files plus 3 test files, not "11 platform-channel bridges". The gap is now a figure the guard recomputes and fails on when it grows.

- [x] **The three `*_spa_shell.ts` header comments still name `build/index.html` after the shell moved.** `share_run_spa_shell.ts:7` and `share_route_spa_shell.ts:7` each describe the embedded artifact as "`apps/web/build/index.html` -- adapter-static's SPA fallback", and `entity_spa_shell.ts:10` says the Lambda's bundler embeds `apps/web/build/index.html`. All three are stale since [§ 1268](../architecture/decisions.md): the five `build.mjs` resolve `build/200.html`, and `spa_shell_filename.test.ts` proves it on every PR. `entity_spa_shell.ts:36` is doubly stale — "the distribution replaces every 4xx body with `/index.html` (decisions § 1022)" is wrong about the filename AND about "every 4xx", since [§ 1084](../architecture/decisions.md) removed the 404 mapping and the 403 one now names `/200.html`. Cosmetic, not load-bearing: § 1272's guard reads the adapter config, the five `build.mjs` and the Terraform, not these comments — which is precisely why they could drift. `docs/ops/deployment.md` used to list them as work owed and no longer does, so this is now their only record. Owner tree: `apps/web/src/lib/share/`. **CLOSED 2026-09-06.** All three embed-path lines now say `build/200.html`. The doubly-stale paragraph was rewritten rather than patched: it claimed the distribution replaces every 4xx body with `/index.html`, which was wrong about the filename AND about "every 4xx", and its closing sentence still called the 404 mapping "redundant rather than load-bearing" when § 1084 had removed it outright. It now states that the distribution maps only 403, that `scripts/check_infra_error_responses.mjs` fails the PR if a 404 mapping comes back, and that this function is therefore the only thing putting a designed card in front of a dead share link — verified against `infra/modules/web-stack/main.tf` and the five `build.mjs`.

- [x] **`terraform validate` passes on all five root stacks and on no module — re-run each round.** Standing item, re-filed rather than retired because its reason has not changed: no lane holds AWS credentials by design, so nothing behind §§ 1021-1024, 1084-1085, 1111 or [§ 1268](../architecture/decisions.md)'s `custom_error_response` has ever been `plan`ned against real state. Round 41's run and its planted-mutation measurement are recorded on the round-40 entry above and in [decisions § 1308](../architecture/decisions.md). Two things settled and not to be re-asked: the module cannot be validated standalone ([§ 1267](../architecture/decisions.md) — `configuration_aliases` makes a module a non-root by construction), and its coverage is transitive and **measured**, not asserted. The next round should run `init -backend=false` + `validate` in `infra/bootstrap`, `infra/dns`, `infra/github-oidc`, `infra/envs/prod` and `infra/envs/preview`, plus `terraform fmt -check -recursive infra`, and plant one undeclared-variable reference in the module to confirm the transitive path still reports. Note what this class of check structurally cannot see — round 41's own three false comments passed `validate` and `fmt` without a murmur. Owner tree: `infra/`. **Re-run for round 42, 2026-09-06 — [decisions § 1353](../architecture/decisions.md) — and re-filed on the same unchanged reason.** All five root stacks: `init -backend=false` then `validate`, five "Success! The configuration is valid.", plus `terraform fmt -check -recursive infra` exit 0. Local Terraform is 1.13.0, which is exactly what `terraform.yml` pins for both its `fmt` and `validate` jobs, so this is the binary CI runs rather than an approximation. Planted-mutation measurement re-taken as prescribed: an undeclared-variable reference in `infra/modules/web-stack/outputs.tf` was reported by BOTH consuming root stacks — "Error: Reference to undeclared input variable", naming the module file and line — each exiting 1; restored and re-validated clean. The fresh example of what this class cannot see is the same round's three `*_spa_shell.ts` comments, which described the CloudFront error-response contract wrongly and passed `validate` and `fmt` without a murmur. **Re-run for round 43, 2026-09-06 — [decisions § 1394](../architecture/decisions.md#1394-round-43s-terraform-re-run-five-stacks-valid-transitive-module-coverage-re-measured-in-a-file-no-previous-round-used) — and re-filed below on the same unchanged reason.** Terraform v1.13.0, matching `terraform.yml`'s pin for both its `fmt` and its `validate` jobs, so this is CI's binary rather than an approximation. All five root stacks: `init -backend=false` then `validate`, five "Success! The configuration is valid."; `terraform fmt -check -recursive infra` exit 0 before and after this round's edits, which touch no `.tf` file. Planted-mutation measurement taken in **`infra/modules/web-stack/variables.tf`** — deliberately the one module file none of the four previous measurements (§§ 1111, 1267, 1308, 1353) used, so the claim is about the module and not about one file in it: an `output` referencing an undeclared variable was reported by BOTH consuming root stacks, naming the module file and line, each exiting 1; restored byte-clean (`git diff -- infra/` empty) and both roots re-validated Success. No standalone `init`, so no stray `.terraform.lock.hcl`; `git status --porcelain --untracked-files=all infra/` empty. § 1267's standalone-module finding was not re-litigated. New this round rather than a caution: the "a stack nobody listed" shape that the round's other two items are instances of is, on the Terraform side, already closed — `check_infra_coverage.mjs` reads the `validate` matrix AND the `fmt` invocations as scopes against a walk of the tree (§ 1111), and both it and its 173-test suite pass. What remains uncovered is unchanged: no lane holds AWS credentials, so nothing behind §§ 1021-1024, 1084-1085, 1111 or § 1268 has ever been `plan`ned.

- [ ] **`POST /api/coach/turn` answered 200 text/html on prod, and the coach path's own statuses say that means the Lambda was never reached.** Split out of the distribution-wide-403 entry above, because that entry is a design question that will stay open and this is a possible live outage that should not wait behind it. The measurement is round 34's, on prod 2026-09-05, and is not disputed; the attribution was. `apps/web/src/lib/coach/handler.ts` returns **401** for an unauthenticated call at two separate exits, nothing on this distribution maps 401, and the only 403 the coach path emits is the AI-disclosure consent refusal — which needs an authenticated caller with a profile row and is unreachable by the request that was measured. The cache behaviour is not the explanation either: `/api/coach*` has its own `ordered_cache_behavior` targeting `lambda-coach` with `POST` in `allowed_methods`. So the 200 text/html was not a coach 403 being rewritten. Three things produce that reading and all three are bad: the request never reaching the Lambda (the issue #590 shape `main.tf` records as proven once — one of the two OAC grants missing, the Function URL 403ing *before invocation*, the mapping rewriting it to the shell at 200, "the surface reads healthy while the Lambda never runs"), a stale prod bundle, or a prod `/api/coach*` behaviour that does not match the repo. **Neither coach alarm can see any of them**: `-coach-lambda-errors` and `-coach-lambda-p95` key on Lambda `Errors` and `Duration`, which a request that is never invoked does not emit — the § 890 invisible-failure class, in the one place the module's own comment warns about it. **The check needs no credentials**: an unauthenticated `POST https://threkir.com/api/coach/turn` that reaches the Lambda answers 401 with a JSON body, so anything else is proof it did not, independently of the mapping. One `curl`. If it does answer 401 today, the round-34 reading was a transient or a since-fixed deploy and this closes; if it answers 200 text/html, the coach endpoint is down and has been since at least 2026-09-05. Not run by this lane, which is forbidden from touching the production distribution. Owner: anyone who can issue that request, then whoever holds prod credentials. [decisions § 1307](../architecture/decisions.md).

## Surfaced by the #789 round 41 edge-fns lane (2026-09-06)

- [x] **The lane's own item — "the 23 `handler_envelope.test.ts` integration tests are outside every mutation operator this repo has" — was a stale restatement of an entry closed twice on 2026-08-31** (lines 966 and 1008 of this file, [decisions § 815](../architecture/decisions.md)). **Corrected, and measured rather than inferred** ([decisions § 1319](../architecture/decisions.md)): on run 34037778107 (main `7e56afb78`) the `Edge Function tests (Deno)` job prints `running 25 tests … ok | 25 passed | 0 failed`, then `25 served-host handler-envelope cases measured against 22 single-gate mutations of the SERVED Edge Function tree (26 declared kills, 0 survived, 0 unmeasured)`. They run, and they run on every code change: the `changes` filter is an EXCLUSION (`^(docs/|\.claude/)|\.md$`) that fails open, so no Edge Function edit can skip the job.
- [x] **One edge below the filing was real and is closed: a case that never RUNS was measured by neither operator.** ([decisions § 1320](../architecture/decisions.md).) Both `check_edge_function_test_vacuity.mjs` and `check_served_envelope_mutations.mjs` enumerate a baseline **run**, and `parseJunit` drops deno's `<skipped/>` entries deliberately — measured: a case gated on an unset var, run with `SUPABASE_TEST_URL` set and a `--filter` naming it, yields `0 cases` from `parseJunit`. So it is absent from `unmeasuredCases` (which filters `ran`) and from `phantomKills` unless a mutation happens to name it — and a case nobody wrote a mutation for is exactly the case nobody names. The guard's own header had claimed the stronger rule ("a new case fails this guard until a mutation names it") that these two edges could not deliver. Closed with a static, host-free source census (`declaredCases` + `unnamedDeclaredCases` + `staleExemptions`, the § 1273 declared-exemption shape): with `ran ⊆ kills`, `kills ⊆ ran` and `declared ⊆ kills` the three sets are one, and all four quadrants are covered. **The class has exactly one instance in the repo**, ruled out by measurement: `ignore:` appears in no other of the 70 `*.test.ts` files under `supabase/functions/`, nothing else registers a `Deno.test` inside a conditional, every test file declares at least one case, and the only test-shaped non-`*.test.ts` file is a production module.
- [x] **`Deno.env.get` answers `""` for a set-but-empty variable, and the auth-email skip gate fired on exactly that.** ([decisions § 1321](../architecture/decisions.md).) `SKIP_MAIL = SKIP || SEND_EMAIL_SECRET.length === 0` beside a `?? '<literal>'` reads as a dead branch and was first written up here as one; measured (`FOO= deno eval 'Deno.env.get("FOO")'` → `""`, a string), `??` coalesces only on `undefined`, so an ABSENT secret ran the case and an EMPTY one skipped it. Empty is what a `SEND_EMAIL_HOOK_SECRET:` line with nothing after the colon produces, two lines from a `SENTRY_DSN=` in the same `.env.local` heredoc. Removed rather than made symmetric with `SKIP_DB` — making it coherent would add a *second* way for the file's only mail-delivery assertion to vanish.
- [x] **The `edge-functions` job's `::error::` for the `Validate the served-tree mutation table` step does not name the census failure mode.** It reads "an anchor … no longer occurs exactly once in the handler it names, or a mutation claims a test case the file no longer carries", which was the complete list before [decisions § 1320](../architecture/decisions.md) and is now missing the third: a case the file declares that no mutation kills and no `UNMEASURED_CASES` entry excuses. The assertion messages carry their own diagnosis, so a reader is not stranded — but the step summary is now incomplete, and § 764's rule is that a bundled job's steps each print their own honest `::error::`. One line in `.github/workflows/ci.yml`; not taken because the round-41 lane split gave `.github/` to nobody and it is the most conflict-prone file in the tree. **REFUTED 2026-09-06 — already fixed before this was read.** `git log -L 2811,2811:.github/workflows/ci.yml` shows commit `856150274` (PR #859, the base of round 42) rewriting exactly this string to add the third mode: "or the source census found a declared case that no mutation names (a case whose ignore: gate is never false in CI runs nowhere, so neither the unmeasured-case nor the phantom-kill check can see it -- register it or exempt it in UNMEASURED_CASES with a reason)". The filing was written against the pre-#859 text. No change made.
- [ ] **The served-tree mutation round was not re-run end-to-end this round, and the reason is a shared-resource constraint rather than a code one.** `main()` is unchanged apart from its header comment, and every static claim is verified host-free (26 unit tests, two mutations against the real test file, `deno check`, `check:script-types`, the full 925-case pure-helper suite). What is unverified locally is the ~22-round served run, because booting `supabase functions serve` against the one shared local stack and then mutating served handlers would have corrupted the `backend-sql` and `e2e` lanes mid-flight — and, found the hard way, a host launched under `flock … -c 'nohup … &'` inherits the lock fd and holds it for its whole life, which starved both lanes until it was killed. A future lane wanting this verification should either boot the host **outside** the lock or take a disposable stack.

### Surfaced by the #789 round 41 watch lane (2026-09-06)

- [x] **A permanently-rejected run keeps the queue non-empty forever, and there is no surface that says so or lets the runner clear it.** `drainQueueLoop` classifies a 400/404/409/422 as `SkipAndContinue`, leaves the entry in the queue, and its own comment says "the run stays in the queue until the user manually discards it from the UI". Two things make that untrue. **(1)** `lastError = null` runs on every successful push, so any later success in the same pass erases the skipped run's message; `anyTransientFailure` stays false, the caller runs `drainBackoff.onSuccess()` and sets `syncError = null`. Every Sync tap therefore reports success while the queue count never falls. **(2)** The discard the comment points at is `RunViewModel.discard()`, which acts on `state.thisRunId` — the run just finished, on the PostRun screen. Once the runner leaves that screen a stuck entry has no discard affordance at all. **Filed rather than changed, deliberately.** The `lastError` behaviour is not an oversight: `DrainQueueLoopTest` asserts it twice, in terms ("Skip-followed-by-success clears the banner", "Trailing success clears the banner even if a middle run was skipped"), so flipping it is reversing a stated decision, not fixing a slip. And flipping it alone would surface a message the runner can act on in exactly no way — the useful remedy is a queue-management surface the watch does not have, which [§ 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive) says is not a thing to invent on the wrist without an owner decision. What is owed is that decision: either the wrist gets a "1 run can't sync — discard?" affordance and `lastError` is retained for skipped entries, or the entry is auto-dropped after N failed passes with the reason logged. Verified by reading only; nothing here was run against a watch. **CLOSED 2026-09-06 — [decisions § 1347](../architecture/decisions.md).** Both halves, and the owed decision is the first of the two the filing offered: the wrist gets the affordance, the entry is never auto-dropped — destroying a runner's only copy of a run unasked is the call [§ 1253](../architecture/decisions.md) had just made in the opposite direction on the PostRun `×`. The `lastError` behaviour is **not** flipped: `DrainQueueLoopTest`'s two assertions are right about what they govern (the banner is a fact about the pass), and the rejections ride `rejectedIds` / `attemptedIds` because they are a fact about the QUEUE and have to outlive every later success. `rejectedAfterPass` carries them across passes — this pass's verdict for a run it attempted, the last pass's for one it never reached, dropped for anything no longer queued. On the wrist the rejected chip takes the PreRun slot ahead of the counted one ([§ 1104](../architecture/decisions.md)'s precedent: the figure is right and the sentence around it is not), behind the estate's two-press confirm, stating the count in both states and disarming if a drain changes what the second tap would destroy. `dropQueuedRun` gives the entry-before-file ordering one home now that the drain is not its only caller. 15 new Wear tests (767 at that commit, from 752; 78 classes from 77), eleven planted mutations each refused. **Rung: the Kotlin is host-tested — the module suite is green on this workstation — and the 192 dp round-bezel layout of the armed chip plus its stake caption has NOT been rendered anywhere, because no Wear emulator was run here.**

- [ ] **`metadata.hr_coverage` from `apps/watch_ios` has still never been produced by a real `HKWorkoutSession`.** Re-filed from the round-38/39 entry above, narrowed to exactly the part [§ 1299](../architecture/decisions.md) does not discharge. The accumulator is now *host-tested* — nine cases walk whole runs through `HeartRateCoverageAccumulator` — but host-tested is two rungs below what this figure needs before any doc may call it verified, and the tests themselves have not run anywhere yet either: no Swift compiler exists on this workstation or in Linux CI, and the macOS `test-watch-ios` job is the only machine that will ever see them. What is owed is unchanged: one recorded run on a simulator or a paired watch, `hr_coverage` present on the row and plausibly non-zero, per the *bench-verified* rung in [`docs/custom_watch/quality_standards.md`](../custom_watch/quality_standards.md). `metadata.md` and § 1207 both already say no doc may describe the figure as verified until then. Owner: whoever next has a Mac and a paired watch. **Partly corrected 2026-09-06 by the round-42 watch lane — [decisions § 1350](../architecture/decisions.md) — and the substance stays open.** The parenthetical "the tests themselves have not run anywhere yet either" was true when written and is not now, measured rather than assumed: `HealthKitFailureTests.swift` carries four `project.pbxproj` references including an `… in Sources */` build-file entry, so it is a member of the test target; `test-watch-ios` runs `xcodebuild test` against a booted watchOS simulator whenever `changes.outputs.code` is true, which is any diff outside `docs/` and `.claude/`; and that job is one of the 34 the required `CI gate` needs. #859 landed on protected `main` carrying those nine cases, so they have executed. **That moves nothing about the figure**: unit tests over a value type say nothing about whether HealthKit's sample timestamps on hardware are what the accumulator assumes, which is the one silent failure mode. What is owed is unchanged — one recorded run on a simulator or paired watch, `hr_coverage` present and plausibly non-zero on the row. What DID land in code around it is claim (13): every Swift file must be a target member, so the § 1156 class (a file Xcode never compiles, a suite green having run nothing) cannot silently take this evidence away. Also owed and NOT taken here, because it is another lane's tree: `docs/backend/metadata.md`'s `hr_coverage` row says the accumulator "has never executed on a simulator or a device", which is now too strong on the first half.

- [x] **The two watch clients' heart-rate coverage helpers are a parity pair in meaning and in nothing else.** `HeartRateCoverage` (Swift) and `HeartRateCoverage.kt` (Kotlin) write the same `runs.metadata.hr_coverage` column, share the 0.5 suppression threshold and the 30 s freshness window by hand, and say so in each other's doc comments — but they are in neither parity registry, `check_parity_pair_registry.mjs` cannot see them, and the `shared-library-syncer` agent's table does not carry them. That is [§ 641](../architecture/decisions.md)'s shape exactly: a declared lockstep nothing can detect a divergence in. The registries pair web with mobile and the two watch clients are additive surfaces under § 24, so the answer is probably a third rail in `scripts/check_shared_constants.mjs` — which already compares figures across non-registry rails — reading `minAverageBPMCoverage` / `MIN_AVG_BPM_COVERAGE` and `sampleFreshInterval` / `HR_SAMPLE_FRESH_MS` out of both files and failing on disagreement. Not taken in-round: `scripts/check_shared_constants.mjs` is shared with every other lane, and the round-41 watch lane already touched one shared script. Small and mechanical once owned. **CLOSED 2026-09-06 — [decisions § 1348](../architecture/decisions.md).** The rail landed as claim (12) of `scripts/check_watch_ios_source.mjs` rather than in `check_shared_constants.mjs`: that file is shared with every lane, and this one already reads outside `apps/watch_ios` for the same reason (claims 6 and 7 read the phone's end of an envelope the watch does not own). Two design points beyond the filing's sketch. It compares the FIGURES, not the spelling — the window is seconds on one wrist and milliseconds on the other on purpose, so an integer `kotlinPerSwift` converts and respelling the Wear constant as `30L` is caught as the divergence it is rather than accepted as agreement. And a rail it cannot READ fails rather than skipping: a rename, or a value that stops being a literal, is the edit most likely to take the two figures apart unobserved, and that refusal is the only thing making the name anchor safe. 6 new cases in the guard's own suite. Both source files' comments now name the rail instead of asserting a lockstep nothing checked.

## Surfaced by the #789 round 41 guards lane (2026-09-06)

Everything below lives outside `scripts/` and the two parity registries, which
is the only reason the lane did not take it. The first two are one-line
corrections to headers the lane read closely enough to measure.

- [x] **`weigh_in_flag.dart`'s header names a web environment variable that exists nowhere.** [§ 1315](../architecture/decisions.md). `apps/mobile_android/lib/weigh_in_flag.dart:5-7` says "Web spells the same flag `PUBLIC_WEIGH_IN_GATE`; mobile drops the web-only `PUBLIC_` prefix, as every other mobile deploy flag does" — but web reads `PUBLIC_WEIGH_IN_ENABLED` (`apps/web/src/lib/runs/weigh_in_flag.ts`, `apps/web/.env.example:157`, and `weigh_in_flag.test.ts` pins it), while mobile reads `WEIGH_IN_GATE` (`apps/mobile_android/.env.example:75`, `main.dart:159`). Grepped: `PUBLIC_WEIGH_IN_GATE` appears in no file in the repo. So the STEMS differ and the dropped-prefix convention does NOT hold for this one gate, which matters operationally — someone who has set one has not set the other, and the header says the opposite. The two sibling gates (`adaptive_fitness_flag`, `off_route_flag`) do follow the convention and their headers are correct. The syncer row and the CLAUDE.md entry added this round state the real pair of names; the header is what still needs the one-line correction, and the sentence should say the convention is broken here rather than that it holds. Owner tree: `apps/mobile_android/lib/weigh_in_flag.dart` (+ the iOS twin). **CLOSED 2026-09-06** — [decisions § 1354](../architecture/decisions.md). The header now states both live names and that the convention is broken in the STEM rather than the prefix. The dead token is not mentioned at all, and that was decided by the new guard rather than by taste: the first correction explained that `PUBLIC_WEIGH_IN_GATE` was the name the comment used to carry, and `deploy_gate_names.test.ts`'s "a name the repo reads nowhere is never stated as the counterpart" check failed it — correctly, since reading negation to excuse a token is the shape `check_twin_claims.mjs` already refuses.
- [x] **`this_week_strip.dart` calls a TS↔Dart pair "byte-identical", which only the iOS twin ever is.** `apps/mobile_android/lib/widgets/this_week_strip.dart:19` reads "(`current_week.dart`, byte-identical twin of web's `current_week.ts`)". [§ 1244](../architecture/decisions.md) corrected exactly this wording in `current_week.ts` itself a round earlier and this second copy was missed; [decisions § 39](../architecture/decisions.md#39-mobile_android-and-mobile_ios-share-a-byte-for-byte-dart-codebase) is what "byte-identical" means in this repo. Swept the rest of `apps/web/src/lib`, `apps/mobile_android/lib` and `packages/core_models/lib` for the phrase: the other eleven non-iOS uses are about OUTPUTS being byte-identical (`training.dart`, `route_gpx.dart`, `cross_source_dedup.dart`'s "the near-duplicate math is"), which is a different and correct claim. This is the only remaining misuse. Owner tree: `apps/mobile_android/lib/widgets/` (+ the iOS twin). **CLOSED 2026-09-06.** Reworded to "the Dart half of a registered parity pair with web's `current_week.ts` — same algorithm, edge cases, outputs and test counts, not the same bytes", with the § 39 meaning stated inline so the next reader does not have to look it up. The sweep was re-run and confirmed: the eleven other non-iOS uses are all about OUTPUTS being byte-identical, which is a different and correct claim.
- [x] **`off_route_flag` and `weigh_in_flag` are now registered pairs with no Dart mirror suite, and `off_route_flag` has none on either side.** [§ 1315](../architecture/decisions.md). The registration itself was the urgent half — an unregistered pair is invisible to the only automated divergence detector this repo has — and the rows say honestly what exists, but the debt is real. The precedent for what to write is one file over: `adaptive_fitness_flag` carries `training/adaptive_fitness_gate_guard.test.ts` ↔ `test/adaptive_fitness_gate_guard_test.dart`, SOURCE guards on the gate (that the read goes through the shared parse, that the fail-closed default is not weakened) rather than value tests, which is the right instrument for a two-line env binding. `weigh_in_flag` already has the web half (`runs/weigh_in_flag.test.ts`); what is owed is a Dart mirror for it and both halves for `off_route_flag`. A third guard is worth writing while there: the three gates' env-var NAMES are a stated convention with one live exception (the entry above), and nothing tests the convention. Owner trees: `apps/web/src/lib/safety/` + `apps/mobile_android/test/`. **CLOSED 2026-09-06** — [decisions § 1355](../architecture/decisions.md). All three written, in `adaptive_fitness_gate_guard`'s source-guard shape: `safety/off_route_flag.test.ts` (3) ↔ `test/off_route_flag_test.dart` (4), `test/weigh_in_flag_test.dart` (3) beside the existing web half, and `safety/deploy_gate_names.test.ts` ↔ `test/deploy_gate_names_test.dart` (4 each) for the naming convention. The third guard earned itself on its first run: the convention list was drafted with the three gates this entry names, and its own census walk found a FOURTH cross-platform gate, `nearby_flag`, that nothing had been comparing. Both registries updated with the new suites and counts. The Dart halves could not be executed from the lane that wrote them (two others held the Flutter budget) — every assertion was instead replayed against the real sources by an independent script, which found and fixed one wrong negative lookahead.
- [x] **Six member-level "Mirrors `core/data.ts:createClub`" claims name a module no reader can act on.** Measured while widening the reader ([§ 1314](../architecture/decisions.md)): outside their file headers, `social_service.dart` (3), `training_service.dart` (5), `race_service.dart` (1) and `preferences.dart` (1) each say a method mirrors a symbol inside `apps/web/src/lib/core/data.ts`, a 12,000-line module. The guard deliberately checks only that the path exists for these, because a claim about one call is not a claim about a module — but the claims are near-useless as written: `core/data.ts:createClub` tells a reader which file to open and nothing about which of its 504 exports is meant unless the `:symbol` suffix is there, and five of the ten omit it. The cheap durable fix is the suffix everywhere (`data.ts:armRace` rather than `data.ts`), which costs nothing and makes the claim followable; the guard already reads that form. Not a lockstep obligation and should not become one — `core/data.ts` is a no-touch tree for most lanes and cannot be half of a pair. Owner trees: `apps/mobile_android/lib/`. **CLOSED 2026-09-06.** Eleven claims carry the suffix now, not six: the filing counted the four prose-named ones in `social_service.dart` and missed that `training_service.dart`'s six spelled their symbol with a `#`, which `check_twin_claims.mjs`'s path regex cannot match at all — so those read as followable and were invisible to the guard. `race_service.dart`'s class-level claim names the six exports it mirrors instead of "the race helpers". Two corrections to the filing's own inventory: `preferences.dart` carries no `core/data.ts` claim (its `Mirrors` lines are about settings-bag keys and `format/weight.ts`), and `training_service.dart:897` is deliberately left module-scoped — it mirrors the `s?.trim() || null` PATTERN used across the module, which is not a claim about one export.

### Surfaced by the #789 round 41 e2e lane (2026-09-06)

- [x] **A run's fundraiser is invisible to every reader but the run's owner, and the component's own doc comment says the opposite.** [§ 1309](../architecture/decisions.md#1309-runsid-never-mounts-the-fundraiser-section-for-a-non-owner-so-the-spec-written-to-prove-the-non-owner-branch-was-asserting-against-a-component-that-was-not-on-the-page). `FundraiserSection`'s header frames its non-owner branch as being for "a donor following a shared link to a run whose fundraiser read failed", and `read_failure_guards.test.ts:298` pins that branch by source-grep — but `/runs/[id]` mounts the component only inside the final `{:else}`, and a viewer who does not own the run is returned from the `otherRunOwner` branch two branches earlier, into `RunShareView`. `RunShareView` has no fundraiser section, and neither does `/share/run/[id]`. So the campaign attached to a run is reachable only by its own `/fundraisers/[id]` link: the shared run link, which is what a fundraising runner actually posts, carries nothing. **The exact change:** mount `<FundraiserSection runId={pageData.id} isOwner={false} />` in `apps/web/src/lib/components/RunShareView.svelte` (rendered by both `/runs/[id]`'s non-owner branch and `/share/run/[id]`); the `fundraisers` RLS already admits the reader — `fundraiser_anchor_visible` delegates to `is_run_visible_to`, which is owner-or-public, and the component's `isOwner={false}` keeps the create CTA hidden. The mobile twin is a separate question: check `parity.md` before flipping a cell. Not taken here — `apps/web/src/` is another lane's tree, and this lane owns only `tests-e2e/`. Owner tree: `apps/web/src/lib/components/`. **CLOSED 2026-09-06 — already fixed when this was filed.** The exact change the entry asks for (`<FundraiserSection runId={run.id} isOwner={false} />` in `RunShareView.svelte`) landed in the SAME PR (#859, commit `856150274`), made by a sibling lane while this lane was recording it as untaken; neither box was ticked. Verified rather than assumed: `tests-e2e/fundraising/` runs 13/13 green, `shared-run-section.spec.ts`'s non-owner test asserts USER_B sees USER_A's campaign and reaches `/fundraisers/[id]`, and deleting the mount from `RunShareView.svelte` was measured killing that test. No code change owed.
- [x] **`docs/testing/test_inventory.md:1060` still carries the refuted `vite dev` rationale, in the paragraph headed "Why `vite dev` and not `vite preview`".** [§ 1311](../architecture/decisions.md#1311-the-reason-web-e2e-runs-on-vite-dev-is-that-eleven-serverts-endpoints-are-not-in-a-static-build--not-an-asset-base-break-which-the-artifact-refutes-twice). The config header it duplicates is fixed; this copy names `fallback: 'index.html'` (renamed to `200.html` by [§ 1268](../architecture/decisions.md#1268--is-prerendered-and-the-spa-shell-moved-to-200html--two-defects-at-once-each-of-which-hid-the-other-from-a-previous-diagnosis)), attributes the prerendered pages' relative asset base to the fallback (kit skips that computation for the fallback), and credits a CloudFront viewer-request rewrite that does not exist. The replacement wording is in the round-41 e2e lane's report; deliberately not edited here because that file is the parent's and its census is reconciled centrally. Owner: whoever reconciles `docs/testing/test_inventory.md`. **CLOSED 2026-09-06.** Paragraph replaced (it had drifted to line 1077). It now states the real reason — nine of the eleven `+server.ts` routes declare `prerender = false` and are not in a static build — and names the three refuted halves separately: the `index.html` fallback (renamed to `200.html` by § 1268), the relative asset base (computed only for a page that is not the fallback), and the CloudFront rewrite (`infra/modules/web-stack` declares exactly one `aws_cloudfront_function`, the www→apex 301). Every figure re-derived from the tree before being written. No count in that file was touched.
- [x] **`toHaveText` / `toContainText` against an SVG element neither pass nor fail on the text, and nothing in the suite stops a spec from using them that way.** [§ 1313](../architecture/decisions.md#1313-tohavetext-against-an-svg-text-is-not-a-text-assertion-and-the-matched-track-render-path-had-no-e2e-coverage-at-all). Playwright reads `innerText`, which is an `HTMLElement` property; against an SVG `<text>` it is undefined, so the matcher's verdict is independent of the element's content. Measured on the run-detail elevation chart: the same assertion passed against the wrong track's elevation band on one run and failed against the same band on the next. No other spec asserts text against an SVG element today (checked across `tests-e2e/`), so this is a trap rather than a live defect — but the suite has precedent for scanning itself for exactly this class (`fixtures/dates.test.ts` bans local-zone date getters, `fixtures/db-read.test.ts` bans an unconsulted read inside an `expect`). A third scan banning `toHaveText`/`toContainText` on a locator that resolves into an `<svg>` is not statically decidable from the selector alone, so the cheap honest version is to ban it for the handful of class names that are SVG-only in `apps/web/src` (`.extreme-text`, `.y-label`, `.x-label`) and grow the list when another chart gains a label. Owner tree: `apps/web/tests-e2e/fixtures/`. **REFUTED 2026-09-06 ([decisions § 1357](../architecture/decisions.md)).** Measured, not reasoned: against an SVG `<text>`, `'innerText' in el` is false — so the DOM premise holds — but `toHaveText`, `toContainText` and the array form all read the element's `textContent` and FAIL on a wrong expectation, and a zero-element locator fails too. Playwright falls back to `textContent` for a non-`HTMLElement`. No guard built: banning a matcher that works would fail correct code. The real cause of the § 1313 non-determinism is that `/runs/[id]` fires `fetchRunMatchedTrack` un-awaited and draws the RECORDED track first, so an auto-retrying matcher asking for the recorded band is satisfied by the first frame — reproduced surviving 1 run in 3. Fixed at the root with a settle anchor in `runs/matched-track-render.spec.ts`; the same mutation now fails 5 of 5.
- [x] **`insertRun`'s companion helpers now write three Storage objects per run and only `deleteRun` knows all three, so a spec that deletes the row directly orphans them.** Noted while adding `insertMatchedTrack`. `deleteRun` reads `track_url`, `hr_series_url` and now `run_matched_tracks.matched_track_url` before deleting, but several specs delete a run with `admin.from('runs').delete().eq('id', …)` and never call the helper — the row's cascade takes `run_matched_tracks` with it and the gzipped objects stay in the `runs` bucket for the life of the local stack. Harmless locally (a `supabase db reset` does not clear Storage either way), and this lane did not survey how many specs do it. Recorded because the fix is cheap if someone is already in the file: make the direct deletes call `deleteRun`. Owner tree: `apps/web/tests-e2e/`.

### Surfaced by the #789 round 42 web-gym-key lane (2026-09-06)

- [x] **Thirteen pgtap positives supply a value a BEFORE trigger may overwrite, and three of them make a claim the assertion cannot support.** [§ 1324](../architecture/decisions.md) drew the guard's line at UNCONDITIONAL stamps, because an assignment inside an `if` may legitimately be a branch the fixture is asserting does not fire. That leaves 13 `lives_ok` calls supplying a conditionally-stamped column, measured this round and read by nobody. Three are worth an actual read. `paid_events_test.sql:110` asserts "going on a priced event with a matching paid order **succeeds**" while `enforce_event_capacity` may have rewritten `new.status` to `waitlisted` — a `lives_ok` cannot tell a seated attendee from a silently waitlisted one, and this is the money path (`:119`, `:161`, `:223`, `notify_event_rsvp_organisers_test.sql:40` and `refund_failed_ledger_test.sql:145` are the same shape). `unbounded_numeric_column_bounds_test.sql:79` and `:164` assert an ordinary live/race ping "still stores" while `live_run_pings_drop_in_zone` / `race_pings_drop_in_zone` may have coarsened the coordinates and nulled the elevation — so the numeric-bound CHECK those assertions exist to exercise may be running against values the trigger substituted. Each may well be fine on its fixture (an event with `capacity is null` returns early; a user with no privacy zones returns early); none has been checked, and the fixture is exactly what a later edit changes without noticing. The durable repair per site is the same one § 1324 applied twice: read the stored value back through `returning` and assert it, so the assertion states what it actually proved. Owner tree: `apps/backend/supabase/tests/`. **CLOSED 2026-09-06** — [decisions § 1372](../architecture/decisions.md) + [§ 1373](../architecture/decisions.md). All thirteen read on their own fixtures: ten hold, the three named here could not have failed, and a FOURTH beside them could not either — `payment_refund_ledger_test.sql:207`, which is written as the positive control for the refund latch and which a latch widened to hold every status leaves green. Seven read-back assertions added across the four files, each mutation-verified against the real defect (`capacity = 0` kills all three seat read-backs with `have: waitlisted` while every `lives_ok` beside them stays green). The durable half is `CONDITIONALLY_STAMPED_ASSERTIONS` in `check_pgtap_refusal_assertions.mjs`: all thirteen registered, eight naming a read-back that must still exist in the same file, five carrying a reason that rests on the trigger's own control flow rather than on a fixture value — so a fourteenth site fails the PR unread.

- [x] **`ApiClient.createCustomExercise` still takes a `required String nameKey` and sends a column the server derives.** The last of [§ 1323](../architecture/decisions.md)'s rails. Left in place because dropping the parameter changes a shared package's public signature and its one caller is `apps/mobile_android/lib/widgets/exercise_catalogue_picker.dart:129`, which another lane held this round. The change is: delete the parameter and the `ExerciseRow.colNameKey` line in `packages/api_client/lib/src/api_client.dart` (~:6127), drop the `nameKey: normaliseExerciseName(name)` argument at the picker call site, mirror the picker into the iOS twin, and check whether `normaliseExerciseName` is still used in that file before removing its import. Nothing observable changes — `exercise_key_server_stamped_test.sql` assertion 10 already pins that omitting `name_key` stamps the canonical key — so the pin is the one that already exists. Owner trees: `packages/api_client/` + `apps/mobile_android/lib/widgets/` + the iOS twin. **CLOSED 2026-09-06** ([§ 1370](../architecture/decisions.md)). The parameter and the `ExerciseRow.colNameKey` line are gone, the picker call site drops the argument, and `normaliseExerciseName` stays imported there — it folds the query in three other places, which the filing asked to check. `createCustomExercise`'s own `name.trim().isEmpty` guard stays: `packages/api_client` has no access to the folded class, and the one caller now gates on `namesAnExercise` before reaching it.

- [x] **The web raw-comparison guard has no Dart sibling, so the same shape can return on the phone unseen.** `apps/web/src/lib/gym/exercise_key_source_guard.test.ts` gained a second scan this round (`rawNameComparisonHits`) that bans comparing two exercise spellings with `===`. Its Dart half, `apps/mobile_android/test/exercise_key_source_guard_test.dart`, still carries only the fold scan — and the mobile tree had exactly one instance of the comparison shape (`gym_detail_screen.dart:529`, fixed this round with no guard behind it). The port is mechanical: Dart's `==` in place of `===`/`==`, the same "either operand names an exercise spelling, neither side is a literal, neither side is wrapped in `normaliseExerciseName`" rule, and the same planted-violation battery. Not taken here because the Dart guard file is a mobile-test tree this lane touched only to mirror. Owner tree: `apps/mobile_android/test/` + the iOS twin. **CLOSED 2026-09-06** ([§ 1369](../architecture/decisions.md)), and the port was NOT mechanical in the way that mattered. It found a SECOND live instance of the shape the filing recorded as singular — `gym_compose_sheet.dart:259` grouped editor blocks on `last.name.text == name` — and the port's own first draft walked past it, because neither operand names an exercise where the comparison is written. Both halves now judge an operand on its DECLARATION as well as on itself ([§ 1368](../architecture/decisions.md)). A third scan, `blankSpellingTestHits`, ships on both halves for the § 1367 shape; on this rail it needs a pattern the web has no analogue for, since `.isEmpty` is a member read rather than a comparison.

- [x] **`routineFromWorkout`'s drop guard and `GymEditor`'s block builder disagree about a set whose canonical key is empty.** [§ 1322](../architecture/decisions.md) moved the promotion's guard onto the key, so a set named with a single U+0085 is dropped from a routine draft on both rails rather than minting an `exercise_key` of `''` the column's CHECK refuses. The DISPLAY surfaces still render such a set: `GymEditor`'s `initExercises`, `/gym/[id]`'s `blocks` and the share page all group it (now merging every blank-key spelling into one block, which is an improvement) and none drops it, while `distinctExerciseCount` counts it as nothing — so the editor can show a block the header stat says is not there. Deliberately not changed here: dropping a set from a DISPLAY of what the runner logged is a product decision, not a consistency fix, and the honest alternative is a placeholder label, which needs a key in all seven web catalogues and all seven ARBs. Reachable only through a name made entirely of U+0085 — the one code point where JS `trim()` and the shared whitespace class disagree — so it is a coherence gap rather than a live defect. Owner trees: `apps/web/src/lib/components/` + `apps/web/src/routes/gym/` + `apps/web/src/routes/share/workout/` + the i18n catalogues. **CLOSED 2026-09-06, and the honest repair was upstream of the display, not in it** ([§ 1367](../architecture/decisions.md)). The filing framed the choice as whether a DISPLAY should drop the set; the live half is that `GymEditor.buildSets` SAVES it — its drop guard was `name.trim()`, so a U+0085 name minted the `gym_sets` row with the empty `exercise_key` in the first place. Blankness is a property of the key, so it gets a name: `namesAnExercise`, the companion to `sameExerciseName`, on both rails. Two more web sites had it worse than an incoherence — `RoutineEditor.buildInput` and `ExerciseCataloguePicker.create` write columns whose key CHECK is `length(...) between 1 and 120`, so the same name was a raw 23514. No i18n key was needed: the editor's existing "add at least one exercise" error is the honest answer, and it is now reachable. The DISPLAY surfaces are untouched and no longer inconsistent, because nothing can mint the row.

### Surfaced by the #789 round 42 web-routes-types lane (2026-09-06)

- [x] **`fetchRunsForRecap` and `fetchRunsForDashboard` still declare `Run[]` over a nine- and a ten-column read, because narrowing them moves the error into four registered TS↔Dart parity pairs.** Both projections are now checked tuples (`RECAP_RUN_COLUMNS`, `DASHBOARD_RUN_COLUMNS`, `as const satisfies RunColumns`) and both carry the truthful `Pick` beside them (`RecapRun`, `DashboardRun`), so the remaining step is one line each — but it does not compile until the consumers' `Run[]` parameters take a structural bound. Measured, not guessed: narrowing `fetchRunsForRecap` produces exactly one error (`mergeRecapRuns` in the web-only `runs/recap_window.ts`, which then pushes it into `buildYearInRunningRecap` and `computeRunStreaks`); narrowing `fetchRunsForDashboard` produces twelve, all in `routes/dashboard/+page.svelte`, all `DashboardRun[]` into a `Run[]` parameter of `runs/streaks.ts`, `training/fitness.ts`, `training/race_predictor.ts` and their siblings. Each of those is a pair half (`streaks`, `recap`, `fitness`, `race_predictor`), so widening a parameter is a change to the pair and wants the `shared-library-syncer` in the loop plus a decision on whether the Dart side records an idiomatic-shape difference (Dart has no structural typing, so its `List<Run>` cannot narrow the same way — the same carve-out `password_change` records for its union-vs-class). The two widenings are stated in `data.ts` at the one line where each happens, not scattered. Owner trees: `apps/web/src/lib/runs/` + `apps/web/src/lib/training/` + `apps/web/src/lib/core/`. **CLOSED 2026-09-07** ([decisions § 1414](../architecture/decisions.md)) — the consumers took a structural bound rather than a widened `Run[]`, so the lockstep edit the filing feared was never needed.
- [x] **The web Supabase client is untyped, so every `.select()` in `data.ts` — the tuple-derived ones included — infers nothing, and the "one string literal" rule is currently inert.** `core/supabase.ts` calls `createBrowserClient(url, key)` with no `Database` generic, so `supabase.from('runs').select(anything)` yields `any[]` and none of the row types in this file are checked against the query at all; they are asserted. That is the standing item at followups.md:1609 and is not re-filed here — what IS worth recording is the two consequences this lane measured. **(1)** `CLUB_SELECT_COLS`'s comment justifies its `as const` by saying supabase-js's select parser needs a literal or "every `.from('clubs').select(CLUB_SELECT_COLS)` falls back to `GenericStringError` and downstream type assertions fail svelte-check" — that is true of a typed client and describes nothing that happens today; the `as const` is currently load-bearing for the guard that parses it, not for inference. **(2)** `data.ts:2559` builds its select as a template literal with `${CLUB_SELECT_COLS}` interpolated, which is exactly the shape that cannot infer, and `fetchRuns` / `fetchRunsForDashboard` now `join` their tuples for the same reason a typed client would reject. Whoever types the client has to convert all three back to literals; `routes/route_list_columns.ts` already carries the `Join<T, D>` template-literal type that does it from a tuple. Owner tree: `apps/web/src/lib/core/`. **CLOSED 2026-09-07** ([decisions § 1468](../architecture/decisions.md), [decisions § 1469](../architecture/decisions.md)) — and the filing was stale in its premise: the client took the `Database` generic in #873. What the typing had not bought was the CHECK, and it was hiding two things — `RunColumns` was `keyof Run`, so `track` and `has_track` were projectable and each is a 42703 that fails the whole read, and removing the last hand-written `any` surfaced a club read whose `join_policy` was never narrowed and was asserted behind a fail-open `?? 'open'` on a membership gate.
- [x] **The gym, meal-template and recipe fetchers in `data.ts` were swept but not touched, because another round-42 lane owns `lib/gym/`.** Ten narrowed selects return purpose-built summary/detail interfaces (`GymRoutineSummary`, `GymRoutineDetail`, `MealTemplateSummary`, `MealTemplateDetail`, `RecipeSummary`, `RecipeDetail`, `PublicGymRoutineEntry`, `GymSetWithDate`), each hand-written beside its own projection rather than derived from it — the [§ 641](../architecture/decisions.md) two-declaration shape, which is what `EVENT_SELECT_COLS` and the `Event` overlay were before § 1329. None was measured as divergent this round; the question is whether each interface's key set equals its select's column set, and the cheapest way to answer it for all ten at once is to extend the equality guard added in `data.test.ts` (`the enumerated club / event reads and the row types they are read as name the same columns`) to read an inline select literal plus a named interface, rather than a shared `*_SELECT_COLS` constant plus a `types.ts` overlay. Owner tree: `apps/web/src/lib/core/` (+ whoever owns `lib/gym/`). **CLOSED 2026-09-07** ([decisions § 1467](../architecture/decisions.md)) — seventeen inline reads are pinned against their row types in both directions plus column existence, with four declared exceptions that fail when they go stale. No divergence was found, which is the result the filing predicted.
- [x] **`RouteListWithheldColumn` in `routes/route_list_columns.ts` is exported and referenced by nothing.** It is `Exclude<keyof Route, ROUTE_LIST_COLUMNS[number]>` with a doc comment saying it exists "so a caller that needs one has to say so", but no caller names it and no guard reads it — so it documents an intention nothing enforces. Either give it a use (the withheld-column pins in `route_list_columns.test.ts` could be typed against it rather than spelled out one property at a time, which would make the list self-maintaining) or delete it per the house rule on unused code. Left alone this round because the § 1327 change altered what it evaluates to (`shadow_hidden` left `Route`) and a change of use is not a change of type. Owner tree: `apps/web/src/lib/routes/`. **CLOSED 2026-09-07** by verification rather than by an edit — the export was already deleted (decisions.md:27247).

### Surfaced by the #789 round 42 web-catalogue-picker lane (2026-09-06)

- [x] **Shadowing a seeded global with an owner custom is deliberate, and no surface says what happens once it exists.** **CLOSED 2026-09-08** ([decisions § 1574](../architecture/decisions.md)). Found while pricing the repair [§ 1332](../architecture/decisions.md) refused. `exercises`' two uniques are PARTIAL — `exercises_global_name_key` on `(name_key) where author_id is null`, `exercises_author_name_key` on `(author_id, name_key) where author_id is not null` (migration `20270222_001`) — and `api_database.md` states the intent outright: "so a user may shadow a global name with a custom one". The schema question is therefore settled; the CLIENT question is not. With a shadow present the picker lists two rows carrying one display name and one folded key, `normaliseExerciseName` calls them equal, and which `exercises.id` a logged set binds to is decided by whichever the user happens to tap — which is the ambiguity `gym_sets.exercise_id` exists to remove. Nothing in the tree states a precedence rule, and the obvious one (the owner's row wins, deduped at read, since a shadow is by construction a deliberate personal override) is a one-function change applied at every catalogue read on both platforms. Worth doing BEFORE any surface starts aggregating by `exercise_id` rather than by key, at which point one lift silently becomes two. Owner trees: `apps/web/src/lib/components/` + `apps/web/src/lib/core/data.ts` + `apps/mobile_android/lib/widgets/`. **PARTIALLY CLOSED by the round-43 mobile-picker-tests lane** ([decisions § 1384](../architecture/decisions.md)), and the mechanism above is REFUTED: the pick discards the id (`gym_compose_sheet.dart` sets only `ex.name.text`; `GymEditor.svelte` the same), so which `exercises.id` binds is decided by `catalogueByKey` / `_catalogueByKey`, where the LAST catalogue row under that key wins — never by which row the user tapped. Taken: the picker's exact-match scan is ordered by the list's own comparator, so the "already in the catalogue, under X" sentence names the row the reader will find and stops following the fetch's tie. Still owed, and still the durable fix: dedupe every catalogue read by folded key with the owner's row winning, plus the missing `id` tiebreak on web's `fetchExerciseCatalogue` — all three sites are outside that lane's tree. **Web half CLOSED 2026-09-07** ([decisions § 1481](../architecture/decisions.md)) — the read resolves the shadow with the owner's row winning, and the picker's snapshotted prop, a second route into the fail-open state, went with it. **Web half CLOSED 2026-09-07** — the read deduped, the picker's snapshotted prop folded through the same rule, the `(name, id)` tiebreak in place, and the pair's two registries corrected ([decisions § 1510](../architecture/decisions.md)). **The phone's composer and picker landed the same rule 2026-09-08** ([decisions § 1513](../architecture/decisions.md)). What remains is the product half the entry is actually about: no surface tells a runner that their custom is shadowing a seeded global.
- [x] **The Playwright case for the picker is still owed, and it now has two assertions rather than one.** [§ 1333](../architecture/decisions.md) moved the picker's decision into a pure module with 13 mutation-tested cases, which pins the DECISION and not the rendering: nothing proves that `view.hiddenExact` reaches the DOM as a sentence, that `data-testid=catalogue-other-category` is what carries it, or that the create button is absent beside it. The spec [§ 1278](../architecture/decisions.md) described is the first half — seed `Bench<U+00A0>Press`, type `bench press`, assert the entry lists — and the second is new: seed a chest entry, select the `legs` option in `[data-testid=catalogue-category]`, type that entry's exact name, and assert `catalogue-other-category` is present while `catalogue-create` is not. Note `tests-e2e/gym/exercise-key-fold.spec.ts` exists and its `test.fixme` was resolved by the round-41 e2e lane (§ 1312), so read it first. Owner tree: `apps/web/tests-e2e/gym/`. **CLOSED by the round-43 e2e lane**: `apps/web/tests-e2e/gym/exercise-catalogue-picker.spec.ts`, two tests. The fold case seeds an NBSP-named custom and reaches it with an ordinary space (entry side) and reaches the seeded `Bench Press` with a doubled one (query side); the hidden-exact case asserts `catalogue-other-category` naming "Chest" while `catalogue-create` is absent, plus two controls -- the same query under `all` lists the row, and a name the catalogue does not hold offers the create button under the same category filter. Mutation-verified three ways: restoring the naive `toLowerCase` fails both tests, forcing `hiddenExact` null fails the second, and narrowing the exact-match test to the visible set fails it too.
- [x] **The mobile picker has no test file at all, so both of this round's Dart fixes are unpinned.** `apps/mobile_android/lib/widgets/exercise_catalogue_picker.dart` is the only gym widget with no counterpart under `test/` — checked by name and by import across the whole suite. Its ordering comparator and its `_hiddenExact` getter are both private to the library, so even a widget test could only reach them through the rendered `ListView`; making `byName` public and pinning it directly is the cheaper half and would catch a regression in the ordering the web module's own suite cannot see. The web side of both fixes is pinned five ways over; the phone side rests on a reading. Owner tree: `apps/mobile_android/test/` (+ the iOS mirror). **CLOSED by the round-43 mobile-picker-tests lane** ([decisions § 1382](../architecture/decisions.md)): `apps/mobile_android/test/exercise_catalogue_picker_test.dart`, 20 widget cases, mirrored to the iOS twin. A widget test rather than the `byName`-made-public reading the filing proposed, and rather than the pure-module extraction the web side took — `flutter_test` already renders widgets, so the pin reaches the rendered sentence and the rendered order instead of the value behind them. Mutation-verified four ways.
- [x] **Two web modules now answer "how is a catalogue ordered" with two different instruments, and only one of them says why in a way the other could read.** `segments/catalogue_browse.ts`'s `byName` compares FOLDED names with `<` / `>` deliberately, because "collation varies with the host's ICU data, and a catalogue that reorders itself between a test runner and a browser is a bug that only shows up in CI". `components/exercise_catalogue_picker.ts`'s `byName` uses `localeCompare`, deliberately, because [§ 1276](../architecture/decisions.md) ruled that ordering a human-facing list is not keying it. Both arguments are sound and they point opposite ways; what is missing is a decision about which one this product wants for a user-facing catalogue, applied to both. Live consequence today: the web picker's order can differ between a developer's browser and CI while the segment catalogue's cannot, and — since [§ 1334](../architecture/decisions.md) — the MOBILE picker now folds while its web twin collates, so the two agree on the eight names measured and are not guaranteed to agree on a name built from `ø`, `đ`, `ł`, `ß` or `æ`. The same open question sits under the round-41 `routes_screen` filing, which named it and left it. Owner trees: `apps/web/src/lib/segments/` + `apps/web/src/lib/components/` + `apps/mobile_android/lib/widgets/`. **CLOSED by the round-43 mobile-picker-tests lane** ([decisions § 1383](../architecture/decisions.md)): the picker delegates to `compareFoldedNames`, so the product has one comparator for a name list rather than two. Measured before choosing — over the 43 seeded globals the two instruments are indistinguishable (0 discordant pairs of 903, identical under ten host locales); over those plus nine non-English customs, 118 of 1,326 pairs invert and the collation disagrees with itself across `sv-SE` / `da-DK` / `tr-TR` / `pl-PL`. The round-41 `routes_screen` filing that named the same question is answered by the same change.

### Surfaced by the #789 round 42 mobile-fold-club lane (2026-09-06)

- [x] **`dm_recipients` held a fourth private copy of the accent fold, and it had reintroduced the sigma-sensitivity the canonical fold exists to remove.** [decisions § 1340](../architecture/decisions.md). Found sweeping for the § 1337 class. `apps/web/src/lib/social/dm_recipients.ts` carried its own `fold` — NFD, strip `\p{Diacritic}`, lowercase — identical to `catalogue_browse`'s except that it omitted the final-sigma collapse, which is the one step that makes the fold decidable one code point at a time. `filterDmRecipients` is a SEARCH, so it was reachable: a display name `ΟΔΟΣ` folds to a trailing `ς` while the reader's keyboard produces the medial `σ`, and the query then matched nothing. **CLOSED 2026-09-06** — the copy is deleted and the canonical `fold` imported, which also gains the search the accent folding every other search key has; pinned by two tests, the sigma one mutation-verified. Web-only, no Dart twin.

- [x] **A privately reimplemented shared helper is invisible to every guard the tree has.** § 1340 is the second instance this round of the same shape § 852 named: `check_parity_pair_registry.mjs` compares the two REGISTRIES, not the two behaviours, so it cannot see a fifth hand-written fold appearing inside some web module — and `check_shared_constants.mjs` only compares rails it has been told about. The fold now has one definition and four consumers (`catalogue_browse`, `club_slug`, `food_search`, `dm_recipients`), which is exactly the state in which the next copy is cheapest to write and hardest to notice. The shape that would catch it is a source guard in the web unit suite: fail when any file outside `segments/catalogue_browse.ts` contains `normalize('NFD')` or `\p{Diacritic}` without importing `fold`. Cheap, and it generalises to the other single-sourced primitives (`normaliseExerciseName`, `isTruthyFlagValue`, which already carry per-platform source guards of exactly this kind — this one is missing rather than novel). Owner tree: `apps/web/src/lib/segments/` (+ wherever the web suite keeps its source guards). **CLOSED by the #789 round 43 guards-shared lane** ([§ 1397](../architecture/decisions.md)) — as `scripts/check_shared_reimplementations.mjs`, and NOT in the shape proposed here. The suggestion was to fail on the literal `normalize('NFD')` outside `catalogue_browse.ts`, which is a spelling: it fails correct code that mentions the string, passes a copy written any other way, and needs a new rule per primitive, so the register of primitives becomes the thing that goes stale. The guard is keyed instead on two things that cannot be removed without changing behaviour — the normalised function BODY (every token it runs, with comments, whitespace, its own name and the spelling of its parameters and locals erased) and the DISTINCTIVE OPERATIONS it reaches for (method calls carrying a string or regex literal). Both survive renaming the function, its parameters and its locals; only the second sees § 1340's copy, which omitted a step. Five mutations verified. It found two live instances on its first run over the real tree: § 1398's `planSlug` and § 1399's second SVG rasteriser.

- [x] **Four web surfaces still sort user-authored display names with `localeCompare`, whose answer depends on the host's ICU data.** Swept while closing § 1337. `social/search_ranking.ts:27` (`comparePeopleRank`), `core/data.ts:5582`, `routes/coaching/+page.svelte:114` and `social/dm_recipients.ts:75` each order a `display_name` through a collation. **None is a cross-platform divergence** — verified, no Dart twin exists for any of them — so this is NOT the § 1337 bug and must not be "fixed" reflexively; a collation is arguably the RIGHT order for a human reader on a web-only surface, which is why `ExerciseCataloguePicker.svelte:53` documents staying on it deliberately. The residual concern is narrower and worth stating once: `localeCompare` can order the same list differently in a test runner and a browser, so any of these that later grows an order-asserting test needs the folded comparator instead. Filed as an observation with its rule-out recorded, so a later round does not re-derive the sweep. Owner trees: `apps/web/src/lib/social/` + `apps/web/src/lib/core/` + `apps/web/src/routes/coaching/`. **CLOSED as DELIBERATELY-NOT-CHANGED by the #789 round 43 guards-shared lane** ([§ 1400](../architecture/decisions.md)). The rule-out was re-verified and holds — none of the four modules is in either parity registry, no Dart file is named for any of them, and the Dart tree's only name-sorts (`catalogue_browse.dart`, `routes_screen.dart`, `exercise_catalogue_picker.dart`) all fold. Then measured: over nine realistic names, seven locales give **five distinct orders** — `sv-SE` files `Å`/`Ä`/`Ø` after `Z`, `da-DK` orders those three differently again, `tr-TR` puts `İ` after `I`, `cs-CZ` files `Chana` after `Hana`. That variation is the FEATURE on a web-only human-facing list: each reader gets their own correct order, where a folded compare is stable precisely because it hands every reader the English one. § 1337's premise was a second platform whose runtime ships no collator, and there is no second platform here. The reason is now a comment at the three sites outside `core/data.ts`, because leaving it only here is what let two consecutive rounds re-derive the same sweep.

- [x] **The web half of § 1337 ships with no behavioural pin, for the reason § 1278 already recorded.** The mobile fix is pinned by three widget tests in `routes_screen_test.dart` (two of them mutation-verified against the old code), but `/routes` is a `.svelte` page and the web unit suite runs under `tsx --test`, which cannot compile one — so the web side of the İstanbul reversal and the `localeCompare` sort are pinned only through `compareFoldedNames`' own unit tests, which prove the comparator is right and not that the page calls it. What is owed is a Playwright case: seed routes named `İstanbul Loop` and `Åre Trail` alongside `Zaragoza Loop`, type `istanbul` into the routes search and assert the row is listed, then pick the A–Z sort and assert `Åre` precedes `Zaragoza`. Both assertions fail against the pre-§ 1337 page. This is the identical gap § 1278 filed for the catalogue picker, now with a second instance — the pattern is that every fold fix lands unpinned on web. Owner tree: `apps/web/tests-e2e/routes/`. **CLOSED by the round-43 e2e lane**: `apps/web/tests-e2e/routes/name-fold-sort.spec.ts`, two tests, both verified failing against a page reverted to `toLowerCase` + `localeCompare`. **The proposed sort assertion does not discriminate and was replaced**: a collation files `Å` beside `A`, so `localeCompare` puts `Åre` before `Zaragoza` too and that assertion passes against the very page it was meant to fail against. `Ørsted` is the pair that separates the two orderings -- `Ø` has no canonical decomposition, so the fold leaves U+00F8 and a code-unit compare files it after `z` while a collation files it beside `O`. Measured in Node ICU under the `en-GB` locale this suite pins: `localeCompare` gives Åre, İstanbul, Ørsted, Zaragoza; the folded order gives Åre, İstanbul, Zaragoza, Ørsted.

### Surfaced by the #789 round 42 mobile-stores-time lane (2026-09-06)

- [x] **REFUTED, and the box is already correct: `docs/testing/test_inventory.md`'s row for `offline_sync_store_drain_race_test.dart` is NOT three tests short.** The entry above (near followups.md:1658) says [§ 1196](../architecture/decisions.md) added a group without updating the inventory. Measured: the file runs **9** tests (`flutter test --concurrency=2 test/offline_sync_store_drain_race_test.dart`, `+9: All tests passed!`), and `docs/testing/test_inventory.md:490` already reads "9 tests". Either the drift was repaired before the entry was written or it was never there. Left unticked because the entry belongs to another lane's file and the correction is what matters, not the box. No action. **CLOSED 2026-09-07** — recomputed: 8 `test(` + 1 `testWidgets(` = 9, which is what the row says, and `check_test_inventory_counts.mjs` covers that heading and is green.
- [x] **`race_controller.dart` serialises `instance_start` with a bare `toIso8601String()` at four sites; all four are safe TODAY only because their input happens to be UTC.** [§ 1343](../architecture/decisions.md) normalised the six `api_client` sites and found `social_service.dart` had already normalised all eleven of its own. The residue is `race_controller.dart:186` / `:187` (the RSVP window bounds, built from `DateTime.now().toUtc()`), `:202` (a re-filter on a value just parsed from a server string) and `:318` (`pushPing`, whose `_hostingInstance` is traced to `ActiveRace.instanceStart`, itself parsed from `event_attendees.instance_start`). Every one is UTC by construction, so there is nothing to fix behaviourally — the filing is that the CONTRACT is now single-sourced (`instanceStartKey`) everywhere except here, and a future change to how `attachRecorder`'s `instance` is obtained would reintroduce the § 1343 defect silently. `instance_start_key_test.dart`'s source guard covers `api_client.dart` only. Owner tree: `apps/mobile_android/lib/race_controller.dart` + the iOS twin (a store lane should not cross into the run-screen files mid-round). **CLOSED 2026-09-06 ([decisions § 1378](../architecture/decisions.md)).** The safety judgement is **confirmed independently**: `add`/`subtract` preserve `isUtc`, and Dart's `DateTime.parse` answers an offset-bearing string — which is every `timestamptz` PostgREST returns — as UTC (measured across `+00:00`, `Z` and `+02:00`; only a zone-less literal gives a local value), and `_hostingInstance` traces to that same parsed value through `attachRecorder`'s single caller. Both halves were taken rather than one: all twenty-three sites across `race_controller.dart` and `social_service.dart` now call `instanceStartKey`, so the phone states the contract instead of re-deriving it, AND `instance_start_wire_guard_test.dart` refuses a new bare site anywhere under `apps/mobile_android/lib`, floors the helper's call count so the rule cannot pass vacuously, and pins that the helper is `api_client`'s rather than a local copy of the name — because an edit that restores an already-correct state is worth nothing without the guard that keeps it. **§ 1343's own count is superseded**: it says `social_service.dart` writes `instance_start` at eleven sites; measured now, eighteen.
- [x] **The strict-parse hardening of [§ 1344](../architecture/decisions.md) stops at the `OfflineSyncStore` family; `local_run_store.dart` and `social_service.dart` still take `DateTime.tryParse`'s rollover.** `local_run_store.dart:704` (`_lastModifiedOf`, reading `metadata.last_modified_at`) and `social_service.dart:1355` / `:1381` / `:1382` (`event_exceptions.instance_start`) each parse a stored date-time with the bare platform call, so an impossible value there is still answered rather than refused. Both are compare-or-store callers, which is the case § 1344 judged survivable on the timestamp side, so this is consistency work rather than a defect — but `parseIsoStrict` is a top-level function in `offline_sync_store.dart` that either file could import, and the guard that keeps the family honest derives its membership from `extends OfflineSyncStore<`, so neither is covered. The durable shape is probably to move `parseIsoStrict` out of `offline_sync_store.dart` into a small shared module both families import, then widen the guard's root. Not attempted here: `local_run_store.dart` is the largest file in `lib/` and belonged to another lane this round. Owner tree: `apps/mobile_android/lib/local_run_store.dart` + `social_service.dart` + the iOS twins. **CLOSED 2026-09-06 ([decisions § 1377](../architecture/decisions.md)).** The rollover reproduces exactly as filed and the proposed shape was taken, one layer lower than proposed: `parseIsoStrict` is in `packages/core_models/lib/src/iso_parse.dart` rather than in a mobile module, because `api_client` and the row DTOs in that package need it too and a module under `lib/` is unreachable from both. The entry's "consistency work rather than a defect" framing is **too generous to two of the sites**: `_lastModifiedOf` is the clock deciding which copy of a run survives a merge, so a rolled-over stamp far enough ahead wins every later `isAfter` and the server copy can never reach the phone again (the § 1342 shape); and `race_controller`'s `PendingRaceResult` keys a finisher time on `instance_start`, so a rolled-over occurrence replays the runner's official result against a different race night. Also more sites than filed: `social_service.dart` carried twelve raw parses, not three — the other nine are `DateTime.parse`, which is the same rollover with a throw bolted on. The guard's root is widened as proposed, to the family plus every file that USES the reader, with a call-site floor so a file cannot leave the covered set quietly. Frontier that remains is filed below.
- [x] **`recurrence.dart` / `recurrence.ts` compute the same occurrence and neither registry records that the pair's LOCAL-instant branch is only safe because of what happens downstream.** [§ 1343](../architecture/decisions.md) settled that the pair must not move — a legacy zone-less event's occurrence is a local `DateTime` on Dart and a local-constructed `Date` on JS, and those name the same absolute instant — but that equivalence holds only while every consumer serialises through something that normalises. Dart's is now `instanceStartKey`; JS gets it for free from `toISOString()`. Nothing enforces the Dart half beyond `api_client`'s own source guard, and nothing in either registry says the pair has this precondition. A note now sits in `recurrence.dart`'s doc comment; the durable version is a `shared-library-syncer` row that names the precondition, which is a registry edit and therefore a cross-lane change. Owner tree: `.claude/agents/shared-library-syncer.md` + `CLAUDE.md`'s parity-pair list. **CLOSED 2026-09-06 ([decisions § 1379](../architecture/decisions.md)).** Both registries carry it, in the same change — `check_parity_pair_registry.mjs` fails the PR otherwise, and the mutation was run. The entry is written to lead with the sentence that matters (moving either half to UTC is the one edit the pair must not take, because it re-keys every RSVP already placed) rather than with the mechanism. **Not two-sided**: `recurrence.ts` needs no change and gets none, because the precondition is a fact about the DART consumers — web's `Date` is an instant and `toISOString()` always emits `Z`. The Dart header now names the guard (§ 1378) instead of restating the rule.

### Surfaced by the #789 round 42 watch lane (2026-09-06)

- [x] **A transient sync failure is invisible on `PreRunScreen`, which is the screen a runner is on for every drain but the first.** `syncError` renders on `PostRunScreen` only. The chip slot on the PreRun top arc now has three branches — unreadable queue, permanently-rejected entries ([§ 1347](../architecture/decisions.md)), counted `Sync N` — and none of them can say *why* a count is not falling when the cause is a 5xx or a dead network. The runner taps Sync, the chip spins, the count stays, and nothing on that screen names the reason; `drainBackoff` is meanwhile armed, so an auto-trigger inside the window returns without even attempting. Not taken in-round, and the reason is the same one § 1104 recorded for its own case: what a 1.4-inch arc should say when the failure is transient is a design decision, not a wiring one, and the three candidates are not equivalent — a fourth chip branch competes for the slot the counted chip needs (Sync is still the useful affordance during a transient), a caption line under the chip pushes the arc toward the Start button, and a colour change alone is refused by this module's own `ScreenWiringTest`. The `offline` caption already covers the commonest sub-case honestly, which is why this is a gap and not a defect. Owner tree: `apps/watch_wear/`. **CLOSED in round 43**: the counted `Sync N` chip itself now says it — same slot, same `onSync`, a `Retry N` label plus the warning colour plus a content description carrying the sentence, which is the three-signal shape § 1104 settled for the unreadable chip. The verdict rides a new `RunUiState.syncFailed` set from `result.anyTransientFailure` rather than off `syncError`, because `startNextRun` clears the banner on exactly the transition that puts the runner in front of it. Gated on `online`, so the disabled offline chip is never labelled Retry. Host-tested only — no Wear OS device or emulator ran it; `PreRunSyncFailureTest` (5 source-level cases, 3 mutations verified killing it) is the module's stated convention for Compose wiring. See [§ 1390](../architecture/decisions.md).
- [x] **`RunViewModel.discard()` removes the queue entry but leaves the track file, and now it is the only drop path that does.** The drain and the new rejected-queue discard both go through `dropQueuedRun` ([§ 1347](../architecture/decisions.md)), which removes the entry and then deletes the file it points at. `discard()` — the PostRun `×`, behind § 1253's confirm — is `store.remove(id)` and nothing else, so a discarded run's track (multiple megabytes on an ultra) survives until `sweepOrphanTracks` gets it, which runs from `reconcileTrackStorage`: once per process, latched on `trackStorageReconciled`, and skipped entirely while a recording is live. So the file can outlive the run by a whole session. Storage-only, no correctness consequence, and the fix is now two lines — read the snapshot, call `dropQueuedRun`. Not taken in-round because `discard()` sits on the PostRun path § 1253 had just rewritten and its `thisRunId` may name a run the queue never held (an already-synced one), so the snapshot read wants a decision about the not-in-queue case rather than a mechanical edit. Owner tree: `apps/watch_wear/`. **CLOSED in round 43**: `discard()` is now `dropQueuedRun(id, store.queue.first())`. The not-in-queue case needed no decision after all — the snapshot lookup finds nothing for an already-synced run, only the no-op removal runs, and there is no file left to delete. `UnsyncedRunDurabilityWiringTest` now pins that `store.remove(` has exactly one call site in `RunViewModel.kt` and that it is `dropQueuedRun`'s, so a fourth drop path fails without being listed. See [§ 1388](../architecture/decisions.md).
- [x] **`docs/backend/metadata.md`'s `hr_coverage` row says the watchOS accumulator "has never executed on a simulator or a device", which is now too strong.** Measured this round: `HealthKitFailureTests.swift` is a member of the `WatchAppTests` target, `test-watch-ios` runs `xcodebuild test` on a booted watchOS simulator for any diff outside `docs/` and `.claude/`, and the job is one the required `CI gate` needs — so the nine `HeartRateCoverageAccumulator` cases have run since #859 landed on `main`. The sentence's *point* survives and must not be deleted: no real `HKWorkoutSession` has ever driven the accumulator, so the figure is still not run-verified and the "**Not run-verified on watchOS**" heading stays. What needs correcting is the clause about the accumulator never having executed. Not taken here because `metadata.md` is a shared doc no lane owned this round. One sentence. **CLOSED at round-42 integration**, in the same round that filed it: the row now reads "Not run-verified against a live workout on watchOS" and states that the `test-watch-ios` macOS job runs the unit cases on a booted simulator. A real `HKWorkoutSession` still has not driven it.
- [x] **`discard_stake` is singular-agreeing in French and Portuguese, and the rejected-queue discard can arm over N runs.** "Enregistrée nulle part ailleurs" / "Não guardada em mais nenhum lado" agree with one feminine `course` / `corrida`; the new armed chip ([§ 1347](../architecture/decisions.md)) renders the same caption when the count is two or more. The key was written for the single-run discards it already backs (crash recovery, PostRun) where the agreement is correct, and the new caller reuses it rather than introducing a fifth string set across seven catalogues. English, German and Japanese are unaffected — the German predicate adjective is invariable and Japanese has no agreement. Either make it a `<plurals>` in all seven catalogues, or give the multi-run arm its own key. Cosmetic, and only visible to a fr/pt runner with two or more permanently-refused runs. Owner tree: `apps/watch_wear/`. **CLOSED in round 43**: it is a `<plurals>` in all seven catalogues, and the three call sites pass their own count. **Spanish was affected too and this entry missed it** — "No se guarda en ningún otro sitio" is a *pasiva refleja* whose verb agrees with the elided subject. The plural wordings were lifted verbatim from `cd_sync_rejected_confirm`, which already carried them. The conversion also exposed that `L10nResourceParityTest` never compared a key's resource KIND across locales, which is a `Resources.NotFoundException` on one locale only; a fifth test does now. See [§ 1389](../architecture/decisions.md).
- [x] **`check_watch_ios_source.mjs` decides each claim's `ok` line off the GLOBAL error count, so one failing claim silently withholds another's success line.** Claim (10) ends `if (errors.length === 0) { ok.push(...) }` — that array holds every claim's errors, not its own, so a failure anywhere above it removes claim (10) from the `ok:` list even when claim (10) itself holds. The exit code and the error list are unaffected, so this is a reporting defect rather than a coverage one: a reader triaging a red run sees a shorter `ok` list than the truth and can conclude a claim failed silently. The durable shape is for each claim to count its own pushes (`const before = errors.length` … `if (errors.length === before)`), which is what claims (4), (12) and (13) already do with their own counters. Three or four call sites. Not taken in-round: the lane had already made four substantive edits to this file and a reporting-only refactor across claims it did not otherwise touch is a separate, reviewable change. Owner tree: `scripts/`. **CLOSED in round 43**: claims (8), (9) and (10) each open a `const before = errors.length` window. Measured before: breaking claim (5) alone gave 1 error and 19 `ok:` lines against 23 clean, three of the four losses being claims that held. After: the same break gives 1 error and 22 `ok:` lines. The regression test is anchored on the output — it breaks the FIRST claim and asserts only that claim's lines are lost — so a later claim reading the global count fails it unregistered. See [§ 1387](../architecture/decisions.md).

### Surfaced by the #789 round 42 infra-ci-guards lane (2026-09-06)

- [x] **Nothing in CI compiles `apps/mobile_android/android`, so an AGP / Gradle / plugin break in the Flutter Android host is invisible until a release tag.** Found while costing the CodeQL Kotlin coverage fix ([decisions § 1352](../architecture/decisions.md)). `ci.yml` has `build-watch-wear` running `./gradlew assembleDebug testDebugUnitTest` for `apps/watch_wear/android`, and nothing at all for the Flutter host — `release-android.yml`'s `flutter build appbundle` is the first thing that ever configures it. The three JUnit tests under `android/app/src/test/kotlin` (`HealthRoutePermissionBridgeTest`, `WearAuthBridgeArgsTest`, `WearRoutesBridgeArgsTest`) therefore run in no job either: they were written, they are committed, and they have never been executed by CI. The root `build.gradle.kts` already carries two `afterEvaluate` workarounds for third-party plugins pinning old `compileSdk` / `sourceCompatibility` values, which is precisely the class of breakage a Dependabot bump introduces silently and a release discovers. This is the same job the CodeQL gap wants (Flutter SDK + `melos bootstrap` + a Gradle invocation), so the two should be costed together: one job could compile the host, run its unit tests, AND give the java-kotlin extractor its source. Owner tree: `.github/workflows/`. **CLOSED 2026-09-07** ([decisions § 1393](../architecture/decisions.md) refuted the compile half — `build-mobile-android` configures and assembles the whole host — and [§ 1439](../architecture/decisions.md) closed the test half).
- [x] **The `weigh_in_flag` pair's own suites cannot see the SQL half of the Art 9 gate, and nothing else does either.** Written while adding the Dart mirror ([decisions § 1355](../architecture/decisions.md)). Both halves of the client gate are now guarded: the surface is unreachable while the flag is off, and the consent term has exactly one writer. But the module's header calls the client "the client half of a defence-in-depth pair, not the sole guard" — the other half being that a crossing's health columns persist only when the checkpoint `requires_weigh_in` AND the RPC caller passes consent — and no test in either suite touches it. A pgtap case in `apps/backend/supabase/tests/` asserting that the upsert RPC drops `body_weight_kg` / the medical-hold flag when `p_health_consent` is false, and when the checkpoint does not require a weigh-in, is what would make the "defence in depth" claim measured rather than asserted. Not taken here: this lane owned no backend path and the pgtap stack is under a lock shared by two other lanes. Owner tree: `apps/backend/supabase/tests/`. **CLOSED 2026-09-06, with its premise refuted** — [decisions § 1374](../architecture/decisions.md). "Nothing else does either" is wrong: `checkpoint_crossings_test.sql` has carried all three fail-closed cases for `body_weight_kg` since the migration landed. What was genuinely unsaid is narrower and worse. `medical_hold` is `not null default false`, so a closed gate has to ACTIVELY discard a supplied `true` rather than merely decline to write it, and nothing measured that it does; `body_weight_pct` and `medical_note` were never read at all; and the whole MERGE branch — the `else cc.<col>` arms deciding what a SECOND volunteer's write does to an existing crossing — had never run under the gate, in either of its two opposite failure directions. `weigh_in_health_gate_test.sql` is 11 assertions over both branches and all four columns, mutation-verified seven ways against the RPC, with every merge claim preceded by one proving the merge actually merged.
- [x] **`check_codeql_coverage.mjs` reads the two compiled legs and is silent about the two interpreted ones, which have their own way of going quiet.** Closed 2026-09-06 ([decisions § 1392](../architecture/decisions.md#1392-build-mode-none-narrows-nothing-which-is-not-the-same-as-nothing-narrowing-it--the-two-interpreted-codeql-legs-are-read-too)) in the durable form the filing named. `INTERPRETED` reads all three levers an `init` step has — `paths` / `paths-ignore` and `query-filters` inside `config`, and `queries:` itself, because a suite below `security-and-quality` drops whole query families. Each declared narrowing costs a reason of at least the same `MIN_REASON_CHARS` a compiled leg's exclusion costs, and must still name something real: a path glob matching nothing in the tree has outlived what it excused, and a `query-filters` id whose language family the leg cannot emit filters nothing while reading as a deliberate exclusion. `build-mode: none` is asserted rather than assumed (a leg whose scope becomes build-decided belongs in `SURFACES`), and a `config-file:` is refused rather than followed. The one existing narrowing keeps its reason, moved inside the `config` block scalar where it is a comment in the document CodeQL parses and where the guard reads it; both YAML parses were measured unchanged by the move. Nine mutations run against the shipped `security.yml`, eight failing as intended. 13 tests added to `scripts/check_codeql_coverage.test.mjs` (13 → 26).
- [x] **~~Nothing in CI compiles `apps/mobile_android/android`~~ — REFUTED; what IS true is that its 32 `@Test` methods run in no job, and that is now a red check rather than a note.** Closed 2026-09-06 ([decisions § 1393](../architecture/decisions.md#1393-a-gradle-projects-committed-unit-tests-must-be-run-by-some-job--and-appsmobile_androidandroid-is-compiled-by-ci-which-is-not-what-the-filing-said)). The headline claim does not survive measurement: `ci.yml`'s **`build-mobile-android`** job runs `flutter build apk --release --no-tree-shake-icons --target-platform android-arm64` on every PR and every push to `main`, gated only on the `code` filter and named in the `CI gate` aggregator's `needs:` list at `ci.yml:3555`. That configures and assembles the entire Gradle host — AGP, KGP, the root `build.gradle.kts` `afterEvaluate` `compileSdk` override, every plugin subproject `melos bootstrap` supplies — which is exactly the Dependabot-bump breakage class the filing was written about, and the job's own comment says so. `release-android.yml` is not the first thing that configures it. The residual half is confirmed and was the real finding: `assembleRelease` has no dependency on a `test*UnitTest` task, so `HealthRoutePermissionBridgeTest`, `WearAuthBridgeArgsTest` and `WearRoutesBridgeArgsTest` (3 files, **32 `@Test` methods**, not three tests) are executed by nothing. A third blocker the filing did not name: this project ships **no `gradlew` wrapper** — `gradle/wrapper/gradle-wrapper.properties` is committed, `gradlew` and `gradle-wrapper.jar` are not — so even inside the job that already has the SDK and the bootstrapped plugins there is no wrapper to invoke. `scripts/check_gradle_test_coverage.mjs` now walks the tree for Gradle projects holding a `src/test` source set and reads every workflow for the invocation that runs them, matching on the task list handed to Gradle; the gap is declared in `GRADLE_UNTESTED` on `build-watch-wear`, echoed as a `::warning::` on every run, and re-measured on every PR. The generalisation is what mattered: `apps/watch_wear/android`'s **767 `@Test` methods across 76 files** hung on one unguarded task word. Running the Flutter host's own tests is re-filed below with its cost.
- [ ] **Two of this lane's five assigned items were already fixed on the commit the lane was branched from, by a sibling lane in the same PR.** The fundraiser mount (`RunShareView.svelte`) and the matched-track fixture (`insertMatchedTrack` + `runs/matched-track-render.spec.ts`) were both filed as untaken and both landed in #859 / `856150274` — the filing lane could not take them (`apps/web/src/` was someone else's tree) and the lane that did take them did not tick the boxes, because a lane sees only its own worktree. That is structural, not carelessness: it will recur every round in which one lane files what another fixes. The cheap fix is at integration — after merging the branches, re-read every `- [ ]` the round's own PR body claims to have closed and tick the ones the merged tree already satisfies. Owner: whoever integrates a round's branches.
- [x] **`docs/testing/test_inventory.md` states `@playwright/test` 1.59.1; the pinned dependency is `^1.62.1`.** In the `apps/web/tests-e2e/**/*.spec.ts` section's **Stack:** line. `apps/web/package.json` carries `"@playwright/test": "^1.62.1"` and `npx playwright --version` reports 1.62.1. Not touched by this lane — the surrounding paragraph is the parent's, and this lane's prose exemption was scoped to the `vite dev` paragraph. It is prose rather than a count, so it can be corrected in the same pass that reconciles the counts. Owner: whoever reconciles `docs/testing/test_inventory.md`. **CLOSED at round-42 integration**, in the same round that filed it: the line now reads 1.62.1, matching the `^1.62.1` pin and the binary.
- [x] **`simulate.insertRun`'s track upload failed once in fifteen runs of `runs/matched-track-render.spec.ts` against the shared local stack, and the fixture reports it as an unattributed error.** One occurrence at `fixtures/simulate.ts:99` (`simulate.insertRun track upload failed: …`), zero in the ten repeats immediately after, on a box where a second lane was hitting the same Supabase Storage container. The throw carries `upErr.message` but nothing that separates "Storage refused this object" from "Storage was busy", so a CI flake here would read as a spec defect. Worth an explicit retry or a message that names the bucket, path and status. Not chased this round: one occurrence, and it is upstream of anything the spec asserts. Owner tree: `apps/web/tests-e2e/fixtures/`. **CLOSED by the round-43 e2e lane, as a MESSAGE and deliberately not a retry** (decisions § 1403). Verdict: **shared-stack contention, not a fixture race.** 1,800 uploads through the identical path -- 300 sequential and 1,500 at concurrency 12 -- failed zero times, and no race is available anyway since the path embeds a freshly minted `runs.id` under `upsert: true`. `docker restart` of `supabase_storage_project-running` during a slow upload loop reproduces it exactly: a contiguous burst of eight `502 An invalid response was received from the upstream server` over ~5.5 s, then nothing. The container timestamps show the shared stack really is restarted mid-round by a sibling worktree. `storageFailure` now names the bucket, path and status, and `fixtures/storage-errors.test.ts` follows the error VARIABLE each `.upload(` binds so a hand-rolled message at a fourth upload fails it.
- [x] **The elevation chart is the only projection of `baseTrack` with numbers in the DOM, so nothing e2e covers the matched line reaching `RunMap` or the direction scrubber.** `runs/matched-track-render.spec.ts` proves the preference through the chart because the map is a MapLibre canvas. `RunMap`'s pace heatmap gates on `hasTrackTimestamps(track)` over the same preferred track and the scrubber interpolates along it, and both were part of what [§ 1170](../architecture/decisions.md) fixed on the worker side; neither is asserted. A `page.evaluate` against the map instance's own source data, or a scrubber-marker position read against the matched polyline (the shape `runs/scrubber.spec.ts` already uses), would cover them. Owner tree: `apps/web/tests-e2e/runs/`.

### Surfaced by the #789 round 43 web-typed-client lane (2026-09-06)

- [x] **Four named record shapes are refused by `Json` on a declaration technicality, and the one-token fix is in four parity modules this lane could not touch.** TypeScript gives an object type an implicit index signature only when it is a type **alias** — an `interface` stays open to declaration merging and therefore never gets one — so `PrivacyZone` and `LatLng` (`routes/privacy.ts`, `routes/route_simplify.ts`), `WorkoutStructure` (`training/training.ts`) and `EventGymTemplate` (`social/event_gym_template.ts`) cannot be assigned to a jsonb column however JSON-shaped they are ([decisions § 1363](../architecture/decisions.md)). Each is declared `export interface X {`; changing it to `export type X = {` is behaviour-neutral, has no Dart-side analogue at all (Dart has no such distinction), and would delete all four restatements this lane wrote instead — the `TrackPoint[]` annotation in `saveRunAsRoute`, the `{ ...gym_template }` spread in `createEvent` and `updateEvent`, the `{ ...wo.structure }` spread in `createTrainingPlan`, and the three-field projection in `settings/preferences/+page.svelte`'s `persistZones`. Not taken here: all four are TS↔Dart parity modules, and this lane's scope excluded them. Owner tree: `apps/web/src/lib/routes/`, `apps/web/src/lib/training/`, `apps/web/src/lib/social/`. **`routes/` share CLOSED 2026-09-07** ([decisions § 1474](../architecture/decisions.md)) — `PrivacyZone` and `LatLng` are type aliases now, because only an alias carries an implicit index signature. The `training/` and `social/` halves, and the four restatements the aliases make deletable, are untouched. **CLOSED 2026-09-07** for `training/` and `social/` ([decisions § 1522](../architecture/decisions.md)), which is the whole of what the `routes/` closure left owed. Four of the five restatements the filing named are deleted; `persistZones`' three-field projection is deliberately KEPT and its comment corrected — the index signature was only half its reason and the § 33 privacy contract is the half that still holds.
- [x] **`gym_session_draft.ts`'s two return types are `Record<string, unknown>` for a bag it has already proved is JSON, and two call sites carry a cast because of it.** `draftMetadata` builds a literal whose every value is JSON, and `stripSessionDraft` runs the module's own `isJsonObject` guard over its input before copying it — yet both declare `Record<string, unknown>`, which is not assignable to the jsonb `gym_workouts.metadata` the results go back into. `GymSessionRunner.svelte` and `routes/gym/+page.svelte` each carry an `as JsonObject` naming this as the fix. Retyping the two returns to `JsonObject` deletes both. Not taken: `apps/web/src/lib/gym/**` was another lane's tree this round. Owner tree: `apps/web/src/lib/gym/`. **CLOSED 2026-09-07** ([decisions § 1480](../architecture/decisions.md)) — both builders return the bag they had already proved is JSON, and the two call-site casts are gone.
- [x] **A failed exercise-catalogue read degrades to an empty catalogue on both platforms, and an empty catalogue makes every name look free.** **CLOSED 2026-09-08** ([decisions § 1570](../architecture/decisions.md)). `fetchExerciseCatalogue` returns `[]` on error (web `core/data.ts`, mobile `api_client.dart`), so a transient failure is indistinguishable from "you have no catalogue": `GymEditor`'s `catalogueByKey` is empty, every typed name binds to no `exercises.id` and logs as free text, the browse affordance hides itself (`catalogue.length > 0`), and the create-custom path is offered for a name that already exists — where the insert then succeeds or 23505s against a partial unique the client cannot see. This is the same "unavailable is not empty" collapse `fetchRunsForDashboard`, `browseClubsWithError` and `fetchGlobalSegmentsWithError` each already have a `WithError` sibling for. The durable form is that sibling plus a retry affordance on both surfaces, and it is a cross-platform change rather than a one-liner. Filed by the round-43 mobile-picker-tests lane, verified by this one. Owner trees: `apps/web/src/lib/core/`, `packages/api_client/`. **Web half CLOSED 2026-09-07** ([decisions § 1419](../architecture/decisions.md)): the web read returns `{ catalogue, error }` and the picker's create affordance fails closed while the catalogue is unknown. **The phone still degrades to `[]`** — `ApiClient.fetchExerciseCatalogue` is unchanged, so the mobile picker still offers to create a name the catalogue may already hold. **Re-attributed 2026-09-07**: `ApiClient.fetchExerciseCatalogue` never returned `[]` on error — `readAllPages` propagates and there is no catch — so the phone half is a SURFACE change in `gym_screen.dart`, not a data-layer one. The API layer states that contract now and a guard refuses any `try`/`catch` in the method ([decisions § 1462](../architecture/decisions.md)). **Web half CLOSED 2026-09-07** — the read returns `{ catalogue, error }` and the picker's create fails closed while the catalogue is unknown. **Mobile half CLOSED 2026-09-08** ([decisions § 1514](../architecture/decisions.md)): `gym_screen.dart` carries `catalogueUnavailable`, true until a read answers, through the composer to the picker, with the browse affordance deliberately kept reachable. Kept open only until a later round confirms the two surfaces state the third state identically; no code work is known to remain.
- [x] **The web exercise-catalogue read is unpaged where the mobile one pages, so a catalogue past the PostgREST row ceiling silently truncates.** `api_client.dart`'s `fetchExerciseCatalogue` wraps its query in `readAllPages`; the web one issues a single unranged `.select('*')`, which PostgREST answers capped at `db-max-rows` (1000) with no flag. The seeded catalogue is well under that today and a user's customs are few, so nothing is wrong yet — but the two clients disagree about a bound, and the web side is the one that gives no signal when it hits it. Noticed while adding the `id` tiebreak the two now share ([decisions § 1362](../architecture/decisions.md)). Owner tree: `apps/web/src/lib/core/`. **CLOSED 2026-09-07** ([decisions § 1419](../architecture/decisions.md)) — the web read walks explicit ranges on the rule `paged_read.dart` states, that a short page is not proof of exhaustion. Verified again 2026-09-07: `fetchExerciseCatalogue` walks explicit `.range()` pages, so the annotation was already correct and only the checkbox was missed.
- [x] **`fetchRunsForDashboard` and `fetchRunsForRecap` still declare `Run[]` over a ten- and nine-column read.** Each ends `as unknown as Run[]`, stated at the line where it happens with `DashboardRun` / the recap tuple exported beside it. Typing the client did not create this and does not close it: it is [§ 1330](../architecture/decisions.md)'s open item, and narrowing the declarations to the projection types needs the `Run[]` parameters of several registered TS↔Dart parity helpers (`streaks`, `recap`, `race_predictor`, `training_load`) to take a structural bound first, which is a change across both platforms. Owner trees: `apps/web/src/lib/`, `apps/mobile_android/lib/`. **CLOSED 2026-09-07** ([decisions § 1414](../architecture/decisions.md)) — the same finding as the entry above, filed twice; both fetchers now return their `Pick<>`, and the dashboard read gained the three client narrows the cast was hiding.
- [x] **`RestoreBackend`'s three upserts cast an archived row to the table's `Insert` shape, and nothing validates the archive before the server does.** The rows come out of a ZIP the user supplied, so their column set is whatever build wrote the archive — not something the current schema can check, and the casts in `backup.ts` say so. Today a stale or hand-edited archive reaches PostgREST and comes back as a 400 that the restore loop records in `result.warnings`, which is the honest outcome but a late one: the run is uploaded to Storage before the row is attempted. A reader-side validation that drops unknown keys against the generated `Insert` shape would move the refusal before the upload and turn "1 warning per row" into one message about the archive. Owner tree: `apps/web/src/lib/backup/`. **CLOSED 2026-09-07** ([decisions § 1521](../architecture/decisions.md)). The filing's premise had a concrete defect behind it: `runs.kind` was dropped by `20261206_001`, so any archive written before that migration failed EVERY run it carried — PGRST204 is whole-row — each failure landing after that run's track blob had already been uploaded. Rows are now filtered against the generated `Insert` keys first, one warning per section instead of one per row.

### Surfaced by the #789 round 43 web-gym-residual lane (2026-09-06)

- [x] **`createGymRoutine` and `createGymWorkout` in `data.ts` trim an exercise name they do not blank-test.** `apps/web/src/lib/core/data.ts:10047` sends `exercise_name: ex.exercise_name.trim()` into `gym_routine_exercises` and `:9684` the same into `gym_sets`, with no guard of their own — every caller is now gated on `namesAnExercise` ([§ 1367](../architecture/decisions.md)), which is why nothing reaches them blank today, but the data layer states no such requirement and a third caller would learn it from a 23514. The durable shape is the one the layer already uses for `exercise_key`: drop or refuse at the boundary rather than trusting each composer. Not taken here because `data.ts` was exclusively another lane's tree this round. Owner tree: `apps/web/src/lib/core/`. **CLOSED 2026-09-07** ([decisions § 1466](../architecture/decisions.md)) — all three writes drop through `namesAnExercise`, deciding blankness on the KEY rather than the spelling, and `replaceGymSets` filters before stamping `set_index` so the numbering gap goes with it.
- [x] **`ApiClient.createCustomExercise` blank-tests with `name.trim()`, and `packages/api_client` cannot reach the folded class.** `packages/api_client/lib/src/api_client.dart:6112`. Harmless on this rail — Dart's `trim()` strips exactly `kExerciseWhitespace` — and unreachable from the one caller, which now gates on `namesAnExercise` first. It is nonetheless the rule stated by a runtime coincidence rather than by the class ([§ 1367](../architecture/decisions.md)), in a package that also holds `profile_query`, `strava_sync_result` and `import_completeness`'s Dart halves. The durable fix is to move `normaliseExerciseName` + `namesAnExercise` + the frozen fold table into `core_models`, as those three pairs were moved, and have `apps/mobile_android/lib/gym_prs.dart` re-export them — which relocates a registered parity pair and its generated table, so it is a change of its own rather than a line. Owner trees: `packages/core_models/` + `packages/api_client/` + `apps/mobile_android/lib/` + both registries. **Narrowed to nothing behavioural 2026-09-07** ([decisions § 1464](../architecture/decisions.md)): the case fold never deletes a code point, so a name is empty after folding exactly when every code point is in `kExerciseWhitespace` — which is Dart's `trim()` verbatim, now measured in both directions over every assignable code point. The relocation is still owed but buys correctness nowhere. **CLOSED 2026-09-07** ([decisions § 1515](../architecture/decisions.md)). `normaliseExerciseName` / `namesAnExercise` / `kExerciseWhitespace` / the frozen fold table moved into `core_models`; `gym_prs.dart` re-exports them, both registries name the new paths, and the call site states the rule. Extending the source scan over the two packages found a second site of the same coincidence in `upsertGymRoutine`, fixed in the same change.
- [x] **`gym_sets.exercise_key` is the only one of the three key columns with no `length(...) >= 1` half, and that asymmetry is what made the incoherence possible.** `gym_routine_exercises.exercise_key` and `exercises.name_key` both carry `length(...) between 1 and 120`; `gym_sets.exercise_key` carries only `exercise_key = normalise_exercise_name(exercise_name)`, and [20270706000002](../../apps/backend/supabase/migrations/20270706000002_gym_sets_exercise_key_reads.sql) records the omission deliberately — "a name that is only whitespace passes `gym_sets_exercise_name_check` and folds to `''`". Every web and mobile writer is now gated ([§ 1367](../architecture/decisions.md)), so nothing MINTS such a row, but the database still accepts one from any other client and every read-side aggregate then disagrees with the row's own existence. Closing it means a `NOT VALID` CHECK plus a `VALIDATE` and a decision about pre-existing rows (a count first — it may be zero). Owner tree: `apps/backend/supabase/migrations/` + the pgtap suite. **CLOSED 2026-09-07** ([decisions § 1424](../architecture/decisions.md)) — `gym_sets_exercise_key_nonempty_chk`, added `NOT VALID` and validated in a second migration per the online-DDL playbook, with a counting pre-flight that names the offending rows instead of letting `VALIDATE` answer "violated by some row".
- [x] **`ExerciseCataloguePicker`'s `create()` is fixed but not pinned by the scan.** **CLOSED 2026-09-08** ([decisions § 1573](../architecture/decisions.md)). `blankSpellingTestHits` chases a bare identifier through its `const` declaration and stops at the first expression that is not one, so the picker's `const trimmed = $derived(query.trim())` — a value that arrives as a PROP — is out of its reach on both rails ([§ 1368](../architecture/decisions.md)). The shape is asserted as a known-spared case in the guard's own battery so the limit is stated rather than rediscovered, but a regression at that call site would not fail CI. Closing it means either resolving `$derived`/`$props` initialisers (web) and widget fields (Dart), or a Playwright/widget test on the create affordance. Owner trees: `apps/web/src/lib/gym/` + `apps/mobile_android/test/`. **Web half CLOSED 2026-09-07** ([decisions § 1483](../architecture/decisions.md)) — the scan reads the subject identifier rather than only a member read, and the `$derived` resolution the filing proposed would not have changed that. The `apps/mobile_android/test/` half is untouched. **Web half CLOSED 2026-09-07** — the from-disk mutation case in `blankSpellingTestHits`' battery fails on a planted regression at the real call site. The `apps/mobile_android/test/` half is untouched; that scan now reaches `packages/core_models` and `packages/api_client` too ([decisions § 1515](../architecture/decisions.md)), so the remaining ask is the mobile picker's own pin.

### Surfaced by the #789 round 43 mobile-picker-tests lane (2026-09-06)

- [x] **`fetchExerciseCatalogue` orders by `name` on web and by `(name, id)` on the phone, so a shadowed exercise binds to a different row on each.** `apps/web/src/lib/core/data.ts`'s read is `.order('name', { ascending: true })` with no tiebreak; `ApiClient.fetchExerciseCatalogue` carries a second `.order(colId)` and its own comment says why ("A seeded global and a user's custom entry can carry the same name"). Two rows sharing a folded key and a raw name are then an unspecified order in Postgres, and both platforms resolve a typed name to ONE id by walking that list last-wins — so the web binding can differ between two loads of the same page, and from the phone's, for the same user and the same lift. One `.order('id')` closes the non-determinism; it does not close the ambiguity, which wants the dedupe below. Not taken: `core/data.ts` was exclusively another lane's tree this round. Owner tree: `apps/web/src/lib/core/`. **Still open 2026-09-07, and narrowed**: web now resolves the shadow at the read with the owner's row winning ([decisions § 1419](../architecture/decisions.md)), so the web answer no longer depends on order at all. The phone still binds by whichever row its ordering returns first, so the two platforms still disagree — closing this is the Dart half filed below. **CLOSED 2026-09-07** ([decisions § 1460](../architecture/decisions.md), [decisions § 1461](../architecture/decisions.md)) — the phone resolves the shadow at the read now, with the owner's row winning, so neither platform's answer depends on the order any more. The web half needed no change: the `.order('id')` tiebreak and the dedupe were already there and already pinned.
- [x] **A failed catalogue read degrades to an EMPTY catalogue on both platforms, which is exactly the state in which the picker offers to create a name that already exists.** **CLOSED 2026-09-08** ([decisions § 1570](../architecture/decisions.md)). `data.ts`'s `fetchExerciseCatalogue` returns `[]` on error; `gym_screen.dart` catches, logs, and leaves `_catalogue` at its previous value, which on a first load is `const []`. The picker's `canCreate` is a catalogue-wide exact test, so it is only as good as the catalogue — with an empty one every name looks free, and creating "Bench Press" then SUCCEEDS against the partial unique (the author index cannot see a row whose `author_id` is null) and mints the shadow [§ 1384](../architecture/decisions.md) describes. The honest shape is a third state: a catalogue that failed to load is not an empty catalogue, and the create affordance should be suppressed rather than offered on absent evidence — the same fail-closed rule the tree applies everywhere else. Needs a loaded / empty / unavailable signal out of both read sites, so it is bigger than a picker edit. Owner trees: `apps/web/src/lib/core/` + `apps/mobile_android/lib/screens/` + the two picker files. **Web half CLOSED 2026-09-07** ([decisions § 1419](../architecture/decisions.md)); the phone half is the same open item as the entry above, and both close together only when the Dart read is written. **Same re-attribution as the entry above, 2026-09-07** — the `[]` is the screen's, not the client's.
- [x] **`normaliseExerciseName` collapses U+00A0 and the catalogue `fold` does not, so two rows that ARE one exercise can sort apart.** Found while writing the shadow cases: `Bench Press` and `Bench<U+00A0>Press` have one exercise key — they bind to the same PRs, the same routine rows, the same grouping — while `compareFoldedNames` compares `bench press` against `bench\u00a0press` and files them in different places in the list. That is not a bug in either function (the key answers "is this the same exercise", the fold answers "where does a reader look for it", and § 1334 is explicit that keeping them apart is the point), but the case where a shadow's two rows do NOT sit adjacent is worth knowing about before a surface starts rendering them as a group. No fix proposed; filed so the next reader does not discover it inside a shadow investigation. Owner trees: `apps/web/src/lib/segments/` + `apps/mobile_android/lib/`. **CLOSED 2026-09-07** ([decisions § 1484](../architecture/decisions.md), [decisions § 1496](../architecture/decisions.md)) — both halves, and the entry named one code point where the measured number is 25 of the 26: only U+2000 and U+2001 carry canonical decompositions and those land on U+2002/U+2003, still not a space. The consequence is stronger than "sort apart" too — every kept member sorts ABOVE U+0020, so the two same-key rows are separated by the whole block of names sharing a first word. No behaviour changed, per § 1334.
- [x] **`docs/testing/test_inventory.md` needs a row for `apps/mobile_android/test/exercise_catalogue_picker_test.dart` (20 tests), and the `exercise_catalogue_picker.test.ts` count moves 13 -> 16.** The lane was instructed not to edit the inventory. The Dart file is new on both twins; the web suite gained three cases (two ordering, one shadow-determinism) and had two retitled. Owner: whoever reconciles `docs/testing/test_inventory.md`. **CLOSED 2026-09-07** — both suites are indexed and the web count now reads 20, reconciled at integration with the rest of the census.

### Surfaced by the #789 round 43 mobile-parse lane (2026-09-06)

- [x] **The `DateTime.tryParse` rollover is still live in 202 sites the strict reader can now reach, and the largest block is one line in a generator.** [§ 1377](../architecture/decisions.md) put `parseIsoStrict` in `core_models`, below every consumer. What still takes the rollover, measured: `packages/core_models/lib/src/generated/db_rows.dart` (**167**, all of the form `DateTime.parse(json['x'] as String)` emitted by `scripts/gen_dart_models.dart:643-646`), `packages/api_client/lib/src/api_client.dart` (**21**), `packages/core_models/lib/src/social.dart` (**7**), and three files under `apps/mobile_android/lib` reading a row map by quoted key — `backup.dart` (**4**, the archive-restore path, where a rolled-over `started_at` files a restored run on a day the runner never ran), `watch_ingest_queue.dart` (**2**, where the surrounding code already throws on a malformed payload so the entry is quarantined — the cheapest of the three to convert), `training_service.dart` (**1**). The generated block is a one-line change in the generator plus a regeneration, which is a schema-codegen change and wants the lane that owns `scripts/gen_dart_models.dart`; the `.g.dart` files (`run.g.dart` 2, `route.g.dart` 1, `waypoint.g.dart` 1) read this app's OWN serialisation and are a different provenance. Note the mobile guard's covered set is derived from *using* the reader, so converting a file joins it automatically — but the packages are outside that guard's root entirely and would need their own. Owner tree: `scripts/gen_dart_models.dart` + `packages/core_models/**` + `packages/api_client/lib/src/api_client.dart` + `apps/mobile_android/lib/{backup,watch_ingest_queue,training_service}.dart` + the iOS twins. **181 of the 202 CLOSED 2026-09-07** ([decisions § 1430](../architecture/decisions.md)): the generator emits the strict reader (167 regenerated), and `social.dart` (7), `backup.dart` (4), `watch_ingest_queue.dart` (2) and `training_service.dart` (1) were converted with it. **What remains is `packages/api_client/lib/src/api_client.dart` (21)**, which is outside the mobile guard's root and would need its own coverage claim. **CLOSED 2026-09-07** ([decisions § 1463](../architecture/decisions.md)) — the last 21 sites, in `packages/api_client/lib/src/api_client.dart`, read through the strict parser, and each package gained its own whole-`lib/` instrument because the app guard derives its covered set from `apps/mobile_android/lib` and never reached a package.
- [x] **`RunSummary.fromIndexJson` still hard-casts every field of the store's own index file, and one bad index row throws the whole cold load.** Found while fixing the sibling defect in `fromRun` ([§ 1377](../architecture/decisions.md)): `packages/core_models/lib/src/run_summary.dart:147` reads `j['id'] as String`, `DateTime.parse(j['started_at'] as String)`, `(j['duration_us'] as num)` and six more casts off `index.json`. Lower severity than `fromRun`'s, and deliberately not fixed in the same change: the index is written by `toIndexJson` in this same build, so a wrong TYPE there means the file is corrupt rather than that another writer disagreed — and `LocalRunStore` already rebuilds the index from the run files when it detects drift, which is the recovery this would want to reach. What is NOT verified is whether a throw from `fromIndexJson` reaches that rebuild or aborts the load; that trace is the work. Owner tree: `packages/core_models/lib/src/run_summary.dart` + `apps/mobile_android/lib/local_run_store.dart`. **CLOSED 2026-09-07** ([decisions § 1431](../architecture/decisions.md)) — every field refuses by name and `started_at` reads through the strict parser, so a corrupt row reaches the lossless rebuild instead of sorting the list by a day the runner never ran.
- [x] **The `recurrence` mirror suites are 30 web / 25 Dart by a crude count, and the pair's registry entry makes no claim about it.** Counted as top-level `it(`/`test(` occurrences in `apps/web/src/lib/social/recurrence.test.ts` and `apps/mobile_android/test/recurrence_test.dart` while registering the pair's precondition ([§ 1379](../architecture/decisions.md)). A crude count is NOT evidence of a coverage gap — parameterised cases and nested groups make it unreliable in both directions — which is exactly why no count claim was written into either registry. The work is to read the two suites case by case and either state the count in the registry entry the way `catalogue_browse` and `plan_ramp` do, with the per-platform extras named, or close the gap. Owner tree: `apps/web/src/lib/social/recurrence.test.ts` + `apps/mobile_android/test/recurrence_test.dart` + the iOS twin + both registries. **CLOSED 2026-09-07** ([decisions § 1444](../architecture/decisions.md)) — decomposed case by case into 23 mirrors, 2 Dart-only source guards, 2 web-only cases already covered differently, and 5 that are a real gap; the gap is re-filed below rather than left inside a count.
- [x] **`CLAUDE.md`'s analyzer note is right about `dart analyze` and silently wrong for anyone who types `flutter analyze`.** Measured on this tree ([§ 1380](../architecture/decisions.md)): `flutter analyze` over `apps/mobile_android` exits **1** with 3,482 issues, every one of them `info`; `dart analyze` over the same directory exits **0**. The gotcha entry states the 0/2/3 ladder without naming which command it describes, so a lane following it with `flutter analyze` reads a green tree as red — and the documented next move, grepping the output for `warning`, is the pipeline-exit-code trap the same file warns about two lines later. Not edited here because this lane already owns one contiguous edit to `CLAUDE.md` and a second hunk in a different section complicates the splice. One clause, in the "Tools and workspace" gotcha that begins "**`dart analyze` exits 0 on `info`…**". Owner tree: `CLAUDE.md`. **CLOSED 2026-09-07** — the note names the command its 0/2/3 exit ladder describes.

### Surfaced by the #789 round 43 backend-pgtap lane (2026-09-06)

- [x] **The conditional-stamp scan's model is `(table, column)` with no OPERATION, so an INSERT is judged against a trigger arm that only runs on UPDATE.** One of the thirteen sites is exactly that: `payment_refund_ledger_test.sql:281` inserts a `payment_refunds` row, and both of `lock_payment_refund_writes`' assignments sit inside `if tg_op = 'UPDATE'`, so the supplied `status` / `failure_reason` were never reachable by the rewrite at all. It costs a registry entry that says so in prose. The fix is to teach `assignedColumns` the `tg_op` guard the way it already reads `if` / `end if` — track which operations the enclosing branch admits and return `{insert, update}` sets rather than one — and to read the `create trigger`'s own `before insert or update` clause, which `stampedColumns` currently only tests for a match. It would also sharpen the UNCONDITIONAL population, where the same conflation is possible and simply has no instance today. Owner tree: `apps/backend/scripts/`. **CLOSED 2026-09-07** ([decisions § 1485](../architecture/decisions.md)) — populations are keyed by operation, read from both the `create trigger` event clause and the `tg_op` guards in the body, and depth-counting gave way to a must-assign walk over the body's exits, which fixed two real misclassifications in opposite directions. The registry entry that existed only to say the scan could not see this is deleted.
- [x] **`notify_event_rsvp_organisers_test.sql`'s registered read-back is INDIRECT, and nothing notices if the indirection stops holding.** Its entry's reason is that `notify_event_rsvp` returns early on any status other than `'going'`, so the three notification counts beneath the `lives_ok` would all be 0 had `enforce_event_capacity` waitlisted the row — the fan-out IS the read-back, and it is a real proof today. But it is a proof about a SECOND trigger's control flow, and a change that made the fan-out notify a waitlisted RSVP (a plausible product change — the organiser arguably wants to know) would silently make the reason false while the guard stayed green, because the named read-back assertion still exists. The honest repair is one direct `is((select status …), 'going', …)` beside it, which costs one assertion and removes the dependency. Not taken this round because it would have made a passing entry the only one in the registry with two justifications. Owner tree: `apps/backend/supabase/tests/`. **CLOSED 2026-09-07** ([decisions § 1487](../architecture/decisions.md)) — a direct `is((select status …), 'going')`, with the isolating mutation confirming the five pre-existing assertions stay green while only the new read-back goes red.
- [x] **The scan reads INSERT column lists and UPDATE SET lists, so a value supplied through an RPC ARGUMENT is invisible to it.** `stampedValueWrites` regexes `insert into <t> (cols)` and `update <t> set …` out of the `lives_ok` SQL. A `lives_ok` whose SQL is `select upsert_checkpoint_crossing(…, true, 64.5, …)` supplies four Art 9 columns to a function that filters them, and matches neither pattern — so the entire RPC-mediated write surface, which for several tables (`checkpoint_crossings`, `event_results`) is the ONLY write surface, sits outside both populations. This is a false-negative class rather than a defect: nothing the guard reports is wrong. Closing it means resolving a function argument to the column it lands in, which is a real static-analysis step and not a regex; the cheaper first move is to fail on a `lives_ok` calling a known single-writer RPC unless it is registered, which reuses the machinery already here. Filed with its cost stated so a later round does not re-derive it. Owner tree: `apps/backend/scripts/`. **CLOSED 2026-09-07** ([decisions § 1486](../architecture/decisions.md)) — closed at the durable end rather than by listing RPC names: an argument resolves through the parameter list to the column and operation it reaches, verbatim landings rejoin the trigger scan and filtered ones go to `FILTERED_RPC_ARGUMENTS`.
- [x] **Both `weigh_in_flag` halves say the DB enforces the real gate, and neither names the file that now measures it.** `apps/web/src/lib/runs/weigh_in_flag.ts` says "The DB enforces the real gate too … so this is the client half of a defence-in-depth pair, not the sole guard"; the Dart twin's header carries the same claim. That claim is now measured, by `apps/backend/supabase/tests/weigh_in_health_gate_test.sql`, and a reader of either module has no way to find it — which is the state that let the SQL half go unpinned for the merge branch in the first place. One line in each header. Not taken here: both are client trees this lane does not own, and the Dart edit carries the iOS-twin mirror obligation. Owner trees: `apps/web/src/lib/runs/` + `apps/mobile_android/lib/` (+ the iOS twin). **CLOSED 2026-09-07** ([decisions § 1497](../architecture/decisions.md)) — both headers name the pgtap file and the exception, and the Dart guard measures the declaration in both directions. The flag-name asymmetry the entry claimed checks out; what it missed is that the web header named neither the mobile key nor the exception, so an operator flipping the web gate had no way to learn the phone was still closed on Art 9 data.
- [x] **`checkpoint_crossings_test.sql`'s header enumerates a seven-point contract, and point 6 now describes only half of what the suite pair covers.** Its item 6 reads "the Art 9 health value persists ONLY when the checkpoint requires_weigh_in AND the caller consented" — accurate for that file, and no longer the whole of the gate's coverage now that the merge branch and the other three columns live one file over. Left alone deliberately: rewriting a neighbouring suite's header to advertise a new file is the kind of edit that reads as scope creep, and the new file's own header points back at this one. Worth folding in whenever that suite is next touched. Owner tree: `apps/backend/supabase/tests/`. **CLOSED 2026-09-07** ([decisions § 1488](../architecture/decisions.md)) — point 6 names which half of the Art 9 gate the file covers and points at the file that measures the other.

### Surfaced by the #789 round 43 watch lane (2026-09-06)

- [x] **`syncError` reaches the wrist as a raw exception message — the one user-facing string on the watch that never goes through a catalogue.** `drainQueueLoop` sets `lastError = e.message ?: e.javaClass.simpleName`, `drainQueueLocked` copies it onto `RunUiState.syncError`, and `PostRunScreen` renders it verbatim in `DuskPalette.error`. So a runner in any of the seven locales can be shown `Unable to resolve host "…supabase.co"` or `SocketTimeoutException` on a 1.4-inch display, English, technical, and unbounded in length where every other caption on that screen is a `caption3` resource. The one path that does not do this is the unreadable-queue read failure, which sets the localized `sync_queue_unreadable` instead — so the field already carries two different KINDS of value. The durable shape is the one `classifyDrainError` already has the information for: map the classification to a resource key, keep the raw message in `Log.e` where a bug report can reach it, and let the field hold a key rather than a sentence. Not taken here: [§ 1390](../architecture/decisions.md) deliberately gave PreRun a fact (`syncFailed`) rather than a message, so nothing this lane added consumes `syncError`, and rewriting what PostRun renders is a separate change with its own seven-catalogue cost. Owner tree: `apps/watch_wear/`. **CLOSED 2026-09-07** ([decisions § 1490](../architecture/decisions.md)) — a classified `SyncFault` resolved through a compiler-total `when` into strings present in all seven `values-*` sets, with the raw throwable surviving on `DrainQueueLoopResult.failures` and reaching `Log.e` with its stack, which is strictly more than the field preserved.
- [x] **The PostRun `×` is silently inert on an unreadable queue, and that is exactly when a runner most wants off the screen.** `discard()` runs under `launchGuarded`, whose `crashGuard` only logs — so a DataStore read or write that throws aborts the coroutine before `startNextRun()`, leaving the runner on PostRun with a confirm they have already given and no visible result. Pre-existing and unchanged by [§ 1388](../architecture/decisions.md): the previous shape threw at `store.remove(id)` for the same reason and reached `startNextRun()` no more often. The recovery prompt's Discard was deliberately kept live for this exact reason (§ 1107's "stranding a runner who wants to record NOW behind a corrupt file would cost them the next run as well as this one"), and the PostRun one has the same claim on it. The fix is a `try`/`catch` that surfaces the fault the way `discardRejectedRuns` does and still advances the stage; the question worth deciding first is whether a `×` that could not remove the entry should advance at all, because the run is then still queued and will drain later. Owner tree: `apps/watch_wear/`. **CLOSED 2026-09-07** ([decisions § 1491](../architecture/decisions.md)) — `discardRun` is extracted the way `drainQueueLoop` was, so a DataStore throw is a case rather than a crash, and the stage advances unconditionally while raising `queueUnreadable` so the arc states the fault on the screen the runner lands on.
- [x] **The PreRun top arc's four-way slot precedence is guarded only by source greps, and it now has four arms.** `if (queueUnreadable && authed)` → `else if (rejectedCount > 0 && authed)` → `else if (queuedCount > 0)` → `else if (!online && authed)`, plus the failed/ordinary split inside the third ([§ 1390](../architecture/decisions.md)). Which arm wins for a given `(queueUnreadable, rejectedCount, queuedCount, syncFailed, online, authed)` is a pure decision that three separate test files now assert by reading the source in three different ways — `ScreenWiringTest`, `PreRunRejectedQueueTest`, `PreRunSyncFailureTest` — and none of them can evaluate it. Extracting a `syncChipState(...)` returning a small enum would move it to real JVM coverage (a truth table over the six inputs) and leave the Compose layer a `when`. Deliberately not attempted mid-round: it would make the three existing grep-based guards stale in the same change that removed the thing they read, and two of them were written by the previous round. Owner tree: `apps/watch_wear/`. **CLOSED 2026-09-07** ([decisions § 1434](../architecture/decisions.md)) — `syncChipState` is a pure function over the six inputs, all 64 tuples of the state space are asserted, and the three greps are narrowed to what a grep can hold.
- [x] **This lane moves two `docs/testing/test_inventory.md` rows and was instructed not to edit that file.** `check_test_inventory_counts.mjs` fails on `test_inventory.md:848` (`apps/watch_wear/.../*Test.kt`: states 767 tests across 74 files, now **774 across 75**) and `test_inventory.md:915` (`scripts/check_watch_ios_source.test.mjs`: states 87, now **88**). The census is reconciled at integration by design, after every lane's branch is merged — recomputing it per-lane would conflict in every round. Recorded here so the reconciliation has the two figures rather than having to re-derive which lane moved them. Owner: whoever integrates the round's branches. **CLOSED 2026-09-07** — reconciled at integration, together with the four further rows this round moved.

### Surfaced by the #789 round 43 infra-ci lane (2026-09-06)

- [ ] **`terraform validate` passes on all five root stacks and on no module, and no lane can `plan` — re-run each round.** Standing item, re-filed rather than retired because its reason has not changed: no lane holds AWS credentials by design, so nothing behind §§ 1021-1024, 1084-1085, 1111 or [§ 1268](../architecture/decisions.md)'s `custom_error_response` has ever been `plan`ned against real state. Round 43's run and its planted-mutation measurement are recorded on the round-41 entry above and in [decisions § 1394](../architecture/decisions.md#1394-round-43s-terraform-re-run-five-stacks-valid-transitive-module-coverage-re-measured-in-a-file-no-previous-round-used). Three things settled and not to be re-asked: the module cannot be validated standalone (§ 1267 — `configuration_aliases` makes a module a non-root by construction), its coverage is transitive and **measured** rather than asserted, and the "a stack nobody listed" gap is closed by `check_infra_coverage.mjs` reading both the `validate` matrix and the `fmt` scopes against a walk of the tree (§ 1111). The next round should run `init -backend=false` + `validate` in `infra/bootstrap`, `infra/dns`, `infra/github-oidc`, `infra/envs/prod` and `infra/envs/preview`, plus `terraform fmt -check -recursive infra`, and plant one undeclared-variable reference in the module — rotating the file, as round 43 did, so the claim stays about the module. Note what this class structurally cannot see: round 41's three false comments and round 42's three `*_spa_shell.ts` ones each passed `validate` and `fmt` without a murmur. Owner tree: `infra/`.
- [x] **`apps/mobile_android/android`'s 32 `@Test` methods still run nowhere, and the cheapest path is a committed `gradlew` wrapper plus one step in `build-mobile-android`.** Declared in `GRADLE_UNTESTED` and echoed on every run since [§ 1393](../architecture/decisions.md#1393-a-gradle-projects-committed-unit-tests-must-be-run-by-some-job--and-appsmobile_androidandroid-is-compiled-by-ci-which-is-not-what-the-filing-said), which makes the gap visible rather than closing it. The cost is now precise, and it is smaller than the original filing assumed because `build-mobile-android` **already** pays for the pinned Flutter SDK, `melos bootstrap`, the pub cache and a warm Gradle cache, and already runs the full Gradle configuration — so the marginal cost of `testDebugUnitTest` after `flutter build apk --release` is the test compile plus 32 JVM assertions, not a second toolchain. The blocker is that this project ships **no wrapper**: `gradle/wrapper/gradle-wrapper.properties` is committed but `gradlew` and `gradle-wrapper.jar` are not, so there is nothing to invoke. Committing a `gradle-wrapper.jar` is a binary-blob supply-chain decision (the `scorecard` / pinning posture in this repo argues against adding one casually) and was refused rather than taken by a lane that could not execute Gradle to verify the result. Three options, in preference order: (1) commit the wrapper Flutter's own template ships and add `./gradlew testDebugUnitTest` to `build-mobile-android` after the APK build; (2) invoke the runner image's preinstalled `gradle` CLI instead, accepting that the version is then not pinned by `gradle-wrapper.properties`; (3) leave it declared. Whoever takes it must delete the `GRADLE_UNTESTED` entry in the same change — `check_gradle_test_coverage.mjs` fails on an excuse for a project whose tests do run. Owner tree: `.github/workflows/`. **CLOSED 2026-09-07** ([decisions § 1439](../architecture/decisions.md)) — the wrapper is committed byte-identical to `apps/watch_wear/android`'s, and `build-mobile-android` runs `./gradlew testDebugUnitTest` after the build that writes `local.properties`.
- [x] **`build-watch-wear`'s Gradle cache key hashes `apps/watch_wear/android/**` only, so a second tested Gradle project would share a key that ignores its own build files.** `check_gradle_test_coverage.mjs` now makes a third project's tests somebody's decision the day it lands, and the natural way to take that decision is another invocation in this job — at which point `key: gradle-watch-wear-${{ hashFiles('apps/watch_wear/android/**/*.gradle*', …) }}` restores a cache that a change to the new project's `build.gradle.kts` does not invalidate. A stale-but-valid Gradle cache is a slow build or a confusing one, not a wrong result, which is why this is a nit and not a defect today: there is exactly one project in the job. Fix at the same time as adding the second. Owner tree: `.github/workflows/`. **CLOSED 2026-09-07** ([decisions § 1440](../architecture/decisions.md)) — claim 4 of `check_gradle_test_coverage.mjs` now fails a job whose `~/.gradle` key hashes no file under a project that job invokes, which is exactly the day the filing said to fix it.
- [x] **The `actions` CodeQL leg's `queries: security-and-quality` is a value CodeQL silently downgrades, and nothing measures the downgrade.** The workflow's own comment says the `actions` language "only ships a `security-extended` ceiling; CodeQL silently falls back to that when security-and-quality isn't available", and [§ 1392](../architecture/decisions.md#1392-build-mode-none-narrows-nothing-which-is-not-the-same-as-nothing-narrowing-it--the-two-interpreted-codeql-legs-are-read-too)'s guard checks the value in the workflow, not the suite that actually ran. So the guard confirms we ASKED for the broadest suite and cannot confirm we got it — and if a future CodeQL release stopped honouring the fallback, or renamed the suite, the leg would run something narrower with the guard still green. The observable is in the run: the `init` step logs the resolved query suite, and the SARIF's `runs[].tool.driver.rules` names what actually executed. Reading either needs a completed scan, which is not available to a `workflow-lint` job with no CodeQL and no network — so this is not a guard, it is a periodic manual check or a separate scheduled job that reads the last run's SARIF through the API. Owner tree: `.github/workflows/`. **CLOSED 2026-09-07** ([decisions § 1503](../architecture/decisions.md)) — the job keeps its SARIF and reads back which suite actually ran, discriminating on quality-tagged rules, and the guard fails if the measurement is deleted. Testing the discriminator found two real defects in it first: a missing results directory and malformed JSON each aborted under `set -e` printing no diagnosis at all.
- [x] **`security.yml`'s `codeql-kotlin` job would fail on `apps/mobile_android/android` for a reason its own exclusion does not name — there is no `gradlew` there to run.** `CODEQL_KOTLIN_UNBUILT`'s reason (from [§ 1352](../architecture/decisions.md#1352-codeqls-kotlin-leg-had-the--1304-defect-too-and-the-fix-is-a-guard-that-runs-the-workflows-own-walk-rather-than-reading-it)) names the Flutter SDK and the `melos bootstrap` and is right about both, but the step's command is `./gradlew --no-daemon compileDebugKotlin` and that file is not committed for this project — so even with the SDK and the plugins in place, the loop would fail on a missing executable rather than on a configuration error, and whoever takes the exclusion off would spend the first hour on the wrong problem. One clause, not a code change; deliberately left to whoever takes [§ 1393](../architecture/decisions.md#1393-a-gradle-projects-committed-unit-tests-must-be-run-by-some-job--and-appsmobile_androidandroid-is-compiled-by-ci-which-is-not-what-the-filing-said)'s wrapper decision, because the two exclusions close together or not at all. Owner tree: `.github/workflows/`. **The blocking reason is gone as of 2026-09-07**: [§ 1439](../architecture/decisions.md) commits the wrapper, so `./gradlew --no-daemon compileDebugKotlin` now has an executable to run. What is still owed is the exclusion clause itself, and a decision about whether the Kotlin leg pays for the Flutter SDK + `melos bootstrap` its reason names. **CLOSED 2026-09-07** ([decisions § 1501](../architecture/decisions.md)) — the wrapper § 1439 committed removed the reason the exclusion named, so the exclusion states the real remaining cost now, and the gap it hides is a figure the guard recomputes rather than prose.
- [x] **`docs/architecture/architecture.md` § CI says "seventeen jobs" and `docs/testing/testing.md` says "nineteen"; `ci.yml` has 36.** Neither list names `workflow-lint`, `env-isolation`, `infra-guards`, `terraform`, `gitleaks`, `sim-firmware`, `sim-scenarios`, `watch-ble-uuids`, `watch-wire-vectors`, `ios-native-declarations`, `watch-ios-locale-parity`, `watch-garmin-source`, `lambda-alias-coverage`, `cross-client-roundtrip`, `e2e-web-exporthub` or `test-watch-ios`, and the two disagree with each other as well as with the file. Both paragraphs assert a count in prose that `scripts/check_ci_diagnostics.mjs` already derives from the `CI gate` aggregator's `needs:` list, so the number is computable and could be asserted rather than written — the same shape as the watch doc counts `check_watch_doc_counts.mjs` already reads. Not taken this round: the two paragraphs are enormous and shared with several lanes' subjects, and a lane that rewrites them collides with everyone. Owner tree: `docs/`. **CLOSED 2026-09-07** — both paragraphs name the recompute instead of asserting a number that goes stale on the next job.

### Surfaced by the #789 round 43 guards-shared lane (2026-09-06)

- [x] **Three of the four display-name sorts are not total orders, and the fourth is — for a reason it writes down.** Surfaced while closing the `localeCompare` filing above, and it is a different question from the collation ([§ 1400](../architecture/decisions.md)). `dmRecipientCandidates` breaks ties on `id` "so two renders of the same follow graph list people in the same order"; `comparePeopleRank`, `fetchSuggestedPeople`'s sort and `/coaching`'s roster sort return the collation's answer as their last term, and a collation returns 0 for two DIFFERENT strings — two people genuinely called `John Smith`, or one display name stored precomposed against another stored decomposed. `Array.prototype.sort` is stable, so the order then falls through to whatever the query returned, and no `ORDER BY` in these paths makes that unique. Not fixed here: the natural fix widens `RankablePerson` with an id, and the caller that would supply it is in `core/data.ts`, which was exclusively another lane's tree this round. Owner trees: `apps/web/src/lib/social/search_ranking.ts` + `apps/web/src/lib/core/data.ts` + `apps/web/src/routes/coaching/`. **CLOSED 2026-09-07** ([decisions § 1465](../architecture/decisions.md)) — `RankablePerson` carries the id, the shared name-then-id tail is one function, and the coach roster's comparator moved out of the markup with a tiebreak covering all five columns rather than only the collated one.
- [x] **Eleven great-circle distance functions live in `apps/web/src/lib`, and two of them are byte-for-byte the same function.** Registered in `check_shared_reimplementations.mjs` rather than fixed. `integrations/import.ts:haversine` and `routes/geocoding_math.ts:haversineM` have identical normalised bodies; nine more (`RouteBuilder.svelte`, `PrivacyZonePicker.svelte`, `garmin-fit.ts`, `privacy.ts`, `routing_quality.ts`, `route_snap.ts`, `nearest_track_point.ts`, `race_match.ts`, `run_stats.ts`) each carry their own. Ten use the `atan2` form and `run_stats.ts:haversineMetres` uses `asin` with the clamp § 305 records as a near-miss — so this is not only duplication, it is two formulas. Consolidating is a route/geo refactor with a parity-pair obligation on `run_stats`, and does not fit one lane. Owner trees: `apps/web/src/lib/routes/` + `apps/web/src/lib/integrations/`. **CLOSED 2026-09-07** ([decisions § 1470](../architecture/decisions.md), [decisions § 1471](../architecture/decisions.md)) — and the census was wrong in both directions: seventeen, not eleven, in three spellings. It was not merely duplication either — `route_snap.ts` carried a private UNCLAMPED arc while `route_snap.dart` has always imported the clamped canonical, so on a near-antipodal leg the phone returned a distance and the web returned NaN. 17 down to 10, with a guard that reads the class the clone guard states it cannot see.
- [x] **Seven copies of XML entity escaping under two names, and nine of `escapeJsonLd`, none of them importable.** Also registered rather than fixed. `routes/gpx.ts:escapeXml`, `routes/route_gpx.ts:escapeXml` and five `xmlEscape` under `lib/share/` have identical normalised bodies; the nine `escapeJsonLd` are deliberately INVISIBLE to the guard, which reports a group only when one member is an exported `lib/` function — duplication with nothing shared to import is a different finding, and the house rule tolerates it. Nine copies is past where it does. `route_gpx` is half of a registered parity pair, so the consolidated home has to be chosen with the Dart side in view. Owner trees: `apps/web/src/lib/share/` + `apps/web/src/lib/routes/`. **XML half CLOSED 2026-09-07** ([decisions § 1450](../architecture/decisions.md)): all seven call `util/html_escape`, the registration is deleted from `check_shared_reimplementations.mjs`, and the Dart half of the `route_gpx` pair moved in the same commit so both emit the same bytes. **The nine `escapeJsonLd` copies remain.** **CLOSED 2026-09-07** ([decisions § 1475](../architecture/decisions.md), [decisions § 1476](../architecture/decisions.md)) — the nine become one `serialiseJsonLd` that stringifies and escapes, since every call site spelled the two together, and a censusing test calls all twelve exported builders with a hostile field because a builder that omits the escape entirely is invisible to the clone guard.
- [x] **`localeCompare` is used as a string comparator on ISO dates in seven places, where it is both wasteful and slightly risky.** `runs/+page.svelte` (twice), `plans/+page.svelte`, `coaching/athletes/[id]/+page.svelte`, `routes/+page.svelte`, `CoachChat.svelte` and `learn/guides_index.ts` each sort `started_at` / `start_date` / `scheduled_date` / `created_at` / `archived_at` / a locale tag through a full ICU collation. Over ASCII digits and hyphens a collation and a code-unit order agree today, so nothing is broken — but a collation is answering a question about ordinals, and the numeric-collation option would reorder them if anyone ever passed it. `<` / `>` is the instrument. Not touched by this lane, which owns none of those trees. Owner trees: `apps/web/src/routes/` + `apps/web/src/lib/`. **CLOSED 2026-09-07** ([decisions § 1451](../architecture/decisions.md)) — and it was not merely wasteful: Postgres trims a zero fractional part, so `+` against `.` puts the older row on top of a newest-first list in six measured locales.
- [x] **`§ 1400`'s criterion decides the open catalogue-ordering question above, which is still filed as undecided.** The entry "Two web modules now answer 'how is a catalogue ordered' with two different instruments" asks which of `localeCompare` and a folded compare this product wants, and says both arguments are sound and point opposite ways. § 1400 supplies the deciding term: fold where a Dart twin exists, because that runtime ships no collator and the two lists must agree; collate where the surface is web-only, because a collation gives each reader their own correct order and a fold gives everyone the English one. By that criterion the exercise-catalogue picker — which HAS a twin, and whose mobile half has folded since § 1334 — should fold on web too, and the four surfaces § 1400 examined should not. Not applied here: `lib/components/` and `lib/segments/` were other lanes' trees this round. Owner trees: `apps/web/src/lib/components/` + `apps/web/src/lib/segments/`. **CLOSED 2026-09-07** ([decisions § 1482](../architecture/decisions.md)) — applied, and it demanded no behaviour change: the criterion is a guard now rather than a fifth derivation of it.
- [x] **The web half of § 1398 ships with no behavioural pin — the third instance of the gap § 1278 filed.** `planSlug` now folds, and `/plans/[id]` is a Svelte page that the `tsx --test` suite cannot compile, so nothing asserts the exported filename. What is owed is a Playwright case: create a plan named `İstanbul Marathon`, export it, and assert the download's filename is `istanbul-marathon.md` rather than `i-stanbul-marathon.md`. It fails against the pre-§ 1398 page. Owner tree: `apps/web/tests-e2e/plans/`. **CLOSED 2026-09-07** ([decisions § 1530](../architecture/decisions.md)), differently from how it was filed. It asked for a Playwright case; what landed is `planSlug` extracted into `lib/training/plan_slug.ts` with a five-case suite, two of which fail against the pre-§ 1398 form. Removing the constraint — a `.svelte` file has no `tsx --test` harness — is the durable fix rather than routing around it.

### Surfaced by the #789 round 43 web-write-bounds lane (2026-09-06)

- [x] **`resting_hr_bpm` has exactly the write-side gap `max_hr_bpm` just had, on the same two pages.** `apps/web/src/routes/settings/account/+page.svelte:605` writes `prefs.resting_hr_bpm = parseInt(restingHr, 10) || null` and `apps/web/src/routes/settings/preferences/+page.svelte:991` autosaves `parseInt(restingHr, 10) || null` on blur; both inputs carry an advisory `min="30" max="120"` that never reaches constraint validation for the same two reasons [§ 1407](../architecture/decisions.md) records (no `<form>` on the account card, an `onblur` autosave on the other). It is a jsonb prefs key with no column and therefore no CHECK, so nothing refuses a typo. **Not fixed here, and deliberately not by analogy**: unlike `max_hr_bpm` this key has no named bound anywhere to reuse — the two 30/120 attributes are the only numbers in the tree and they are advisory — so closing it means first deciding the range and naming it, then checking what the readers do with an out-of-range value. `training_load.dart` / `training_load.ts` consume it for TRIMP and `defaultZoneCutoffs` does not; whether they bound it needs checking before a write gate is added, or the write and the read disagree in the direction § 1407 exists to prevent. Owner trees: `apps/web/src/routes/settings/`, `apps/web/src/lib/training/`. **CLOSED 2026-09-07** ([decisions § 1409](../architecture/decisions.md)) — the range was decided first (20..200, the wider of the two the tree already shipped) and named once per rail, and `check_shared_constants.mjs` compares them.
- [x] **The mobile `_pickInt` dialog refuses an out-of-range value silently, indistinguishably from Cancel.** `apps/mobile_android/lib/screens/settings_preferences_screen.dart:509`'s Save button does `if (v == null || v < minValue || v > maxValue) Navigator.pop(ctx, null);` — the dialog closes, the caller reads `null` and returns, and nothing is written or said. A runner typing 300 into max HR, or `abc` into any of them, gets the same outcome as tapping Cancel and is never told the range. This is the mobile half of the "a refusal the user cannot act on is a bug" rule [§ 1407](../architecture/decisions.md) applies on web, and it affects **every** numeric preference on that screen, not just max HR — resting HR, carbs/h, fluid/h and the rest all share the helper. The fix is one `StatefulBuilder` in `_pickInt` keeping the dialog open with an `errorText` naming `minValue`..`maxValue`, plus one interpolated key in all seven ARBs. **Not attempted in this lane**: it is a shared dialog behind ~10 call sites and needs Flutter widget-test verification, which this lane was not permitted to run. Owner tree: `apps/mobile_android/lib/screens/` (+ the iOS twin). **CLOSED 2026-09-07** ([decisions § 1410](../architecture/decisions.md)) — one `StatefulBuilder`, `prefsValueOutOfRange` in all seven ARBs, and four widget tests through the resting-HR call site, including that Cancel is still distinguishable from a refusal.
- [x] **`web-write-bounds`' max-HR gate throws on every keystroke, so a VALID max HR is now silently dropped on both write paths it was added to guard.** Reproduced on `test/789-r43-web-write-bounds` **in isolation** (not only in the round's merge): `tests-e2e/settings/preferences.spec.ts` "Resting + Max HR inputs render and round-trip through save" fails — 182 is typed, "Saved" appears, and after reload the field is empty while resting HR round-trips. The browser reports `$.get(...).trim is not a function`. `bind:value` on an `<input type="number">` hands Svelte a NUMBER, so `const maxHrParsed = $derived(maxHr.trim() === '' ? null : Number.parseInt(maxHr, 10))` throws the moment anything is typed; `saveMaxHr` never runs and the pref is never queued. The code it replaced — `maxHr ? parseInt(maxHr, 10) || null : null` — worked because `parseInt` coerces and the falsy test covers the empty string. **Both write paths carry the identical line**: `settings/preferences/+page.svelte:~336` and `settings/account/+page.svelte:~88`, and on the account page the Save button's `disabled={saving || maxHrOutOfRange}` reads the same throwing derived. The fix is to stop treating a number-bound value as a string (`maxHr === '' || maxHr == null ? null : Number(maxHr)`, or declare it `$state<number | null>(null)`), and to pin it with the existing preferences spec. Owner tree: `apps/web/src/routes/settings/` (the `web-write-bounds` lane). **CLOSED — verified 2026-09-07.** The throwing derived was already replaced within #873 itself by `numberInputValue` (`apps/web/src/lib/settings/number_input.ts`), which reads a `bind:value` that Svelte has turned into a number, so the filing describes a state that never reached `main`. Confirmed rather than assumed: `tests-e2e/settings/preferences.spec.ts` runs green on the round-44 integration tree, "Resting + Max HR inputs render and round-trip through save" included, alongside the resting-HR gate [§ 1409](../architecture/decisions.md) adds on the same two pages.
- [x] **`routes/detail.spec.ts` "star + reload + starred-only filter shows it + unstar restores" fails under a full-directory run and passes in isolation, on every tree tested.** Not a round-43 regression — measured failing on this lane's own branch AND on `test/789-r43-web-typed-client`, and passing alone on both; an earlier full-directory run on the same branch had passed, so it is state- or load-dependent rather than diff-dependent. At the timeout the page is back on `/routes/[id]` with the button reading "Unstar route" while the DB row is already unstarred, so the final click's write landed and only the class never flipped inside the 10 s window. `page.waitForLoadState('networkidle')` is the test's own stated hydration gate and it is not one. Worth reproducing under load and anchoring the last step on something that proves the handler is live, rather than on the network going quiet. Owner tree: `apps/web/tests-e2e/routes/`. **CLOSED 2026-09-08** ([decisions § 1556](../architecture/decisions.md)). Root cause: `toggleStar` flips the class synchronously then awaits `setRouteStar`, so `page.reload()` cancelled the in-flight PATCH and the step that exists to prove persistence was racing the write. Proved deterministically by stalling `PATCH /rest/v1/routes` by 1500 ms — the old shape fails after the reload with the row unstarred, the new shape passes under the same stall.
- [x] **`routes/builder.spec.ts` save-flow and four `routes/import.spec.ts` cases failed once, on the six-lane merged tree only, and did not reproduce.** Seen in a single full-directory run of the round-43 cross-lane integration (`mobile-picker-tests` + `web-gym-residual` + `web-typed-client` at `c80d58c7f` + `guards-shared` + `web-write-bounds` + `e2e`); a later full-directory run on `web-typed-client` alone at its current tip (`271cbdf27`, two commits further on) had none of them, and neither did this lane's own branch. Either the two later `web-typed-client` commits closed it or it shares the load-dependence of the star case above. Re-run `tests-e2e/routes/` on the integrated tree before merging the round; do not assume the single observation was noise. Owner: whoever integrates the round. **CLOSED 2026-09-08** ([decisions § 1554](../architecture/decisions.md)), and it was not load-dependence: `rate_limits` is one row per `(user, bucket, hour)`, so it is shared by the whole run, and the two `create_route` cap tests planted `USER_A` at 30 and undid it in an in-body `finally` — which Playwright skips for a test it abandons on timeout. With 30 planted the two files together fail 5 / pass 4, exactly the filing's list; after the fix, 9 pass under the same plant. A clean `tests-e2e/routes/` run peaks the counter at 1, so the budget was never the constraint.
- [x] **`routes/heatmap-pins.spec.ts` cannot pass on this workstation, and the reason is a grant rather than the diff.** Both `clubs_in_bbox` cases fail with `[42501] permission denied for function clubs_in_bbox` reading through the service-role fixture client; `pg_proc.proacl` for that function grants EXECUTE to `postgres`, `anon` and `authenticated` and not to `service_role`. Confirmed identical on an untouched worktree of the base commit, and no round-43 lane touches SQL at all. It is the same local-CLI-image split already recorded for `anon` EXECUTE — worth one line in `docs/testing/` naming these two tests so the next lane does not spend a run on them. Owner tree: `docs/testing/`. **CLOSED 2026-09-07** ([decisions § 1505](../architecture/decisions.md)) — `docs/testing/` carries the register of locally-unpassable specs and why each is one.

### Surfaced by the #789 round 44 integration (2026-09-07)

- [x] **The phone's exercise catalogue has neither the shadow resolution nor the failure state the web read just gained.** [§ 1419](../architecture/decisions.md) fixed `fetchExerciseCatalogue` on web — explicit-range paging, `{ catalogue, error }` instead of `[]`, and the two partial uniques resolved with the owner's row winning — and `apps/web/src/lib/gym/exercise_catalogue.ts` is a pure module written to be twinned. `ApiClient.fetchExerciseCatalogue` still returns the shadowed pair unresolved and still degrades a failed read to an empty catalogue, which is the state in which the mobile picker offers to create a name the catalogue already holds. Writing `dedupeShadowedExercises` in `packages/api_client` closes three filings at once (this one, the both-platforms degradation entry, and the `(name)` vs `(name, id)` ordering divergence) and makes the pair registrable — which is itself an edit to `CLAUDE.md` and the `shared-library-syncer` table, per [decisions § 604](../architecture/decisions.md). Owner trees: `packages/api_client/` + the two registries. **Mostly closed 2026-09-07** ([decisions § 1460](../architecture/decisions.md), [decisions § 1461](../architecture/decisions.md), [decisions § 1462](../architecture/decisions.md)): the shadow resolution and the paged read landed in `packages/`, registered as a parity pair. What remains is the SURFACE — `gym_screen.dart` is where the `[]` actually came from, not the API layer, which never swallowed an error at all. **CLOSED 2026-09-07** ([decisions § 1514](../architecture/decisions.md)). Its own amendment said what remained was the SURFACE; `gym_screen.dart` now carries `catalogueUnavailable` — true until a read answers — through the composer to the picker.
- [x] **`describeRecurrence` is a lockstep half with zero Dart coverage, and it is live on the phone.** The five web-only cases [§ 1444](../architecture/decisions.md) isolated out of the `recurrence` pair's 30/25 count: all four `describeRecurrence` cases and `nextInstanceAfter` past `recurrence_until`. The function carries the same hard-coded English, the same ISO day ordering and the same ` · ` separator on both platforms, and `screens/event_detail_screen.dart` renders it — so a divergence would show a runner a different recurrence sentence on the phone than on the web with nothing failing. The Dart cases are a direct port of the web ones. Owner tree: `apps/mobile_android/test/recurrence_test.dart` (+ the iOS twin). **CLOSED 2026-09-07** ([decisions § 1495](../architecture/decisions.md)) — all five cases ported, and the port found NO divergence, which is the result rather than a null one: the separator and all four sentence literals were additionally compared byte-for-byte, because two passing suites cannot see a lookalike character. The pair is 30/30 now and both registries are updated.
- [x] **`apps/mobile_android/android/gradlew.bat` is committed with LF endings where `apps/watch_wear/android`'s is CRLF.** `.gitattributes` says `* text=auto eol=lf`, so the new file was normalised on add while the sibling's blob predates that rule and still holds CRLF. Nothing in CI reads either — both jobs run the Linux `gradlew` — and a `.bat` with LF endings is a Windows-only hazard, in a repo whose only documented workstation is Fedora. Worth deciding once rather than rediscovering: either a `*.bat text eol=crlf` rule (which renormalises the sibling on its next touch) or a note that the tree deliberately ships LF batch files. Owner tree: `.gitattributes`. **CLOSED 2026-09-07** ([decisions § 1504](../architecture/decisions.md)) — decided in the CRLF direction, `*.bat text eol=crlf` as the mirror of the `*.sh text eol=lf` rule the tree already has, and the renormalisation collapses both `gradlew.bat` onto one blob.
- [x] **`settings/account.spec.ts` "change email — request path" cannot pass on this workstation, and the reason is a missing container rather than the diff.** The assertion fails as `element(s) not found` on the pending-change row, which is the symptom already recorded for a dead local edge runtime: `docker ps` shows no `supabase_edge_runtime_project-running` at all, and `supabase start` does not recreate it, so GoTrue's e-mail-change hook has nothing to call. Confirmed environmental rather than diff-borne — the round-44 tree touches the HR fields on that page and nothing on the change-email path — but it cost a run to establish, and the sibling entry for `routes/heatmap-pins.spec.ts` above exists for exactly this reason. Worth one line in `docs/testing/` naming this spec beside those two. Owner tree: `docs/testing/`. **CLOSED 2026-09-07** ([decisions § 1505](../architecture/decisions.md)) — registered, and the filing was only half right: that spec's `page.route('**/auth/v1/user')` has never matched, because `auth-js` appends `?redirect_to=…` and Playwright anchors globs at both ends, so the case drives live GoTrue — which is why the absent container reaches it. The sibling case's `expect(sawRequest).toBe(false)` runs through the same dead handler and cannot fail; filed separately.
- [x] **`dashboard/page.spec.ts` "PR hide (×) control has a >=44px tap target" failed under a multi-directory run and passed in isolation on the same tree.** Same load- or state-dependence as the `routes/detail.spec.ts` star case filed by round 43, on a different spec, so the class is not specific to that one test: at the failure the control was present but its measured box was short, which reads as a layout still settling rather than a wrong rule. Both want reproducing under load, and anchoring on something that proves the surface has finished laying out rather than on the network going quiet. Owner tree: `apps/web/tests-e2e/`. **CLOSED 2026-09-08** ([decisions § 1555](../architecture/decisions.md)). `boundingBox()` is a one-shot read, not a web-first assertion, and returns null for a row mid-swap; each dimension is now polled and the threshold is untouched. Measured 480 samples over 12 loads: the control is exactly 44.0 x 44.0 every time, so it has zero headroom and any transient read fails.

### Surfaced by the #789 round 45 lanes (2026-09-07)

- [x] **The web `dedupeShadowedExercises` re-derives the key the two partial uniques are enforced on, and the two can legitimately disagree.** `apps/web/src/lib/gym/exercise_catalogue.ts` groups by `normaliseExerciseName(e.name)`; the Dart half written this round groups by the row's own `name_key` ([decisions § 1460](../architecture/decisions.md)). The stored value is what `exercises_global_name_key` and `exercises_author_name_key` are built on and it is stamped by the `exercises_stamp_name_key` trigger (20270711000001), where the client fold is this build's answer to the same question — and the two agree only while the frozen fold table matches the server's, which [§ 1176](../architecture/decisions.md) records as moving by MIGRATION rather than by codegen. So a row stamped before the last regeneration can carry a key the current table would not produce, and web then splits a pair the index considers one (rendering the shadow as two exercises) or joins two it does not (dropping a row the database is still serving). The fix is one line — read `e.name_key` — plus the Dart suite's ninth case ported, and it makes the pair's two halves answer the same question rather than two questions that usually agree. Owner tree: `apps/web/src/lib/gym/`. **Already CLOSED on landing** — commit `02926ac8f` (round 45) reads `e.name_key`; verified this round. What the filing did not anticipate is that BOTH parity registries went on describing the fold-vs-stored-key divergence, with its full rationale, for a round after it was closed — `check_parity_pair_registry.mjs` compares the two registries with each other and not with the code, so it saw them agree perfectly about something neither half does ([decisions § 1510](../architecture/decisions.md)).
- [x] **The phone's gym screen still swallows a failed catalogue read, so the picker offers to create a name the catalogue may already hold.** `gym_screen.dart:226` catches the fetch, `debugPrint`s, and leaves `_catalogue` at its previous value — `const []` on a first load. The data layer is done: `ApiClient.fetchExerciseCatalogue` propagates and a guard refuses any catch in it ([decisions § 1462](../architecture/decisions.md)), so the third state is reachable and only the surface has to carry it. What is owed is what web already has: a loaded / empty / unavailable signal on the screen, a create affordance in `widgets/exercise_catalogue_picker.dart` that fails closed while the catalogue is unknown rather than treating an empty list as "every name is free", and a retry. `canCreate` is a catalogue-wide exact test, so it is only ever as good as the catalogue. Both mobile files are twinned, so the change is doubled into `apps/mobile_ios`. Owner trees: `apps/mobile_android/lib/screens/` + `apps/mobile_android/lib/widgets/` + the iOS twins. **CLOSED 2026-09-07** ([decisions § 1514](../architecture/decisions.md)). The loaded / empty / unavailable signal, the fail-closed create affordance and the retry all landed, with the browse affordance deliberately kept reachable while the catalogue is unknown. Doubled into `apps/mobile_ios`.
- [x] **`packages/api_client`'s suite cannot be run with `dart test`, and nothing says so.** **CLOSED 2026-09-08** ([decisions § 1592](../architecture/decisions.md)). Every test file that imports `package:api_client/api_client.dart` crashes the front-end compiler — `type 'InvalidType' is not a subtype of type 'FunctionType' in type cast` out of `_FfiUseSiteTransformer`, from the Flutter FFI reached through `supabase_flutter`. CI is correct and uses `flutter test` (`ci.yml`'s `test-packages` job, `melos exec --scope="api_client" -- flutter test`), but the failure at a workstation reads as a broken tree rather than as the wrong runner, and it cost a run to establish. One line beside the existing analyzer-command gotcha in `CLAUDE.md`, or a row in `docs/testing/`, naming which packages need `flutter test` (`api_client`, `run_recorder`, `ui_kit`, `mobile_android`) and which take `dart test` (`core_models`, `gpx_parser`). Owner tree: `CLAUDE.md` or `docs/testing/`.
- [x] **`assignedColumns` reads `case` and `loop` as blocks but cannot close an EXPRESSION `case`, and nothing in the tree makes that visible.** The must-assign walk in `check_pgtap_refusal_assertions.mjs` tokenises `case` / `end case` and `loop` / `end loop`, which is right for the statement forms. A plpgsql *expression* `case when … then … end` closes with a bare `end`, so the frame it opens is never popped and the next `end if` pops the wrong frame — everything after it reads as conditional, and in a body with several such the operation narrowing could unwind onto the wrong branch. Measured at this commit: **zero** of the 61 BEFORE INSERT/UPDATE trigger functions that assign `new.<col>` contain `case`, `loop`, `elsif` or an exception block at all, which is why the tree is clean and why nothing would notice if it stopped being. The failure direction is safe today (an unpopped frame over-reports conditionality, which asks for a registry entry rather than skipping one) but the mis-nesting is not inherently safe — a `tg_op` frame popped early hands the wrong operation set to the assignments after it, which under-reports. The durable close is to pop on a bare `end` that no keyword follows; the cheap and honest one is a source guard failing when a trigger body in the population contains `case` or `loop`, so the day it happens is the day it is read. Owner tree: `apps/backend/scripts/`. **CLOSED 2026-09-07** ([decisions § 1538](../architecture/decisions.md)) — the walk now pops on a bare `end`, with `begin` pushing a transparent frame and an `exception when` handler taking it back; a `case`'s arms are separated on `when`, and string payload is masked out of the token scan because `raise exception 'no end if here'` already closed the enclosing `if`.
- [x] **`functionBodies` collapses an overloaded function onto its last definition, and `rpcArgumentLandings` then resolves call sites against the wrong parameter list.** Both the trigger scan and the new RPC scan key on the bare function NAME, so `f(uuid)` and `f(uuid, text)` become one entry and the later `create function` wins. Nothing in this tree overloads a function that writes a table — checked at this commit — and the failure direction is the safe one, because a mismatched parameter list makes the guard ask about columns a call site never reached rather than skip ones it did. It is still an assumption the code does not state and no test pins. Closing it means keying on `(name, arity)` and resolving a call by its own argument count, which is a small change to both the map key and the lookup; the argument-count resolution is already there for the positional binding. Owner tree: `apps/backend/scripts/`. **CLOSED 2026-09-07** ([decisions § 1539](../architecture/decisions.md)) — keyed on `(name, arity)`, with a call site resolved by its own argument count plus named arguments and an ambiguous overload resolving to nothing rather than to a guess. The premise had gone stale: the tree carries 9 overloaded function names and 3 of them write a table, harmless only because in all three the surviving definition was also the last written.
- [x] **The RPC scan resolves an argument to a column but not to a VALUE, so a `lives_ok` that supplies a literal the function then plants verbatim is still unmeasured for anything but a trigger.** § 1486 answers "does the argument reach the table unchanged", which is the question the trigger scan needed. It does not answer the question a read-back answers: is the stored value the one the assertion named. A verbatim landing into a column no trigger stamps is currently silent — correctly, because nothing rewrites it — but a `lives_ok` whose whole content is "this value is accepted" is still weak there for the § 741 reason, since a function that authorised the caller and wrote nothing at all passes it too. `checkpoint_crossings_test`'s organiser-write assertion was exactly that shape until this round and was repaired by hand, not by a guard. The population is worth measuring before it is ruled on: every `lives_ok` whose SQL is a bare `select <writer_fn>(…)` with no read-back in the file. Owner tree: `apps/backend/scripts/`. **CLOSED 2026-09-07** ([decisions § 1540](../architecture/decisions.md)) — the population was measured before it was ruled on: 46 `lives_ok` calls whose whole SQL is one bare call to one of the 109 functions the migrations leave that write a table, of which 45 are read back by some assertion in their own file and 1 is registered with its reason.
- [x] **`docs/architecture/monorepo.md`'s CI table says `build-watch-wear` "declares `GRADLE_UNTESTED` and echoes it", and no workflow declares one any more.** [§ 1439](../architecture/decisions.md) wired up the last excused Gradle project and deleted both the env value and the echo loop, so the row describes a mechanism that is gone; `docs/architecture/decisions.md` § 1393's paragraph reads the same way. Nothing is broken — `check_gradle_test_coverage.mjs` treats an absent declaration as an empty list, which is the state the rail is meant to end in ([§ 1500](../architecture/decisions.md)) — but a reader following the doc looks in `ci.yml` for a block that is not there. One row and one clause. Owner tree: `docs/`. **CLOSED 2026-09-07** ([decisions § 1533](../architecture/decisions.md)). The row now says no workflow declares one and states what a future declaration would owe. Two corrections the filing did not name: the same row gave 767 `@Test` methods, and the `build-mobile-android` row still said it does NOT run that project's JUnit tests, which stopped being true when § 1439 added `./gradlew :app:testDebugUnitTest`. The Wear OS figure was re-measured at integration, after a sibling lane added 14 tests: **816 across 80 files**. § 1393's own paragraph is deliberately left alone — an ADR records what was decided when.
- [x] **`check_gradle_test_coverage.mjs` does not require the echo step it says the declaration is read by, so the guard would pass a `GRADLE_UNTESTED` nobody prints.** [§ 1393](../architecture/decisions.md)'s design is that the excuse is "a declared VALUE the job echoes rather than a note in a document" — the echo is what puts the gap in every run's log. The guard checks the value's shape, its path, its reason, and now its size ([§ 1500](../architecture/decisions.md)), and checks nothing about the step that echoes it: a job could declare the env and print nothing, and the guard would call it declared. Unreachable today because the list is empty, which is exactly why it is worth closing before the next entry lands rather than after. The instrument is the one `check_codeql_coverage.mjs` just gained for `suiteMayDowngrade` — a `run:` step in the same job must reference the declared value — and it costs one check plus two fixtures. Owner tree: `scripts/`. **CLOSED 2026-09-07** ([decisions § 1533](../architecture/decisions.md)). A `run:` script that can SEE the value must read it, scoped the way GitHub scopes an `env:` mapping, and anchored on a variable reference rather than the bare word — so a comment naming the variable earns nothing. Mutation-tested both directions against a declaration planted in the shipped `ci.yml`.
- [x] **`.tool-versions`' `flutter`, `golang`, `rust`, `terraform` and `deno` lines are all commented, so the file a contributor runs `asdf install` against installs Node and nothing else.** `check_toolchain_pins.mjs` compares commented lines too (§ 911), so every one of them is verified against the pin the repo enforces — the values are right. What no rail asks is whether a line is ACTIVE, and six of the seven are not, which means the file passes its guard while materialising one toolchain out of six for anyone who uses it as documented. Either the lines are meant to be live (uncomment them, and the guard already holds them correct) or the file is a pin registry rather than an install manifest and its own header should say so instead of opening with "Run `asdf install` … to materialise everything listed below". Not decided here because it is a contributor-workflow question rather than a CI one, and getting it wrong in the other direction makes `asdf install` try to build five toolchains on a laptop. Owner trees: `.tool-versions` + `scripts/`. **CLOSED 2026-09-07** ([decisions § 1536](../architecture/decisions.md)), decided the second way the filing offered: it is a pin registry, and its header now says so with the reason. The filing's other half mattered more — with the file demoted to a registry, what a contributor installs from is `monorepo.md`'s Prerequisites table, and nothing had ever read it: Flutter `3.19+`, Melos `7.x`, Node.js `20 LTS` against a CI running 3.47.0, 7.8.2 and 24.20.0. `check_toolchain_pins.mjs` now reads those rows.
- [x] **`settings/account.spec.ts`'s change-email mock has never matched, so one case is not hermetic and the other cannot fail.** Both cases in `test.describe('change email — request path')` register `page.route('**/auth/v1/user', …)`. The request never matches it: `handleChangeEmail` calls `supabase.auth.updateUser(…, { emailRedirectTo })`, `@supabase/auth-js`'s `_request` appends that as `?redirect_to=…`, and Playwright anchors a glob at both ends — measured against playwright-core 1.62.1's own `globToRegexPattern`, `**/auth/v1/user` compiles to `^(.*/)auth/v1/user$`, which matches the bare URL and not one carrying a query string. Two consequences. The "valid new address" case falls through to live GoTrue and therefore to the `[auth.hook.send_email]` hook, which is why it fails on any workstation without a live edge runtime while passing in CI ([decisions § 1505](../architecture/decisions.md)). And the "invalid or unchanged address" case's `expect(sawRequest).toBe(false)` is scored against a handler that is never invoked, so that half of its assertion passes whether or not the guard exists — delete the guard in `handleChangeEmail` and the case still goes green. Fix is one pattern (`'**/auth/v1/user*'`, or a `RegExp`, or a predicate), and it must be mutation-tested in the second case's direction: break the guard, and the case must go red. Owner tree: `apps/web/tests-e2e/settings/`. **CLOSED 2026-09-08** ([decisions § 1553](../architecture/decisions.md)). Confirmed against playwright-core 1.62.1's own `globToRegexPattern`: `**/auth/v1/user` compiles to `^(.*/)auth/v1/user$`, which cannot match the `?redirect_to=` the SDK sends. All three registrations are now a `RegExp`, the unchanged-address case ABORTS the PUT rather than continuing it, and the valid-address case asserts its stub fired.
- [x] **`payouts.merchantNote` tells the host they own refunds and chargebacks, and they own neither.** `apps/web/src/lib/i18n/locales/en.ts:4519` reads "You are the merchant of record: you own refunds and chargebacks, and the platform fee is returned when you refund." Every clause but the last is false against the shipped code, and `club_events.md § Refunds`' own pre-deploy checklist already names this: "Chargeback liability is the platform's on a destination charge regardless of that choice. Confirm the host-onboarding copy says so rather than the reverse." It says the reverse. `on_behalf_of` is absent so the platform is the business of record ([decisions § 769](../architecture/decisions.md)); the platform issues the refunds with its own key; and Stripe debits dispute amounts from the platform account for a destination charge *with or without* `on_behalf_of`, so the chargeback half is wrong even if the open `on_behalf_of` question is later decided the host's way. Only the fee sentence survives (`refund_application_fee: true` pushes the fee to the connected account). This is host-facing copy on a money surface, so the rewrite wants the same care as `/terms` §6 — and note it is the one merchant-of-record statement whose *chargeback* clause the pending sign-off cannot vindicate. Seven locales. Owner trees: `apps/web/src/lib/i18n/locales/` + `apps/web/src/routes/settings/payouts/`. **CLOSED 2026-09-07** ([decisions § 1548](../architecture/decisions.md)). All seven web locales rewritten; no mobile ARB carries the key, because there is no Flutter `/settings/payouts`. Pinned by `merchant_note_guard.test.ts`, which deliberately does NOT read the sentence — six of the seven catalogues are not English and a wording-keyed check is escaped by rewording — but reads the two Stripe parameters that make it true: `on_behalf_of` absent from both checkout call sites, `refund_application_fee` and `reverse_transfer` set on refunds.
- [x] **Nothing compares `docs/compliance/retention.md`'s cron table with the migrations that schedule and unschedule those jobs.** [§ 1507](../architecture/decisions.md) found the register listing `cleanup-stale-export-blobs` as running daily nine migrations after `20270709000001` unscheduled it, missing `enqueue-export-blob-reap` (the job that replaced it) and missing `cleanup-account-deletion-receipts` entirely, and the paragraph under the table naming a job (`refresh-mv-weekly-mileage`) that `20270530_001` dropped. Every one of those is mechanically derivable: `cron.schedule('<name>', '<expr>'` and `cron.unschedule('<name>')` are greppable across `apps/backend/supabase/migrations/*.sql`, and a guard can require that the live set and the table's job names agree — a scheduled job absent from the table fails, a table row naming an unscheduled job fails unless it is marked as such, and a cron expression that disagrees with the stated schedule fails. Retention docs are the one class where hand-maintained drift has legal weight, and this table drifted for nine migrations with nobody noticing. The exclusion list (alerts, enqueues, sweeps that delete nothing) needs to be declared by name so the guard's population cannot be escaped by omission, the shape `check_test_inventory_counts.mjs` uses. Owner trees: `scripts/` + `.github/workflows/ci.yml` (the `workflow-lint` job) + `docs/compliance/`. **CLOSED 2026-09-08** ([decisions § 1550](../architecture/decisions.md)) by `scripts/check_retention_cron_register.mjs` + its 22-test suite, invoked from `ci.yml`'s `parity-matrix` job. The job matters: `workflow-lint` is gated on `code == 'true'`, so a docs-only edit to the table — exactly the diff the guard exists to catch — would have skipped it. The live set is derived in APPLY order, since a plain filename sort puts `20270708000010` before `20270708_001`.
- [x] **`account_deletion_receipts` is swept on a schedule but appears in no category row of the retention register.** [§ 1507](../architecture/decisions.md) added it to the register's *jobs* table (hourly, 30-day window, `20270217_001`), but the document's leading "Category | Storage | Retention | Trigger | Notes" table — the one that answers what personal data is kept and for how long — does not mention it. The table holds one row per user, keyed on a hex SHA-256 of the lowercased address, with no FK, and it exists precisely to outlive the deleted account's cascade; the migration's own comment argues it is not a directory of deleted-account addresses because only the hash is stored. Whether a hash of an identifier is personal data for Art 5(1)(e) purposes is a compliance judgement rather than a transcription, which is why this lane added the job row and not the category row. Decide it once and record the reasoning either way — the answer also governs `deletion_audit_log`, whose pseudonymisation intent the migration says it mirrors. Owner tree: `docs/compliance/retention.md` (+ whoever holds the compliance call). **CLOSED 2026-09-07** ([decisions § 1551](../architecture/decisions.md)). Two category rows added: the receipt guard — an unsalted, unkeyed digest of a guessable identifier is pseudonymisation rather than anonymisation, so the 30-day sweep is the Art 5(1)(e) answer — and `deletion_audit_log`, which the same reasoning decides the other way and which the receipt migration wrongly claims to mirror.
- [x] **`20261229_001_paid_events.sql`'s header still explains the money ledger by saying the host is merchant of record.** Line 8: "The host is merchant of record, not the platform or the club." That is the claim [§ 769](../architecture/decisions.md) disproved and [§ 1506](../architecture/decisions.md) has now swept out of the last five documents; the migration header is where it survives, and a migration header is read as an explanation by anyone opening the file to understand `event_orders`. Applied migrations are not edited here as a rule, so this is a judgement call rather than an obvious fix: either a dated one-line correction in place (the file is tracked by version key, so a comment edit changes nothing about how it applies), or a `comment on table event_orders` in a later migration that states the current position, which has the advantage of living in the database where `\d+` shows it. Note the migration's line 128 `comment on ...` mentions "the merchant account", which is the Stripe connected account and is correct — do not sweep on the word. Owner tree: `apps/backend/supabase/migrations/`. **CLOSED 2026-09-07** ([decisions § 1549](../architecture/decisions.md)) by a dated in-place correction quoting what it replaced. DDL byte-identical. Line 128's "the merchant account" left alone as the entry itself instructs.
- [x] **The web `deploy_gate_names.test.ts` justifies its declared exception with a claim it does not measure, which is the defect § 1354 was written about.** Its `KNOWN_EXCEPTIONS` reason ends "Both headers now say so outright (decisions § 1354)" — an assertion about the CONTENT of two files, in a guard that reads both of them for the key and neither of them for the claim. The Dart mirror now measures it ([decisions § 1497](../architecture/decisions.md)): for every declared exception, each side must name the other's key, checked in both directions and mutation-tested both ways. The web half is a four-line addition in the exception branch of "every deploy gate either follows the dropped-prefix convention or is a declared exception", and it passes today only because this lane put `WEIGH_IN_GATE` into the web header. Until it lands, deleting that sentence from the web header would fail the Dart guard and not the web one, which is the wrong asymmetry for a gate on Art 9 data. Owner tree: `apps/web/src/lib/safety/`. **CLOSED 2026-09-07** ([decisions § 1534](../architecture/decisions.md)). Each side must name the other's key, checked in both directions, matching the Dart mirror's § 1497 measurement. Mutation-tested in a sandbox tree rather than by editing the shipped modules, since the Dart half belonged to another lane.
- [x] **Four mobile test files carry no `test_inventory.md` row at all, so their counts are uncovered by construction.** `apps/mobile_android/test/recurrence_test.dart` (30), `gym_prs_test.dart` (35), `deploy_gate_names_test.dart` (4) and `weigh_in_flag_test.dart` (3) have no `### ` heading in the inventory. `scripts/check_test_inventory_counts.mjs` validates the counts that ARE stated — 232 of them across 153 headings, all green — and says nothing about a file that states none, so an unlisted suite can shrink to zero without the census noticing. Found because this lane moved two of the four and had nothing to reconcile. The population rule the guard would need is the one `deploy_gate_names_test.dart` itself applies to gate modules: walk the tree and fail on a file the index omits. That is a bigger change than four rows, so the cheap half (adding the rows) and the durable half (the coverage rule) are worth deciding together. Owner tree: `docs/testing/` + `scripts/`. **CLOSED 2026-09-07** ([decisions § 1535](../architecture/decisions.md)) — but not as filed, because the arithmetic does not survive measurement: **493 of the 553 mobile suites carry no `### ` heading**, not four, and that is the census's design, since the headings index the web suite and name the Dart mirror in prose. The four rows were refused and the rule turned round instead: **no test file may declare ZERO**, over 2,219 suites across four naming conventions plus pgTAP, with one exemption. Closing it required teaching the Dart counter `realtimeWidgetTest`, a `testWidgets` wrapper the census's own prescribed `grep` reports 0 for — a blind spot hiding 16 committed suites and one wrong figure.
- [x] **The exercise key and the display fold part company on 25 code points, and the first surface to render a shadowed pair as a group will inherit it.** **CLOSED 2026-09-08** ([decisions § 1565](../architecture/decisions.md)). [decisions § 1496](../architecture/decisions.md) measures the boundary and pins it, and deliberately changes no behaviour: the two functions answer different questions (§ 1334) and widening the fold would reach the region dedupe, the routes list, `club_slug` and the `food_search` dedupe key, all of which refuse compatibility equivalences on purpose (§ 856). What is still owed is the SURFACE rule, for the day one exists: a list that presents shadowed rows as one exercise must group by `normaliseExerciseName` and order the groups by `fold`, not rely on the fold to place same-key rows together — because it does not, and the gap between them is the whole block of names sharing the first word. Filed with the measurement already done so the author does not re-derive it. Owner trees: `apps/web/src/lib/gym/` + `apps/mobile_android/lib/screens/`.
- [x] **The PreRun arc says "Retry N" for a 401 the refresh cannot repair, which is a tap that can never fire.** `classifyDrainError` reads a 401 as `RetryAfterRefresh`; when the refresh fails, `drainQueueLoop` sets `anyTransientFailure` and `drainQueueLocked` publishes `syncFailed = true`, so `syncChipState` resolves `RetryQueued` and the chip reads "Retry N" with the content description "The last sync did not get through. Tap to try again." Every tap re-runs the same drain, 401s again, fails the same refresh. The remedy is signing in again, which the arc's own `SyncFault.SignInRequired` now knows about but the chip does not — `syncFailed` is a bare boolean and the fault rides a separate field that renders only on PostRun. `syncChipState` is the right place to decide it and the reason this is filed rather than taken: it would be a seventh input, doubling `SyncChipStateTest`'s state space from 64 tuples to 128, and it needs a slot of its own with its own label, content description and seven-catalogue cost — a change on the scale of [decisions § 1390](../architecture/decisions.md), not a tail-of-round sweep. The honest interim state is that the chip is merely unhelpful rather than wrong: it is enabled, it does fire, and it reports failure each time. Owner tree: `apps/watch_wear/`. **CLOSED 2026-09-08** ([decisions § 1544](../architecture/decisions.md)). The pass now names what stopped it rather than that it stopped: `DrainQueueLoopResult.blockedBy` carries the fault, `anyTransientFailure` survives as a derived `val` off it so backoff and the arc cannot disagree, and a sixth `SyncChipState.SignInRequired` takes the counted chip's slot rather than relabelling it.
- [x] **`AuthFault.SignInRequired`'s sibling on the sync side has no affordance on the screen that shows it.** `SyncFault.SignInRequired` renders on `PostRunScreen` as "Sign in again to sync", and PostRun carries no sign-in control — the runner has to press Next to reach PreRun's sign-in chip, which the banner does not say. Small, and deliberately not taken here because the two candidate fixes are both bigger than the sentence: adding a sign-in route to PostRun puts a settings affordance on a recording surface (the `apps/watch_wear/CLAUDE.md` scope rule argues against it), and rewording the string to name the Next button couples a catalogued sentence to a control's position. Worth deciding once. Owner tree: `apps/watch_wear/`. **CLOSED 2026-09-08** ([decisions § 1545](../architecture/decisions.md)), by neither of the two candidates the filing priced: the primary control is REPLACED rather than joined, so no affordance is added to the recording surface and a dead one is removed, and the catalogued string is untouched.
- [x] **Nothing holds `DrainQueueLoopResult.failures` to being logged, so the § 1490 diagnostic can be dropped silently.** The classified fault reaching the wrist is pinned by `SyncFaultTest` and the compiler; the raw throwable reaching `Log.e` is pinned by nothing at all. A refactor that stops iterating `result.failures` in `drainQueueLocked` loses the only diagnostic a wrist ever produces, and every test still passes — which is exactly the state the field was in before this round, one level down. The cheap version is a source grep in `ViewModelStreamResilienceTest` (the file that already guards the drain's other logging obligations); the durable version is making the loop take a reporter seam it cannot be constructed without, which costs an argument at 23 existing call sites in `DrainQueueLoopTest`. Priced here so a later round does not re-derive it. Owner tree: `apps/watch_wear/`. **CLOSED 2026-09-08** ([decisions § 1543](../architecture/decisions.md)), taking the durable option the filing priced rather than the cheap one. `failures` is gone from the result; `drainQueueLoop` takes a required `DrainFailureReport` with no default and reports each failure as it happens. A required argument only proves a reporter is GIVEN, so a structural guard parses the call and fails unless the body logs at ERROR and names the error — which surfaced that the view model's `refresh` lambda ended `catch (_: Throwable) { false }`, leaving the one drain fault whose remedy is an action on the wrist with no diagnostic at all.
- [x] **`fetchRuns` is the one narrowed read in `data.ts` whose select the typed client cannot check, and `rows: any[]` is the consequence.** Its columns are a caller's parameter rather than a module constant, so the joined list is `string`, supabase-js's select parser answers `GenericStringError`, and the result has to be walked as `any[]`. Every other narrowed read here — `fetchRunsForDashboard`, both route-list reads — takes a `as const` tuple that `Join<T, D>` re-states as a real literal, and those infer. The check `fetchRuns` does get is at the tuple's own `satisfies RunColumns`, which since § 1468 refuses a name that is not a column, and the row type is `Pick`ed from the same tuple — so the two halves cannot drift; what is missing is the parser's own reading of the row. The durable close is to make the implementation generic over `C` and hand the select `opts.columns.join(SELECT_SEPARATOR) as Join<C, typeof SELECT_SEPARATOR>`, which is a deferred template-literal type rather than `string` — measure whether supabase-js's `ParseQuery` machinery resolves it before committing to that shape, because if it defers to `GenericStringError` anyway the honest answer is to leave the join and keep the comment that says why. All four callers already pass module-level `as const` tuples (`RECAP_RUN_COLUMNS`, `DASHBOARD_RUN_COLUMNS`, `PERIOD_SUMMARY_RUN_COLUMNS`, and the gear page's), so the alternative — a per-projection sibling with a literal select, the way `fetchRunsForDashboard` is one — is also reachable. Owner tree: `apps/web/src/lib/core/`. **CLOSED 2026-09-07** by MEASUREMENT plus a different fix than the one proposed ([decisions § 1518](../architecture/decisions.md)): the generic `Join<C, D>` was probed and is unreachable — it defers rather than resolving, and the body cannot then read the row at all. What was fixed is the `any[]` and what it hid ([decisions § 1519](../architecture/decisions.md)).
- [x] **`exercise_key_source_guard.test.ts` spares `lib/core/data.ts` from the blankness scan, and the exemption is now redundant for that half.** The spare exists because the scan's heuristic is file-scoped and `data.ts` holds club, checkpoint and meal-template names beside its exercise ones; § 1466 answered it locally with a receiver-scoped scan in `data.test.ts` that reports a blankness test whose receiver names an exercise. The two now overlap, which means a future editor reading the tree-wide guard still sees `data.ts` listed as un-scanned and cannot tell that it is covered elsewhere. Either narrow the spare to a note pointing at the local guard, or adopt the receiver-scoped scan tree-wide and delete the file-scoped heuristic — the receiver form has no false positives on `data.ts` today and is the stricter of the two. Owner tree: `apps/web/src/lib/gym/`. **CLOSED 2026-09-07** ([decisions § 1508](../architecture/decisions.md) + [decisions § 1509](../architecture/decisions.md)), and the filing's premise was half wrong in a way worth recording: the exemption was not merely redundant, it was load-bearing cover. `blankSpellingTestHits` recognised only `x === ''` and `x.length === 0`, so two of the three writes § 1367 fixed were spelled `.trim().length > 0` and one `!exerciseName.trim()` — invisible tree-wide. Extended and run against the tree it found exactly one offender and zero false positives across 2,572 files. `rawNameComparisonHits`' blanket skip of broad modules is gone; it never had a file-level rule to waive.
- [x] **`fetchRuns` sets `track: null` on every row it returns, including the narrowed reads whose row type does not declare it.** The unnarrowed branch is right — `Run.track` is a lazy Storage download and null is the honest value — but the narrowed overload returns `Pick<Run, C[number]>`, and since § 1468 `track` cannot be one of those keys, so the object carries a property its type does not declare and no caller can read. `fetchRunsForDashboard` states the opposite rule for its own read ("`track` is NOT set — it is not one of the ten columns"), so the two run readers disagree about what a narrowed row carries. Harmless today (an excess property is invisible to a structural consumer) but it is a claim about the shape that is false, and the two readers should answer the same way. Owner tree: `apps/web/src/lib/core/`. **CLOSED 2026-09-07** ([decisions § 1520](../architecture/decisions.md)). `Pick<Run, C[number]>` cannot declare `track` since § 1468, so the key was one no caller could read through the type; `fetchRunsForDashboard`'s own doc comment contradicted itself about it two paragraphs apart and is corrected.
- [ ] **`import_failures`'s web half orders with a collation its Dart half cannot run, and it is the only registered pair that does.** Measured across all 125 pairs in `.claude/agents/shared-library-syncer.md` by the guard [§ 1482](../architecture/decisions.md) adds: `groupImportFailures` orders equal-count buckets with `a.reason.localeCompare(b.reason)` where `import_failures.dart` uses `a.reason.wire.compareTo(b.reason.wire)`. Not a live divergence, and the reason is worth keeping: the underscore is the only character the two instruments rank differently over this vocabulary, and none of the seven reasons (`auth`, `network`, `rate_limited`, `rejected`, `too_large`, `unknown`, `unparseable`) is ordered against another by one — so the two rails agree today and stop agreeing the moment an eighth reason lands whose ordering the underscore decides. The fix is one line, `a.reason < b.reason ? -1 : 1` beside the existing count term, and it is carried in the guard as a stated PENDING exemption with a staleness test until then. Owner tree: `apps/web/src/lib/integrations/`.
- [x] **The phone's exercise catalogue has both defects [§ 1481](../architecture/decisions.md) closed on web, and one more that only it has.** Three sites, none of which applies the shadow precedence rule and all of which the web tree now does: (1) `ApiClient.fetchExerciseCatalogue` returns the shadowed pair unresolved and still degrades a failed read to `[]`, so the mobile picker offers to create a name the catalogue already holds — the standing item filed by round 43; (2) `apps/mobile_android/lib/widgets/gym_compose_sheet.dart:175` does `_catalogue = [...widget.catalogue]` in `initState`, the exact mount-time snapshot of an asynchronously-filled prop the web picker carried, so a catalogue that lands after the sheet opens is never seen and `_catalogueByKey` binds every typed name to nothing; (3) the same file merges a created custom at `:337` with `if (!_catalogue.any((e) => e.id == created.id))`, an id test that cannot see a shadow, because the created custom and the global it shadows are two ids under one folded key. The api-client lane has written the Dart half of `dedupeShadowedExercises` this round, which is what makes `exercise_catalogue.ts` a registered parity pair — so the registry edit (`CLAUDE.md` + the syncer table) and the remaining two call sites above are what is left. Owner trees: `packages/api_client/` + `apps/mobile_android/lib/widgets/` + `apps/mobile_android/lib/screens/` + both registries (+ the iOS twin). **CLOSED 2026-09-07** ([decisions § 1513](../architecture/decisions.md) + [decisions § 1514](../architecture/decisions.md)). Site (1) was already closed and the registry edit had landed; the composer's mount-time prop snapshot and its `id`-keyed merge are closed here, the second by reducing through the registered `dedupeShadowedExercises` rather than by a second copy of the rule.
- [x] **The picker's prop-tracking fix has no behavioural pin, which is the third instance of the gap § 1278 filed.** **CLOSED 2026-09-08** ([decisions § 1572](../architecture/decisions.md)). [§ 1481](../architecture/decisions.md) is a reactivity property of a Svelte component, so the `tsx --test` suite cannot compile it and no source guard can honestly express it — seeding editable local state from a prop with `untrack` is the CORRECT pattern in five other components in this tree (`GymEditor`'s `seed`, `RoutineEditor`'s `title`, `PeriodSummary`'s `type` / `anchor`, `GymSessionRunner`'s restore), so a blanket ban would fail correct code. What is owed is a Playwright case in the existing `tests-e2e/gym/exercise-catalogue-picker.spec.ts`: open the browse modal before the catalogue read resolves, let it resolve, and assert the seeded list renders and `catalogue-create` is absent for a seeded name. It fails against the pre-§ 1481 component. Not written here rather than written unexecuted — this lane was not permitted to run Playwright, and an unexecuted spec is worse than none. Owner tree: `apps/web/tests-e2e/gym/`.
- [x] **Five great-circle copies remain in this lane's own trees, and each needs its Dart half to move in the same change.** `integrations/race_match.ts`, `routes/privacy.ts`, `routes/roadbook.ts`, `routes/route_description.ts` and `routes/turn_cues.ts` each still compute their own arc, and so does the Dart half of each pair — `race_match.dart:haversineMetres` (public, so its body becomes a delegation and the export stays), `privacy.dart:_haversine`, `roadbook.dart:_haversineM`, `route_description.dart:_haversineM`, `turn_cues.dart:_haversineM`. Moving only the web half is what the `addDays` registration in `check_shared_reimplementations.mjs` refuses, so all ten files move together: delete the private function, `import 'run_stats.dart' show haversineMetres;` on the Dart side and `import { haversineMetres } from '../runs/run_stats';` on the web side, reorder the arguments where the signature differs, mirror each Dart file to `apps/mobile_ios` byte-identically, and delete the file's row from `routes/great_circle_sources.test.ts` — the guard fails on a row whose file no longer computes an arc, so the bookkeeping cannot be forgotten. `privacy` is the one where the behaviour changes: both halves are the unclamped `atan2` today, so the matched move adds § 305's clamp on both sides at once. Not taken here because it is five pairs across both platforms and this lane could run one targeted Flutter file at a time, and one of five would have been arbitrary. Owner trees: `apps/web/src/lib/routes/`, `apps/web/src/lib/integrations/`, `apps/mobile_android/lib/` (+ the iOS twin). **CLOSED 2026-09-08** ([decisions § 1523](../architecture/decisions.md)). All nine non-canonical copies under `apps/web/src` moved onto the clamped `run_stats.ts:haversineMetres`, each registered pair's Dart half in the same commit and mirrored to iOS.
- [x] **`segments/pace_segments.ts` holds the last unclamped great-circle arc in the tree, and it is the live end of § 305.** `2 * r * Math.asin(Math.sqrt(h))` with no clamp of any kind is NaN for `h > 1` exactly as the `atan2` form is — the one remaining copy where the § 305 near-miss can still fire, since the other seven survivors either clamp with `Math.min(1, …)` or are in a component. `pace_segments` is half of a registered pair, so this is a matched move like the five above, and it is a fix rather than a tidy. `segments/segments.ts` carries a clamped copy in the same tree and moves with it (`segments.dart:_haversine` on the other side). Owner trees: `apps/web/src/lib/segments/`, `apps/mobile_android/lib/` (+ the iOS twin). **CLOSED 2026-09-08** ([decisions § 1523](../architecture/decisions.md)). Two of the moves were behaviour rather than tidying — `privacy` (unclamped `atan2` on both platforms) and `pace_segments` (unclamped `asin` on both) gained § 305's clamp on both sides at once.
- [x] **`RouteBuilder.svelte` holds a private great-circle copy beside an import of the shared one, and `RunSegmentEfforts.svelte` computes one inline.** `RouteBuilder.svelte` already imports `haversineM as routingHaversineM` from `routes/routing_quality`, which now delegates to the canonical, while keeping its own unclamped `atan2` at line 1076 for a different set of call sites — so one component measures the same thing two ways. `RunSegmentEfforts.svelte`'s arc is inside the effort loop rather than in a named function, which is why `check_shared_reimplementations.mjs` cannot see it at all (it does not extract anonymous bodies) and why `routes/great_circle_sources.test.ts` had to read the source rather than the function set. Neither has a Dart twin, so both are one-sided deletions: import `haversineMetres` from `$lib/runs/run_stats` and delete the row from the guard's table. Owner tree: `apps/web/src/lib/components/`. **CLOSED 2026-09-08** ([decisions § 1523](../architecture/decisions.md)). The web guard table is down to its canonical row.
- [x] **`route_loop`'s two halves were measuring distance with different functions while its registry entry claimed nine decimal places of agreement.** Found while consolidating, and closed as a side effect rather than deliberately: `route_loop.dart` imports the clamped `haversineMetres` from `run_stats.dart`, while `route_loop.ts` reached the unclamped `atan2` through `routing_quality.haversineM` — which now delegates to the same function, so the two agree by construction today. What is worth recording is that the pair's registry entry says the halves "were measured identical to nine decimal places over 5,543 inputs", and a nine-decimal claim about metres is 1e-9 m while the two forms can differ by up to 8.2e-8 m: the measurement was taken over loop-scale inputs where the divergence is nearer 1e-11 m, so the claim was true of the sample and not of the functions. No action is owed on the code; the entry's number should be read as a property of the sweep, not a bound on the pair. Owner tree: `docs/architecture/` (whoever next edits the `route_loop` registry entry). **CLOSED 2026-09-08** ([decisions § 1526](../architecture/decisions.md)). Read the nine-decimal figure as a property of the SWEEP rather than as a bound on the pair.
- [x] **`fastestWindowSeconds` compares an accumulated float sum against the window exactly, so a track that IS exactly the distance can report no best effort.** Surfaced by § 1473 and deliberately not fixed there. A hundred-leg cumulative haversine carries about 4e-12 m of rounding, and the comparison `cum[j] - cum[i] < windowMetres` has no tolerance, so whether a nominally-10.00 km track yields a `fastest_10k_s` is decided by the last bit. Unreachable in the field — no real GPS trace lands on a window boundary to within a picometre, and the two importers that call this both read third-party tracks — which is why the fixture moved rather than the algorithm. Worth a relative epsilon (the window times 1e-9, say) the next time this function is opened for another reason, together with a case that pins the exactly-on-the-boundary track. Owner tree: `apps/web/src/lib/integrations/`. **CLOSED 2026-09-08** ([decisions § 1525](../architecture/decisions.md)), and it was worse than filed: the boundary is reachable with the suite's own fixture — fifty 100 m equator legs sum to 4999.9999999999982 m and returned no `fastest_5k_s`, while the Deno importer's unclamped `atan2` summed the SAME legs to 5000.0000000000018 m, so it wrote a 5 km best for a track the phone found none in, on one ULP. `WINDOW_TOLERANCE_RATIO` = 1e-9 landed on all three rails and is registered in `check_shared_constants.mjs`.
- [x] **`SeoHead.svelte`'s head comment names `escapeJsonLd`, a function that no longer exists.** `apps/web/src/lib/components/SeoHead.svelte:14` says the JSON-LD payloads "run escapeJsonLd" before reaching `{@html}`. The claim is still true of the behaviour and false of the name — it is `serialiseJsonLd` from `$lib/util/json_ld` since [§ 1475](../architecture/decisions.md), and that function stringifies as well as escapes, so a reader following the comment looks for a step the builders no longer have. One word plus the module path. Not taken by the lane that made it stale: `apps/web/src/lib/components/` was another lane's tree this round. Owner tree: `apps/web/src/lib/components/`. **CLOSED 2026-09-07** ([decisions § 1531](../architecture/decisions.md)). The serialiser is `serialiseJsonLd`; `og_route_image.ts`'s claim of a wrap at ~30 characters and a 90-character cap named neither a wrap nor a cap that exists.
- [x] **The `<head>` text budgets are counted in UTF-16 code units and read by humans in characters, so an emoji costs double.** [§ 1478](../architecture/decisions.md) stopped `clipText` splitting a surrogate pair but deliberately left the UNIT alone, because changing it would have been a behaviour change beyond the defect. The consequence is that a 160-emoji club description is cut to 79 emoji against a budget meant to approximate the ~155 characters Google renders, while a 160-character CJK description keeps all 160 — the same budget means two different things depending on the script. `char_length` in Postgres, the `text_limits.ts` caps it mirrors, and the crawler all count code points; only this clipper counts code units. Deciding it needs a measurement of what the unfurl consumers actually truncate on, which is why it is filed rather than guessed. Owner tree: `apps/web/src/lib/util/`. **CLOSED 2026-09-07** ([decisions § 1528](../architecture/decisions.md) + [decisions § 1529](../architecture/decisions.md)), together with the split-cluster entry below, as that entry said they should be: the budget and the cut are one change. Deliberately NOT what the filing literally asked for — consumers truncate on PIXEL width, so the clipper's number is a ceiling on what we EMIT rather than a prediction of what an unfurl shows.
- [x] **`clipText` can still split a grapheme cluster, and nothing measures how often.** Stated in the module and in [§ 1478](../architecture/decisions.md) as the deliberate floor: the cut guarantees a well-formed string, not an unchanged glyph, so a ZWJ sequence (a flag, a profession emoji, a family) or a base letter and its combining mark can be separated — the result is valid text that renders as something the author did not write. The honest fix is `Intl.Segmenter` with a budget in graphemes, which is the same decision as the code-unit filing above and should be taken with it rather than separately. Deliberately not attempted here: it changes the budget's meaning, and the round's evidence was about well-formedness. Owner tree: `apps/web/src/lib/util/`. **CLOSED 2026-09-07** ([decisions § 1528](../architecture/decisions.md) + [decisions § 1529](../architecture/decisions.md)). The cut lands only on a cluster boundary, so a ZWJ family, a flag pair, a skin-tone modifier and a base letter with its combining mark survive whole or are dropped whole, and § 1478's well-formedness falls out by construction. The fast path is unchanged, so no segmentation happens on any real call, and an absent `Intl.Segmenter` falls back to the previous code-unit cut, pinned by a case that deletes the constructor.

## Surfaced by the #789 round 46 lanes (2026-09-08)

- [x] **The Dart exercise-key source guard cannot see the two blankness shapes the web one gained, and its raw-comparison scan may carry the same blanket waiver.** **CLOSED 2026-09-08** ([decisions § 1567](../architecture/decisions.md)). `apps/web/src/lib/gym/exercise_key_source_guard.test.ts` now reads blankness written as an ordering comparison (`.trim().length > 0`, `.length < 1`) and as a bare negated `.trim()`, and consults the enclosing top-level declaration's name where the file's says nothing ([decisions § 1508](../architecture/decisions.md)). `apps/mobile_android/test/exercise_key_source_guard_test.dart` has none of that. Its analogues are not the same expressions — Dart asks the question with `.isEmpty` / `.isNotEmpty` member reads ([§ 1369](../architecture/decisions.md)) — so the port is: `.length > 0` / `< 1` beside the existing `== 0`, `if (!x.trim().isNotEmpty)` and the plain `x.trim().isEmpty` guard shape, and the declaration rule (Dart's top-level `Foo bar(` / `void _baz(` at column 0, plus a class member one indent in). Worth doing with a sweep of `apps/mobile_android/lib/` for the shapes first: the web sweep found one live instance the guard had never been able to see. The raw-comparison half should also be checked for the blanket broad-module `return []` the web one carried, which spared a receiver-scoped scan for a file-level reason it never had ([§ 1509](../architecture/decisions.md)). Owner trees: `apps/mobile_android/test/` + the iOS twin.
- [x] **`packages/core_models/lib/src/exercise_catalogue.dart`'s header still describes the web half as folding the display name.** **CLOSED 2026-09-08** ([decisions § 1568](../architecture/decisions.md)). Lines 30-35 say the key is the row's own `name_key` "where web folds `name` through `normaliseExerciseName`", and give the divergence's rationale — which is the same stale claim [§ 1510](../architecture/decisions.md) corrected in both registries, in the one place a reader of the Dart half will actually look. The web half has read `e.name_key` since commit `02926ac8f`. One paragraph; the rationale stays, it just stops being a contrast. Not taken here because `packages/` is outside this lane. Owner tree: `packages/core_models/`.
- [x] **`fetchExerciseSetHistoryBatch`'s blankness decision is reachable by no source scan, on either platform.** **CLOSED 2026-09-08** ([decisions § 1566](../architecture/decisions.md)). Its subject is an arrow parameter re-bound from a `.map` (`names.map(...).filter((n) => …)`), so neither the receiver rule, the declaration chase nor the enclosing-declaration rule can name it — measured while closing [§ 1508](../architecture/decisions.md). The cover is the positive assertion in `data.test.ts`, which asserts `namesAnExercise(` appears in the function body and is mutation-tested; that is strictly weaker than the scan, because a body could hold the call and still decide on the spelling elsewhere. Two durable options: teach `originOf` to resolve an arrow parameter to the collection it iterates (real work, and the same limit exists on the Dart rail), or move the filter into a named exported helper the scan's receiver rule can see. Neither is urgent — the call site is correct today and pinned — but the limit should be stated somewhere a future editor of that function will find it. Owner trees: `apps/web/src/lib/gym/` + `apps/web/src/lib/core/`.
- [ ] **The round-46 lanes share one scratchpad directory, and a `.bak` written by one lane can be restored into another lane's worktree.** `/tmp/claude-1000/-home-jhoward-github-project-running/<session>/scratchpad/` is keyed on the SESSION, not the lane, so all ten lanes resolve it to the same path — observed holding `data.ts.bak`, `account.page.svelte.bak`, `run_narrow.ts.bak`, `patch_gradle.py` and twenty more from lanes that never spoke to each other. This lane wrote `data.ts.bak` for a mutation test, the interactive `cp -i` alias silently refused to overwrite the file another lane had already put there, and the restore step then copied THAT lane's `data.ts` into this worktree — an in-flight `run_narrow.ts` / `overrideTypes` change from a tree it does not own. Recovered with `git checkout HEAD --`, and it would have been committed unnoticed by a lane that did not diff before committing. The durable fix is a per-lane subdirectory in the round harness (`.../scratchpad/<lane>/`); the interim rule is that no lane writes a bare filename into the shared scratchpad. Owner: the round harness / `CLAUDE.md § Working alongside other Claude sessions`.
- [x] **The picker's prop-tracking fix has no behavioural pin on WEB; the phone's half of that gap is now closed.** **CLOSED 2026-09-08** ([decisions § 1572](../architecture/decisions.md)). This is the residual of the round-45 filing of the same name. Its owner tree is `apps/web/tests-e2e/gym/` and the pin it asks for is a Playwright case — open the browse modal before the catalogue read resolves, let it resolve, assert the seeded list renders and `catalogue-create` is absent for a seeded name — which this lane was not permitted to run, and an unexecuted spec is worse than none. The MOBILE analogue is closed: `gym_compose_sheet_test.dart` now pins that a catalogue landing after the sheet mounts still binds a typed name to its id, mutation-tested against the mount-time snapshot it replaced ([decisions § 1513](../architecture/decisions.md)), and Flutter renders widgets natively so no equivalent harness gap exists there. Owner tree: `apps/web/tests-e2e/gym/`.
- [x] **The mobile catalogue picker is pushed as a route with a snapshot of the catalogue, so it alone cannot track a late-arriving read.** **CLOSED 2026-09-08** ([decisions § 1571](../architecture/decisions.md)). `gym_compose_sheet.dart`'s `_openPicker` builds `ExerciseCataloguePickerScreen(catalogue: _catalogue, …)` inside a `MaterialPageRoute` builder that runs at push time and is not rebuilt when the composer's own state changes, so the value the picker holds is fixed for the life of that route. The picker itself is correct — it reads `widget.catalogue` on every build since [decisions § 1513](../architecture/decisions.md) — and the composer is correct, so the residual is only the seam between them, and it is far narrower than the defect § 1513 closed: the window is a user opening browse during the seconds a catalogue read is still in flight, on a screen that fetches on mount before the composer can be reached. Web has no analogue because its picker lives in a `Modal` inside the editor and takes a reactive prop. The durable fix is to carry the catalogue into the route as a `ValueListenable` (or rebuild the route's content off the composer's state), which is a change to one seam and not to either widget. Owner tree: `apps/mobile_android/lib/widgets/` + the iOS twin.
- [x] **The Dart restore has the same unknown-column gap the web one just closed, and a pre-`20261206_001` archive still fails every run it carries there.** **CLOSED 2026-09-08** ([decisions § 1564](../architecture/decisions.md)). The web orchestrator now filters each archived row against the generated `Insert` keys before sending it ([decisions § 1521](../architecture/decisions.md)), so a column this schema has no home for — `runs.kind`, dropped by `20261206_001` — is dropped rather than failing the whole row as a PGRST204 AFTER that run's track blob has been uploaded to Storage. The mobile restore is the same shape and has none of it: `restore_orchestrator.ts`'s own header says it "mirrors the mobile shape", and the on-disk format is pinned across platforms by `apps/mobile_android/test/backup_format_compat_test.dart` — so the same archive that now restores on the web still imports zero runs and orphans one Storage object per run on the phone. The Dart half needs the same allowlist, and it needs a different mechanism for the coverage half of it: `satisfies Record<keyof Insertable<T>, true>` is what makes a newly-added column fail the build rather than being silently discarded, and Dart has no structural analogue — the generated `db_rows.dart` is the place to derive it from, since `scripts/gen_dart_models.dart` already parses every `create table` / `add column` / `drop column`. Owner tree: `apps/mobile_android/lib/` (+ the iOS twin), `scripts/`.
- [x] **`fetchRuns`' unnarrowed branch asserts its own completeness, because the select is a ternary and no literal survives it.** **CLOSED 2026-09-08** ([decisions § 1560](../architecture/decisions.md)). `select(opts?.columns ? opts.columns.join(SELECT_SEPARATOR) : '*')` is a union of `string` and `'*'`, which is `string`, so the parser reads nothing off EITHER arm — including the `'*'` one, which is a literal and would infer the whole row on its own ([§ 1518](../architecture/decisions.md) measured the concrete-tuple case answering `{ id: string; started_at: string; distance_m: number }[]`). The rows are read as `Partial<RunRow>[]` and the unnarrowed branch states `rows as RunRow[]` in one line, which is honest and reviewable but is still the last assertion in the function. Splitting the two reads so the `*` branch hands `.select('*')` a literal of its own would delete it — the cost is that `build()`, the paging loop past PostgREST's 1000-row cap, `throwOnError` and the `started_at`/`id` secondary key would have to be shared by a helper generic in the row type rather than by a closure, and whether `overrideTypes<R[]>` behaves under an uninstantiated `R` is the thing to measure first, since § 1518 is a record of exactly that kind of deferral not working. Owner tree: `apps/web/src/lib/core/`.
- [x] **A restored `user_profiles.handle` collides with the unique index, and the restore has no answer for it.** **CLOSED 2026-09-08** ([decisions § 1563](../architecture/decisions.md)). `stripServerManagedProfileFields` drops `subscription_tier` / `subscription_at` / `parkrun_number`, and the new allowlist admits everything else the column set has — including `handle`, which `20270424000002` makes unique. Restoring an archive into a FRESH account (the documented wipe-and-restore path: delete the account, sign up again, import) therefore either re-claims a handle the deleted account released, or fails the profile row outright if anyone has taken it since; restoring into a DIFFERENT account fails it always. The row is now filtered rather than sent whole, so the fix has a natural home — either strip `handle` beside the other three and let the runner re-pick it, or resolve the collision by suffixing — but which of those is right is a product call about whether a handle is part of the backup at all, and it should be recorded next to the § 33-style contract rather than decided in the adapter. Today it presents as one opaque 23505 in `result.warnings`, which the account page renders as a bare count. Owner tree: `apps/web/src/lib/backup/` + `apps/mobile_android/lib/`.
- [x] **No consumer of a narrowed `fetchRuns` row can read `track` through its type any more, but nothing pins that none reads it at runtime.** **CLOSED 2026-09-08** ([decisions § 1561](../architecture/decisions.md)). § 1520 removed the blanket `track: null` from the projected branch, which is right: `Pick<Run, C[number]>` cannot declare `track` since § 1468, so the key was invisible to every structural consumer and no caller could read it through the type. What was not swept is whether any consumer reaches a projected row through an `as`, a `JsonObject` index or a `Run`-typed parameter and reads `.track` off it at runtime — `PlanEditor`, `/nutrition`, `/nutrition/targets` and the gear page each pass their rows into a shared helper, and `recentRunVolume` / `gearBackfillCandidates` take structural bounds that would not object. Measured statically the answer looks like "none", but the honest close is a source guard that no consumer of a `fetchRuns({ columns })` result names `track`, which is cheap and would also pin the § 1468 rule at the call sites rather than only at the tuple. Owner tree: `apps/web/src/lib/core/`.
- [x] **`race_match`'s exported `haversineMetres` has no production consumer on either platform, and now that its body is a delegation it is an adapter whose only caller is its own test.** **CLOSED 2026-09-08** ([decisions § 1578](../architecture/decisions.md)). Both halves are public "so a caller can compute `distance_m_away` when the RPC didn't", and no caller does: on web the only importer of the symbol is `integrations/race_match.test.ts`, on Dart only `test/race_match_test.dart` (`race_service.dart` and `widgets/run_race_section.dart` import the module but not this function). § 1523 kept the export because the followup that scheduled the move prescribed keeping it and because removing a public symbol from a registered pair is a scope expansion no filing asked for — but the house rule is "if something's unused, delete it", and the honest options are now two: delete it from both halves plus the four tests, or give it the caller its doc comment describes. Deciding is cheap; deciding by default is what leaves an adapter and two tests certifying `haversineMetres` a third time. Owner trees: `apps/web/src/lib/integrations/`, `apps/mobile_android/lib/` (+ the iOS twin).
- [x] **`apps/backend` has no great-circle census, so the importer's arc is pinned by one test rather than by a guard.** **CLOSED 2026-09-08** ([decisions § 1577](../architecture/decisions.md)). `routes/great_circle_sources.test.ts` scans `apps/web/src` and `test/great_circle_sources_test.dart` scans `apps/mobile_android/lib`; nothing scans `apps/backend/supabase/functions`, where `_shared/strava.ts:embeddedHaversineM` lives. § 1525 unified it with the canonical and pinned it to an exact fifty-leg sum, and `check_shared_constants.mjs` pins the tolerance beside it, so the one copy that exists today is covered — but a SECOND copy added to another Edge Function would be invisible to all three, which is exactly the shape § 1524 records for the phone. The scan is the same anchor as the two existing ones (an `asin`/`atan2` of a square root) over a third root, and it belongs in the Deno suite rather than in a `.test.ts` under `apps/web`, because that is where the tree it scans is tested. Owner tree: `apps/backend/supabase/functions/`.
- [x] **`watch_core::haversine_metres_f32` does not clamp, and its consumers are the per-fix ones.** **CLOSED 2026-09-08** ([decisions § 1576](../architecture/decisions.md)). Its `f64` sibling clamps `a` into `[0, 1]` before the arc and says at length why (§ 305, restated on the function); the `f32` copy right below it computes `libm::atan2f(sqrtf(a), sqrtf(1.0 - a))` with no clamp, so an `a` rounding past 1 gives `sqrtf` of a negative and a NaN. `trackback.rs` is the only caller — the anchor-separation test at `:393` and the leg distance at `:448` — and a NaN there fails every `>` comparison, so a breadcrumb leg silently never accumulates and back-to-start under-reports the way home. In `f32` the argument reaches 1 far more easily than in `f64` (~1e-7 relative precision against ~1e-16), so this is the *more* exposed of the two copies, not the less. The fix is one `.clamp(0.0, 1.0)`; what needs checking first is whether any pinned `trackback` figure moves, since the clamp is identity for every `a` below 1 and should move none. Owner tree: `apps/custom_watch/core/src/`.
- [x] **`share_head_escaping.test.ts` censuses nine renderers by import and would not notice a tenth.** **CLOSED 2026-09-08** ([decisions § 1581](../architecture/decisions.md)). Its `cases` array is hand-maintained and enumerated by `import` rather than by reading the directory, so a `renderShare<X>HeadTags` added tomorrow is escaped by nobody's assertion — the exact shape [§ 1476](../architecture/decisions.md) built the JSON-LD census to close, and [§ 1531](../architecture/decisions.md) built the clipping census to close, one file over. The clipping census's staleness half is the pattern to copy: scan the directory for `export function renderShare\w+HeadTags`, require every match to be named, and fail in both directions. All nine renderers that exist today ARE listed, so this is a gap in what the file can promise rather than a hole in what it covers; the concrete sign that the two sets already drift is that there are ten `buildShare*` builders and nine renderers — the badge entity returns a `ShareRunMeta` and is rendered by `renderShareRunHeadTags`, so it is covered by inheritance rather than by a row that says so. Owner tree: `apps/web/src/lib/share/`.
- [x] **The og:image route card's 30-character title is a pixel budget wearing a character budget's clothes.** **CLOSED 2026-09-08** ([decisions § 1580](../architecture/decisions.md)). `og_route_image.ts` puts the route name in a single SVG `<text>` at 56px, which neither reflows nor clips, so anything past the 1120px between the margins paints over the polyline and off the card. 30 is sized for a Latin name; 30 CJK glyphs at 56px is about 1680px and overruns it, and that has been true since the card was written — [§ 1528](../architecture/decisions.md) changed the unit but not the number, and states this. The durable fix is not a smaller constant (which would truncate Latin names for no reason) but a width estimate per script class, or an SVG `<foreignObject>`/`textLength` that the rasteriser honours. Needs a measurement against the actual renderer before a number is chosen. Owner tree: `apps/web/src/lib/share/`.
- [ ] **`clipText`'s grapheme guarantee is conditional on a runtime the browser half cannot promise.** [§ 1529](../architecture/decisions.md) records the trade: CI and every share Lambda are Node 24 with full ICU so the crawler-facing head always segments, but the `/share/<entity>` SPA pages build the same head in the tab and fall back to the code-unit cut where `Intl.Segmenter` is absent. Nothing measures how often that is — there is no analytics on the share pages, by design — so the residue is that a title can differ between what the crawler was served and what the reader's tab shows, on a browser old enough to lack the constructor. If the fallback is ever to be removed rather than kept, what is needed first is a stated minimum-browser policy for `apps/web`, which the repo does not have (no `browserslist`, and Vite's target governs syntax rather than APIs). Owner tree: `apps/web/`.
- [x] **`docs/ops/deployment.md § Merge gates` describes the required set by hand, and nothing points a reader at the guard that now enforces its shape.** **CLOSED 2026-09-08** ([decisions § 1585](../architecture/decisions.md)). [§ 1537](../architecture/decisions.md) added rule 7 to `check_ci_diagnostics.mjs`: every workflow triggering on a pull request either reaches the `CI gate` or is named in `PR_ADVISORY` with a reason. That register now holds, in code, the same five decisions the Merge-gates section explains in prose — `security.yml`, `pr-title-lint.yml`, `compliance-drift.yml`, `labeler.yml`, `dependabot-auto-merge.yml` — and neither names the other. The section is the operator-facing document and the register is the machine-checked one, so the two should point at each other or they will drift the way § 1393's paragraph and the `monorepo.md` row did. Not taken here because `docs/ops/` was not this lane's tree. Two paragraphs. Owner tree: `docs/ops/`.
- [x] **`pr-title-lint.yml` is a second instance of the class § 1264 closed for gitleaks, and CLAUDE.md describes it as if it gated.** **CLOSED 2026-09-08** ([decisions § 1585](../architecture/decisions.md)). The root `CLAUDE.md` says "PR titles are lint-gated — get this right or the `lint title` check fails the PR on open", which reads as a merge block. `docs/ops/deployment.md § Merge gates` says branch protection requires **exactly one** context, `CI gate`, and `pr-title-lint.yml` is a workflow of its own on `pull_request_target` — so its red is a red row and not a block, exactly as gitleaks' was. It is now declared in `PR_ADVISORY` with the reason the fold is wrong ([§ 1537](../architecture/decisions.md)): it triggers on `edited`, which is how a corrected title re-lints, and `ci.yml` carries no `types:` and deliberately not that one, since adding it rebuilds every job on a description typo. So the code is right and the SENTENCE is wrong. Either correct the CLAUDE.md clause to say it is an advisory check, or add `lint title` to the required contexts — a repo setting, and a cheap one, since the job is 20 s and has no dependency on the gate. Owner: `CLAUDE.md` + repo settings.
- [x] **The census does not name 493 of the 553 mobile suites, and whether that is the design or a gap has never been decided in writing.** **CLOSED 2026-09-08** ([decisions § 1586](../architecture/decisions.md)). Measured 2026-09-07 with the census's own parser plus `git ls-files` while closing the four-rows filing ([§ 1535](../architecture/decisions.md)): `docs/testing/test_inventory.md`'s 153 headings and their bullets name 1,493 files, of which 60 of the 553 `apps/mobile_android/test/*.dart` suites. The document's practice is to give the WEB suite a heading and name the Dart mirror inside its prose (`recurrence_test.dart` appears that way; `gym_prs_test.dart` appears nowhere), and two carve-outs in the guard — `Screen + widget smoke tests` and `Pure-helper extractions` — say outright that some sections describe a shape rather than a file set. The zero-declaration rule closes the harm that motivated the filing, so nothing is broken; what is missing is a sentence at the top of the census saying which files it undertakes to index, so the next reader who counts does not re-file this. If the answer is "web suites plus anything with no web twin", that is checkable and should become a claim. Owner tree: `docs/testing/` + `scripts/`.
- [x] **`monorepo.md`'s Prerequisites table pins three tools and floors two, and nothing says which of the two states a new row should take.** **CLOSED 2026-09-08** ([decisions § 1587](../architecture/decisions.md)). [§ 1536](../architecture/decisions.md) put Flutter, Melos and Node.js under `check_toolchain_pins.mjs`, because the repo pins all three somewhere a guard can read. Xcode (`15+`) and Android Studio (`Hedgehog+`) stay floors because there is no in-repo pin to compare them against — which is correct today and is also exactly how the three that drifted got there. `release-ios.yml` and `release-android.yml` do name toolchain versions; if either pins an Xcode or an AGP/Android Studio version the table should state, `PREREQ_ROWS` should grow and the floor should go. Worth ten minutes reading those two workflows before the next contributor installs from the table. Owner trees: `docs/architecture/` + `scripts/`.
- [x] **Four bare RPC-write positives are read back only by an assertion that runs BEFORE the write.** **CLOSED 2026-09-08** ([decisions § 1605](../architecture/decisions.md)). `check_pgtap_refusal_assertions.mjs --static-only` now fails a `lives_ok` whose whole SQL is one call to a writing function when NO assertion in the file reads a table that function writes ([decisions § 1540](../architecture/decisions.md)). The rule deliberately accepts a read anywhere in the file, because the stronger and more honest form — some assertion AFTER the call must read it — leaves four sites the round that added the guard could not repair, the repair being in the pgtap suite rather than in the scanner: `clone_session_template_test.sql:106` ("the template author can clone their own plan", writes `session_plans` / `session_plan_blocks` / `session_plan_items`), `gym_routine_club_templates_test.sql:159` ("the author can clone their own routine", writes `gym_routines` / `gym_routine_exercises` / `gym_routine_sets`), `reports_test.sql:104` ("after the prior report is dismissed, the same reporter can re-file", writes `reports`) and `user_blocks_test.sql:95` ("Alice can unblock Bob via the RPC", writes `user_blocks`). Each is the § 741 shape at its sharpest: a clone that copied the parent row and none of its children, a re-file that returned without inserting, and an unblock that deleted nothing all pass as written. The fix is one read-back assertion each — the cloned child counts, the new report's id, the absent block row — after which the guard can be tightened to the ordered form and the weaker rule deleted rather than left as the standing one. Owner tree: `apps/backend/supabase/tests/` (needs the local stack). — filed 2026-09-07
- [ ] **The `gym_routine_exercises (routine_id, exercise_key)` negative has no automated guard, only prose.** Recorded in [decisions § 1286](../architecture/decisions.md) + [§ 1489](../architecture/decisions.md), in `followups.md` and now in `api_database.md`, and nothing fails if a migration adds the index — it fails against production data at `db push`, with a 23505 from the first lifter who programmed a top set and a back-off block. The guard's shape should be behavioural rather than nominal: no unique index or unique constraint on `gym_routine_exercises` may cover a column set that is a SUBSET of `(routine_id, exercise_key)`, since that is exactly the condition under which two rows of one routine sharing a key stop being representable — keying it on the literal index name or the literal column pair would pass a `unique (exercise_key)` that forbids the same shape. The natural home is a pgtap test reading `pg_index`, which is why it did not land in the scanner lane that re-verified the negative: every CI-run script has a different subject, and a new script needs both a `ci.yml` job and a line in the `CI gate` aggregator's `needs:` ([§ 764](../architecture/decisions.md)). Owner tree: `apps/backend/supabase/tests/`. — filed 2026-09-07
- [x] **`gym_programming.md` still describes `gym_routine_exercises.exercise_key` and `exercises.name_key` as client-stamped, which migration `20270711000001` ended.** **CLOSED 2026-09-08** ([decisions § 1568](../architecture/decisions.md)). The `exercise_key` bullet under `#### gym_routine_exercises` says the key is "stamped at write time (reuse the `gym_prs` parity-pair helper)" and its indented paragraph reasons at length from "this column and `exercises.name_key` are stamped by the CLIENT", closing with "the durable fix is to stamp both columns server-side the way `gym_sets.exercise_key` already is … filed in `followups.md`". That fix shipped: `gym_routine_exercises_stamp_exercise_key_trigger` and `exercises_stamp_name_key_trigger` are BEFORE INSERT OR UPDATE triggers that overwrite a client-supplied value rather than refusing it ([decisions § 1284](../architecture/decisions.md)), and `api_database.md` and `derived_state.md` both record it. So the § 1252 stale-client refusal the paragraph is about is no longer reachable through these two columns at all, and the doc reads as a live constraint on client builds. Rewrite the bullet to match the other two docs and keep the § 1252 reasoning only where it still applies. Owner tree: `docs/features/`. — filed 2026-09-07
- [x] **A run the server permanently refuses on the POST-REFRESH retry is never recorded as rejected, so § 1347's discard chip cannot see it.** **CLOSED 2026-09-08** ([decisions § 1591](../architecture/decisions.md)). `rejected += run.id` appears exactly once in `drainQueueLoop`, in the `SkipAndContinue` arm of the outer `when (classify(e))`. The retry that follows a successful token refresh has its own `catch (inner)` which sets `lastFault`, reports, sets `blockedBy` and breaks — it never reaches a `classify` at all. So a 400/404/409/422 on that path leaves the entry queued, arms backoff as though it were transient, and puts the PreRun arc on `RetryQueued` for a run no retry will ever move: exactly the state § 1347 built `rejectedRunIds` and the two-press discard chip to escape, reachable through the one branch that bypasses them. Pre-existing and untouched by round 46, which only renamed the flag that branch sets. The fix is to classify `inner` in that branch as the outer arm does and let a `SkipAndContinue` verdict append to `rejected` and continue rather than break — but it is a change to the loop's stop semantics on a path that has a live one-shot-refresh contract, so it wants its own round and its own cases in `DrainQueueLoopTest`. Owner tree: `apps/watch_wear/`.
- [x] **The sync fault claimed for a failed token refresh is derived from the 401 that provoked it, not from the refresh's own error.** **CLOSED 2026-09-08** ([decisions § 1590](../architecture/decisions.md)). `lastFault` is set to `syncFaultFor(e)` — `SyncFault.SignInRequired` — before the `RetryAfterRefresh` arm runs, and the arm never revises it. The refresh's own throwable now reaches the log (§ 1543) but not the classification, so a refresh POST that dies on a dropped socket, or a `sessionStore.save` that throws on an EncryptedSharedPreferences fault, both tell the runner their session has expired. Since § 1544 that sentence is also an affordance — the PreRun arc offers a sign-in for it — so the cost went up from a misleading caption to a runner retyping a password they did not need to. Narrow in practice (the 401 proves the server answered moments earlier, so a non-auth refresh failure needs a small window) and it fails in the recoverable direction, which is why it is filed rather than taken. The durable fix is a third classifier beside `syncFaultFor` and `refreshFaultFor`: a refresh grant's 400 is a session the server will not renew, where an upload's 400 is a run it refuses, and `syncFaultFor` would report that as `Refused` — the exact asymmetry `refreshFaultFor` was written for, one vocabulary over. Owner tree: `apps/watch_wear/`.
- [x] **`ViewModelStreamResilienceTest`'s "no guard is completely silent" rule reads one file, and the module has thirty.** **CLOSED 2026-09-08** ([decisions § 1595](../architecture/decisions.md)). The test greps `catch (…) {}` with an empty body out of `RunViewModel.kt` alone, on the reasoning that sign-out's tile-cache wipe was one and the cache it leaves is a map of where the signed-out user runs. The rule it states is about the codebase, not about that file: `DrainQueueLoop.kt` now carries one such catch itself (deliberately, and commented — the sink that threw IS where failures are said, so there is nowhere left to report to), and nothing would notice a second one appearing in `SupabaseClient.kt`, `SessionStore.kt`, `LocalRunStore.kt` or the whole `recording/` package. Widening the scan to every `.kt` under `src/main` needs a declared exemption shape for the genuinely-nowhere-to-report cases, which is the same `allowMissing`-with-a-reason pattern the repo's other guards use and the reason this is not a one-line change. Owner tree: `apps/watch_wear/`.
- [x] **`SupabaseService.signIn` on Apple Watch has exactly one caller and it is a debug helper with the seed account hardcoded.** **CLOSED 2026-09-08** ([decisions § 1596](../architecture/decisions.md)). Found while correcting the `parity.md` Auth row (§ 1546): the watchOS target can perform GoTrue's password grant and has no way for a person to invoke it — no `SecureField` exists anywhere in `apps/watch_ios`, so the only path to a session is the phone handover over Watch Connectivity. That is a defensible product position (it is what the row now says), but the shape is worth a decision rather than an accident: either the method is dead code behind a debug caller and should say so, or watchOS owes the `SignInScreen` its Wear OS sibling has ([§ 1546](../architecture/decisions.md)), which is what a watch separated from its phone needs. The hardcoded `runner@test.com` / `testtest` in a shipped target is its own smaller question. Owner tree: `apps/watch_ios/`.
- [x] **The account-deletion receipt hash has no keyed mode, and the audit log it claims to mirror does.** **CLOSED 2026-09-08** ([decisions § 1600](../architecture/decisions.md)). `account_deletion_receipts.email_hash` is a bare `sha256.Sum256(strings.ToLower(strings.TrimSpace(email)))` in `apps/job_worker/internal/handler_lifecycle_email.go`. An email address is a guessable input, so anyone holding a candidate address can compute the digest and ask the table whether that person deleted their account — the 30-day sweep time-bounds that window but does not close it ([decisions § 1551](../architecture/decisions.md)). `deletion_audit_log` already has the instrument: `hashUserIdForAudit` takes `DELETION_AUDIT_KEY` and emits HMAC-SHA256 when it is set, falling back to the legacy salt when it is not, and the receipt path could take the same env var with the same fallback. The send-once guard only needs stable equality, so an HMAC serves it identically. Two things make this more than a swap: existing rows are unkeyed, so a keyed build would re-send a receipt for anyone deleted inside the last 30 days unless the worker checks both digests during the changeover, and the key must reach the Go worker's env rather than the Edge Function's. Owner trees: `apps/job_worker/` + `apps/backend/supabase/migrations/` (nothing schema-shaped, but the table comment states the pseudonymisation claim) + `docs/compliance/retention.md`.
- [x] **`deletion_audit_log` retention is unbounded and nothing has decided what it should be.** **CLOSED 2026-09-08** ([decisions § 1602](../architecture/decisions.md)). It is the only personal-data table in the register with no window at all: `20260917_001` created it, `20261111_001` widened its result codes, and no migration sweeps it. That is defensible — it is Art 17(2) and Art 5(2) accountability evidence, deliberately outliving the account it records, and a regulator asking "did you delete user X on date Y" three years later is exactly the question it answers. But "defensible" is not "decided", and Art 5(1)(e) wants a stated period rather than an absent one. The register now says so in its own row ([decisions § 1551](../architecture/decisions.md)). Settle the number (a limitation period plus a margin is the usual shape), then either schedule a sweep — in which case `scripts/check_retention_cron_register.mjs` requires the register row to move with it — or record in the register why the retention is indefinite and under which basis. Owner trees: `docs/compliance/` + `apps/backend/supabase/migrations/` (+ whoever holds the compliance call).
- [x] **`comment on table event_orders` still says nothing about who the merchant of record is.** **CLOSED 2026-09-08** ([decisions § 1603](../architecture/decisions.md)). § 1549 corrected `20261229_001`'s header in place, which fixes the file but not the database: `\d+ event_orders` shows the table's own comment, and `20261229_001` never wrote one for `event_orders` at all — only for `host_can_take_payment`. A DBA reading the live schema to understand the ledger gets no statement of the money model, and the one they would find by opening the migration is now correct only because someone edited it. A later migration writing the comment puts the current position where the schema-reader is standing. Small, but it is DDL rather than prose, which is why the compliance lane that corrected the header left it: that lane's scope was migration *header comments*, explicitly not DDL. Owner tree: `apps/backend/supabase/migrations/`.
- [x] **`/terms` §6 still tells the buyer the host is the merchant of record.** **CLOSED 2026-09-08** ([decisions § 1603](../architecture/decisions.md)). This is the last surviving statement of the claim § 769 disproved, and it is deliberately still standing: § 1506 left it because it is a **buyer-facing contract** whose correct wording depends on how the `on_behalf_of` sign-off lands, and `tests-e2e/legal/pages.spec.ts:85` pins that the page says it. What has changed is that the host-facing side no longer agrees with it: `payouts.merchantNote` now tells the instructor the platform is merchant of record and carries the chargebacks ([decisions § 1548](../architecture/decisions.md)), so the two documents contradict each other on the same fact, in the same product, today. Whichever way the sign-off goes, one of them is rewritten — and if it goes the platform's way (the status quo), the Playwright assertion goes with it. Track it as one item rather than two, on the `club_events.md § Refunds` checklist where the sign-off already lives. Owner trees: `apps/web/src/routes/terms/` + `apps/web/tests-e2e/legal/`.
- [x] **`clubs/new.spec.ts`'s `create_club` cap test still undoes its plant in an in-body `finally`, which a timed-out test skips.** **CLOSED 2026-09-08** ([decisions § 1606](../architecture/decisions.md)). The route-side equivalents moved to `test.afterEach` this round because Playwright abandons a timed-out test rather than unwinding it, so a `try/finally` in the test body is not a cleanup guarantee: a cap test that times out between the plant and its cleanup leaves `USER_A`'s counter at 5 for the rest of the clock hour. The club side survives that today only because all four specs that create a club as `USER_A` (`clubs/new.spec.ts`, `clubs/detail.spec.ts`, `clubs/club-event-journey.spec.ts`, `cross-cutting/clubs-journey.spec.ts`) already reset the bucket in `beforeEach` — which is the pattern the route side was missing. It is one line to move and a `tests-e2e/clubs/` run to confirm; left undone this round only because the verification run did not fit. Same for `admin/admin-moderation-journey.spec.ts`'s `create_report` bucket, which is deleted inline rather than from a hook. Owner tree: `apps/web/tests-e2e/clubs/`. See `docs/testing/testing.md § 7`.
- [x] **No guard stops a new Playwright spec creating a route or a club without stating its rate-limit budget.** **CLOSED 2026-09-08** ([decisions § 1607](../architecture/decisions.md)). `docs/testing/testing.md § 7` now spells the rule out and every current creating spec follows it, but the rule is prose: the next spec that drives a create through the UI without a `resetRateLimit` in `beforeEach` re-opens exactly the ordering dependence that produced the round-43 report, and it will pass in isolation and in CI's fresh-per-job stack while failing locally an hour after someone ran the cap test. The check is mechanically decidable in the same family as `fixtures/dates.test.ts` and `fixtures/db-read.test.ts` — a source scan for a spec that reaches a create affordance (the `Save route` / `Create club` submit controls, or `saveRoute` / `createClub` through a fixture) and does not name its bucket in a `beforeEach` — and it belongs in the web unit suite for the same reason those two do: the defect is invisible to any run whose hour happens to be clean. Owner tree: `apps/web/tests-e2e/fixtures/`.
- [x] **`fetchRouteById` hands the OWNER a privacy-clipped route when `auth.ready()` times out before the fetch.** **CLOSED 2026-09-08** ([decisions § 1562](../architecture/decisions.md)). `/routes/[id]`'s `onMount` awaits `auth.ready()` and then loads regardless of whether it resolved or fell through on its timeout; `fetchRouteById` resolves `viewerId` from `auth.user?.id`, so a null viewer takes the non-owner branch and returns `{ ...owned, geom: null, start_point: null, waypoints: <server-clipped> }` for the owner's own route. `isOwner` then flips true as soon as the session lands, so the owner-only affordances render over a row whose geometry was stripped — the map and the waypoint list are the visible surface, and nothing re-fetches. Found while tracing the star-toggle flake; NOT the cause of it, and not fixed here because `core/data.ts` was another lane's tree this round. The durable shape is for the page to hold the load until the session is actually resolved (or for `fetchRouteById` to take the viewer explicitly rather than reading a store mid-race), not for the test to work around it. Owner tree: `apps/web/src/lib/core/data.ts` + `apps/web/src/routes/routes/[id]/`.
- [ ] **`.pr-hide` measures exactly 44x44, so the dashboard tap-target guard has no headroom at all.** Sampled 480 times across 12 `/dashboard` loads, the control reports 44.0 x 44.0 every time — it sits precisely on the threshold the assertion tests, so any sub-pixel effect (a fractional device pixel ratio, an ancestor transform mid-animation, a compositor quad rounding to 43.999…) turns a correct control into a failed assertion. The guard is now polled, so a transient no longer fails it, but the underlying fact is that the project's own 44 px target bar is met with zero margin on this control while the surrounding icon buttons are not audited at all. Worth a sweep: which controls in the tree clear 44 px only exactly, and is exactly-at-the-line the intended design or an artifact of a `min-width` added to satisfy a single test? Owner tree: `apps/web/src/routes/dashboard/`.
- [ ] **A route mock that never fires is silent, and only one of the tree's `page.route` patterns was ever checked.** This round found `**/auth/v1/user` never matching the `?redirect_to=`-carrying request the SDK sends, and fixed it plus asserted the stub fired. A sweep of every `page.route('…')` glob in `tests-e2e/` found no other pattern that cannot match its endpoint today — the `**/rest/v1/<table>*` and `**/rest/v1/<table>**` forms all admit a PostgREST query string, and the bare `**/functions/v1/<fn>` and `**/api/<route>` forms address POSTs that carry none. That is a point-in-time result with nothing behind it: the next `emailRedirectTo`-shaped option added to any of those calls re-opens the same hole, in the same silence. The general instrument is to assert the mock fired wherever the mock is what stands between a spec and a real mutation, which one spec now does and the rest do not. Owner tree: `apps/web/tests-e2e/`.

## Surfaced by the UI/UX agent refresh (2026-09-17)

- [ ] **Eleven repo paths cited across the persona and audit agents no longer exist, and nothing checks them.** `apps/web/src/lib/ux_agent_guards.test.ts` ([decisions § 1643](../architecture/decisions.md)) asserts path liveness for the five UI/UX toolchain files only, because in the persona and audit agents a citation is often a hypothesis rather than a directive — `compliance-auditor`'s "`apps/web/src/lib/sentry.ts` if present" and `release-readiness`'s "`apps/osrm/` if it exists" are honest sentences about a tree that may not have the file, and failing them would assert something the author never claimed. Seven are plain swaps: `apps/web/src/lib/hr_zones.dart` → `apps/mobile_android/lib/hr_zones.dart` (runner-older; a `.dart` path under `apps/web`), `apps/web/src/lib/run_intensity.ts` → `apps/web/src/lib/training/intensity.ts` (runner-pro), `packages/run_recorder/lib/workout_runner.dart` → `.../lib/src/workout_runner.dart` (runner-pro), `apps/watch_wear/build.gradle.kts` → `apps/watch_wear/android/build.gradle.kts` (audit/deps), `apps/web/src/lib/sentry.ts` → `apps/web/src/lib/sentry/redact.ts` (compliance-auditor), `apps/web/src/routes/signup/` → there is no signup route, it is `/login` + `/onboarding` (runner-family-club, whose finding is an age gate — so the swap changes which files get audited), and `apps/web/src/routes/+layout.server.ts`, which the cookie-consent walk lists among real siblings and which does not exist. **Two are real drift worth investigating rather than swapping**: `apps/mobile_android/pubspec.lock` and `apps/mobile_ios/pubspec.lock` are cited five times by `dep-bump-twin` and `.gitignore:11` says in a comment that `pubspec.lock` is COMMITTED "because these are applications, not libraries" — yet neither file is tracked by git nor present on disk, so either the policy lapsed or the ignore comment is wrong, and the dep-bump flow is documented against a file it will not find; and `apps/backend/supabase/functions/_shared/handler_envelope.ts`, cited by `audit/pii-in-logs` as "the error-response shape", exists only as `handler_envelope.test.ts` with no implementation beside it. Once the eleven are resolved, `UX_TOOLCHAIN` in the guard can widen to the whole fleet, provided a shape is chosen first for the conditional citations — the `allowMissing`-with-a-reason pattern the repo's other guards use is the natural fit. Owner trees: `.claude/agents/personas/`, `.claude/commands/audit/`, plus `apps/backend/supabase/functions/_shared/` and whoever owns the mobile lockfile policy. — filed 2026-09-17

## Surfaced by the first iOS build + run (2026-09-18)

- [ ] **The iOS launch image is still Flutter's default placeholder.** Flutter's own archive validator flags it: `Launch image is set to the default placeholder icon. Replace with unique launch image.` The app icon is real (15 sizes including the 1024x1024 App Store asset); only `Runner/Assets.xcassets/LaunchImage.imageset` is untouched template output, and its `README.md` is still the Flutter-generated one. Not a submission blocker on its own, but it is the first frame of every cold start and it currently shows a generic mark to a reviewer. Needs a designed asset at 1x/2x/3x, so it is an asset task rather than a code one. Measured on Xcode 26.4 ([decisions § 1661](../architecture/decisions.md)). Owner tree: `apps/mobile_ios/ios/Runner/Assets.xcassets/`.
- [ ] **Nothing holds `releasing.md`'s release table to the workflows it describes.** [§ 1661](../architecture/decisions.md) found the `mobile_ios@*` row claiming an `.ipa` artifact that the workflow's own `--no-codesign` build cannot produce, and the neighbouring prose claiming a watchOS target that `Runner.xcodeproj` does not reference — both wrong for as long as nobody ran an iOS release, which is the entire life of the file. The table's columns are each derived facts: which runner a workflow declares, whether it installs signing assets, what it deploys to, and what it attaches back. `check_toolchain_pins.mjs` already reads `.github/workflows/release-*.yml` but its subject is toolchain pins, and hanging an artifact claim off it would be the same mis-hosting this repo keeps finding; the honest home is a guard of its own, which means a `ci.yml` job and a line in the `CI gate` aggregator's `needs:` ([§ 764](../architecture/decisions.md)). Owner tree: `scripts/`.
