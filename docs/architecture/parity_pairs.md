# TS↔Dart parity pairs — the registry

This is one of the **two** registries `scripts/check_parity_pair_registry.mjs`
cross-checks on every PR. The other is the `## The pairs (canonical list)` table
in [`.claude/agents/shared-library-syncer.md`](../../.claude/agents/shared-library-syncer.md),
which is the list the `shared-library-syncer` agent actually works from.

**Adding a pair means editing both, in the same change.** A pair added here with
no row in the syncer table is a pair whose divergence is never detected; a row
there with no entry here reads to a session as a single-platform helper. The
guard fails the PR in either direction, and when either registry names a path
that no longer exists. See [decisions.md § 604](decisions.md).

This file was split out of the root `CLAUDE.md` because it had grown to 115 KB —
about 29,000 tokens re-sent on every single prompt of every session, to carry a
registry that only matters when a session touches one of the pairs. `CLAUDE.md`
keeps the rule and points here; the content below is unchanged.

Read this before editing any pure-logic helper under `apps/web/src/lib/` or
`apps/mobile_android/lib/`, and invoke the `shared-library-syncer` agent after
touching one side of a pair.

## The pairs

- **TS↔Dart parity helpers must stay in lockstep.** Several pure-logic modules exist on both web and mobile and must match — algorithm, edge cases, outputs, test counts. The pairs are: `training`, `segments`, `privacy`, `pace_segments`, `training_load`, `fitness`, `rate_limit_errors`, `runner_handle`, `recurrence` (web `social/recurrence.ts` ↔ mobile `recurrence.dart`; the pair agrees on the INSTANT, not on the type, and the registries never said so. For a legacy event declaring no `timezone`, `expandInstances` mints a LOCAL `DateTime` on Dart and a local-constructed `Date` on JS, and those name the same absolute instant — so the pair is CORRECT and moving either half to UTC is the one edit it must not take, because it would re-key every RSVP already placed under the old value (decisions § 1343). It stays correct only while every CONSUMER normalises at serialisation: web gets that free because a JS `Date` is an instant and `toISOString()` always emits `Z`, while Dart's `toIso8601String()` writes no zone designator at all for a local `DateTime` and Postgres then resolves the zone-less literal in the session's own TimeZone — filing one occurrence under a different key for every reader zone, which is how a phone RSVP came to be invisible in the web attendee list. The Dart precondition is `instanceStartKey` in `api_client`, guarded there by `instance_start_key_test.dart` and, since § 1378, across `apps/mobile_android/lib` by `instance_start_wire_guard_test.dart`. One-way asymmetry rather than a divergence: the precondition is a fact about the Dart CONSUMERS, not about the helper, so web needs no change and gets none. **30 tests each, and the five-case gap the count once hid is closed.** Counted 2026-09-07 and decomposed case by case (§ 1444), because a count is not evidence of a gap in either direction: **23 direct mirrors**, **2 Dart-only** cases that are source guards with no web analogue by construction (web steps its cursor with `Date.setDate()`, which is calendar-based natively, where Dart's `add(Duration(days: n))` is absolute and repeats a local day across a fall-back, and CI runs in UTC so no behavioural test can fail on it), and **5 web-only cases that were a real gap** — all four `describeRecurrence` cases and `nextInstanceAfter` past `recurrence_until`. Those five were ported on 2026-09-07 (§ 1495), taking the Dart suite to 30, and the port found NO divergence: the ` · ` separator is U+00B7 on both rails and all four sentence literals are identical byte for byte, checked outright because two passing suites cannot see a lookalike character.), `distance_bands` (web `routes/distance_bands.ts` ↔ mobile `distance_bands.dart`), `exif_strip` (web `util/exif_strip.ts` `stripJpegExif` ↔ mobile `exif_strip.dart`), `grade_adjusted_pace` (web `runs/grade_adjusted_pace.ts` ↔ mobile `grade_adjusted_pace.dart`), `pace_analysis` (web `runs/pace_analysis.ts` ↔ mobile `pace_analysis.dart`; `analysePacing` cuts the run into halves by DISTANCE — "negative split" is a claim about the second half of the COURSE, and cutting by time puts the mark short of it on any run that faded — and offers the same comparison grade-adjusted whenever the track carries elevation, so a climbing second half is not read as a blow-up; `gradeAdjustedSplitPaces` slices the track on the running sum of the splits' own lengths, so the GAP column lands on exactly the boundaries the splits list already displays rather than on a second, independently-walked set. Two idiomatic shape differences, not divergences: web's `PacingVerdict` string union is a Dart enum, and `gradeAdjustedSplitPaces` takes the split LENGTHS on the Dart side because mobile's `RunSplit` carries only a tick and a duration where web's `Split` carries `distance_m` — every split mobile emits is exactly one tick long, so the caller hands over `List.filled(n, tickLength)`. Backs the run-detail pacing-halves card + the grade-adjusted split column on both platforms, 13 mirror tests each), `gym_prs` (web `gym/gym_prs.ts` ↔ mobile `gym_prs.dart`; the pair carries a THIRD rail that is not another client — `normaliseExerciseName`'s whitespace class is also `public.normalise_exercise_name` in SQL (migration `20270623000001`, decisions § 790), because the clients PERSIST the key as `gym_routine_exercises.exercise_key` / `exercises.name_key` and four RPCs re-derive it from `gym_sets.exercise_name` at read time. All three must fold the same set or one exercise buckets as two: the local PR tracker says PR where `gym_workout_summaries.is_pr` says no. The class is spelled out by code point on every rail rather than left to a runtime default, and the SQL one deliberately does NOT use `\s` — `\s` is `[[:space:]]`, whose membership past ASCII is the database collation's opinion, so the four hand-written copies it replaced answered differently on an ICU-provider database and a libc one. U+001C-U+001F stay OUT of the class on all three: Postgres folding them under one provider was a divergence to close, not a rule to copy. `scripts/check_shared_constants.mjs` compares the three rails and `apps/backend/supabase/tests/normalise_exercise_name_test.sql` pins the SQL one. The pair carries a FOURTH rail since § 1175: the generated `exercise_fold_table.ts` / `exercise_fold_table.dart`, a frozen Unicode 17.0 simple-lowercase table both `normaliseExerciseName`s fold through instead of their runtime's `toLowerCase()`, mirrored by the `translate()` in `public.exercise_fold_case`. Never hand-edit it — re-run `scripts/gen_exercise_fold_table.mjs` (`pnpm gen:exercise-fold-table`), and note that regenerating it is a MIGRATION rather than a codegen refresh (§ 1176), because two of the three keyed columns are client-stamped under a validated CHECK naming the SQL function. **The Dart halves of the KEY and its table live in `packages/core_models/lib/src/exercise_key.dart` + `exercise_fold_table.dart`, not under `apps/mobile_android/lib/`** (§ 1515): `packages/api_client` decides the same blankness question and could not reach the class, so it tested `name.trim()` — an answer that is right only by a coincidence of Dart's own whitespace set. `gym_prs.dart` re-exports them, so every caller's import is unchanged and the Dart PR metrics stay where the rest of the pair is; and because `core_models` is shared rather than twinned, the generator now writes ONE Dart table instead of two. **The CASE half is the same problem and was closed the same way** (decisions § 830, migration `20270630000001`): `lower()` answers with the collation of its argument, so the SQL rail pins `collate "und-x-icu"` — measured over all 1,112,063 assignable code points the old form disagreed with itself at 1 point between the ICU and libc providers, 2 between ICU `en-US` and `tr-TR` (one of them ASCII `I`, so every "Incline Press" re-keyed on a Turkish database), 3 under `lt-LT` and 1,406 under `C`; under the pin, zero. The CLIENTS need no such pin but are not each other: JS `toLowerCase()` is Unicode full case mapping from a newer table, Dart's is SIMPLE mapping from an older one, and they **disagree at 466 code points**. Two of them are reachable in a Latin or Greek name and are folded by name on all three rails — U+0130 → `i` before the lowercase, U+03C2 → U+03C3 after — registered as a second entry in the same guard. The residual 465 / 410 / 55 are runtime-version skew, not collation, and closing them needs a frozen 1,488-pair table whose `translate()` costs 60 µs a call against 0.34 µs for `lower()`; that is filed and sequenced behind persisting the key on `gym_sets`), `nutrition_targets` (web `nutrition/nutrition_targets.ts` ↔ mobile `nutrition_targets.dart`; the "non-physical metric" filter that hides the calorie rings reads its weight and height ceilings from `column_limits`' `columnCheckMax` / `kColumnCheckMax` rather than as literals, because it filters a value read BACK out of the database — a cap below the column's own CHECK discards a legitimately stored weight, and both halves of a lockstep pair are wrong together, so the pair's own mirror tests cannot catch it (decisions § 819). The age ceiling stays a stated constant: `date_of_birth` has no CHECK. 19 mirror tests each), `lift_load` (web `gym/lift_load.ts` `liftsFromSetHistory` ↔ mobile `lift_load.dart`), `calories` (web `runs/calories.ts` ↔ mobile `calories.dart`; `estimateRunCalories` — the run-detail calorie estimate, weight x per-activity kcal/kg/km x distance with the female calibration of decisions § 77. Distinct from `exercise_calories`, which is the nutrition side's own estimator. Registered 2026-09-02 after the two were found to answer differently on the same input: a non-finite figure fails every `> 0` test the way a zero does but survives `Math.max`, so web rendered `NaN kcal` where Dart answered 0, and an Infinity threw outright on the Dart side because `.round()` refuses a non-finite double — inside a getter read during a widget build. Both now check every input for FINITENESS rather than only for sign: an unusable distance contributes nothing, an unusable weight or coefficient falls back to the documented default exactly as an absent one does, and a product past the range the two platforms SHARE is no estimate rather than two different figures -- `MAX_ESTIMABLE_KCAL`/`kMaxEstimableKcal` is 2^53-1, past which a JS number stops being an exact integer while Dart's `int` saturates at 2^63-1. The three halves were measured identical over 13,500 input combinations before the pair was registered. One idiomatic shape difference: web's `CalorieGender` string union is a Dart `String?` typedef. 18 mirror tests each), `exercise_calories` (web `nutrition/exercise_calories.ts` ↔ mobile `exercise_calories.dart`), `gear_wear` (web `gear/gear_wear.ts` ↔ mobile `gear_wear.dart`), `rotation_pick` (web `gear/rotation_pick.ts` ↔ mobile `gear_rotation_pick.dart`; `rotationPick` ranks a rotation's in-service members least-worn-first and names the pair due out next, so the `is_default` star the `runs`-insert trigger auto-tags with can follow the rotation instead of banking every kilometre on the one pair that last held it. A pair at or past its own replacement target sorts behind every pair that is not, whatever the shares say — recommending a shoe the app itself calls worn is worse advice than none; a pair carrying no target of its own is measured against the MEDIAN of its siblings' targets rather than dropped (a mean lets one 5,000 km outlier bury it); retired members are absent from `ranked` entirely, so `ranked.length` — not the membership count — is what a caller gates the affordance on, or a rotation holding one live pair and one retired one offers a "wear this next" with nothing to choose between. The star is scoped to (owner, kind), so both surfaces run the pick within one kind: web within the tab it is showing, mobile once per kind the rotation holds. 18 mirror tests each), `gear_backfill` (web `gear/gear_backfill.ts` ↔ mobile `gear_backfill.dart`; `gearBackfillCandidates` picks the past runs a just-registered piece of gear could plausibly have been worn for — activity matching the gear kind, started on/after the purchase date, newest-first — behind the post-create "attach past runs" prompt. The gear-kind match is **derived** as "the bike takes `cycle`, everything else takes shoes", which is the `auto_tag_default_gear` trigger's mapping verbatim; it is deliberately NOT an enumerated shoe allowlist, because `{run, walk, hike}` silently dropped `stroller` — a real `runs_activity_type_check` value the trigger auto-tags with the current pair (decisions §598). Both suites pin that an activity neither side has heard of is foot-powered, so a re-enumeration fails even if `stroller` is remembered. The pair's scope is that one function: web's `gearPurchaseSince` (a `purchased_at` date resolved at LOCAL midnight, so a purchase-day morning run isn't dropped east of Greenwich) is glue for what mobile's date picker hands the Dart side directly. 15 tests web / 11 Dart — the extra web cases cover `gearPurchaseSince` plus an unparseable `started_at`, which a typed Dart `DateTime` can't reach the helper as), `age_grade` (web `runs/age_grade.ts` ↔ mobile `age_grade.dart`; their embedded factor tables `age_grade_tables.ts` ↔ `age_grade_tables.dart` are generated from `scripts/age_grade/` and stay identical by construction — never hand-edit them), `exercise_history` (web `gym/exercise_history.ts` ↔ mobile `exercise_history.dart`; carries `previousExerciseSession` for the detail-screen "vs last time" hint), `nutrition_budget` (web `nutrition/nutrition_budget.ts` ↔ mobile `nutrition_budget.dart`), `hydration` (web `nutrition/hydration.ts` ↔ mobile `hydration.dart`), `nutrition_week` (web `nutrition/nutrition_week.ts` ↔ mobile `nutrition_week.dart`), `event_occurrence` (web `social/event_occurrence.ts` ↔ mobile `event_occurrence.dart`; `isOccurrenceCancelled` + `nextLiveInstance` — which occurrence of a series is still ON once `event_exceptions` is subtracted, so no surface advertises one the organiser called off (decisions § 620). Both fetchers behind the mobile club Events tab and the Run-tab upcoming-event card feed it a BATCHED exception read, never one query per event. Web's `upcomingCancelledOccurrences` is NOT twinned — it backs the organiser reinstate picker, which mobile does not have; and web's string-vs-string `sameInstant` reads as Dart's `isAtSameMomentAs`, so web's unparseable-instant case has no Dart analogue (the parse guard lives in the service's batched read). 11 in-scope tests web / 10 Dart), `event_category` (web `social/event_category.ts` ↔ mobile `event_category.dart`; the athletic-vs-attendance split for typed club events), `event_gym_template` (web `social/event_gym_template.ts` ↔ mobile `event_gym_template.dart`; the typed `{discipline, duration_min}` contract + `parseGymTemplate` / `gymTemplateFromInputs` / `workoutDraftFromTemplate` for the class→gym seam, plus `workoutDraftFromSession` for the session-plan→gym-workout seam), `live_freshness` (web `runs/live_freshness.ts` ↔ mobile `live_freshness.dart`; the spectator staleness `freshnessFor` — age clamp + stale flag + display bucket, so a lost-signal runner is shown stale, not fresh, on both platforms — plus the two time helpers the spectator surfaces used to conflate. `raceClockS(startedAtMs, atMs)` is the RACE CLOCK — wall-clock seconds since the runner started, taken straight off the `started_at` that `public_runs` already serves an anonymous spectator, so it is a fact rather than an estimate and no auto-pause judgement enters it. `atMs` is the instant measured to, `now` while live and the CONCLUSION instant once the run has ended, or a finished run's clock ticks on forever; an unknown start or end claims null rather than reconstructing a clock from the frozen timer, and a start stamped in the viewer's future clamps to 0 like `freshnessFor`'s age. `liveElapsedS` is the FALLBACK for a caller that cannot resolve the start: the last ping's `elapsed_s` advanced by that ping's age, which is a LOWER BOUND on the same clock (it credits nothing for time the runner had the recording paused), and both suites pin that ordering. A cut-off is a wall-clock deadline measured from the start, so driving cut-off maths off the raw ping value freezes the clock the moment the signal drops and a limit that expires during a dead zone never registers; only time is added, never distance. The ping's `elapsed_s` is the runner's own RECORDING TIMER, not moving time — the recorder has had no auto-pause layer since moving time became a track-derived post-run metric, so it stops only on an explicit manual pause (decisions § 712)), `session_steps` (web `social/session_steps.ts` ↔ mobile `session_steps.dart`; `expandSessionSteps` flattens a session plan's blocks→items into ordered timed steps, splitting per-side items into L/R — the yoga/pilates session_planner.md P1 expand-once helper; P2 adds `computeSessionAdherence` for the follow-along planned-vs-actual roll-up), `gym_routine` (web `gym/gym_routine.ts` ↔ mobile `gym_routine.dart`; `routineFromWorkout` + `prefillFromRoutine` for the gym-programming P1 save-as-routine / repeat-last shaping — reuses `normaliseExerciseName` from `gym_prs`; P2 adds `expandRoutineSteps` for the GymWorkoutRunner step sequence), `exercise_catalogue` (web `gym/exercise_catalogue.ts` ↔ mobile `packages/core_models/lib/src/exercise_catalogue.dart`; `dedupeShadowedExercises` is the catalogue once the two PARTIAL uniques on `exercises.name_key` are taken into account — `exercises_global_name_key` on `(name_key) where author_id is null` and `exercises_author_name_key` on `(author_id, name_key) where author_id is not null` (migration 20270222_001), which between them let a user's custom SHADOW a seeded global under one name, exactly as that migration says it intends. So a read legitimately returns TWO rows for one exercise while every consumer downstream binds a typed name to ONE `exercises.id`. The precedence nothing in the tree stated is that **the owner's row wins**: a shadow exists only because the user deliberately created a custom under a name the catalogue already held, so resolving it to the seeded global resolves it the one way they did not ask for. Applied at the READ on both platforms, so no consumer carries the rule and none can disagree with another about it; RLS is what makes “the owner's row” cheap to recognise, because the read policy returns seeded globals plus the caller's OWN customs and nothing else, so a non-null `author_id` in a result set is by construction the caller's and no user id is taken. **Both halves key on the row's STORED `name_key`**, which is the column the two uniques are actually enforced ON and is stamped by the `exercises_stamp_name_key` trigger (20270711000001). The web half re-derived it through `normaliseExerciseName` for a while and this registry recorded that as a deliberate divergence; it is not one and never was, and the entry outlived the fix by a round (§ 1510). A client fold is this build's answer to the same question, and the two agree only while the frozen fold table matches the server's — which § 1176 records as moving by MIGRATION rather than by codegen, so a row stamped before the last regeneration carries a key the current table would not produce and a re-derivation then splits a pair the index considers one. Reading the column also keeps `packages/` clear of a fourth copy of the 1,488-entry fold table, which it cannot reach at all. The Dart side is the fourth pair after `profile_query`, `strava_sync_result` and `import_completeness` to live in the SHARED `core_models` package rather than under `apps/mobile_android/lib/`, because `api_client` consumes it — so it owes no iOS-twin mirror and its suite is `packages/core_models/test/exercise_catalogue_test.dart`. One further idiomatic shape difference, not a divergence: web states the bound structurally as `E extends ShadowableExercise`, where the Dart half is generic over two ACCESSORS — Dart has no structural typing over records, and `ExerciseRow` is GENERATED by `scripts/gen_dart_models.dart` and so cannot be given an `implements` clause. Being generic is load-bearing on both platforms rather than a convenience: the read is not the only list a surface reduces. A custom created in the picker can shadow a global the CLIENT's snapshot still carries, because the author's partial unique cannot see a row whose `author_id` is null, so the insert succeeds and a merge of the two holds both — which is why `GymEditor`'s `entries` and `gym_compose_sheet.dart`'s `_catalogue` both reduce through this function rather than de-duplicating by `id`, a test that cannot see a shadow at all (decisions § 1513). 11 tests web / 10 Dart — the two extra web cases are the MERGE shapes web's own callers apply the rule to, a custom created against a stale snapshot and a row present in both the fetched list and the created one, which the Dart half has no caller for. The stored-key case the Dart suite once carried alone is now on both sides, because the web half no longer re-derives), `gym_adherence` (web `gym/gym_adherence.ts` ↔ mobile `gym_adherence.dart`; `computeRoutineAdherence` reduces planned set targets against logged sets into per-set verdicts + a session roll-up, matched by `(exerciseKey, setIndex)`, weights canonical kg, 80 % completed threshold mirroring the run side; grades per-axis at the 80 % cutoff — reps, then a `targetDurationS` axis, then a `targetDistanceM` axis (issue #328: a distance-modality set is graded on distance, not fall-through `hit`)), `routine_history` (web `gym/routine_history.ts` ↔ mobile `routine_history.dart`; `routineHistoryFromAggregate` shapes one routine's `gym_routine_history` RPC row (migration `20270528_001`) into what the history panel renders — the bounded page of recent sessions classified and ordered newest-first, days since last (floored, and a row stamped ahead of the reader's clock reads as today rather than as a negative), and the completed-of-GRADED ratio. The tallies are **complete** and arrive already reduced: a count is an aggregate, and both platforms used to window the read at 500 rows and reduce them client-side, so a lifter running one routine weekly for a decade was shown a capped figure (decisions § 658). The two exclusions are the whole point and the SQL reproduces them exactly: a row still carrying a `gym_session_draft` snapshot is an in-flight session, not a session performed, so a resume cannot inflate the routine's usage (the marker must be a JSON OBJECT on all three rails — Dart's `is Map`, web's `hasSessionDraft`, and the RPC's `jsonb_typeof(...) = 'object'`; `typeof x === 'object'` is true for an array in JS, which is exactly how web drifted, decisions § 662); and a "save as is" row keeps `routine_id` while deliberately claiming no adherence, so it counts as a session but sits OUTSIDE the rate denominator rather than counting as a miss (decisions § 617). `lastPerformedAt` comes from the aggregate, never from the page — a bounded page can be empty while the routine has been run hundreds of times. Web's `RoutineVerdict | 'ungraded'` union reads as one four-value Dart enum, an idiomatic shape difference like `password_change`'s union-vs-class; the Dart sort carries an input-index tiebreak because Dart's `List.sort` is unstable where JS's is stable. 17 mirror tests each), `gym_progression` (web `gym/gym_progression.ts` ↔ mobile `gym_progression.dart`; `nextPrescription` suggests the next routine prescription from logged-set history, plus the `workingSets` / `fiveByFiveTargets` / `fiveByFiveSessionSucceeded` primitives the miss-streak reducer shares so the two grade a session identically. `workingSets` narrows every scheme's judgement to the completed sets at the session's TOP weight — warmup exclusion cannot rest on `set_type` alone, because the batched history RPC does not return the column and an unlabelled ramp-up cannotherwise fails the rep test (holding the load forever) or pads a 5×5 into an unearned promotion, (decisions § 602). The `set_type` label carries the rest: both history RPCs return the column since migration `20270525_001` and all three `GymSetWithDate` producers map it, which is what excludes a warmup logged AT the working weight — the one case the narrowing cannot separate, and previously a web-vs-mobile divergence (decisions § 605)), `plan_adherence` (web `training/plan_adherence.ts` ↔ mobile `plan_adherence.dart`; `weeklyDrift` >±20% over/under + `weeklyDriftToDate` + `missedWorkoutAdvice` make-up/skip — backs the plan-detail adherence banner on both platforms. `weeklyDrift` takes a finished week's two totals and is what `plan_replan` / `plan_adaptive_replan` grade COMPLETED weeks with; the banner's current week goes through `weeklyDriftToDate`, which windows both the planned and the actual side to the days that have already ended — `scheduled_date < today`, not `<=`, on both sides, so a session due at the end of today counts on neither and a runner level with the plan mid-week is not flagged (decisions § 1652). **21 tests each.**), `plan_replan` (web `training/plan_replan.ts` ↔ mobile `plan_replan.dart`; `replanRemaining` proposes future-only make-up/ease-off changes around missed sessions, reusing `plan_adherence` — backs the plan-detail Re-plan flow. Both halves SCAN for the earliest future non-taper long run rather than sorting: only the minimum is ever read, and a strict `<` keeps the FIRST of two long runs sharing a `scheduled_date` — the plan's own week-then-workout order — so a double long-run day puts the make-up on the same session on both platforms and on the firmware port, which already scanned. Deliberately not the `routine_history` / `readiness` sort-plus-index shape, which those two need because they consume a whole ordered list; here a sort fed a single subscript, and web's comparator never returned 0 (decisions § 1200). `easeOffNextWeek` is the second export, shared with `plan_adaptive_replan` so the deload skip rules have one home. 17 mirror tests each), `plan_week` (web `training/plan_week.ts` ↔ mobile `plan_week.dart`; `currentPlanWeekIndex` buckets today into a plan week off whole UTC epoch-days, never a local-time difference — a start→today span crossing a DST transition is 167/169 h, so `Date`-ms division / Dart's `difference().inDays` truncates a day short and reports the previous week (issue #338); backs `currentWeekIndex` on `/plans/[id]` and `_currentWeekIndex` on `plan_detail_screen.dart`, 5 mirror tests each), `plan_ramp` (web `training/plan_ramp.ts` ↔ mobile `plan_ramp.dart`; grades the plan the wizard just generated against the runner's trailing-28-day average — `recentRunVolume` buckets runs into four ROLLING 7-day windows (a partly-elapsed calendar week under-reports and would drag the average down for no reason), `planRampCheck` bands the opening week and the peak week through `coach_load`'s `injuryRiskBand` rather than inventing thresholds, and `shouldSurfaceRampNote` decides whether the wizard says anything. The two directions grade DIFFERENT weeks on purpose: `elevated`/`high` come from the opening week (where a plan injures someone), `under` from the peak, because every well-formed plan opens far below its own peak and grading "under" off the opening week would tell a runner already at the plan's peak volume that it is too light for them. Silent below `MIN_ACTIVE_WEEKS` (3 of the 4 windows carrying a run) — one 3 km jog in a month divided into a C25K opening week reports a beginner plan as a high injury risk. The shared reduction lives HERE, not in a private copy: web's `self_load.ts` and `comeback.ts` both import `recentRunVolume` / `volumeSample` / the constants from this module, so a later Dart port of either reduces the same runs through the same filter. Web additionally consumes it from `PlanEditor.svelte`, mobile from `plan_new_screen.dart`'s preview; 34 mirror tests each), `self_load` (web `training/self_load.ts` ↔ mobile `self_load.dart`; `selfLoad` grades the runner's OWN acute:chronic workload ratio — the same `coach_load` band edges the coach roster classifies an athlete through, and the same `plan_ramp` reduction, turned on the person holding the phone. Composed rather than re-derived in both directions: the reduction is imported from `plan_ramp`, never copied, and the ratio is taken over DISTANCE rather than the roster's km×10 stress proxy, which is linear in distance so the quotient is identical and the constant has one home. Refuses below `MIN_ACTIVE_WEEKS` of the chronic window — one 3 km jog in a month is not a chronic base, and dividing by it manufactures a terrifying ratio out of a runner who has barely trained. The DNF divergence from the roster is deliberate (decisions § 592): nothing rewrites `distance_m` when the flag is set, so the load was absorbed and dropping it would under-report a spike. Web's `shouldSurfaceSelfLoad` type predicate narrows the band to the four with copy; Dart returns a plain bool and the caller's exhaustive `switch` is what the analyzer checks instead — an idiomatic shape difference like `password_change`'s union-vs-class. 15 mirror tests each), `comeback` (web `training/comeback.ts` ↔ mobile `comeback.dart`; `comebackLoad` grades a returning runner's first week back against the weeks they were running BEFORE the break, because ACWR is the wrong instrument for someone whose chronic month is near-empty — their own pre-break average is real data, the empty month is not. Fires only where `selfLoad` is silent (the same `activeWeeks` gate read the other way round), so the two cards are mutually exclusive BY CONSTRUCTION rather than by a dashboard remembering to choose, which both suites pin over a shared set of histories. `LAYOFF_MIN_DAYS` is deliberately `training_load`'s own `kLayoffResetDays` — a break big enough to zero the fitness EWMAs is the same break this card is about, and two numbers for one idea would drift. Fail-closed everywhere: no break, a base past `LAYOFF_MAX_DAYS` stale, no running this week, or a pre-break base thinner than `MIN_ACTIVE_WEEKS` each claim nothing. Future-stamped rows are clamped to now on this path too, or a clock running ahead opens a gap that reads as a layoff nobody took. 21 mirror tests each), `race_plan_preset` (web `training/race_plan_preset.ts` ↔ mobile `race_plan_preset.dart`; `racePlanPreset` sizes the plan a race on the calendar seeds, so the generated plan's FINAL `race` week contains race day — the start date is derived BACKWARDS from race week rather than forwards from today, and is always a Sunday because `generatePlan` hard-anchors day 0 of the start week to the Sunday long run. Weeks are capped at `defaultPlanWeeks(goal)` rather than filling the gap: a marathon eight months out wants the standard 16-week build starting in four months, not a 32-week grind. Every comparison is in whole UTC epoch-days, never elapsed milliseconds — a span crossing a DST transition is 167/169 h and truncates a day short, shifting the anchor by a whole week. It REFUSES rather than proposes (`past` / `too_soon` / `invalid`) and both surfaces hide the affordance on a refusal instead of offering a squeezed plan. `goalEventForDistance` matches the four standard rungs within 2 % (listings round — "21.1 km") and a distance matching none claims NO goal event rather than snapping to the nearest: the wizard has no custom-distance input, so a 50k presented as a marathon would be a plan for a different race (decisions § 606). Web's discriminated-union result reads as a small Dart result class, the same idiomatic difference `password_change` records. 17 mirror tests each), `plan_adaptive_replan` (web `training/plan_adaptive_replan.ts` ↔ mobile `plan_adaptive_replan.dart`; `adaptiveReplanRemaining` gates a re-plan on a multi-week adherence trend — 2-of-3 trailing completed weeks flagged — then delegates the deltas to `plan_replan`; the plan-generator-v2 P1 "Adaptive re-plan" surface), `starter_plans` (web `training/starter_plans.ts` ↔ mobile `starter_plans.dart`; `STARTER_PLANS` catalogue + `instantiateStarter` — built-in starter plans as `generatePlan` presets instantiated through the normal create-plan path, Training P3), `routine_editor_build` (web `gym/routine_editor_build.ts` ↔ mobile `routine_editor_build.dart`; `assignSupersetGroups` turns the per-exercise "superset with next" flag into the relational `(superset_group, superset_order)` honouring the both-null-or-both-set CHECK — the gym-programming P2 authoring helper), `progression_prefill` (web `gym/progression_prefill.ts` ↔ mobile `progression_prefill.dart`; `lastSessionSets` reduces an exercise's logged history to the most-recent session for the P4 `nextPrescription` prefill; `consecutiveMissSessions` + `progressionParamsWithStreak` derive the `consecutiveMisses` count the 5×5 deload gates on — authored `progression_params` never carried it and no caller supplied it, so the deload branch was unreachable and a stalled lifter held the same weight forever; a session with no completed working set is skipped rather than counted, so a logging artifact cannot prescribe a load reduction, decisions § 602), `relink_candidates` (web `training/relink_candidates.ts` ↔ mobile `relink_candidates.dart`; `filterRelinkCandidates` picks the runs eligible to re-link to a planned workout — in ±7-day window, excluding runs already linked to another workout so `plan_progress` can't double-count, current run always kept; backs the workout-detail Re-link picker on both platforms, 9 mirror tests each), `plan_start` (web `training/plan_start.ts` ↔ mobile `plan_start.dart`; `nextSundayIso` / `isSundayIso` ↔ `nextSunday` / `isSunday` — the plan wizard's start-date alignment. `generatePlan` hard-anchors day 0 of the start week to the Sunday long run and every day role in the week generator is an OFFSET from the start date, not a real weekday, so a plan started on a Wednesday puts the long run midweek and the rest day on Thursday. Web can only snap the value after the fact (`<input type="date">` takes no predicate) and backstops it on submit; the Material picker refuses a non-Sunday outright in both its calendar and keyboard modes, which is why the Dart side is consumed as `selectableDayPredicate` rather than as a snap. One idiomatic shape difference, not a divergence: web takes the ISO `yyyy-mm-dd` string its input yields where Dart takes the `DateTime` the picker hands over. 4 mirror tests each), `plan_progress` (web `training/plan_progress.ts` ↔ mobile `plan_progress.dart`; `orderedPlanPhases` + `longestCompletedLongRunMetres` + `planDistanceBanked` back the plan-detail base→build→peak→taper phase marker + longest-completed-long-run header stat + distance-banked stat (km run vs km planned; skipped excluded from both, actual linked-run distance preferred), 16 mirror tests each), `route_description` (web `routes/route_description.ts` ↔ mobile `route_description.dart`; `describeRoute` buckets a route's stored stats — race-distance band, surface, m/km elevation character, loop-vs-point-to-point shape — into structured locale/unit-agnostic `RouteDescriptionParts`, with `assembleEnglish` the canonical English assembler that seeds the AI-enhancement prompt and is the offline fallback string; the always-works L1 baseline behind the route-detail "Describe this route" affordance, 12 mirror tests each), `current_week` (web `training/current_week.ts` ↔ mobile `current_week.dart`; `currentWeek` buckets activities onto the seven days of the REAL calendar week containing `now` — honouring the `week_start` pref, local-day bucketing so a late-evening run stays on its own day — for the dashboard `ThisWeekStrip` / `this_week_strip.dart` activity ribbon. Distinct from the plan-anchored `CurrentWeekStrip` / `current_week_strip.dart` on plan-detail, which windows on `start_date + week_index*7`. 10 mirror tests each), `route_markers` (web `routes/route_markers.ts` ↔ mobile `route_markers.dart`; the course-marker kind catalogue (`ROUTE_MARKER_KINDS` — shared pin colour + i18n label key + which `meta` fields a kind carries) + `sortMarkers` (course-schedule order, position_m nulls-last) + `parseCutoff` (validate/normalise a cutoff's clock/elapsed) + `parseTarget` (same shape for the #608 `target_clock` / `target_elapsed_s` per-marker target-time meta — any kind may carry one) + the `AID_SERVICES` vocabulary; backs the route-detail course-schedule list + map pins on both platforms, 14 mirror tests each), `roadbook` (web `routes/roadbook.ts` ↔ mobile `roadbook.dart`; `buildRoadbook` turns a route's waypoints + course markers + a goal time into the per-checkpoint race-crew schedule — cumulative distance, projected arrival (elapsed + wall clock), per-leg vert, and cutoff verdict (safe/tight/miss) — allocating the goal by grade-adjusted **effort** (`gradeFactor`) not even pace, degrading to even when there's no elevation; reuses `parseCutoff` from `route_markers`. The grade behind the effort allocation is measured over an **anchored window** that accumulates horizontal distance until it clears `MIN_SEGMENT_M`, exactly as `gradeAdjustedPaceSecPerKm` walks a track — grading each point-pair on its own collapsed effort onto even pace on any densely-sampled course (decisions §600). 12 mirror tests each), `checkpoint_projection` (web `runs/checkpoint_projection.ts` ↔ mobile `checkpoint_projection.dart`; `projectRunner` grades a race-director's runner from their actual aid-station crossings — last checkpoint reached, average pace, linearly-projected arrival at every remaining checkpoint, and a safe/tight/miss cutoff verdict (reusing `CutoffStatus`/`CUTOFF_TIGHT_S` from `roadbook`) + a racing/finished/dnf status; the live-results companion to `roadbook` shared by race_director_ops.md P2 + predictive_live_tracking.md, 10 mirror tests each), `turn_cues` (web `routes/turn_cues.ts` ↔ mobile `turn_cues.dart`; `generateTurnCues` — every bend is measured on the two segments that TOUCH its pivot vertex, never on one spanning a corner, and a turn is the net direction change accumulated within `mergeWithinM` metres of where the turning starts, classified on the sum and reported at the halfway-of-turning vertex. The old distance-only collapse deleted the corner vertex it existed to preserve, so one 90-degree corner announced twice at half its angle and a 40-degree bend vanished — in all three implementations at once, because the pair was registered in neither registry and its divergence was therefore never detectable, decisions § 641. A third rail exists in the watch firmware (`apps/custom_watch/core/src/turn_cues.rs`), a one-way faithful port outside the enforced lockstep. 14 mirror tests each), `fuel_plan` (web `routes/fuel_plan.ts` ↔ mobile `fuel_plan.dart`; `buildFuelPlan` scales per-leg carbs/fluid onto the roadbook timeline + `carryToNextAid` per refill — the race-fueling layer of the roadbook, 9 mirror tests each), `route_gpx` (web `routes/route_gpx.ts` ↔ mobile `route_gpx.dart`; `toRouteGpxWithMarkers` / `routeGpxFromRoute` emit a route's GPX — the line as a `<trk>` plus one privacy-clipped `<wpt>` per course marker (kind→`<sym>`, cutoff + aid services in `<desc>`) — so a watch surfaces "Aid 2 in 1.3 km" mid-race; reuses `parseCutoff` from `route_markers`, the course_waypoint_export.md v1 export, 10 mirror tests each), `route_geometry` (web `routes/route_geometry.ts` ↔ mobile `route_geometry.dart`; `interpolateAlongRoute` (fraction→position, route-detail scrubber) + `polylineLengthMetres` + `distanceAlongRoute` (the inverse: nearest point on the line→cumulative distance-along-route, projecting an off-line live GPS fix onto the planned polyline — the predictive_live_tracking.md input; a non-finite live fix returns null, never 0), 43 mirror tests each), `tile_pack` (web `routes/tile_pack.ts` ↔ mobile `tile_pack.dart`; the pure slippy-map enumeration behind an offline route tile pack — `tilesForBbox` / `estimateTileCount` over the z12-z16 band with the 5,000-tile per-pack ceiling of decisions § 170. The fetch and the disk write are mobile-only (`offline_tile_pack.dart`); web carries the twinned math. Registered 2026-09-02, when a NON-FINITE bbox corner turned out to give two wrong answers to one question: it survived every clamp on web and yielded an EMPTY pack, while on Dart it reached `.floor()`, threw an unrelated "Unsupported operation", and the caller's catch-all reported the route as "too large". Both now refuse the corner outright — a silently empty offline pack is the worst of the three for a runner about to lose signal. 10 mirror tests each), `route_loop` (web `routes/route_loop.ts` ↔ mobile `route_loop.dart`; the Route Builder's "generate by distance" math — `generateLoopWaypoints` (a radial scaffold when the end pin is absent or within `NEAR_POINT_M` of the start, a perpendicular-curved point-to-point otherwise, both keeping the user's own pins EXACTLY at the endpoints because the routing layer trusts them), `bisectScale` over `[0.05, 2]`, `isWithinAcceptBand`, `isValidTargetDistance` and `selectLoopAnchors`. Registered 2026-09-02 after both halves were measured identical to nine decimal places over 5,543 inputs — until then the pair's own headers declared lockstep while neither registry carried it, so nothing had ever compared them, and the web header pointed at a path the file had moved out of. **Read that nine-decimal figure as a property of the SWEEP, not as a bound on the pair** (§ 1526): at the time the two halves were reaching two different great-circle functions — `route_loop.dart` the clamped `haversineMetres`, `route_loop.ts` the unclamped `atan2` through `routing_quality.haversineM` — and those two forms differ by up to 8.2e-8 m over the globe (§ 1470's measured figure; a 160,000-pair sweep taken while writing this reached 7.8e-8 m), which is two orders of magnitude coarser than the 1e-9 m a nine-decimal claim about metres asserts. The sample was loop-scale, where the divergence measures 4.5e-13 m over 200,000 legs under 2.2 km, so the number was true of the inputs and not of the functions. It is now true of the functions too: § 1523 moved every remaining copy onto the canonical, so both halves compute one expression and the pair agrees by construction rather than by measurement. One idiomatic shape difference, not a divergence: web's `isValidTargetDistance` takes `unknown` and rejects a non-number, which Dart's `num?` signature makes unreachable. 71 tests web / 52 Dart — the Dart suite was brought up to the web one's field-coordinate battery when the pair was registered; web runs nine cases per coordinate where the mirror runs five, because three of the nine are one case on the Dart side and the other two are already covered by that suite's own non-parameterised cases), `route_snap` (web `routes/route_snap.ts` ↔ mobile `route_snap.dart`; `snapToPolyline` projects a dropped lng/lat onto the nearest perpendicular foot of a route's polyline (not merely the nearest vertex), returning the snapped point + segment index + along-route distance preview + perpendicular offset — the render-only projection behind the course-marker "snap to route line" editor toggle (`position_m` is still derived server-side); the pure half of the route_markers.md mobile snap-to-line followup, 10 mirror tests each), `live_cutoff_eta` (web `runs/live_cutoff_eta.ts` ↔ mobile `live_cutoff_eta.dart`; `nextCutoffEta` fuses a live runner's distance-along-route + recent pace with the roadbook cutoff legs to project arrival at the next cut-off and grade it on/tight/behind — but returns `unknown` when the position is stale, never a false ETA off an old fix; reuses `CUTOFF_TIGHT_S` from `roadbook`, flat-pace projection like `checkpoint_projection`, the spectator-side companion behind the /live/[id] next-cut-off card — and since #607 also reports `requiredPaceSecPerKm`, the flat pace still sufficient to make the next cutoff, computed even when the fix is stale because it doesn't depend on recent pace, plus an explicit `limitPassed` flag — requiredPace is null for TWO reasons (limit passed / <50 m out) and a "you cannot make it" surface must never fire from the second (feeds the mobile cutoff catch-up voice cue, and since decisions §601 both the /live/[id] card and the mobile `CutoffCard` state the expiry and the required pace outright rather than only "Signal lost"). `distAlongRouteM` is **nullable**: an unlocated runner collapses to the no-checkpoint shape, because a caller substituting 0 names the FIRST cutoff and measures the distance-to-go from the start line, 186 mirror tests each), `race_phases` (web `runs/race_phases.ts` ↔ mobile `race_phases.dart`; `buildPhasePlan` turns a race distance + preset (`ten_ten_ten` / `negative_split` / `even`) into distance-bounded intent phases — 10-10-10 generalised proportionally from the marathon 10mi/10mi/10K, final-phase factor DERIVED so the distance-weighted mean factor is exactly 1.0 — plus `phaseAt` (start-inclusive/end-exclusive, clamps, empty → -1), `phaseTargetPaceSecPerKm`, and `goalPaceSecPerKm`; intent is an identifier, labels resolve at the render layer; backs the #609 mobile race-strategy sheet, live phase chip, and phase-transition voice cues, 11 mirror tests each), `recap` (web `runs/recap.ts` ↔ mobile `recap.dart`; `buildYearInRunningRecap` + `buildMonthInRunningRecap` + `computeRecapBadges` + `recapHeadline` — the Year-in-Running / "Wrapped" aggregator: totals, longest/fastest (run-family only, cross-modal totals), top week, monthly strip, streaks, the earned trophy grid, and the `RecapExtras` photo / PR counts. The monthly builder projects out one calendar month off the same engine. The share-image builders `recap_share_image.ts` / `og_recap_image.ts` are web-only — the mobile share path links to the web public-share page, no Dart twin. The Dart-only `recapSnapshotJson` is glue that serialises the recap into the SAME field shape the web `YearInRunningRecap` object carries, so a mobile-published `public_recaps` snapshot renders identically on the web share page. 31 mirror tests web / 30 Dart — the extra Dart cases pin `recapSnapshotJson`), `challenge_progress` (web `social/challenge_progress.ts` ↔ mobile `challenge_progress.dart`; `progressFraction` (clamp 0..1, null-goal→null) + `isComplete` + `progressParts` + `metricFromActivity` (the SAME metric-extraction the `challenge_leaderboard` / `recompute_challenge_completion` SQL aggregate uses — including the DNF exclusion: a DNF'd run contributes 0, matching the server's `is_dnf = false` filter, with `is_dnf` projected into the activities-view runs summary by migration `20270408_001` — so an offline-optimistic client estimate can't drift from the server board) + `rankParticipants` (deterministic value-desc → id-asc sort + dense `rank()` ranking mirroring the SQL window) + `challengePace` (on-pace projection for a time-boxed goal challenge — even-pace line, ahead/on_track/behind verdict within the shared `ON_PACE_BAND`, linear final-value projection, and the daily rate still needed to finish; a read-time re-shape of the leaderboard value, no new data) + `challengesToRecomputeForRun` (picks the runner's joined, goal-bearing, not-yet-completed challenges whose `[startMs, endMs)` window covers a just-saved run — the set the client fans the opportunistic `recompute_challenge_completion` RPC over on save (web `recomputeChallengesForRun` in `data.ts` from `createManualRun`/`saveRun`; mobile `SocialService.recomputeChallengesForRuns` from `SyncService` after `saveRunsBatch`), so completion no longer waits up to ~24h for the cron sweep); backs the challenge progress bar + leaderboard + the on-pace hint on both platforms, 31 mirror tests each), `catalogue_browse` (web `segments/catalogue_browse.ts` ↔ mobile `catalogue_browse.dart`; the browse shaping behind the famous-segment catalogue list — `catalogueRegions` / `catalogueSurfaces` build the two filter vocabularies, `filterCatalogue` narrows on an accent-folded name-or-region query plus whole-value region / surface, and `sortCatalogue` orders by name / shortest / longest / most-climb. Region and surface are deliberately asymmetric: a region is free-text curator input so it folds on BOTH the dedupe and the compare — a dropdown that collapses “Zürich, CH” and “zurich, ch” onto one option must not then filter out half the rows that option claims exist — while `surface` is a CHECK-constrained token compared verbatim, because folding a database identifier would invent an equivalence the schema does not have. An absent or unusable distance / elevation sorts LAST under every numeric order INCLUDING the descending ones: an unknown climb must never be presented as the biggest climb. **The accent fold is a THIRD rail, generated rather than written**: web computes it (NFD, drop every `Diacritic`, lowercase, collapse the Greek final sigma) and Dart's core library can do none of those, so the answer is generated from Unicode's own data by `scripts/gen_catalogue_fold_table.mjs` into the committed `apps/mobile_android/lib/catalogue_fold_table.dart` — never hand-edit it, re-run the generator. The hand-written table it replaced stopped at Latin Extended-A and left **14,719 code points folding differently** (every Vietnamese letter, all of Greek, the pinyin tone letters, 57 Cyrillic, the CJK compatibility ideographs, all 11,172 Hangul syllables, 7 spacing diacritics inside Latin-1), so a reader typing `hai` reached `Đèo Hải Vân` on the web and not on the phone and `sortCatalogue` ordered the catalogue differently on the two platforms — invisible to `check_parity_pair_registry.mjs`, which compares the two REGISTRIES and not the two behaviours (decisions § 852). The table carries the case mapping too rather than composing with `String.toLowerCase`, because Dart's case tables are older than the web's on 466 code points (§ 854); Hangul is computed, not tabled (§ 856). Letters with no canonical decomposition (`ø`, `đ`, `ł`, `ß`, `æ`, …) stay unfolded on BOTH sides on purpose — folding them would invent an equivalence Unicode does not have. Two guards, two claims: `scripts/check_catalogue_fold_table.mjs` compares the committed table with a fresh render, and `segments/catalogue_fold_table.test.ts` runs the SHIPPED web `fold` against that table (§ 855). One idiomatic shape difference beyond that: web's `CatalogueSort` string union is a Dart enum. 47 tests web / 46 Dart — web's extra case pins the PostgREST stringly-`numeric` coercion `GlobalSegmentRow.fromJson` performs at the Dart side's own boundary, the Dart extra pins the generated table's own shape, which web has no analogue for), `leaderboard_standing` (web `social/leaderboard_standing.ts` ↔ mobile `leaderboard_standing.dart`; `standingFor` re-shapes the leaderboard rows the caller already holds into the viewer's own line — rank, board size, tie count, and the metric gap to the nearest entrant strictly above and strictly below — behind the "Your standing" card above the board on `/challenges/[id]` and on `challenge_detail_screen.dart` (decisions § 615). Rank is DERIVED as one plus the count of strictly better values rather than read off the row, which is `rank() over (order by value desc)` by definition, so the card cannot contradict the rank the SQL sent and the list renders; neighbours tie-break on `entryKey` ascending, mirroring `challenge_leaderboard`'s `order by rank, <key> nulls last`, so two refreshes name the same entrant. It fails closed to claiming NOTHING — an unidentifiable viewer, one absent from the board, and a non-finite own value each return null rather than a fabricated rank, and a non-finite RIVAL lands in neither neighbour slot while still counting toward the board size. A tied leader reports the tie rather than "Leading": sole leadership is a claim the data does not support. On a club-vs-club board the entrant is the club the viewer JOINED UNDER, off their own `challenge_participants` row (`ChallengeWithMeta.my_team_club_id` / `ChallengeView.myTeamClubId`) — resolving it from whichever of their clubs appears on the board tells the trailing team it is leading whenever a runner belongs to two competing ones. Both surfaces suppress the card on a board of one, so nothing ever renders "#1 of 1". One idiomatic shape difference, not a divergence: TS's structurally-satisfied `StandingEntry` bound is a declared abstract `StandingEntry` interface in Dart, which `ChallengeLeaderboardEntry` implements. 16 mirror tests each), `fundraiser_progress` (web `social/fundraiser_progress.ts` ↔ mobile `fundraiser_progress.dart`; `fundraiserProgress` resolves the goal-thermometer state from `(raisedCents, goalCents)` — the clamped bar fill, the UNCAPPED true percentage so the UI can say "118% — over goal!", the cents still needed, and the coarse state that drives the styling. A non-finite or non-positive goal yields a zeroed `starting` rather than a division by zero, and a negative raised floors to 0. Registered 2026-09-02 after the two were measured identical over every combination of 15 values of each argument; the ONE difference the sweep surfaced is unreachable, not a divergence — `remainingCents` is a Dart `int` against web's `number`, so a goal past 2^63 cents saturates on one side, and `fundraisers.goal_cents` is a postgres `integer` with a `> 0` CHECK. Web's `ThermometerState` string union is a Dart enum, as `password_change` records), `challenge_list` (web `social/challenge_list.ts` ↔ mobile `challenge_list.dart`; `mergeMyProgress` folds the `my_active_challenges` aggregate onto the joined-challenge list and `myProgressView` decides what a row may claim about it. The per-caller value exists only in the `challenge_leaderboard` aggregate, never on the `challenges` row, and that RPC's window is live-now-or-ended-within-7-days — so a row outside it has NO known value and both surfaces say so rather than drawing a confident 0 % beside the goal. `notStarted` is the one *known* zero (the window has not opened, so nothing can have counted yet), a served value always outranks the clock comparison so a client clock behind the server's cannot downgrade a real number to a presumed zero, and a non-finite value fails closed to unknown. `teamLabel` resolves a club-vs-club row's `team_club_id` against the names the viewer could read, reporting the unaffiliated bucket (`team_club_id` is nullable and its FK is `on delete set null`, so a deleted club leaves its people as a group the aggregate still sums) separately from a club RLS refused — and never the raw uuid, which is meaningless to a reader. Two idiomatic shape differences, neither a divergence: Dart has no object spread, so `mergeMyProgress` returns each row paired with the values it should carry and the caller applies them through `ChallengeView.withProgress`, where web returns rebuilt rows; and the web twin's unparseable-`starts_at` guard has no analogue against a typed `DateTime`. 17 mirror tests Dart / 18 web), `challenge_goal` (web `social/challenge_goal.ts` ↔ mobile `challenge_goal.dart`; what a challenge goal is TYPED in against what `challenges.goal_value` STORES, and whether the stored figure can be reached at all. The column is metres for `distance` and `vert`, seconds for `duration` and a bare count for the other two, so `challengeGoalUnit` names the unit the field asks for and `challengeGoalToStored` converts at the boundary — the entry/exit split `paceSecPerUnit` / `paceSecPerKm` already keeps for a typed pace. `checkChallengeGoal` is the CLIENT half of `challenges_goal_ck` (migration `20270615_001`), which is a third rail like `nearby`'s: the SQL computes the same ceiling and the two must agree or a refusal reaches the author as a raw 23514 naming neither bound. **`streak_days` is the only metric the window bounds**, and both suites pin that a `duration` goal longer than its own window is NOT refused — the aggregate sums `duration_s` over runs whose START falls inside the window, so a run begun a minute before it closes carries its whole duration and a 112-hour Moab finish can satisfy it. The ceiling is `floor(length / one day) + 1`, the count of UTC dates a window of that length can touch, and it is deliberately the LOOSE bound: a window ending exactly at midnight touches one fewer, and refusing a goal the aggregate could award is worse than admitting one it cannot. `not_positive` covers 0 as well as the negatives because the completion RPC compares `value >= goal` with no floor, so a stored 0 awards the badge to every participant on the nightly sweep while both clients render the board as goal-less. Two idiomatic shape differences, neither a divergence: web's string unions are Dart enums, and web's non-finite guards on the window bounds have no Dart analogue against a typed `int`. `challengeGoalFromStored` is web-only glue for the edit path, which has no mobile surface per § 24. 16 tests web / 14 Dart), `weekly_goal` (web `settings/weekly_goal.ts` ↔ mobile `weekly_goal.dart`; the weekly distance goal the runner TYPES in km or mi against the `weekly_mileage_goal_m` metres the bag STORES (issue #902 § 5), the same entry/exit split `challenge_goal` keeps. `weeklyGoalToInput` shows a stored goal to one decimal in the reader's unit; `weeklyGoalFromInput` rounds a typed goal to whole metres and hands back the stored metres UNCHANGED when the typed figure is exactly what the field shows for them, because the display rounds and converting it back would move a goal nobody edited (50000 m shows as 31.1 mi, which is 50051 m). `isUsableWeeklyGoalInput` is the 0.1–500 range in the typed unit, the bounds mobile's dialog already enforced. Both suites sweep every one-decimal goal in the range through a save and a reload in both units. Rounding differs for negative halves (`Math.round` toward +∞, Dart `round` away from zero), which is why both halves refuse a non-positive figure before rounding. 9 tests web / 9 Dart), `badges` (web `social/badges.ts` ↔ mobile `badges.dart`; `BADGE_CATALOGUE` + `evaluateBadges(input) → EarnedBadge[]` + `tierFor` for the achievements/badges read surface — the catalogue ids, tier thresholds, tier order, icon ligature names, and dispatcher output must be identical on both platforms and consistent with the SQL `award_achievements_for_user` thresholds; only the i18n key strings differ by convention — web carries dotted `MessageKey`s, mobile the camelCase ARB keys — so the label/desc keys are NOT part of the lockstep, the thresholds/ids/tiers/icons are. 15 mirror tests each), `onboarding` (web `settings/onboarding.ts` ↔ mobile `onboarding.dart`; the post-signup setup-wizard contract — `primaryGoalKey`/`PRIMARY_GOAL_KEY`, the six-value `primaryGoalValues`/`PRIMARY_GOAL_VALUES` enum in lockstep order, `onboardingTotalSteps`/`ONBOARDING_TOTAL_STEPS`, and `planPresetForGoal` (maps a primary-goal answer → `{goalEvent, beginnerWalkRun}` create-plan preset for the finish-step "create my training plan" CTA — beginner goals seed the walk-run 5K). Both sides carry the enum + key + step count + preset mapping only — goal *labels* are localized on both platforms (mobile via ARB, web via the `onboarding.goal.<value>` i18n keys resolved with `m()`), never held as English strings in the helper. Each side ALSO derives which steps its own wizard walks, and those two are counterparts rather than a pair: web's `visibleOnboardingSteps` drops `notifications` when this browser cannot be subscribed, mobile's `visibleSetupWizardSteps` drops `run-privacy` when the first-launch flow already asked it. `ONBOARDING_TOTAL_STEPS`/`onboardingTotalSteps` stays 7 on both — the full wizard, before either drop), `run_heatmap` (web `routes/run_heatmap.ts` ↔ mobile `run_heatmap.dart`; `buildHeatCells` grid-quantises many of the runner's own tracks into clamped weighted cells + `heatBounds` returns the `[[w,s],[e,n]]` fit box — the personal run-track heatmap (`/runs/heatmap` ↔ `run_heatmap_screen.dart`) aggregation; the web GeoJSON emitters are MapLibre-specific and have no Dart twin (mobile renders cells/tracks directly), 9 mirror tests each), `finisher_certificate` (web `runs/finisher_certificate.ts` ↔ mobile `finisher_certificate.dart`; web builds the certificate SVG string it rasterises to a PNG download, mobile renders the card natively via `RepaintBoundary` → share-sheet — so only the unit/locale-agnostic shaping is a true twin: `isCertificateEligible` (finished + organiser-approved) + the `formatCertificateTime` / `formatCertificateDistance` / `ordinalPlace` formatters mirroring web's `fmtTime` / `fmtDistance` / `ordinal`; the persona #44 downloadable finisher certificate), `cycle_plan` (web `training/cycle_plan.ts` ↔ mobile `cycle_plan.dart`; the menstrual-cycle- and pregnancy-aware plan adjustment of decisions § 231 — the PURE half only: the inputs are GDPR Art 9 special-category reproductive-health data, so the consent gate, the fail-closed feature flag and the persistence all live at the call sites. Eases only menstrual and late-luteal days, by 15 % and never the 40 % of a recovery week, and never touches the goal race. Registered 2026-09-02 after closing a divergence in exactly the direction the module's own fail-safe rule forbids: the anchors (last-period start, due date) come out of the jsonb settings bag, and web's `parseInt` turned a malformed one into a NaN that propagated to "no adjustment" while Dart's `int.parse` THREW. Both now state the ISO-date check outright and `daysBetweenIso` returns null on either side. Web's discriminated `CyclePlanConfig` union is a Dart class with two named constructors, `Trimester` is a plain `int?`, and the patch is a field map rather than an optional-property object. 26 mirror tests each; no mobile surface consumes it yet, a ported twin like `race_predictor`'s), `readiness` (web `training/readiness.ts` ↔ mobile `readiness.dart`; `computeReadiness` folds training balance (TSB), last night's sleep and resting-HR drift into deltas around a neutral 75 and bands the clamped result. Registered 2026-09-02, and the registration found a latent divergence: `dominantAdvice` picks the contributor with the largest `abs(delta)` and TIES ARE COMMON — a TSB of -12 and six hours of sleep both score -12, and the two carry different sentences. `Array.prototype.sort` is stable so web deterministically shows the first contributor added; Dart's `List.sort` is NOT, and only happened to agree because its small-list path is an insertion sort. The Dart half now carries an explicit index tiebreak, the same shape `routine_history` uses. Web's `ReadinessBand` string union is a Dart enum), `race_predictor` (web `training/race_predictor.ts` ↔ mobile `race_predictor.dart`; `predictRaceLadder` projects the 5K/10K/Half/Marathon ladder from a pool of qualifying efforts — picks the anchor by recency-weighted Riegel equivalence (a recent strong effort outranks a stale PR, half-life 30 d) and grades each rung with `predictionConfidence` from `training`; reuses `riegelPredict` + `predictionConfidence` so the numbers can't drift; backs the `/dashboard` RacePredictorCard, 11 mirror tests each — Dart twin ported for a future mobile surface, web-only card today), `meal_template` (web `nutrition/meal_template.ts` ↔ mobile `meal_template.dart`; `templateFromEntries` (logged food entries → a named, ordered template draft, dropping blank-named items, deriving a default meal slot when the source entries agree) + `entriesFromTemplate` (a saved template → food-log inputs ready to insert, slot resolving item → template-default → log-time override; the one-tap "Log meal template" path) — the multi_modal.md Nutrition mid-tier saved-meals helper, mirroring `gym_routine`'s save/repeat shaping one tier simpler, 22 mirror tests each), `recipe` (web `nutrition/recipe.ts` ↔ mobile `recipe.dart`; `recipeFromEntries` (logged food entries → a named recipe draft, ingredients at quantity 1) + `sumRecipe` (sum every ingredient's macros × its quantity into ONE total, divide by `servings` for the per-serving macros — a missing macro on one ingredient contributes 0, never poisons the sum; rounds to 0.1) + `logInputFromRecipe` (the SINGLE summed `food_log` input one logged serving instantiates into) — the multi_modal.md Nutrition mid-tier "N ingredients → one logged meal" helper, sibling of `meal_template` but where a template logs each item, a recipe logs the SUM, 25 mirror tests each), `extended_nutrients` (web `nutrition/extended_nutrients.ts` ↔ mobile `extended_nutrients.dart`; the day roll-up for the five nullable `food_log` nutrient columns — `extendedNutrientTargets` (fibre off the BASE calorie goal because the extra carbohydrate a long-run day earns is deliberately low-residue; sodium a flat 2300 mg ceiling plus a sweat allowance capped at `hydration`'s own `MAX_EXERCISE_MINUTES`; saturated fat 10 % of the FULL dynamic goal; sugar and cholesterol deliberately ungraded) + `extendedNutrientBudgets`. **Coverage is the whole point**: both food sources carry these fields unevenly, so a plain sum reads as the day's intake while sitting under a ceiling it may have blown past. A total therefore carries how many entries reported it, the monotone claims (`exceeded` past a ceiling, `reached` at a floor) are offered under partial coverage because the unreported entries can only add more, and `remaining` — a claim about the day's WHOLE intake — is withheld outright, which is why neither surface renders an untargeted chip in its place. A nutrient nothing reported is omitted rather than shown as zero, and an empty result self-hides the section. Two idiomatic shape differences, neither a divergence: TS indexes the row structurally with `keyof` where Dart carries a nominal `ExtendedNutrientRow` with a `valueOf(column)` lookup, and `labelKey` is web's dotted `MessageKey` against mobile's camelCase ARB identifier — like `badges`, the key strings are NOT part of the lockstep, the kinds / columns / directions / units / thresholds are. Backs the self-hiding Nutrients section on `/nutrition` and `nutrition_screen.dart`, 22 mirror tests each), `diary_day` (web `nutrition/diary_day.ts` ↔ mobile `diary_day.dart`; which local calendar day the nutrition diary is showing, and the fetch window, water-storage key and entry `started_at` that day implies — every step goes through `DateTime(y, m, d + n)` / `new Date(y, m, d + n)` so a 23- or 25-hour DST day cannot repeat or skip a day (decisions § 589), and a future day always resolves back to today. Web's `DIARY_DATE_PARAM` is `/nutrition?date=` routing with no mobile analogue; web's ISO-instant `DiaryWindow` + string-returning `entryTimestampFor` read as Dart `DateTime`s, an idiomatic shape difference like `undo_queue`'s — the mobile store converts to UTC at its own boundary. 26 mirror tests each), `locale_defaults` (web `format/locale_defaults.ts` ↔ mobile `locale_defaults.dart`; `regionOfLocale` + `defaultUnitForLocale` (mi for US/GB/LR/MM) + `defaultWeekStartForLocale` (Sunday-first region table) — the locale-derived first-run defaults seeding web `/onboarding` + the mobile setup wizard's units step and the week-start fallback on both preferences screens; web additionally consults `Intl` week data when available, so the lockstep contract is the region tables), `integration_visibility` (web `integrations/integration_visibility.ts` ↔ mobile `integration_visibility.dart`; `integrationStatus` / `integrationIsActionable` / `integrationIsVisible` / `integrationIsStranded` / `gateIntegrations` / `visibleIntegrations` — whether a deployment may OFFER an integration, fail-closed, with a connected row kept visible whatever its gate says so it can still be disconnected. 12 mirror tests each. Web's `IntegrationGate` / `IntegrationStatus` are string unions where Dart uses enums, which is the only shape difference; the catalogues that feed it are deliberately NOT part of the pair — web's `availability.ts` lists four connect providers because a browser can be offered Garmin Connect and HealthKit cards it can never honour, while the phone's two are the two it has screens for, and parkrun's gate means different things on each side (a placeholder row on web, a real athlete-number import on mobile)), `parkrun_regions` (web `integrations/parkrun_regions.ts` ↔ mobile `parkrun_regions.dart`; `parkrunLikelyUnavailable` over the parkrun country footprint — gates the "may not have events near you" disclosure on the integrations parkrun card/tile without gating the connect path), `import_failures` (web `integrations/import_failures.ts` ↔ mobile `import_failures.dart`; `classifyImportFailure` buckets a thrown value into the reason a bulk import surfaces — network / auth / rate_limited / too_large / unparseable / rejected / unknown — and `recordImportFailure` / `groupImportFailures` / `importFailureReportCsv` build the capped, shareable report behind it. Precedence is the contract: an "invalid token" reads as auth rather than as an unreadable file, and any remaining SQLSTATE means the server answered and refused, so a 22P02 is `rejected` rather than falling through to the `invalid` message pattern. `detail` carries only the log-safe `.code` / `.message`, never `.details` / `.hint`. Web renders it as `ImportFailureReport.svelte` on `/settings/integrations` behind the Strava + Garmin ZIP importers; mobile as `widgets/import_failure_report.dart` on the import screen behind Strava / Health Connect / CSV, each importer recording at the point it catches rather than downstream of a discarded error. Two idiomatic Dart differences, neither a divergence: web's structural `.message` / `.code` / `.status` reads become a `Map` branch plus a duck-typed getter read, and a Dart `Exception` carries no `.message` getter at all so its `toString()` is the only message there is. 23 mirror tests each), `notification_groups` (web `social/notification_groups.ts` ↔ mobile `notification_groups.dart`; `groupNotifications(rows, nowMs?)` collapses same-kind + same-target notifications within a 24 h window into a lead + `otherCount` + `unreadCount` — the "Alice and N others" inbox collapse; target key per kind (run/event/club/plan/achievement/challenge id, follows share a bucket, messages group by actor, anything else stays solo), clock-skew-clamped effective time, groups returned newest-lead first; backs the web `NotificationsList` + mobile `profile_screen` notifications tab, 17 mirror tests each), `safety_nudge` (web `safety/safety_nudge.ts` ↔ mobile `safety_nudge.dart`; `shouldNudgeSoloSafety` + `shouldSurfaceSoloSafetyNudge` (folds `nudgeThrottled` over an explicit acted-on `lastActedAtMs` so a caller can't throttle from shown-not-acted — issue #264) + `isDarkOutside` + `isNightWindow` (fixed 20:00–06:00 local FLOOR) + `isSunDown` (seasonal, latitude+day-of-year sunrise/sunset — solar noon assumed 12:00, catches dark pre-dawn winter runs at higher latitudes) + `nudgeThrottled` (30-day suppression, anchored on the runner's ACTION not on merely showing the banner) — the solo-run after-dark "share a live link" nudge decision. `SoloSafetyNudgeInput` / `SoloSafetyNudgeSurfaceInput` carry an optional `latitude`+`dayOfYear`; null (no GPS fix) degrades to exactly the fixed window, and the seasonal test only ever ADDS darkness (the window widens, never narrows, so the fixed floor absorbs the noon-assumption error). Mobile-only surface (recording is mobile-only, decisions §235): the run screen renders the L4 **persistent dismissible** `SafetyNudgeBanner` (stamps the throttle only when the runner acts) and feeds `_currentPosition?.lat` + the local day-of-year, web carries only the twinned decision logic, 30 mirror tests each), `live_motion` (web `safety/live_motion.ts` ↔ mobile `live_motion.dart`; `motionFor` grades a window of recent spectator pings into moving / stopped / unknown — the complement to `live_freshness`, which answers whether the runner can be SEEN but not whether they are going anywhere. A runner still pinging from one spot renders as a fresh live dot and, because the pace delta is zero, drops the recent-pace readout entirely, so "not moving" and "no data yet" looked identical and the more alarming of the two is the one that vanished. Fail-closed in three directions, and all three are the point of the helper: a STALE fix claims nothing (the last position being old is exactly when "they have not moved" is unknowable), a GAP inside the buffer discards everything before it (a runner who reconnects near where they dropped out was never observed standing there — the outage must not be counted as stillness), and a window shorter than `motionMinWindowMs` claims nothing (every runner alive stands still for twenty seconds at a road crossing). The `atLeast` flag says the stop reached the start of the vouched window, so the surface must say "at least N min" rather than state the figure flat. Web renders the chip on `/live/[id]`, mobile on `live_spectator_screen.dart`, both neutral rather than alarming — a runner stopped for six minutes is at an aid station. `atMs` is a Dart `int` epoch-ms against web's `number`, so the non-finite guard covers only the odometer on the Dart side; 18 mirror tests each), `column_limits` (web `core/column_limits.ts` ↔ mobile `column_limits.dart`; the bound a CLIENT puts on an input for a column the database also bounds — eight `<table>.<column>` entries plus `COLUMN_CHECK_MAX`/`kColumnCheckMax`, which is the opposite direction: the ceiling a defensive filter over a value read BACK out must use, so a row the column legitimately holds is not discarded as garbage. A THIRD rail like `text_limits`': `scripts/check_shared_constants.mjs` resolves each column's CHECK by replaying every migration and demands CONTAINMENT of the input bounds and EQUALITY of the check maxima (decisions § 792 + § 819). Registered 2026-09-02 — both headers declared the pair while neither registry carried it, though the shared-constants guard was already reading both halves. The accessors are named differently by convention, not by behaviour: Dart splits web's `{min, max}`-returning `valueLimit` into `columnMin` / `columnMax`, and `withinValueLimit` is `withinColumnLimit`), `text_limits` (web `core/text_limits.ts` ↔ mobile `text_limits.dart`; the four character caps on user-authored text — club name / description / location label and display name — and a THIRD rail like `nearby`'s: each has a `<table>_<column>_len_chk` of the same number in migration `20270502_001`, and both suites parse that file to prove it. A composer that caps LOWER than the constraint is merely conservative; one that caps higher hands the user a postgres 23514 they cannot act on, which is what web's 600-character club description and mobile's 500 both did against a column that then had no constraint at all (decisions § 545). Registered 2026-09-02 — both headers had declared the pair while neither registry carried it. The Dart map is keyed constraint-name-to-cap where web's is cap-name-to-constraint, an idiomatic shape difference the two suites each read their own way), `club_slug` (web `social/club_slug.ts` ↔ mobile `club_slug.dart`; the public club URL, derived from the club name and PERSISTED as `clubs.slug` at create time. The derivation used to be written twice — inline in web's `core/data.ts` and as `_slugify` in the phone's club form sheet — as "lower-case, then `[^a-z0-9]+` to `-`", which reads as one expression and is not one function: the two runtimes' `toLowerCase` disagree at 466 code points and the one reachable in Latin text is U+0130, where JS emits `i` plus a combining dot that the strip then turns into a SEPARATOR. `İzmir` therefore minted `i-zmir` on the web and `izmir` on the phone — a different permanent public URL for the same club name depending on which client created it (decisions § 1251 + § 1279). The fold is `catalogue_browse`'s generated accent-plus-case table rather than either runtime's own lower-case, which also makes the slug do what a slug is for: `Zürich Runners` reaches `zurich-runners` rather than `z-rich-runners`, because a stripped diacritic leaves its base letter where the raw strip left a hyphen. Letters with no canonical decomposition (`ß`, `ø`, `đ`) are deliberately NOT transliterated — the fold refuses equivalences Unicode does not have (§ 856) — so they still strip. The 48-character cap is part of the pair for the same reason: `clubs.slug` carries no CHECK, so nothing but these two rails bounded it and only ONE of them did. Empty is a real answer rather than an error, so the helper returns it and both callers substitute `CLUB_SLUG_FALLBACK` / `kClubSlugFallback` — which is in the pair for the same reason the derivation is, since it too reaches `clubs.slug` as a public URL. The phone used to refuse that case instead, so a club named entirely in Cyrillic, Greek, Hebrew, Arabic or CJK could not be created there at all while the web created it happily (decisions § 1281); the phone's pre-flight tests the NAME rather than the slug, and that predicate — `clubNameNamesSomething` — is now IN the pair rather than the mobile-only affordance it started as: the web editor gated on `!name.trim()` and nothing else, so a club called `!!!` was creatable there and refused on the phone, landing under the fallback slug (decisions § 1338). It is a REFUSE-list, `[^\p{Z}\p{P}\p{S}\p{Cc}\p{Cf}]`, and deliberately not the accept-list `[\p{L}\p{N}]` the phone used: a Unicode property class is answered by each runtime's own tables exactly as `toLowerCase` is, and over all 1,112,064 assignable scalar values the accept-list diverges between Node and Dart at **4,657 code points** — every one a letter the newer table knows and the older refuses, which is § 1281's bug verbatim — against **104** for the refuse-list, all of them newly-assigned SYMBOLS in the harmless direction (web stricter). `\p{Cn}` stays OUT of the class for that reason even though `\p{C}` would be shorter; `Cc` and `Cf` are named individually so a name of nothing but control characters is still refused. 18 mirror tests each), `auth_gates` (web `core/auth_gates.ts` ↔ mobile `auth_gates.dart`; `checkPasswordPair` + the `MIN_PASSWORD_LENGTH`/`minPasswordLength` floor — each a re-export of the canonical minimum (`PASSWORD_MIN_LENGTH` in `auth_rules.ts` / `kPasswordMinLength` in `auth_validation.dart`, 8, enforced server-side via config.toml's `minimum_password_length`) — the two-field check every password-MINTING surface runs: at least the floor, then exact equality, length reported first, **neither side trimmed** (whitespace is a real password character and is exactly the typo class this catches). A single-field sign-up bakes a typo into the account: GoTrue hashes it, the confirmation mail is clicked, and the account is then unreachable by its owner — visible to us only as a confirmed account whose `last_sign_in_at` stays null. Web has three minting surfaces (sign-up / reset / change-password), mobile only sign-up, so web's `checkSignUpGates` consent half is NOT twinned — mobile's sign-up screen is separate from sign-in, so those gates stay inline in `sign_up_screen.dart`'s `_checkGates`. Web's `{ok} | {ok, reason}` union is a nullable reason in Dart; 17 mirror tests each), `password_change` (web `core/password_change.ts` ↔ mobile `password_change.dart`; the change-password step-up — `checkPasswordPair` first (reused from `auth_gates`), then a non-empty current password, then a POSITIVE `verifyCurrentPassword` before `updatePassword`; a rejection, a thrown error, and a session with no email all fail closed identically (OWASP ASVS V2.1.14 / CWE-620, issue #381, decisions §278). Web models the failure as a `{ok, reason, detail?}` union, Dart as a small `PasswordChangeResult` class; the reason set is the lockstep. 7 mirror tests each), `streaks` (web `runs/streaks.ts` ↔ mobile `streaks.dart`; `computeRunStreaks` — the COMPUTE pair `streak_card` refers to, deliberately separate from it. A streak is consecutive LOCAL days each holding a run, with the Strava grace day (a missing today does not break it while yesterday had one) and a `bestSince` bound that admits a streak the moment it reaches the period and then records its FULL length, days before the bound included, so one running 28 Dec to 3 Jan is seven days long on the January card. Every day step goes through the y/m/d constructor on both sides rather than an 86,400,000 ms subtraction, because a 23- or 25-hour DST day would otherwise repeat or skip one. Registered 2026-09-02 — both headers had declared the pair and CLAUDE.md's own `streak_card` entry called it "the compute pair", while neither registry carried it; measured identical over 16,449 cases, every subset of a 12-day window crossed with four `bestSince` bounds, plus a spring-forward boundary and future-dated runs), `streak_card` (web `runs/streak_card.ts` ↔ mobile `streak_card.dart`; `streakCardState` — which claim the dashboard streak card may make from which source: the `run_streaks_for_user` all-time row drives headline + sub-label when it has answered, and a null — loading, offline, signed out — suppresses the numeric best/"all-time best" claim entirely rather than presenting the windowed/resident figure as truth (decisions § 471/§ 475). Deliberately separate from `streaks` (the compute pair). The Dart-only `mergeAllTimeStreaks` is a documented mobile extra outside the pair: it pointwise-max-folds the RPC row with the local compute so a just-recorded unsynced run is never walked back — web needs no twin because its windowed fetch reads the same database the aggregate does. 7 mirror tests each), `route_simplify` (web `routes/route_simplify.ts` ↔ mobile `route_simplify.dart`; `simplifyTrack` (RDP) + `computeElevationGain` + the `ELEVATION_GAIN_MIN_DELTA_M` / `kElevationGainMinDeltaM` noise gate. Both doc comments had claimed lockstep for a long time while the two carried DIFFERENT algorithms and each suite pinned the opposite answer — see [decisions.md § 305](decisions.md). Gain is always taken over the RAW track: RDP measures perpendicular distance in 2-D, so a straight road over a summit collapses to its endpoints and grading the simplified line reads the climb as zero. The scope of the pair is those two functions plus the constant, 13 mirror tests each — `summarizeRouteFromTrack` is web-only (mobile composes the two calls at the save-as-route call site) and `computeElevationLoss` is mobile-only (the run-detail stat shown beside gain, gated identically so the pair can't read as a bug), as is `simplifyToBudget` (priority DP to a fixed POINT COUNT rather than to a tolerance — the custom watch's 256-point `CRS1` course budget, decisions §370; web has no fixed-capacity course consumer to mirror it)), `geo` (web `routes/geo.ts` ↔ mobile `geo.dart`; `lonDeltaDeg` / `unwrapLonDeg` / `wrapLonDeg` — the longitude normalisation EVERY local planar frame on both platforms must take its east-west deltas through, ported from the firmware's `watch_core::geo` (decisions §463/§468). A plain longitude subtraction is wrong by a whole turn across 180°, so an uncorrected frame reads a point just past the line as ~40,000 km away and a bounding box as spanning 359.9°. Trigonometric formulae (haversine lengths, great-circle bearings) are periodic and immune — `privacy` is the named helper that needed no fix for exactly this reason, pinned by a test on both sides. Consumers today: `route_geometry`, `route_snap`, `route_simplify`, `track_projection` (incl. its `isTrackRenderable` 5 m span gate), `run_heatmap`, `route_loop` (both platforms), and the web-only `routing_quality` + `nearest_track_point` (decisions §473). Identity bit for bit inside a hemisphere — `unwrapLonDeg` is written as the longitude less whole turns, NOT the watch's `reference + delta`, so no rounding creeps into the common path; 6 mirror tests each), `undo_queue` (web `core/undo_queue.ts` ↔ mobile `undo_queue.dart`; the deferred-commit undo contract — `undoWindowSFromPref` (an absent / non-numeric / unrecognised `undo_window_s` falls back to 8 s and **never** to `0`) plus the queue itself: the mutation is HELD for the window rather than performed and later compensated, so undo cancels a timer and cannot fail; one slot (a second destruction commits the first and disarms its timer); `restore` puts back a list snapshot so ordering survives; a commit that later fails restores AND reports; `pause`/`resume` spend only the remaining time and keep the published `id` + `windowMs` stable. Web's `createUndoQueue(deps)` factory reads as a Dart `UndoQueue(UndoQueueDeps)` constructor — an idiomatic shape difference like `password_change`'s union-vs-class, not a divergence. The HOSTS are deliberately NOT twinned and cannot be: web's `UndoBar.svelte` widens `Modal.svelte`'s Tab ring, mobile's `widgets/undo_bar.dart` is a root `Overlay` entry that outlives a route pop and lands above a modal barrier's `BlockSemantics` — Flutter has no Tab ring and its snack bar is unreachable under a barrier (decisions § 523). 14 mirror tests each), `ai_disclosure` (web `core/ai_disclosure.ts` ↔ mobile `ai_disclosure.dart`; the versioned AI-processing consent ladder — `AI_DISCLOSURE_VERSION_COACH`/`_ROUTE_AI`/`_CURRENT_VERSION` + `AI_DISCLOSURE_ERROR` and the fail-closed `checkAiDisclosure` / `aiDisclosureFromProfileRow` grading (`missing` / `stale` / `unknown`, where a version above the build's ladder DENIES rather than counting as more-than-enough). The pair's scope is that client half only — web's `gateAiDisclosure` + `aiDisclosureDenialBody` are server-side and owe no Dart twin, and the constants must also stay in lockstep with the SQL `ai_disclosure_current_version()`, which both suites pin by parsing migration `20270511_001`. decisions § 571 + § 573; 16 tests web / 11 Dart — the count differs by design, web's extra cases cover `gateAiDisclosure`), `coach_load` (web `training/coach_load.ts` ↔ mobile `coach_load.dart`; `acwr` + `injuryRiskBand` + `loadTrend` — the acute:chronic-workload-ratio band edges the coach roster classifies through. The `coach_roster_summary` RPC returns RAW acute (7 d) and chronic (28 d-averaged-per-7 d) sums and every surface bands them here, so the thresholds cannot drift between the roster column, the athlete detail, and mobile), `nearby` (web `social/nearby.ts` ↔ mobile `nearby.dart`; `NEARBY_BUCKET_BOUNDS_M` / `NEARBY_BUCKET_COUNT` / `nearbyDistanceBucket` / `nearbyBucketUpperBoundM` — the coarse distance buckets opt-in "runners nearby" discovery reports instead of metres, so a caller cannot triangulate an opted-in runner. A THIRD copy of the boundaries lives in the SQL `CASE` behind `discoverable_runners_near` (migration `20270424000005`); all three must agree or the phone and the web disagree about how far away the same person is), `off_route_alert` (web `safety/off_route_alert.ts` ↔ mobile `off_route_alert.dart`; `OffRouteAlertDetector` + `OFF_ROUTE_ALERT_DISTANCE_M` + `OFF_ROUTE_ALERT_SUSTAIN_MS` + `offRouteEscalationEnabled` — the sustained-off-route escalation to a trusted contact, the moving-but-lost complement to the overdue net that only fires on telemetry silence. Deliberately a larger distance than the run screen's 40 m off-route *banner*: a nudge and a contact-ping are different claims. Mobile-only surface like `safety_nudge` — web carries the twinned decision logic), `e164` (web `safety/e164.ts` ↔ mobile `e164.dart`; `normaliseE164` repairs a typed or pasted phone number into the bare E.164 the `safety_contacts.contact_phone` CHECK enforces, and REFUSES anything it cannot — a national-format number carries no country, and inventing one would arm the overdue escalation at a stranger. The one repair that is not cosmetic is the parenthesised trunk zero: `+44 (0) 7700 900123` stripped of brackets alone yields `+4407700900123`, a DIFFERENT number that still passes the CHECK, so the alert would deliver to nobody while the UI reported success — `(0)` is deleted whole before any other bracket is touched. Both suites pin that case. The ITU access prefix `00` reads as `+`; the hyphen family, non-breaking spaces, brackets, dots and slashes are stripped. 16 mirror tests each), `profile_query` (web `social/profile_query.ts` ↔ mobile `packages/core_models/lib/src/profile_query.dart`; `extractProfileId` resolves a pasted bare uuid or `/u/<uuid>` URL straight to that runner. The Dart side is the one pair that does NOT live under `apps/mobile_android/lib/` — it sits in the shared `core_models` package that `api_client` consumes, so it needs no iOS-twin mirror and its mirror suite is `packages/core_models/test/profile_query_test.dart`), `env_flag` (web `core/env_flag.ts` ↔ mobile `env_flag.dart`; `isTruthyFlagValue` — the ONE accepted-affirmative set every fail-closed feature gate on either platform parses through: `1` / `true` / `yes` / `on`, trimmed and case-insensitive, everything else — unset, empty, `false`, a typo — off. The set being one contract across both platforms is the whole point of the pair: it was previously copied inline into four mobile gates and three more web ones beside the canonical, and the narrowest copy (`checkpoint_checkin_screen.dart`'s `_weighInGate`, `1`/`true` only) meant an operator who set `WEIGH_IN_GATE=yes` got the Art 9 weigh-in fields left off while the same string turned every other gate on (decisions § 709). The two flags whose parser lives INSIDE another parity pair — `off_route_alert`'s `offRouteEscalationEnabled` and `plan_adaptive_replan`'s `adaptiveFitnessGateEnabled` — keep their named function and delegate to this one, the same re-export shape `auth_gates` uses for the password floor, so the feature keeps owning its flag while the parse stays single-sourced. Both suites carry a source guard that no other module may spell the set out; the mobile suite additionally pins that every dotenv key read under `lib/` is bridged from a `--dart-define` in `main.dart`, because release builds never load `.env.development` and three sign-off-gated flags were therefore unflippable in production. 7 tests web / 11 Dart — the five parser cases are the mirrored half; the Dart suite's four per-gate delegation cases and its release-reachability case have no web analogue, and web's `*_flag.ts` routing guard has no Dart one because mobile's gates do not live in `*_flag` modules), `health_consent` (web `core/health_consent.ts` ↔ mobile `health_consent.dart`; `healthUseDob` + `healthUseDobState` — which date of birth a HEALTH inference may use. `date_of_birth` is two records under one column name (§ 718): the `user_profiles` column is the **age record**, written by every entry point with no consent term because the under-18 floor in `search_user_profiles` / `discoverable_runners_near` is a child-protection purpose that must not be defeated by declining consent; the `user_settings.prefs` key is the **Art 9 health-use mirror**, written only under consent and cleared on withdrawal. Reading the column and feeding it to a VO2max, an HR max, a calorie target, an age grade or the masters recovery calibration spends the ungated record on the gated purpose, so those reads resolve it here and emit null without `health_data_consent_at` — the same shape `coach/context.ts` already applies to its own read of the mirror. A surface reading the settings-bag mirror needs no gate. The third state is the point of the pair being a grader rather than a bare accessor: `absent` (no date on record) and `consent_withheld` (a date on record the runner asked us not to use this way) are different sentences to show a reader, and both nutrition-targets metrics cards say the second outright rather than reporting "Not set" about a date the runner did set. Fail-closed everywhere — an unreadable row, a missing stamp, an empty stamp and a value too short to be a date each withhold. Two idiomatic shape differences, neither a divergence: web takes a `get_my_profile()` row of `unknown` shape where Dart takes the generated `UserProfileRow`, and web's string union is a Dart enum, as `password_change` records. Both platforms carry a source guard that the four health-use surfaces read the date through this module and never off the row (decisions § 722). 9 tests web / 6 Dart — web's extra cases are the empty stamp, the too-short date and the non-object row, none of which a typed `DateTime` can reach the Dart side as, `locale` (web `i18n/locale.ts` ↔ mobile `l10n/locale_support.dart`; the locale-negotiation contract every client shares — `SUPPORTED_LOCALES`, the endonym table, the exact-tag map, the base-language fallback and the RTL base set. The two DESCRIBED each other as twins in their own doc comments for eighteen days while being registered in neither registry, which is exactly how they came to hold different Portuguese answers with nothing able to notice (decisions § 760). Portuguese is the pair's whole reason to exist: two catalogues, one language, so the base tag has to name one of them and both platforms have to name the same one — `pt` → `pt-PT`, with `pt-BR` reached by its exact tag because Android, iOS and every browser send the region. **Order is load-bearing on ONE side only**: following the device locale leaves `MaterialApp.locale` null and Flutter resolves against `supportedLocales` itself, taking the first entry matching on language alone, so `pt-PT` before `pt-BR` is what sends a bare-`pt` device where `_baseToLocale` says it goes — measured, reversing them silently routes it to Brazilian. Web never negotiates off the order and uses it only to order the picker, but the lists are kept identical anyway. Three idiomatic shape differences, none a divergence: web's `parseAcceptLanguage` has no Dart analogue (a device hands Flutter a `List<Locale>`, not a q-list header), Dart carries the `localeToTag` / `localeFromTag` / `activeLocaleTag` registry web does not need because web speaks tags natively, and web's `'ltr' | 'rtl'` reads as Dart's `TextDirection`. 9 tests web / 19 Dart — the Dart extras are the tag registry web has no need of and the Flutter-resolution pin, which has no web analogue because no browser resolves a catalogue on our behalf)), plus the `track_projection.ts` ↔ `projectTrack` / `isTrackRenderable` helpers inside `track_preview.dart`. The `shared-library-syncer` agent (under `.claude/agents/`) detects divergence proactively after edits — invoke it whenever you touch one side of a pair. **That agent works from its own table, not from this list**, so a pair added here without a row there is a pair whose divergence is never detected (and the reverse reads as a single-platform helper); `scripts/check_parity_pair_registry.mjs` fails the PR when the two registries disagree in either direction, or when either points at a file that no longer exists. Adding a pair means editing both, in the same change. See decisions.md § 604.), `strava_sync_result` (web `integrations/strava_sync_result.ts` ↔ mobile `packages/core_models/lib/src/strava_sync_result.dart`; `parseStravaSyncResult` grades the `strava-import` Edge Function's backfill response. The page walk has seven exits and five of them leave activities in the lookback window unfetched — Strava throttling us (429/503), an upstream error, a transport failure, a malformed page, the 20-page safety cap — and only the throttle case ever carried a field, which neither client declared. `complete` is what separates them, and only an explicit `true` earns it: an unrecognised shape, an absent field and an embedded `error` all read as partial. That is the OPPOSITE direction to `cloud_export_helpers`' own `cloudExportShortfall`, deliberately — there an absent `complete` means an older deployment of one of two transports and warning on every export would be its own dishonesty, whereas here there is one transport shipped alongside its callers and the costs are not symmetric: the lookback window is measured from now, so a false "complete" is what stops the runner syncing again and the un-imported runs are then reachable only until they age past `lookbackDays`. A count is a non-negative integer or it is 0 — and a whole `double` counts on the Dart side too, because JSON has one number type and web's `Number.isInteger` cannot tell `12` from `12.0`. The Dart half is the second pair after `profile_query` to live in the SHARED `core_models` package rather than under `apps/mobile_android/lib/`, because `api_client` consumes it — so it owes no iOS-twin mirror and its suite is `packages/core_models/test/strava_sync_result_test.dart`. `resumable` is the same fail-closed shape one layer on (decisions § 768): the function records the window still to walk on `integrations.sync_cursor`, so a re-sync continues from the frontier rather than re-walking the same pages, and only an explicit `true` lets a surface say so — a truncation that got nowhere recorded no point, and a body claiming both `complete` and `resumable` resolves to `complete`, since a finished window subsumes any point inside it. An embedded `error` forces `complete` false but does NOT drag the resume point down with it. The pair also carries the lookback contract — `STRAVA_LOOKBACK_OPTIONS` / `_DEFAULT_DAYS` / `_MAX_DAYS` and `isStravaLookbackReachable` — because what a client may ASK for and what it may BELIEVE about the answer are one contract; the maximum is a third rail with the Edge Function's own `400 invalid_lookback_days` bound, pinned by a web guard that reads it. 17 tests web / 16 Dart — the extra web case is that guard, which has no Dart analogue because a Dart suite cannot read the TypeScript side's Edge Function), `import_completeness` (web `integrations/import_completeness.ts` ↔ mobile `packages/core_models/lib/src/import_completeness.dart`; `parseImportCompleteness` grades what a SCRAPER importer says it read — `parkrun-import`'s `{ imported, skipped, total, complete }` and `race-results-import`'s `complete` — where neither count reveals a shortfall on its own: a parkrun history capped at `MAX_PARKRUN_ROWS` and a finisher field truncated at 2,000 both present as a successful import of everything that was there. Fail-closed in `strava_sync_result`'s direction and for the reason that rule was written rather than by analogy — one transport per importer, shipped from this repo alongside its callers, so an absent `complete` is a body this build does not recognise rather than an older deployment of a second transport, which is what makes `cloud_export_helpers`' own `cloudExportShortfall` fail the other way. A `total` below `imported + skipped` is DISCARDED rather than shown: "12 of 5" is worse than no denominator at all, which is why `total` is nullable and the two sentences are separate keys rather than one with a defaulted placeholder. The module also owns the count/text primitives BOTH parsers grade with — `importResponseCount` / `importResponseText` — and `strava_sync_result` imports them rather than keeping a copy, because nothing compares two modules on the same platform and a copy would drift in silence; each suite carries a source guard pinning that composition. The direction is deliberate: naming the general rule after one provider is what put parkrun and race-results grading in a file called `strava_sync_result` for eighteen days. The Dart side is the third pair after `profile_query` and `strava_sync_result` to live in the SHARED `core_models` package rather than under `apps/mobile_android/lib/`, because `api_client` consumes it — so it owes no iOS-twin mirror and its suite is `packages/core_models/test/import_completeness_test.dart`. 7 tests each: six mirrored cases plus the per-platform source guard), `cloud_export_helpers` (web `backup/cloud_export_helpers.ts` ↔ mobile `export_job.dart`; the JOB half of the queued Art 20 export rail — `exportJobFromResponse` / `isExportJobActive` / `exportPollDelayMs` / `exportJobShortfall` plus the two endpoint-URL builders. Both clients read the same `POST /v1/export/jobs` + `GET /v1/export/jobs/latest`, so the status vocabulary and the fail-closed normalisation have to agree: an unrecognised status is TERMINAL on both sides carrying its own raw token (a client that keeps polling a status it cannot interpret polls until the tab closes or the battery dies), and a `ready` job that arrived without a URL is reported as a failure rather than rendered as a dead download button. Only an explicit `complete: false` claims a shortfall and `total` is floored at `count`, the same rule the synchronous response carried. The names differ because the registry keys a pair on its WEB basename; the Dart file is `export_job.dart` because “cloud export” is not mobile's vocabulary. The pair's scope is that job half only — web's `buildCloudExportBody` / `CloudExportResponse` belong to the Edge Function fallback, which mobile has never had. 14 mirror tests Dart / 12 in-scope web — the Dart extras pin `none` as a real answer and the enqueue body reading on the same vocabulary), `guided_runs` (web `training/guided_runs.ts` ↔ mobile `guided_runs.dart`; the scripted coach-voice cue library — three workouts, each an id, a `duration_sec` / `durationSec` and an ordered list of cue second marks, plus `cuesDue` (the cues falling in `(prev, now]`, idempotent on a repeated tick) and `isGuidedRunValid`. Both files' doc comments asserted lockstep and named the syncer agent by name while the pair was registered in NEITHER registry — § 641's shape exactly, a pair whose divergence nothing could detect. Registration alone would not have caught anything either, because this pair is **data, not an algorithm**: each suite tests its own rail against itself ("ids are unique", "a finish cue at exactly duration") and neither reads the other, so a mark moved, a duration changed or a workout renamed on one side passes both suites and the two surfaces then describe the same named workout differently — a different finish time under one title, or a cue one platform lists and the other has never heard of. It therefore carries a THIRD rail that is not another client: `scripts/check_shared_constants.mjs`'s `guided-run cue library` entry reads the ids **in order**, each duration and each ordered cue list out of BOTH files and fails the PR on any disagreement, refusing outright — rather than skipping — a mark written in a form it cannot evaluate, a run with no cues and a parse shorter than a library (decisions § 801). Deliberately NOT in the lockstep: the i18n KEY IDENTIFIERS, web's dotted `guidedRuns.easy30.cue0` against mobile's camelCase `guidedEasy30Cue0` — the same carve-out `badges` and `extended_nutrients` record for their label keys — 37 library keys on each of the seven web catalogues and 37 in each of the seven ARBs, which carry 8 further `guidedRun*` keys that are mobile screen chrome rather than library. Web's `GuidedTranslate` callback reads as the Dart side's `AppLocalizations` parameter, the idiomatic shape difference `password_change` records. 19 mirror tests each, `goals` (web `training/goals.ts` ↔ mobile `goals.dart`; the personal weekly / monthly run-goal engine — `evaluateGoal` folds a period's runs into per-target progress for the four `GoalTargetKind`s (distance, time, average pace, run count), and the period arithmetic under it (`periodStart` / `periodEnd` ↔ `goalPeriodStart` / `goalPeriodEnd` / `weekStartLocal`) steps whole calendar days through the y/m/d constructor on both sides rather than subtracting 86,400,000 ms, so a 23- or 25-hour DST day cannot move a goal window by a day. Two idiomatic shape differences, neither a divergence: web's `GoalPeriod` string union is a Dart enum, and web's `loadGoals` / `saveGoals` are `localStorage` glue against the Dart side's `toJson` / `fromJson` on the row class -- the STORE is per-platform, the evaluation is the pair), `workout_kind_color` (web `training/workout_kind_color.ts` ↔ mobile `workout_kind_color.dart`; which of the six `--kind-*` marks a `plan_workouts.kind` takes. Nine kinds share six marks, so the GROUPING is the whole contract — a kind that moves group on one platform gives the same plan two different calendars. The colour VALUES are not in this pair: they live once per brightness as `--kind-1..6` in `app.css` and as `ChartPalette.kinds` in `ui_kit`, asserted in lockstep by `contrast_guard.test.ts`. Two idiomatic shape differences: the ladder is 1-based on web because it indexes a CSS custom-property name and 0-based on Dart because it indexes a list, and web takes a `string` with an unknown value falling to the quietest mark where Dart takes the `WorkoutKind` enum and is exhaustive over it), `run_stats` (web `runs/run_stats.ts` ↔ mobile `run_stats.dart`; the three track-derived stats both platforms compute at display time rather than storing — `movingTimeSeconds` ↔ `movingTimeOf` (elapsed less every segment under `minSpeedMps`, default 0.5 m/s), `computeRealSplits` ↔ `computeSplitDurations`, and `haversineMetres`, whose `a > 1` clamp before the arc is the pair's own recorded near-miss: an earlier pass fixed only the Dart side on the false belief that web already clamped, which left the pair divergent with the NaN moved rather than removed. **Since § 1523 this is THE great-circle distance for both trees.** Nothing else under `apps/web/src` or `apps/mobile_android/lib` computes an arc — the nine web copies and the thirteen Dart ones (fourteen arcs, of which seven answered NaN past `a = 1` and seven half a circumference) all import it, object-shaped callers going through a one-line door like `routing_quality.haversineM` rather than a second formula, and `routes/great_circle_sources.test.ts` + `test/great_circle_sources_test.dart` fail the moment a new one appears. The one copy that must stay is the Deno importer's `embeddedHaversineM`, which cannot import from `apps/web/src/lib`; it is the canonical expression for expression and pinned to it bit for bit (§ 1525). A third rail exists in the watch firmware (`apps/custom_watch/core/src/run_stats.rs`), a one-way faithful port outside the enforced lockstep. Everything else in the Dart file is mobile-only and outside the pair: `fastestWindowOf`, the PB bracket tables, `bestEffortDistanceLabel`, `averagePaceSecPerKm`, `liveDistanceMetres`. One idiomatic shape difference: web returns whole seconds where Dart returns a `Duration` carrying the milliseconds), `elevation` (web `routes/elevation.ts` ↔ mobile `elevation.dart`; the Open-Meteo elevation lookup behind a route's stored `elevation_m` — `fetchElevations` (100-point batches, the same public no-key endpoint), `calculateElevationGain` (sum of positive deltas, rounded whole; `Math.round` and Dart's `.round()` differ only on negative halves and a gain is never negative) and `sampleCoordinates` (at most `maxPoints` evenly spaced, `idx = round(i * step)`). A route built on either platform must store the same number or the same course reads as two different climbs. Web additionally returns the sampled INDICES, which the Dart caller does not need), `adaptive_fitness_flag` (web `training/adaptive_fitness_flag.ts` ↔ mobile `adaptive_fitness_flag.dart`; the fail-closed deploy gate for the plan-generator-v2 P2 fitness direction (decisions §144 + §150), deliberately kept OUT of the `plan_adaptive_replan` pair so the engine's logic and its env binding move independently — both halves delegate the parse to `adaptiveFitnessGateEnabled`, which delegates to `env_flag`, the same re-export shape `auth_gates` uses for the password floor. The flag NAME is the part that differs by convention and not by behaviour: `PUBLIC_ADAPTIVE_FITNESS_GATE` on web, `ADAPTIVE_FITNESS_GATE` in the dart-defines, as every other mobile deploy flag drops the web-only prefix. The Dart read is additionally wrapped because `dotenv.env` throws `NotInitializedError` until something has loaded it, and a gate that throws is a gate whose answer depends on the caller's error handling), `off_route_flag` (web `safety/off_route_flag.ts` ↔ mobile `off_route_flag.dart`; the same deploy-gate shape one feature over — the fail-closed binding for the sustained-off-route escalation to a trusted contact, kept OUT of the `off_route_alert` pair so the detector and its env binding move independently, both halves delegating the parse to `offRouteEscalationEnabled` and thence to `env_flag`, and the Dart read wrapped against `dotenv.env`'s `NotInitializedError` for the same reason. Registered 2026-09-06: the header had declared the twin since the module was written, but named it RELATIVELY and below a top-level `const`, so neither the guard nor either registry could see it — a § 641-class pair whose divergence nothing detected. Both mirror suites written 2026-09-06 (§ 1355) — `safety/off_route_flag.test.ts` ↔ `test/off_route_flag_test.dart`, SOURCE guards on the gate rather than value tests (the affirmative set belongs to `env_flag`), 3 web / 4 Dart: the extra Dart case is the run screen's detector arming, which has no web analogue because recording is mobile-only), `weigh_in_flag` (web `runs/weigh_in_flag.ts` ↔ mobile `weigh_in_flag.dart`; the third of the deploy-gate trio, gating the Art 9 weigh-in / medical surface, both halves delegating to `env_flag`. **The flag NAMES are not the usual dropped-prefix pair** and this one is worth reading twice: web reads `PUBLIC_WEIGH_IN_ENABLED`, mobile reads `WEIGH_IN_GATE`, so the stems differ and an operator who set one has not set the other. The Dart header used to claim web spelled it `PUBLIC_WEIGH_IN_GATE`, a variable that exists nowhere, which is the one place a reader could have learned about the exception telling them its opposite (§ 1354). The exception is now MEASURED rather than described: `safety/deploy_gate_names.test.ts` ↔ `test/deploy_gate_names_test.dart` reads all four cross-platform gates' keys out of the source on both trees, fails on an undeclared divergence, fails when a declared one starts agreeing, fails on a `PUBLIC_*` name no web gate reads, and walks for `*_flag` modules so a gate it has never heard of fails rather than going unmeasured — which is how `nearby_flag` was found missing from its own first draft (4 tests each). Web has `runs/weigh_in_flag.test.ts` (5) and the Dart mirror `test/weigh_in_flag_test.dart` (3) landed 2026-09-06; the count differs because web's Svelte-block scanner needs its own fixture case and mobile's screen gates in Dart), `hr_zones` (web `training/hr_zones.ts` ↔ mobile `hr_zones.dart`; the DEFAULT heart-rate zone ladder — `tanakaMaxHr` (208 − 0.7 × age, which does not overestimate a masters runner's ceiling the way 220 − age does), `zoneCutoffsFromMaxHr` (60/70/80/90/100 %) and `defaultZoneCutoffs`' precedence of an in-range `max_hr_bpm`, then Tanaka from age, then the legacy 190 ladder. `isUsableMaxHrBpm` is the range test `defaultZoneCutoffs` itself calls, and both web WRITE paths gate on it since decisions § 1407, so what a settings page refuses is by construction what the readers ignore — the key has no column and therefore no CHECK, so before that a typed 300 was stored and then silently discarded by all three rails. An explicit `hr_zones` setting wins over all of it and never reaches this module. The TS-Dart half is measured CLEAN over every age 5..120 and every max HR 80..240 — `Math.round` and Dart's `.round()` differ only on negative halves and neither range reaches one. **A THIRD rail exists and is not another client's copy of the same thing**: the Wear OS `resolveZoneCutoffs` in `apps/watch_wear/android/app/src/main/kotlin/com/runapp/watchwear/SupabaseClient.kt`, which reads the same jsonb prefs bag. `max_hr_bpm` has no column and therefore no CHECK, so the 80..240 bound is the ONLY thing between a typo and a zone ladder, and it is now named once per rail (`MAX_HR_BPM_MIN`/`_MAX`, `kMaxHrBpmMin`/`Max`, `MAX_HR_BPM_RANGE`) rather than spelled inline three times. The watch applied the value flat until decisions § 1245, so a stored 300 gave that runner `[180, 210, 240, 270, 300]` on the wrist and the sensible ladder on the phone. The one place the three rails still differ is DELIBERATE and stated on all three: with no usable signal at all web and mobile return the legacy 190 ladder where the watch returns null and shows no zones), `auth_errors` (web `core/auth_errors.ts` ↔ mobile `auth_error.dart`; the auth-failure classification both banners render from -- `classifyAuthError` reads the error's `code` and `status` structurally where they exist and falls back to matching the message, so a plain `Failed to fetch` classifies too. The BRANCHES are the lockstep: an error that reads as `invalidCredentials` on one platform and `generic` on the other tells the same runner two different things about the same failure. `notSignedIn` is a declared mobile-only member (it classifies the `ApiClient` session guards, which have no web counterpart), and the i18n key identifiers are per-platform as `badges` records), `food_search` (web `nutrition/food_search.ts` ↔ mobile `food_search.dart`; the two-source food lookup behind nutrition logging -- the Open Food Facts and USDA parses, the portion scaling, and the fail-closed USDA gate (no key, no source, no error). Both halves isolate the HTTP call behind an injectable fetcher so the parse and scale are testable without network. `dedupeFoods`' name+brand key folds through `catalogue_browse`'s generated accent-plus-case table rather than either runtime's `toLowerCase`: the two disagree at 466 code points, so a Turkish product name collapsed two rows into one on the phone and left two on the web from the same pair of searches — a divergence THIS registry could not have caught, because both halves were spelled `trim().toLowerCase()` and it compares source, not answers (decisions § 1280). Folding the accents is the point of the key rather than a side effect: the two sources are independent catalogues that spell one product two ways), `nutrition_totals` (web `nutrition/nutrition_totals.ts` ↔ mobile `nutrition_totals.dart`; the daily-view aggregation: sum a day's logged food into macro totals and group it by meal slot, with the slot ORDER, null-as-zero and empty-slot-omission all part of the contract), `payload_hash` (web `util/payload_hash.ts` ↔ mobile `payload_hash.dart`; hex SHA-256 of a request body for the `x-amz-content-sha256` header. CloudFront's Lambda OAC sigv4-signs every origin request but cannot hash a body it streams, and an OAC-signed Function URL refuses an unsigned payload, so a client that hashes differently gets a 403 before invocation rather than a wrong answer), `race_match` (web `integrations/race_match.ts` ↔ mobile `race_match.dart`; the pure auto-match scoring behind the offer to import an official race result after a run is saved -- same calendar day, start within `raceMatchRadiusM` of the listing, distance inside the race's band, offered at or above `raceMatchThreshold`. INFORM-tier on both platforms: nothing writes without confirmation, so the two must agree about WHICH runs are offered or the same run is a race on one client and not on the other), `workout_labels` (web `training/workout_labels.ts` ↔ mobile `training_labels.dart`; the presentation-layer labels for the training enums, kept out of `training.ts` / `training.dart` so that pair stays locale-free. What is in lockstep is the COVERAGE -- every `WorkoutKind` and every `PlanPhase` resolving to a label on both platforms, so a kind added to the union cannot render as a raw token on one client. The key IDENTIFIERS are not: web carries dotted `MessageKey`s resolved through `m()`, mobile the camelCase ARB names resolved through `AppLocalizations`, the same carve-out `badges` and `extended_nutrients` record. The Dart file is named for the enums it labels rather than for the web basename the registry keys on)), plus the `track_projection.ts` ↔ `projectTrack` / `isTrackRenderable` helpers inside `track_preview.dart`. The `shared-library-syncer` agent (under `.claude/agents/`) detects divergence proactively after edits — invoke it whenever you touch one side of a pair. **That agent works from its own table, not from this list**, so a pair added here without a row there is a pair whose divergence is never detected (and the reverse reads as a single-platform helper); `scripts/check_parity_pair_registry.mjs` fails the PR when the two registries disagree in either direction, or when either points at a file that no longer exists. Adding a pair means editing both, in the same change. See decisions.md § 604. **A registry compares two REGISTRIES, not two behaviours, so a helper reimplemented PRIVATELY is not a pair anyone declared and no registry guard can see it** — the tree hit that four times (§ 852's hand-written Dart fold table, § 1340's fourth private `fold`, the round-42 catalogue picker's byte-identical `compareFoldedNames`, § 1398's `planSlug` lower-casing where `clubSlug` folds) with every guard green through all four. `scripts/check_shared_reimplementations.mjs` reads the SOURCE instead, on two anchors that cannot be removed without changing behaviour: the normalised function BODY (every token it runs, with comments, whitespace, its own name and the spelling of its parameters and locals erased) and the DISTINCTIVE OPERATIONS it reaches for (a method call carrying a string or regex literal — what a copy cannot drop; § 1340's copy differed from the canonical by one `.replace`, so only the second anchor sees it). Both survive renaming the function, its parameters and its locals; a guard keyed on a NAME would not, and one keyed on a spelling like the literal `normalize('NFD')` fails correct code and passes a copy written any other way. A group is reported only when one member is an EXPORTED `apps/web/src/lib/**` function — duplication with nothing importable is a different finding the no-preemptive-abstractions rule tolerates, so the nine copies of `escapeJsonLd` are deliberately invisible. Deliberate duplication goes in the `REGISTERED` census, whose entries state what the pair actually IS and which FAILS when one goes stale — that is what caught a defect in the guard's own reading of an inline parameter type. decisions § 1397. `rate_limit_errors` is PARSE-ONLY since decisions § 744: both halves return the `{bucket, seconds}` the postgres P0001 carries and neither holds a sentence, because the activity is a clause whose verb inflects and the wait pluralises. The wording lives in the seven web catalogues and the seven ARBs, assembled by render glue — `i18n/rate_limit_message.ts` resolving through `m`, `rate_limit_message.dart` resolving through `AppLocalizations` — which is per-platform (the key identifiers differ by convention) and, like `badges`' label keys, is NOT part of the enforced lockstep. The bucket→sentence mapping therefore costs nothing to extend on one platform, which is what § 737 had priced as a lockstep edit when it left both direct-message buckets on the generic wording.

Many of these pure helpers also carry a **third parity rail** in the research-tier watch firmware: a `no_std` Rust port under `apps/custom_watch/core/src/<name>.rs`, host-tested test-for-test against the web `.test.ts`. Because the watch is additive per [decisions.md § 24](decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive), these are **one-way faithful ports, NOT part of the enforced web↔mobile lockstep** the `shared-library-syncer` checks — the watch [`README.md` § step 7](../../apps/custom_watch/README.md) tracks the full set. Ported 2026-07-11: `age_grade`, `hydration`, `exercise_calories`, `training_paces` (the pace-zone surface of `training`, reusing the watch's ported Riegel), `fitness` (VO2max / VDOT / per-run TSS / recovery advice + the Fitness-card snapshot, with the web's ISO `started_at` + `nowMs` collapsed to an integer day index like `training_load`), then a second batch — `route_markers`, `checkpoint_projection` (reuses the watch's `roadbook::CutoffStatus` + `cutoff_eta::CUTOFF_TIGHT_S`), `route_description` (reuses `distance_bands`), `plan_progress`, `plan_adherence`, `current_week` (ISO dates collapsed to a day index), `nutrition_targets`, `challenge_progress` (reuses `pacer::ON_PACE_BAND`), `badges` (thresholds in lockstep with the SQL awarder; i18n label keys carried as identifiers, not strings), and `locale_defaults`, then a third batch (track / route / race / training) — `privacy` (`clipPointsToZones`), `segments` (segment effort + competition-rank + crown label), `track_projection`, `run_heatmap` (`buildHeatCells` + `heatBounds`; the MapLibre GeoJSON emitters are web-only), `relink_candidates` (dates collapsed to a day index), `live_freshness`, and `finisher_certificate` (the shaping half; the SVG/PNG builder is web-only). Ported 2026-07-26: `race_phases` (the 10-10-10 / negative-split / even phase plan; the closing factor is DERIVED so the distance-weighted mean factor is exactly 1.0 — surfaced on the watch's Pacer glance page) and `course_profile` (the watch-only distance-even resampler that turns a pushed course's per-point elevation into the drawn climb profile; no web twin — web renders the profile from the full polyline). The same batch caught the watch's `cutoff_eta` up to web issue #607 (`requiredPaceSecPerKm` + `limitPassed`) and closed a divergence it surfaced: the watch gated pace on a bare `p > 0.0`, accepting `+Infinity` where web gates on `Number.isFinite`. Ported 2026-07-28: `workout` — the mobile `run_recorder` `WorkoutRunner` state machine (a shipped-phone-engine port, not a web pair), executing `WKT1`-pushed pre-expanded steps with its own glance page + lap-press step-skip (decisions § 354; the Dart `watch_workout.dart` encoder + iOS twin pin the shared golden vectors), and `daylight` (the seasonal sunrise/sunset half of `safety_nudge` — Cooper's declination, the −0.833° horizon, the polar clamps — driving the watch's tz-gated Daylight countdown page, decisions §355). **The 2026-07-29 batch is watch-native, not ported at all** — three parity rows with no web twin to mirror: `waypoints` (an eight-slot newest-wins mark store + `WPT1` codec behind BTN5's hold, decisions §357), `ice` (the ICE / medical-ID card on `SET1` v6 + an `ICE1` flash record, rendered as a third idle face, decisions §358), and `climb` (live ascent segmentation plus the ClimbPro-style crest ahead read off the already-pushed course profile, decisions §359). Nothing in that batch belongs in the parity-pair list above: adding a web twin later would be a *new* web feature, not a sync obligation. **Watch-native again 2026-07-30, three at once:** `sleep_station` (decisions §373) — the nap budget a 200-mile racer can afford before the next cut-off, computed as `cutoff_eta`'s margin less a reserve and inheriting its refusal to project off a stale fix; `timers` (decisions §375) — a countdown + stopwatch as one instrument behind an idle BTN2 modal, whose ADR also records why an **alarm** and a **find-my-phone** were refused rather than built; and `backyard` (decisions §372) — the wall-clock-anchored corral bell, the loop count riding the recorder's lap, the learned loop length and the projected return margin. None of the three has a web twin, and none is owed. **Watch-native once more 2026-07-31:** `storm` (decisions §376) — baro-driven storm detection, which trends a GPS-compensated **sea-level reduction** rather than raw station pressure because on a mountain ultra a 500 m climb moves the barometer twenty times further than a front does, and which refuses outright (`NO ALT REFERENCE`) when no GPS altitude corroborates the sensor rather than reporting a climb as weather. No web twin and none owed — the phone has no barometer. Most of these cores are wired to a run-view glance page (41 as of 2026-07-31); the ones that are not are deliberately UI-less per § 24 — see `docs/custom_watch/roadmap.md § Feature parity backlog` before assuming a missing page is owed.
