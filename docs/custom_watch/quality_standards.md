# Quality standards — the verification ladder + the tier-1 bench checklist

Tier 1 has exactly one formal completion bar: [decisions.md § 82](../architecture/decisions.md#82-tier-1-firmware-is-done-when-one-outdoor-run-syncs-end-to-end-to-supabase-from-the-bench-prototype) — one real outdoor run producing a GPS+HR-tagged track that syncs end-to-end to Supabase. That bar is deliberately narrow and it is not changing. But it left two things undefined, and both cost real credibility:

1. **The verification vocabulary was never defined.** The docs and the workspace README use a four-rung ladder — *host-tested* → *build-verified* → *sim-verified* → *bench-verified* — that has never been written down anywhere. The rung that actually matters (bench-verified) has the least written about it, and 21 of the 50 feature-parity items in [`roadmap.md`](roadmap.md#feature-parity-backlog-garmin--coros-table-stakes) sit annotated "implemented" but deliberately unticked "pending bench verification" against a definition that doesn't exist. (Those two counts were 13 of 43 when this doc was written; the ratio is the point, and it has only grown.)
2. **There is no falsifiable hardware-side standard for tier 1 at all.** Tier 2 has real targets (≥24 hr GPS battery, 100 % outdoor fix reliability — [`prototyping.md`](prototyping.md#tier-2--wearable-prototype-15k40k-diy-80k250k-consultant-built-918-months)). Tier 1 has a DoD about a run syncing and nothing about whether the hardware behaved while it did. [§ 83](../architecture/decisions.md#83-tier-1-power-measurement-uses-nordic-power-profiler-kit-ii-applied-per-subsystem) is explicit that the PPK2 numbers *inform* tier-2 planning and **do not gate** tier-1, and § 82 explicitly declines to set kill criteria. So the day the parts arrive, "did bring-up work?" would be decided by vibe.

This doc closes both gaps. **It does not change § 82's Definition of Done** — see [What tier 1 does not have to hit](#what-tier-1-does-not-have-to-hit). It defines what each rung means and what may be claimed at it, and it turns the bench-verification phase into a checklist someone can run against real hardware and get a yes/no or a number out of.

Read alongside [`roadmap.md`](roadmap.md) (tier structure + the parity backlog), [`parts.md`](parts.md) (what hardware will exist), [`performance_path.md`](performance_path.md) (where battery life comes from), [`prototyping.md`](prototyping.md) (what "working" means per tier), and [`tier1_log.md`](tier1_log.md) (where the evidence gets recorded). Per-step bring-up status lives in [`apps/custom_watch/README.md`](../../apps/custom_watch/README.md).

## The four verification rungs

The ladder is about **what evidence exists**, not about how much work went in. A rung is a claim about the world, so each one has to name the evidence that earns it.

| Rung | Evidence | Ceiling |
|---|---|---|
| **host-tested** | `cargo test` green on the host for a pure `no_std` crate | Says nothing about the target, the peripherals, or the wire |
| **build-verified** | Compiles + links for `thumbv7em-none-eabihf` in the relevant feature sets; `fmt` + the clippy gate green | Says nothing about runtime behaviour |
| **sim-verified** | The unmodified release ELF ran on Renode and a named observable changed as predicted | Says nothing about real silicon, analog behaviour, the radio, or power |
| **bench-verified** | The same firmware ran on a real nRF52840 DK with the real part wired, and a recorded measurement or capture met a stated criterion | Says nothing about production silicon, in-case RF, or multi-day endurance |

### host-tested

**Earned by:** a `cargo test` pass on the host for hardware-free logic — `watch_core`, the driver crates' pure halves, `watch_render` and its ASCII-preview harness. For a parity port ([§ 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive)), the tests must mirror the web `.test.ts` case-for-case, so the rung also carries "this agrees with the canonical web helper".

**Does NOT prove:** that the code compiles for the MCU target (host `std` tests can pass on code the embedded build rejects); that any register sequence matches silicon; that a wire format survives a real transport; that the numbers reach a panel. A host test over a *driver* pins what the driver believes about the part — it cannot pin what the part does.

**May be claimed:** "host-tested", "mirrors the web helper test-for-test", "the logic is pinned". Never "works", never "verified", never a bare "implemented" without the rung attached.

### build-verified

**Earned by:** a clean build for `thumbv7em-none-eabihf` across each feature set the change touches — the default (sim/UART) build, `--no-default-features --features ble`, and the sim feature set — plus `cargo fmt --check` and the CI `clippy -D warnings` gate. For the BLE build this includes linking against the S140 SoftDevice and the `memory-ble.x` script.

**Does NOT prove:** that anything runs. It does prove three specific things worth naming, because they were the real risks: the dependency stack resolves (the one plausible § 80 "blocking driver issue" for Rust + Embassy), the linker script and memory-map arithmetic are self-consistent, and the code is reachable from a genuine target build rather than living behind a host-only cfg.

**May be claimed:** "compiles and links for the target". Per [§ 210](../architecture/decisions.md#210-tier-1-ble-s140-softdevice-is-a-compile-verified-feature-gated-build--mutually-exclusive-with-the-sim-off-by-default) this is the **ceiling** for the whole SoftDevice path short of the bench — the sim cannot run proprietary Nordic firmware, so BLE can never be sim-verified and jumps host/build → bench directly.

### sim-verified

**Earned by:** running the *unmodified* release ELF on Renode (`bin/watch-sim.sh`) and capturing a named observable that changed as predicted — a specific `defmt` line, a panel dump, a fixture-driven sequence. A claim must say *what* was observed, not merely that the sim ran. The precedent for how to record it is `apps/custom_watch/sim/verification-2026-07-19/` (evidence excerpts plus an explicit verified / not-verified matrix).

`apps/custom_watch/sim/ci_smoke.py` is the same rung run non-interactively, and the `sim-firmware` CI job is what keeps it from rotting between manual passes. It carries a `--scenario` selector — `smoke` (the record → store → panel → stop sequence that the 2026-07-19 pass stands behind), plus `pages` (each run-view page produces a non-blank frame), `alerts` (the fuel / zone / off-course banner path), `terrain` (the pages the flat fixture cannot arm, plus the § 359 climb detector's own output and the crest read off the pushed profile — asserted on the recorder's defmt lines, since a page dump cannot speak to a number), `dropout` (the recorder across a signal void), `screens` (the runner's own composed data screens lead the cycle and their multi-value layouts draw, § 364), `idle` (the § 351 settings menu and the three idle faces BTN4 walks — the only scenario that boots `--no-autostart`), `workout` (the § 354 / § 356 structured-workout rail end to end) and `storm` (§ 376 — a scripted sea-level fall driven past the barometer while its altitude stays put, asserted on the baro task's own tendency line). **The `pages` and `alerts` scenarios first ran green in CI on 2026-07-26** — the steps executed rather than being skipped, which is worth checking explicitly since a skipped step also leaves a job green. They now carry the rung for exactly what they assert: the cycle advances with the panel following it (the `ui: page` line cross-checked against the button task's), the named pages of `PAGES_OF_INTEREST` render non-blank and byte-distinct, and an alert banner inks the hero band.

**Two rails moved onto this rung on 2026-07-30 by being folded into `smoke`**, neither of which had ever been asserted by any scenario. The optical-HR path now has to read a BPM in a band around the MAX86177 model's synthetic ~72 — the model has existed since 2026-07-19 to take that path off the bench gate, but nothing had read a pulse off it, so the whole TWIM → tagged-sample → AGC → detector chain could break with every scenario still green. And the **phone link** now has to answer on its TCP socket with schema-valid v1 frames whose `uptime_s` advances and whose first fix matches the one the decoded log reported — the sim's second rail, the one the mobile dev screen consumes, which until then could emit nothing while the panel looked perfect. Both ride the boot `smoke` already pays for; neither earns a rung for anything beyond the observable it names, and in particular the link assertion says nothing about BLE, which can never be sim-verified.

**`dropout` (added 2026-07-30) is the only scenario whose subject is a bug that has already happened.** The `gps_dropout` fixture was written for the 2026-07-19 manual pass and caught the un-ported `run_recorder` #330 gap re-anchor, which froze recorded distance for the rest of a run after any 1 Hz dropout that displaced the runner past `MAX_JUMP_M`. That was fixed the same day and guarded since by two host tests over `Recorder` and by nothing that drives the whole NMEA → parser → publish-cadence → recorder path — the fixture itself stayed a manual poke, so the end-to-end route that surfaced the bug had no regression guard. The scenario asserts the leg banks distance, the void freezes it with every snapshot inside reading `paused`, and the reacquire moves it again. It was verified to **fail** as well as pass before landing: with the re-anchor branch disabled the void and freeze assertions still pass and the resume one reports the frozen plateau by name, which is the shape a guard has to have to be one. It also reports a host too slow to run the fixture as exactly that — the void's length is measured on the emulator's clock, and a runner compressed below `GPS_REANCHOR_AFTER_S` leaves the anchor legitimately held, which must not be reported as a frozen recorder.

**It then caught a second bug — by going red the moment an unrelated fix stopped it passing vacuously ([§ 367](../architecture/decisions.md)).** Its freeze claim ("every snapshot inside the void reads `paused`") had been satisfied by a recorder that was pinned in `Paused` almost permanently, because a GGA completing a fix made every 1 Hz epoch lead with a zero-displacement sample. When [§ 366](../architecture/decisions.md) fixed that, the clean leg correctly read `Recording`, the void held it there, and the scenario reported that nothing had ever paused the recorder on the *absence* of fixes. The guard was right the whole time; what it was guarding had never been built. **A scenario that passes over a broken mechanism also certifies everything downstream of it** — this one had been signing off on lost-signal honesty since the day it was written. Worth remembering when reading a green scenario as evidence: it proves the assertion held, not that the assertion was reached the way you imagine.

**And the counterpart for reading a RED one: since 2026-08-11 ([§ 582](../architecture/decisions.md)) the harness distinguishes a failed claim from a dead guest, because it could not before.** A wedged firmware used to be reported as a failure of whichever assertion happened to be holding the wait — [#754](https://github.com/Absence0760/threkir/issues/754) surfaced as `expected the tracker's first measured tendency … no decoded log line matched`, which reads as a storm-classifier regression and was nothing of the sort: the executor had stopped at t≈19 s and the classifier was never reached. A failure that names a **WEDGED GUEST** is therefore *no evidence at all* about the assertion printed beside it — read the tail of that run's `renode.log` (and, since the same change, the `firmware.elf` staged into the artifact) for what the CPU did last. The discrimination is measured on the WALL clock, which is the one place in this harness that does not use the firmware's own: a guest that stops executing stops stamping lines, so its virtual clock freezes with it and cannot notice its own death. The live-stream branch now says so explicitly too — "the firmware was running and the claim genuinely failed" — since naming a wedge is only worth having if the other branch is equally unambiguous.

**A wedge message must carry its own signature, because there is more than one wedge (2026-08-18).** The message used to name [#754](https://github.com/Absence0760/threkir/issues/754) on every wedge. That issue is now CLOSED and describes a different one — scenario `storm`, ~1-in-30, dying at t=19.0 s mid-run — so a reader hitting the live boot-time wedge on `pages` ([#788](https://github.com/Absence0760/threkir/issues/788): three consecutive CI runs on PR #787, and 0/4 reproductions on arm64 macOS at CI's own `--budget 300`) was pointed at a resolved bug. What the message states now is what it measured: the last STAMPED firmware clock — the observed CI wedge ends on an unstamped continuation line of embassy-nrf's multi-line UICR warning, so quoting only the last line drops the one number saying it died at boot — plus a probe of the machine itself. The probe is the part that was missing: silence cannot say whether the FIRMWARE stopped or the RENODE EXECUTOR did, and no artifact distinguished them. It samples the CPU `PC`, the retired-instruction count and the machine's elapsed virtual time twice 1.5 s apart, resolves the PC through the loaded ELF's symbols (so no local rebuild is needed to read it), and says which of the two stopped — a PC pinned across both samples with instructions still retiring is a guest busy-wait, no instruction and no virtual time is the emulator. The `renode.log` tail is filtered the same way: on a boot-time wedge all twenty of its last lines are the host feeder writing NMEA at a UARTE the guest never enabled, which buried the `gpio1 … LATCH` write that located #788's wedge inside `embassy_nrf::init` in the first place. **None of this is a fix** — #788's cause is a finding, not a verified diagnosis, and the harness must not be quietened with a longer budget, a retry, or a skipped scenario while it stands: 512 s of virtual time is what a missed `RtcDriver::init` counter read costs, which no budget in this job can absorb.

**And that probe paid for itself on the next run (2026-08-18, [§ 666](../architecture/decisions.md)).** The first CI failure carrying it named `pc=0x1a000` inside the embassy main task's `poll` with 3.9 M instructions retiring across the two samples — a guest busy-wait, and the ELF staged beside it made the address readable without a rebuild: `ldr` / `cmp` / `bne` against `0x40011504`, RTC1's COUNTER, which is `RtcDriver::init`'s spin. So the § 582 discrimination and the ELF staging are what turned a week of speculation into an afternoon. Two things the harness had asserted about #788 were **wrong**, and both were wrong in the same direction — they blamed the environment rather than the artifact. It does not reproduce on arm64 macOS *with a locally built ELF*; the ELF CI itself uploaded wedges on this Mac at the same PC, so "CI's x86_64 host loses a race" was never the shape of it. And a wedge that is deterministic per BINARY, not per host, is not a race at all. The fix is in `sim/NRF52840_RTC_Overflow.cs`, not in the firmware or the budget. **The standing rule survives the fix**: a wedge is still never to be answered with a wider budget, a retry, or a skip, and a probe that names a PC is worth more than any number of re-runs.

**`workout` (added 2026-07-30, [§ 371](../architecture/decisions.md)) is the case where the rung had been claimed off a session rather than a guard.** The structured-workout rail was recorded as sim-verified on the strength of the demo workout having been walked by HAND under bench_jog — true, and worth nothing after the day it was done, because nothing re-ran it. The scenario asserts what that session watched: the pushed step list arming the recorder, the Workout page entering the cycle and rendering, the runner auto-advancing in order over both end axes (the plan mixes 50-60 m distance steps with a 30 s duration one, so a runner that settles only one axis stalls partway), exactly one end-of-step warning across the plan, a BTN5 lap press skipping the active step and then completing the workout, and the finished trail flushing into the run blob with nothing dropped. Two of those need conditions the other scenarios do not have, and both are recorded in § 371: the COUNT of warnings is only a claim about the `ENDING_MIN_STEP_*` gate while nothing else can take the single alert slot (hence `--no-alerts`), and the lap-skip has to be separated from the auto-advance that would produce the same edge on its own — so it is timed on the FIRMWARE's virtual clock, not the harness's wall clock. Both were verified to **fail** as well as pass before landing: with `Recorder::lap`'s `skip_step` call removed the auto-advance still ends the step and the scenario reports the edge arriving 30.4 s in, on schedule rather than on the press; with `ENDING_MIN_STEP_M` dropped from 100 m to 10 m it reports three warnings where one belongs.

**`terrain` (added 2026-07-29) is the positive half of a claim the others could only make negatively.** A page absent from the bench_jog walk is absent for a legitimate reason — nothing arms it — so the walk alone cannot distinguish "correctly hidden" from "data-presence bit wired to the wrong field", which reads identically. `terrain` boots `mountain_loop`, marks a waypoint with the BTN5 hold (§ 357) and ramps the BMP581 model past the 20 m a climb opens at (§ 359), then asserts both pages enter the cycle and render. Its two arming steps are asserted in their own right, so a failure names the arming rather than the page. Note the fixture is bound to the scenario in `SCENARIO_FIXTURES`, not passed at the call site: run on bench_jog it would fail while the firmware was correct.

**What a scenario boots is a declaration, and the quiet window it dumps into is a firmware promise ([§ 465](../architecture/decisions.md)).** A page dump taken under an alert banner is a dump of the banner — the band covers the two hero rows — so every page assertion here first waits for the alert slot to go empty. Until 2026-08-03 that wait was a bet rather than a contract twice over, and it cost four red runs on `main` that touched no firmware. The engine expired a banner and promoted the next queued one inside the same `on_update`, so a slot with anything owed behind it went banner-to-banner and the `record: alert cleared` transition the harness waits on could not fire; and the wait was bounded in *wall* seconds against a schedule kept in *firmware* seconds, so a loaded runner read as a broken alert engine. Both sides are fixed: `watch_core::alerts::ALERT_QUIET_S` guarantees the page comes back between banners, and the harness reads that constant out of the firmware source and judges the wait on the decoded stream's own stamps. Scenarios now declare the rails they assert on (`SCENARIO_ARMED_RAILS`) and the launcher's `--no-*` flags are derived from that, so a rail added to the sim is off in every scenario that does not name it — the three earlier per-scenario patches are folded into the one rule. Read a green sim step accordingly: it means the panel dump was of the page, and that claim now rests on a guarantee rather than on the arms happening to be sparse.

**A panel dump is synchronised on a line the composer emits AFTER the flush, never on the state that caused it ([§ 716](../architecture/decisions.md)).** The alert ENGINE's `record: alert <Kind>` line is emitted by the record task *before* the alert is even published to the screen task, so a dump taken when it decodes reads whatever the panel held one repaint earlier — a valid frame of the same page, just not the one being asserted about. Which side of the repaint the dump lands on comes down to how much of Renode's virtual clock passes during the host's decode-and-dump round trip, which is a property of the runner, not of the firmware: the `alerts` scenario's first attempt missed 3 times in 8 identical local runs and missed all four of its attempts on a contended CI runner, on a PR that touched no firmware. The screen task therefore states the panel's own banner condition — `ui: banner <Kind>` / `ui: banner none`, change-gated, after `display.flush` returns, exactly as `ui: page` has since § 361 — and every dump here waits for that line and re-reads it once the frame is on disk, so a repaint colliding with the dump is DETECTED rather than sampled around. Read a panel-dump claim accordingly: it is a claim about a frame the firmware said it had drawn, and the retry counts around it are failure bounds, not extra tickets in a lottery.

**`bin/watch-shots.sh` (added 2026-07-29, [§ 360](../architecture/decisions.md)) is this rung's viewing tool, not a new rung.** It walks every screen the sim can arm and writes one PNG per screen plus a contact sheet, so a layout can be *looked at* rather than only asserted non-blank. What a capture earns is exactly what a `ci_smoke` panel dump earns and no more: the named screen composed and inked pixels, with the page identity coming from the ui task's own `ui: page` / `ui: idle` line rather than from the image. **Nothing in it reads a glyph.** A sheet that looks right is not evidence a number is right — value correctness stays with the host tests, and legibility stays bench-only (step 4). Two caveats worth stating because a sheet invites over-reading: a frame can arrive under an alert banner or as a stale repeat of the previous screen (both are detected on the pixels and re-shot, and a frame that survives four tries is captured *labelled* rather than dropped — since 2026-07-30 both sessions boot `--no-alerts`, because the re-shoot loop can only wait a banner out if there are gaps to wait in and the sim's shortened cadences leave none past ~100 s; that had cost six of twenty-one run screens, § 362), and the sheet is not the whole UI — thirteen pages have no `SET1` wire field to arm them, so ~25 screens is the ceiling of what the sim can show, not of what the watch renders. Its first honest run is also a worked example of why the rung is worth having: it surfaced a full-width gauge that had been painting through a text row on the pacer page since #609, which every existing test passed over because the face suite asserts row strings, the widget suite asserts bar columns, and neither composed the two.

Renode **is** installable on the current authoring workstation (1.16.1, Fedora rpm), so a scenario can now be run locally before it reaches CI — `terrain` and `dropout` both were. That lowers the cost of authoring one; it does not change what the rung means, and CI remains what keeps these from rotting between passes. Nothing else moves rung on their strength; the observable a scenario names is what earns the rung, item by item. Note also what a render assertion is *not*: a non-blank frame is not a correct number and not a legible one. Value correctness stays with the host tests, and legibility is bench-only (step 4).

**Does NOT prove — the honesty limits, which are load-bearing:**

- **BLE is out of scope entirely.** Renode cannot run the S140 SoftDevice ([§ 210](../architecture/decisions.md#210-tier-1-ble-s140-softdevice-is-a-compile-verified-feature-gated-build--mutually-exclusive-with-the-sim-off-by-default)). RAM origin, interrupt priorities, connection parameters, pairing/bonding, and every characteristic — `frame`, `run_manifest`, `run_chunk`, the write-only pushes `settings` / `course` / `workout` / `screens` / `roadbook`, and the read-only `push_status`: none of it is sim-reachable.
- **Power is out of scope entirely.** Renode does not model current. Every power claim in the workspace is a *derivation*.
- **The sensor models are synthetic and pinned to the driver, not to silicon.** The MAX86177 and BMP581 Renode models answer the register sequences the drivers issue, with a deterministic triangular PPG and a scripted altitude. That verifies the *path* — bus, FIFO demux, duty-cycle shutdown/wake, the accumulators, the render — and not register semantics, bus timing, analog noise, settling, or the real optical signal. A model that agrees with the driver's belief cannot detect a wrong belief.
- **No SAADC.** The battery-gauge sampling path is not sim-verifiable; only "the task parks cleanly and the faces render without it" is.
- **No RF, no antenna, no thermal, no mechanical.**

A sim pass is nonetheless worth a lot: the 2026-07-19 pass found a real recorder bug (the un-ported `run_recorder` #330 gap re-anchor, which froze distance for the rest of a run after a dropout), an RTC overflow that froze the recorder 8.5 min in, and a free-running HR waker re-rendering the UI 50×/s. That is what the rung is *for* — it just isn't the wrist.

**May be claimed:** "sim-verified: \<the observed behaviour\>". Never generalised to the device, and never used to tick a parity-backlog item.

### bench-verified

**Earned by:** the same firmware running on a real nRF52840 DK with the real breakout wired, where a **stated criterion from the checklist below was met and the evidence recorded** — a `defmt` capture, a PPK2 trace, a photo of the panel, a Supabase row id. "I flashed it and it seemed fine" is not the rung; the criterion and its evidence are the rung.

**Does NOT prove:**

- **Production power.** The DK burns ~30 mA at idle from its onboard J-Link, LEDs and non-sleep-optimised LDOs, which dwarfs anything the firmware does — this is exactly why [§ 83](../architecture/decisions.md#83-tier-1-power-measurement-uses-nordic-power-profiler-kit-ii-applied-per-subsystem) mandates *per-subsystem* PPK2 measurement and calls whole-device readings useless as a baseline. A bench-verified subsystem current is a tier-2 *projection input*, not a battery claim.
- **Anything about tier-2/3 silicon.** Bench results are for nRF52840 + MAX-M10S (single-band L1) + MAX86177 + BMP581-class parts on breakouts. The production targets are Apollo510B + a dual-band snapshot GNSS ([§ 90](../architecture/decisions.md#90-bom-refresh-2026-05-28--apollo510b--bmp581-swap-ins-supply-alternates-qualified)); breakout results do not transfer.
- **In-case RF.** Breakouts have poor RF tuning and no case. Per [`prototyping.md`](prototyping.md#why-rf-is-the-line-item-that-surprises-everyone), getting from a working breakout fix to a working in-case fix is 2–6 weeks of an RF engineer's time. A good bench fix says nothing about the tier-2 unit.
- **Endurance, weather sealing, shock, foliage, urban canyon, or a multi-day run.** § 82 puts all of that at tier 2+.
- **Button feel.** The DK's tactile switches are not watch buttons; see the [step 7 checklist](#step-7--integration-recording-storage-controls) for what *is* testable.

**May be claimed:** "bench-verified on the nRF52840 DK: \<criterion\> = \<result\>". This is the rung that lets a parity-backlog item be ticked and the only rung that satisfies § 82.

### Rung rules

- **A rung is not inherited.** Editing a bench-verified module drops it back to whatever the *new* evidence supports. The rung belongs to the code that was tested, not to the module name.
- **A rung claim names its evidence.** A doc line that says "verified" without saying which rung and against what is a defect in the doc.
- **Rungs are not strictly sequential, but they are not substitutes.** BLE skips sim by necessity. A pure parity core may never leave host-tested and that is fine ([§ 24](../architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive) — some ported cores have no wrist surface). What is never allowed is letting a lower rung stand in for a higher one in a claim.
- **Where evidence lives.** Sim evidence follows the `sim/verification-<date>/` precedent. Bench evidence belongs in a dated [`tier1_log.md`](tier1_log.md) entry — which already reserves a photo/video slot per entry for exactly this — with per-subsystem PPK2 numbers captured as § 83 requires. [`parts.md`](parts.md)'s order checklist is ticked on receipt, not on order.

## Tier-1 bench-verification checklist

One line per claim, each a yes/no or a number, ordered by the bring-up steps in [`apps/custom_watch/README.md`](../../apps/custom_watch/README.md). **None of this gates tier-1 completion** — § 82's DoD does. This is how the bench phase is *conducted and claimed*: a failed item is a recorded finding and a tier-2 input, not an un-completion.

Where a criterion could not be justified from an existing doc it is marked **(open value)** — record the first real measurement and set the bar then, rather than inventing a precise-sounding figure now.

### Step 0 — before the board is powered

- [ ] **Every part on [`parts.md`](parts.md) received and ticked** (tick on receipt, not on order).
- [ ] **The delivered Sharp panel's resolution matches the framebuffer.** `sharp_mip` compiles a fixed 168×144 (21 text columns × 9 rows of the 8×16 font). The ordered part is **LS013B7DH05** on Adafruit 3502; the 96×96 **LS013B4DN04** (Adafruit 1393) is a different, EOL panel that earlier revisions of `parts.md` and `prototyping.md` both named by mistake. Confirm the delivered module's datasheet resolution *before* wiring. A mismatch silently corrupts every line-update address.
- [ ] **EXTMODE / EXTCOMIN strapping confirmed on the real breakout.** The driver holds EXTMODE high and drives a ~3.8 Hz PWM on EXTCOMIN so a clean framebuffer flushes zero SPI. Adafruit's board is documented as *pulling* EXTMODE low (software-VCOM default) rather than strapping it, which a push-pull GPIO overrides — but confirm on the schematic that it is a resistor. If it is a hard strap, the hardware-VCOM power path is invalid and the software VCOM bit must come back. Note the silkscreen names while you are there: `EIN` is EXTMODE, `EMD` is EXTCOMIN, and `DISP` takes no MCU pin.
- [ ] **The BMP581's address jumper is set before the first probe.** `drivers/bmp581` addresses **0x46**; Adafruit 6407 ships at **0x47**. Tie `SDO` to GND or close the back-side `addr` jumper. Un-set, the probe simply fails and the baro task parks — indistinguishable at a glance from an absent part.
- [ ] **The delivered HR breakout is a MAX30101.** [§ 623](../architecture/decisions.md) chose it for tier 1 over the NDA-gated production MAX86177, and `drivers/max30101` is written against it. Confirm the delivered module is not a **MAX30102** — red + IR only, no green LED to read a moving wrist with. Check the defmt stream rather than the silkscreen, but know what the stream can and cannot tell you ([§ 625](../architecture/decisions.md)): the `PART_ID` read cannot distinguish the family (all of them answer `0x15`), so the refusal to look for is `hr: AFE init failed NoGreenChannel(..)` — the driver writing `LED3_PA` and finding it will not hold. A **MAX30105** passes both checks and is a genuine substitute; nothing needs adding for it.
- [ ] **First flash: toolchain end-to-end.** `bin/watch-doctor.sh` clean, `bin/watch-flash.sh` builds → flashes → streams decoded `defmt` from the real board. This is the single item that retires "no on-board verification yet".
- [ ] **`UICR.REGOUT0` programmed to 3.0 V, and the rail measured in the Li-Po position before a breakout is connected.** With SW9 in its default `VDD` position the board rail is the onboard 3 V buck and nothing is wrong, which is exactly why this is easy to miss on the bench and then lose an evening to outdoors. Move SW9 to Li-Po for the untethered run and the SoC runs on its high-voltage regulator, `VDD_nRF` becomes whatever `UICR.REGOUT0` says — **1.8 V on an erased UICR** — and the DK's own VDD *follows VDD_nRF* by design (HW user guide § 4.3.2), so the header rail every breakout is powered from goes to 1.8 V with it. Every one of them is then below its minimum supply. Boot the firmware and read its `supply:` line (`app/src/supply.rs`; the `error!` variant names this exact case), then confirm with a meter that the header VDD reads ~3.0 V **in the Li-Po position** — the firmware can report the configuration, only a meter confirms the rail. [`parts.md`](parts.md) note 0.

### Step 3 — GNSS (u-blox MAX-M10S, single-band L1)

- [ ] **The link comes up at 38400 with no configuration step.** [§ 622](../architecture/decisions.md) sets the firmware to the M10's factory default precisely so a receiver nobody has touched works cold; this is the first evidence either baud has ever had, since Renode's PTY feed ignores the baud register entirely. Confirm sentences parse on a module straight from the bag, and — the case the decision exists for — again on one that has sat unpowered long enough for its MS621FE backup cell to lapse. If a previous owner set it to something else in u-center, reset the module to defaults rather than editing the firmware.
- [ ] **Cold time-to-first-fix under open sky ≤ 60 s.** From an unpowered receiver, clear sky, stationary. Source: [`prototyping.md`](prototyping.md#what-working-means-at-this-tier) tier-1 "GPS fix in <60 s outdoor". Record the actual value.
- [ ] **Warm/hot restart TTFF recorded. (open value)** No existing doc sets a tier-1 warm-start bar. Measure it (receiver re-powered within a few minutes, ephemeris still valid) and set the bar from the first measurement.
- [ ] **Sustained 1 Hz fix rate in Performance mode.** Source: `prototyping.md` "logs NMEA at 1 Hz". Confirm from the `defmt` stream over ≥ 5 continuous minutes: fix count ≈ elapsed seconds, no parser rejections.
- [ ] **Fix cadence matches the selected GNSS mode: ~1 s / ~15 s / ~60 s** for Performance / Balanced / Expedition. Sim-verified already; bench-confirm on real NMEA where the receiver, not a fixture, sets the timing.
- [ ] **The receiver actually enters backup mode on UBX-RXM-PMREQ, and wakes on the 0xFF byte.** This is the *only* place the GNSS power-down lever can be verified — the sim's feed ignores PMREQ by design. Confirm both directions: fixes stop during the window, and the first fix after the wake byte arrives inside the reacquire margin. Then measure the receiver's sleep-vs-tracking current on the PPK2 (below); that measurement is the lever's real payoff.
- [ ] **Dropout honesty over a measured jog.** Run a route with a known signal-poor stretch (underpass, dense canopy, building line). Confirm: the panel reads `STALE` / 0 signal bars during the void rather than a frozen-fresh position; and — the bug the 2026-07-19 sim pass found — **distance resumes accruing after reacquire** rather than freezing for the rest of the run (`run_recorder` #330 gap re-anchor). Record the observed dropout fraction of the jog. **The acceptable dropout rate is an open value** — tier 2's "100 % outdoor fix reliability" bar is explicitly *not* tier 1's, and a breakout antenna with no case cannot be held to it.
- [ ] **GSA-derived signal meter is honest on real sky.** Fix type 0/1 reads 0 bars even with satellites in view; 2D caps; 3D uses the full ladder.

### Step 4 — display (Sharp MIP, reflective, no backlight)

- [ ] **Panel initialises and renders the idle face on the real module** — line-update wire protocol correct at the delivered resolution, no shear, no inverted rows.
- [ ] **A resting screen flushes zero SPI lines.** The dirty-line + compare-write contract plus hardware EXTCOMIN VCOM is a load-bearing power claim; confirm on the logic analyser (no SPI traffic while the framebuffer is unchanged) and again on the PPK2 (display-rail current with a static screen).
- [ ] **Direct-sun legibility, read at arm's length, no touching the watch.** Midday direct sun. Read (a) the 32×48 numeral hero on the Distance/Pace glance pages, (b) the 16×32 medium face, (c) the 8×16 context rows, (d) an inverse-video banner (`! DRINK`, `OFF COURSE`, `STOP? BTN2`). Yes/no per tier. A reflective MIP panel *gains* contrast in sun, so a failure here is a **layout / font-size finding**, not a panel finding — record which tier failed and at what distance.
- [ ] **Headlamp legibility at night, same four tiers, same protocol.** There is no backlight at tier 1, so this is the honest night test.
- [ ] **Off-axis + sweat/rain-on-glass legibility recorded. (open value)** No doc sets a bar; record whether the hero stays readable at the wrist angle a running arm actually presents.
- [ ] **A composed `Duo` and `Trio` are legible at 45 cm, at hour 60, by headlamp.** The layouts are sim-verified to *render* (§ 364) and their arcmin figures — 38.8 for the 32x48 face, 19.4 for the 16x32 — are **derived, not measured**. Whether a tired runner actually reads two or three values at once is the claim no simulator settles, and the one that decides whether the reserved `Quad` seat ever gets built. (The `SCR1` push that puts them there is a step-6 radio item.)
- [ ] **The `+` / `|` glyph repair holds on the real panel.** The Pacer page's whole purpose is a signed `+0:42` / `-1:05` delta and the VERT row's `+gain -loss`; two sim passes independently caught these rendering as `-` and as blank. Confirm on glass, not only in a framebuffer test.
- [ ] **Battery-gauge icon: no fabricated percentage on the DK.** USB-powered, the DK regulates VDD to ~3.0 V and the plausibility band must read that as **absent** (no icon, no `BAT` row) — not a confident 0 %. Then wire the 500 mAh LiPo and confirm a plausible percent appears. The DK takes the cell on its own Li-Po connector (J6/P27) with **SW9 in the Li-Po position**, which supplies the SoC's high-voltage regulator, so use it from the start rather than jumpering to VDD. **The firmware now picks the SAADC input from the supply mode** ([decisions § 691](../architecture/decisions.md)), so this item has a second thing to check that costs nothing: the boot log's `supply:` line must say **HIGH-voltage mode** in the Li-Po position and the `battery: streaming on VddhDiv5` line must name that sense. If the streaming line says `VddDirect` while the cell is on the Li-Po connector, the mode read is wrong and the percent is coming off the internal regulator, not the cell — a wrong `MAINREGSTATUS` read is the one failure that would put a plausible-looking constant on the face.

### Step 5 — optical HR (MAX30101 at tier 1; MAX86177 stays the production pick)

Retargeted by [§ 623](../architecture/decisions.md). The behavioural claims below are part-independent and stand as written; the three that name a MAX86177 mechanism are marked, because on the MAX30101 the *design* is the same and the *mechanism* is not.

**Do this step's items in order, and expect the first one to be the hard one.** Every other subsystem on this list is a wire-it-and-read-it proposition; optical HR is the one whose signal depends on *mechanics* — skin contact pressure, stray light, and the distance between emitter and photodiode — as much as on any register. The breakouts on [`parts.md`](parts.md) are **fingertip-demo boards**: they are built to be pressed against a fingerpad, not strapped to a wrist, and none of them ships the light barrier a wrist sensor needs between the LED and the diode. A bare board Velcro'd to an arm may well produce nothing a detector could lock onto, and that failure will look exactly like a driver bug. It is not one. Budget for iteration here.

- [ ] **The emitter check is proved, not assumed — a negative control, and it takes a minute.** [§ 625](../architecture/decisions.md) added two guards against a MAX30102 arriving in a MAX30101's place, and a guard nobody has seen fire is a guard nobody knows works. Force `LED_PA_DEFAULT_CODE` to `0` (or lift the LED3 line) with a finger on the sensor and confirm the `hr: LED at max drive with no reflected signal` line appears within ~30 s of the auto-gain loop reaching its ceiling; then restore and confirm it never appears again for the rest of the session. Doing this on the *first* day means that if the real thing ever fires, it is believed. **Also record the corrected DC an off-wrist sensor reaches at maximum drive** — package crosstalk alone should keep a genuine emitter above the dark floor, and if it does not, this check's false-positive surface is wider than § 625 assumed and the dwell needs raising.
- [ ] **A pulse at all, fingertip first.** Before anything is strapped to anything: hold a finger on the sensor, still, indoors, and confirm the detector locks. This isolates *the optical stack works and the driver reads it* from *the wrist mounting is any good*, and doing it in the other order confuses the two for an afternoon. If this fails, the problem is registers or wiring; if it passes and the wrist fails, the problem is mechanical.
- [ ] **A wrist mount that actually excludes light. (open value)** Record what it took: contact pressure, whether a foam or heat-shrink light barrier between emitter and diode was needed, and how much the reading degrades when the strap is one notch loose. **This is a tier-1 finding worth more than most of the numbers on this page** — it is the input to whether the tier-2 enclosure needs a designed optical well, and nothing in the firmware can substitute for it.
- [ ] **~25 Hz sampling with a displayed BPM on-wrist.** Source: [`prototyping.md`](prototyping.md#what-working-means-at-this-tier) "Optical HR sample at 25 Hz, displays current bpm".
- [ ] **Resting BPM within ±5 bpm of a reference over a 60 s still hold. (open value on the tolerance)** No doc sets an accuracy bar and the licensed Maxim algorithm is explicitly post-tier-1 (§ 82 accepts "raw photodiode reads + naive peak-detect"). Compare against a chest strap or a manual pulse count, record the delta, and set the bar from the first measurement. The tier-1 claim is "a plausible pulse is detected and displayed", not "clinically accurate".
- [ ] **Off-wrist reads honest "no HR", never a garbage BPM off ambient light.** Lift the sensor off the skin under room light and under bright sun; confirm `--` rather than a fabricated number in both.
- [ ] **Bright-sun saturation is honest both ways.** With ambient subtraction: a corrected DC back in band recovers a real BPM; a raw sample pinned at full scale **stays** saturated and blank. **Part delta, now implemented:** full scale is the MAX30101's **18-bit** ceiling (`PpgScale::BITS_18`, half the MAX86177's, derived by proportional rescale rather than re-guessed), and the ambient (LED-off) sample comes from a **multi-LED slot** rather than a MEAS2 channel told apart by a FIFO tag. Same design, different mechanism. This is the one place the ambient/dark-slot design can be tested at all; the sim model's ambient is synthetic common-mode.
- [ ] **Duty-cycling really shuts the part down and the register file survives it.** Balanced mode: 15 s on per 60 s window. Confirm the part is in shutdown between windows (PPK2 current step, not just a `defmt` line), that the FIFO is flushed on wake so pre-shutdown counts can't replay, that no re-init or AGC re-walk is needed after wake, and that the displayed BPM is honestly *held* then blanked at its budget rather than re-sampled. **Register retention across shutdown is a bench-verify assumption, and § 623 makes it a *new* one** — it was logged against the MAX86177 and does not transfer to a different part's silicon, so it is re-opened here rather than inherited. The Renode model that exercised it was told to retain.
- [ ] **LED AGC converges on real skin, across skin tones and under motion. (open value)** The AGC's step/hysteresis/clamp constants are conservative starting points flagged for on-device calibration. Record where it settles; do not claim tuned. **And note what the register map's being public does *not* buy:** two boards carrying identical MAX30101 silicon can need different `LED_PA_DEFAULT` and different contact bounds, because emitter-to-diode spacing, the window material and any light barrier are the board's design, not the chip's. So the tuning constants are specific to the module actually bought — record which one they were measured on, and treat a board swap as re-tuning rather than a drop-in.
- [ ] **The 18-bit thresholds are a rescale of a guess, and this is where that gets settled.** `PpgScale::BITS_18` is `BITS_19` scaled proportionally ([§ 623](../architecture/decisions.md)), and the 19-bit set was itself chosen without a wrist — so the contact band, the rail floor and the envelope floor are *consistently* unmeasured rather than *independently* wrong, which is the honest starting point and not the same as correct. Log the raw DC and envelope over a worn 60 s hold, an off-wrist 60 s, and a bright-sun 60 s, and set all four bounds from those three traces.
- [ ] **Motion-artifact rejection on a descent.** The 30–220 bpm physiological band + peak-hold SNR gate should keep `valid=false` under sustained artifact rather than reporting a spurious-fast BPM. Wrist HR on high-cadence trail descents is famously bad ([`vision.md`](vision.md) req #8) — the tier-1 claim is honest invalidity, not correctness.

### Step 6 — BLE GATT (S140 SoftDevice) — the whole rung is new at the bench

Everything here is currently **build-verified only** and can never be sim-verified ([§ 210](../architecture/decisions.md#210-tier-1-ble-s140-softdevice-is-a-compile-verified-feature-gated-build--mutually-exclusive-with-the-sim-off-by-default)). This is the largest block of untested surface in the workspace.

- [ ] **S140 hex + app flash together and boot.** Confirm the RAM origin the SoftDevice reports at boot matches `memory-ble.x`'s assumption, and that peripheral interrupt priorities at P2 clear the SoftDevice's reserved 0/1/4.
- [ ] **The phone discovers and connects to the Threkir service**, and the negotiated connection interval is ~1 s (not the 7.5 ms HID default) — the ~100× idle-radio-power lever from [`performance_path.md`](performance_path.md#ble-connection-interval-tuning--5-to-20-radio-power).
- [ ] **Just-works LESC pairing completes, one bond persists, and an unbonded central can read nothing.** Confirm the `BND1` record survives a power cycle and that a re-connect after reboot resumes encrypted without re-pairing.
- [ ] **Every config-page record coexists** — `CFG1`, `BND1`, `WPT1`, `ICE1` and `SCR1` share one erase page, so writing any one must carry the other four forward across a real erase/write cycle. The host tests prove the offsets do not overlap and the decoders do not confuse each other; only the board proves the rewrite actually lands all five.
- [ ] **`SCR1` push and restore.** A composed-screen set pushed over the unchunked `screens` characteristic survives a power cycle, and a frame with a bad CRC or an unknown metric byte is **refused whole** (the watch keeps the screens it had) rather than partially applied.
- [ ] **Live link frames notify at ~1 Hz** and decode in the app with the transport-agnostic decoder (same bytes as the UART transport).
- [ ] **Settings push applies over the radio.** A current-version `SET1` frame (v8; the phone's encoder emits it) sets max HR / pacer goal / fuel intervals / QNH / timezone / page mask; a bad-flags frame is **rejected** (fail-closed) rather than partially applied. Confirm the timezone push flips the home clock hero `UTC` → `LOCAL`. Since § 403 every accepted version carries a checksum, so also confirm a **v1 or v2 frame is refused** — those arms no longer decode, and a CRC-sealed legacy frame must still be refused on its version byte alone.
- [ ] **A bonded watch refuses a second pairing (§ 401).** Pair phone A, then attempt to pair a second central B while A's bond is stored: B must be refused *and* dropped, not silently bonded. Confirm A still re-encrypts without re-pairing after a power cycle (the `SEC_INFO_REQUEST`/LTK path).
- [ ] **The § 432 pairing window admits exactly one wearer-sanctioned re-pair.** With phone A bonded, open the window from the menu's PAIR PHONE row (arm + confirm) and pair phone B inside it: B must bond, `BND1` must now hold B, and A must fail to re-encrypt. Confirm the window closes the moment B bonds (a third central inside the residual 90 s must be refused), that an *expired* window refuses again, that a left press closes an open window early, and that a reboot with a window open comes back closed (the atomic deadline does not persist — fail-closed). Then the support case: a phone whose own bond was deleted re-pairs through the window with no FACTORY ERASE and no data loss; the erase remains the fallback and must still clear `BND1` and allow a fresh pair.
- [ ] **The run manifest is correct between changes (§ 402).** It is now rebuilt on a store generation counter rather than every second, so read `run_manifest` at a moment when nothing has changed and confirm the run list is still right, and that a newly-finished run appears promptly both on a fresh connect and mid-connection.
- [ ] **The phone reads the manifest from the right characteristic (§ 410).** The Dart client's UUIDs were shifted by one against the firmware's GATT table, so watch→phone sync could never have worked; confirm on the wire that the manifest read lands on `..e2` and the chunk request/notify pair on `..e3`, and that the write to `..e3` is actually honoured by the SoftDevice.
- [ ] **Course push over the chunked write characteristic** loads a real course; the Nav page draws it and stops reading `NO COURSE LOADED`.
- [ ] **A refused push says so on both sides (§ 464).** For EACH of the five pushes — settings, course, workout, screens, roadbook — deliberately corrupt the frame (flip a CRC byte, or write chunk 1 before chunk 0) and confirm three things: the previous value is still armed, the wrist raises that push's own `! SET / CRS / WKT / SCR / RBK FAIL` banner *during a run*, and the phone reports a refusal rather than success. Then the opposite half, which is the one an ack channel gets wrong: an ACCEPTED push must raise no banner and must not be reported as refused. Finally the ordering hazard the design assumes away — read `push_status` immediately after a write and confirm the polling loop still converges (the SoftDevice may answer the read from the attribute table before the handler has run), and confirm a push whose verdict never arrives completes as *unconfirmed* rather than failing.
- [ ] **Flash writes are SoftDevice-arbitrated.** The `ble` build aliases the run-store backend to `nrf_softdevice::Flash`. Confirm a commit *and* a mid-run checkpoint complete with the radio connected and advertising — a raw-NVMC write under an active SoftDevice is the classic way to hang or corrupt.

### Step 7 — integration (recording, storage, controls)

- [ ] **§ 82's DoD, in one run.** Outdoors, on a real wrist (Velcro is fine), the run records GPS + HR, shows pace + distance on the MIP panel, and syncs over BLE to the paired phone, which writes `{user_id}/{run_id}.json.gz` to the `runs` Storage bucket. **This is the DoD, not a checklist item among equals.**
- [ ] **The synced row matches the wrist.** Distance, elapsed, moving time, and lap count on the Supabase row agree with the panel's final numbers within rounding; the track renders as a sane line on the map; per-point `bpm` and altitude are present where the sensors were live and *empty* where they were not (never stale). **Do this test below 3,276.7 m, or read the next item first.**
- [ ] **The synced track carries an altitude everywhere the panel showed one — including above 3,276.7 m, which it could not until run-store v5.** `run_store::TrackPoint::ele_m` is an `i16` of **metres** as of [§ 1026](../architecture/decisions.md), so `record_cadence::ele_m_from_m` now stores every altitude `elevation::plausible_alt` admits (-500 m..=9,000 m) and the two windows nest — pinned by `every_plausible_altitude_now_reaches_the_track`, which sweeps all 9,501 metres of the plausible window. Through v4 the field held **decimetres** and capped at 3,276.7 m, so the watch went on trusting and displaying an altitude it could no longer write down: the whole band this device's races live in was inside that gap (Hardrock's Handies Peak is 4,285 m against a 2,830 m low point, so **69 % of a Silverton-Handies-Silverton profile stored no elevation**; Leadville's Hope Pass is 3,840 m), and because the gain rule carries the last reading across a dropout the summit push netted to **zero vert** rather than to a gap. So an ALT column empty over the high half of a mountain run is now a **finding**, not the expected result of the format — check the barometer and the `plausible_alt` gate rather than the wire. Two bench claims this makes newly checkable: that a summit above 3,276.7 m appears in the synced track's `ele`, and that the run's cumulative vert over such a climb matches the panel. `CRS1`, the course frame, chose `i16` metres for this reason back in [§ 334](../architecture/decisions.md) — the run track took until § 1026 to follow, and until it did the same watch could be handed a course it could not record itself running. **A v3/v4 blob still in a bench board's flash keeps its decimetre meaning**, resolved per header version by `run_store::ele_metres`, so an old blob read back is 10x smaller than a new one carrying the same bytes and that is correct.
- [ ] **Laps: the armed auto-lap and a BTN5 manual lap both close and both persist.** Run-store v2 stores laps as interleaved tagged records; confirm they arrive as the registered `metadata.laps` shape. Since [§ 374](../architecture/decisions.md) the trigger is chosen (`watch_core::auto_lap`, default 1 km) — so also confirm a `SET1` v7 push changes it, that a power cycle restores the pushed rung from `CFG1` rather than reverting to 1 km, that the Lap page names it, and that a watch left stationary for an hour with a **time** rung armed closes **no** lap (the moving-time budget, not the elapsed clock).
- [ ] **Auto-pause at the 0.5 m/s gate, and a snapshot-honest state tag.** Stop dead → `AUTO`; press BTN1 → `PAU`; walk slowly through the 3 m min-move filter → steady `REC`, not a flicker.
- [ ] **The 253-point flash cap behaves at the boundary.** Run past the cap in Performance mode and confirm run-store v2 **decimates rather than truncates**: the whole run stays represented at coarser resolution, laps are never dropped, the wrist shows `! TRACK 1/k RES`, and the phone still verifies the CRC and ingests it. Then check the boundary exactly: 252, 253, 254 stored anchors.
- [ ] **All four slots fill, and eviction sacrifices the right run.** Record 5 runs without syncing; confirm the `synced`-flag victim rule sacrifices a fully-pulled run before a still-unsynced finished one.
- [ ] **A run survives a reboot mid-recording.** Pull power mid-run and confirm the 300 s / 60-point checkpoint recovers a *partial* run rather than nothing; that boot-time slot recovery re-advertises runs from a prior power cycle (footer magic + CRC scan, no RTC); that `next_run_seq` resumes above the highest recovered seq; and that the phone dates a prior-boot run sensibly rather than in the future.
- [ ] **The 256-point course cap rejects an overflow rather than truncating mid-race**, and a longer course is phone-simplified before the push. Check the boundary: 255, 256, 257 points.
- [ ] **Measure the residual flash-write stall and the NMEA loss it still causes.** Partly fixed 2026-08-01 (§ 419): writes now yield every 256 bytes (~42 ms → ~2.6 ms max contiguous), but a **page erase halts the CPU for ~85 ms and that is irreducible** — the nRF52840 halts the core while the NVMC writes or erases, so yielding cannot help when every other task also lives in flash. A checkpoint erase is exactly one page (`SLOT_LEN` == `PAGE_SIZE` == 4096), so the recording path keeps one 85 ms stall. **Correcting an earlier note on this line: the `ble` build is NOT exempt** — `sd_flash_page_erase` halts the core for the same 85 ms, so "it awaits, therefore it is fine" is true of the executor and false of the CPU. **The NMEA-loss consequence is fixed in software as of 2026-08-19 and is now what this item measures, rather than what it predicts.** `gps.rs` used to re-arm a single 32-byte DMA read, which fills in 8.3 ms at the link's 38400 baud and then left the receiver disarmed, so **up to 326 bytes** of NMEA could be lost per erase (the parser resyncs on `$`, so sentences were lost, not the run — those figures re-derived at 38400 by [§ 622](../architecture/decisions.md); the ~82-byte prediction this line used to carry was a 9600 number). The receiver now runs on `BufferedUarte` over a **2048-byte** ring ([§ 698](../architecture/decisions.md)), which pre-arms the next transfer and chains it over PPI so a CPU halt loses nothing; the ring is sized off the guaranteed `half_len` headroom — 1024 B = 267 ms, three erases' worth — where the 512 bytes the earlier notes specified buys 66.7 ms and covers none. **The clause that said the fix was blocked on the simulator no longer holds**: § 421's SHORTS timer was necessary and not sufficient (Renode 1.16.1's UART implements no `INRFEventProvider`, so every PPI channel sourced from a UARTE event was refused outright), and `sim/NRF52840_UARTE_Events.cs` closed that second gap. Nine sim scenarios pass with the swap in. **What the simulator still cannot do is produce the stall at all** — Renode's NVMC model is absent entirely — so nothing has ever observed this code path meeting an erase, and the bench item stands unchanged. Pass criterion, same as it was: capture UARTE0 RX across a checkpoint boundary and count lost sentences, now against an expectation of **zero** rather than the 326-byte worst case (a loss here says the ring is undersized or the chain never armed), and instrument button-press-to-`RECORD_CMD` latency with a GPIO toggle timed to straddle one.
- [ ] **An undroppable record at the slot cap costs one record, never the run (§ 409).** Fill a slot to the 253-record cap, then close a lap and stop — confirm `finalize` still writes the footer and the phone ingests the blob, rather than the whole run being discarded. Repeat with a workout armed, which is the deterministic trigger (`flush_workout` then `commit_run`, no intervening thin).
- [ ] **Off-course alert latches at 40 m and re-arms below 20 m** on a real GPS trace — no flapping at the boundary, and the banner is steady (never blinking, so a lost runner cannot catch a blank frame).
- [ ] **Barometric altitude and vert on real weather.** QNH re-zero (BTN3 hold on the idle face) snaps the bias, refuses honestly with `NO GPS FIX` / `NO BARO`, and never banks the step as vert. Over a flat out-and-back with a pressure trend, confirm the complementary filter subtracts drift instead of banking phantom vert; over a real climb, confirm gain is within a plausible margin of a known reference. **The filter's tuning constants are conservative starting points — record what they need, don't claim tuned.**
- [ ] **Press grammar, one-handed, with cold and gloved fingers (§350).** Debounce holds; a BTN4 tap pages right and a BTN3 tap pages left — the spatial pair, in every run view including `FIN`; a 0.5 s hold on either paging key opens the page grid *at the threshold* (mid-hold, no release timing); the grid's 3 s inactivity auto-select works and BTN1 confirms a jump (`B1 GO`); BTN2's two-press stop guard arms with a visible `STOP? BTN2` prompt and disarms after 4 s; BTN5 — the external momentary on P0.02, the DK has no fifth button — takes a lap with no perceptible latency; BTN1 dismisses a finished run. **Explicitly out of scope: button *feel*.** The DK's tactile switches are not watch buttons — actuation force, travel, and glove operability are tier-2 mechanical concerns ([`prototyping.md`](prototyping.md#tier-2--wearable-prototype-15k40k-diy-80k250k-consultant-built-918-months) "has buttons that feel like buttons"). What tier 1 can falsify is whether tap and hold are distinguishable by a tired hand at the single 0.5 s boundary — record any press that landed on the wrong tier.
- [ ] **The settings menu, one-handed (§351, directional).** Idle BTN5 opens it; BTN2/BTN3 (the UP/DOWN slots) step the cursor across all **eight** rows and wrap; BTN1/BTN5 edit right/left — GNSS MODE ladders toward Expedition on right and clamps at both ends (no wrap-teleport), HIDE EMPTY is right=ON / left=OFF, PROFILE (§ 353) ladders Run → Trail → Ultra → Hike and applies a page mask *and* a GNSS mode through the same channels a phone push rides, all three **surviving a power cycle** (CFG1); RE-ZERO fires on right and closes with the idle banner answering; MEDICAL ID (§ 358) opens the ICE card; BACKYARD (§ 372) is right=ON / left=OFF and its arm re-points the auto-lap onto the corral bell; PAIR PHONE (§ 432) is a guarded row whose radio half has its own item above; FACTORY ERASE (§ 378) is the other guarded row and has its own item below; BTN4 (the BACK slot) exits; 30 s of no presses closes it back to the clock; and no press inside it can start, pause, or lap a run. **Time the farthest rows**: PAIR PHONE sits four cursor steps out at 7 presses to open a window (arm + confirm), BACKYARD and FACTORY ERASE three steps out at 5–6, while every mid-race row kept its ≤ 4 — record whether the extra steps are felt on the rows that pay them. Also read the list itself: § 432 put the body on § 333's scroll window (eight items, seven visible rows), so confirm the scroll is legible — MEDICAL ID below the fold at the top of the list, GNSS MODE leaving the top when the cursor reaches the last row — and that the title band still reads as chrome.
- [ ] **The filtered page cycle is honest.** An unsynced watch cycles only data-present pages (roughly 8–12, not 40 empty-state stops); every synced-only page reads `NOT SYNCED` rather than zeros; the page-dot indicator counts the filtered set. Stays bench-gated: the sim's `pages` scenario is authored to check that each page *renders*, which is a different claim from the filter being right on a watch that has actually been synced by a phone — and the sim has no phone.
- [ ] **A waypoint mark survives a battery pull (§357).** Hold BTN5 mid-run, confirm the `WPT` page shows a distance that grows as you walk away and a bearing that points back; pull power, reboot, and confirm the mark is still there with the same coordinates. Then check what the host cannot: that the hold is distinguishable from the lap tap by a cold hand at the single 0.5 s boundary (record any press that landed on the wrong tier — a mis-tiered press here costs a lap, not just a page), and that the eight-slot store's newest-wins eviction behaves on the ninth mark.
- [ ] **The ICE card reaches a stranger (§358).** The one item on this list whose pass criterion is another person: hand the powered watch to someone who has not seen it and ask them to find the wearer's emergency contact. Record how long it took and what they pressed. Confirm the card survives a power cycle (the `ICE1` record), that a `SET1` push carrying a corrupt ICE field leaves the *previous* card standing rather than half-applying, and — the known tier-1 limit, so measure its cost rather than assuming it — how much worse the same task is while a run is recording, since the face is idle-only.
- [ ] **The timer, and the thing it deliberately cannot do (§375).** Idle BTN2 opens the modal; BTN2/BTN3 walk the eleven-rung preset ladder and clamp at both ends; BTN1 starts, stops and resumes; BTN5 resets and is refused while running; BTN4 exits *without* stopping the instrument. Confirm the countdown keeps running across a run start and a run stop, that the `TIMR` page joins the cycle only while it is armed, that an expiry raises `! TIME UP` exactly once per arming, and that past zero the page reads `+M:SS` and keeps climbing. Then the item that is not a software check and is the whole point of the design: **hold the watch at arm's length and confirm you cannot tell an expired timer from an unexpired one without looking at it.** That is the honest statement of the tier-1 limit — there is no motor and no buzzer, so the banner is all there is. Record how long the runner went past zero before noticing on a real jog; the number is the tier-2 haptic channel's justification. **Not a check of an alarm: there is none, by decision (§ 375), and finding one on the device would be the defect.**
- [ ] **A backyard bell over several real hours (§372).** The one surface no
  simulator can reach — the sim's NMEA fixtures carry no multi-hour clock, so
  nothing has ever driven a bell. On the bench, arm BACKYARD from the settings
  menu and let a run stand through at least three hour boundaries. Confirm: the
  countdown reaches `0:00` at the *receiver's* hour, not at an hour after the
  run started; the bell closes a lap with no press and the loop count reaches
  it; a BTN5 press before the bell closes the loop instead and the bell's own
  lap then does **not** fire; the corral row flips back to `ON LOOP` at the
  bell with nothing pressed; the whistles fire once each at 3, 2 and 1 minutes
  and are visible on a page other than the Backyard page; and the arm survives
  a power cycle (the `CFG1` bit). Then check the two refusals on real inputs:
  with no timezone pushed the page reads `NOT SYNCED`, and before the first
  RMC clock `AWAITING FIX` — never a counting hero. Record the drift between
  the panel's `0:00` and a reference clock over the three hours; the countdown
  extrapolates on uptime between fixes, and how far a crystal walks in an hour
  is **an open value** nothing on this workstation can settle. Since 2026-08-18
  also record whether the extrapolated local time ever steps **backwards** when
  a fix re-disciplines it, and by how much: a retreat used to ring a phantom
  bell, and the guard that now absorbs one (`BELL_ROLLOVER_MIN_DROP_S`, half a
  window) is sized against a *derived* ~20 ppm drift, not against this
  crystal. The countdown must never gain a second back.
- [ ] **A pressure tendency against a real front (§376).** The one item here
  that needs the *weather* to cooperate, so it is opportunistic rather than
  scheduled: wear the prototype through an approaching front (a met office
  station's own 3-hourly tendency is the reference) and confirm the reduced
  pressure tracks it within a hectopascal or so, and that the banner fires
  ahead of the weather rather than with it. Then the two the bench can force:
  carry the watch up 500 m of real ascent under settled conditions and confirm
  the trend stays `STEADY` — the whole feature is that this is not a storm —
  and walk into a canyon or under heavy canopy and confirm the page goes to
  `NO ALT REFERENCE` rather than continuing to trend off the last good fix.
  **Record what the 4 hPa / 3 h threshold needs; do not claim it tuned.** The
  window and the threshold are chosen values (register above), and the first
  real front is the first evidence either has ever had.
- [ ] **Climb detection on a real hill (§359).** The thresholds (20 m open, 10 m close, ≥ 2 %) are conservative starting points chosen against baro noise, not tuned against terrain — **record what they need, don't claim tuned**, exactly like the vert filter above. On a real rolling course confirm a roller never opens a climb and a saddle mid-ascent never re-zeroes the banked gain; on a pushed course with elevation, confirm the crest-ahead number shrinks monotonically toward a summit you can see and does not stop early on a shelf.
- [ ] **The factory erase actually clears flash (§ 378).** The one item on
  this list whose whole point is a byte pattern, and the half no simulator can
  settle — Renode answers `NVMC:READY` from its SVD and swallows the
  `ERASEPAGE` write, so the firmware reports success over an emulator that
  changed nothing. On the board: record several runs, mark waypoints, push an
  ICE card and pair a phone, then run the erase from the settings menu and
  **read the flash back over the probe**. Pass criterion: every byte from the
  config page through all four run slots reads `0xFF` — not "the manifest is
  empty", which a directory drop would also produce, and which is exactly the
  distinction this erase exists to make. Then the four consequences: the
  manifest advertises nothing, the `WPT` page has no mark, the ICE face reads
  its empty state, and **the previously paired phone must re-pair rather than
  re-encrypt** (the `BND1` record is gone). Also confirm the two limits stated
  in [`privacy.md`](privacy.md): a connection that was live *across* the erase
  is not torn down (only the durable bond goes), and the erase says nothing
  about a probe attached before it — there is no encryption at rest.
- [ ] **The erase's guard, with cold hands.** Right on the row must arm and
  not wipe, the legend must read `B1 ERASE    B4 CANCEL` legibly, any other
  press must cancel, and the 4 s window must lapse *visibly* back to
  `FACTORY ERASE`. Then the question a bench answers and a test cannot:
  **hand the watch to someone and ask them to change a setting** — record
  whether they ever land on the erase row, and whether the armed prompt reads
  as a question rather than as a state. A guard that a tired runner reads as
  "it already erased" is a failed guard even though it wiped nothing.
- [ ] **≥ 8 hours of continuous GPS recording on the 500 mAh LiPo.** Source: [`prototyping.md`](prototyping.md#what-working-means-at-this-tier) tier-1 "Battery powers the system for ≥8 hours continuous GPS (the dev board itself burns most of the budget)". Note the caveat *in the claim*: this is a DK-plus-breakouts figure dominated by the debugger and the non-sleep LDOs, and it is **not** a watch battery number.
- [ ] **A 24 h+ uptime soak with no freeze.** The RTC overflow the sim found froze the recorder 8.5 min in; every timer, counter and `999h` display field wants a long real run over it. Record the longest continuous recording achieved.
- [ ] **The 3D-printed chassis holds the breakouts + LiPo and takes a Velcro strap** well enough to jog in. Source: `prototyping.md` tier-1 deliverables. Not a case, not sealed, no IPX claim.

### Power instrumentation (per [§ 83](../architecture/decisions.md#83-tier-1-power-measurement-uses-nordic-power-profiler-kit-ii-applied-per-subsystem))

Five rigs, PPK2 wired **between each isolated subsystem and its power input** — never whole-device, because the DK's ~30 mA idle makes that reading meaningless. Each produces a number recorded in [`tier1_log.md`](tier1_log.md).

- [ ] **Bare-MCU deep-sleep current.** Everything else powered off.
- [ ] **GNSS module: acquisition vs tracking vs backup**, measured separately — this is what turns the UBX-RXM-PMREQ lever from a derivation into a number.
- [ ] **The HR AFE across LED-on / LED-off / readout**, and the shutdown current between duty-cycle windows. The delivered part is a MAX30101 breakout ([§ 623](../architecture/decisions.md)), which regulates its own rails on-board — so this rig measures the *module*, and its figures do not project to a bare production AFE the way the other four rigs project to their parts.
- [ ] **Sharp MIP: static, per-line update, full-frame redraw.** Datasheet-derived sanity anchor: ~10 µA static ([`vision.md`](vision.md) req #3, [`performance_path.md`](performance_path.md#display-sharp-mip-vs-amoled--3-to-4-total-battery-life)).
- [ ] **Combined "all sensors live" rig**, still excluding the debugger and LEDs.

**Pass criterion for all five:** a number exists, and it is within an order of magnitude of the part's datasheet figure. A reading that disagrees with the datasheet by more than ~10× means **the rig is wrong**, not the datasheet — re-wire before recording. Absolute targets are deliberately absent: § 83 is explicit that these inform tier-2 planning and do not gate tier-1, and the only figures the docs commit to are tier-2/3 projections (nRF52840 ~50 µA/MHz active, Apollo-class ~3 µA sleep, MIP ~10 µA static). **Any tier-1 sleep-current target is an open value** to be set from the first clean measurement.

## Derived-not-measured numbers this checklist finally lets someone confirm or refute

These are the figures currently shipping in the firmware and its docs as **derivations, not measurements** — honest, and labelled as such (the idle face even renders the battery figure as `EST`), but nothing on the device is checkable against reality until the PPK2 rigs run. Listed so the bench phase knows exactly which claims it is there to settle.

**Battery projections (highest-value, least-supported):**

- **The ~110 / ~180 / ~220 h GNSS-mode figures** for Performance / Balanced / Expedition. Derived in `watch_core::gnss_mode`'s module docs from [`vision.md`](vision.md)'s tier-2 ~110 h target plus [`performance_path.md`](performance_path.md)'s GNSS duty-cycling lever. They already *assume* the UBX-RXM-PMREQ receiver power-down, which has never run on a receiver. Refuting these is the single most valuable bench outcome, because they are the numbers a reader is most likely to mistake for measurements.
- **The HR duty-cycle saving** (15 s per 60 s / 120 s window). Deliberately **not** folded into the GNSS-mode projections precisely because it is unmeasured — keep that separation until the PPK2 says otherwise.
- **The GNSS power-down scheduler's constants** (sleep window, 5 s reacquire margin, PMREQ self-wake backstop duration).
- **The battery-percent curve.** A piecewise-linear 1S LiPo *resting*-discharge model; a loaded cell sags below the anchors, so the displayed percent is systematically optimistic under load by an unmeasured amount.
- **The SAADC millivolt scale itself** (`battery_sense::FULL_SCALE_VDD_MV` = 3600, `FULL_SCALE_VDDHDIV5_MV` = 6000, [decisions § 691](../architecture/decisions.md)). Pure datasheet arithmetic — the 0.6 V internal reference times the configured gain, times five for the `VDDHDIV5` divider — with **no allowance for the converter's own gain and offset error, and none for the divider ratio's tolerance.** So the percent is a derivation on top of a derivation: an unmeasured curve read through an unmeasured scale. The scale is the cheaper of the two to settle (one multimeter reading across the cell against one `battery:` log line) and it is the bench item above.
- **The mid-run checkpoint's flash-wear estimate** (~1200 page erases in a 100 h run, ~12 % of endurance).
- **The bench rig's own ~20-25 h run time on the 500 mAh cell** ([`parts.md`](parts.md)). A datasheet sum — ~10-13 mA GNSS (u-blox's own 7.0 mA VCC + 2.2 mA V_IO acquisition figures), ~6 mA MCU, ~2 mA board overhead, under 1 mA for everything else — against a nominal capacity, with no allowance for the cell's unusable tail or the DK's real quiescent draw. The MCU and board terms are the guesses; the receiver's is the only one with a datasheet behind it. Quoted only to say the DoD run fits inside one charge, which is a much weaker claim than the figures above and must not be repeated as a device capability.

**Capacity limits — bench-prototype foundations, explicitly not shipping capacity:**

- **The 253-point-per-run cap over 4 × 4 KiB internal-flash slots.** A few minutes of track per run at 1 Hz. A real ultra is ~360k points; that needs the tier-2 external QSPI flash, and the retention/overflow policy for *that* store is still an open architectural choice ([`roadmap.md § Smaller considerations`](roadmap.md#smaller-considerations)). Run-store v2's decimation makes a full slot hold the whole run *coarser* rather than losing its tail — which is the right behaviour and still not capacity.
- **The 256-point course cap.** Any real ultra course must be phone-simplified before the push. The oft-quoted "4 KiB" is the *polyline* (256 × 16 B); since § 405 cached each segment's length and `cos_lat` at build time, `size_of::<Course>()` is **8,784 B**, held by three concurrent owners (the `state::COURSE` watch, `nav`, `ui`) for ~12.5 KiB of the 256 KiB budget, and a compile-time size assert makes the next field a deliberate decision.
- **The 64-lap storage budget, the 96-point trackback breadcrumb, and the 64-sample elevation ring.** All fixed-RAM display structures sized for a bench prototype.

**Sensor tuning constants flagged for on-device calibration:** the GPS-baro complementary filter's slew, the ambient-subtraction and raw-DC-rail thresholds, the LED AGC's step/hysteresis/clamps, and the off-wrist / saturated contact bands. Every one is a conservative starting point chosen without a sensor on a wrist.

**Publish-gate quanta (new 2026-07-25):** `elevation::PUBLISH_STEP_M` = 0.1 m is chosen as an order of magnitude above the BMP581's noise floor at the driver's configured 16× OSR + IIR coeff-7 — a **datasheet-class derivation, never measured on the part**. Set it too fine and a resting wrist wakes the CPU on noise; too coarse and the live GAP estimator sees a stepped altitude. **That second argument is weaker than when it was written** and the figure it rests on has moved: [§ 992](../architecture/decisions.md) raised the estimator's window from 5 m to 20 m, so a metre-coarse altitude now puts ~5 % onto a segment's grade rather than ~20 %. The step stays 0.1 m because the other half of its derivation is untouched — it is the finest quantum anything downstream can represent at all, and still an order of magnitude above the sensor's noise floor. The bench item is: log the raw baro stream over a 60 s motionless hold and record the actual peak-to-peak spread, then confirm the step still clears it. The GSV/GSA bar-count gate needs no calibration (the quantum is the drawn meter itself) but is **untestable under Renode** — every sim NMEA fixture is single-constellation GPS-only, so the multi-constellation repeat the gate exists to absorb has never been exercised.

**The sleep-station reserve fraction (new 2026-07-30, [§ 373](../architecture/decisions.md)):** `sleep_station::SLEEP_RESERVE_FRACTION` = 0.25 of the time still needed to reach the next cut-off is a **judgement call, not a measurement** — it stands in for how much slower a runner is after a nap than before it, and no post-sleep pace-degradation corpus exists here or in anything this project can point at. Its companion `SLEEP_RESERVE_FLOOR_S` is *not* on this list: it is `CUTOFF_TIGHT_S` reused, a product decision the app already publishes, not a physical claim. The bench cannot settle the fraction either — it needs a *field* corpus (paired pre-nap and post-nap leg paces from real multi-day races), which is the one item in this register no rig on a desk can produce. Until then the honest reading of the sleep page is that its number is conservative by a chosen margin, not by a measured one. **And the wake is not a derived number, it is an absent capability:** the tier-1 BOM has no vibration motor and no buzzer, so the page's `WATCH CANNOT WAKE YOU` row is a statement of hardware fact, not a temporary caveat — nothing bench-side can retire it, only tier-2 haptics.

**The storm thresholds (new 2026-07-31, [§ 376](../architecture/decisions.md)):** the trend window (3 h) and the fall that opens a storm (`storm::STORM_FALL_HPA` = 4.0) are **chosen, not measured**. The window is the synoptic pressure-tendency interval every barometric convention is quoted over; the threshold is what the mainstream outdoor watches ship as their storm-alarm default. Nothing in this repository has watched a real front cross a real barometer, and the sim cannot settle it either — its BMP581 model serves a scripted pressure, so a scenario proves the *path* and never the number. So do the derived pieces around them: `STORM_BUCKET_MIN_SAMPLES` = 3 and the ±1.8 hPa-per-fix noise figure it is sized against come from a ±15 m receiver-altitude assumption, which is a datasheet-class estimate of the MAX-M10S and not a measurement of one; `STORM_STEADY_BAND_HPA` = 1.0 is the residual left after two averages, rounded up. This is why a runner-tunable threshold was deliberately **not** exposed — the wire carries one so the capability exists, but tuning a number whose centre is unvalidated is guessing twice. The bench item is below.

**Backyard + nav-gate windows (new 2026-08-18, from the round-2 firmware bug-hunt):** three chosen values, none measured. `backyard::BANK_CONSUME_WINDOW_S` moved 300 s -> 900 s and changed meaning with it — it is now *the shortest time in which a loop could be run*, derived as 6.706 km in 15 min (2:14/km, faster than the 10 km world record), and the old five minutes was sized only as "how long a walk-in takes", which left the ten minutes after it counting a bell-banked loop a second time. `backyard::BELL_ROLLOVER_MIN_DROP_S` = half a bell window is derived from the fold cadence (1 Hz for as long as a run is live, so a real rollover always drops by nearly a whole window); what it is *sized against* is a ~20 ppm crystal-drift assumption, and the bench item above is what settles that. `nav_project::REANCHOR_MIN_REJECTS` = 3 is a judgement call about how long a stream of refused fixes must run before it is a stale anchor rather than a run of bad fixes; its time half is `record::GPS_REANCHOR_AFTER_S`, which is the phone recorder's own value and is not new here. None of the three can be confirmed on this workstation — a backyard bell needs multi-hour wall time the sim's fixtures do not carry, and the re-anchor needs a receiver that has actually been driven somewhere.

**One structural assumption, not a number:** that the HR AFE **retains its register file across `shutdown()`**. The firmware's wake path depends on it (no re-init, no AGC re-walk), the Renode model was written to honour it, and no silicon has confirmed it. [§ 623](../architecture/decisions.md) re-opened rather than inherited it: the assumption was logged against the MAX86177 and the delivered part is a MAX30101, so it is an untested claim about *that* part's silicon. Called out in the step-5 checklist for that reason.

### Added 2026-07-25 (from the defect sweep, decisions § 316 + § 317)

- [ ] **Battery: confirm the VDDH/5 read tracks a real discharge.** **The firmware half of this is done** — [decisions § 691](../architecture/decisions.md) (2026-08-19) selects the SAADC input from the supply mode, so the high-voltage build reads the cell through `VDDHDIV5` at gain 1/2 (6000 mV full scale) instead of reading the internal regulator through `VDD`. What was recorded here as "blank across 4.2→3.6 V" was in fact blank at *every* charge level on that wiring. Host tests pin the arithmetic and the refusals; **what is left is entirely a measurement.** Pass criterion: a freshly charged cell reads near 100 %, the percent falls monotonically across a discharge, and the reading agrees with a multimeter across the cell to within a few percent of state of charge at three or four points down the curve. Watch for a systematic *low* reading, which would point at the channel's acquisition time (left at the driver's default 10 µs) being short for the internal divider's source impedance rather than at the scale. **This item needs the charger on [`parts.md`](parts.md)** — the DK supplies from a Li-Po but does not charge one, so without it the discharge sweep is a one-shot.
- [ ] **Exercise a pipelined BLE chunk pull.** The chunk-request path is now a depth-8 queue rather than a single-value signal, so two requests in flight can no longer silently drop the first. Pass criterion: a phone that issues several `run_chunk` requests without waiting receives an answer to every one, and a deliberate overflow past the queue depth produces the `ble: chunk queue full` warning rather than a silent stall. The queue's own semantics — FIFO order, the depth, an overflow refused rather than swallowed — are **host-tested** since decisions § 320, against the same `embassy_sync` `Channel` the task builds; what the bench still owes is that a real phone over a real radio drives them, which cannot be sim-verified (no SoftDevice under Renode).
- [ ] **Confirm checkpoint ping-pong survives a real brownout.** Checkpoints alternate between two slots so a torn write leaves an intact predecessor. Pass criterion: cutting power mid-checkpoint leaves a recoverable run-so-far on flash at the next boot, repeatedly, and the recovered copy is the newer intact one.
- [ ] **Confirm a failed commit still serves the surviving checkpoint, without a reboot.** The commit now seals before it drops (decisions § 322): the superseded checkpoint keeps its directory entry until the new bytes are down, and a failed erase/write promotes that checkpoint to advertisable instead of losing the run from RAM. Pass criterion: force the commit's flash write to fail on a run that has checkpointed (a write-protected page, or a deliberate error injected at the NVMC seam), and the still-connected phone can pull the run — reading back the checkpoint's blob, verifying, at the size the manifest advertises — with no power cycle in between. Host-tested over a flash model only; whether a real NVMC failure surfaces as an `Err` at all, rather than as a silent partial write, is exactly what the bench settles.
- [ ] **Verify the settings frame's integrity fix on-device.** The `SET1` v3 CRC32 bump has landed (decisions § 319): `WatchSettings::decode` rejects a frame whose checksum does not match, and the property in `core/tests/prop_settings.rs` that recorded the defect is un-ignored and passing on the host. What the host cannot show is the radio path. Pass criterion: a settings push whose bytes are deliberately corrupted in flight is rejected by the watch — no field applied, the previous config intact — while an untouched push still applies, and the fully-populated frame survives a real GATT write without truncation. **The frame is now v8** (decisions § 336 added five fields plus a 64-bit page mask at v4; § 352 the resting HR at v5; § 358 the ICE / medical-ID card at v6, spending `flags2`'s last bit; § 374 the auto-lap trigger at v7 on a third presence byte; § 376 the storm threshold at v8) and a full push is **196 bytes**, not 49 — still one write at the 256-byte ATT MTU the `ble` task configures, so nothing chunks, but the size to confirm on the wire is the v8 one, and it leaves only ~60 bytes of MTU headroom. The phone's encoder emits v8 as of 2026-07-31, so the frame the bench pushes is the frame the goldens pin on both sides. Cannot be sim-verified (no SoftDevice under Renode).
- [ ] **Exercise the WKT1 workout push end-to-end.** The structured-workout runner (decisions § 354) is host-tested and its engine + banners are sim-verified over the sim's demo workout, but the radio leg cannot be (no SoftDevice under Renode). Pass criterion: a chunked workout push over the real `workout` characteristic arms the Workout page and a bench jog walks its steps (auto-advance on both axes, `! REP` / `! STEP END` banners, the lap button skipping the active step); a deliberately corrupted chunk stream is rejected with the previously armed workout intact; and a manual pause verifiably freezes the step clock while a standing rest inside a timed recovery does not.
- [ ] **Confirm run-store v3 rejects a corrupted footer on real flash.** The blob CRC now covers the footer's totals as well as the track (§ 321), so bit-rot in `distance_m` / `moving_s` / `elapsed_s` can no longer sync a run with silently wrong numbers. Pass criterion: a committed blob whose summary bytes are deliberately disturbed on flash fails `recover_slot` at boot and is never advertised, and a phone handed the same bytes counts the pull failed rather than saving a run. Also confirm the compat decision on real hardware: a v1/v2 blob left in a slot from an earlier firmware is rejected by the version gate and its slot is reused, not read as corrupt.

### Added 2026-08-18 (from the round-2 bug-hunt firmware fixes)

- [ ] **Confirm the barometer plausibility gate on real silicon — both directions.** The trigger is bench-only and the sim cannot stand in for it: Renode's BMP581 model serves the value the scenario scripts, so it models *the driver's own belief* and never a bus fault. The fix is therefore **host-tested**, not sim-verified and not bench-verified. Pass criterion, the refusal half: induce a stuck-high read (hold SDA/SCL through a pull-up, or unseat the STEMMA QT connector mid-run) and confirm the baro task logs `implausible altitude ... sample dropped`, that ALT and VERT *freeze* rather than jumping, that the run's total descent does not move, that the stored track's points carry the receiver's altitude rather than the sensor's, and that no storm banner fires. Then the half that matters more, because a gate that refuses real data is worse than the bug: carry the prototype to the highest and lowest ground available and confirm nothing real is ever dropped — the window is -500 m..9,000 m, so the failure it would show is a *missing* ALT row, not a wrong one.
- [ ] **The nav fix gate after driving to a start line.** The measured defect (a course pushed at home, 40 km driven, 0 fixes accepted and the first acceptance 36 minutes after arriving) came from a sim run, and the fix is host-tested. Pass criterion on the bench: push a course at home, drive to a trailhead, and confirm the Nav page is tracking the runner within ~10 s of the last fast fix rather than tens of minutes later, that the off-course latch is live before the first step, and that the map marker and the gate agree about where the runner is. Then the guard the escape must not have weakened: under real canyon or dense-canopy multipath, confirm a single bounced fix still never fires `! OFF CRS`.

## What tier 1 does not have to hit

This section exists so the checklist above cannot be misread as scope creep past [§ 71](../architecture/decisions.md#71-own-hardware-an-ultra-marathon-watch-stays-research-only-watch-development-is-deferred-indefinitely)'s owner-personal boundary, or as importing tier-2's targets.

**Stated plainly: [§ 82](../architecture/decisions.md#82-tier-1-firmware-is-done-when-one-outdoor-run-syncs-end-to-end-to-supabase-from-the-bench-prototype)'s Definition of Done remains the Definition of Done. This checklist is how bench verification gets conducted and claimed — it is not a new gate on tier-1 completion.** One outdoor run that produces a GPS+HR-tagged track and syncs end-to-end completes tier 1, even with checklist items open. Open items become recorded findings and tier-2 inputs.

Tier 1 explicitly does **not** have to hit:

- **Any power or battery target.** § 83 is explicit: the PPK2 numbers inform tier-2 planning and do not gate tier-1 completion. § 82's "what this does not require" said the same. The ~110 h headline is a tier-2/3 *product* target from [`vision.md`](vision.md) req #1; the ≥ 8 h figure in the checklist is a DK-and-breakouts sanity check from `prototyping.md`, not a watch claim.
- **Tier 2's ≥ 24 hr GPS battery life** or **100 % outdoor fix reliability**. Those are the tier-2 wearable-prototype bars ([`prototyping.md`](prototyping.md#tier-2--wearable-prototype-15k40k-diy-80k250k-consultant-built-918-months), [`roadmap.md § Tier 2`](roadmap.md#tier-2--wearable-prototype-gated)) and tier 2 is gated on § 71's three triggers. Importing either into tier 1 would be exactly the goal-post drift § 82 was written to prevent.
- **Dual-band L1+L5 GNSS or foliage/canyon accuracy.** Tier 1 is single-band MAX-M10S by design; dual band is the Sony CXD5610 / u-blox X20P migration ([`vision.md`](vision.md) req #2).
- **In-case RF performance, an antenna design, or a chamber measurement.** Tier-2+ ([`prototyping.md`](prototyping.md#why-rf-is-the-line-item-that-surprises-everyone)).
- **IPX7, a sealed enclosure, case CAD, drop/vibe/thermal, or FCC/CE.** Tier-2/3 line items; the printed chassis is a board-holder, not a case.
- **Field validation under stress** — foliage, urban canyon, a multi-hour ultra, a 100 km event. § 82 puts all of that at tier 2+.
- **More than one qualifying run.** § 82 pins the bar at one deliberately; a second run is "reassuring, not decision-changing". The checklist's soak and boundary items are *characterisation*, not additional DoD runs.
- **A finished UI, data-screen customisation, OTA, vector maps, or ANT+.** Strict tier-1 scope is GPS + HR + display + sync (§ 82); § 84 and § 85 own OTA and maps at tier 2.
- **Kill criteria.** § 82 declined to set them and this doc does not add any. An owner-personal investigation ends when the owner stops investigating; a red checklist item is data, not a termination trigger.

## Pinning

- Decision recording this doc: [decisions.md § 314](../architecture/decisions.md).
- Definition of Done: [§ 82](../architecture/decisions.md#82-tier-1-firmware-is-done-when-one-outdoor-run-syncs-end-to-end-to-supabase-from-the-bench-prototype) (unchanged).
- Power methodology: [§ 83](../architecture/decisions.md#83-tier-1-power-measurement-uses-nordic-power-profiler-kit-ii-applied-per-subsystem).
- BLE verification ceiling: [§ 210](../architecture/decisions.md#210-tier-1-ble-s140-softdevice-is-a-compile-verified-feature-gated-build--mutually-exclusive-with-the-sim-off-by-default).
- Evidence destinations: [`tier1_log.md`](tier1_log.md) (bench), `apps/custom_watch/sim/verification-<date>/` (sim), [`parts.md`](parts.md) (receipt).
