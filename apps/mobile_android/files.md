# mobile_android — file inventory

What every file under `lib/` is for. Split out of this app's `CLAUDE.md`, which
is loaded in full whenever a session touches a file in this app: the inventory
had reached 153 KB, roughly 38,000 tokens, to answer a question a session only
asks about the handful of files it is about to open.

`CLAUDE.md` keeps the scope rules, the stack, the i18n contract, the analyzer
policy and the gotchas. This file is the reference behind them — grep it for a
screen or a store before adding one, so you extend the existing file rather than
minting a second one beside it.

Paths are relative to `apps/mobile_android/`. Per
[decisions.md § 39](../../docs/architecture/decisions.md), `lib/` and `test/`
are byte-identical to `apps/mobile_ios/`, so every entry describes both apps.

**Screens** (`lib/screens/`):
- `onboarding_screen.dart` — first-launch permission ask (local `Preferences.onboarded` flag, pre-sign-in)
- `setup_wizard_screen.dart` — post-signup account-setup wizard, mobile twin of web's 7-step `/onboarding`. Gated on `user_profiles.onboarded_at`: `home_screen.dart` fetches the profile after the first frame and pushes this once when the column is null, then `Finish` / header-`Skip setup` stamps it via `ApiClient.completeOnboarding` / `markOnboarded`. Collects display name → units → primary goal (`primary_goal` bag) → demographics (gender + DOB + weight) under the Art 9 health-consent gate (mirrors `settings_body_metrics_screen` — gender + consent stamp only under the checkbox; DOB written unconditionally for the minor-exclusion floor) → privacy default → notifications (`push_notifications` bag) → done. Pure contract in `onboarding.dart` (TS↔Dart pair with web `onboarding.ts`). **Distinct from `onboarding_screen.dart`** (first-launch permission flow). 10 unit/widget tests (`onboarding_test.dart` + `setup_wizard_screen_test.dart`).
- `sign_in_screen.dart` — email/password + Google sign-in
- `sign_up_screen.dart` — email/password registration + Google sign-in, linked from sign-in. Asks for the password **twice** (`auth_gates.dart#checkPasswordPair`, checked before `signUp` — a typo in a single field is silently baked into the account and locks its owner out permanently); the GDPR Art 8 age + ToS gates are inline here as `_checkGates`
- `home_screen.dart` — the multi-modal nav shell (G5, Fitness-hub redesign): a `BottomAppBar` (Home / Fitness / Social / You) + a docked centre Log FAB, over a `PageView` whose pages are `[Home(0), Fitness(1=FitnessHubScreen), Run(2=_pageRun), Gym(3=_pageGym), Nutrition(4=_pageFood), Social(5), You(6=_pageYou=YouScreen)]`. Run / Gym / Nutrition have no nav destination but stay keep-alive dwell-in capture pages reached via the Log action so a session survives. Log FAB: tap → `showLogSpeedDial` fans the capture options above the button and navigates to the picked modality's page (or one-tap run start when `keepRunPrimary`), long-press → repeat last (or the speed-dial). At expanded widths (≥840dp, `widthClassOf`) the same destinations + Log action render as a `NavigationRail` (Log in the leading slot, speed-dial fanning from the button's anchor) instead of the BottomAppBar + docked FAB — chrome swap only, same pages (decisions §256). Hosts the dashboard on page 0. **Swipe-lock during recording (#490):** the `PageView` physics flips to `NeverScrollableScrollPhysics` while a run is actively recording, driven by the process-global `runRecordingActive` `ValueNotifier<bool>` that `run_screen.dart` sets from its `_state` (recording — which includes a mid-session pause — locks; idle/countdown/finished swipeable). Only the swipe drag is blocked; nav-bar/rail taps still navigate (they `jumpToPage` directly). Fail-open: the notifier defaults false and resets false on `RunScreen.dispose`.
- `fitness_hub_screen.dart` — the Fitness modality hub (G5): a `TabController(length: 4)` + top `AppTabBar` strip (History / Runs / Gym / Nutrition) over a `TabBarView`. History = `RunsScreen` WITH gym+food stores + `showKindChips: false` (the absorbed History timeline); Runs = `RunsScreen` WITHOUT a gym store + `surfacePeers` + `showSyncActions: false` + `titleText: "Runs"` (the dedicated run list, statically titled like its Gym/Nutrition siblings — the range + count status renders in the filter header instead; the cloud sync/refresh slot lives only on the History tab — side-by-side duplication overflowed the Runs AppBar title); Gym = `GymScreen`; Nutrition = `NutritionScreen`. Each sub-tab body owns its own Scaffold/AppBar/composer; the hub provides only the tab-strip chrome (mirrors `social_screen.dart`). Both take ui_kit's `AppTabBar`, which derives `isScrollable` from whether the labels fit — the two hubs used to disagree on it and on `TabAlignment`, so the strip jumped 26dp taller and slid 52dp right when you switched destinations (#666 C6). FitnessHubScreen takes a required `TrainingService` (`training`, threaded from `home_screen`) so it can build `PlansScreen`, and owns a `RaceService` for the Races peer (`RacesScreen` is reachable with no provider key and while signed out — the search is a public RPC; the MapTiler key for "near a place" is read defensively from dotenv). "Start with this route" lands on the keep-alive recorder page through the global `pendingStartRunWithRoute` notifier, which every route surface sets directly — there is no `onStartRun` callback threaded down the push path. DISTINCT from the keep-alive capture pages.
- `you_screen.dart` — the You tab (G5): a thin host that renders a profile-header tile (own `ProfileScreen`) above the unchanged `SettingsScreen` body. Settings folded in here from its old bottom-nav slot; `SettingsScreen`'s constructor is unchanged.
- `run_screen.dart` — the primary recording screen (countdown, live stats, route overlay, finish summary); hosts the recorder state machine and all the hardening described in [../../docs/features/run_recording.md](../../docs/features/run_recording.md)
- `dashboard_screen.dart` — weekly mileage, PBs, goal progress; AppBar actions push `coach_screen.dart` (AI Coach), `feed_screen.dart` (activity feed) and `profile_screen.dart` (own profile)
- `coach_screen.dart` — AI Coach chat. Mirrors web `CoachChat.svelte` + `/coach/+page.svelte`. Plan switcher in the AppBar (when >1 plan), end-Drawer archive sidebar, context strip (plan / runs window 10|20|50|100 / HR pill / weekly-goal pill), markdown bubbles via `flutter_markdown`, inline edit / regenerate / copy / thumbs actions, multi-line composer with circular send button, daily-cap banner driven by `get_coach_usage` + `is_pro`. Streams from the web `/api/coach` endpoint via `dart:io` HttpClient + UTF-8 SSE parser; Realtime subscription on `coach_messages` stitches in messages persisted by other tabs.
- `coaching_screen.dart` — human coach-athlete roster (mirror web `/coaching`, persona #46). Mint/copy/share + revoke invite links, athlete list (tap → `coaching_athlete_screen`), my-coaches list with leave; end/revoke gated behind an `AlertDialog`. Reached from the Settings → Coaching tile (`settings_screen.dart`, Profile section, after sign-in). **Distinct from `coach_screen.dart`** (the AI chat) — this is the human coach relationship surface. Reads/writes through the `api_client` coaching methods (`createCoachInvite` / `fetchMyAthletes` / `fetchMyCoaches` / `fetchPendingCoachInvites` / `revokeCoachInvite` / `endCoachLink`). The public-link host comes from `WEB_BASE_URL` (pure `coachInviteLink`, falls back to `https://threkir.com`).
- `coaching_athlete_screen.dart` — coach run-review surface (mirror web `/coaching/athletes/[id]`). A linked athlete's recent runs (public + private, column-narrowed — no track) + active-plan compliance window + the assign-a-plan control (when the athlete has no active plan and the coach has plans). The `assign_plan_to_athlete` RPC RAISE (e.g. "athlete already has an active plan") is surfaced via `showTopBanner`, not swallowed. `fetchAthleteRuns` / `fetchAthletePlanOverview` / `fetchMyPlans` / `assignPlanToAthlete`.
- `feed_screen.dart` — activity feed of public runs from people you follow (14-day window, mirrors web `/feed`); activity-segmented filter + author dropdown + infinite-scroll. Tapping a card body pushes `public_run_screen.dart`
- `profile_screen.dart` — public user profile (`/u/[id]` mirror): header, Follow toggle, and tabs for Runs / Achievements / Followers / Following / Notifications (notifications tab gated to self). Since §545 the header is a **scroll-away hero over a pinned tab strip** via `widgets/collapsing_tab_host.dart` — the strip left `AppBar.bottom`, so the host renders in **every** state (loading / error / not-found swap the header band and the current tab's body) or a slow fetch would withdraw the strip a user reached for.
- `public_run_screen.dart` — read-only mirror of web `/share/run/[id]`. Reads via `ApiClient.fetchRunById` (RLS hides private), reuses `LiveRunMap` + segment chips + social section. Owner is detected by `api.userId == row.userId` and reads the blob directly via `fetchTrackByPath`; every non-owner (anon included) reads through `fetchClippedTrackForRun`, so the unclipped bytes never cross the wire and the clip never runs client-side (decisions §33). Reachable from feed-card body taps
- `public_route_screen.dart` — mirror of web `/share/route/[id]` (the screen a follower lands on from a shared link, and the club/heatmap route viewer). Reads via `ApiClient.fetchRouteById` (returns `({Route? route, String? ownerId})` so the screen can clip for non-owner viewers); renders the planned-route polyline + stats + segments panel. Same owner gate as `public_run_screen` (decisions §33). **No longer view-only (decisions §298):** a **Start run** FAB lets a non-owner run the route (carrying the CLIPPED waypoints via the unit-tested pure `publicRouteStartTarget`, never the owner's full trace) through the global `pendingStartRunWithRoute` handoff; a **Save to my routes** (bookmark) AppBar action mirrors route-detail's non-owner bookmark. Reachable from the Routes tab on `club_detail_screen`
- `live_spectator_screen.dart` — read-only mirror of web `/live/[run_id]`. On hydrate it reads the run's terminal state via `fetchPublicRunById` (mirror of web's `runIsFinished` + the `is_dnf` column, migration `20261207_001`): a **DNF** (race-marked) or **Finished** (`runIsFinished` — saved duration ends >2 min ago) run freezes on its saved totals and never opens the realtime subscription. Otherwise it hydrates existing `live_run_pings`, subscribes via `Supabase.channel.onPostgresChanges(insert)` filtered by `run_id`, and renders the trace on `LiveRunMap` (follow-runner). The status badge has five distinct states — **DNF** (danger) / **Finished** (neutral, terminal) / **Delayed** (amber — a *live* runner whose last ping went stale, NOT terminal) / **Live** (green) / Idle / Connecting; the terminal verdict outranks the live/stale freshness axis. Since issue #613 it also reads `concluded_at` from `public_runs` as the positive finish marker (winning over the duration inference on load, and a ~15 s poll flips a mid-watch run when the marker appears); a concluded run renders a **conclusion card** ("Run complete" + a view-the-full-run CTA → `PublicRunScreen`), and while live the page adds a recent-pace tile + a course-progress bar (when the run follows a known route). `runIsFinished` is `@visibleForTesting`
- `devices_screen.dart` — Settings → **Signed-in devices**: lists `user_device_settings` rows with Rename / Edit overrides… / Remove popup actions. Mirrors web `/settings/devices`. Named "Devices" until issue #666 I10 — the screen is sessions plus their per-device overrides, while the actual hardware pairing (heart-rate strap, treadmill, watch) lives under Integrations, so the old name sent a user hunting for a strap to the wrong tile. Renamed on web first per § 24.
- `privacy_zones_screen.dart` — Settings → **Preferences → Privacy & sharing → Privacy zones** (moved off the account screen in issue #666 I11, matching web, where the picker now lives on `/settings/privacy`): tap-to-add geofence picker on `flutter_map`. Persists to `user_settings.prefs.privacy_zones` via `SettingsService.updateUniversal`. Mirrors web `PrivacyZonePicker`
- `runs_screen.dart` — dual-purpose: the Fitness hub's **All** sub-tab AND (mounted without a `gymStore`) the dedicated **run-list** screen (the hub's **Runs** sub-tab + the "View all" / period drill-ins). **History-tab / timeline mode** (gym+food stores wired): renders the day-grouped cross-modality timeline (`widgets/activity_timeline_list.dart`). It still owns the All/Runs/Lifts/Meals kind chips when mounted standalone, but the Fitness hub passes `showKindChips: false` so the hub's TabBar is the single kind selector (no duplicate strip) — the timeline then stays pinned to All. The `surfacePeers` list (set by the hub's Runs tab) renders the labelled `Runs · Routes · Plans · Races` strip above the body — planning tools are named destinations, not tooltip-only AppBar glyphs (decisions §488). Shows All/Runs/Lifts/Meals kind chips once a second modality has data and — mirroring web — **every chip (including Runs) renders the day-grouped cross-modality timeline** (`widgets/activity_timeline_list.dart`). The timeline is assembled from the **local stores** by `lib/local_activities.dart` (`buildLocalActivities` over `LocalRunStore` + `LocalGymStore` + `LocalFoodStore`) — offline-first, always all modalities, rebuilt on any store change (so a lift/meal logged from the Log FAB shows at once). History **hydrates the gym + food stores itself on mount** (`_hydrateModalities`, best-effort, mirroring dashboard) — it's a first-class store consumer now, so it can't assume it was reached via dashboard/gym; skipping this was the "no Lifts tab when History is the entry point" bug. Each single-modality tab shows a `View all` link (`historyViewAll`) that pushes that modality's full surface — Runs → `RunsScreen` **without** a `gymStore` (the run list below), Lifts → `GymScreen`, Meals → `NutritionScreen` (needs `foodStore`, plumbed from `home_screen`). Run rows open run-detail (local lookup, else `fetchRunById`), lift rows open `GymDetailScreen`, meals are read-only. The **add FAB is per-chip** (mirroring web's `/history` Log action): on the cross-modal **All** chip it opens the run/lift/meal `showLogSheet` picker → that modality's one-shot composer; a single-modality chip adds straight in (Add run → `AddRunScreen`, Log lift → `showGymComposeSheet`, Log food → `showNutritionLogSheet`); a lift/meal save kicks a best-effort store sync. **Run-list mode** (no `gymStore`, or a pure runner with no lift/meal): the full run list with sorting / source / date-range filters, bulk-delete, pagination, FAB → `add_run_screen`. The inline run list is NOT an offline fallback (the timeline is local-first and renders offline) — it's just what a single-modality (run-only) user sees. See [decisions.md §63 amendment](../../docs/architecture/decisions.md#63-single-app-multi-modal-expansion-run--gym--nutrition-under-one-nav-one-db).
- `add_run_screen.dart` — manual-entry form: date/time + duration + distance + optional saved route. Pushed as a full-screen dialog (`fullscreenDialog: true`) to match the other create/edit-entity surfaces; keeps its own `Scaffold` (Form + validation + nested route picker) rather than the `FullScreenFormBody` chrome.
- `run_detail_screen.dart` — single run map + stats (primary + secondary, both `StatTile` cells laid out by the same ui_kit `StatGrid`: a reflowing grid whose column count comes from the width a cell needs at the current text scale, so eight secondary stats become two rows of four rather than eight 40dp ellipsis stubs) + interactive elevation/pace chart + best efforts + pace-bar splits + laps. **Map-match offline fallback:** the matched-track read (`_maybeFetchMatchedTrack`) classifies an unreachable-backend error (`isMatchUnreachableError`) distinctly from a server verdict — on offline it keeps the raw track rendering (L1) and shows the "Offline — will retry" pill (`MatchPillKind.offline`) rather than a hard error; subscribes to connectivity and re-fetches on reconnect, bounded by `shouldRetryMatchFetch` so a settled (`failed`/`skipped`/clean-`matched`) run never re-hits the backend. The pill state + retry gating are the pure `core_models/run_match_info.dart` selectors.
- `import_screen.dart` — bulk import: Strava export ZIP / Health Connect / CSV / backup ZIP (the GPX / KML / KMZ / GeoJSON / TCX single-file picker is `shared_file_import.dart` + `routes_screen`)
- `routes_screen.dart` — saved routes list with per-row star toggle (owner-only) that drives the watch's starred-only route picker (see `docs/architecture/decisions.md § 44`). Pushed standalone from the Fitness hub's Runs sub-tab (AppBar titled "Routes"); the `embedded: true` body-only path remains for a hosting parent. Both mounts share the labelled "Discover" strip at the top of the body — Public routes / Routes heatmap (community, `routes_heatmap_screen.dart`) / Run heatmap (`run_heatmap_screen.dart`); both heatmaps api-gated plus the compact sync affordance — the single home of every map/discovery entry point. The dual "Build" + "Import" FAB column is exposed via `RoutesScreenState.buildRouteFabs` so an embedded host can hoist it on its own Scaffold. **Bulk delete** mirrors `runs_screen`: long-press an owned route to enter selection, tap rows to add/remove, then delete from a top-of-body banner (a banner instead of an AppBar replacement so the embedded-under-SocialScreen path doesn't need to plumb selection state up). Bookmarks aren't deletable since they're someone else's data — only OWNED rows enter selection. Calls `ApiClient.deleteRoute` per id with per-row failure isolation; successful ids drop locally via `LocalRouteStore.deleteMany` (batched notify so the list doesn't flicker).
- `routes_heatmap_screen.dart` — route-discovery browser; mobile mirror of web `/routes/heatmap` (decisions §§ 99–102, layout § 112). Heat-density background + clustered discoverable pins (pure-Dart `heatmap_clustering.dart`), a Filters bottom sheet (lens `popular`/`friends`/`featured`/`hidden_gems` + multi-select race-distance bands via `distance_bands.dart`). The map is full-bleed: a floating "N routes" pill opens the results list as a dismissible modal sheet, and tap-to-preview (tap a pin or row → that route's line + halo via `fetchRouteById`) shows a compact dismissible selection card → View. **Mobile-only layout** — web keeps its desktop sidebar (§ 112); don't port the pill/modal/card to web. Reached from the labelled "Routes heatmap" button in the Routes screen's Discover strip (`routes_screen.dart`).
- `run_heatmap_screen.dart` — personal run-track heatmap; mobile mirror of web `/runs/heatmap`. A Strava-style "everywhere you've run" map of the user's OWN tracks (distinct from `routes_heatmap_screen.dart`, the public route-discovery heatmap). Sources tracks from the **union of the local store and the server**: `LocalRunStore.runs` carry their track inline, so they need no download and no session (signed-out recording is a supported flow — reading only the server told a signed-out runner with local tracks "No mapped runs yet", issue #239); the server leg then downloads only the runs that aren't already local, via the owner-only `fetchTrackByPath` (newest-first `getRuns`, `_maxTracks` cap + bounded concurrency), and is skipped entirely when signed out. Aggregates the union with the pure `run_heatmap.dart`, and renders heat cells as low-opacity `CircleLayer` dots that crossfade into the actual track `PolylineLayer` as you zoom past street level (flutter_map has no GPU heatmap layer). Owner-only path → no privacy-zone clip. Signed out with nothing local, the empty state offers sign-in rather than claiming the runner has never run. Reached from the labelled "Run heatmap" button in the Routes screen's Discover strip (`routes_screen.dart`) — every map/discovery entry point is consolidated there; icon-only AppBar entries proved undiscoverable (field reports for both heatmaps).
- `route_detail_screen.dart` — route map + metadata + reviews + public/private toggle + star-for-watch toggle (owner-only). The toolbar is built by `widgets/app_bar_actions.dart` and holds three slots: the share menu (pinned), one promoted action (bookmark for a non-owner, offline pin for an owner), and an overflow carrying the rest — star, visibility, transfer-to-club and delete for an owner; offline pin and report for a non-owner. Six concurrent owner actions measured 0dp of title on a 360dp phone. Share menu offers **Share link** (the intuitive send-to-a-follower path — flips the route public if the owner hasn't, then hands the `/share/route/[id]` URL to the OS share sheet via `routeShareUrl` + `shareTextFrom`; gated to public/owner so no dead link) alongside the image / GPX / GPX+markers / KML file exports and — behind the loopback-backend dev rail the Sim Watch link uses (`devBackendUrl`, decisions §209) — a **Send to watch** entry that pushes the route to the custom watch as a `CRS1` breadcrumb course (`courseFromWaypoints` + `encodeCourse` + `chunkCourse` over `WatchSyncClient.pushCourse`, decisions §370) **and, when the route carries course markers, the `RBK1` race schedule those markers imply** (`buildRoadbook` → `watchRoadbookFromRoadbook` → `encodeRoadbook` + `chunkRoadbook` over `WatchSyncClient.pushRoadbook`, decisions §413). The two ride one action because every checkpoint is a distance *along the course* — a schedule alone would arm Roadbook / CutoffEta / Fuel pages that can never resolve a position. That push reads `_displayWaypoints` (privacy-clipped for non-owners, §33) like the GPX exporter does, thins an over-long route to the 256-point frame budget rather than cutting it, and refuses what it cannot represent with a banner — never a partial course. The schedule half is held to the same bar: an over-cap one is reduced deliberately (cut-offs never dropped, farthest-point fill), a route with more cut-offs than the frame holds is refused with the count and the cap, a clock-only cut-off that could not resolve is **counted and reported** rather than silently missing, and a marker-fetch failure degrades to a course-only push (the course already landed). The goal time is `kRoadbookDefaultSecPerKm` from `roadbook_screen.dart` — the crew sheet's own opening default, shared so the arrivals on the wrist and on the sheet cannot drift. The **Start run** FAB hands off through the global `pendingStartRunWithRoute` notifier (decisions §298) rather than popping the route back to the caller — so it works from any pusher (incl. the shared-file import landing).
- `route_builder_screen.dart` — full-screen in-app route builder (Phase 3, web-parity). Tap-to-place waypoints with OSRM road / trail / straight modes; long-press a pin → tap-to-move drag-reshape; snap-to-start (pulsing halo on the start pin when 3+ waypoints) closes the loop with a tap near the start; MapTiler place search in the AppBar; "Locate me" FAB; live elevation gain via Open-Meteo; purple over-stroke on out-and-back retraced sections (`route_overlap.dart`); undo / clear / save (name + public toggle) → `ApiClient.saveRoute` + `LocalRouteStore.save`. Reached from the second "Build" FAB on `routes_screen`. Pure helpers live in `routing.dart`, `elevation.dart`, `geocoding.dart`, `route_overlap.dart`, `widgets/snap_to_start.dart`. **OSRM degrade (`routing.dart#isOsrmSnapAvailable`):** on a release build with no `OSRM_URL`, the prod privacy guard refuses the public demo — so Trail/Road placement would `throw` out of `snapToRoad` and silently drop the tap ("route builder won't let me tap waypoints"). The screen checks `_snapAvailable` up front and, when snapping is unavailable, treats Trail/Road as straight-line placement (`_effectiveStraight`) so a tap always drops a pin, disclosing it once via `routeBuilderSnapUnavailable`. Set `MOBILE_OSRM_URL` (deployment.md) to make snapping actually work.
- `period_summary_screen.dart` — browsable weekly/monthly/all-time summary with stats, run list, and share (text or screenshot). The dashboard's Week / Month / All-time stat cards and a tap on any "Last 20 Weeks" heatmap column open it (`PeriodType.all` = lifetime, nav arrows disabled). Mirrors web's `PeriodSummary`.
- `recap_screen.dart` — the "Wrapped" recap, reached from the dashboard toolbar. **One screen covers both web routes** (`/recap/[year]` + `/recap/[year]/[month]`): a `RecapPeriod` Year / Month `SegmentedButton` picks which engine builds the card (`buildYearInRunningRecap` / `buildMonthInRunningRecap` from `recap.dart`, the TS↔Dart parity pair) and the chevrons under it step by year or by calendar month — `DateTime(_year, _month ± 1)` normalises the year rollover, and the next chevron disables on the current period so the card can never show a window that hasn't happened. Reads `LocalRunStore.summaryRuns` (the track-less full-history index) and subscribes to the store + preferences, so a run syncing in while the screen is open refreshes it. Share swaps its headline + subject for the month label; **Publish & share link** (`api != null`) sends `period_kind` / `period_key` = `'year'`/`'2026'` or `'month'`/`'2026-03'`, the same wire pair the web routes upsert on, so a month published from the phone refreshes the one share link rather than minting a second. Distinct from `period_summary_screen.dart`, which is a plain per-period stats + run-list drill-in.
- `explore_routes_screen.dart` — search and browse public routes with filters (distance, surface), save to library
- `settings_screen.dart` — Settings landing. Mirrors the web `/settings` left-nav shape (`apps/web/src/routes/settings/+layout.svelte`) with three sections — **Profile** (Account, Preferences, Safety, Coaching, Guided runs), **Apps & data** (Integrations, Signed-in devices, Gear), **Account & legal** (Pro & support, About & updates) — each tile drilling into a dedicated sub-screen. Guided runs sits beside Coaching here rather than three taps down inside Account (issue #666 I7): it is coaching content, and because the library is local TTS scripts with no API dependency the tile must stay reachable signed-out, which rules out the sign-in-gated Coach screen as its only home. A tile whose destination cannot be built on this mount renders **disabled with the reason in its subtitle** (the Gear tile when no gear store is wired) rather than as a tappable row that silently does nothing (I16). The landing is a thin router; all tile content lives on its sub-screen. Keeps the public `SettingsScreen` constructor unchanged — its mount site moved from `home_screen.dart`'s old Settings nav slot into `you_screen.dart` (the You tab hosts it below a profile header).
- `settings_account_screen.dart` — email + sign-in/out, Profile photo (avatar upload/remove via `ApiClient.uploadAvatar`/`removeAvatar` → public `avatars` bucket, EXIF-stripped client-side; mirrors web `/settings/account`), Display name, Sentry opt-out, the AI-disclosure consent pair (a review/accept tile whenever the versioned record is not current — missing **or** stale — plus the withdraw tile whenever anything is on record; both over `ai_disclosure.dart` + the shared `widgets/ai_disclosure_notice.dart`, decisions § 573), Import / **Account export** / Full backup / CSV export / Restore-from-backup, Change email, Change password, Delete account — fourteen rows. The two archive rows are deliberately separate ([decisions § 724](../../docs/architecture/decisions.md)): **Account export** enqueues the server's Art 20 build (`POST /v1/export/jobs`), watches it with the shared backoff, and resumes from `GET /v1/export/jobs/latest` on mount — so an app the OS killed mid-build finds it waiting, with nothing persisted on the device — while **Full backup** is the on-device writer that carries runs / routes / profile / prefs / gym / food but NOT the account-record set, and says so in a persistent notice once it has produced one. `BackupService` no longer reaches for the server at all: its old silent fall-back turned a refused Art 20 request into a narrower file nobody was told was narrower. Guided runs and Privacy zones moved out (issue #666 I7 / I11) and so did a third "View profile" link to the viewer's own `ProfileScreen`, which the You tab already carries directly above this screen's host (I12); the two that remain — the You tile and the dashboard toolbar — now say "Your profile" in both places.
- `settings_preferences_screen.dart` — **the one Settings surface that is a scroll rather than a router**: 47 rows in eight groups (46 without a settings-sync wired, which gates the Privacy zones tile), each group a `SliverMainAxisGroup` whose `SliverPersistentHeader(pinned: true)` keeps its `SectionHeader` eyebrow on screen (a bare pinned header pins against the viewport and eight of them pile up — decisions §538). The header extent is derived from the current text scale, never a literal. Carries units, pace format, map style, dark mode, **Show calorie estimates** (`show_calories`, universal bag, default on — gates the run-detail calorie tile, mirrors web `/runs/[id]`), default activity, audio cues, split interval, live pace alert, keep screen on (subtitle discloses its battery cost — issue #271), **Dim screen while recording** (`dimScreenWhileRecording`, device pref, default off — darkens the run-screen map while recording to save battery; disabled while keep-screen-on is off), advanced GPS, **Show raw GPS track** (`showRawTrack` — forces the raw recorded line on run-detail even when a map-matched track exists; verification aid, off by default), **Run as primary action** (`keepRunPrimary`, G5), a **Body metrics** tile → `settings_body_metrics_screen.dart`, DOB, resting / max HR, HR zones, weekly goal, week start, default privacy, Strava auto-share, **Privacy zones** (→ `privacy_zones_screen.dart`, in the Privacy & sharing section, mirroring web — it used to sit under Account, #666 I11), coach personality, email notifications, push notifications. Takes `apiClient` (for the body-metrics drill-in). All bag-backed prefs route through `SettingsSyncService.updateUniversal`.
- `settings_body_metrics_screen.dart` — Settings → Body metrics (G5, multi_modal.md § "Body metrics & sensitive data"). An explicit Art 9 health-data consent toggle gates height + weight (height → `user_profiles.height_cm` via `api.setMyHeightCm`; weight → `body_metrics` via `api.recordBodyWeightKg`, appended only on change; withdrawal calls `withdrawHealthDataConsent` + `clearBodyWeightHistory`). Activity-level + goal are ordinary nutrition prefs that auto-save to the universal bag. Feeds `loadNutritionTargets`. Toasts via `showTopBanner` (not ScaffoldMessenger — arch guard).
- `nearby_area_screen.dart` — the coarse-area setter behind opt-in "runners nearby" discovery (issue #466, decisions §270 + §701). Reached from the **Your area** row in the Privacy group of `settings_preferences_screen.dart`, itself rendered only while the default-off `ENABLE_NEARBY_RUNNERS` gate is on. A place-search picker (`searchPlaces`, so an unavailable provider reads differently from a place that does not exist) writes the chosen centroid through `ApiClient.setDiscoverableArea`, which rounds it to ~1 km server-side; never live GPS, and no surface ever reads the coordinate back — `my_discoverable_area` returns only the label. "Forget my area" goes through `confirmDestructive` and the affordance is absent when no area is set. Injectable `geocodingFetcher` test seam.
- `nutrition_targets_screen.dart` — Nutrition → Targets, the mobile mirror of web `/nutrition/targets` (decisions § 695). The **derivation** of the day's calorie goal (resting metabolism → activity factor → goal delta → base → today's workout add-on) then the macro split, composed only from what `nutrition_targets.dart` already exports — no arithmetic of its own, and a source guard fails the build if one of the engine's constants reappears here as a literal. Carries the two **non-sensitive** levers (activity level, weight goal) writing the same universal-bag keys `settings_body_metrics_screen` writes, over the shared `activityLevelLabel` / `weightGoalLabel`. Height / weight / DOB / sex are **read-only** here and keep their single Art 9 consent-gated entry point in `settings_body_metrics_screen.dart`. Reached from an **ungated** entry on `nutrition_screen.dart`'s rings card — it renders whether or not targets resolve, because the runner with none is the one who needs the derivation.
- `settings_integrations_screen.dart` — Strava connect/sync/disconnect (`flutter_web_auth_2` + Edge Function), parkrun athlete-number import, one race-provider tile per `raceImportProviders` entry (RunSignUp / UltraSignup / ChronoTrack — generated from the catalogue, each probed separately so an unconfigured leg shows its own explainer and never a peer's; every tile stays tappable because it is a deep link into the public calendar), BLE heart-rate strap pairing (`HeartRateMonitorTile` + scan sheet, both moved off the old monolithic screen).
- `settings_pro_screen.dart` — Subscribe to Pro, Restore purchases, Manage subscription, Support the app. Apple Guideline 3.1.1 + Play subscription-policy disclosures (price + auto-renewal + management URL) are pinned by the `Restore purchases (Apple/Play subscription policy)` group in `architecture_guards_test.dart` against this file. The Subscribe tile is gated on `pro_sellable.dart`'s `ProPerks.sellable` — mirroring web's `proSellable` branch, it renders the `proComingSoonTitle` / `proComingSoon` teaser instead when the deploy has no live Pro perk, and unknown counts as not sellable (decisions §466). `loadPerks` is a widget-test injection seam for both states.
- `settings_about_screen.dart` — **About & updates** (renamed from `settings_licenses_screen.dart`, issue #666 I1: the app's only version display and only update path were hidden behind a tile named "Licenses"). Ordered version → **Check for updates** → Open-source licenses (`showLicensePage`) → the four legal documents. Real installed version via `package_info_plus`; a **Play In-App Updates** check (`in_app_update`) offering an "Update available → Update" affordance (immediate flow) for the internal/closed/open testing tracks. Guarded by `Platform.isAndroid` and fail-closed via injectable `checkUpdate`/`performUpdate` seams — a non-Play build (dev / sideload / iOS) reports `unavailable` and simply hides the update row (iOS still shows the real version). The check runs **on demand**, never from `initState` — it used to fire on every visit to what was nominally a legal page.
- `legal_links.dart` — `LegalDoc` enum (privacy, terms, cookie notice, health-data notice) + `legalDocLabel` + `openLegalDoc`. One definition of the four document URLs, consumed by both `settings_about_screen.dart` (signed-in access, issue #666 I4) and `sign_up_screen.dart`'s GDPR Art 7(2) consent links, which previously carried their own inline `https://threkir.com/$path` builder.
- `sim_watch_screen.dart` — dev-only Settings → Developer → Sim watch link (visible only against a loopback backend, the `isLocalSupabaseUrl` rail). Connects to the custom-watch simulator's phone link (`bin/watch-sim.sh`, TCP; Android emulator default `10.0.2.2:7788`) and renders live status frames (uptime / position / speed / sats / altitude / fix age, plus the barometric elevation block — baro altitude / ascent / descent — when the watch's `elev` object is present). Decoder + client in `lib/sim_watch_link.dart` (frame schema v1 from `watch_core::link`; `elev` is an additive optional object tolerated as absent; the future BLE GATT payload). The six push actions run through `widgets/app_bar_actions.dart`, so only **Sync** and **Push settings** sit on the toolbar and the other four are one tap into its overflow. A **Push settings** action (`Icons.tune`) encodes a demo `WatchSettings` (`lib/watch_settings.dart`) and pushes it over the same injectable `WatchBleTransport` the run-sync action uses (`WatchSyncClient.pushSettings` → `transport.writeSettings`); a sibling **Push workout** action (`Icons.checklist`) encodes the golden demo workout (`lib/watch_workout.dart` `encodeWorkoutSteps` + `chunkWorkout`) and pushes it via `WatchSyncClient.pushWorkout` → `transport.writeWorkout` (the `...e6` workout characteristic) — the WKT1 frame whose CRC the watch echoes back in run-store v4's workout summary (decisions §356) — and a **Push course** action (`Icons.route`) sends the golden three-point sim course + elevation as a CRS1 frame via `WatchSyncClient.pushCourse` → `transport.writeCourse` (the `...e5` course characteristic), lighting up the hardware build's Nav / Roadbook / CutoffEta pages that otherwise honestly read NO COURSE LOADED. A **Push roadbook** action (`Icons.table_chart_outlined`) sends the canned sim schedule — byte-for-byte the fixture the firmware's own `SIM_ROADBOOK` and the `roadbook_store` golden vector pin (start / aid at 90 m / 180 m finish, cut-offs at 90 m and 170 m) — as an `RBK1` frame via `WatchSyncClient.pushRoadbook` → `transport.writeRoadbook` (the `...e8` roadbook characteristic). A fifth **Compose watch screens** action (`Icons.dashboard_customize`) pushes `watch_screens_editor_screen.dart`, threading the same `transportFactory`. NOT a product surface — see decisions.md § 209.
- `watch_screens_editor_screen.dart` — dev-only composed-data-screen authoring surface (SCR1, decisions §364), reached from the Compose action on `sim_watch_screen.dart` and therefore behind the same loopback gate. Composes up to `kMaxWatchScreens` screens, each a `WatchLayout` (single / duo / trio) plus exactly that many ordered `WatchMetric`s, and pushes the set via `WatchSyncClient.pushScreens` → `transport.writeScreens` (the `...e7` screens characteristic, unchunked — the whole set is 28 B). **The editor's contract is that an invalid set cannot be built**: the draft always carries exactly `layout.slots` metrics, the Add affordance disables at the cap, and widening a layout seeds the new slots, so `encodeWatchScreens`'s two `ArgumentError`s are unreachable from here rather than surfaced as a failed push. Narrowing a layout confirms first and **names the metrics it would drop** (never a silent discard); removing a screen confirms too. Slot order is meaning on the wire, so each slot carries move-up / move-down. The set persists device-locally to `SharedPreferences` under `kWatchScreensPrefsKey` via the pure top-level `encodeWatchScreenDraft` / `decodeWatchScreenDraft` (keyed by `wireName`, whole-set fail-closed like the firmware's own decode — a stored set that no longer parses lands on a Start-over state, never a partially-restored one). Metric + layout display names are localized (`watchMetric*` / `watchLayout*` ARB keys); `wireName` is an identifier and is never shown. NOT a product surface.
- `clubs_screen.dart` — Browse / My clubs, hosted as the Clubs sub-tab of `SocialScreen` (the 4th bottom-nav tab). FAB opens `widgets/club_form_sheet.dart` (Create club)
- `social_screen.dart` — the Social bottom-nav tab. Five sub-tabs: Feed · People · Clubs · Discover · Challenges. The five English labels need a 609 dp strip, so on a phone `AppTabBar` derives a scrolling strip and Challenges starts off the right edge; the four that preceded it needed 436 dp and did not fit a 360 dp phone either, so this is § 533's derivation behaving rather than something the fifth tab introduced (measured in `test/social_tab_strip_width_test.dart`). Each embeds its screen widget in `embedded: true` mode (returns body only, no Scaffold/AppBar/FAB) and SocialScreen hosts the chrome — one AppBar with an `AppTabBar` at the bottom and a single FAB slot that hoists whichever child FAB matches the active tab (Clubs → New club, Challenges → Create challenge, both through the shared `_hoisted` helper that schedules the post-frame rebuild a GlobalKey binding needs; Feed / People / Discover have none). Routes used to be a sub-tab here; the Fitness-hub redesign relocated it to Fitness → Runs (a run-modality surface). `SocialScreen` still takes `routeStore` (ClubsScreen surfaces club-owned routes) but no longer takes `preferences` / `onStartRun`.
- `discover_screen.dart` — the Social hub's Discover tab (mirror web `SocialDiscover.svelte` + `/social?tab=discover`). Cross-club activity search over the `search_public_events` RPC (public clubs only): a debounced search field + category chips (run/cycle/class/social, reusing `eventEditorCat*` labels) + cadence / weekday / time-of-day dropdowns + free/paid chips. Result cards show discipline-or-title + club name + `category · cadence` + a free/currency price chip, and open `EventDetailScreen` on tap. Calls `ApiClient.searchPublicEvents` → `PublicEventResult` (core_models); failures fall back to an empty state (L4). Embedded under `social_screen.dart`; standalone Scaffold path kept for any direct caller. Time-of-day is timezone-aware server-side (`events.timezone`).
- `club_detail_screen.dart` — **six** tabs (a scroll-away hero over a pinned strip via `widgets/collapsing_tab_host.dart`, decisions §545; Photos is the sixth): Feed (threaded posts) · Events (admin Create button + RSVP cards) · Members (admin pending-requests panel + count) · Routes (club-owned routes via `routes.club_id`, lazy-loaded; tap → `public_route_screen.dart`) · Templates (admin/member-visible plan templates with per-row Adopt → `clone_plan_template` RPC; a Session-templates section → `clone_session_template`; and a gym-routine-templates section → `ApiClient.cloneGymRoutineTemplate`, member-gated Adopt → navigate to the cloned personal routine) · Photos (`widgets/club_photos.dart`)
- `event_detail_screen.dart` — per-instance RSVP + admin update composer + admin-only Race control card (Arm / Fire Go / End / Cancel, gated on `ClubView.isRaceDirector`, backed by `SocialService.armRace` / `startRace` / `endRace`). **Slice E (typed club events):** every athletic affordance (distance / target-pace metrics, Race control, results leaderboard / Submit-my-time) is gated behind `isAthleticEventCategory(e.row.category)` — `run`/`cycle` render as today; `class`/`social` are attendance-only. A `class` with a `discipline` shows the prominent `EventDisciplineLabel` (`@visibleForTesting`) eyebrow + free-text label in the header instead. `_eventSelectCols` in `social_service.dart` selects `category, discipline` (required — `EventRow.fromJson` casts `category` non-null). It does NOT select `host_user_id` — that column stays revoked from authenticated/anon (the paid-events payout recipient, `20261228_001`/`20261230_001`), so selecting it returns 42501; mirrors web's `EVENT_SELECT_COLS`. **Add to calendar (decisions § 692):** an "Add to calendar" action for the occurrence on screen, plus "Add whole series" when `calendarSeriesFor` can state the recurrence exactly; both hidden for a past or called-off occurrence, both routed through `calendar_intent.dart`. Cancelled occurrences ahead are named in a note under the series button, because the platform hand-off has no `EXDATE`.
- `plans_screen.dart` — Training plans list (accessed from Run tab idle button)
- `plan_new_screen.dart` — Wizard with live pace + week-outline preview
- `plan_detail_screen.dart` — Hero + progress ring + today card + current-week strip (`CurrentWeekStrip`) + month calendar + week grid. **Owner-only (mirror web `/plans/[id]`):** adherence banner (`plan_adherence.dart`), Re-plan + Adaptive re-plan flows (`plan_replan.dart` / `plan_adaptive_replan.dart` → preview modal → apply), per-week Duplicate action (`TrainingService.duplicatePlanWeek`). Owner gate reads the Supabase auth uid (`viewerIdOverride` test seam). **P2 adaptive fitness gate (gated OFF):** `_proposeAdaptiveReplan` passes an `AdaptiveFitness?` from `_adaptiveFitnessInput()` — computed from the threaded `LocalRunStore`'s FULL `Run`s (carrying `metadata.avg_bpm`, NOT `_recentRuns`/`RecentRunRow`) via `computeTrainingLoadSeries`, mirroring web's `adaptiveFitnessInput`. Returns null unless `adaptiveFitnessGate` (from `adaptive_fitness_flag.dart`) is on (OFF by default, pending P2 CISO sign-off, decisions §144); when on + the runner is fatigued (TSB < 0), an under-trend add-volume is held with `planDetailAdaptiveFitnessHeld`. `runStore` is threaded from the Fitness hub via `PlansScreen`; other call sites pass null (P1 behaviour). Owner gate reads the Supabase auth uid (`viewerIdOverride` test seam).
- `nutrition_meal_detail_screen.dart` — Per-meal detail (mirror web `/nutrition/[date]/[slot]`): one meal slot's items + macro breakdown + a 7-day calorie trend, reached by tapping a meal-group header on `nutrition_screen.dart`. Reads the offline-first `LocalFoodStore`.
- `workout_detail_screen.dart` — Structured-interval breakdown + per-kind advice
- `gym_screen.dart` — Phase 4 multi-modal Gym (decisions §63). Workout list + PR badges + a log composer (AppBar +). Mirrors web `/gym`. Reads + writes through `LocalGymStore` (offline-first); a best-effort `api.fetchGymWorkoutsWithSets` overlay on mount hydrates the store, pending writes drain on the next online refresh. List stats (volume / exercise count) + PR badges compute from the store's inline sets via the pure top-level helpers `gymWorkoutVolume` / `gymExerciseCount` / `gymPrWorkoutIds` / `gymExerciseSuggestions` (unit-tested, mirror web `/gym`'s derivations). **Reachable (G5)** via the centre Log sheet ("Log lift") and the Home today's-lift card (`widgets/gym_summary_card.dart`). Carries the labelled peer strip `Log · Routines · Sessions · Records` (`widgets/surface_peer_strip.dart`, mirroring web `/gym`'s header links) — Routines waits on the routine store's disk hydration, Sessions needs a signed-in client (it reads `session_plans` from the server), Records is data-gated on a weighted set exactly as web is. **The exercise catalogue has THREE states here, not two** (decisions § 1514): `_catalogueUnavailable` starts true and is cleared only by a read that ANSWERS, so "not yet read" and "the read failed" both reach the composer as unknown. It matters because an empty catalogue is the state in which every typed name looks free — the picker's exact-match test finds nothing and offers to create a name the catalogue already holds, which mints a shadow against a seeded global the author's partial unique cannot see. The failed read still keeps whatever rows were last known and still never blocks the workout list (L4).
- `gym_prs.dart` — the gym PR metrics (heaviest set / most volume / best e1RM), the Dart half of the `gym_prs` parity pair. **The grouping KEY it computes under is no longer here**: `normaliseExerciseName`, `namesAnExercise`, `kExerciseWhitespace` and the frozen 1,488-entry fold table live in `packages/core_models/lib/src/exercise_key.dart` + `exercise_fold_table.dart` (decisions § 1515), because `packages/api_client` decides the same blankness question and could not reach the class from an app — it tested `name.trim()`, right only by a coincidence of Dart's own whitespace set. This file re-exports them, so every `import '../gym_prs.dart'` is unchanged, and `core_models` being shared rather than twinned means the generator now writes ONE Dart table instead of two.
- `gym_detail_screen.dart` — single gym workout (mirror web `/gym/[id]`): exercise blocks with per-exercise PR chips (`gym_prs.dart#workoutPrs` judged against earlier workouts in the store), each set's reps × weight + RPE, notes, owner edit / delete. Reads from `LocalGymStore`; `_ensureLoaded` pulls the list once if reached before the list hydrated the store (deep-link path). **Save as routine** (promotes the session's grouped sets into a routine draft via `gym_routine.dart#routineFromWorkout`, opens `routine_builder_sheet.dart`) + **Repeat last** (seeds a fresh gym log via the existing composer) are AppBar actions (gym-programming P1); both own a lazily-init'd `LocalRoutineStore`.
- `routine_library_screen.dart` — gym-programming P1 routine library (mirror web `/gym/routines`). Authored routines most-recently-modified first; self-hiding empty onboarding state; `New routine` opens `routine_builder_sheet.dart`; tap → `routine_detail_screen.dart`. Reads `LocalRoutineStore` (offline-first), best-effort server hydrate on mount. Reached from the Routines peer on `gym_screen.dart`'s surface strip.
- `routine_detail_screen.dart` — single routine (mirror web `/gym/routines/[id]`): planned targets per exercise (reps range + weight), a **Start routine** FAB (P1 prefill-only — expands via `gym_routine.dart#prefillFromRoutine` and seeds a fresh gym log through the composer, no execution loop), and Delete behind an `AlertDialog`. Reads `LocalRoutineStore`. **Club templates (migration `20270109_001`):** an author + admin-club-gated publish-row (pick a club → `ApiClient.publishGymRoutineAsTemplate`) renders when an optional `SocialService` is wired (`fetchMyClubs` filtered to `isAdmin`; `viewerIdOverride` test seam); a "Club template" badge shows once the routine is club-owned (`StoredRoutine.clubId`). **History panel (decisions § 617):** an author-gated read-back of the routine's own past sessions — count, days since last done, completed-of-graded ratio, and the five most recent as rows into `GymDetailScreen` — shaped by the `routine_history.dart` parity twin off `ApiClient.fetchGymRoutineHistory` (the `gym_routine_history` RPC, migration `20270528_001`: complete tallies plus a bounded page of recent sessions, aggregated server-side). Read from the SERVER rather than from `LocalGymStore`, which holds only the most recent page of workouts, so a routine last run months ago would read as never run. Self-hides at zero sessions but NOT on a failed read: that renders the error card with a Retry, because "couldn't load" and "you have never run this" are different claims.
- `sessions_screen.dart` — the user's own session plans (session_planner.md P1; mirror web `/sessions`), read-only list → `session_detail_screen.dart` (the follow-along runner). Reached from the **Sessions** peer on `gym_screen.dart`'s surface strip, which is why the peer needs a signed-in `ApiClient` (the list is a server read, not a local store). Before decisions §488 the class had no instantiation anywhere in the app — only the club-detail path reached its *detail* sibling, so a user could open someone else's class sequence but never their own list.
- `races_screen.dart` — the public race calendar (race_calendar.md; mirror web `/races`): name search + distance bands + "near a place" geocode over `search_race_listings`, a Register link and an import affordance per result, and an "Add a race" crowd submission. Reached from the **Races** peer on the run surface's strip and, secondarily, from the race-provider tiles under Settings → Integrations. **Nothing gates reaching it** — the search is a public RPC; `RaceService.isProviderConfigured` gates only the provider import inside the per-race sheet (decisions §488; before that, both entry tiles were `onTap`-gated on the probe, so an unconfigured deployment had zero reachable entry points). The sheet is **provider-driven, not RunSignUp-only**: it resolves the listing's provider against the `raceImportProviders` catalogue in `race_service.dart`, probes THAT provider, and offers its pull when configured / its own explainer when not — a `parkrun`, `manual` or `raceresult` listing has no import leg and falls through to manual paste, which is offered in every case. The scoping input comes off the spec's `RaceImportScope`: a bib for RunSignUp and ChronoTrack (an unscoped pull returns the whole finisher field, issue #360), the athlete id for UltraSignup, which an UltraSignup listing carries as `provider_race_id` so nothing needs typing. **The catalogue is the only provider vocabulary the app holds** — don't add a fourth hand-written list; `race_provider_labels.dart` keys the icon + the four sentences on the same token, and `test/race_providers_test.dart` reads the catalogue against `race-results-import`'s own accepted-provider literal.
- `nutrition_screen.dart` — Phase 4 multi-modal Nutrition (mirror web `/nutrition`). Mifflin-St Jeor macro rings vs targets (hidden when body metrics are absent — anti-clutter), meal-slot daily view, a tap-to-increment water tracker (per-day `SharedPreferences` int), a self-hiding **Nutrients** roll-up for the five extended nutrients (`extended_nutrients.dart`, mirroring web's Nutrients section — "at least" under partial coverage, `remaining` withheld, and no chip at all rather than a false "No daily target"), and a 7-day calorie trend whose header carries the `X under/over goal/day` calorie chip + a `Protein met/total days` consistency chip (both from `nutrition_week.dart`). Reads + writes through `LocalFoodStore` (offline-first); a best-effort `api.fetchFoodLog` overlays the last 7 days on mount. Targets are resolved by the shared top-level `loadNutritionTargets(api, settings)` helper (height/sex from `fetchMyProfile`, weight from `fetchLatestBodyWeightKg`, age from the date-of-birth pref, activity-level + goal from the universal bag — set in **Settings → Body metrics**, `settings_body_metrics_screen.dart`, G5). **Reachable (G5)** via the centre Log sheet ("Log food") and the Home nutrition rings card (`widgets/nutrition_rings_card.dart`); takes an optional `settingsSync` so the rings hide when body metrics are absent. **Date-navigable since 2026-08-18** (mirror of web's `?date=` stepper, decisions § 591): a day stepper above the rings walks `_viewDate` (a local `YYYY-MM-DD` held over `diary_day.dart`) and every read AND write follows it — the food window, the exercise add-on's day, the seven trend buckets, the water key, the meal-detail deep link, and all three write paths (the `nutrition_log_sheet` composer via its new `diaryDate`, a logged meal template, a logged recipe), each stamping `entryTimestampFor(_viewDate, DateTime.now())` at save time so a forgotten yesterday can be back-filled.

Sixteen screens were absent from this list until issue #666 I18 re-derived it
against `lib/screens/` (75 files). The gap is the entries below; keep it closed
— `gear_rotations_screen.dart` had drifted into the top-level section and moves
here.

- `challenges_screen.dart` — the Social hub's **Challenges** sub-tab (its fifth): browse public challenges + the user's joined ones, and author one. `embedded: true` returns the body only so `SocialScreen` owns the chrome — including the create FAB, which the public `ChallengesScreenState.buildCreateChallengeFab` exposes for hoisting exactly as `ClubsScreenState.buildCreateClubFab` does; the standalone mount renders it on its own Scaffold. Tap → `challenge_detail_screen.dart`. Joined rows carry the caller's own progress — value against goal, bar, rank, earned badge — folded in from `my_active_challenges` by `mergeMyProgress` (`challenge_list.dart`) and gated by `myProgressView`: a challenge outside that RPC's live-plus-7-day window has NO known value and the row says so instead of drawing a 0 % bar (decisions §694).
- `challenge_detail_screen.dart` — one challenge: goal progress bar, on-pace hint, and the dense-ranked leaderboard, all off the `challenge_progress.dart` parity pair. Join / leave, and the metric formatter honours the viewer's unit pref. The caller's own value is read off the board the page already holds (`fetchChallengeById` fills no `myValue`, so a `?? 0` there was a confident zero for everyone), and the club-vs-club team column goes through `teamLabel` so a club RLS refused reads as a label, never as a raw uuid.
- `widgets/challenge_progress_bar.dart` — the one challenge progress bar (value/goal label + complete chip + `ProgressBar` + on-pace hint) plus `challengeValueLabel` and `challengeMetricLabel`, shared by every challenge surface as web shares `ChallengeProgressBar.svelte`.
- `widgets/challenge_form_sheet.dart` — the create-challenge form (decisions §754), a `showFullScreenForm` child mirroring the create half of web's `ChallengeEditor.svelte`: title, description, metric + scope chips, goal, activity type, club anchor (admin clubs only, hidden for `club_vs_club`), window. **The goal is typed in the reader's own unit and converted at the boundary** (`challengeGoalSuffix` / `challengeGoalToStored`) — the column stores metres / seconds / a bare count, and web's unit-less field means a typed `100` on a distance challenge is a 100 metre goal. `challenges_window_ck` is checked before the insert because its 23514 names neither bound, and the create throttle's P0001 goes through `rateLimitErrorMessage` **before** `friendlyError`, whose rate-limited branch would otherwise collapse the bucket and the wait into a generic sentence (decisions §747). Editing an existing challenge stays web-only per §24.
- `checkpoint_checkin_screen.dart` — offline aid-station check-in for race-crew volunteers (race_director_ops.md): pick the checkpoint, enter/scan a bib, stamp IN / OUT through `LocalCrossingsStore` so the surface works with no signal; the server merges two volunteers' stamps via the upsert RPC. Reached from an organiser-gated action on event detail. **P3 weigh-in fields are Art 9 health data and stay fail-closed behind the `WEIGH_IN_GATE` dotenv flag** (off by default, decisions §150).
- `club_invite_screen.dart` — redeem a club invite token; mirror of web's public `/clubs/join/[token]`. Reached from "Join with code" on Social → Clubs or from a deep link that pre-fills `initialToken`. Redemption failures route through `friendlyError` rather than surfacing a raw `PostgrestException`.
- `gear_screen.dart` — Settings → Gear: shoes + bikes with per-item accrued mileage, offline-first over `LocalGearStore`. Carries the labelled peer strip `Gear · Rotations` (issue #666 I8) and a `+` create action.
- `gear_rotations_screen.dart` — Gear → **Rotations**: named multi-pair groupings ("Daily trainers", "Race day"), additive to the single `is_default` current pair. Online-only by design — rotations live OUTSIDE `LocalGearStore`, read/written through the `ApiClient` rotation methods (roadmap §7, decisions §183). Each row also carries the **next-up** read-out + a **Make current** action (decisions §613): `gear_rotation_pick.dart#rotationPick` over the rotation's members ranks the in-service pairs least-worn-first, and the action hands the `is_default` star to the pick via `ApiClient.setDefaultGear` — the bridge between a rotation and the flag the `runs`-insert trigger actually auto-tags with. Run once per gear kind the rotation holds (the star is scoped to `(owner, kind)`), and gated on the pick having ranked two or more pairs, not on the membership count. The member rows themselves come from the passed `LocalGearStore`, which the screen now listens to so the star moves in place.
- `guided_runs_screen.dart` — the guided-run library (coach-voice scripted workouts + their TTS cue scripts), reached from the **Guided runs** tile beside Coaching on the Settings landing and from the `Coach · Guided runs` peer on `coach_screen.dart` (issue #666 I7). No API dependency, so it stays reachable signed-out; the recorder-side cue firing is `audio_cues.dart#speakGuidedCue`.
- `gym_exercise_screen.dart` — per-exercise progression (mirror web `/gym/exercise`): headline latest est-1RM + delta vs the first session, then a newest-first session list with an e1RM-relative bar and a PR badge.
- `gym_records_screen.dart` — per-exercise PR roll-up (mirror web `/gym/records`): est-1RM / heaviest / top-volume + last performed + session count from `LocalGymStore`, each row drilling into `gym_exercise_screen.dart`. Reached from the **Records** peer on `gym_screen.dart`, data-gated on a weighted set existing.
- `gym_session_screen.dart` — the guided gym session (gym_programming.md P3): drives a `GymWorkoutRunner` over a routine's expanded per-set steps with reps / weight / RPE entry, Complete (logging + a rest countdown), Skip, Previous, Abandon. Finishing persists a `StoredGymWorkout` carrying `{routine_id, gym_step_results}`.
- `people_screen.dart` — the Social hub's **People** sub-tab: name search + "suggested" people from the viewer's clubs. Mirrors web's `SocialPeople.svelte`.
- `plan_library_screen.dart` — the public plan library (mirror web `/plans/library`): browse + search published plans, preview one, clone it into your account. Reached from the **Browse library** peer on `plans_screen.dart` (issue #666 I8).
- `route_picker_screen.dart` — full-screen route picker, the `MaterialPageRoute` replacement for the old `showModalBottomSheet` route list; used by the run-start and manual-run flows.
- `routine_public_library_screen.dart` — the public gym-routine library (mirror web `/gym/routines/library`, migration `20270224_001`): browse + search public routine templates, preview, adopt (clone). Reached from the **Library** peer on `routine_library_screen.dart` (issue #666 I8).
- `settings_safety_screen.dart` — Settings → Safety contacts (decisions §131; mirror web `/settings/safety`): add a contact by email, see pending/confirmed status, remove it, and confirm/decline incoming requests where you are the named contact. A safety contact is emailed when the owner finishes a run.
- `watch_live_screen.dart` — Settings → Integrations → Custom watch: relays the watch's per-second BLE status frames into the live spectator feed so a wrist-recorded run is followable at `/live/{run_id}`. The production caller for `WatchStatusLink` (decisions §456) + `WatchLiveForwarder` (§447).

**Top-level (`lib/`):**
- `main.dart` — app entry, Supabase init, service wiring; calls `WearAuthBridge().attach(...)` so the paired Wear OS watch inherits the Supabase session, plus `WearRoutesBridge().attach(routeStore)` so the watch's route picker reflects the user's starred subset without needing its own network
- `sync_service.dart` — bulk-sync button, auto-sync on connectivity/foreground, conflict resolution
- `wear_auth_bridge.dart` — forwards Supabase session changes to the paired Wear OS watch via a `run_app/wear_auth` method channel (native `WearAuthBridge.kt` under `android/app/src/main/kotlin/com/threkir/app/` writes to the Wearable Data Layer)
- `wear_routes_bridge.dart` — sibling of `wear_auth_bridge.dart`. Subscribes to `LocalRouteStore`; on every change pushes the user's **starred** subset (id + name + distance + waypoints) to the watch at `/saved_routes` via the `run_app/wear_routes` method channel. Native `WearRoutesBridge.kt` writes the DataItem. Watch-side `RoutesBridge.kt` listens at the same path and updates its DataStore-backed `LocalRouteStore`. Local-only "Save for offline" pin (separate from star) is gated on `LocalRouteStore.pinOffline` / `unpinOffline` / `isOfflinePinned`. See [decisions.md § 64](../../docs/architecture/decisions.md#64-offline-pin-is-a-separate-local-only-flag-from-star-phone-pushes-starred-routes-to-wear-os-via-datalayer)
- `run_notification_bridge.dart` — replaces geolocator's "Run in progress" foreground-service notification with live time/distance/pace (native `RunNotificationBridge.kt` reposts on the same channel id so the lock-screen row is live instead of static). Also owns the per-split shade row (#303): `updateSplit` posts on a single fixed split id (distinct from the ongoing-run id) so splits replace in place instead of stacking, the native side makes the row auto-cancel + time out, and `clearSplit` / `clear` drop it on run start / stop. **Lock-screen Pause/Resume/Stop action buttons (#14, #270)** fire via `PendingIntent.getBroadcast` → the manifest-declared `RunActionReceiver` (`android/.../RunActionReceiver.kt`, `exported=false`) → `RunNotificationBridge.instance.dispatchAction` → the Dart `action` method-channel call. Broadcast (not a `getActivity` MainActivity launch) is deliberate: an activity launch trips the keyguard unlock prompt on a locked phone, a broadcast doesn't. The Dart side is unchanged (`onPause`/`onResume`/`onStop`); the fix is native + the manifest
- `push_messaging_bridge.dart` — native-push (FCM/APNs) device-token registration, sibling of `wear_auth_bridge.dart`. Attached in `main.dart` after Supabase init; on sign-in requests the OS notification permission, fetches the FCM token, and upserts it onto `device_tokens` via `ApiClient.registerDeviceToken` (`platform = Platform.isIOS ? 'ios' : 'android'`); re-registers on `onTokenRefresh`; removes the row + deletes the platform token on sign-out so the next account on the device doesn't inherit pushes; routes a notification tap's deep link through `onOpenNotification`. The settings `push_notifications` toggle mirrors its choice to this device's `is_notifications_enabled` flag via `PushMessagingBridge.instance.setNotificationsEnabled`. **Credential gate (L4):** every call routes through the `PushMessaging` seam (`firebase_push_messaging.dart` is the production impl over `firebase_messaging`); when Firebase isn't configured on the device (no `google-services.json` / `GoogleService-Info.plist`) `isAvailable` is false and `attach()` is a complete no-op — the app compiles + runs in dev without the operator's Firebase artifacts. The server leg is the Go worker's `native_push` consumer. See [../../docs/features/native_push.md](../../docs/features/native_push.md) + [decisions.md § 166](../../docs/architecture/decisions.md). The native glue (Android `FirebaseMessagingService` / iOS `AppDelegate` APNs registration) lives outside `lib/` (`android/` / `ios/`) so it is **not** twin-shared — each app owns its native side; the operator supplies the config files.
- `firebase_push_messaging.dart` — the production `PushMessaging` impl over `firebase_messaging` + `initFirebaseForPush()` (best-effort `Firebase.initializeApp`, never rethrows). Every method is gated on `Firebase.apps.isNotEmpty` so an uncredentialed build runs inert.
- `background_sync.dart` — WorkManager periodic background sync (hourly, network-connected)
- `background_location_nudge.dart` — pure `shouldNudgeBackgroundLocation(...)` (persona #57). Android 11+ only grants "while in use" from the initial dialog; `run_screen._maybeNudgeBackgroundLocation` calls this before a run starts and, when foreground location is granted but background isn't, shows a dialog that deep-links to app settings (`openAppSettings()`). Non-blocking — the run proceeds either way. Android-only (the helper returns false on iOS). Shows once: a dismissal persists to `Preferences.backgroundLocationNudgeDismissed` (issue #266, mirroring the battery-opt hint's flag) and `shouldRearmBackgroundLocationNudge` clears it when always-on is observed granted, so a later revocation re-nudges. 12 unit tests in `test/background_location_nudge_test.dart`.
- `calendar_intent.dart` — hands a club event to the phone's own calendar (decisions § 692). `addToDeviceCalendar` invokes `run_app/calendar` → `ACTION_INSERT` on `CalendarContract.Events` (`android/.../CalendarBridge.kt`) / `EKEventEditViewController` (`apps/mobile_ios/ios/Runner/CalendarBridge.swift`), so the OS calendar's own editor opens pre-filled and the runner confirms the save — no calendar permission on Android, write-only access on iOS. Returns false and never throws (L4). `buildRrule` + `calendarSeriesFor` shape the whole series as an RFC 5545 RRULE value — the one representation crossing the channel, since Android stores it verbatim and iOS parses it into an `EKRecurrenceRule`. Mirrors web's `event_ics.ts` **decisions** (monthly past the 28th withheld, `until` + `count` resolved to whichever binds first, BYDAY read off the expansion in UTC) but is **not a parity pair** — the `.ics` text builder is a browser-download concern per § 24. Neither hand-off carries `EXDATE`, so `unsubtractedCancellations` reports the called-off occurrences ahead (over `event_occurrence.dart`) for the screen to name. 30 tests in `test/calendar_intent_test.dart`.
- `battery_optimisation_hint.dart` — `shouldShowBatteryOptHint(...)` + `openBatteryOptimisationExemption(...)` (persona round-5, issue #260). Aggressive OEM app-killers (Samsung Stamina, Xiaomi, OnePlus) freeze the recording foreground service mid-run unless the app is exempted from battery optimisation, silently truncating a long effort. `run_screen._maybeShowBatteryOptHint` calls the decision once (gated on `Preferences.batteryOptHintShown`) before a run starts and, on Android, surfaces a non-blocking dismissible dialog whose "Open settings" deep-links to the system battery-optimisation screen (`ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS` via the `run_app/battery_settings` channel, native `BatterySettingsBridge.kt`), falling back to the generic App Info page (`openAppSettings()`) when the intent can't resolve. Android-only; the run proceeds either way. 10 unit tests in `test/battery_optimisation_hint_test.dart`.
- `local_run_store.dart` / `local_route_store.dart` — `ChangeNotifier`-style on-disk stores. **`LocalRunStore` is windowed** (decisions § 135): cold-load reads a compact `index.json` (one `RunSummary` per run — scalars only, no track) into the full in-memory `summaries`, and hydrates only a **resident window** of full `Run`s into `_runs` — newest `kResidentWindow` (200) by date **∪ all unsynced** (the residency invariant the sync drain relies on). Read the right seam: `summaries` / `summaryRuns` (full history, track-less) for all-time stats / filters; `runs` (the resident window) + `runById(id)` (resident → on-disk hydrate) for display / detail-nav; `recentWindow(N)` for the timeline feed; `hasRun(id)` for a whole-history existence check (`contains` only sees the window). The index is a batched, atomic, crash-safe **cache** of the per-run files (never the source of truth) — a missing / drifted index self-heals via the full parallel walk on cold-load. **Don't reintroduce a full-history read of `runs`** — it's the window now; reach for `summaries`/`summaryRuns`. `residentWindow` is `@visibleForTesting`-injectable. **Every sidecar of both stores is a merge-under-lock, never a whole-file replace** (decisions § 303 + § 309) — `background_sync.dart` runs a second instance of each over the same directory, so a snapshot write silently discards the other isolate's work. `index.json` merges per id on `last_modified_at`; the run sidecars decide a known id from memory; the three route sidecars let additions win and carry removals in an explicit cleared-ledger, because for routes the losing direction is a cross-account transfer. Any local mutation that makes a run unsynced must also write `synced_ids.json` — the index's flag does not survive cold load (§ 308).
- `gear_backfill.dart` — pure helper `gearBackfillCandidates(gearKind, since, runs)` that filters `LocalRunStore.runs` for the activity-type+date-range that matches a new gear's `purchased_at`. Backs the post-create "Attach past runs to …" prompt on `gear_screen`. Shoes match `run` / `walk` / `hike`; bikes match `cycle`. Missing `metadata.activity_type` defaults to `run`. Newest-first sort. 9 unit tests in `test/gear_backfill_test.dart`.
- `widgets/gear_backfill_sheet.dart` — modal bottom sheet with one [CheckboxListTile] per candidate run. Calls `ApiClient.addGearToRuns(gearId, runIds)` on confirm. All candidates default to selected; user can deselect individuals or use the master "Select all / none" toggle. Online-only (requires server round-trip for `run_gear` upsert); skipped silently when api is null or signed-out. 4 widget tests in `test/gear_backfill_sheet_test.dart`.
- `local_gear_store.dart` — `ChangeNotifier`-style on-disk gear store. Mirrors `LocalRunStore`'s pattern: one JSON file per row at `<appDocs>/gear/<id>.json`, in-memory cache, full CRUD that works offline. Each row carries a `GearSyncState` (`synced` / `pendingCreate` / `pendingUpdate` / `pendingDelete`); `syncWithServer(api)` drains every non-synced row in create → update → delete order and is a no-op on a clean store. Offline-create uses a client-minted v4 UUID that becomes the server id on the eventual INSERT, so no temp-id reconciliation is needed. See [decisions.md § 73](../../docs/architecture/decisions.md#73-gear-has-a-localgearstore-mirroring-localrunstore-client-minted-uuids-sidestep-temp-id-reconciliation) for the rationale.
- `local_routine_store.dart` — gym-programming P1 offline cache for reusable routines (`StoredRoutine`), sibling of `local_gym_store.dart` over `OfflineSyncStore` (one JSON file per routine under `<appDocs>/routines/`, exercises + their planned sets carried **inline**, client-minted UUID = server id, `syncWithServer` drains create → delete). P1 has no edit path (build / save / delete only, mirroring web), so a routine never reaches pendingUpdate. Drives `routine_library_screen.dart` + `routine_detail_screen.dart`; pushed via the `api_client` routine CRUD (`createGymRoutine` / `fetchGymRoutines` / `fetchGymRoutineDetail` / `deleteGymRoutine`). See [gym_programming.md](../../docs/features/gym_programming.md).
- `offline_sync_store.dart` — the disk-backed per-row sync-state machine the gear / gym / food / routine / meal-template / recipe / crossings / session stores extend. **A screen that constructs one of these must call `init()` on it, never a bare `loadAll()`** — `loadAll` tolerates a null `dir` and returns, so the store reads as alive while every write refuses; `nutrition_screen.dart` shipped in that state and its saved meals + recipes never reached disk or the server. Every write path (`persist` / `dropRow`) goes through `requireInitialised` and raises a named `StateError` **before** touching `rowsById`, so an undurable write leaves no row behind for the UI to present as saved. The rule is enforced source-level by the `a screen-owned OfflineSyncStore is init()ed` group in `architecture_guards_test.dart`. **Every directory-mutating entry point (`persist` / `dropRow` / `rewriteAll` / `restoreFromBackup` / `clear` / `loadAll`) runs on one serial write chain keyed on the DIRECTORY, not the instance** — a new one must go on it too. An atomic write is atomic against itself only: unserialised, sign-out's `clear()` deleted the `.tmp` of an in-flight `persist` (a `PathNotFoundException` naming a temp path the caller never saw) and a delete overtaken by a write to the same id left the row's file back on disk with `rowsById` saying it was gone, resurrecting it at the next cold load. Instance-keying would not have covered the first: `offline_store_wipe.dart` wipes from a throwaway instance precisely because all instances of a type share one directory ([decisions § 821](../../docs/architecture/decisions.md)). **In a widget test, the in-memory row lands a frame before the file does** — `persist` installs it synchronously — so waiting on a UI signal and then deleting the temp directory tears the `.tmp` out mid-rename; wait on the file, or on `debugWritesSettled`. **It also owns the family's two timestamp readers** — `parseServerTimestamp` (a `timestamptz` column off a row map, normalised to UTC) and `storedClockOrEpoch` (the record's own clock off disk or a backup archive, falling back to the EPOCH so an unknown clock loses every newer-wins comparison rather than freezing the row against the server, [decisions § 1342](../../docs/architecture/decisions.md)) — beside `outsideFetchWindow`. The read used to exist nineteen times in five spellings across the family, and `store_timestamp_reader_guard_test.dart` now derives the family from `extends OfflineSyncStore<` and refuses any other `DateTime.parse` / `tryParse` in it unless the site states a `zone-verbatim:` reason. There is no standing exemption any more: gear's private `_parseDate` is now the shared `parseCalendarDate`, the second named reader in the same file, and the guard additionally pins that `offline_sync_store.dart` holds exactly ONE `DateTime.tryParse` — inside `parseIsoStrict`, which range-checks every component because `tryParse` ROLLS an impossible date over rather than refusing it (`2026-06-32` is the 2nd of July, and `gear.purchased_at` goes straight back to a `date` column). The two readers differ only in the `.toUtc()`, which `parseCalendarDate` must NOT have: normalising local midnight moves the calendar day itself for a device AHEAD of UTC ([decisions § 1289](../../docs/architecture/decisions.md) + [§ 1344](../../docs/architecture/decisions.md), which corrects § 1289's hemisphere).
- `local_gym_store.dart` / `local_food_store.dart` — Phase 4 multi-modal offline caches, same shape as `local_gear_store.dart` (one JSON file per row under `<appDocs>/gym/` and `<appDocs>/food/`, per-row sync state, client-minted UUIDs, `syncWithServer(api)` drains create → update → delete). `LocalGymStore` stores each workout's sets inline (`StoredGymWorkout.sets`, positional `set_index`) so a workout + its `gym_sets` travel as one file; the drain replays them via `createGymWorkout`/`updateGymWorkout`. `LocalFoodStore` exposes `entriesForRange(from, to)` for the single-day nutrition view. **Both share the `OfflineSyncStore` summary index** (decisions § 135): cold-load reuses a drift-validated `index.json` (a `summaryOf` record per row) for a fast summary view + the `loadInWindow(from,to)` / `estimateRowsInWindow` API, falling back to the full walk + rebuild on a missing/drifted index. Unlike `LocalRunStore` they still hydrate every full row on load (their consumers read full rows directly); gear implements `summaryOf` but writes no index (no windowed surface). **Driven by the gym/nutrition screens + the Home dashboard** — `gym_screen.dart` and `nutrition_screen.dart` each hydrate their store on mount (best-effort `api` fetch → `replaceFromServer`) and drain pending writes via `syncWithServer(api)`; `dashboard_screen.dart` also best-effort hydrates both on mount (G5) so the self-hiding today-modality cards surface on a fresh launch (using the same 7-day food window). They are **not** wired into the global `main.dart`/`sync_service` sweep (each surface owns its store's sync). See [decisions.md § 122](../../docs/architecture/decisions.md#122-gym--food-get-localgymstore--localfoodstore-mirroring-localgearstore-gym-stores-its-sets-inline-one-file-per-workout).
- `off_route_flag.dart` / `adaptive_fitness_flag.dart` / `weigh_in_flag.dart` — the other three fail-closed deploy-gate bindings, same shape as `nearby_flag.dart` below and mirroring web's `*_flag.ts` layer: each holds its key const plus a named gate getter that answers `false` on an uninitialised dotenv rather than throwing. **A surface imports the gate; it never spells `dotenv.env['KEY']` itself** — `env_flag_test.dart` fails on a literal read of a gate key outside its own module, and on a gate that throws uninitialised ([decisions § 822](../../docs/architecture/decisions.md)). The parse stays in its parity-pair file (`off_route_alert.dart` / `plan_adaptive_replan.dart`), so § 709's one accepted-affirmative set is untouched.
- `nearby_flag.dart` — the fail-closed `ENABLE_NEARBY_RUNNERS` deploy gate for opt-in coarse-location discovery (decisions §270 + §701). `nearbyRunnersEnabled(raw)` is the pure parse (`1`/`true`/`yes`/`on`, trimmed, case-insensitive — the same set web's `core/env_flag.ts#isTruthyFlagValue` accepts for `PUBLIC_ENABLE_NEARBY_RUNNERS`); `nearbyRunnersGate` is the dotenv read, which fails closed on an uninitialised dotenv rather than throwing past the gate. Deliberately its OWN file rather than a function in `nearby.dart` — that is a registered parity pair whose web twin carries no flag. `main.dart` forwards the define into dotenv, without which the flag would be unreachable in a release build.
- `settings_destination.dart` — the route/intent seam for "open a Settings sub-screen" (decisions § 710). `SettingsDestination` names the pushable settings screens and `openSettings(...)` parks the intent; `home_screen.dart` drains it and pushes with the dependencies it already holds, so an embedded surface needs none of them. Use it from any surface that would otherwise thread `Preferences` / `SettingsSyncService` / `ApiClient` down a widget tree purely to reach Settings, or that names a Settings path in prose. **Not** for a caller that must act when the screen closes — a parked intent is fire-and-forget and cannot report a pop, which is why `nutrition_screen.dart`'s `_openBodyMetrics` (await, then refresh) keeps its own `Navigator.push`, as does `run_detail_screen.dart`, which already carries the dependencies for its own reads. Callers today: the runners-nearby empty state on `people_screen.dart` and the age-estimated-max-HR caveat on `widgets/intensity_card.dart`.
- `preferences.dart` — SharedPreferences wrapper for settings. **`ActivityType` no longer lives here** — it moved to the leaf `packages/core_models/lib/src/activity_type.dart` on 2026-09-02 so `csv_run_importer.dart`, a pure parser, could reach the vocabulary without importing a file that also pulls in `package:flutter/material.dart` (decisions § 1013). The enum still carries only the physics (stride, GPS filter, calorie factor, max speed). Two members could not travel with it: **the display name** was never on it — a hardcoded English `label` getter printed "Run" / "Walk" / "Trail run" in every non-English locale, so it lives in `activity_type_labels.dart` behind an `AppLocalizations` (decisions § 547) — and **`IconData get icon`** joined it there as an extension, `core_models` having no Flutter dependency. **`splitIntervalMetresFor` is back ON the enum** since 2026-09-03 (decisions § 1040): it reads `DistanceUnit`, which used to be declared here, and it was an extension in this file only for as long as that was true. `DistanceUnit` and `kMetresPerMile` are now the leaf `packages/core_models/lib/src/distance_unit.dart`, so a file that needs either imports `package:core_models/core_models.dart` — this one no longer re-exports them, and Dart imports are not transitive. `check_constraint_unions.mjs` names the leaf paths for `runs.activity_type`, `challenges.activity_type` and `user_profiles.preferred_unit`, so moving a declaration again means editing that registry in the same change.
- `activity_type_labels.dart` — the ONE `runs.activity_type` display vocabulary (`activityTypeLabel(l10n, type)` + `activityTypeLabelFor(l10n, raw)`), Dart mirror of web's `runs/activity_type.svelte.ts`. Every screen that names an activity resolves through it; `hike` reads "Trail run" (the stored value stays `hike`). Pinned per locale, and against the SQL CHECK, by `test/activity_type_vocabulary_test.dart`.
- `auth_change_aware.dart` — the auth-transition seam for identity-bearing screens (decisions §252). A `State` mixin that subscribes to `ApiClient.authUserChanges` on mount and calls `onAuthUserChanged` only when the authoritative `ApiClient.userId` actually changed (token refreshes dedupe away; test fakes drive it by overriding `userId` + emitting a tick on an overridden stream). Consumers: `you_screen` (rebuild), `dashboard_screen` (drop + refetch per-user caches), `settings_account_screen` (reload avatar + coach consent), `home_screen` (re-arm the setup-wizard gate). **Adopt this instead of caching auth-derived state in `initState`** on any new screen that renders viewer identity or per-user server fetches.
- `fab_clearance.dart` — `fabScrollClearance(context, {fabCount})`, the measured bottom padding a scroll view owes the `FloatingActionButton` floating over it: `MediaQuery.padding.bottom + 16 + 56` per button (plus `56 + 12` for each extra one in a stacked column). Reads `padding`, not `viewPadding`, so an enclosing `SafeArea` or a parent `Scaffold` with a `bottomNavigationBar` doesn't get counted twice. Guarded in `architecture_guards_test.dart`; conventions § Mobile FAB clearance.
- `map_tile_readiness.dart` — `MapTileReadiness`, wired as `TileLayer.tileBuilder` through `basemapTileLayer`, so a share-card rasteriser can wait for the basemap tiles it is actually showing instead of sleeping. `settled({ceiling})` resolves the moment every observed tile carries a `loadFinishedAt` — which flutter_map stamps on a FAILED tile too, so an offline or 403 tile settles rather than running out the 8 s ceiling and exporting the same frame anyway. Replaced the fixed 700 ms sleep in `run_share_card.dart` + `route_share_card.dart` that baked a part-black map into a cold-fetch export (decisions §538). Tests in `test/map_tile_readiness_test.dart`.
- `adaptive_width.dart` — the Material width-class helper (`WidthClass.compact/medium/expanded`, breakpoints 600/840dp, `kContentMaxWidth` 720dp) behind every tablet/expanded recomposition (dashboard grid, NavigationRail shell, run-detail two-pane, runs card grid, feed/timeline/plan-detail content caps). Also owns **`contentColumn(context, body, {maxWidth})`** — the centre-and-cap idiom as one call, adopted by dashboard / plan_detail / feed / runs / settings_preferences / gym / nutrition / event_detail; `adaptive_width_test.dart` fails a screen that names `kContentMaxWidth` without it (decisions §538). A full-bleed map surface (route_detail, public_run, public_route, both heatmaps) deliberately takes no clamp; `club_detail` + `profile` clamp through `widgets/collapsing_tab_host.dart` rather than at the screen (decisions §545), which is why the adoption sweep checks the host separately. Wide branches gate on `expanded` only — the 800dp flutter_test surface must stay on the phone paths (decisions §256, conventions § Mobile adaptive width). Tests in `test/adaptive_width_test.dart`.
- `locale_defaults.dart` — locale-derived first-run defaults (`regionOfLocale` / `defaultUnitForLocale` / `defaultWeekStartForLocale` over the device locale). Dart twin of web `format/locale_defaults.ts` (TS↔Dart parity pair; the lockstep contract is the region tables — web additionally consults `Intl` week data). Seeds the setup wizard's units step + the preferences screen's week-start fallback. Tests in `test/locale_defaults_test.dart`.
- `ai_disclosure.dart` — the versioned AI-processing consent ladder (v1 Coach, v2 all AI features) + the fail-closed `checkAiDisclosure` grading. Dart twin of the client half of web `core/ai_disclosure.ts` (TS↔Dart parity pair); `test/ai_disclosure_test.dart` parses migration `20270511_001` so the constant cannot drift from `ai_disclosure_current_version()` in SQL. The record is read via `ApiClient.fetchAiDisclosure` (through `get_my_profile()` — both columns are outside the cross-user column grant, so a direct select is rejected) and written via `recordAiDisclosureConsent` / `withdrawAiDisclosureConsent`; the v1 `record_coach_consent()` wrappers have no client left. Hosts render `widgets/ai_disclosure_notice.dart`, never their own copy. Decisions § 571 + § 573.
- `auth_gates.dart` — pure `checkPasswordPair` + the `minPasswordLength` floor (a re-export of `kPasswordMinLength` from `auth_validation.dart`, 8, matching the server's `minimum_password_length`) for the sign-up confirm field. Dart twin of the `checkPasswordPair` half of web `core/auth_gates.ts` (TS↔Dart parity pair). At least the floor, then exact equality, length first; **neither side trimmed** — whitespace is a real password character and is exactly the typo class this catches. Web's `{ok} | {ok, reason}` union is a nullable reason here. Web's `checkSignUpGates` consent half has no twin — mobile's sign-up screen is separate from sign-in, so those gates stay inline in `sign_up_screen.dart` (`_checkGates`). Tests in `test/auth_gates_test.dart`.
- `password_change.dart` — pure `changePassword` step-up for the Settings → Account change-password dialog (issue #381, decisions §278). Dart twin of web `core/password_change.ts` (TS↔Dart parity pair). Reuses `checkPasswordPair` from `auth_gates.dart` (no reimplementation), then requires a non-empty current password, then a POSITIVE `ApiClient.verifyCurrentPassword` (re-auth over `signInWithPassword`) before `updatePassword` — a rejection, a throw, and a session with no email all fail closed identically, so a live token alone can't rotate the password (OWASP ASVS V2.1.14 / CWE-620). Web's `{ok, reason, detail?}` union is a small `PasswordChangeResult` class here. The dialog also offers "Email me a reset link" for OAuth-only accounts with no password to prove (lands on web `/auth/reset`). Tests in `test/password_change_test.dart`.
- `integration_visibility.dart` — whether a deployment may OFFER an integration. Dart twin of web `integrations/integration_visibility.ts` (TS↔Dart parity pair, 12 mirror tests each). Grades a gate (`env` / `probe` / `always` / `unsupported` / `unbuilt`) against its resolved verdict, fail-closed, and keeps a CONNECTED row visible whatever the gate says so it can still be disconnected. The settings screen renders its `_connectSpecs` through `visibleIntegrations`; see [decisions § 1621](../../docs/architecture/decisions.md). Tests in `test/integration_visibility_test.dart` + `test/settings_integrations_gate_test.dart`.
- `parkrun_regions.dart` — parkrun country footprint + `parkrunLikelyUnavailable`. Dart twin of web `integrations/parkrun_regions.ts` (TS↔Dart parity pair). Swaps the Settings → Integrations parkrun tile subtitle to a "may not have events near you" disclosure outside the footprint without gating the import. Tests in `test/parkrun_regions_test.dart` + `test/settings_integrations_parkrun_note_test.dart`.
- `text_limits.dart` — character caps on user-authored text (`kClubNameMaxLength` 80, `kClubDescriptionMaxLength` 2000, `kClubLocationLabelMaxLength` 80, `kDisplayNameMaxLength` 60), stated once and consumed by `widgets/club_form_sheet.dart` + `setup_wizard_screen` + `settings_account_screen`. Dart twin of web `core/text_limits.ts`; each number has a matching `*_len_chk` CHECK in migration `20270502_001` and `test/text_limits_test.dart` **parses that migration** to prove they match, because a composer capped above the constraint hands the user a postgres 23514 it cannot explain (decisions §545). Adding a cap? Add it here, to the TS twin, and to the migration together.
- `tile_cache.dart` — disk-backed map tile cache glue (general LRU `map_tiles`). Distinct from the eviction-resistant `offline_tile_pack.dart` sibling cache.
- `offline_tile_pack.dart` — offline map-tile packs for a route pinned "Save for offline" (trail_navigation.md, decisions §170). `OfflineTilePackStore` (a `ChangeNotifier`) downloads the `{z}/{x}/{y}` tiles covering the route bbox (via the pure `tile_pack.dart` `tilesForBbox` + 5000-tile cap) to `${cacheRoot}/offline_packs/<routeId>/` — a **sibling** of `map_tiles` so a pinned pack is never LRU-evicted. Hooked onto `route_detail_screen`'s offline-pin toggle (download on pin, `deletePack` on un-pin). `OfflinePackTileProvider` is the read-through `TileProvider` (disk pack first, network/LRU fallback) wired into `LiveRunMap` via the optional `offlineTileProvider` param. **L4:** each tile fetch is isolated (a failed tile → partial pack + retry, never aborts the pin or the online map); injectable `TileBytesFetcher` test seam. **Unpin-during-download is deterministic** (issue #401): `downloadPack` registers an in-flight future per route id and bails at each loop step on a per-route cancel flag; `deletePack` sets that flag, awaits the in-flight download, then clears `_packs` + deletes the dir — so a completed unpin can't be followed by resurrected tiles or a stale present-state. 9 tests in `test/offline_tile_pack_test.dart`.
- `turn_cues.dart` — pure geometric turn-cue generator over a route polyline (`generateTurnCues`). Dart twin of `apps/web/src/lib/routes/turn_cues.ts` (TS↔Dart parity pair, 14 mirror tests in `test/turn_cues_test.dart`). The offline-first turn-by-turn baseline (decisions §169). A turn is the NET direction change accumulated within `mergeWithinM` metres of where the turning starts, reported at the vertex where that turning passes its halfway point — a bearing is never measured across a segment that SPANS a corner, which is what made one 90° corner announce as two under-classified slights.
- `tile_pack.dart` — pure slippy-map tile-enumeration math (`tilesForBbox` / `estimateTileCount` + the per-pack cap). Dart twin of `apps/web/src/lib/routes/tile_pack.ts` (TS↔Dart parity pair, 8 mirror tests in `test/tile_pack_test.dart`). The fetch/write lives in `offline_tile_pack.dart`.
- `turn_cue_announcer.dart` — pure, Flutter-free decision core for turn-by-turn voice cues (`TurnCueAnnouncer`). Holds per-turn announce state and, given the runner's distance-along-route, decides which cue to fire (announce at ~300/100/0 m, once per band per turn, nearest turn only). Mobile-only (not a parity pair). Wired into `run_screen.dart`'s snapshot handler behind the `Preferences.turnByTurnCues` toggle; fires through the best-effort `_ttsCue` wrapper → `audio_cues.announceTurn`. 17 tests in `test/turn_cue_announcer_test.dart`.
- `audio_cues.dart` — TTS for splits and pace alerts, **locale-aware** (Epic C3): phrases read from the gen-l10n `tts*` keys for the active locale, engine language follows `ttsLanguageTag(activeLocaleTag)`. Pace alerts are paired with a `HapticFeedback.heavyImpact()` pulse in `run_screen.dart` (two pulses for "speed up", one for "slow down") so the cue registers with headphones paused. `announceTurn(direction, distance:)` speaks the turn-by-turn cue (decisions §169).
- `run_stats.dart` — pace/distance/split formatting helpers (tested)
- `pace_analysis.dart` — first-half vs second-half pacing for a run's track (`analysePacing`) plus per-split grade-adjusted pace (`gradeAdjustedSplitPaces`). Dart twin of `apps/web/src/lib/runs/pace_analysis.ts` (TS↔Dart parity pair). Halves are cut by DISTANCE, never by time — a time midpoint lands short of halfway on any run that faded — and both figures are also offered grade-adjusted via `grade_adjusted_pace.dart` so a climbing second half isn't read as a blow-up. `gradeAdjustedSplitPaces` takes the split LENGTHS rather than the splits, because `RunSplit` carries only a tick and a duration where web's `Split` carries `distance_m`; every split `computeSplitDurations` emits is exactly one tick long, so the caller hands over `List.filled(n, tickLength)`. Backs the pacing card + the grade-adjusted split lane on `run_detail_screen.dart`. 13 mirror tests in `test/pace_analysis_test.dart`.
- `goals.dart` — `RunGoal` model + pure `evaluateGoal` for dashboard goal cards (tested)
- `live_hub_client.dart` — HTTP push wrapper around the Go live-hub's `POST /v1/live/{run_id}/push` endpoint. `LiveBroadcaster` swaps to this path when `LIVE_HUB_URL` is in `dotenv.env`. Pluggable fetcher seam mirrors `routing.dart` / `elevation.dart` / `geocoding.dart`. See `apps/job_worker/internal/livehub/` for the server.
- `revenuecat.dart` — Dart port of `apps/web/src/lib/billing/revenuecat.ts`. RevenueCat native SDK wrapper for the in-app Pro purchase sheet. `isRevenueCatConfigured` + `configureRevenueCat` + `startProCheckout` + `managementUrl` + `proMonthlyPriceString` (the store-localised price for the Subscribe tile — Apple 3.1.1 / Play policy: the displayed amount must come from the store, not a hard-coded USD string), all gated on `dotenv.env['REVENUECAT_API_KEY_ANDROID']` / `_IOS` so unconfigured builds compile + the "Subscribe to Pro" tile in `settings_pro_screen` falls back to the existing web `/settings/upgrade` URL (and to the `$9.99` USD list price + regional note when no offering resolves). User-cancelled purchases map to `PurchaseResult.cancelled` so no red error toast on a normal dismissal. 8 unit tests pin the unconfigured behaviour. Live sheet still needs an RC dashboard / `pro_monthly` product / API keys on the operator side — see `docs/product/followups.md § #9`.
- `pro_sellable.dart` — the mobile half of the hollow-subscription gate (decisions §466). `fetchProPerks()` GETs `${WEB_BASE_URL}/app-capabilities.json` — the manifest the web build prerenders from the same `coachEnabled()` / `routeGenEnabled()` flags its own storefront reads — and returns `ProPerks{coach, routeGen}` whose `sellable` is `coach || routeGen`. Never throws: unreachable host, timeout, non-200, malformed body, missing field, and truthy-but-not-boolean field all resolve to `ProPerks.none`, so a client that can't establish a perk is live never offers a purchase. `parseProPerks` is the pure half (7 tests in `pro_sellable_test.dart`).
- `strava.dart` — Dart port of `apps/web/src/lib/integrations/strava.ts`. Strava OAuth helpers for the future native connect flow: `isStravaConfigured()` reads `STRAVA_CLIENT_ID` from `dotenv.env` (with the same 12345-placeholder check as the web); `stravaAuthUrl(redirectUri)` builds the `/oauth/authorize` URL with the `activity:read_all,read` scope and `approval_prompt=auto`. The connect button on `settings_integrations_screen` now drives a native `FlutterWebAuth2` flow when `STRAVA_CLIENT_ID` is in `dotenv` and falls back to the web `/settings/integrations` page otherwise. The `strava-import` Edge Function (wrapped by `ApiClient.syncStrava`) handles the code-for-token exchange. 7 unit tests on `isStravaConfigured` + URL building.
- `route_simplify.dart` — Ramer–Douglas–Peucker track simplifier used when saving a run as a route (tested). Also holds the mobile-only `simplifyToBudget` (priority DP to a fixed point count — the watch course push's 256-point budget; **outside** the `route_simplify.ts` parity pair, like `computeElevationLoss`) and `computeElevationGain` / `computeElevationLoss`.
- `routing.dart` — the sole client-side OSRM implementation, and deliberately NOT a lockstep parity pair: web's own `routes/routing.ts` lost `snapToRoad` / `fetchRoute` / `fetchFullRoute` in decisions § 1119 (nothing imported them) and is now the `/api/routes/osrm` proxy client alone, with `RouteBuilder.svelte` building its service paths inline. OSRM client for the in-app route builder: `snapToRoad(point, profile: foot|car)` for the nearest-road service + `fetchRouteThrough(points, profile, {cache})` for a road-snapped polyline through N waypoints. Pass a `RouteSegmentCache` (the builder holds one, clears it on Clear) so re-routing after adding one waypoint fetches only the new segment — keeps a long route responsive on every pin tap; only ok segments are cached (`segmentCacheKey` is order- + profile-scoped). Mirrors web `segment_cache.ts` — keep in lockstep (decisions §136). Pluggable `OsrmFetcher` callback so unit tests can replay canned bodies (`test/routing_test.dart`, 36 tests). Default fetcher uses dart:io's `HttpClient` (no new package dep). The public OSRM endpoint is unauthenticated and free for low volumes; if usage grows past the demo server's quota, swap the base URL for a self-hosted instance.
- `elevation.dart` — Dart port of `apps/web/src/lib/routes/elevation.ts`. Open-Meteo elevation client used by the route builder to render `N m ↑` while drawing. `fetchElevations` (batched at ≤100 points per request, falls through to all-zero on error), `calculateElevationGain` (sums positive deltas), `sampleCoordinates` (down-sample a dense polyline before the API call). Same pluggable-fetcher seam as `routing.dart`. 15 tests in `test/elevation_test.dart`.
- `geocoding.dart` — MapTiler Geocoding API client used by the three map screens' place-search. `searchPlaces(query, apiKey, limit)` returns a `PlaceSearchOutcome` — `ok` with up to N `PlaceResult`s, or `unavailable` when the provider failed / rate-limited / timed out / answered a malformed body. Never throws, so the search box stays off an error toast per keystroke, but the caller **must** render `unavailable` differently from `ok`-with-no-results: collapsing both into an empty list is how a reachable place came to read as a non-existent one, and because each dropdown only opened on a non-empty list, a failed lookup showed nothing at all. The web twin (`geocoding_math.ts`) carries a third `aborted` state for its AbortSignal call sites; here the debounce is a cancelled `Timer`, so a superseded keystroke never reaches this layer. Rendered by `widgets/place_search_panel.dart`. 32 tests in `test/geocoding_test.dart`.
- `geo.dart` — antimeridian-safe longitude arithmetic (`lonDeltaDeg` / `unwrapLonDeg` / `wrapLonDeg`). **EVERY local planar frame must take its east-west deltas through this** — anything that subtracts two longitudes and calls the result metres east, or takes min/max over longitudes for a bounding box. A raw subtraction is wrong by a whole turn across 180°, so a point just past the line reads ~40,000 km away and a box spans 359.9° (which collapses a fitted scale to a dot). Trigonometric formulae (haversine, great-circle bearing) are periodic and need no correction — that's why `privacy.dart` was untouched. Consumers: `route_geometry.dart`, `route_snap.dart`, `route_simplify.dart`, `widgets/track_preview.dart` (`projectTrack` + `isTrackRenderable`), `run_heatmap.dart`, `route_loop.dart`. Dart twin of `apps/web/src/lib/routes/geo.ts` (TS↔Dart parity pair), ported from the firmware's `watch_core::geo`. Decisions §463/§468/§473. 6 tests in `test/geo_test.dart`.
- `diary_day.dart` — which local calendar day the nutrition diary is showing, plus the fetch window, water-storage key and entry `started_at` that day implies. Dart twin of `apps/web/src/lib/nutrition/diary_day.ts` (TS↔Dart parity pair, 26 mirror tests in `test/diary_day_test.dart`). Every step goes through `DateTime(y, m, d + n)` so a 23- or 25-hour DST day cannot repeat or skip one (decisions § 589), and a future day always resolves back to today. Web's `DIARY_DATE_PARAM` is `/nutrition?date=` routing and has no mobile analogue; web's ISO-instant `DiaryWindow` and string-returning `entryTimestampFor` read as Dart `DateTime`s (`LocalFoodStore.createLocal` converts to UTC at its own boundary).
- `extended_nutrients.dart` — the day roll-up for the five nullable `food_log` nutrient columns (fibre / sugar / sodium / saturated fat / cholesterol). Dart twin of `apps/web/src/lib/nutrition/extended_nutrients.ts` (TS↔Dart parity pair, 22 mirror tests in `test/extended_nutrients_test.dart`). Coverage — not arithmetic — is the point: a total carries how many entries reported it, the monotone `exceeded` / `reached` claims survive partial coverage, and `remaining` is withheld outright when any logged entry didn't report the nutrient. Backs the self-hiding **Nutrients** section on `nutrition_screen.dart` under the water card.
- `distance_bands.dart` — race-distance bands (5K/10K/Half/Marathon/Ultra) for `routes_heatmap_screen`. Dart twin of `apps/web/src/lib/routes/distance_bands.ts` (TS↔Dart parity pair) — `bandForDistance` badges list rows, `bandsToRanges` builds the parallel min/max arrays the `discoverable_routes_in_bbox` RPC takes. 9 tests in `test/distance_bands_test.dart`.
- `heatmap_clustering.dart` — pure-Dart pixel-proximity pin clustering for `routes_heatmap_screen` (flutter_map has no native clustering and web gets it free from MapLibre; this avoids a new twin dep). `clusterPins` greedily merges pins within `radiusPx` at the current zoom via an injected projector. 6 tests in `test/heatmap_clustering_test.dart`.
- `run_heatmap.dart` — pure grid-quantised aggregation for the personal run-track heatmap (`run_heatmap_screen.dart`). Dart twin of `apps/web/src/lib/routes/run_heatmap.ts` (TS↔Dart parity pair) — `buildHeatCells` collapses many tracks' points into weighted cells (clamped at `kMaxCellWeight`), `heatBounds` returns the `[[w,s],[e,n]]` fit box. The web GeoJSON emitters are MapLibre-specific and have no Dart twin (the mobile renderer consumes `HeatCell`s + tracks directly). 10 tests in `test/run_heatmap_test.dart`.
- `route_overlap.dart` — `detectOverlapSpans(polyline, toleranceM, maxScannedPoints)` walks segment pairs with a haversine endpoint + midpoint test plus a path-distance gap gate so dense polylines don't false-positive. Down-samples to ≤200 points for the O(n²) scan. Used by the route builder to render a purple over-stroke on out-and-back retraced sections. 8 tests in `test/route_overlap_test.dart`.
- `widgets/snap_to_start.dart` — `shouldSnapToStart(tap, existingWaypoints, toleranceM)` — pure helper for the route builder's loop-close affordance. Returns true when ≥ 3 existing waypoints AND the tap is within `kSnapToStartMetres` (30 m default) of the first one. The builder uses it both to decide whether to close the loop on tap and to flag the start pin's pulsing halo on. 15 tests in `test/snap_to_start_test.dart`.
- `import_failures.dart` — the classified failure log every bulk importer records into (`classifyImportFailure` → network / auth / rate_limited / too_large / unparseable / rejected / unknown, a 200-entry cap counting its overflow, and the shareable CSV). Dart twin of `apps/web/src/lib/integrations/import_failures.ts` (TS↔Dart parity pair, 22 mirror tests in `test/import_failures_test.dart`). Rendered by `widgets/import_failure_report.dart`.
- `health_connect_importer.dart` / `strava_importer.dart` / `csv_run_importer.dart` — bulk importers (the CSV path round-trips Settings → "Export runs as CSV"; lossy, no track; offline-first; idempotent via `csv:<iso>-<dist>-<dur>` external_id — see `decisions.md § 65`). **All three derive the local `Run.id` deterministically from the stable `external_id` via `imported_run_id.dart#stableRunIdFromExternalId`** so a same-source re-import maps to the same local id — `LocalRunStore.save` (id-dedupe) replaces the prior copy instead of duplicating, and `saveRunsBatch`'s `(user_id, external_id)` upsert never rewrites the server primary key (decisions § 267, #361).
- `imported_run_id.dart` — pure `stableRunIdFromExternalId(externalId)` (v5 UUID over a fixed namespace). The shared idempotency helper for the three importers above. Tests in `test/imported_run_id_test.dart`.
- `shared_file_import.dart` — OS "Open with" / share-target GPX/KML → route import (device-led, decisions §297). `SharedFileImportService` receives a file handed to the app by another app (WhatsApp / Files / Drive) via `receive_sharing_intent` and imports it as a route through the shared pure `routeFromImportedFile` (also used by `routes_screen`'s file-picker import — single source of truth) + `detectRouteFormat` (extension, else a `<gpx`/`<kml` content sniff for the octet-stream / extensionless case). **The read path is BYTES**, not text, since 2026-09-02: both entry points `readAsBytes`, the two `compute` requests carry a `Uint8List`, and `routeTextFromImportedBytes` turns them into `(format, content)` before the dispatch — which is what makes KMZ importable at all, since a KMZ is a zip and `readAsString` throws on one before any parser is reached. A KMZ is unwrapped there (recognised by the zip magic number, because an OS share drops the extension; `doc.kml` preferred, any `.kml` accepted) and the dispatch never sees `kmz` (decisions § 1025). Success / failure surface to `home_screen` via the global `incomingRouteImport` notifier (banner + open the route on `RouteDetailScreen`) — the `pendingStartWorkout` handoff pattern. Started in `main.dart`; the OS registration lives in the native projects (AndroidManifest `ACTION_VIEW` / `ACTION_SEND` intent-filters — `octet-stream` included because WhatsApp shares GPX as that; iOS Info.plist `CFBundleDocumentTypes` + imported UTIs). The iOS system *share sheet* needs a Share Extension target (deferred). Tests in `test/shared_file_import_test.dart`.
- `health_connect_exporter.dart` — the write-back inverse of the importer (persona #36). `writeRun(run)` maps `metadata.activity_type` → `HealthWorkoutActivityType` (pure `healthWorkoutTypeForActivity`) and calls `health.writeWorkoutData` (ExerciseSession + Distance), then best-effort writes heart rate (persona round-5): per-point chest-strap BPM from the track, or a single `metadata.avg_bpm` sample when no per-point data — sample selection is the pure `heartRateSamplesForRun(run, start)`. Needs `WRITE_HEART_RATE` (added to `AndroidManifest.xml` + `res/xml/health_permissions.xml`) alongside `WRITE_EXERCISE`/`WRITE_DISTANCE`. The HR write is wrapped in its own try/catch so a missing grant can't roll back the workout write. Opt-in per device via `Preferences.writeToHealthConnect` (off by default), toggled from Settings → Integrations; `run_screen` fires a best-effort fire-and-forget write after the local save, Android-only. iOS/HealthKit write path is gated off (needs separate entitlements). 9 tests in `test/health_connect_exporter_test.dart` (mapping + HR-sample selection).
- `widgets/live_run_map.dart` — live map with route overlay, off-route banner, NRC-style pace heatmap (when `activity` is non-null, falls through to the legacy single gradient polyline otherwise). On the run-detail surface (`showDecorations: true`) it also overlays the per-km / per-mile distance pins and the direction-arrow chevrons via `widgets/track_decorations.dart` so the trace mirrors the web run-detail map. Live recording leaves these off — the pulsing dot already makes direction implicit.
- `widgets/track_decorations.dart` — pure helpers `computeDistanceMarkers` + `computeChevrons` that walk a polyline and emit km-/mile-boundary pins and direction arrows along the track. Mirrors the `computeDistanceMarkers` + `trace-arrows` symbol layer on `apps/web/src/lib/components/RunMap.svelte`. 10 unit tests in `test/track_decorations_test.dart`.
- `widgets/track_segment.dart` — pure helpers backing the tap-to-select segment popup on the run-detail map. `nearestTrackIdx` is a linear scan, `buildSegmentAt` expands ±150 m around a click index and aggregates distance / duration / pace / avg BPM / elevation gain + loss for the slice. Mirrors `buildSegment` in `apps/web/src/lib/components/RunMap.svelte` — keep both paths in sync so a click on web and a tap on mobile produce identical stats. 9 unit tests in `test/track_segment_test.dart`.
- `widgets/pace_segments.dart` — pure helpers `buildPaceSegments` / `paceBucketForSpeed` / `ageBandFor`. Pace-coloured, age-faded segment builder for the live track. No Flutter state — unit-tested in `test/pace_segments_test.dart`
- `widgets/track_preview.dart` — pure `CustomPainter` thumbnail mirroring the SVG geometry of `apps/web/src/lib/components/TrackPreview.svelte` (viewBox short axis = 100, PAD = 4, four directional chevrons, green-start / red-end caps). `projectTrack` is the testable pure-math projection (cos(midLat) longitude correction so a square loop renders square at any latitude — see [decisions.md § 51](../../docs/architecture/decisions.md)); `isTrackRenderable` is the 5 m jitter guard, antimeridian-unwrapped, in lockstep with `isTrackRenderable` in web's `routes/track_projection.ts` (decisions §473). Mounted on `routes_screen`, `explore_routes_screen`, and the Routes tab of `club_detail_screen` (routes carry waypoints inline) and as the inner renderer for `RunTrackPreview`. See [decisions.md § 49](../../docs/architecture/decisions.md).
- `widgets/run_track_preview.dart` — lazy track-fetcher around `ApiClient.fetchTrackByPath` with a static URL→`List<Waypoint>?` cache (the `null` entry is a "fetch failed" sentinel so broken Storage objects don't get retried on every rebuild). Mounted on the run-list rows in `runs_screen` and the per-card thumbnail strip in `feed_screen`. Mirrors `apps/web/src/lib/components/RunTrackPreview.svelte`.
- `widgets/collapsing_tab_host.dart` — the hero-scrolls-away-over-a-pinned-tab-strip shape `club_detail_screen` + `profile_screen` wear (decisions §545). A `NestedScrollView` whose header slivers are the hero (plus a whole-surface banner) and whose strip is a `SliverAppBar(primary: false, pinned: true, toolbarHeight: 0, bottom: AppTabBar(...))` inside a `SliverOverlapAbsorber`, wrapped in `contentColumn` so the strip and the body it labels share one clamped column. **Every tab body must be a real scrollable on `AlwaysScrollableScrollPhysics`** — not for the header collapse (the outer scrollable takes that drag either way) but so the body can scroll its OWN content: `TabBarView` hands each child a bounded box, and a bare `Center` empty state overflowed it by 620 px at 2.0x on a 320 dp phone. ui_kit's `EmptyState` under bounded height already IS that scrollable; use it rather than re-solving it. Each child owes a `Padding(top: AppTabBar.height)` back to the absorber — a fully pinned header's obstruction is its min extent, so the inset is constant and `collapsing_tab_host_test.dart` pins the equality. Tests in `test/collapsing_tab_host_test.dart` (6).
- `widgets/collapsible_panel.dart` — the run screen's expandable stats panel
- `widgets/run_share_card.dart` — portrait share card + modal sheet; captures a PNG via `RepaintBoundary.toImage` and hands it to `share_plus`
- `widgets/full_screen_form.dart` — the shared create/edit-entity presentation. `showFullScreenForm<T>(context, title:, builder:)` pushes a `MaterialPageRoute(fullscreenDialog: true)` wrapping the body in `Scaffold` + `AppBar(title)` + `SafeArea`; `FullScreenFormBody` is the scrollable column chrome (20px inset that grows to clear the soft keyboard `viewInsets` else the gesture/nav bar `viewPadding`); `FormSectionLabel` is the uppercase muted group label. Every add/edit form routes through it (gear, club, event, goal, gym, planned-workout edit) so the surface is one consistent full-screen dialog. See [decisions.md § 129](../../docs/architecture/decisions.md).
- `widgets/log_sheet.dart` — the Log capture sheet (G5): `showLogSheet` → `LogAction?` (run / lift / food — one per modality, mirroring web; food is a single entry, the meal slot is picked in the composer as on web's `/nutrition/log`). Pure `orderedLogActions(recent)` floats the most-recently-used to the top; `logActionFromWire` / `.wire` round-trip `Preferences.lastLogType`. A single Semantics focus group; the caller does the navigation so the sheet stays store/api-free. **Used by the History Log FAB (`runs_screen.dart`)**; the centre nav Log FAB uses the `log_speed_dial.dart` variant instead.
- `widgets/log_speed_dial.dart` — the centre-Log speed-dial (G5): `showLogSpeedDial` → `LogAction?`, an `Overlay`-based fan of the three actions as **icon-only buttons in a shallow arc around the docked FAB** (`_kSlotUnits`: top-centre / down-left / down-right) over a dismiss scrim, each icon riding out along its slot vector on a staggered entrance. `orderedLogActions` puts the recent action in the top-centre slot. Labels are on `Tooltip` + `Semantics` (no visible text), so tests find them via `find.byTooltip`. Same resolve-the-picked-action / caller-navigates contract as `showLogSheet`, so `home_screen`'s `_openLogMenu` swaps one for the other. Reuses `LogAction` / `orderedLogActions` from `log_sheet.dart`; no new i18n keys (reuses `logRun`/`logLift`/`logFood`/`logSheetTitle`). Distinct from the History Log FAB, which keeps the bottom sheet.
- `widgets/gym_summary_card.dart` — Home "today's lift" card (G5): title + exercise count + unit-aware volume (via `gymExerciseCount` / `gymWorkoutVolume` from `gym_screen.dart`), dumbbell glyph + label, tap → `GymScreen`. Self-hiding is the dashboard's job.
- `widgets/nutrition_rings_card.dart` — Home "today's nutrition" card (G5): the four macro rings (kcal/P/C/F) vs targets; unfilled (raw consumed numbers, no zeroed target) when targets are absent; tap → `NutritionScreen`.
- `widgets/recent_lifts_card.dart` — dashboard "Recent lifts" trend card: the five most-recent gym sessions (date + title + exercise count + unit-aware volume), each row tapping into `GymDetailScreen`, a "View all" link to `GymScreen`. Sits below the training-load chart; self-hides when the gym store is empty. Mirrors web `/dashboard`'s recent-lifts card.
- `widgets/activity_timeline_list.dart` — the unified History timeline (multi_modal.md § History): a day-grouped, reverse-chronological list of `ActivityRow`s. Source-agnostic — `runs_screen.dart` feeds it rows assembled from the local stores (`lib/local_activities.dart`); the `summary` map keys match what the server `activities` view would emit. Type-coded by leading glyph + label (run / lift / meal), run/lift rows tappable (chevron) + meal rows read-only. Grouping is the pure `lib/activity_timeline.dart` (`groupActivitiesByDay`). Hosted by `runs_screen.dart`'s History tab.
- `local_activities.dart` — pure `buildLocalActivities({runs, workouts, foods, limit})` → newest-first `List<ActivityRow>` assembled from the three offline-first local stores. The History timeline's data source (replaced the server `fetchActivities`): always all modalities, offline, live. **Bounded-build:** each source is **required to arrive newest-first** (which `LocalRunStore.runs` / `LocalGymStore.workouts` / `LocalFoodStore.rows` all guarantee), so it caps each source at `limit` *before* building rows — a rebuild is O(limit log limit) regardless of total history, instead of allocating an `ActivityRow` for every run/lift/meal and sorting the lot on every store-change rebuild. Don't feed it unsorted lists. Unit-tested in `test/local_activities_test.dart`.
- `activity_timeline.dart` — pure `groupActivitiesByDay(List<ActivityRow>)` → per-local-day buckets for `widgets/activity_timeline_list.dart`. Mirrors the web History `timelineGroups` derivation; unit-tested.
- `widgets/gear_form_sheet.dart` — full-screen-dialog gear add/edit form (`showGearFormSheet` → `GearFormResult?`, null on cancel). Name / brand / model / purchased-at / unit-aware retirement target / notes. Writes through `LocalGearStore` (offline-first). Mounted from `gear_screen`.
- `widgets/goal_editor_sheet.dart` — full-screen dialog (via `showFullScreenForm`) for creating/editing/deleting a `RunGoal` (type + period + target)
- `widgets/nutrition_log_sheet.dart` — fullscreen-dialog nutrition log composer (`showNutritionLogSheet` → `NutritionLogSheet`). Flutter twin of web `/nutrition/log`: meal-slot dropdown, debounced Open Food Facts search → tap a result → confirm-portion `AlertDialog` → log, with a manual macro-entry fallback. **Camera barcode-scan fast-path (2026-06-20, decisions §180):** a Scan-barcode `IconButton` opens a `_BarcodeScanScreen` (full-screen `MobileScanner`, EAN/UPC formats) → on detect, `food_search.dart#lookupBarcode` resolves the product → on match drops into the same `_PortionDialog`; no-match shows the not-found message, scan/lookup failure shows a distinct scan-failed message, both keeping the search / manual fallback. The whole scan+lookup is wrapped in one try/catch (L4 — never breaks manual logging); camera-permission denial renders an inline message + `openAppSettings()`. Injectable `BarcodeScanner` test seam (returns a canned raw code) drives the lookup path without a camera; the camera widget itself isn't unit-tested. Writes through `LocalFoodStore.createLocal` so logging works offline; injectable `fetcher` test seam. Resolves `true` when an entry was logged (caller kicks a sync). `food_search.dart` (Open Food Facts client — search `searchFoods` + barcode `lookupBarcode`/`parseOffProduct`/`normaliseBarcode`, injectable fetcher like `routing.dart`) + `nutrition_totals.dart` (`sumMacros` / `groupByMealSlot` / `ringFraction`) are the pure helpers behind the screen + sheet — Dart twins of the web `food_search.ts` / `nutrition_totals.ts`. Camera dep: `mobile_scanner` (BSD-3) + Android `CAMERA` permission / iOS `NSCameraUsageDescription`.
- `widgets/gym_compose_sheet.dart` — fullscreen-dialog gym-workout composer (`showGymComposeSheet` → `GymComposeSheet`). Flutter twin of web `GymEditor.svelte`: optional title, free-text exercise name with `RawAutocomplete` over the user's history, inline sets (reps / weight-kg / RPE), add/remove exercise + set, share-to-feed toggle. Writes through `LocalGymStore.createLocal` / `updateLocal` so logging a lift works offline. Resolves `true` when a workout was saved (the caller kicks a sync). Mounted from `gym_screen` (new) + `gym_detail_screen` (edit). The optional `seedSets`/`seedTitle` prefill a NEW log on the create path — the gym-programming P1 "Start routine" / "Repeat last" entry seeds the composer with a routine's planned targets (or a prior session's sets) as the new session's actuals. **The catalogue is read off `widget` on every build and reduced through `dedupeShadowedExercises`** (decisions § 1513) — never snapshotted in `initState`, because the host fills it from an async read, and never merged by `id`, because a custom created from the picker under a seeded global's name is a second id under ONE folded key and holding both leaves the key map deciding which id a logged set binds to. `catalogueUnavailable` rides alongside it: the browse affordance stays reachable while the catalogue is unknown (a feature that silently vanishes on a transient error explains nothing) and `widgets/exercise_catalogue_picker.dart` behind it refuses every create.
- `widgets/routine_builder_sheet.dart` — fullscreen-dialog routine builder (`showRoutineBuilderSheet` → `RoutineBuilderSheet`, resolves the new routine's id). Flutter twin of web `RoutineEditor.svelte` (+ the `gym_compose_sheet.dart` idiom): routine name, free-text exercise with `RawAutocomplete`, inline planned sets (target reps / weight), add/remove exercise + set. `normaliseExerciseName` stamps the `exercise_key`; weight entry honours the `weight_unit` pref. Writes through `LocalRoutineStore.createLocal` (offline). gym-programming P1 — build / save only, no execution / supersets / progression.
- `widgets/upcoming_event_card.dart` — Run tab idle-state card shown when the user has a `going` RSVP within 48h
- `widgets/todays_workout_card.dart` — Run tab idle-state priority card when an active plan has a workout scheduled today
- `widgets/plan_calendar.dart` — Dart port of `apps/web/src/lib/components/PlanCalendar.svelte`. Month-by-month grid with prev/next chevrons, Monday-first DOW row, kind-coloured cells, completed-tick. Renders on `plan_detail_screen` between the today card and the week cards.
- `widgets/current_week_strip.dart` — Dart twin of `apps/web/src/lib/components/CurrentWeekStrip.svelte`. Focused current-week 7-day planned-vs-completed ribbon, Monday-first, kind-coloured cells + completed-tick + a `done / active` count. The window is anchored to `startDate + weekIndex*7` (the plan's week bucket, NOT the calendar week) so the count matches the week card. Renders on `plan_detail_screen` between the re-plan section and the month calendar; tapping a cell opens the workout. 4 widget tests in `test/current_week_strip_test.dart`.
- `widgets/this_week_strip.dart` — Dart twin of `apps/web/src/lib/components/ThisWeekStrip.svelte`. Dashboard activity ribbon for the REAL calendar week containing today (7 day cells, busiest-day-scaled bars, today highlight + future dimming, header distance + activity count). Honours the `week_start_day` pref + the user's `DistanceUnit`; day-bucketing lives in the pure `current_week.dart` helper. Distinct from `current_week_strip.dart` (plan-anchored). Mounts on `dashboard_screen.dart` below the period-stat row; 6 widget tests in `test/this_week_strip_test.dart`.
- `widgets/workout_edit_sheet.dart` — full-screen dialog (via `showFullScreenForm`) for inline edit of a `PlanWorkoutRow` (kind, target distance, target pace, notes). Opened via the pencil button or long-press on a workout row in `plan_detail_screen`; saves through `TrainingService.updateWorkout`.
- `widgets/workout_execution_band.dart` — top-of-map overlay shown while a structured workout is running. Reads through a `ValueListenable<WorkoutBandState>` so transitions / progress events don't trigger a Stack-wide rebuild. Surfaces the current step label, pace pip with signed delta, progress bar + remaining metres, and Skip / Abandon controls.
- `widgets/safety_nudge_banner.dart` — the persistent solo-run safety prompt on the recording screen (docs/features/safety.md, issue #264). A dismissible in-tree card (sibling of the off-route / GPS banners), gated on `run_screen._safetyNudgeVisible`, replacing the old 6-second auto-dismissing top banner that was easy to miss. **Share** shares a live link (`_onSafetyNudgeShare`), **Not now** dismisses (`_dismissSafetyNudge`); both stamp the acted-on throttle. Decision logic is the twinned `safety_nudge.dart#shouldSurfaceSoloSafetyNudge`. 3 widget tests in `test/safety_nudge_banner_test.dart`.
- `widgets/live_share_indicator.dart` — the persistent live-share indicator on the recording screen (docs/features/safety.md, issue #613). A standing "Live" pill in the recording chrome (top-left), gated on `run_screen._liveShareActive`, shown whenever a live broadcast is active. Tapping it opens `showLiveShareSheet` with **Share link again** (re-hands the `/live/{run_id}` URL to the system share sheet) and **Stop sharing** (`concludeLiveBroadcast` — concludes the broadcast mid-run without ending the run). The share persists until the run finishes: the run screen is a keep-alive tab, so tab-switch / minimize never tears the broadcast down.
- `widgets/workout_review_section.dart` — post-run planned-vs-actual table on `run_detail_screen`. Reads `runs.metadata.workout_step_results` + `workout_adherence`; hidden when those keys are absent. Mirrors the web `/runs/[id]` Workout section. Pure widget + helpers (`paceDeltaOf`, `formatPace`) — no controller state, widget-tested.
- `widgets/run_social_section.dart` — kudos pill + one-level comment thread + composer for `run_detail_screen`. Self-fetches engagement and `fetchRunCommentsWithAuthors` on mount. Optimistic kudos toggle with rollback. Replies bucket client-side; author/owner gate the Delete affordance. Mirrors `apps/web/src/lib/components/RunSocial.svelte`.
- `widgets/run_photos.dart` — run-detail photo gallery. Owner gates Add photo (uses `image_picker` to pick from gallery, runs the bytes through `stripJpegExif` to drop EXIF/XMP GPS before upload — persona #52 — then uploads via `ApiClient.addRunPhoto` to the `run-photos` Storage bucket), inline caption edit + delete. Tap a tile for a full-screen `InteractiveViewer` lightbox. Mounted on both `run_detail_screen` (owner: full controls) and `public_run_screen` (read-only when viewer is not the owner). Mirrors `apps/web/src/lib/components/RunPhotos.svelte`.
- `widgets/route_photos.dart` — route-detail photo gallery (backlog C1 — the `run_photos` capability applied to routes). Same shape as `run_photos.dart`: owner gates Add photo (EXIF-stripped, uploaded via `ApiClient.addRoutePhoto` to the private `route-photos` bucket), caption edit, delete, lightbox. Mounted on `route_detail_screen` (owner controls) and `public_route_screen` (read-only for non-owner). Mirrors `apps/web/src/lib/components/RoutePhotos.svelte`. Helpers pinned by `test/route_photos_helpers_test.dart` (2 tests).
- `widgets/event_photos.dart` — the multi-attendee event photo gallery on `event_detail_screen.dart` (persona #49; mirror of the web event-detail Photos section). Reads via `ApiClient.fetchEventPhotos` (which selects the run-less `EventPhotoView` so a private run's id can't leak, + a batched uploader-name join) and renders a grid (thumb + caption + uploader) with a full-screen lightbox. Any signed-in viewer can add: a finisher attaches to their own event run (`_myEventRunId` from `_results`), everyone else picks a recent run via a bottom-sheet (`SocialService.fetchRecentRuns`); the upload reuses the `run_photos.dart` EXIF-strip + `addRunPhoto`'s new `eventId`/`eventInstanceStart` params + the signed-URL path. Anon viewers see it read-only. Widget tests in `test/event_photos_test.dart` (empty/anon/seeded render, finisher-direct add, recent-run-picker add).
- `widgets/route_conditions.dart` — route-detail community condition-reports panel (trail_navigation.md, decisions §171). Lists reports newest-first (condition chip + severity colour + relative age + optional "at X km" + note, fading a report older than 30 days) with a report composer for a signed-in viewer who can see the route. Reads through `ApiClient.fetchRouteConditions` (the privacy-redacting RPC; failures fall back to empty, L4); author-or-owner gates Delete (`AlertDialog`). Mounted on `route_detail_screen` (full) + `public_route_screen` (`canReport: false`). Mirrors `apps/web/src/lib/components/RouteConditions.svelte`. 5 widget tests in `test/route_conditions_test.dart`.
- `calendar_intent_test.dart` — 30 tests for `lib/calendar_intent.dart`: the `buildRrule` group (weekday ordering, the fortnightly interval, BYMONTHDAY, the monthly-past-the-28th withholding, COUNT / UNTIL, a local-zone UNTIL converted rather than stamped as written, COUNT winning over UNTIL); the `calendarSeriesFor` group (a one-off has no series; the anchor is the first occurrence rather than the Tuesday a Saturday series was created on; a sub-second `starts_at` still anchors on its own first occurrence and does not cost the series an occurrence; `until` + `count` resolving to whichever binds; called-off occurrences ahead reported and past ones not); and the `addToDeviceCalendar` group over a mocked channel (UTC epoch-ms, no `endMs` without a duration, a refused hand-off reported as false, a throwing channel and a missing native handler both false rather than propagating).
- `event_detail_calendar_test.dart` — 8 widget tests for the calendar actions on `event_detail_screen.dart`: a one-off offers only the single-date hand-off; the payload carries title / start / end / location / description / share URL and no rule; a recurring event also offers the series and sends an RRULE; called-off occurrences are disclosed and a clean series says nothing; a past and a called-off occurrence each offer nothing; a refused hand-off surfaces the banner. Occurrence fixtures are whole-second (the expansion stamps at that resolution) and the recurring cases scroll the lazy ListView past a year of chips.
- `event_photos_test.dart` — 5 widget tests for `widgets/event_photos.dart` (fake `ApiClient` subclass overriding `fetchEventPhotos` + `addRunPhoto`): empty gallery + canAdd renders the title/Add-photo/empty-hint; an anon viewer's empty gallery renders nothing; seeded photos render tiles with uploader name + the "A runner" null-uploader fallback; a finisher add attaches to `myEventRunId` (asserts `addRunPhoto` gets the run id + `eventId`/`eventInstanceStart`); a non-finisher add opens the recent-run picker sheet then uploads against the picked run.
- `widgets/club_form_sheet.dart` — full-screen-dialog create-club form (name, description, location, public/private, join policy). Auto-slugifies the name; visibility ↔ policy linking matches web `ClubEditor.svelte`. Mounted from the FAB on `clubs_screen.dart`.
- `widgets/event_form_sheet.dart` — full-screen-dialog create-event form. **Typed events (slice E):** a `category` picker (run/cycle/class/social) is the first control, a free-text `discipline` field shows only for a `class`, and the athletic distance field self-hides + clears off the athletic categories via `isAthleticEventCategory` (mirrors web `EventEditor.svelte`'s `pickCategory`); `SocialService.createEvent`/`buildCreateEventBody` persist `category`/`discipline`/`gym_template`. Plus title, starts-at picker, description, meet label, distance / duration, recurrence none/weekly/biweekly/monthly with auto-byday from the picked DOW. **Members-only toggle (§148):** a `SwitchListTile` shown only when `clubIsPublic` (threaded from `club_detail_screen` as `c.row.isPublic`) writes `events.is_public=false` via `buildCreateEventBody`; hidden for a private club whose events are already members-only. Mounted from the admin Create button on the Events tab of `club_detail_screen`.
- `widgets/run_segment_efforts.dart` — per-run segment-effort chips on `run_detail_screen`. Famous-segment chips push `GlobalSegmentDetailScreen` (decisions § 693) — before that they computed a rank and led nowhere on the phone. On mount, when the viewer owns the run and it's linked to a saved route, walks the track via `lib/segments.dart#autoComputeEffortsForRun` to insert any new efforts (idempotent against the unique(segment_id, run_id) constraint), then fetches the joined effort + segment + rank rows via `ApiClient.fetchEffortsForRunWithSegments`. Mirrors `apps/web/src/lib/components/RunSegmentEfforts.svelte`.
- `widgets/segments_panel.dart` — route-detail segments panel on `route_detail_screen`: lists every segment, expand-on-tap to a per-segment leaderboard via `fetchSegmentLeaderboardWithAthletes`. Route owner gets a "New segment" form (name + start_m + end_m, ≥100 m enforcement) and a per-row delete button (uses `ApiClient.deleteSegment`). Mirrors `apps/web/src/lib/components/SegmentsPanel.svelte`.
- `screens/global_segments_screen.dart` — the famous-segment catalogue (decisions § 693), mirroring web's `/segments`. `GlobalSegmentsScreen` browses the active catalogue over the `catalogue_browse.dart` parity pair: accent-folded search across name + region, region and surface dropdowns built from the rows themselves, and four sorts (name / shortest / longest / most climb) where a row missing the sorted column goes last under every direction. `GlobalSegmentDetailScreen` in the same file renders the curated polyline through `TrackPreview` — public catalogue geometry, so no `fetchClippedTrackForRun` — plus the gender / age-band leaderboard and `crownLabel`. Reached from the Runs peer strip and from the run-detail famous-segment chips.
- `widgets/training_load_chart.dart` — dashboard "Fitness, Fatigue & Form" `CustomPaint` chart. Reads HR prefs from `SettingsService` (`resting_hr_bpm` + `max_hr_bpm`); renders three line series (CTL / ATL / TSB) over the last 90 days plus a dashed zero line, a legend, and a one-line reading. Mirrors `apps/web/src/lib/components/TrainingLoadChart.svelte`.
- `widgets/intensity_card.dart` — dashboard "Training Intensity" card showing time-in-zone (Z1..Z5) over the last 30 days. Self-hides when the user has no `hr_zones` configured or no HR-tracked runs in the window. Renders a 5-segment bar + per-zone legend with rounded percentages (uses `<1%` rather than `0%` for non-empty slivers). Backed by `lib/run_intensity.dart`. Mirrors the inline intensity-breakdown card on web's `/dashboard`.
- `widgets/mileage_trend_card.dart` — dashboard "Mileage" card with a weekly / monthly / yearly SegmentedButton toggle (12-bucket window in each mode). Renders a bar chart of total distance per bucket; bar height is fraction of the tallest bucket, numeric value above the bar honours the user's `DistanceUnit`. Self-hides when there are no runs. Mirrors web's mileage-chart card on `/dashboard` — same Monday-anchored ISO-week grouping, same metres-stored / unit-formatted-on-render split.
- `run_intensity.dart` — pure helpers backing `widgets/intensity_card.dart`. `computeIntensityBreakdown(runs, zones, windowDays, now)` classifies each run by `metadata.avg_bpm` against the supplied 5-cutoff ladder; `parseHrZones(raw)` parses the universal-bag `hr_zones` map into a strictly-ascending 5-element list (shared with `run_detail_screen` for the per-run HR-zone panel).
- `mileage_trend.dart` — pure helper backing `widgets/mileage_trend_card.dart`. `aggregateMileage(runs, view, now, maxBuckets)` groups runs by `MileageView.{weekly, monthly, yearly}` and returns the most-recent N `MileagePeriod` buckets (label + summed `distanceM` + `startsAt` sort key) in chronological order. Weekly buckets are Monday-anchored to match `lib/goals.dart`.
- `training_load.dart` — pure Dart port of `apps/web/src/lib/training/training_load.ts` (decisions §34). Same TRIMP-when-HR / distance-fallback stress ladder, same daily aggregation, same EWMA trio (ATL halflife 7, CTL halflife 42); `hasTrimpSignal` drives the "HR-based" vs "volume-based" subtitle.
- `segments.dart` — pure Dart port of `apps/web/src/lib/segments/segments.ts` (decisions §37). `computeEffortFromTrack` walks a track once, accumulates haversine distance, and interpolates timestamps at the start / end crossings; `autoComputeEffortsForRun` fans the walker over every segment on a route and posts new efforts. Sparsity guard mirrors web (median sample step must be ≤ segLen / 5). 8-test mirror suite in `test/segments_test.dart`.
- `privacy.dart` — pure Dart port of `apps/web/src/lib/routes/privacy.ts` (decisions §33). `PrivacyZone` shape, `isInAnyZone` haversine check, `clipPointsToZones` walks-from-each-end clipper. Owner-side preview only — non-owner tracks go through the `clip_track_for_user` SECURITY DEFINER RPC server-side. 8-test mirror suite in `test/privacy_test.dart`.
- `widgets/fitness_card.dart` — the dashboard's Fitness card (VO₂ max + VDOT + qualifying-run count, then CTL / ATL / TSB, then a recovery-advice line). Renders nothing when `qualifyingRunCount == 0`. Reuses `lib/fitness.dart` for the math; widget-tested.
- `widgets/race_predictor_card.dart` — the dashboard's Race-time predictor card (mirror web `RacePredictorCard.svelte`): the 5K / 10K / Half / Marathon ladder with each rung's predicted time + unit-aware pace + a confidence chip (long-press tooltip = the reason). Feeds `race_predictor.dart#predictRaceLadder` the `qualifyingRuns` mapped to efforts; self-hides when the gate yields no anchor. Mounts on `dashboard_screen.dart` below `FitnessCard`. Widget-tested.
- `social_service.dart` — `ChangeNotifier` wrapping all Supabase calls for clubs / events / posts
- `training_service.dart` — `ChangeNotifier` wrapping Supabase calls for training plans + workouts
- `ble_heart_rate.dart` — BLE chest-strap GATT client for live BPM stream (tested); wires into the run screen via `BleHeartRate.stream`. Auto-reconnects on a mid-run drop (exponential backoff via the pure `bleReconnectDelay`, capped at 20 attempts) and exposes `BleHrStatus` transitions on `statusStream` so `run_screen` can disclose the drop. Initial connect failures (strap off at launch) don't auto-retry — they now emit the distinct `BleHrStatus.connectFailed` (vs `disconnected`) so `run_screen` shows a one-tap "Reconnect" banner (`_reconnectHeartRate` → `BleHeartRate.reconnect()`, a thin `connectCached` wrapper). Only a connection that dropped after working triggers the auto-reconnect loop.
- `ble_treadmill.dart` — BLE FTMS treadmill GATT client (service 0x1826, Treadmill Data characteristic 0x2ACD). Same GATT-client/status-stream/auto-reconnect shape as `ble_heart_rate.dart`: pure `parseTreadmillData` decodes flags + instantaneous speed (km/h, 0.01) + total distance (uint24 m) + inclination (sint16 0.1%) into a `TreadmillSample`; `bleTreadmillReconnectDelay` is the backoff schedule. Constructed once as an **app-owned singleton** in `main.dart` (`connectCached` kicked at startup, like `BleHeartRate`) and threaded through `HomeScreen` → `RunScreen` (the `treadmill` constructor arg) and `HomeScreen` → `YouScreen` → `SettingsScreen` → `SettingsIntegrationsScreen` → `TreadmillTile`, so the belt paired in Settings is the same instance the run screen reads — no second GATT client. Paired from Settings → Integrations via `TreadmillTile`; persists to its own `treadmill_device_id`/`treadmill_device_name` SharedPreferences keys (device-local, not the synced settings bag — see `docs/backend/settings.md`). **Additive/opt-in distance source** for the recorder via the `setTreadmillSample` seam (see below); never participates in a GPS run. **Live run-screen toggle shipped** — `run_screen.dart` `_toggleTreadmillMode` renders a paired-belt-gated `SwitchListTile` over the recording view; turning it on pumps belt samples into `setTreadmillSample` (L4 opt-in, own try/catch + banner-revert, never disturbs the L0/L1 path), turning it off calls `clearTreadmillMode`. `debugEmitSample`/`debugEmitSampleError`/`debugEmitStatus` are `@visibleForTesting` seams for driving the screen's wiring without a BLE stack.
- `hr_zones.dart` — pure helpers `hrZoneBreakdown` (time-weighted, 30 s pause cap) and `bpmStatsOf`. No Flutter state — unit-tested. Backs the HR-zone panel on `run_detail_screen` for runs whose track carries per-point `bpm` (Strava, FIT/TCX imports, watch recorders).
- `fitness.dart` — Dart port of `apps/web/src/lib/training/fitness.ts` (VDOT / VO₂ max from Daniels' formula, EWMA-based ATL/CTL/TSB, `recoveryAdvice`). Pure functions; backs the dashboard's Fitness card. Keep in sync with the web module.
- `race_controller.dart` — live-race orchestration: pings spectator feed, auto-submits finisher time to the leaderboard
- `settings_sync.dart` — reads/writes the user-preferences row so settings roam across devices. Accepts a `SettingsCache` so the bag-backed prefs stay readable + writable when offline; on cold start the cached bags hydrate before the server fetch completes, on offline-edit the change is mirrored to cache and queued for the server.
- `settings_cache.dart` — `SharedPrefsSettingsCache`: SharedPreferences-backed [`SettingsCache`] implementation. Keys are user-scoped (and device-scoped for the device bag) so a sign-out + sign-in as a different user on the same device can't read another user's data. Pending writes are JSON-encoded so a new prefs key round-trips without a cache-format migration.
- `backup.dart` — export + import of the local run / route stores (troubleshooting + device-swap path). The local writer's row reads are **paged and uncapped** (`readAllPages` from `api_client`; an unranged PostgREST select is clamped to `db-max-rows` = 1000 and answers 200, so a 3000-run account used to archive 1000 and nobody was told). The one thing that can still come up short is a blob download the writer swallows to keep the rest of the archive alive, so `writeBackupZipStreaming` returns a `LocalArchiveSummary` and the manifest carries `complete` + `incomplete` — the same vocabulary the Go writer publishes (`apps/job_worker/internal/dataexport/server.go`). Restore reads it back into `RestoreResult.archiveIncomplete`; **only an explicit `complete: false` claims a shortfall**, since a web-built or older archive carries no such field. Restore is additive on every path, and a blob the archive lacks now leaves its column OUT of the upsert rather than nulling it. See [../../docs/ops/backup_restore.md](../../docs/ops/backup_restore.md)
- `recurrence.dart` — Dart port of `apps/web/src/lib/social/recurrence.ts`, keep in sync
- `training.dart` — Dart port of `apps/web/src/lib/training/training.ts` (VDOT, Riegel, plan generator); keep in sync, 17-test mirror suite in `test/training_test.dart`
- `plan_adherence.dart` — Dart twin of `apps/web/src/lib/training/plan_adherence.ts` (`weeklyDrift` >±20% over/under + `missedWorkoutAdvice` make-up/skip). Pure; 11-test mirror suite in `test/plan_adherence_test.dart`. Backs the plan-detail adherence banner.
- `plan_replan.dart` — Dart twin of `apps/web/src/lib/training/plan_replan.ts` (`replanRemaining` — make-up a missed long ≤+15%, ease-off after over-running; past + taper + explicitly-skipped frozen). Reuses `plan_adherence.dart`; 13-test mirror suite in `test/plan_replan_test.dart`. Backs the plan-detail Re-plan flow.
- `routine_history.dart` — Dart twin of `apps/web/src/lib/gym/routine_history.ts` (TS↔Dart parity pair). `routineHistoryFromAggregate` shapes one routine's `gym_routine_history` RPC row into its performance history: the bounded page of recent sessions newest-first, days since last (floored; a row stamped ahead of the clock reads as today, never negative), and a completed-of-GRADED ratio. The tallies are complete and arrive already reduced — a count is an aggregate, and the old 500-row client window under-reported a routine run weekly for years (decisions § 658). `lastPerformedAt` comes from the aggregate, never from the page. A row still carrying a `gym_session_draft` snapshot is dropped — an in-flight session is not a session performed — and a "save as is" row counts as a session but sits outside the rate denominator rather than counting as a miss (decisions § 617). Pure; 16-test mirror suite in `test/routine_history_test.dart`. Backs the history panel on `routine_detail_screen`; `ApiClient.fetchGymRoutineHistory` does the owner-scoped read.
- `relink_candidates.dart` — Dart twin of `apps/web/src/lib/training/relink_candidates.ts` (`filterRelinkCandidates` — eligible runs to re-link to a planned workout: ±7-day window, excludes runs already linked to another workout so `plan_progress` can't double-count, keeps the current run). Pure; 9-test mirror suite in `test/relink_candidates_test.dart`. Backs the `workout_detail_screen` Re-link picker; `TrainingService.fetchRelinkCandidates` does the owner-scoped fetch.
- `race_predictor.dart` — Dart twin of `apps/web/src/lib/training/race_predictor.ts` (`predictRaceLadder` — multi-distance race-time predictor: 5K/10K/Half/Marathon ladder from recency-weighted qualifying efforts, anchor by recency-weighted Riegel equivalence, per-rung confidence via `predictionConfidence`). Pure; reuses `riegelPredict` + `predictionConfidence` from `training.dart`; 11-test mirror suite in `test/race_predictor_test.dart`. Backs `widgets/race_predictor_card.dart` on the Home dashboard (mirrors web's `RacePredictorCard` on `/dashboard`). Backlog #11.
- `backend_timeout.dart` — guarded `Future.timeout` wrapper for every PostgREST + Storage call so a single hung request can't lock the UI for the system default. Used by the home screen's pull-to-refresh and the sync paths.
- `watch_ingest_queue.dart` — disk-backed buffer for incoming watch payloads. iOS only writes to it (the Android-side method channel is a no-op); replays on sign-in / connectivity return so a phone restart doesn't lose a run that landed from the watch while the user was signed out.
- `widgets/error_state.dart` — reusable empty/error/retry surface used by every screen that fetches from Supabase. Widget-tested.
- `share_sheet.dart` — the single door to the OS share sheet: `shareFilesFrom(context, files:)` / `shareTextFrom(context, text:)`, and the only file under `lib/` that may import `package:share_plus/share_plus.dart`. It exists because iPadOS presents the sheet as a **popover** and refuses to present one without a non-empty anchor inside the host view — share_plus's iOS plugin turns a missing anchor into a `PlatformException`, so the sheet never appears — and the apps ship to iPad (`TARGETED_DEVICE_FAMILY = "1,2"`) while all 21 call sites once omitted it. The `BuildContext` is a **required positional** parameter and there is no `Rect`-taking overload, so a share without an anchor is inexpressible. `shareOriginFor` degrades rather than throwing (widget bounds clipped to the view → a small rect at the view centre → a one-pixel rect at its origin) because a share is an L4 effect, and it never returns an empty rect. 6 tests in `test/share_sheet_test.dart` plus four source guards in `architecture_guards_test.dart`. See [../../docs/architecture/conventions.md § Mobile OS share sheet](../../docs/architecture/conventions.md#mobile-os-share-sheet--sharefilesfrom--sharetextfrom-never-share_plus-directly) and [decisions.md § 714](../../docs/architecture/decisions.md).
- `widgets/route_share_card.dart` — portrait-orientation share card for routes (sibling of `widgets/run_share_card.dart`). Same `RepaintBoundary.toImage` → `share_sheet.dart` flow.
- `widgets/app_bar_actions.dart` — `AppBarActions` + `AppBarAction`: a toolbar-slot budget. Promotes the head of the action list into the slots left over after `pinned`, folds the rest into one overflow menu, and never promotes a `destructive: true` action (decisions §498). Used by `route_detail_screen` and `sim_watch_screen`; conventions § Mobile app-bar actions.
- `widgets/info_tip.dart` — the `(i)` a runner taps to find out what a feature IS and how to use it. An `IconButton` (48dp box, 18dp glyph — shrink the glyph, never the target) opening an `AlertDialog`; `infoTipTitle` pairs it with a `ListTile` title. Mobile twin of web's `InfoTip.svelte`, a dialog rather than a popover because there is no room beside a `ListTile` on a phone. Its accessible name is "About {name}", never "info". See [decisions § 1622](../../docs/architecture/decisions.md).
- `widgets/top_banner.dart` — top-anchored notification pill, the canonical replacement for `ScaffoldMessenger.showSnackBar` across the mobile app. Renders via `Overlay`, auto-positions below the nearest `AppBar`, and coalesces to a single banner at a time. See [../../docs/architecture/conventions.md § Mobile in-app notifications](../../docs/architecture/conventions.md#mobile-in-app-notifications--showtopbanner).
- `widgets/undo_bar.dart` — the deferred-commit undo offer (`deferDestructive`), the mobile counterpart of web's `UndoBar.svelte` over the `undo_queue.dart` parity twin. Seven deletes adopt it and **dropped their confirm** (confirm and undo are alternatives, [decisions.md § 514](../../docs/architecture/decisions.md)): food-log entry, route condition report, run comment/reply, notification dismiss (single row or a collapsed group — one intent, one slot, one batched `deleteNotifications`), route review, course marker, gear wear-log observation. **The host is a root `Overlay` entry and must stay one.** A `SnackBar` lives inside its route's `Scaffold`, and a modal route's barrier carries `BlockSemantics`, so the whole route below it — snack bar included — is dropped from the compiled semantics tree: measured in `undo_bar_test.dart`, a snack bar raised while a dialog is up has no semantics node at all, and an undo TalkBack cannot reach is not an undo (WCAG 2.1.1). An `Overlay.insert` entry lands above the barrier and stays reachable, which is why the in-sheet gear delete needs no exemption here. The queue is a top-level singleton, so **a route pop cannot discard a pending commit** and we deliberately do not flush on navigation the way web does — the cost is that `commit` must never read a `BuildContext` and `restore` must be mount-guarded, both pinned per call site by `architecture_guards_test.dart`. The window comes from `undo_window_s` (`Preferences.undoWindowS`, editable in Settings → Preferences); a corrupt value reads back as 8 s, never as no-limit.
- `widgets/place_search_panel.dart` — the body of the place-search dropdown shared by the three map screens that carry one (`privacy_zones_screen`, `routes_heatmap_screen`, `route_builder_screen`). Renders exactly one of three states off `geocoding.dart`'s `PlaceSearchOutcome`: the result tiles, a **no places found** row, or an **unavailable** row with a Retry. `unavailable` outranks a stale result list, so a failed retry can't fall back to showing the previous lookup's hits as current. It exists because all three screens rendered `if (_searchOpen && _searchResults.isNotEmpty)` — a failed lookup produced no feedback at all. 4 tests in `test/place_search_panel_test.dart`.
- `widgets/import_failure_report.dart` — the Import screen's "what didn't import, and why" card (mobile twin of web's `ImportFailureReport.svelte`): reason chips, an expandable per-activity list, and a Share-report action that writes the `import_failures.dart` CSV to a temp file and hands it to the OS share sheet. Every importer records its failures at the point it catches them, so the card is one list rather than a per-stage pile. 5 widget tests in `test/import_failure_report_test.dart`.
- `widgets/pending_sync_banner.dart` — shared persistent disclosure for `OfflineSyncStore` rows that haven't reached the server (issue #666 U2). Rendered from the stores' pending state (`pendingCount`), NOT from connectivity, so a push rejected while online (RLS denial, 500, expired token) is visible — pending + offline/signed-out shows "saved on this device — will sync when online", pending + online shows "N changes haven't synced" with a Retry wired to `syncWithServer` on every passed store. Mounted by `gym_screen` (+ its routine store), `nutrition_screen` (food + template + recipe stores), `gear_screen`, `routine_library_screen`, `routine_detail_screen`, and `gym_detail_screen` (+ its routine store); the screens' old `_isOnline`-gated banners now cover only the nothing-pending "showing cached data" case. 8 tests in `test/pending_sync_banner_test.dart`.
- `widgets/report_sheet.dart` — modal report sheet behind `showReportSheet(context, api:, targetKind:, targetId:)`. Five fixed reasons (`spam` / `harassment` / `inappropriate` / `impersonation` / `other`) keyed to the `reports` CHECK constraint, optional notes (trimmed; empty → null so the moderator dashboard's "had context" filter stays accurate). `targetKind ∈ {'user','club','route','comment','club_post','run'}` (the title switch renders a per-kind title, falling back to "Report content"). Mounts the flag IconButton in the AppBar of `profile_screen` (when not self), `club_detail_screen`, `route_detail_screen` (when not owner), `public_run_screen` (when a logged-in non-owner views a public run → `run`), and on each non-author club-feed post in `club_detail_screen` (→ `club_post`). Friendly copy for the 23505 duplicate-report path; P0001 rate-limit parsed by `rate_limit_errors.dart` and rendered from the ARBs by `rate_limit_message.dart` (decisions § 744). Mirrors web's `submitReport` flow. 10 widget tests in `test/report_sheet_test.dart`.
- FIT / GPX / TCX writers for a run — `_runToFitBytes`, `_runToGpx`, `_runToTcx` and the exported `fitCrc`, all inside `widgets/run_share_card.dart` behind `showRunShareSheet`, which run detail's share action opens. There is no `fit_export.dart` or `gpx_export.dart`; the settings screen's own `_exportRunsCsv` is a different, CSV-only path. 3 tests in `test/fit_export_test.dart`, which imports `fitCrc` from the share card.
- `exif_strip.dart` — pure `stripJpegExif(Uint8List)` — a lossless JPEG marker-walk that removes the APP1 (EXIF + XMP, incl. GPS) segment and copies every other segment (JFIF/APP0, ICC/APP2) + scan data verbatim. Non-JPEG input returned unchanged. Called by `widgets/run_photos.dart` before photo upload so a geotagged original never reaches the bucket ahead of the server worker's async strip (persona family-club #52). 14 tests in `test/exif_strip_test.dart`.
- `finisher_certificate.dart` — pure finisher-certificate shaping (`isCertificateEligible` + `formatCertificateTime` / `formatCertificateDistance` / `ordinalPlace`). TS↔Dart parity twin of `apps/web/src/lib/runs/finisher_certificate.ts` (web builds an SVG string; mobile renders natively, so only the unit/locale-agnostic shaping is shared). Backs the event-detail finisher certificate (persona #44). 4 test groups in `test/finisher_certificate_test.dart`.
- `widgets/finisher_certificate_card.dart` — the downloadable finisher certificate on mobile. `showFinisherCertificateSheet(...)` opens a modal hosting `FinisherCertificateCard` (a landscape card mirroring the web SVG — cream ground, amber double border, finisher name hero, time/distance/placing) and shares the `RepaintBoundary.toImage` PNG via `share_plus` (the run-share-card idiom). Mounted on each finished + organiser-approved row of `EventResultsSection` in `event_detail_screen.dart`. Widget-tested in `test/finisher_certificate_test.dart`; row visibility pinned in `test/event_results_section_test.dart`.
