# `runs.metadata` key registry

The `runs.metadata` column is `jsonb` — a schema-less bag that any client can write into. Schema codegen ([schema_codegen.md](../architecture/schema_codegen.md)) cannot catch drift in here: a mobile client can write `metadata.activity_type` and a web client can read `metadata.activityType` and nothing in the type system notices. This file is the coordination point.

**Rule:** If you add, rename, remove, or change the shape of a metadata key, update this file in the same change. If you're reading a key that isn't listed here, add it. If you find a key in the registry that isn't used anywhere in code, delete it.

**Dart side:** key names are mirrored as `MetadataKeys.*` constants in `packages/core_models/lib/src/metadata_keys.dart` (and bucket names as `StorageBuckets.*`). Prefer the constant over a bare string literal in Dart so a typo is a compile error; `metadata_registry_test.dart` resolves `MetadataKeys.*` back to the wire key, so a key referenced only through the constant is still checked against this registry. When you add a key here, add the matching constant (see [decisions.md § 124](../architecture/decisions.md)).

**Enforced on every writing platform.** Each client carries a source-level guard that scans its own sources and fails on a key with no row below, so an unregistered key can't reach `main` from any of them:

| Platform | Guard |
|---|---|
| Dart (mobile) | `apps/mobile_android/test/metadata_registry_test.dart` |
| TypeScript (web) | `apps/web/src/lib/metadata_registry_guard.test.ts` |
| Kotlin (watch_wear) | `apps/watch_wear/android/app/src/test/kotlin/com/runapp/watchwear/MetadataRegistryTest.kt` |
| Swift (watch_ios) | `apps/watch_ios/WatchAppTests/MetadataRegistryTests.swift` |

All four parse the key column of the tables below with the same regex, so they can't disagree about what "registered" means. Each has an exemption set for a `metadata` identifier that genuinely isn't `runs.metadata` — the watchOS one exempts the run **columns** (`id`, `started_at`, `duration_s`, `distance_m`, `source`) that ride the `WCSession.transferFile(_:metadata:)` transport envelope alongside the real metadata keys. Weakening a guard to make a key pass is never the fix; add the row here instead.

---

## Key registry

Each row: what the key is, its shape, which platforms *write* it, which platforms *read* it, whether the client must tolerate its absence, and whether the key is **public-safe** (preserved by the `public_runs` view) or **owner-only** (stripped from the view's `metadata` projection — see migration `20260626_001_public_runs_view.sql` and decisions §33's wire-leak follow-up). "Optional" means "a consumer must be safe with it missing." All keys are optional unless explicitly required. **When you add a new key, classify it.** If the public_runs strip-list and this column drift, the next public-rows audit will catch it; we'd rather catch it at PR review.

### Promoted to real columns (no longer metadata keys)

Migration `20261207_001_promote_activity_type_is_dnf.sql` (F3, decision D4)
moved two load-bearing keys out of the jsonb bag into real `runs` columns,
backfilled from the bag, and stripped the keys from existing rows. They are
no longer metadata keys — read/write `runs.activity_type` / `runs.is_dnf`.

| Was-key | Now-column | Migration |
|---|---|---|
| `activity_type` | `runs.activity_type text not null default 'run'` + CHECK `in ('run','walk','hike','cycle','stroller')`. Exposed on `public_runs` as a column, the `activities` view summary, the gear auto-tag trigger, and VDOT qualification. **The CHECK is the authoritative source for the client display vocabulary too** — web's `ACTIVITY_TYPES` + `activityType.<value>` catalogue keys and mobile's `ActivityType` enum + `activityTypeRun`… ARB keys and Wear OS's `activity_<value>` string resources are all mirrors of it, and all three `activity_type_vocabulary` guards PARSE this constraint out of the migration, so widening it fails the build until every one of the six web locales, seven ARBs and six Wear catalogues carries the new label (decisions § 547). Since decisions § 713 the Wear guard also asserts each wrist word EQUALS the ARB's and the web catalogue's, so the vocabulary is one across all three. `hike` is displayed as "Trail run" on web and mobile; the stored value stays `hike` everywhere, and the wrist still shows "Hike" pending an owner call on the rename (`docs/product/followups.md`). | `20261207_001` (was `20260601_001`'s jsonb CHECK, now dropped) |
| `is_dnf` | `runs.is_dnf boolean not null default false`. The personal-records refresher filters on it (a DNF must not be promoted as a distance PR). Exposed on `public_runs` as a column. | `20261207_001` (was `20260530000001`'s `metadata->>'is_dnf'` filter) |
| `fastest_5k_s` | `runs.fastest_5k_s integer` (nullable). Seconds of the fastest rolling-5km window inside the GPS track — the embedded-best PR candidate `refresh_personal_records_for_user` reads alongside whole-run brackets (a sub-20 5k inside an 18 km long run). Exposed on `public_runs` as a column (was a public-safe bag key). Absent (null) when the track is too short / has no per-point timestamps — no fake bests. | `20270325_001` (was `20260529000002`'s `metadata->>'fastest_5k_s'` read) |
| `fastest_10k_s` | `runs.fastest_10k_s integer` — same contract, rolling-10km window. | `20270325_001` |
| `fastest_half_marathon_s` | `runs.fastest_half_marathon_s integer` — same contract, rolling-21.0975km window. | `20270325_001` |
| `fastest_marathon_s` | `runs.fastest_marathon_s integer` — same contract, rolling-42.195km window. | `20270325_001` |

**Server-side writers/readers already migrated (Round 3, this change):**
`_shared/strava.ts`, `parkrun-import`, `export-data` (CSV + backup),
the Go worker (`InsertStravaRun`, `FetchExportRuns`, `FetchPremiumRuns`,
`isRunlike`, the dataexport CSV/backup), the `activities` + `public_runs`
views, `auto_tag_default_gear`, `refresh_personal_records_for_user`;
`race-results-import` (column-only since 2026-07 — it shipped writing the
column AND a stray bag copy, fixed alongside `20270314_001`);
`coach_roster_summary` (read the stripped `metadata->>'is_dnf'` until
`20270314_001` switched it to the column — the filter was a silent no-op).

`20270314_001` also re-stripped the stray bag copies that post-promotion
writers (race-results-import, the watch direct uploads) had re-added to
existing rows, rescuing any non-`'run'` watch value into the column first.

**Web client read/write sites — switched to the columns (Round 4, F3
Tier-2):** the SvelteKit app no longer touches `metadata.activity_type` /
`metadata.is_dnf` anywhere — `runs/[id]/+page.svelte` (read + DNF write +
chip), `core/data.ts` (`createManualRun`/`saveRun` + the following-feed
`activity_type` filter + `fetchRecentRunsForPicker` / `fetchRunsOnRoute` /
`fetchAthleteRuns`), `integrations/strava-zip.ts` + `garmin-zip.ts`,
`backup/backup_reader.ts` (`coalesceRunActivity`) + `restore_orchestrator.ts`,
`training/goals.ts`, `runs/recap.ts`, `routes/route_history.ts`,
`components/RunShareView.svelte`, `routes/history/+page.svelte`, and
`coaching/athletes/[id]/+page.svelte` all read/write the real columns. The
narrow `ActivityType` TS union (`types.ts`) ↔ CHECK lockstep is guarded by
`check_constraint_unions.mjs`.

**Mobile (Flutter) clients — switched to the columns (Round 4, F3 Piece A):**
the `Run` domain object still carries `activity_type` / `is_dnf` inside its
in-memory metadata bag (one read path for the dozens of screen readers), but
the bag is no longer the persisted copy. `ApiClient.saveRun` / `saveRunsBatch`
lift the two keys into the `activity_type` / `is_dnf` **columns** and strip
them from the bag before upsert (`_metadataWithoutPromotedColumns`);
`_runFromRow` surfaces the columns back onto the bag on read (the same
convenience stash as `track_url`). Raw-`RunRow` consumers that bypass
`_runFromRow` read the column directly — `profile_screen` (public runs), the
following-feed activity filter (`fetchFollowingFeed` filters on the
`activity_type` column, not `metadata->>activity_type`), `fetchRecentRuns`, and
`beginLiveBroadcast` (writes the column on the stub row). The recorder /
importer / `add_run` / `applyDnfFlag` write sites set the bag key as before;
the lift makes that the column write. The byte-identical iOS twin matches.

**Client read/write sites STILL on the jsonb keys (Tier-2, deferred — these
silently read `null` / write a now-ignored bag key until switched):**
- `apps/watch_wear/.../WatchRunMetadata.kt`, `apps/watch_ios/.../ContentView.swift` + `SupabaseService.swift` (the watch→phone bridge still carries `activity_type` in its WCSession metadata; the phone-side ingest lifts it into the column via the same `Run`-carrier path — see § Known issues).

**Embedded-best writers/readers — all switched with `20270325_001`:**
the SQL refresher (`refresh_personal_records_for_user`) reads only the
columns and the statement-trigger watch list swapped `metadata` for the four
columns (a bag-only edit no longer forces a PR recompute); web's Garmin
importers pass `computeEmbeddedBests` to `saveRun`'s `embedded_bests` input
(`core/data.ts`) which writes the columns; the Strava EF (`_shared/strava.ts`
`ingestActivity`) updates the columns after the track upload; Flutter keeps
the keys in the in-memory `Run.metadata` bag (written by
`embedded_bests.dart` at save/import time) and `ApiClient.saveRun` /
`saveRunsBatch` lift them onto the columns via `_embeddedBestSeconds`
(non-negative-integer domain, mirroring the old SQL regex validation) while
`_runFromRow` stashes non-null columns back onto the bag — the same carrier
pattern as `activity_type` / `is_dnf` above. The Go worker's Strava path
never computed embedded bests (pre-existing gap, unchanged).

### Core run properties

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `pacing_strategy` | `object` — `{ preset: 'ten_ten_ten' \| 'negative_split' \| 'even', distance_m: double, goal_time_s?: int }` | `mobile_android/screens/run_screen.dart` (final save; only when a race strategy was active for the recording, issue #609) | none yet — reserved for the run-detail per-phase grading follow-up | Optional; absent when no strategy was chosen | `preset` values are the `race_phases` parity pair's wire strings; `distance_m` is the resolved race distance the phase plan was built from (manual entry or the followed route's length), `goal_time_s` only when the runner set a goal time. Rebuild the phases with `buildPhasePlan(distance_m, preset)` — the plan itself is derivable and deliberately not stored. |
| `steps` | `int` (stringified on the wire? — **investigate**) | `mobile_android/screens/run_screen.dart` (pedometer); `apps/watch_wear/android/.../RunRecordingService.kt` (via `Pedometer.kt` → `Sensor.TYPE_STEP_COUNTER`); `mobile_android/lib/health_connect_importer.dart` (`stepsForWorkout`, from the `health` plugin's `WorkoutHealthValue.totalSteps`); `apps/watch_ios/WatchApp/ContentView.swift` (`syncRun`, from the `Pedometer` wrapper around Core Motion's `CMPedometer`) | `apps/web/src/routes/runs/[id]/+page.svelte`, `apps/backend/supabase/functions/export-data/render.ts` (CSV column) | Optional; only present when pedometer data is available | Only written when `steps > 0`. Android + Wear OS omit the key entirely if the pedometer never fired. Wear OS requires `ACTIVITY_RECOGNITION` at runtime; if the user denies, the Pedometer flow is silent and `metadata.steps` stays absent. **Apple Watch does the same shape** (2026-09-18): `CMPedometer.startUpdates(from:)` behind a per-run baseline (`PedometerMath.stepsSinceBaseline`, the twin of Wear's), gated on `NSMotionUsageDescription` + the Motion & Fitness grant, and the count rides the `WCSession` envelope as `steps` — lifted back out by `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift` and forwarded onto the row by `runFromWatchPayload`. The 15 s crash checkpoint carries it, so a recovered run keeps its steps rather than uploading short (the Wear lesson of #389). **Not measured on any real wrist**: the watchOS simulator has no pedometer, so what is verified is the baseline arithmetic, the checkpoint round-trip and both ends of the envelope — not that `CMPedometer` delivers. The Health Connect importer needs `READ_STEPS` for the same reason — the plugin derives a workout's `totalSteps` by summing every `StepsRecord` in the session window, so without the grant the field is null and the key is omitted. It screens before writing: never for `cycle` (the stride table's "pedometer not meaningful for cycling"; the tile would render pocket jostle as spm) and never for a count implying a cadence past 300 spm, which is a foreign record summed into the window. It writes **only** this key — the cadence comes from the shared `steps / moving_time_minutes` derivation, never a second stored value. |
| `laps` | `array` of `{ index: int, start_offset_s: int, distance_m: double, duration_s: int }` | `packages/run_recorder/lib/src/run_recorder.dart` (final save only, via `lapsToCanonicalJson`); `apps/watch_wear/.../WatchRunMetadata.kt` (`buildRunMetadata`, called from `RunViewModel.pushRun`); `apps/web/src/lib/integrations/garmin-zip.ts` (Garmin FIT import, via `buildCanonicalLaps` in `garmin-fit.ts` — projects FIT `lap` messages, drops the degenerate single-lap file); `apps/watch_ios/WatchApp/ContentView.swift` (`syncRun`, via `RunLaps.splits` + `RunLaps.envelopeValue` — the same cumulative-marks-to-per-lap-deltas conversion Wear does, including the trailing-partial gate); `apps/mobile_android/lib/sim_watch_sync.dart` (`payloadFromBlob` — custom-watch BLE run sync decodes v2 lap records from the `run_store` blob into this shape, `start_offset_s` reconstructed by summing prior splits; iOS twin byte-identical) | `mobile_android/screens/run_detail_screen.dart` (`_buildLaps`); `apps/web/src/routes/runs/[id]/+page.svelte` (Laps section); `apps/web/src/lib/training/intensity.ts` (`effortSegments` — per-lap pace segments the easy/hard balance card grades, second choice after `workout_step_results`) | Optional; present if the runner marked laps or imported a multi-lap FIT | The recorder sets `metadata = null` entirely when `_laps.isEmpty`, so readers must check for both a null `metadata` and a missing `laps` key. `index` is 1-based (matches `lap.number` on both platforms). `distance_m` and `duration_s` are *per-lap* deltas (not cumulative); `start_offset_s` is the cumulative duration up to the **start** of this lap (so the first lap has `start_offset_s = 0`). The Apr 2026 cross-platform audit caught the Dart recorder writing the older `{ number, timestamp, cumulative_distance_m, cumulative_duration_s }` shape; both writer and `run_detail_screen.dart` reader were migrated to the registered per-lap shape in the same change. The cross-platform fixture test (`fixtures/watch_run_payload.json` consumed by `apps/mobile_android/test/watch_payload_fixture_test.dart`, `apps/mobile_ios/test/watch_payload_fixture_test.dart`, `apps/watch_wear/.../WatchRunPayloadFixtureTest.kt`, and `apps/web/src/lib/watch_payload_fixture.test.ts`) caught a separate Wear OS bug where `start_offset_s` was emitted as cumulative-AFTER (`lap.atMs / 1000`) instead of cumulative-BEFORE — fixed in `buildRunMetadata`. `watch_ingest_queue.dart` forwards the field verbatim when a watch payload includes it — reachable from the Apple Watch since 2026-09-18, when the phone bridge's key list learned `laps` (before that the queue decoder's support could never see WCSession input). The marks live in the crash checkpoint in their CUMULATIVE form, so a recovered run closes its trailing split against the checkpoint's own totals and lands the same rows a stopped run would. |

### User-editable fields

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `title` | `string` — user-entered display title | `mobile_android/screens/run_detail_screen.dart` (edit dialog); `mobile_android/strava_importer.dart` (default from Strava activity name); `apps/web/src/lib/core/data.ts` (`saveRun` merges `input.title` into `metadata`); `apps/web/src/routes/runs/[id]/+page.svelte` (in-place edit on the run-detail page); `apps/backend/supabase/functions/_shared/strava.ts` (defaults to `act.name` from the live Strava API path used by both `strava-import` and `strava-webhook`) | `mobile_android/screens/run_detail_screen.dart`; `mobile_android/screens/import_screen.dart` (names a run in the import failure report when its local save fails); `apps/web/src/routes/runs/[id]/+page.svelte` (header, share-card, social previews); `apps/web/src/lib/components/SocialFeed.svelte` (rendered on each activity-feed card when present); `apps/backend/supabase/functions/export-data/render.ts` (CSV column) + `apps/backend/supabase/functions/export-data/index.ts` (the per-run GPX `<name>`, which falls back to `Run <id>`) | Optional; falls back to the formatted start date when absent | Strava imports default to the Strava activity name. Editable on both platforms. |
| `notes` | `string` — free-form user notes | `mobile_android/screens/run_detail_screen.dart` (edit dialog); `mobile_android/screens/add_run_screen.dart` (manual entry form); `apps/web/src/routes/runs/[id]/+page.svelte` (edit form merges into metadata via `updateRunMetadata`) | `mobile_android/screens/run_detail_screen.dart`; `apps/web/src/routes/runs/[id]/+page.svelte` (rendered below stats as text — no `{@html}`) | Optional; empty string when absent | Public-safe: passes through `public_runs`. The web edit/view path was added after the registry was first written — keep this row in sync with the actual writers/readers. |

### Import provenance

Set by importers when bulk-loading runs from third parties. None of these are read by any current UI — they're audit/debug data.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `imported_from` | `string` — one of `strava`, `health_connect`, `garmin`, `csv` | `mobile_android/health_connect_importer.dart`; `mobile_android/strava_importer.dart`; `mobile_android/csv_run_importer.dart`; `apps/web/src/lib/integrations/garmin-zip.ts`; `apps/backend/supabase/functions/_shared/strava.ts` (writes `'strava'` from the live API path used by `strava-import` + `strava-webhook`) | — | Optional, audit-only | No UI consumer today; keep writing it so future audit tooling has a source. The `csv` value lands when a user re-imports a Settings CSV export via the mobile Import screen (lossy — no track). |
| `created_by_user_id` | `string` — the Supabase `auth.uid()` of the user signed in when the run was saved, or **absent** when saved while signed out. **Local-only marker** — not written to the cloud runs table (the cloud row's `user_id` column is authoritative server-side). | `mobile_android/local_run_store.dart` (every `save()` call stamps via the `currentUserIdProvider` set in `main.dart`) | `mobile_android/lib/sync_service.dart` (`filterRunsForCurrentUser` filters foreign-owner runs out of the batch push) | Optional; absent when saved signed-out, present otherwise | Defends the shared-device sign-out / sign-in flow. Without this, User A's unsynced runs would silently try to push under User B's account after a sign-out + sign-in on the same device, RLS would reject everything, and B's "X unsynced" badge would be stuck forever. Untagged runs (legacy / signed-out) adopt to whoever signs in next; tagged runs only push for the matching user. `update()` does NOT re-stamp (edits preserve the original owner); `saveFromRemote` does NOT stamp (cloud rows carry their own `user_id`). See [decisions.md § 67](../architecture/decisions.md#67-offline-saved-runs-carry-a-created_by_user_id-owner-tag--defends-shared-device-sign-out--sign-in). |
| `imported_at` | `string` (ISO 8601) | Same as above (including `_shared/strava.ts` and `csv_run_importer.dart`) | — | Optional, audit-only | Client wall-clock time of the import, not the original run time. |
| `health_connect_type` | `string` — a `HealthWorkoutActivityType` enum name | `mobile_android/health_connect_importer.dart` | — | Optional, audit-only | Preserves the raw Health Connect type even after `activity_type` narrows it to our enum. Useful if we ever want to recover the original classification. |
| `strava_activity_type` | `string` — raw Strava sport/activity type (e.g. `Run`, `TrailRun`, `VirtualRun`, `Hike`, `Ride`) | `mobile_android/strava_importer.dart`; `apps/web/src/lib/integrations/strava-zip.ts` (CSV `idx.stravaType`, prefers the finer `Sport Type` column over coarse `Activity Type` — see `strava-zip-header.ts`); `apps/backend/supabase/functions/strava-import/index.ts` + `strava-webhook` (via `_shared/strava.ts`) | — | Optional, audit-only | Same rationale as `health_connect_type`. |
| `strava_id` | `string` — Strava activity id | `apps/web/src/lib/integrations/strava-zip.ts`; `apps/backend/supabase/functions/_shared/strava.ts` (live API ingest path used by `strava-import` + `strava-webhook`) | `apps/web/src/lib/integrations/strava-zip.ts` (dedupe check); `apps/backend/supabase/functions/_shared/strava.ts` (`isAlreadyImported` dedupe via `metadata->>strava_id`); `apps/backend/supabase/functions/strava-import/index.ts` (backfill `seen` set) | Optional | Used by every Strava import path to skip already-imported activities. **Note:** `apps/mobile_android/lib/strava_importer.dart` does NOT write this key — it dedupes via the `runs.external_id` column (`strava:<id>` prefix) instead. The Dart Strava ZIP path was never wired to write `metadata.strava_id`. |
| `garmin_id` | `string` — Garmin file id (`<time_created>-<serial>`) | `apps/web/src/lib/integrations/garmin-zip.ts` | `apps/web/src/lib/integrations/garmin-zip.ts` (dedupe check) | Optional | Used by the web Garmin ZIP importer to skip already-imported activities. |
| `indoor` | `bool` — true for treadmill / indoor / virtual sessions | `apps/web/src/lib/integrations/garmin-zip.ts` (FIT `sub_sport` treadmill/indoor/virtual, via `garmin-fit.ts`); `packages/run_recorder/lib/src/run_recorder.dart` (`stop`, BLE FTMS treadmill); `mobile_android/lib/screens/run_screen.dart` (`_stop`, `_saveInProgress`, pedometer-estimated indoor run) | `apps/web/src/lib/training/fitness.ts` + `apps/mobile_android/lib/fitness.dart` (`qualifyingRuns` excludes them from VDOT) | Optional; only written when true | Belt-/estimate-derived distance is not GPS-measured, so it must not feed the VDOT ceiling (#16). **This is the flag every consumer tests** — an indoor source that stamps only its own audit-only tag (`indoor_estimated`, `indoor_source`) is NOT excluded. The pedometer path stamped `indoor_estimated` alone until Aug 2026, so a stride constant that over-read raised the runner's VDOT permanently (the ceiling is a max over runs); it now writes `indoor` as well. |
| `max_bpm` | `number` — peak heart rate in BPM for the run | `apps/web/src/lib/integrations/garmin-zip.ts`; `apps/backend/supabase/seed.sql` (regression-fixture writer for the `public_runs` strip-list assertion) | — | Optional, audit-only | Written by the Garmin ZIP importer when `maxBpm` is present in the parsed FIT data. The seed includes it on the public_runs assertion fixture so a regression that omits it from the strip list fails `supabase db reset`. No UI consumer yet. |
| `cadence_spm` | `number` — average running cadence in steps-per-minute (both feet) | `apps/web/src/lib/integrations/garmin-zip.ts` (from `ParsedFitRun.avg_cadence_spm`; FIT per-foot RPM doubled for foot sports via `fitCadenceToSpm`) | `apps/web/src/routes/runs/[id]/+page.svelte`, `apps/mobile_android/lib/screens/run_detail_screen.dart` (Cadence key-stat) | Optional; only present when the FIT session carried cadence | Written by the Garmin FIT importer for run / walk / hike (null for cycling — crank RPM isn't a step rate). The run-detail Cadence tile prefers this directly-reported value over the pedometer-derived `steps / moving_time_minutes` fallback, since an imported FIT run has no `steps`. Persona-hunt garmin #17. |
| `source_file` | `string` — original filename from the ZIP archive | `apps/web/src/lib/integrations/garmin-zip.ts` | — | Optional, audit-only | Written by the Garmin ZIP importer for both FIT and GPX files. Audit/debug aid. |
| `sub_sport` | `string` — normalised FIT `sub_sport` (e.g. `trail`, `treadmill`, `track`, `road`, `virtual_activity`) | `apps/web/src/lib/integrations/garmin-zip.ts` (from `ParsedFitRun.sub_sport`, via `normalizeSubSport` in `garmin-fit.ts`) | `apps/web/src/routes/runs/[id]/+page.svelte` (header discipline chip) | Optional; only present when the FIT session carried an informative `sub_sport` | Preserves the discipline the coarse `activity_type` discards — there is no `trail` activity_type, so a trail run would otherwise collapse to a generic "Run". `generic` / `all` / `invalid` placeholders are dropped to null. Rendered as a capitalised header chip on web run-detail. Persona round-5 theme F (F1). |
| `running_dynamics` | `object` — `{ vertical_oscillation_mm?: number, gct_ms?: number, stride_length_m?: number, power_w?: number }` | `apps/web/src/lib/integrations/garmin-zip.ts` (from `ParsedFitRun.running_dynamics`, via `buildRunningDynamics` in `garmin-fit.ts`) | `apps/web/src/routes/runs/[id]/+page.svelte` (Running Dynamics stat block) | Optional; only the sub-fields the FIT session actually carried are present (a base watch with no HRM-Pro / Running pod writes none, so the whole key is absent) | Garmin Running Dynamics off the FIT session message: vertical oscillation (mm), ground-contact time (ms), stride length (m, converted from FIT's mm), running power (W). L/R balance is intentionally excluded — fit-file-parser decodes the session field as an enum string, not the numeric percentage. Rendered as a stat block on web run-detail. Persona round-5 theme F (F2). |
| `elevation_m` | `number` — total elevation gain in metres | `apps/web/src/lib/core/data.ts` (`saveRun`, via Strava and Garmin importers) | `apps/backend/supabase/functions/export-data/render.ts` (CSV column) | Optional | Populated for Strava and Garmin imports when ascent data is present. **Now mirrored by the first-class `runs.elevation_gain_m` column** (`20270302_001`, ADR §186) — the column is the additive, queryable source the vert challenge metric sums + the `activities` view projects; this metadata key is **kept** for the existing readers (recap / export / Strava EF / Go worker / backup) and writers populate both going forward. **Public-safe**: both this key and the column pass through `public_runs`. Reveals total ascent for the run (a single integer) — combined with the publicly-visible `started_at` + `distance_m` + clipped track, it adds essentially no new geographic signal that the rendered polyline doesn't already carry. Documented as a deliberate keep, not an oversight, after /audit/all 2026-05-07. |

### Parkrun fields

Written by the `parkrun-import` Edge Function when scraping a runner's results page. See [../apps/backend/CLAUDE.md](../../apps/backend/CLAUDE.md) § Edge Functions.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `event` | `string` — the parkrun event name (e.g. `Richmond`, `Bushy Park`) | `apps/backend/supabase/functions/parkrun-import/index.ts` | `apps/web/src/routes/runs/+page.svelte` | Required when `source = 'parkrun'` | Displayed on the web runs list as a source badge. |
| `position` | `number` — finishing position in the event | Same | `apps/web/src/routes/runs/+page.svelte` | Required when `source = 'parkrun'` | Displayed next to the event name on the web runs list. |
| `age_grade` | `string` — age-graded percentage as a string with `%` suffix (e.g. `"54.23%"`) | Same | `apps/web/src/routes/runs/[id]/+page.svelte` (key-stat tile); `apps/mobile_android/lib/screens/run_detail_screen.dart` (secondary-stat tile) | Optional | Shown as an "Age grade" key-stat on the run-detail page when present (#9). Set by the parkrun importer. **When this key is absent**, run-detail computes the age grade itself from the runner's DOB + sex + the run's standard distance + duration via the shared `age_grade` helper (embedded USATF-MLDR 2025 factor tables); see [docs/features/age_grade.md](../features/age_grade.md). This scraped key still takes precedence when present. |
| `run_number` | `int` — parkrun attendance count (e.g. `17` for "your 17th parkrun") | Documented in `docs/backend/api_database.md` § runs but not yet written by `parkrun-import`; reserved for when the scraper adds it | — | Optional, audit-only | Owner-only — combined with `parkrun_number` (revoked from cross-user reads in `20260707_001`) it could re-identify a runner. Preemptively stripped from the `public_runs` view by `20260724_001` so the future writer doesn't need to land in lockstep with a view migration. |
| `avg_bpm` | `number` — mean heart rate in BPM across the run | `apps/watch_ios/WatchApp/ContentView.swift` (forwards `FinishedRun.averageBPM` from `HealthKitManager`); `apps/watch_wear/android/.../RunRecordingService.kt` (averages from `HeartRateMonitor`); `apps/mobile_android/lib/screens/run_screen.dart` (averages BLE chest-strap samples from `BleHeartRate`); `apps/mobile_android/lib/health_connect_importer.dart` (averages `HEART_RATE` samples inside the workout window); `apps/web/src/lib/integrations/strava-zip.ts` (parsed from the CSV); `apps/web/src/lib/integrations/garmin-zip.ts` (parsed from the FIT); `apps/backend/supabase/functions/_shared/strava.ts` (rounds `act.average_heartrate` from the live Strava API path used by both `strava-import` and `strava-webhook`) | `apps/web/src/routes/runs/[id]/+page.svelte` (post-run detail); `apps/mobile_android/lib/screens/run_detail_screen.dart`; `apps/web/src/lib/training/training_load.ts` + `apps/mobile_android/lib/training_load.dart` (TRIMP calculation for the dashboard fitness/fatigue/form chart); `apps/backend/supabase/functions/export-data/render.ts` (CSV column, with the `hr_coverage` column beside it since § 1134 — a mean the recorder measured over part of the run is not the run's average, and the CSV stated the mean and not the share); watch apps' own post-run summaries | Optional; only present when HR was captured | Recording sources: Apple Watch → HealthKit `HKLiveWorkoutBuilder`; Wear OS → `androidx.health:health-services-client`; Android phone → BLE Heart Rate Service (0x180D) via `flutter_blue_plus`; Health Connect import → averages `HEART_RATE` samples from the importing app's writes. All clamp to 30–230 BPM before averaging to drop sensor noise. **Scalar companion:** per-point BPM lives on the track (`track[].bpm`, not `runs.metadata`) — see [api_database.md](api_database.md#runs) for the shape. Web's run-detail HR-zone panel uses `track[].bpm` when populated and falls back to this scalar when only `avg_bpm` is set. The Dart `Waypoint` model carries an optional `bpm` field too, so `apps/mobile_android` renders the same time-weighted breakdown for runs whose track arrived with per-point HR (Strava, FIT/TCX, future watch streams). The watch-ingest decoder (`apps/mobile_*/lib/watch_ingest_queue.dart`) extracts per-point `bpm` off each waypoint that arrives from the watch payload, so an Apple Watch that streams HR samples through the phone bridge preserves the trace end-to-end. No mobile recorder writes per-point `bpm` yet — added when the recorder gains streaming-HR support. **Art 9 gate:** `apps/web/src/lib/coach/context.ts` also reads `avg_bpm` into the AI-coach payload, but `pickAllowedRunMetadata` strips it unless the user has granted health-data consent (`health_data_consent_at`), matching the gate on `resting_hr_bpm`/`max_hr_bpm`/`hr_zones` (decisions §privacy; persona-hunt privacy Critical). |
| `hr_coverage` | `number` — fraction of the run's ACTIVE elapsed time (0..1, two decimals) during which the heart-rate sensor was delivering samples | `apps/watch_wear/android/.../WatchRunMetadata.kt` (`buildRunMetadata`, from the coverage `RunRecordingService` accumulates on its recording ticker); `apps/watch_ios/WatchApp/ContentView.swift` (`syncRun`, from the `HealthKitManager.heartRateClaim` the same call grades `avg_bpm` with — see the reachability caveat below) | `apps/web/src/routes/runs/[id]/+page.svelte` (via `runs/hr_coverage.ts`, decisions § 1088) and `apps/mobile_android/lib/screens/run_detail_screen.dart` (+ the iOS twin, § 1094) — both render the share the average was taken over, and give a SUPPRESSED average its own sentence so it stops reading as a run recorded with no strap; `apps/backend/supabase/functions/export-data/render.ts` (CSV column, § 1134) — the Art 20 export carries the same three states in the same vocabulary, because an export that contradicts the page is its own defect. The column is GRADED like the page: a value outside `0..1` is refused there rather than handing a consumer a fraction it will read as 8500 %, and the stored value still rides verbatim in the CSV's `metadata` column. **The Go worker's `apps/job_worker/internal/dataexport/server.go` does NOT yet carry the column, and that is the rail most runners reach** — mobile has no Edge Function path at all and always enqueues, web takes the EF only when `PUBLIC_EXPORT_HUB_URL` is unset (§ 724). Known drift, filed | Optional; written whenever heart rate was ENABLED for the run, including when it delivered nothing (`0`) | Exists because `avg_bpm` was saved as *the run's* average with nothing saying what share of the run produced it. Health Services' `MeasureClient` is documented foreground-only, so on a twelve-hour ultra the samples reaching the mean can be the minutes the runner spent looking at the watch (decisions § 1015 / § 1054). The same Wear grading **suppresses `avg_bpm` below 0.5** — a mean over less of the run than not is not the run's average — so the most informative reading of this key is when it is present and `avg_bpm` is absent. Measured against the AGE of the last usable sample rather than by closing an interval on an availability event: a client the platform stops feeding can go quiet without ever reporting `UNAVAILABLE`, and an open interval would credit the whole silence. **Absent ≠ 0**: a run recorded by a build predating the field, or recovered from a checkpoint that never carried it, omits the key rather than claiming zero coverage. **Both watch clients now measure it, and the absence is still ambiguous — read this before inferring anything from a missing key.** `apps/watch_ios` measures coverage the same way Wear does (the age of the newest usable sample, credited on the recorder's own tick — `HealthKitManager.advanceCoverage`) and suppresses `avg_bpm` under the same 0.5 floor; § 1156 built that and spent it locally, § 1207 sends it. **Reachable end to end on the WCSession path since § 1254.** The watch packs the key, `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift` lifts it back out — both ends, because claim (6) of `scripts/check_watch_ios_source.mjs` reads both — and `runFromWatchPayload` (`watch_ingest_queue.dart`) now forwards it onto the row. There is one Dart decoder rather than two: `WatchIngest` in `main.dart` used to carry a second hand-written copy for the signed-in branch, and it is that copy's allowlist, not the queue's alone, that had dropped the key. The value is forwarded verbatim like `avg_bpm` (every reader grades the range itself, and the CSV keeps the raw figure in its `metadata` column) with only a finiteness check, because `metadata` is JSON-encoded on the way to Postgres and a non-finite double would fail the whole run upload. **Both watch clients write `source = 'watch'`** — there is no `watch_ios` value — so a `source = 'watch'` run with no `hr_coverage` is one of four things and nothing on the row distinguishes them: (1) an Apple Watch run whose `HKWorkoutSession` never started (HealthKit unavailable, entitlement missing, a simulator), so nothing measured coverage; (2) an Apple Watch run recorded by a build predating § 1254, whose figure the phone-side Dart decoder dropped; (3) a Wear run recorded by a build predating this key; (4) either client recovered from a checkpoint that never carried the field. A reader must not read the absence as evidence about coverage in either direction. What has LEFT that population since the Dart lift landed is the ordinary Apple-Watch run, which now states its share like a Wear one. **Not run-verified against a live workout on watchOS**: the `test-watch-ios` macOS job does run `HealthKitFailureTests` on a booted watchOS simulator and sits in the required gate's `needs:`, so the accumulator's own unit cases execute on every non-docs diff — but no real `HKWorkoutSession` has ever driven it, on a simulator or a device, and its one silent failure mode — a sample-timestamp source that reads blank on hardware — would send `hr_coverage: 0.0` on every run. The write is gated on a non-nil claim so an unmeasured run omits the key rather than claiming zero, but a bench or simulator run is still owed before the figure is trusted (decisions § 1207). **Public-safe**: a sensor-duty fraction carries no geographic or identity signal, so it rides through the `public_runs` projection beside `avg_bpm` and needs no strip-list migration. decisions § 1083. |

### Race fields

Set on runs imported from a registered race surface (chip-timing exports, the RunSignUp results endpoint, or a manual paste). Written by the `race-results-import` Edge Function (race_calendar.md) — either onto a fresh `source = 'race'` run, or merged onto an existing recorded run via the auto-match-on-record seam. Documented in [api_database.md](api_database.md#runs) for `source = 'race'` runs; classified owner-only and stripped from the `public_runs` view by `20260714_001` (the original four) and `20270214_001` (the three age-group / gun-time keys this importer added). Read by `RaceService.fetchRaceResultForRun` (mobile) + `fetchRaceResultForRun` (web `data.ts`) for the run-detail official-result panel.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `race_name` | `string` — registered race name (e.g. `"Richmond Half Marathon"`) | `race-results-import` EF (RunSignUp + paste) | `fetchRaceResultForRun` (web + mobile) | Optional | Owner-only. Links the runner to a specific race registration; stripped from `public_runs`. |
| `bib` | `string` — runner's bib number | Same | Same | Optional | Owner-only. Permanent real-world identity link; stripped from `public_runs`. |
| `overall_place` | `int` — finishing position overall | Same | Same | Optional, audit-only | Owner-only conservatively — the position is also on the public race results page so this doesn't add information about a runner who's already on the leaderboard, but stripped to keep the view's surface consistent for runners who'd rather not have their finish-time linked from share pages. |
| `chip_time` | `string` — chip-timed race duration (e.g. `"1:47:23"`) | Same | Same | Optional, audit-only | Owner-only. Redundant with `duration_s` but reveals chip-vs-gun precision the runner may not want public; stripped from `public_runs`. |
| `gun_time` | `string` — gun-timed race duration (e.g. `"1:48:01"`) | Same | Same | Optional, audit-only | Owner-only. The fallback when no chip time is available; stripped from `public_runs` by `20270214_001`. |
| `age_group_place` | `int` — finishing position within the runner's age group | Same | Same | Optional, audit-only | Owner-only. Reveals the runner's age band; stripped from `public_runs` by `20270214_001`. |
| `age_group` | `string` — the runner's age-group bucket (e.g. `"M40-44"`) | Same | Same | Optional, audit-only | Owner-only. Age-band identity hint; stripped from `public_runs` by `20270214_001`. |
| `perceived_effort` | `int` — RPE on the Borg 1–10 scale | Seed only (`apps/backend/supabase/seed.sql`) — no application writer exists today | — | Optional, audit-only | Owner-only. Until a real UI for user-entered RPE lands, this key only appears on seed runs; stripped from `public_runs` by `20260714_001` so the seed's public-run rows don't expose it through the view. |

### Training plan linkage

Written when a run was recorded under a structured plan workout. See [workout_execution.md](../features/workout_execution.md) for the full loop.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `plan_workout_id` | `string` (uuid) — the linked `plan_workouts` row | `mobile_android/lib/screens/run_screen.dart` (`_finishRun`, when a workout was active) | `apps/web/src/routes/runs/[id]/+page.svelte`, `mobile_android/lib/screens/run_detail_screen.dart` (both surface a "Workout" section when set) | Optional | Presence implies an explicit link — the auto-matcher (`autoMatchRunToPlanWorkout`) skips runs that already carry it. |
| `workout_step_results` | `array<{ step_index: int, kind: string, rep_index?: int, rep_total?: int, target_distance_m: double, target_duration_s?: int, actual_distance_m: double, target_pace_sec_per_km: int, actual_pace_sec_per_km: int?, tolerance_sec_per_km: int, duration_s: int, status: 'completed' \| 'skipped' }>` | `mobile_android/lib/screens/run_screen.dart` (`_finishRun`, from `WorkoutRunner.snapshotResults`) | `apps/web/src/routes/runs/[id]/+page.svelte`, `mobile_android/lib/screens/run_detail_screen.dart` (planned-vs-actual table); `apps/web/src/lib/training/intensity.ts` (`effortSegments` — the per-step breakdown the easy/hard balance card grades a mixed session by, in preference to `laps`) | Optional; always present alongside `plan_workout_id` | `actual_pace_sec_per_km` is null for steps where `actual_distance_m < ~10 m` (the user skipped almost immediately). One row per expanded step, including skipped ones, so readers can render the full planned sequence. `target_duration_s` is emitted only for duration-based steps (workout execution v2 — e.g. 30 s strides); when present + positive, the review table renders time on the plan + actual columns instead of km. Distance steps omit the key. `tolerance_sec_per_km` is the per-step pace band the workout was AUTHORED with (`plan_workouts.target_pace_tolerance_sec`, a 0-60 field in the web Workout editor, defaulting to 10) — both review tables grade on/amber/off against it, and the web one held a literal 10 until 2026-09-05, so a step a coach authored at 25 s/km graded green on the phone and amber on the web. `rep_total` on a RECOVERY step is the recovery count (`count - 1`), not the rep count: the recorder emits one recovery between consecutive reps and stamps the reduced total, so a reader must not subtract again. |
| `workout_adherence` | `string` — `completed` \| `partial` \| `abandoned` | Same as above | Same as above | Optional; always present alongside `plan_workout_id` | `completed` = every step hit target ± tolerance. `partial` = any step skipped or more than 20 % short. `abandoned` = user explicitly abandoned mid-workout. |
| `watch_workout` | `object` — `{ step_total: int, adherence: 'completed' \| 'partial', workout_crc: int, step_results: array<{ step_index: int, status: 'completed' \| 'skipped', actual_distance_m: double, duration_s: int, actual_pace_sec_per_km: int? }> }` | `apps/mobile_*/lib/watch_ingest_queue.dart` (`runFromWatchPayload`, forwarding the `workout` section `sim_watch_sync.dart`'s `payloadFromBlob` decodes from a custom-watch run blob's v4 step/summary records — `apps/custom_watch/core/src/run_store.rs`, decisions §356) | None yet — the raw watch-side planned-vs-actual trail, stored for the review that lands with the phone workout-push surface | Optional | The custom watch executes a pushed `WKT1` workout on the wrist and persists what each step banked. `workout_crc` is the CRC32 of the canonical `WKT1` frame for the armed step list — the attribution handle: the push transport exists (`WatchSyncClient.pushWorkout` + the dev Sim Watch push action), so once the product surface remembers the frame CRC it pushed it can match this against it (and only then stamp `plan_workout_id` / fold into `workout_step_results`, whose entries carry planned identity the watch records don't). The decoder drops the section whole when it is unattributable (no summary — e.g. a mid-run-checkpoint recovery) or self-contradictory (duplicate step indices — a workout re-armed mid-run), never the run. No `abandoned` value — the watch runner doesn't port abandon (decisions §354). Owner-only; stripped from `public_runs` by `20270430_001`. |

#### Guided runs

Written when a run was recorded under an audio-coached guided run armed on the Run tab. The cue script itself is client-side (`guided_runs.dart` / `guided_runs.ts`); only the id is stored.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `guided_run_id` | `string` — the guided-run library id (`easy-30`, `tempo-builder-25`, `first-timer-15`) | `apps/mobile_*/lib/screens/run_screen.dart` (`_stop`, and mirrored by `_saveInProgress` so a process-kill mid-script keeps the attribution) | `apps/web/src/routes/runs/[id]/+page.svelte` — the run-detail header chip, which resolves the id through `findGuidedRun` and links to `/guided/[id]`; it lives in the owner branch of the page, so a non-owner never reaches it. `apps/mobile_*/lib/screens/run_detail_screen.dart` (`_guidedRunId` → `_buildGuidedRun`) — the same readout on the phone, tapping through to the script, which since the same round can re-arm it. Both resolve against the ACTIVE locale's library, never a cached one, and **neither ever renders the raw slug**. An id the shipped library no longer answers to keeps the claim and drops the name: the run WAS guided, and self-hiding would present it as a silent one — web drops the link with it, since `/guided/<unknown>` is the library's own dead-end empty state | Optional | The **library id**, never an index: `guidedRunLibrary` is rebuilt per locale and a later build inserting or reordering a run would silently re-point every stored index. Absent when no guided run was armed — the arm is per-recording and is cleared on finish, so a run carries the key only if a script was actually playing. **Owner-only**; stripped from `public_runs` by `20270627000001`. It was first classified public-safe by analogy with `sub_sport`, on the reasoning that a catalogue id carries no geographic, identity or pace signal. That reading is backwards on both halves: `trail` / `treadmill` describe where the feet landed, where `first-timer-15` is a legible statement about the RUNNER that any anonymous reader of the share page understands without a lookup; and `plan_workout_id` is an opaque uuid disclosing nothing until it is resolved against a table the reader cannot select from, yet it is stripped — exposing the legible label while hiding the opaque one inverts the projection's own logic. Which workout a run executed is owner-only (the `plan_workout_id` / `workout_step_results` / `workout_adherence` / `watch_workout` group), and a guided run is a workout the run executed. |

### Internal / runtime-only

Keys that carry transient or platform-internal state. Treat these as implementation detail — do not expose them in the UI, and do not depend on them across platforms.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `last_modified_at` | `string` (ISO 8601) | `mobile_android/lib/local_run_store.dart` (on every `update()`); `watch_wear/.../WatchRunMetadata.kt` (on every direct upload); `watch_ios/WatchApp/ContentView.swift` (WCSession metadata, at sync) + `watch_ios/WatchApp/SupabaseService.swift` (DEBUG-direct upload); the Apple-Watch-via-WCSession ingest path forwards the watch's own stamp through `apps/mobile_ios/ios/Runner/WatchIngestBridge.swift` (forwarded-key list) → `WatchIngest._runFromArgs` (`main.dart`) / `runFromWatchPayload` (`watch_ingest_queue.dart`), which both copy it into `metadata` before `ApiClient.saveRun` | `mobile_android/lib/local_run_store.dart` (newer-wins conflict resolution during sync); `mobile_android/lib/screens/runs_screen.dart` (delta-fetch `updatedSince` filter, `metadata->>'last_modified_at' > since`) | **Required** on every cloud-bound row — without it, the row is invisible to mobile's delta refresh after the first ever fetch | Set by the local store on Android/iOS, the watch encoder on Wear OS direct uploads, and the Apple Watch encoders (2026-06-04). The Apple-Watch-via-WCSession ingest path uploads to the cloud directly (it does NOT go through `LocalRunStore.save()`), so the watch's own `last_modified_at` (watchOS R3 H1) is forwarded verbatim through the bridge + both watch-payload decoders — a WCSession run is delta-visible from the first push. The DEBUG-direct watch path writes it straight to the row, so it's effective there too. |
| `recovered_from_crash` | `bool` — always `true` when present | `mobile_android/lib/main.dart` (app launch, when it detects an in-progress crash-time save) | — | Optional, audit-only | Marks a run that was reconstructed from the incremental-save snapshot after a crash mid-recording. No UI consumer yet — would be useful for a "we saved what we had" toast. |
| `recovered_unfinished` | `bool` — always `true` when present | `apps/mobile_*/lib/watch_ingest_queue.dart` (`runFromWatchPayload`, when the watch payload carries `finished: false`); the signal originates in the custom watch's `run_store` header flag `FLAG_FINISHED` (`apps/custom_watch/core/src/run_store.rs`), decoded by `sim_watch_sync.dart`'s `RunHeader.finished` and forwarded as the payload's `finished` field by `payloadFromBlob` | `apps/web/src/routes/runs/[id]/+page.svelte` + `apps/mobile_*/lib/screens/run_detail_screen.dart` (the "Incomplete" header badge) | Optional; **absent** on every finished run — presence is the whole signal | The custom watch checkpoints a run in progress to flash. If it resets mid-run (battery pull, crash, hard reset) boot reconciliation recovers that checkpoint and serves it, because refusing to would destroy the only copy of the run (decisions §316(c)). Its footer totals are therefore its **totals-so-far**, not final totals: a reboot at mile 60 would otherwise present 60 miles as the runner's whole day. Only an explicit `finished: false` stamps the key — a sender that says nothing (the Apple-Watch WCSession bridge, Wear OS) only ever produces finished runs, so silence must not read as partial, and stamping `false` on every watch row would put a meaningless key on all of them. Public-safe (see the classification section) — the honesty has to survive the public projection or the share page repeats the lie. See [decisions.md § 323](../architecture/decisions.md). |
| `in_progress_saved_at` | `string` (ISO 8601) | `mobile_android/lib/screens/run_screen.dart` (periodic incremental save during recording) | `mobile_android/lib/in_progress_recovery.dart` (`evaluateInProgressPartial`) | Optional, audit-only | Timestamp of the last incremental save. Cleared when the run is finalised. Survival indicator for crash recovery. Also gates the **resumable** cold-start outcome: a partial is offered for resume only when this is within the 48 h `kResumableWindow`; older/absent → finalize (decisions §230). |
| `in_progress` | `bool` — `true` only on the live-broadcast stub | `packages/api_client/lib/src/api_client.dart` (`beginLiveBroadcast`) — written into the stub `runs` row created when the user taps "Share live link" (or auto-live-share starts one) so the FK + RLS on `live_run_pings` accept inserts before the real run is saved | `enqueue_safety_overdue_emails()` + the stub-transition guards in `enqueue_safety_finish_emails()` / `notify_run_completed()` (migration `20270401_001`) — the overdue scan's "still out there" predicate, and the reason a stub INSERT no longer fires finish/run_completed notifications | Optional, audit-only | The stub is overwritten on stop by `saveRun`'s upsert (which omits this key), so the value flips from `true` to absent for finished runs — that transition is what fires the finish notifications since `20270401_001`. |
| `safety_escalated_at` | `string` (ISO 8601) — server-written | `enqueue_safety_overdue_emails()` (migration `20270401_001`, pg_cron) AND `escalate_run_off_route()` (migration `20270414_001`, the off-route trigger) each stamp it in the same statement that enqueues the `safety_email` jobs | Both writers gate on `is null` (the shared once-only guard: a stamped run never re-fires, whichever trigger got there first) | Optional, server-only | The safety-escalation idempotency stamp — ONE alert per run across the overdue-silence scan AND the off-route trigger (docs/features/safety.md). Stripped from `public_runs`. Clients never write or read it; the eventual `saveRun` upsert overwrites it away, which is fine — a saved run is out of both predicates. |
| `expected_return_at` | `string` (ISO 8601) — the runner's per-run "not back by X" override | `set_run_expected_return()` RPC (migration `20270410_001`, owner-only, in-progress-stub-only); web `/live/[id]` owner control + the mobile run screen's live-share sheet ([decisions § 719](../architecture/decisions.md)), which READS the key back through `ApiClient.fetchRunExpectedReturn` before offering the picker — the alarm outlives the process, so memory is not evidence about it | `enqueue_safety_overdue_emails()` (migration `20270410_001`) — the per-run override branch fires an escalation once `now()` passes this time, independent of the universal `safety_overdue_minutes` window | Optional, owner-set | The trusted-contact escalation override (docs/features/safety.md). **Owner-private** — stripped from `public_runs` so it never surfaces on the anon live page. Merged/removed atomically by the definer RPC (a client can't clobber the rest of the bag); the eventual `saveRun` upsert drops it when the run finishes. |
| `manual_entry` | `bool` — always `true` when present | `mobile_android/lib/screens/add_run_screen.dart`; `apps/web/src/lib/core/data.ts` (`createManualRun`) | — | Optional, audit-only | Marks a run created via the "Add run" form rather than a live recording or an import. Present on runs with an empty `track` and a `routeId` that the user picked by hand. No UI consumer yet — useful when computing PBs, since a user-estimated time shouldn't outrank a GPS-recorded one. |
| `indoor_estimated` | `bool` — always `true` when present | `mobile_android/lib/screens/run_screen.dart` (`_stop`, `_saveInProgress`) when `_everHadGpsFix` stayed false and the pedometer produced distance | `mobile_android/lib/in_progress_recovery.dart` (`evaluateInProgressPartial` — a trackless partial is only resumable when this is set) | Optional, audit-only | Marks a treadmill / indoor run where `distanceMetres` came from `steps × stride` rather than GPS. Pairs with `distance_source = "pedometer"`, and since Aug 2026 is always written **alongside `indoor`** — this key is audit-only, `indoor` is what the VDOT filter reads. |
| `distance_source` | `string` — `"pedometer"` \| `"treadmill"` (extendable) | `mobile_android/lib/screens/run_screen.dart` when saving an indoor-estimated run; `packages/run_recorder/lib/src/run_recorder.dart` (`stop`) writes `"treadmill"` for a belt-sourced run | — | Optional, audit-only | Explicit tag for *where* the distance came from when it isn't GPS. Present together with `indoor_estimated` (pedometer) or `indoor` + `indoor_source` (treadmill). Leaving room for future sources (e.g. `"strava_import"`, `"user_entered"`) without adding new booleans. |
| `indoor_source` | `string` — `"treadmill"` (extendable) | `packages/run_recorder/lib/src/run_recorder.dart` (`stop`, when the recorder was in treadmill mode via the `setTreadmillSample` seam) | — | Optional, audit-only | Names the indoor distance/speed source when distance came from a paired device rather than a pedometer estimate. Written alongside `indoor: true` + `distance_source: "treadmill"` for a run recorded against a BLE FTMS treadmill (C3). Distinct from the pedometer path's `indoor_estimated`/`distance_source: "pedometer"`: a treadmill belt actually measures distance, but it's still not GPS-measured, so the `indoor` flag keeps it out of the VDOT ceiling the same way. Extendable to future indoor sources (e.g. `"bike_trainer"`). |
| `global_segments_scored_count` | `int` — number of active `global_segments` the run's catalogue efforts were last computed against | `apps/web/src/lib/core/data.ts` (`computeGlobalSegmentEffortsForRun`, via `stampGlobalSegmentsScored` in `data_normalise.ts`) | `apps/web/src/lib/core/data.ts` (`computeGlobalSegmentEffortsForRun`, via `shouldRescoreGlobalSegments`) | Optional; absent = never scored | Idempotency stamp that stops the run-detail global-segment backfill re-fetching the whole 500-row catalogue + re-running client-side haversine matching on every owner view (issue #333). Written after a successful scoring pass (**including when nothing matched**); the next view re-scores only when the active catalogue has *grown* past this count. Bounded by `GLOBAL_SEGMENT_SCORING_LIMIT` (`data_normalise.ts`, 500) — the same constant caps the catalogue fetch and clamps the gate's `count(*)` comparison, so the stamp saturating at the fetch cap can never make the gate permanently true. Value-only (no timestamp) so it carries no per-run private signal — the catalogue size is identical for every run and already public — hence **public-safe** (passes through `public_runs`; the projection is a denylist and this key is not on it). Web-only writer/reader today; the mobile run-detail has no catalogue backfill. |

### Personal-records hints

> The four embedded-best keys — `fastest_5k_s` / `fastest_10k_s` /
> `fastest_half_marathon_s` / `fastest_marathon_s` — were promoted to real
> `runs` columns by `20270325_001` (backfilled from the bag with the same
> non-negative-integer validation the trigger's regex applied, then
> stripped). See [Promoted to real columns](#promoted-to-real-columns-no-longer-metadata-keys)
> above for the column contracts and the per-writer migration state. Value
> semantics are unchanged: auto-computed at save/import time with the
> `fastestWindowOf` sliding window, absent (null) for trackless /
> timestamp-less / too-short runs (no fake bests), a faster manual bag value
> still wins over the auto-computed one on the Flutter carrier path.
>
> `is_dnf` was promoted to a real `runs.is_dnf` column by `20261207_001` —
> same section above.

### Client-side synthetic

**Not persisted.** These keys exist only in in-memory `Run.metadata` maps after deserialisation, added by the client to work around API ergonomics. Do not write them in a `saveRun` call — the DB round-trip will strip them from anywhere that matters, but they'll leak into re-uploads if you're not careful.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `track_url` | `string` — Storage path like `{user_id}/{run_id}.json.gz` | `packages/api_client/lib/src/api_client.dart` → `_runFromRow` (copies the `runs.track_url` column into `metadata`) | `packages/api_client/lib/src/api_client.dart` → `fetchTrack(run)`, `mobile_android/lib/screens/run_detail_screen.dart` | Optional; present whenever the run has an uploaded track | This is a **cross-wiring trick**. The real `runs.track_url` column is not exposed on the Dart domain `Run` class, so `_runFromRow` stuffs the value into `metadata['track_url']` so that downstream callers can pass a `Run` back to `fetchTrack` without also threading the URL separately. If you refactor `Run` to have a real `trackUrl` field, delete this key from the synth code AND from any reader — otherwise both paths will silently diverge. |
| `hr_series_url` | `string` — Storage path like `{user_id}/{run_id}.hr.json.gz` | `packages/api_client/lib/src/api_client.dart` → `_runFromRow` (copies the `runs.hr_series_url` column into `metadata`) | `packages/api_client/lib/src/api_client.dart` → `fetchHrSeries(run)`, `mobile_android/lib/screens/run_detail_screen.dart` | Optional; present only for indoor/trackless runs with an HR sidecar | Same cross-wiring trick as `track_url`, for the indoor/treadmill HR sidecar (decisions §116). The real `runs.hr_series_url` column isn't on the Dart `Run` class, so `_runFromRow` stuffs it into `metadata` so run-detail can pass the `Run` to `fetchHrSeries` for the HR-zone fallback. Web reads the real column directly. |

---

# `gym_workouts.metadata` key registry

`gym_workouts.metadata` is a second `jsonb` bag — the gym/session analogue of `runs.metadata`. It carries the planned-vs-actual linkage written when a `gym_workout` row was logged by following a routine (gym programming P2) or a session plan (session planner P2). Same schema-less-bag caveat applies: codegen can't catch a casing/typo drift here, so route every Dart read/write through the `MetadataKeys.*` constants and keep this section in lockstep.

**All seven keys are classified OWNER-ONLY — never public-safe.** `gym_step_results` / `session_step_results` carry the runner's planned-vs-actual detail (per-set weight/reps, per-item timings, what was skipped) — the gym/session equivalent of the run-side `workout_step_results` that the `public_runs` view already strips. There is no public projection of `gym_workouts` today; if one is ever added, these keys must be on its strip list. Weights inside `gym_step_results` are canonical kg (the pure logic never formats — the reader formats through the weight pref). Retention: lifetime of the `gym_workouts` row; cleared only when the workout row is deleted.

### Routine linkage (gym programming P2)

Written by `GymWorkoutRunner` (`packages/run_recorder/lib/src/gym_workout_runner.dart`) on finish, when the logged workout was started from a saved routine. Planned-vs-actual is computed by the `gym_adherence` parity pair (web `apps/web/src/lib/gym/gym_adherence.ts` ↔ mobile `apps/mobile_android/lib/gym_adherence.dart`); the routine itself is shaped by the `gym_routine` pair.

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `routine_id` | `string` (uuid) — the linked `gym_routines` row | `GymWorkoutRunner` (finish, when a routine was active) | gym-workout detail (web + mobile, planned-vs-actual section) | Optional | Presence implies an explicit routine link. Owner-only. |
| `gym_step_results` | `array<{ exercise_key: string, step_index: int, set_index: int, status: 'hit' \| 'partial' \| 'missed' \| 'extra', reps_delta: int?, weight_delta_kg: double?, target_reps_min: int?, target_reps_max: int?, target_weight_kg: double?, target_duration_s: int?, actual_reps: int?, actual_weight_kg: double?, actual_duration_s: int?, target_distance_m: double?, actual_distance_m: double? }>` | Same | Same (planned-vs-actual table) | Optional; present alongside `routine_id` | One row per planned set, matched to actuals by `(exercise_key, step_index)` — never by name spelling. `step_index` is the ordinal position in the EXPANDED step list; `set_index` is the position within the exercise block and restarts per block, so it is display-only and NOT unique across a routine (a routine programming the same exercise in two blocks mints the same `set_index` twice). `status = 'extra'` marks a logged set with no planned counterpart. Weights are canonical kg. Owner-only — mirrors the run-side `workout_step_results`. `step_index` is ABSENT on rows persisted between the P3 runner shipping (2026-06-12) and decisions §304 (2026-07-25), which used `set_index` as the match identity; a reader must replay those under `set_index` rather than hide them. |
| `gym_adherence` | `string` — `completed` \| `partial` \| `abandoned` | Same | Same | Optional; present alongside `routine_id` | `completed` at ≥ 80 % of planned sets hit, `abandoned` at zero, `partial` between — mirrors the run-side `workout_adherence` threshold. Owner-only. |
| `gym_session_draft` | `{ saved_at: string (ISO 8601), results: array<{ step_index: int, status: 'completed' \| 'skipped', reps: int?, weight_kg: double?, rpe: double?, duration_s: int?, distance_m: double? }> }` | mobile `gym_session_screen.dart` → `_durableSave` (the 10 s crash-safe tick and the guarded leave-with-draft path); web `gym_session_draft.ts` → the same durable tick on `/gym/session/[routineId]` | mobile `gym_screen.dart` (resume card) + `gym_session_screen.dart` (runner restore replay); web `/gym` (resume card) + `/gym/session/[routineId]` (runner restore replay) | Optional; present only while a guided session is in flight | The in-flight runner snapshot behind the gym-session resume path — the gym analogue of the run-side `in_progress` marker, except it rides the (already-syncing) draft `gym_workouts` row instead of a separate file. `results` is the runner's ordered per-step outcome list (weights canonical kg); the current step is derived by replaying it, never stored. Cleared on finish (`_metadataTrio` replaces the whole bag) and on discard (row deleted); "save as is" from the resume card strips it while keeping `routine_id`. **Web writes and reads the identical shape since [decisions §493](../architecture/decisions.md)** — a session started on either platform resumes on the other, which is why the field shape is a cross-platform contract and not a mobile implementation detail. Every reader tests that the value is a JSON **object** — Dart's `is Map`, web's `hasSessionDraft`, the `gym_routine_history` RPC's `jsonb_typeof(...) = 'object'` — so an array or a scalar under the key is not a draft on any rail ([decisions §662](../architecture/decisions.md)). The watches ignore it. Owner-only. |

### Session plan linkage (session planner P2)

Written by the session follow-along runner on finish, when the logged `gym_workout` was driven by a `session_plans` row (yoga/pilates — cross-modal: a session is logged as a `gym_workout`). The timed step sequence is flattened by the `session_steps` pair (`expandSessionSteps`, web `apps/web/src/lib/social/session_steps.ts` ↔ mobile `apps/mobile_android/lib/session_steps.dart`).

| Key | Shape | Writers | Readers | Required? | Notes |
|---|---|---|---|---|---|
| `session_plan_id` | `string` (uuid) — the linked `session_plans` row | session follow-along runner (finish, when a session was active) | session-workout detail (web + mobile) | Optional | Presence implies an explicit session-plan link. Owner-only. |
| `session_step_results` | `array<{ item_id: string, movement_name: string, kind: string, side?: 'left' \| 'right', target_duration_s: int?, actual_duration_s: int?, status: 'completed' \| 'skipped' }>` | Same | Same (planned-vs-actual / completion view) | Optional; present alongside `session_plan_id` | One row per expanded step (per-side items split into L/R, matching `expandSessionSteps`). Both clients write snake_case (`item_id` / `movement_name`): the web writer in `sessions/[id]/+page.svelte` and the Dart `_stepResultJson` in `session_detail_screen.dart` must match. Owner-only — carries the runner's per-item completion detail. |
| `session_adherence` | `string` — `completed` \| `partial` \| `abandoned` | Same | Same | Optional; present alongside `session_plan_id` | Same threshold semantics as `gym_adherence` / `workout_adherence`. Owner-only. |

---

## Conventions

When adding a new metadata key:

1. **Use `snake_case`.** Every existing key does. Don't mix camelCase in.
2. **Prefer a real column.** `metadata` is for data we're not ready to schema-ise yet, or that's genuinely free-form (like `notes`). If a new field is shaped and queried, it belongs in its own column with a migration.
3. **Name it for what it is, not what writes it.** `activity_type` not `recorded_activity_type`. The writer is obvious from the data flow.
4. **Be explicit about absence.** "Optional" is the default. If a reader can't tolerate the key being missing, call that out in the notes column and ask whether it should be a real NOT NULL column instead.
5. **Update this file and remove the key here when you remove it from code.** The schema generators can't do this for you.

## Public-safe vs owner-only classification

When a run's `is_public = true`, the row's `metadata` jsonb travels alongside it. The `public_runs` view (migration `20260626_001_public_runs_view.sql`) projects a *redacted* version that drops the owner-only keys before they cross the wire. The classification:

**Public-safe** (kept by the view's projection):

- `activity_type`, `steps`, `laps`, `cadence_spm` — core run properties (cadence is derived client-side from `steps / moving_time_minutes` for pedometer runs, or read from the stored `cadence_spm` for FIT imports — see the row above)
- `title`, `notes` — user-editable display fields
- `event`, `position`, `age_grade` — parkrun fields
- `avg_bpm` — scalar HR
- `hr_coverage` — how much of the run that scalar covered; a duty fraction, no geographic or identity signal
- `elevation_m` — total elevation gain
- `sub_sport` — FIT discipline (trail / treadmill / track / road); reveals nothing the rendered track + distance don't already imply, so it passes through the view rather than being stripped (no migration needed — the projection is a denylist). Persona round-5 theme F.
- `running_dynamics` — Garmin biomechanic averages (vertical oscillation / GCT / stride length / power / L-R balance); no geographic or identity signal, so kept public-safe. Persona round-5 theme F.
- `global_segments_scored_count` — the run-detail catalogue-backfill idempotency stamp (issue #333). Just the active-catalogue size the run was last scored against — identical for every run and already public, no geographic or identity signal — so classified public-safe and left off the strip denylist (no `public_runs` migration needed).
- `recovered_unfinished` — the custom-watch boot-recovery partial-totals flag. Deliberately public-safe rather than an oversight: it carries no geographic or identity signal (just "this watch reset mid-run"), and it is the one key whose whole purpose is to stop a set of totals being read as complete — stripping it from the view would make the public share page repeat exactly the lie the key exists to prevent. Left off the strip denylist, so no `public_runs` migration is needed.

**Owner-only** (stripped by the view's projection — denylist in the migration body):

- `imported_from`, `imported_at`, `health_connect_type`, `strava_activity_type`, `strava_id`, `garmin_id`, `source_file`, `max_bpm` — import provenance / third-party-id-cross-walks
- `plan_workout_id`, `workout_step_results`, `workout_adherence` — training-plan linkage; leaks the runner's structured-workout paces and adherence
- `watch_workout` — the custom watch's planned-vs-actual trail; leaks the same structured-workout paces and adherence as the row above (added to the strip list in `20270430_001`)
- `guided_run_id` — which of the shipped guided-run scripts a run was recorded under. A legible statement about the runner (`first-timer-15`), not about the run's surface, and which workout a run executed belongs with the group above rather than beside `sub_sport` (added to the strip list in `20270627000001`)
- `last_modified_at` — sync-state internal; leaks device-upload cadence
- `recovered_from_crash`, `in_progress_saved_at`, `in_progress`, `manual_entry`, `indoor_estimated`, `distance_source`, `indoor_source` — recorder internals. The four indoor keys divide on **fact versus provenance**: THAT a run was indoors is a property of the run and stays public (`indoor`, the `sub_sport` class), while WHICH SENSOR produced the distance is an internal and does not. `indoor_source` was public only by omission until `20270628000001` — `distance_source: "treadmill"` and `indoor_source: "treadmill"` carry the same fact in the same words, are written on the same run by the same recorder path, and had landed on opposite sides of the projection
- `safety_escalated_at`, `expected_return_at` — overdue-escalation internals (the once-only stamp + the per-run "not back by X" override); `expected_return_at` is the runner's private plan and must never surface on the anon live page (added to the strip list in `20270401_001` and `20270410_001` respectively)
- `race_name`, `bib`, `overall_place`, `chip_time` — race-source identity link (added to the strip list in `20260714_001`)
- `gun_time`, `age_group_place`, `age_group` — race-source age-band / gun-time fields written by `race-results-import` (added to the strip list in `20270214_001`)
- `perceived_effort` — RPE on the Borg scale (seed-only today; classified owner-only when a real writer lands)

When you add a new key, classify it explicitly — and update the strip list in the most recent `public_runs` projection migration (current head: `20270628000001_public_runs_strip_indoor_source.sql` — the view has been redefined several times since `20260714_001`: `20260724_001`, `20260807_001`, `20260924_001`, `20261105_001`, `20261207_001`, `20270214_001` (added the `race_listing_id` pass-through column + the three race age-group / gun-time keys to the denylist), `20270302_001` (added the `elevation_gain_m` column pass-through), `20270325_001` (the four `fastest_*` embedded-best column pass-throughs), `20270401_001` (added `safety_escalated_at` to the denylist), `20270410_001` (added `expected_return_at`), `20270427_001` (the `concluded_at` pass-through), `20270430_001` (added `watch_workout`), `20270627000001` (added `guided_run_id`), then `20270628000001`, which added `indoor_source`) if it lands on the owner-only side. The seed assertions in `apps/backend/supabase/seed.sql` for `public_runs` exercise the projection — but, like the pgtap file, they assert the keys they NAME rather than the keys the view strips, so a key nobody adds there is not caught by them. The seed stood **seven** keys behind the view before it was held against it (`age_group`, `age_group_place`, `gun_time`, `expected_return_at`, `safety_escalated_at`, plus the two added here). The denylist is also asserted key-for-key by `apps/backend/supabase/tests/rls_public_runs_view_denylist_test.sql`, and both that array and the seed's assertion are held against the live view's own projection — three homes, one value — by the `public_runs metadata denylist` entry in `scripts/check_shared_constants.mjs` — it stood four keys behind the view before the guard existed, which is why the doc instruction alone is not the mechanism.

## Enforcement

**Dart side (mobile + packages):** `apps/mobile_android/test/metadata_registry_test.dart` greps every `.dart` file under `apps/mobile_android/lib/`, `packages/api_client/lib/`, and `packages/run_recorder/lib/` for subscript access (`metadata['xxx']`) and map-literal writes (`metadata: { 'xxx': ... }`), and asserts every key is a row in this file. Runs in the `test-packages` CI job — a PR that adds an unregistered key fails there.

**Web, watch_wear, watch_ios:** all three now carry the equivalent guard — `apps/web/src/lib/metadata_registry_guard.test.ts`, `apps/watch_wear/android/app/src/test/kotlin/com/runapp/watchwear/MetadataRegistryTest.kt`, and `apps/watch_ios/WatchAppTests/MetadataRegistryTests.swift`, alongside the Dart pair and `packages/core_models/test/metadata_keys_test.dart`. Four rendering platforms, four guards; the only residual is a macOS CI run to compile the Swift one.

## Known issues

- ~~**Web has no Dart-style runtime guard.**~~ **Closed — and this entry outlived its truth.** Web is a significant metadata writer (Strava ZIP import sets `strava_id`/`avg_bpm`/`strava_activity_type` in `apps/web/src/lib/integrations/strava-zip.ts`; Garmin ZIP import sets `garmin_id`/`avg_bpm`/`max_bpm` in `garmin-zip.ts`; `saveRun` in `data.ts` merges `title`/`notes`/`manual_entry`/`elevation_m`; the run detail page edits in place), and every one of those writes is now scanned by `apps/web/src/lib/metadata_registry_guard.test.ts`, which runs under the `Web TS unit tests` CI step (`npx tsx --test 'src/**/*.test.ts'`). It walks code→registry; `core/schema.test.ts` walks the other direction (every `METADATA_KEYS` entry needs a doc row). So the "keep web writes in sync by PR review" instruction this entry used to give is wrong — CI does it. The `metadata-key-keeper` agent (`.claude/agents/metadata-key-keeper.md`) is now a diff-time convenience, not the only backstop. Note the Enforcement section above already recorded all four guards: this bullet contradicted it for as long as both stood.
- **Apple Watch has its own Supabase client** (`apps/watch_ios/WatchApp/SupabaseService.swift`) that does not share this registry. Any metadata keys written from the watch have to be manually reconciled with this file. See [../apps/watch_ios/CLAUDE.md](../../apps/watch_ios/CLAUDE.md).
- **Apple-Watch WCSession ingest forwards a hardcoded key list — anything else is dropped.** The phone-side bridge that receives a watch run (`apps/mobile_ios/ios/Runner/WatchIngestBridge.swift`) forwards `id`/`started_at`/`source`/`activity_type`/`last_modified_at` + `duration_s`/`distance_m`/`avg_bpm`/`hr_coverage`/`steps`/`laps`, and `runFromWatchPayload` (`watch_ingest_queue.dart`) rebuilds `Run.metadata` from those metadata keys — there is ONE decoder, `WatchIngest` in `apps/mobile_android/lib/main.dart` having been collapsed onto it. `last_modified_at` IS forwarded end-to-end (2026-06-04 — see the registry row above), so a WCSession run is delta-visible from the first push. Both `steps` and `laps` were lost on this path until 2026-09-18, when the Apple Watch learned to record them: the bridge lifts both, and `runFromWatchPayload` forwards `steps` (it already forwarded `laps`). Forward + apply any new key in BOTH the bridge and the decoders or it silently vanishes. The DEBUG-direct upload path (`SupabaseService.swift`) bypasses the bridge and is unaffected.
- **No runtime validation.** Nothing checks that an incoming `metadata` blob matches this registry — a row written by a client the guards don't scan, or by a hand-run SQL statement, lands unchallenged. At *write* time the four CI guards above are static analogues that catch anything dropping into a grep; at *read* time the check is still only this doc plus whatever type assertions the reader writes at the call site.
- **`steps` wire type is unverified.** The Android code writes it as an `int` from the pedometer; the web reader indexes it as-is. `Json` on both clients will accept either a number or a string, so if a writer ever coerces it, both platforms will silently drift. Worth a future audit — or a cast at the write site.
